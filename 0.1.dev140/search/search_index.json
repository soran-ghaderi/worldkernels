{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"WorldKernels","text":""},{"location":"#worldkernels","title":"WorldKernels","text":"<p>Serving world models as stateful simulation sessions.</p>"},{"location":"benchmarks/","title":"Overview","text":""},{"location":"benchmarks/#benchmarks-metrics","title":"Benchmarks &amp; Metrics","text":"<p>Auto-generated from <code>benchmarks/</code> scripts and <code>worldkernels/runtime/metrics.py</code>.</p>"},{"location":"benchmarks/#prometheus-metrics","title":"Prometheus Metrics","text":"<p>The following metrics are exported on the <code>/metrics</code> endpoint.</p> Metric Name Type Description <code>wk_frames_generated_total</code> Counter  Total frames generated across all sessions. <code>wk_steps_total</code> Counter  Total simulation steps executed. <code>wk_worker_respawns_total</code> Counter  Times an isolated worker was respawned after crashing. <code>wk_active_sessions</code> Gauge  Number of live sessions. <code>wk_cache_hit_ratio</code> Gauge  Denoise-step cache hit ratio in [0, 1]. <code>wk_model_isolation_tier</code> Gauge  Per-model isolation tier (0 = shared env, 1 = isolated subprocess). <code>wk_vram_usage_bytes</code> Gauge  Device memory currently allocated. <code>wk_worker_processes</code> Gauge  Number of isolated worker subprocesses currently live. <code>wk_batch_size</code> Histogram  Number of sessions per batched forward pass. <code>wk_step_latency_seconds</code> Histogram  Wall-clock latency of one simulation step. <code>wk_worker_ipc_latency_seconds</code> Histogram  Engine \u2194 worker RPC round-trip latency."},{"location":"benchmarks/metrics/","title":"Metrics Reference","text":""},{"location":"benchmarks/metrics/#metrics-reference","title":"Metrics Reference","text":"<p>Prometheus-style metrics exported by WorldKernels on the <code>/metrics</code> endpoint.</p> <p>Auto-generated from source files. Do not edit manually.</p> Metric Name Type Description <code>wk_frames_generated_total</code> Counter  Total frames generated across all sessions. <code>wk_steps_total</code> Counter  Total simulation steps executed. <code>wk_worker_respawns_total</code> Counter  Times an isolated worker was respawned after crashing. <code>wk_active_sessions</code> Gauge  Number of live sessions. <code>wk_cache_hit_ratio</code> Gauge  Denoise-step cache hit ratio in [0, 1]. <code>wk_model_isolation_tier</code> Gauge  Per-model isolation tier (0 = shared env, 1 = isolated subprocess). <code>wk_vram_usage_bytes</code> Gauge  Device memory currently allocated. <code>wk_worker_processes</code> Gauge  Number of isolated worker subprocesses currently live. <code>wk_batch_size</code> Histogram  Number of sessions per batched forward pass. <code>wk_step_latency_seconds</code> Histogram  Wall-clock latency of one simulation step. <code>wk_worker_ipc_latency_seconds</code> Histogram  Engine \u2194 worker RPC round-trip latency."},{"location":"blog/","title":"Blog","text":""},{"location":"blog/#blog","title":"Blog","text":""},{"location":"developer_guide/","title":"Developer Guide","text":""},{"location":"developer_guide/#worldkernels-developer-guide","title":"WorldKernels Developer Guide","text":"<p>Welcome to the developer guide for WorldKernels. This is the central entry point for contributing code, understanding system design, and following the performance-first engineering standards used across the runtime.</p> <p>WorldKernels is a GPU-first world model simulation engine built around stateful sessions and staged world execution. The docs in this section focus on practical implementation details, not generic Python conventions.</p>"},{"location":"developer_guide/#a-path-to-contribution","title":"A Path to Contribution","text":"<p>These guides are organized so you can move from setup to architecture to implementation details quickly.</p> <ul> <li> <p> Getting Started</p> <p>Set up your environment, run tests/docs locally, and follow the contribution workflow used in this repository.</p> </li> <li> <p> Code Guidelines</p> <p>Learn the project rules for simplicity, zero redundancy, API consistency, and test expectations.</p> </li> <li> <p> Architecture</p> <p>Understand sessions, staged world execution, the runtime stack, and how core modules cooperate.</p> </li> <li> <p> Performance</p> <p>Follow GPU-first optimization practices for memory, scheduling, backends, and profiling.</p> </li> </ul> <p>If you are new to the codebase, start with Getting Started, then read Architecture before touching runtime hot paths.</p>"},{"location":"developer_guide/architecture/","title":"Architecture","text":""},{"location":"developer_guide/architecture/#worldkernels-architecture","title":"WorldKernels Architecture","text":"<p>WorldKernels is a GPU-first simulation engine for serving learned world models as stateful interactive sessions. Conceptually, it brings paged caching and continuous-batching scheduling, the techniques that make large-scale text inference efficient, to world simulation.</p>"},{"location":"developer_guide/architecture/#design-principles","title":"Design Principles","text":"<ul> <li> <p> Stage-Decomposed Execution</p> <p>World models are executed as three stages: action encoding, state transition, and observation decoding.</p> </li> <li> <p> Stateful Session Runtime</p> <p>Sessions own latent state, support checkpoint/branch/restore, and are scheduled as GPU resources.</p> </li> <li> <p> Performance-First</p> <p>Runtime design prioritizes buffer reuse, minimal transfers, and optimized backend selection.</p> </li> <li> <p> Extensible Adapters</p> <p>New world models and backends are integrated via adapters and plugin-style registration.</p> </li> </ul>"},{"location":"developer_guide/architecture/#runtime-topology","title":"Runtime Topology","text":"<p>At a high level, the system is split into controller, runtime, and serving layers:</p> <ul> <li><code>core/</code>: user-facing engine, sessions, action/observation/config primitives, and errors</li> <li><code>worlds/</code>: abstract world interface, adapters, and world registry</li> <li><code>runtime/</code>: executor hot path, memory/cache manager, scheduler, metrics, and backend dispatch</li> <li><code>serving/</code>: HTTP and WebSocket API surface</li> <li><code>cli/</code>: command-line entry points</li> </ul>"},{"location":"developer_guide/architecture/#core-modules","title":"Core Modules","text":""},{"location":"developer_guide/architecture/#engine-and-session","title":"Engine and Session","text":"<p><code>WorldKernel</code> orchestrates model lifecycle and session creation. A <code>Session</code> manages:</p> <ul> <li><code>session_id</code></li> <li>latent state ownership</li> <li>monotonic step index</li> <li>lifecycle transitions (active, paused, terminated)</li> </ul> <p>Session operations include <code>step</code>, <code>checkpoint</code>, <code>branch</code>, <code>restore</code>, and <code>close</code>.</p>"},{"location":"developer_guide/architecture/#world-interface","title":"World Interface","text":"<p>All adapters implement the staged interface in <code>worlds/base.py</code>:</p> <ol> <li><code>encode_action(action)</code></li> <li><code>transition(state, action_encoded)</code></li> <li><code>decode_observation(state, modalities)</code></li> </ol> <p>This keeps execution policy in runtime while model-specific logic stays in adapters.</p>"},{"location":"developer_guide/architecture/#runtime-components","title":"Runtime Components","text":"<ul> <li><code>runtime/executor.py</code>: executes staged world calls on selected backend</li> <li><code>runtime/scheduler.py</code>: admission control and session scheduling</li> <li><code>runtime/memory.py</code>: VRAM accounting and latent cache lifecycle</li> <li><code>runtime/metrics.py</code>: metric emission for observability</li> <li><code>runtime/backends/</code>: eager and compiled backends</li> </ul>"},{"location":"developer_guide/architecture/#data-flow","title":"Data Flow","text":"<pre><code>graph LR\n    A[Action] --&gt; B[Encode Action]\n    B --&gt; C[Transition State]\n    C --&gt; D[Decode Observation]\n    D --&gt; E[Observation]\n</code></pre> <p>For each session step, the runtime keeps state on-device when possible, performs transition, and decodes only requested modalities.</p>"},{"location":"developer_guide/architecture/#serving-surface","title":"Serving Surface","text":"<p>The serving layer exposes session-centric APIs in <code>serving/routes.py</code> and streaming support in <code>serving/websocket.py</code>. Expected endpoints include:</p> <ul> <li>world load/list/unload</li> <li>session create/list/get/delete</li> <li>session step/checkpoint/restore/branch</li> <li>metrics and health</li> </ul>"},{"location":"developer_guide/architecture/#extension-points","title":"Extension Points","text":"<p>Common extension paths:</p> <ul> <li>Add a new world adapter in <code>worlds/adapters/</code></li> <li>Register adapters via <code>worlds/registry.py</code> and package entry points</li> <li>Add backend behavior in <code>runtime/backends/</code></li> <li>Add telemetry in <code>runtime/metrics.py</code></li> </ul> <p>When extending architecture, keep data paths explicit and avoid introducing parallel implementations of existing operations.</p>"},{"location":"developer_guide/code_guidelines/","title":"Code Guidelines","text":""},{"location":"developer_guide/code_guidelines/#code-guidelines","title":"Code Guidelines","text":"<p>These guidelines define how we write and review code in WorldKernels. The goal is consistent APIs, minimal complexity, and high runtime performance.</p>"},{"location":"developer_guide/code_guidelines/#core-rules","title":"Core Rules","text":"<ul> <li>Prefer the simplest implementation that fully solves the task</li> <li>Reuse existing modules before adding new code paths</li> <li>Keep changes focused and avoid unrelated refactors</li> <li>Preserve API consistency (names, argument order, return contracts)</li> <li>Validate at boundaries, not inside hot loops</li> </ul>"},{"location":"developer_guide/code_guidelines/#style-and-tooling","title":"Style and Tooling","text":"<p>WorldKernels uses <code>ruff</code>, <code>mypy</code>, and <code>pytest</code> in normal development flow.</p> <pre><code>ruff check worldkernels tests\nruff format worldkernels tests\nmypy worldkernels\npytest tests/ -x --tb=short\n</code></pre> <p>General naming conventions:</p> <ul> <li>Classes: <code>CamelCase</code></li> <li>Functions and variables: <code>snake_case</code></li> <li>Constants: <code>UPPER_CASE</code></li> </ul>"},{"location":"developer_guide/code_guidelines/#api-and-architecture-conventions","title":"API and Architecture Conventions","text":"<ul> <li>Public APIs should include complete type hints</li> <li>Keep core orchestration in <code>core/</code>, implementation details in feature modules</li> <li>Use composition for functionality assembly; avoid deep inheritance trees</li> <li>Maintain single implementation per operation (no duplicate kernels/backends)</li> </ul> <p>For world models, follow staged semantics exactly:</p> <ul> <li><code>encode_action</code> handles typed action-to-tensor translation</li> <li><code>transition</code> is the primary state update compute path</li> <li><code>decode_observation</code> is modality-aware and can be skipped when not needed</li> </ul>"},{"location":"developer_guide/code_guidelines/#performance-sensitive-coding","title":"Performance-Sensitive Coding","text":"<p>Runtime hot paths (<code>runtime/executor.py</code>, <code>runtime/memory.py</code>, scheduler loops) require extra discipline:</p> <ul> <li>Avoid per-step tensor allocation when a reusable buffer is possible</li> <li>Avoid unnecessary host-device transfers</li> <li>Prefer vectorized tensor ops over Python loops</li> <li>Keep shapes stable where compiled execution benefits</li> <li>Avoid broad exception handlers in runtime internals</li> </ul>"},{"location":"developer_guide/code_guidelines/#docstrings-and-documentation","title":"Docstrings and Documentation","text":"<ul> <li>Use concise Google-style docstrings for non-trivial public APIs</li> <li>Keep trivial methods undocumented when signatures are self-explanatory</li> <li>Avoid explanatory inline comments unless they clarify non-obvious math or invariants</li> <li>Do not add new docs pages unless explicitly in scope</li> </ul>"},{"location":"developer_guide/code_guidelines/#testing-guidelines","title":"Testing Guidelines","text":"<ul> <li>Place tests under <code>tests/</code> using <code>test_*.py</code> naming</li> <li>Add unit tests for logic changes with deterministic assertions</li> <li>Add integration coverage when component interaction changes</li> <li>Mark GPU-requiring tests with <code>@pytest.mark.gpu</code></li> <li>Do not modify unrelated failing tests</li> </ul>"},{"location":"developer_guide/code_guidelines/#pull-request-expectations","title":"Pull Request Expectations","text":"<p>Before opening a PR:</p> <ol> <li>Ensure your branch contains one coherent change</li> <li>Run relevant tests and checks locally</li> <li>Include a clear summary and validation notes</li> <li>Call out performance implications when touching runtime paths</li> </ol> <p>Use Conventional Commits, for example:</p> <pre><code>git commit -m \"perf(runtime): reduce per-step buffer churn\"\n</code></pre>"},{"location":"developer_guide/getting_started/","title":"Getting Started","text":""},{"location":"developer_guide/getting_started/#getting-started-with-worldkernels-development","title":"Getting Started with WorldKernels Development","text":"<p>Welcome to WorldKernels development. This guide covers local setup, the standard contribution loop, and the minimum validation expected before opening a pull request.</p>"},{"location":"developer_guide/getting_started/#ways-to-contribute","title":"Ways to Contribute","text":"<p>We welcome contributions across runtime, serving, adapters, and documentation.</p> <ul> <li> <p> Report Bugs</p> <p>Report runtime, API, or documentation issues on our issue tracker with reproducible steps.</p> </li> <li> <p> Suggest Features</p> <p>Propose world adapters, scheduler policy changes, or serving/API improvements in discussions.</p> </li> <li> <p> Improve Documentation</p> <p>Improve architecture docs, runtime notes, and examples with clearer and more accurate implementation details.</p> </li> <li> <p> Write Code</p> <p>Contribute focused code changes that preserve API consistency and improve correctness or performance.</p> </li> </ul>"},{"location":"developer_guide/getting_started/#development-setup","title":"Development Setup","text":""},{"location":"developer_guide/getting_started/#1-prerequisites","title":"1. Prerequisites","text":"<p>Make sure you have the following installed:</p> <ul> <li>Python 3.10+</li> <li>Git</li> <li>A GitHub Account</li> </ul>"},{"location":"developer_guide/getting_started/#2-fork-and-clone","title":"2. Fork and Clone","text":"<p>First, fork the WorldKernels repository on GitHub. Then clone your fork locally:</p> <pre><code>git clone https://github.com/YOUR-USERNAME/worldkernels.git\ncd worldkernels\n</code></pre>"},{"location":"developer_guide/getting_started/#3-set-up-virtual-environment","title":"3. Set Up Virtual Environment","text":"<p>Use a virtual environment:</p> <pre><code>python -m venv .venv\nsource .venv/bin/activate\n</code></pre>"},{"location":"developer_guide/getting_started/#4-install-dependencies","title":"4. Install Dependencies","text":"<p>Install WorldKernels in editable mode with developer dependencies:</p> <pre><code>pip install -e \".[dev]\"\n</code></pre> <p>If you are editing docs, also install docs extras:</p> <pre><code>pip install -e \".[docs]\"\n</code></pre>"},{"location":"developer_guide/getting_started/#contribution-workflow","title":"Contribution Workflow","text":""},{"location":"developer_guide/getting_started/#1-create-a-branch","title":"1. Create a Branch","text":"<p>Create a focused branch for one change.</p> <pre><code>git checkout -b fix/your-change\n</code></pre>"},{"location":"developer_guide/getting_started/#2-make-changes","title":"2. Make Changes","text":"<p>Make minimal, targeted changes. Follow Code Guidelines and keep modifications scoped to the problem.</p>"},{"location":"developer_guide/getting_started/#3-run-validation","title":"3. Run Validation","text":"<p>Before committing, run tests:</p> <pre><code>pytest tests/ -x --tb=short\n</code></pre> <p>Run linting/type checks when relevant to your changes:</p> <pre><code>ruff check worldkernels tests\nruff format worldkernels tests\nmypy worldkernels\n</code></pre>"},{"location":"developer_guide/getting_started/#4-commit-your-changes","title":"4. Commit Your Changes","text":"<p>We follow Conventional Commits (<code>feat:</code>, <code>fix:</code>, <code>docs:</code>, <code>refactor:</code>, <code>perf:</code>, <code>test:</code>, <code>chore:</code>).</p> <pre><code>&lt;type&gt;: &lt;description&gt;\n</code></pre> <p>Example:</p> <pre><code>git commit -m \"fix(runtime): correct latent cache eviction\"\n</code></pre>"},{"location":"developer_guide/getting_started/#5-create-a-pull-request","title":"5. Create a Pull Request","text":"<p>Push your branch and open a pull request against <code>main</code>.</p> <pre><code>git push origin fix/your-change\n</code></pre> <p>In the PR description, include:</p> <ul> <li>What changed and why</li> <li>Runtime or API impact</li> <li>Validation performed (<code>pytest</code>, lint, docs build)</li> <li>Linked issues if applicable</li> </ul>"},{"location":"developer_guide/getting_started/#documentation-development","title":"Documentation Development","text":"<p>Serve docs locally:</p> <pre><code>mkdocs serve\n</code></pre> <p>This starts a live-reloading site at <code>http://127.0.0.1:8000</code>.</p> <p>For strict docs validation before a docs-focused PR:</p> <pre><code>mkdocs build --strict\n</code></pre>"},{"location":"developer_guide/performance/","title":"Performance Optimization","text":""},{"location":"developer_guide/performance/#performance-optimization","title":"Performance Optimization","text":"<p>WorldKernels is designed for GPU-first execution. This guide summarizes the optimization rules that matter most in practice when modifying runtime, adapters, or serving paths.</p>"},{"location":"developer_guide/performance/#primary-performance-targets","title":"Primary Performance Targets","text":"<p>Focus on these metrics when evaluating a change:</p> <ul> <li>step latency per session</li> <li>aggregate throughput under concurrent sessions</li> <li>VRAM usage stability over long runs</li> <li>cache hit behavior and scheduler efficiency</li> </ul>"},{"location":"developer_guide/performance/#hot-path-priorities","title":"Hot Path Priorities","text":"<p>Performance-critical modules include:</p> <ul> <li><code>runtime/executor.py</code></li> <li><code>runtime/memory.py</code></li> <li><code>runtime/scheduler.py</code></li> <li><code>worlds/adapters/*</code> transition and decode calls</li> </ul> <p>Prioritize these constraints:</p> <ol> <li>No redundant allocations in per-step paths</li> <li>No unnecessary copies between CPU and GPU</li> <li>No repeated compute when state/cache reuse is possible</li> </ol>"},{"location":"developer_guide/performance/#memory-and-cache-practices","title":"Memory and Cache Practices","text":"<ul> <li>Keep session state resident on GPU while active</li> <li>Reuse allocated buffers where shape constraints permit</li> <li>Treat latent and cache data as managed resources, not disposable tensors</li> <li>Apply explicit offload/eviction policies under VRAM pressure</li> </ul> <p>A good default pattern is pre-allocation and reuse rather than creating tensors every step.</p>"},{"location":"developer_guide/performance/#compute-and-backend-strategy","title":"Compute and Backend Strategy","text":"<ul> <li>Prefer vectorized tensor operations over Python loops</li> <li>Use backend dispatch (<code>eager</code>, compiled backends) through runtime abstractions</li> <li>Warm up models/backends before measurement to remove one-time startup effects</li> <li>Keep dtype policy aligned with device capability (<code>bf16</code> on Ampere+, <code>fp16</code> fallback)</li> </ul>"},{"location":"developer_guide/performance/#session-scheduling-and-batching","title":"Session Scheduling and Batching","text":"<p>Scheduler behavior has first-order impact on throughput:</p> <ul> <li>Batch only compatible sessions when doing so reduces amortized cost</li> <li>Preserve session-device affinity when state is already resident</li> <li>Use admission control to avoid over-committing VRAM</li> <li>Avoid policy changes that increase preemption churn without measured gain</li> </ul>"},{"location":"developer_guide/performance/#serving-layer-performance","title":"Serving-Layer Performance","text":"<ul> <li>Keep API handlers non-blocking around I/O and orchestration</li> <li>Avoid expensive serialization work in tight streaming loops</li> <li>Decode only requested modalities to reduce unnecessary work</li> </ul>"},{"location":"developer_guide/performance/#profiling-workflow","title":"Profiling Workflow","text":"<p>Use a layered profiling approach:</p> <ol> <li>Reproduce with minimal workload</li> <li>Profile runtime hotspots first</li> <li>Validate end-to-end impact under concurrent sessions</li> <li>Compare before/after with the same hardware and config</li> </ol> <p>Useful commands:</p> <pre><code>pytest tests/ -x --tb=short\nworldkernels bench latency --world dummy --steps 100\nworldkernels bench throughput --world dummy --sessions 4\n</code></pre>"},{"location":"developer_guide/performance/#common-pitfalls","title":"Common Pitfalls","text":"<ul> <li>Allocating tensors inside iterative transition loops</li> <li>Calling sync-heavy operations in hot paths</li> <li>Mixing concerns between scheduling and model compute logic</li> <li>Optimizing without representative benchmarks</li> </ul>"},{"location":"developer_guide/performance/#performance-review-checklist","title":"Performance Review Checklist","text":"<p>Before merging a performance-related change, confirm:</p> <ul> <li>Correctness is unchanged</li> <li>Memory behavior is stable for long sessions</li> <li>Measured metric improvements are reproducible</li> <li>Complexity increase is justified by meaningful gains</li> </ul>"},{"location":"api/worldkernels/","title":"worldkernels","text":""},{"location":"api/worldkernels/#worldkernels","title":"worldkernels","text":"<p>WorldKernels - GPU-first world model simulation engine.</p> <p>Modules:</p> Name Description <code>bootstrap</code> <p>Lazy model bootstrap: resolve ref, provision deps, fetch weights.</p> <code>cli</code> <p>WorldKernels CLI package.</p> <code>config</code> <p>Configuration dataclasses for worldkernels.</p> <code>core</code> <p>Core data types: session lifecycle, action, observation, error hierarchy.</p> <code>distributed</code> <p>Distributed-execution primitives: parallel state, collectives, CFG/sequence parallelism.</p> <code>engine</code> <p>Engine layer: request intake, model lifecycle, session registry.</p> <code>envs</code> <p>Central registry of WorldKernels environment variables.</p> <code>integrations</code> <code>models</code> <p>Native video-diffusion model families and their pipelines.</p> <code>platforms</code> <p>Hardware-platform abstraction. <code>current_platform()</code> resolves the active device family.</p> <code>plugins</code> <p>Entry-point plugin system for WorldKernels.</p> <code>runtime</code> <code>scheduler</code> <p>Scheduler layer: continuous-batching dispatch, admission, preemption.</p> <code>serving</code> <code>ui</code> <p>Shared low-level terminal UI kernel: theme, console, verbosity, components, live displays.</p> <code>utils</code> <p>Shared utilities: optional-dependency handling and device detection.</p> <code>worker</code> <p>Worker layer: device infrastructure and the compute runner.</p> <code>worlds</code> <p>World-model taxonomy, registry, and hub.</p>"},{"location":"api/worldkernels/#worldkernels.__version__","title":"__version__  <code>module-attribute</code>","text":"<pre><code>__version__: str = '0.1.dev140+g04d16ba0f'\n</code></pre>"},{"location":"api/worldkernels/#worldkernels.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WorldEngine', 'Session', 'SessionStatus', 'LatentState', 'Action', 'Observation', 'WorldConfig', 'ServerConfig', 'StageConfig', 'StageExecMode', 'StageOutput', 'StageTiming', 'StageType', 'TransitionMode', 'WorldKernelError', 'WorldNotFoundError', 'WorldAlreadyLoadedError', 'WorldInitError', 'SessionLimitError', 'SessionNotFoundError', 'SessionTerminatedError', 'SessionPausedError', 'CheckpointNotFoundError', 'VRAMExhaustedError', '__version__']\n</code></pre>"},{"location":"api/worldkernels/#worldkernels._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'Action': ('worldkernels.core.action', 'Action'), 'WorldConfig': ('worldkernels.core.config', 'WorldConfig'), 'ServerConfig': ('worldkernels.core.config', 'ServerConfig'), 'WorldEngine': ('worldkernels.engine', 'WorldEngine'), 'Observation': ('worldkernels.core.observation', 'Observation'), 'Session': ('worldkernels.core.session', 'Session'), 'SessionStatus': ('worldkernels.core.session', 'SessionStatus'), 'LatentState': ('worldkernels.core.session', 'LatentState'), 'StageConfig': ('worldkernels.runtime.stages', 'StageConfig'), 'StageExecMode': ('worldkernels.runtime.stages', 'StageExecMode'), 'StageOutput': ('worldkernels.runtime.stages', 'StageOutput'), 'StageTiming': ('worldkernels.runtime.stages', 'StageTiming'), 'StageType': ('worldkernels.runtime.stages', 'StageType'), 'TransitionMode': ('worldkernels.runtime.stages', 'TransitionMode'), 'WorldKernelError': ('worldkernels.core.errors', 'WorldKernelError'), 'WorldNotFoundError': ('worldkernels.core.errors', 'WorldNotFoundError'), 'WorldAlreadyLoadedError': ('worldkernels.core.errors', 'WorldAlreadyLoadedError'), 'WorldInitError': ('worldkernels.core.errors', 'WorldInitError'), 'SessionLimitError': ('worldkernels.core.errors', 'SessionLimitError'), 'SessionNotFoundError': ('worldkernels.core.errors', 'SessionNotFoundError'), 'SessionTerminatedError': ('worldkernels.core.errors', 'SessionTerminatedError'), 'SessionPausedError': ('worldkernels.core.errors', 'SessionPausedError'), 'CheckpointNotFoundError': ('worldkernels.core.errors', 'CheckpointNotFoundError'), 'VRAMExhaustedError': ('worldkernels.core.errors', 'VRAMExhaustedError')}\n</code></pre>"},{"location":"api/worldkernels/#worldkernels.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module 'worldkernels' has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/","title":"bootstrap","text":""},{"location":"api/worldkernels/bootstrap/#worldkernels.bootstrap","title":"worldkernels.bootstrap","text":"<p>Lazy model bootstrap: resolve ref, provision deps, fetch weights.</p> <p>Modules:</p> Name Description <code>cache</code> <p>Cache layout: WORLDKERNELS_HOME with packages/, manifests/.</p> <code>deps</code> <p>Provision python deps (uv) and native packages (git clone) with progress.</p> <code>errors</code> <p>Bootstrap-specific errors. Surfaced cleanly to CLI/HTTP without tracebacks.</p> <code>hf</code> <p>HuggingFace download tuning.</p> <code>installer</code> <p>Installer dispatch: shared vs isolated tier.</p> <code>pipeline</code> <p>bootstrap.prepare \u2014 single entrypoint that drives all five phases.</p> <code>progress</code> <p>Bootstrap progress UI: thin adapter over worldkernels.ui (tty/plain/quiet/json/sink).</p> <code>resolve</code> <p>Resolve a user-facing model reference to (ModelCard, variant, ckpt_path).</p> <code>weights</code> <p>Checkpoint provisioning: HF snapshot_download or local path passthrough.</p>"},{"location":"api/worldkernels/bootstrap/#worldkernels.bootstrap.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['prepare', 'PreparedModel', 'ProgressController', 'BootstrapError', 'FetchDisabledError', 'AuthRequiredError']\n</code></pre>"},{"location":"api/worldkernels/bootstrap/#worldkernels.bootstrap._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'prepare': ('worldkernels.bootstrap.pipeline', 'prepare'), 'PreparedModel': ('worldkernels.bootstrap.pipeline', 'PreparedModel'), 'ProgressController': ('worldkernels.bootstrap.progress', 'ProgressController'), 'BootstrapError': ('worldkernels.bootstrap.errors', 'BootstrapError'), 'FetchDisabledError': ('worldkernels.bootstrap.errors', 'FetchDisabledError'), 'AuthRequiredError': ('worldkernels.bootstrap.errors', 'AuthRequiredError')}\n</code></pre>"},{"location":"api/worldkernels/bootstrap/#worldkernels.bootstrap.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/bootstrap/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/","title":"cache","text":""},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache","title":"worldkernels.bootstrap.cache","text":"<p>Cache layout: WORLDKERNELS_HOME with packages/, manifests/.</p>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest","title":"Manifest  <code>dataclass</code>","text":"Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>@dataclass\nclass Manifest:\n    model_id: str\n    variant: str | None = None\n    adapter: str = \"\"\n    hf_repo: str | None = None\n    ckpt_path: str | None = None\n    pip_extra: str | None = None\n    git_packages: list[str] = field(default_factory=list)\n    created_at: str = \"\"\n\n    def write(self) -&gt; None:\n        path = manifests_dir() / f\"{_slug(self.model_id, self.variant)}.json\"\n        self.created_at = self.created_at or datetime.now(timezone.utc).isoformat()\n        path.write_text(json.dumps(asdict(self), indent=2))\n\n    @classmethod\n    def read(cls, model_id: str, variant: str | None = None) -&gt; \"Manifest | None\":\n        path = manifests_dir() / f\"{_slug(model_id, variant)}.json\"\n        if not path.exists():\n            return None\n        return cls(**json.loads(path.read_text()))\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.model_id","title":"model_id  <code>instance-attribute</code>","text":"<pre><code>model_id: str\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.variant","title":"variant  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>variant: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.adapter","title":"adapter  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>adapter: str = ''\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.hf_repo","title":"hf_repo  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>hf_repo: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.ckpt_path","title":"ckpt_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ckpt_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.pip_extra","title":"pip_extra  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pip_extra: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.git_packages","title":"git_packages  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>git_packages: list[str] = field(default_factory=list)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.created_at","title":"created_at  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>created_at: str = ''\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.__init__","title":"__init__","text":"<pre><code>__init__(model_id: str, variant: str | None = None, adapter: str = '', hf_repo: str | None = None, ckpt_path: str | None = None, pip_extra: str | None = None, git_packages: list[str] = list(), created_at: str = '') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.write","title":"write","text":"<pre><code>write() -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def write(self) -&gt; None:\n    path = manifests_dir() / f\"{_slug(self.model_id, self.variant)}.json\"\n    self.created_at = self.created_at or datetime.now(timezone.utc).isoformat()\n    path.write_text(json.dumps(asdict(self), indent=2))\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.Manifest.read","title":"read  <code>classmethod</code>","text":"<pre><code>read(model_id: str, variant: str | None = None) -&gt; 'Manifest | None'\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>@classmethod\ndef read(cls, model_id: str, variant: str | None = None) -&gt; \"Manifest | None\":\n    path = manifests_dir() / f\"{_slug(model_id, variant)}.json\"\n    if not path.exists():\n        return None\n    return cls(**json.loads(path.read_text()))\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.home","title":"home","text":"<pre><code>home() -&gt; Path\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def home() -&gt; Path:\n    env = os.environ.get(\"WORLDKERNELS_HOME\")\n    if env:\n        return Path(env).expanduser()\n    return Path.home() / \".cache\" / \"worldkernels\"\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.packages_dir","title":"packages_dir","text":"<pre><code>packages_dir() -&gt; Path\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def packages_dir() -&gt; Path:\n    p = home() / \"packages\"\n    p.mkdir(parents=True, exist_ok=True)\n    return p\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.manifests_dir","title":"manifests_dir","text":"<pre><code>manifests_dir() -&gt; Path\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def manifests_dir() -&gt; Path:\n    p = home() / \"manifests\"\n    p.mkdir(parents=True, exist_ok=True)\n    return p\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.list_manifests","title":"list_manifests","text":"<pre><code>list_manifests() -&gt; list[Manifest]\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def list_manifests() -&gt; list[Manifest]:\n    out: list[Manifest] = []\n    for p in manifests_dir().glob(\"*.json\"):\n        try:\n            out.append(Manifest(**json.loads(p.read_text())))\n        except Exception:\n            continue\n    return out\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.remove_manifest","title":"remove_manifest","text":"<pre><code>remove_manifest(model_id: str, variant: str | None = None) -&gt; bool\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def remove_manifest(model_id: str, variant: str | None = None) -&gt; bool:\n    path = manifests_dir() / f\"{_slug(model_id, variant)}.json\"\n    if path.exists():\n        path.unlink()\n        return True\n    return False\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache._slug","title":"_slug","text":"<pre><code>_slug(model_id: str, variant: str | None) -&gt; str\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def _slug(model_id: str, variant: str | None) -&gt; str:\n    s = model_id.replace(\"/\", \"_\").replace(\":\", \"_\")\n    if variant:\n        s = f\"{s}__{variant}\"\n    return s\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.directory_size_bytes","title":"directory_size_bytes","text":"<pre><code>directory_size_bytes(path: Path) -&gt; int\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def directory_size_bytes(path: Path) -&gt; int:\n    if not path.exists():\n        return 0\n    total = 0\n    for p in path.rglob(\"*\"):\n        if p.is_file():\n            try:\n                total += p.stat().st_size\n            except OSError:\n                pass\n    return total\n</code></pre>"},{"location":"api/worldkernels/bootstrap/cache/#worldkernels.bootstrap.cache.hf_cache_size_bytes","title":"hf_cache_size_bytes","text":"<pre><code>hf_cache_size_bytes(repo_id: str) -&gt; int\n</code></pre> Source code in <code>worldkernels/bootstrap/cache.py</code> <pre><code>def hf_cache_size_bytes(repo_id: str) -&gt; int:\n    try:\n        from huggingface_hub import scan_cache_dir\n\n        info = scan_cache_dir()\n        return sum(repo.size_on_disk for repo in info.repos if repo.repo_id == repo_id)\n    except Exception:\n        return 0\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/","title":"deps","text":""},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps","title":"worldkernels.bootstrap.deps","text":"<p>Provision python deps (uv) and native packages (git clone) with progress.</p>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._EXTRA_SENTINELS","title":"_EXTRA_SENTINELS  <code>module-attribute</code>","text":"<pre><code>_EXTRA_SENTINELS: dict[str, str] = {'diffusion': 'diffusers'}\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._UV_BIN","title":"_UV_BIN  <code>module-attribute</code>","text":"<pre><code>_UV_BIN: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps.provision_python_deps","title":"provision_python_deps","text":"<pre><code>provision_python_deps(card: 'ModelCard', progress: 'ProgressController | None' = None, allow_fetch: bool = True, target_python: str | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def provision_python_deps(\n    card: \"ModelCard\",\n    progress: \"ProgressController | None\" = None,\n    allow_fetch: bool = True,\n    target_python: str | None = None,\n) -&gt; None:\n    extra = card.pip_extra\n    if extra is None:\n        if progress is not None:\n            progress.event(\"deps\", \"skipped\", \"none required\")\n        return\n\n    sentinel = _EXTRA_SENTINELS.get(extra, extra)\n    if target_python is None:\n        try:\n            importlib.import_module(sentinel)\n            if progress is not None:\n                progress.event(\"deps\", \"skipped\", f\"{extra} already installed\")\n            return\n        except ImportError:\n            pass\n\n    if not allow_fetch or os.environ.get(\"WORLDKERNELS_NO_AUTO_INSTALL\"):\n        raise FetchDisabledError(\n            f\"missing python deps for '{card.adapter}' (extra: {extra})\",\n            f\"install with: pip install 'worldkernels[{extra}]'\",\n        )\n\n    pkg = f\"worldkernels[{extra}]\"\n    if progress is not None:\n        progress.event(\"deps\", \"running\", f\"installing '{pkg}' \u2026\")\n    log.info(\"Auto-installing missing dependencies: %s\", pkg)\n\n    install_packages([pkg], target_python=target_python, progress=progress)\n\n    if progress is not None:\n        progress.event(\"deps\", \"done\", f\"installed {extra}\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps.install_packages","title":"install_packages","text":"<pre><code>install_packages(packages: list[str], target_python: str | None = None, progress: 'ProgressController | None' = None, constraints: str | None = None) -&gt; None\n</code></pre> <p>Install pip packages into the target python via uv (pip fallback).</p> <p>Parameters:</p> Name Type Description Default <code>packages</code> <code>list[str]</code> <p>PEP 508 package specs.</p> required <code>target_python</code> <code>str | None</code> <p>Python interpreter to install into (defaults to the current one).</p> <code>None</code> <code>progress</code> <code>'ProgressController | None'</code> <p>Optional progress controller for live status updates.</p> <code>None</code> <code>constraints</code> <code>str | None</code> <p>Optional path to a requirements/lock constraints file.</p> <code>None</code> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def install_packages(\n    packages: list[str],\n    target_python: str | None = None,\n    progress: \"ProgressController | None\" = None,\n    constraints: str | None = None,\n) -&gt; None:\n    r\"\"\"Install pip packages into the target python via uv (pip fallback).\n\n    Args:\n        packages: PEP 508 package specs.\n        target_python: Python interpreter to install into (defaults to the current one).\n        progress: Optional progress controller for live status updates.\n        constraints: Optional path to a requirements/lock constraints file.\n    \"\"\"\n    if not packages:\n        return\n    python_exe = target_python or sys.executable\n    runner = _select_installer()\n    cmd = _build_install_cmd(runner, packages, python_exe, constraints)\n    quiet = progress is not None and progress.mode in (\"tty\", \"quiet\", \"json\", \"sink\")\n\n    log.debug(\"Running install: %s\", \" \".join(cmd))\n    proc = subprocess.run(cmd, check=False, capture_output=quiet, text=True)\n    if proc.returncode != 0:\n        if progress is not None:\n            progress.event(\"deps\", \"failed\", f\"install failed (exit {proc.returncode})\")\n        if quiet:\n            sys.stderr.write((proc.stdout or \"\") + \"\\n\" + (proc.stderr or \"\") + \"\\n\")\n        raise RuntimeError(f\"install failed for {packages!r} via {runner} (exit {proc.returncode})\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._resolve_uv","title":"_resolve_uv","text":"<pre><code>_resolve_uv() -&gt; str | None\n</code></pre> <p>Locate the bundled uv binary (PATH-independent). None means use pip.</p> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def _resolve_uv() -&gt; str | None:\n    r\"\"\"Locate the bundled uv binary (PATH-independent). None means use pip.\"\"\"\n    global _UV_BIN\n    if os.environ.get(\"WORLDKERNELS_FORCE_PIP\"):\n        return None\n    if _UV_BIN is not None:\n        return _UV_BIN\n    try:\n        from uv import find_uv_bin\n\n        _UV_BIN = find_uv_bin()\n    except (ImportError, FileNotFoundError):\n        _UV_BIN = shutil.which(\"uv\")\n    return _UV_BIN\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._select_installer","title":"_select_installer","text":"<pre><code>_select_installer() -&gt; str\n</code></pre> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def _select_installer() -&gt; str:\n    return _resolve_uv() or \"pip\"\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._build_install_cmd","title":"_build_install_cmd","text":"<pre><code>_build_install_cmd(runner: str, packages: list[str], python_exe: str, constraints: str | None) -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def _build_install_cmd(\n    runner: str, packages: list[str], python_exe: str, constraints: str | None\n) -&gt; list[str]:\n    if runner != \"pip\":\n        cmd = [runner, \"pip\", \"install\", \"--python\", python_exe]\n        if constraints:\n            cmd += [\"--constraint\", constraints]\n        cmd += packages\n        return cmd\n    cmd = [python_exe, \"-m\", \"pip\", \"install\", \"--disable-pip-version-check\"]\n    if constraints:\n        cmd += [\"--constraint\", constraints]\n    cmd += packages\n    return cmd\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps.provision_git_packages","title":"provision_git_packages","text":"<pre><code>provision_git_packages(card: 'ModelCard', progress: 'ProgressController | None' = None, allow_fetch: bool = True) -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def provision_git_packages(\n    card: \"ModelCard\",\n    progress: \"ProgressController | None\" = None,\n    allow_fetch: bool = True,\n) -&gt; None:\n    if not card.git_packages:\n        if progress is not None:\n            progress.event(\"packages\", \"skipped\", \"none required\")\n        return\n\n    if progress is not None:\n        progress.event(\"packages\", \"running\", \"checking \u2026\")\n\n    installed: list[str] = []\n    for gp in card.git_packages:\n        path = _ensure_git_package(gp, progress=progress, allow_fetch=allow_fetch)\n        if path is not None and str(path) not in sys.path:\n            sys.path.insert(0, str(path))\n        installed.append(gp.name)\n\n    if progress is not None:\n        progress.event(\"packages\", \"done\", \", \".join(installed))\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._ensure_git_package","title":"_ensure_git_package","text":"<pre><code>_ensure_git_package(gp: 'GitPackage', progress: 'ProgressController | None' = None, allow_fetch: bool = True) -&gt; Path | None\n</code></pre> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def _ensure_git_package(\n    gp: \"GitPackage\",\n    progress: \"ProgressController | None\" = None,\n    allow_fetch: bool = True,\n) -&gt; Path | None:\n    if gp.env_path_var:\n        env_path = os.environ.get(gp.env_path_var)\n        if env_path and _is_valid(Path(env_path), gp.import_check):\n            return Path(env_path)\n\n    for candidate in _candidate_paths(gp):\n        if _is_valid(candidate, gp.import_check):\n            return candidate\n\n    dest = cache.packages_dir() / gp.name\n\n    if dest.exists() and _is_valid(dest, gp.import_check):\n        return dest\n\n    if not allow_fetch or os.environ.get(\"WORLDKERNELS_NO_AUTO_INSTALL\"):\n        raise FetchDisabledError(\n            f\"missing git package '{gp.name}'\",\n            f\"clone {gp.url} to {dest} or set ${gp.env_path_var}\",\n        )\n\n    if shutil.which(\"git\") is None:\n        raise RuntimeError(\n            f\"git not found on PATH; cannot fetch {gp.name}. Install git or set ${gp.env_path_var}.\"\n        )\n\n    if progress is not None:\n        progress.event(\"packages\", \"running\", f\"cloning {gp.url} \u2026\")\n    log.info(\"Cloning %s to %s\", gp.url, dest)\n\n    args = [\"git\", \"clone\", \"--depth=1\"]\n    if gp.ref:\n        args += [\"--branch\", gp.ref]\n    args += [gp.url, str(dest)]\n\n    proc = subprocess.run(args, check=False, capture_output=True, text=True, timeout=600)\n    if proc.returncode != 0:\n        if progress is not None:\n            progress.event(\"packages\", \"failed\", f\"clone failed: {gp.url}\")\n        sys.stderr.write(proc.stderr)\n        raise RuntimeError(f\"git clone failed for {gp.url}\")\n\n    return dest\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._candidate_paths","title":"_candidate_paths","text":"<pre><code>_candidate_paths(gp: 'GitPackage') -&gt; list[Path]\n</code></pre> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def _candidate_paths(gp: \"GitPackage\") -&gt; list[Path]:\n    return [\n        Path.home() / gp.name,\n        Path.home() / gp.url.rstrip(\"/\").rsplit(\"/\", 1)[-1].removesuffix(\".git\"),\n    ]\n</code></pre>"},{"location":"api/worldkernels/bootstrap/deps/#worldkernels.bootstrap.deps._is_valid","title":"_is_valid","text":"<pre><code>_is_valid(path: Path, import_check: str | None) -&gt; bool\n</code></pre> Source code in <code>worldkernels/bootstrap/deps.py</code> <pre><code>def _is_valid(path: Path, import_check: str | None) -&gt; bool:\n    if not path.exists():\n        return False\n    if import_check is None:\n        return True\n    return (path / import_check.replace(\".\", \"/\") / \"__init__.py\").exists()\n</code></pre>"},{"location":"api/worldkernels/bootstrap/errors/","title":"errors","text":""},{"location":"api/worldkernels/bootstrap/errors/#worldkernels.bootstrap.errors","title":"worldkernels.bootstrap.errors","text":"<p>Bootstrap-specific errors. Surfaced cleanly to CLI/HTTP without tracebacks.</p>"},{"location":"api/worldkernels/bootstrap/errors/#worldkernels.bootstrap.errors.BootstrapError","title":"BootstrapError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/bootstrap/errors.py</code> <pre><code>class BootstrapError(WorldKernelError):\n    pass\n</code></pre>"},{"location":"api/worldkernels/bootstrap/errors/#worldkernels.bootstrap.errors.FetchDisabledError","title":"FetchDisabledError","text":"<p>               Bases: <code>BootstrapError</code></p> Source code in <code>worldkernels/bootstrap/errors.py</code> <pre><code>class FetchDisabledError(BootstrapError):\n    def __init__(self, what: str, hint: str) -&gt; None:\n        super().__init__(f\"{what} (auto-fetch disabled). {hint}\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/errors/#worldkernels.bootstrap.errors.FetchDisabledError.__init__","title":"__init__","text":"<pre><code>__init__(what: str, hint: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/errors.py</code> <pre><code>def __init__(self, what: str, hint: str) -&gt; None:\n    super().__init__(f\"{what} (auto-fetch disabled). {hint}\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/errors/#worldkernels.bootstrap.errors.AuthRequiredError","title":"AuthRequiredError","text":"<p>               Bases: <code>BootstrapError</code></p> Source code in <code>worldkernels/bootstrap/errors.py</code> <pre><code>class AuthRequiredError(BootstrapError):\n    def __init__(self, repo: str) -&gt; None:\n        super().__init__(f\"Model '{repo}' is gated. Run `huggingface-cli login` and retry.\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/errors/#worldkernels.bootstrap.errors.AuthRequiredError.__init__","title":"__init__","text":"<pre><code>__init__(repo: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/errors.py</code> <pre><code>def __init__(self, repo: str) -&gt; None:\n    super().__init__(f\"Model '{repo}' is gated. Run `huggingface-cli login` and retry.\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/errors/#worldkernels.bootstrap.errors.ModelNotFoundError","title":"ModelNotFoundError","text":"<p>               Bases: <code>BootstrapError</code></p> Source code in <code>worldkernels/bootstrap/errors.py</code> <pre><code>class ModelNotFoundError(BootstrapError):\n    pass\n</code></pre>"},{"location":"api/worldkernels/bootstrap/hf/","title":"hf","text":""},{"location":"api/worldkernels/bootstrap/hf/#worldkernels.bootstrap.hf","title":"worldkernels.bootstrap.hf","text":"<p>HuggingFace download tuning.</p> <p>Prefer the fastest transfer backend that is actually installed: Xet (<code>hf_xet</code> + <code>HF_XET_HIGH_PERFORMANCE</code>) on modern hubs, else the legacy <code>hf_transfer</code> Rust downloader. Setting <code>HF_HUB_ENABLE_HF_TRANSFER</code> is avoided unless <code>hf_transfer</code> is present, since recent <code>huggingface_hub</code> deprecated it (it warns and ignores the flag when Xet is the active backend).</p>"},{"location":"api/worldkernels/bootstrap/hf/#worldkernels.bootstrap.hf.enable_fast_hf_transfer","title":"enable_fast_hf_transfer","text":"<pre><code>enable_fast_hf_transfer() -&gt; None\n</code></pre> <p>Enable the fastest available HF transfer backend. Idempotent.</p> <p>Xet's high-performance mode ramps download concurrency aggressively. On unauthenticated transfers the hub rate-limits and resets connections, which can deadlock <code>hf_xet</code> (sockets stuck in CLOSE-WAIT, no progress). So we gate it on a token being present; without one, Xet still runs at its safe default concurrency.</p> Source code in <code>worldkernels/bootstrap/hf.py</code> <pre><code>def enable_fast_hf_transfer() -&gt; None:\n    r\"\"\"Enable the fastest available HF transfer backend. Idempotent.\n\n    Xet's high-performance mode ramps download concurrency aggressively. On\n    *unauthenticated* transfers the hub rate-limits and resets connections, which can\n    deadlock ``hf_xet`` (sockets stuck in CLOSE-WAIT, no progress). So we gate it on a\n    token being present; without one, Xet still runs at its safe default concurrency.\n    \"\"\"\n    if _util.find_spec(\"hf_xet\") is not None:\n        if _has_hf_token():\n            os.environ.setdefault(\"HF_XET_HIGH_PERFORMANCE\", \"1\")\n        os.environ.pop(\"HF_HUB_ENABLE_HF_TRANSFER\", None)\n    elif _util.find_spec(\"hf_transfer\") is not None:\n        os.environ.setdefault(\"HF_HUB_ENABLE_HF_TRANSFER\", \"1\")\n</code></pre>"},{"location":"api/worldkernels/bootstrap/hf/#worldkernels.bootstrap.hf._has_hf_token","title":"_has_hf_token","text":"<pre><code>_has_hf_token() -&gt; bool\n</code></pre> Source code in <code>worldkernels/bootstrap/hf.py</code> <pre><code>def _has_hf_token() -&gt; bool:\n    if os.environ.get(\"HF_TOKEN\") or os.environ.get(\"HUGGING_FACE_HUB_TOKEN\"):\n        return True\n    try:\n        from huggingface_hub import get_token\n\n        return get_token() is not None\n    except Exception:\n        return False\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/","title":"installer","text":""},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer","title":"worldkernels.bootstrap.installer","text":"<p>Installer dispatch: shared vs isolated tier.</p> <p>The shared installer puts deps into the running interpreter's env (the same path the lazy bootstrap has always used). The isolated installer materializes a per-model uv venv and returns its path; the engine then spawns a worker inside that venv via <code>RemoteWorld</code>.</p>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Installer', 'SharedInstaller', 'IsolatedInstaller', 'InstallResult']\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.InstallResult","title":"InstallResult  <code>dataclass</code>","text":"<p>Where the model's deps now live.</p> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>@dataclass\nclass InstallResult:\n    r\"\"\"Where the model's deps now live.\"\"\"\n\n    tier: str  # \"shared\" | \"isolated\"\n    env_path: Path | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.InstallResult.tier","title":"tier  <code>instance-attribute</code>","text":"<pre><code>tier: str\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.InstallResult.env_path","title":"env_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>env_path: Path | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.InstallResult.__init__","title":"__init__","text":"<pre><code>__init__(tier: str, env_path: Path | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.Installer","title":"Installer","text":"<p>               Bases: <code>Protocol</code></p> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>class Installer(Protocol):\n    def install(\n        self,\n        card: \"ModelCard\",\n        progress: \"ProgressController | None\",\n        allow_fetch: bool,\n    ) -&gt; InstallResult: ...\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.Installer.install","title":"install","text":"<pre><code>install(card: 'ModelCard', progress: 'ProgressController | None', allow_fetch: bool) -&gt; InstallResult\n</code></pre> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>def install(\n    self,\n    card: \"ModelCard\",\n    progress: \"ProgressController | None\",\n    allow_fetch: bool,\n) -&gt; InstallResult: ...\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.SharedInstaller","title":"SharedInstaller","text":"<p>Install card's pip_extra (and git packages) into the current interpreter.</p> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>class SharedInstaller:\n    r\"\"\"Install card's pip_extra (and git packages) into the current interpreter.\"\"\"\n\n    def install(\n        self,\n        card: \"ModelCard\",\n        progress: \"ProgressController | None\",\n        allow_fetch: bool,\n    ) -&gt; InstallResult:\n        from worldkernels.bootstrap import deps\n\n        deps.provision_python_deps(card, progress=progress, allow_fetch=allow_fetch)\n        deps.provision_git_packages(card, progress=progress, allow_fetch=allow_fetch)\n        return InstallResult(tier=\"shared\", env_path=None)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.SharedInstaller.install","title":"install","text":"<pre><code>install(card: 'ModelCard', progress: 'ProgressController | None', allow_fetch: bool) -&gt; InstallResult\n</code></pre> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>def install(\n    self,\n    card: \"ModelCard\",\n    progress: \"ProgressController | None\",\n    allow_fetch: bool,\n) -&gt; InstallResult:\n    from worldkernels.bootstrap import deps\n\n    deps.provision_python_deps(card, progress=progress, allow_fetch=allow_fetch)\n    deps.provision_git_packages(card, progress=progress, allow_fetch=allow_fetch)\n    return InstallResult(tier=\"shared\", env_path=None)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.IsolatedInstaller","title":"IsolatedInstaller","text":"<p>Materialize a per-model uv venv and install the card's deps into it.</p> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>class IsolatedInstaller:\n    r\"\"\"Materialize a per-model uv venv and install the card's deps into it.\"\"\"\n\n    def __init__(self, model_id: str, requirements: list[str], device: str = \"cuda\") -&gt; None:\n        self.model_id = model_id\n        self.requirements = requirements\n        self.device = device\n\n    def install(\n        self,\n        card: \"ModelCard\",\n        progress: \"ProgressController | None\",\n        allow_fetch: bool,\n    ) -&gt; InstallResult:\n        from worldkernels.runtime import envs\n\n        if progress is not None:\n            progress.event(\"isolating\", \"running\", f\"materializing env for {self.model_id} \u2026\")\n        env_path = envs.materialize_env(\n            self.model_id,\n            self.requirements,\n            device=self.device,\n            progress=progress,\n            allow_fetch=allow_fetch,\n        )\n        return InstallResult(tier=\"isolated\", env_path=env_path)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.IsolatedInstaller.model_id","title":"model_id  <code>instance-attribute</code>","text":"<pre><code>model_id = model_id\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.IsolatedInstaller.requirements","title":"requirements  <code>instance-attribute</code>","text":"<pre><code>requirements = requirements\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.IsolatedInstaller.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.IsolatedInstaller.__init__","title":"__init__","text":"<pre><code>__init__(model_id: str, requirements: list[str], device: str = 'cuda') -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>def __init__(self, model_id: str, requirements: list[str], device: str = \"cuda\") -&gt; None:\n    self.model_id = model_id\n    self.requirements = requirements\n    self.device = device\n</code></pre>"},{"location":"api/worldkernels/bootstrap/installer/#worldkernels.bootstrap.installer.IsolatedInstaller.install","title":"install","text":"<pre><code>install(card: 'ModelCard', progress: 'ProgressController | None', allow_fetch: bool) -&gt; InstallResult\n</code></pre> Source code in <code>worldkernels/bootstrap/installer.py</code> <pre><code>def install(\n    self,\n    card: \"ModelCard\",\n    progress: \"ProgressController | None\",\n    allow_fetch: bool,\n) -&gt; InstallResult:\n    from worldkernels.runtime import envs\n\n    if progress is not None:\n        progress.event(\"isolating\", \"running\", f\"materializing env for {self.model_id} \u2026\")\n    env_path = envs.materialize_env(\n        self.model_id,\n        self.requirements,\n        device=self.device,\n        progress=progress,\n        allow_fetch=allow_fetch,\n    )\n    return InstallResult(tier=\"isolated\", env_path=env_path)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/","title":"pipeline","text":""},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline","title":"worldkernels.bootstrap.pipeline","text":"<p>bootstrap.prepare \u2014 single entrypoint that drives all five phases.</p>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.PreparedModel","title":"PreparedModel  <code>dataclass</code>","text":"Source code in <code>worldkernels/bootstrap/pipeline.py</code> <pre><code>@dataclass\nclass PreparedModel:\n    adapter: str\n    alias: str\n    variant: str | None\n    kwargs: dict[str, Any] = field(default_factory=dict)\n    ckpt_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.PreparedModel.adapter","title":"adapter  <code>instance-attribute</code>","text":"<pre><code>adapter: str\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.PreparedModel.alias","title":"alias  <code>instance-attribute</code>","text":"<pre><code>alias: str\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.PreparedModel.variant","title":"variant  <code>instance-attribute</code>","text":"<pre><code>variant: str | None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.PreparedModel.kwargs","title":"kwargs  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kwargs: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.PreparedModel.ckpt_path","title":"ckpt_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ckpt_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.PreparedModel.__init__","title":"__init__","text":"<pre><code>__init__(adapter: str, alias: str, variant: str | None, kwargs: dict[str, Any] = dict(), ckpt_path: str | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline.prepare","title":"prepare","text":"<pre><code>prepare(ref: str, variant: str | None = None, ckpt_path: str | None = None, progress: ProgressController | None = None, allow_fetch: bool = True, **adapter_kwargs: Any) -&gt; PreparedModel\n</code></pre> <p>Resolve a ref and provision everything needed to load it.</p> <p>Drives the five phases in order: resolve \u2192 deps \u2192 packages \u2192 weights \u2192 init (the <code>init</code> phase is just marked as <code>running</code> here; the caller does the actual instantiate/warmup so it can update the phase to <code>done</code>).</p> Source code in <code>worldkernels/bootstrap/pipeline.py</code> <pre><code>def prepare(\n    ref: str,\n    variant: str | None = None,\n    ckpt_path: str | None = None,\n    progress: ProgressController | None = None,\n    allow_fetch: bool = True,\n    **adapter_kwargs: Any,\n) -&gt; PreparedModel:\n    r\"\"\"Resolve a ref and provision everything needed to load it.\n\n    Drives the five phases in order: resolve \u2192 deps \u2192 packages \u2192 weights \u2192 init\n    (the ``init`` phase is just marked as ``running`` here; the caller does the\n    actual instantiate/warmup so it can update the phase to ``done``).\n    \"\"\"\n    owns_progress = progress is None\n    if progress is None:\n        progress = ProgressController(mode=\"auto\").__enter__()\n    assert progress is not None\n\n    try:\n        progress.event(\"resolve\", \"running\", ref)\n        resolved = resolve.resolve(ref, variant=variant, ckpt_path=ckpt_path)\n        variant_str = f\" (variant {resolved.variant})\" if resolved.variant else \"\"\n        progress.event(\"resolve\", \"done\", f\"{resolved.name}{variant_str}\")\n\n        deps.provision_python_deps(resolved.card, progress=progress, allow_fetch=allow_fetch)\n        deps.provision_git_packages(resolved.card, progress=progress, allow_fetch=allow_fetch)\n\n        local_path = weights.provision_weights(\n            resolved.card,\n            variant=resolved.variant,\n            ckpt_path=resolved.ckpt_path,\n            progress=progress,\n            allow_fetch=allow_fetch,\n        )\n\n        progress.event(\"init\", \"running\", \"loading \u2026\")\n\n        merged: dict[str, Any] = {**resolved.card.default_kwargs}\n        if resolved.variant is not None:\n            merged[\"variant\"] = resolved.variant\n        if local_path is not None and resolved.ckpt_path is not None:\n            merged[\"ckpt_path\"] = local_path\n        if resolved.card.generator is not None:\n            merged.setdefault(\"generator\", resolved.card.generator)\n        merged.update(adapter_kwargs)\n\n        alias = adapter_kwargs.get(\"alias\") or _alias_for(resolved)\n        manifest = cache.Manifest(\n            model_id=resolved.name,\n            variant=resolved.variant,\n            adapter=resolved.card.adapter,\n            hf_repo=resolved.card.hf_repo,\n            ckpt_path=local_path,\n            pip_extra=resolved.card.pip_extra,\n            git_packages=[gp.name for gp in resolved.card.git_packages],\n        )\n        manifest.write()\n\n        return PreparedModel(\n            adapter=resolved.card.adapter,\n            alias=alias,\n            variant=resolved.variant,\n            kwargs=merged,\n            ckpt_path=local_path,\n        )\n    finally:\n        if owns_progress:\n            progress.__exit__(None, None, None)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/pipeline/#worldkernels.bootstrap.pipeline._alias_for","title":"_alias_for","text":"<pre><code>_alias_for(resolved: ResolvedRef) -&gt; str\n</code></pre> Source code in <code>worldkernels/bootstrap/pipeline.py</code> <pre><code>def _alias_for(resolved: resolve.ResolvedRef) -&gt; str:\n    base = resolved.name.split(\"/\")[-1]\n    if resolved.variant:\n        return f\"{base}:{resolved.variant}\"\n    return base\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/","title":"progress","text":""},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress","title":"worldkernels.bootstrap.progress","text":"<p>Bootstrap progress UI: thin adapter over worldkernels.ui (tty/plain/quiet/json/sink).</p>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.PhaseStatus","title":"PhaseStatus  <code>module-attribute</code>","text":"<pre><code>PhaseStatus = Literal['pending', 'running', 'done', 'skipped', 'failed']\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.PHASES","title":"PHASES  <code>module-attribute</code>","text":"<pre><code>PHASES: tuple[str, ...] = ('resolve', 'deps', 'packages', 'weights', 'init')\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ProgressController', 'PHASES', 'PhaseStatus', 'cli_mode']\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController","title":"ProgressController","text":"<p>Phase-keyed progress UI shared by CLI, HTTP, and Python entry points.</p> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>class ProgressController:\n    r\"\"\"Phase-keyed progress UI shared by CLI, HTTP, and Python entry points.\"\"\"\n\n    def __init__(\n        self,\n        mode: str = \"auto\",\n        phases: tuple[str, ...] = PHASES,\n        sink: Callable[[dict[str, Any]], None] | None = None,\n    ) -&gt; None:\n        self.mode = _resolve_mode(mode)\n        self._pp = PhaseProgress(phases)\n        self._live: Any = None\n        self._console: Any = None\n        self._sink = sink\n\n    def __enter__(self) -&gt; \"ProgressController\":\n        if self.mode == \"tty\":\n            from worldkernels.ui.console import console\n            from worldkernels.ui.live import LiveDisplay\n            from worldkernels.ui.verbosity import OutputMode\n\n            self._console = console()\n            self._live = LiveDisplay(self._pp.render, OutputMode.AUTO).__enter__()\n            if not self._live.active:\n                self.mode = \"plain\"\n                self._live = None\n        return self\n\n    def __exit__(self, exc_type: Any, exc: Any, tb: Any) -&gt; None:\n        if self._live is not None:\n            self._live.__exit__(exc_type, exc, tb)\n\n    def event(\n        self,\n        phase: str,\n        status: PhaseStatus,\n        message: str = \"\",\n        fraction: float | None = None,\n    ) -&gt; None:\n        if not self._pp.set(phase, status, message, fraction):\n            return\n\n        if self._sink is not None:\n            self._sink({\"phase\": phase, \"status\": status, \"message\": message, \"fraction\": fraction})\n\n        if self.mode == \"tty\" and self._live is not None:\n            self._live.update()\n        elif self.mode == \"plain\":\n            ln = self._pp.plain_line(phase, status, message)\n            if ln is not None:\n                print(ln, flush=True)\n        elif self.mode == \"json\":\n            print(\n                _json.dumps(\n                    {\"phase\": phase, \"status\": status, \"message\": message, \"fraction\": fraction}\n                ),\n                flush=True,\n            )\n\n    def finalize(self, success: bool, summary: str = \"\") -&gt; None:\n        if self.mode == \"tty\" and self._console is not None:\n            from rich.text import Text\n\n            from worldkernels.ui.theme import style\n\n            mark = (\n                Text(\"\u2713 ready\", style=f\"bold {style('success')}\")\n                if success\n                else Text(\"\u2717 failed\", style=f\"bold {style('error')}\")\n            )\n            if summary:\n                self._console.print(mark, Text(f\"  {summary}\", style=style(\"muted\")))\n            else:\n                self._console.print(mark)\n        elif self.mode == \"plain\":\n            print((\"\u2713 ready \" if success else \"\u2717 failed \") + summary, flush=True)\n        elif self.mode == \"json\":\n            event = {\"phase\": \"ready\" if success else \"failed\", \"message\": summary}\n            print(_json.dumps(event), flush=True)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController.mode","title":"mode  <code>instance-attribute</code>","text":"<pre><code>mode = _resolve_mode(mode)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController._pp","title":"_pp  <code>instance-attribute</code>","text":"<pre><code>_pp = PhaseProgress(phases)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController._live","title":"_live  <code>instance-attribute</code>","text":"<pre><code>_live: Any = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController._console","title":"_console  <code>instance-attribute</code>","text":"<pre><code>_console: Any = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController._sink","title":"_sink  <code>instance-attribute</code>","text":"<pre><code>_sink = sink\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController.__init__","title":"__init__","text":"<pre><code>__init__(mode: str = 'auto', phases: tuple[str, ...] = PHASES, sink: Callable[[dict[str, Any]], None] | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>def __init__(\n    self,\n    mode: str = \"auto\",\n    phases: tuple[str, ...] = PHASES,\n    sink: Callable[[dict[str, Any]], None] | None = None,\n) -&gt; None:\n    self.mode = _resolve_mode(mode)\n    self._pp = PhaseProgress(phases)\n    self._live: Any = None\n    self._console: Any = None\n    self._sink = sink\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController.__enter__","title":"__enter__","text":"<pre><code>__enter__() -&gt; 'ProgressController'\n</code></pre> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>def __enter__(self) -&gt; \"ProgressController\":\n    if self.mode == \"tty\":\n        from worldkernels.ui.console import console\n        from worldkernels.ui.live import LiveDisplay\n        from worldkernels.ui.verbosity import OutputMode\n\n        self._console = console()\n        self._live = LiveDisplay(self._pp.render, OutputMode.AUTO).__enter__()\n        if not self._live.active:\n            self.mode = \"plain\"\n            self._live = None\n    return self\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController.__exit__","title":"__exit__","text":"<pre><code>__exit__(exc_type: Any, exc: Any, tb: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>def __exit__(self, exc_type: Any, exc: Any, tb: Any) -&gt; None:\n    if self._live is not None:\n        self._live.__exit__(exc_type, exc, tb)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController.event","title":"event","text":"<pre><code>event(phase: str, status: PhaseStatus, message: str = '', fraction: float | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>def event(\n    self,\n    phase: str,\n    status: PhaseStatus,\n    message: str = \"\",\n    fraction: float | None = None,\n) -&gt; None:\n    if not self._pp.set(phase, status, message, fraction):\n        return\n\n    if self._sink is not None:\n        self._sink({\"phase\": phase, \"status\": status, \"message\": message, \"fraction\": fraction})\n\n    if self.mode == \"tty\" and self._live is not None:\n        self._live.update()\n    elif self.mode == \"plain\":\n        ln = self._pp.plain_line(phase, status, message)\n        if ln is not None:\n            print(ln, flush=True)\n    elif self.mode == \"json\":\n        print(\n            _json.dumps(\n                {\"phase\": phase, \"status\": status, \"message\": message, \"fraction\": fraction}\n            ),\n            flush=True,\n        )\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.ProgressController.finalize","title":"finalize","text":"<pre><code>finalize(success: bool, summary: str = '') -&gt; None\n</code></pre> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>def finalize(self, success: bool, summary: str = \"\") -&gt; None:\n    if self.mode == \"tty\" and self._console is not None:\n        from rich.text import Text\n\n        from worldkernels.ui.theme import style\n\n        mark = (\n            Text(\"\u2713 ready\", style=f\"bold {style('success')}\")\n            if success\n            else Text(\"\u2717 failed\", style=f\"bold {style('error')}\")\n        )\n        if summary:\n            self._console.print(mark, Text(f\"  {summary}\", style=style(\"muted\")))\n        else:\n            self._console.print(mark)\n    elif self.mode == \"plain\":\n        print((\"\u2713 ready \" if success else \"\u2717 failed \") + summary, flush=True)\n    elif self.mode == \"json\":\n        event = {\"phase\": \"ready\" if success else \"failed\", \"message\": summary}\n        print(_json.dumps(event), flush=True)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress.cli_mode","title":"cli_mode","text":"<pre><code>cli_mode() -&gt; str\n</code></pre> <p>Map the resolved CLI output mode to a ProgressController mode string.</p> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>def cli_mode() -&gt; str:\n    r\"\"\"Map the resolved CLI output mode to a ProgressController mode string.\"\"\"\n    from worldkernels.ui.verbosity import OutputMode, resolve_output_mode\n\n    return {\n        OutputMode.AUTO: \"tty\",\n        OutputMode.PLAIN: \"plain\",\n        OutputMode.JSON: \"json\",\n        OutputMode.QUIET: \"quiet\",\n    }[resolve_output_mode()]\n</code></pre>"},{"location":"api/worldkernels/bootstrap/progress/#worldkernels.bootstrap.progress._resolve_mode","title":"_resolve_mode","text":"<pre><code>_resolve_mode(mode: str) -&gt; str\n</code></pre> Source code in <code>worldkernels/bootstrap/progress.py</code> <pre><code>def _resolve_mode(mode: str) -&gt; str:\n    if os.environ.get(\"WORLDKERNELS_QUIET\"):\n        return \"quiet\"\n    return \"plain\" if mode == \"auto\" else mode\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/","title":"resolve","text":""},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve","title":"worldkernels.bootstrap.resolve","text":"<p>Resolve a user-facing model reference to (ModelCard, variant, ckpt_path).</p>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.ResolvedRef","title":"ResolvedRef  <code>dataclass</code>","text":"Source code in <code>worldkernels/bootstrap/resolve.py</code> <pre><code>@dataclass\nclass ResolvedRef:\n    card: \"ModelCard\"\n    name: str\n    variant: str | None = None\n    ckpt_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.ResolvedRef.card","title":"card  <code>instance-attribute</code>","text":"<pre><code>card: 'ModelCard'\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.ResolvedRef.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.ResolvedRef.variant","title":"variant  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>variant: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.ResolvedRef.ckpt_path","title":"ckpt_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ckpt_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.ResolvedRef.__init__","title":"__init__","text":"<pre><code>__init__(card: 'ModelCard', name: str, variant: str | None = None, ckpt_path: str | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve.resolve","title":"resolve","text":"<pre><code>resolve(ref: str, variant: str | None = None, ckpt_path: str | None = None) -&gt; ResolvedRef\n</code></pre> <p>Resolve a CLI/HTTP model reference.</p> <p>Resolution order: 1. Local file/dir path (if it exists on disk). 2. Literal hub key. 3. Variant-suffix split (<code>name:variant</code> or <code>name@variant</code>). 4. HF URL strip \u2192 repo ID lookup. 5. Bare HF repo ID (<code>org/repo</code>) \u2014 looked up in hub, else a synthetic card.</p> Source code in <code>worldkernels/bootstrap/resolve.py</code> <pre><code>def resolve(ref: str, variant: str | None = None, ckpt_path: str | None = None) -&gt; ResolvedRef:\n    r\"\"\"Resolve a CLI/HTTP model reference.\n\n    Resolution order:\n    1. Local file/dir path (if it exists on disk).\n    2. Literal hub key.\n    3. Variant-suffix split (``name:variant`` or ``name@variant``).\n    4. HF URL strip \u2192 repo ID lookup.\n    5. Bare HF repo ID (``org/repo``) \u2014 looked up in hub, else a synthetic card.\n    \"\"\"\n    from worldkernels.worlds.hub import get_model_card, infer_card_from_hf\n\n    if ckpt_path is None and _looks_like_path(ref):\n        return _local_ref(ref, variant)\n\n    url_stripped = _strip_hf_url(ref)\n    if url_stripped is not None:\n        ref = url_stripped\n\n    card = get_model_card(ref)\n    if card is not None:\n        return _from_card(card, ref, variant, ckpt_path)\n\n    for sep in (\":\", \"@\"):\n        if sep in ref:\n            base, _, suf = ref.rpartition(sep)\n            base_card = get_model_card(base)\n            if base_card is not None:\n                return _from_card(base_card, base, variant or suf, ckpt_path)\n\n    if \"/\" in ref:\n        card = infer_card_from_hf(ref)\n        if card is not None:\n            return _from_card(card, ref, variant, ckpt_path)\n\n    if _registered_in_worlds(ref):\n        from worldkernels.worlds.hub import ModelCard\n\n        synthetic = ModelCard(adapter=ref, description=f\"registry-only adapter: {ref}\")\n        return _from_card(synthetic, ref, variant, ckpt_path)\n\n    raise ModelNotFoundError(\n        f\"could not resolve model '{ref}'. \"\n        f\"Try a registered alias (`worldkernels models`) or an HF repo id (org/name).\"\n    )\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve._registered_in_worlds","title":"_registered_in_worlds","text":"<pre><code>_registered_in_worlds(name: str) -&gt; bool\n</code></pre> Source code in <code>worldkernels/bootstrap/resolve.py</code> <pre><code>def _registered_in_worlds(name: str) -&gt; bool:\n    try:\n        from worldkernels.worlds.registry import _REGISTRY\n\n        return name in _REGISTRY\n    except Exception:\n        return False\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve._from_card","title":"_from_card","text":"<pre><code>_from_card(card: 'ModelCard', name: str, variant: str | None, ckpt_path: str | None) -&gt; ResolvedRef\n</code></pre> Source code in <code>worldkernels/bootstrap/resolve.py</code> <pre><code>def _from_card(\n    card: \"ModelCard\",\n    name: str,\n    variant: str | None,\n    ckpt_path: str | None,\n) -&gt; ResolvedRef:\n    if variant is None and card.default_kwargs.get(\"variant\"):\n        variant = card.default_kwargs[\"variant\"]\n    return ResolvedRef(card=card, name=name, variant=variant, ckpt_path=ckpt_path)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve._local_ref","title":"_local_ref","text":"<pre><code>_local_ref(ref: str, variant: str | None) -&gt; ResolvedRef\n</code></pre> Source code in <code>worldkernels/bootstrap/resolve.py</code> <pre><code>def _local_ref(ref: str, variant: str | None) -&gt; ResolvedRef:\n    from worldkernels.worlds.hub import ModelCard\n\n    path = Path(ref).expanduser().resolve()\n    suf = path.suffix.lower()\n    if suf in {\".pt\", \".bin\", \".safetensors\"} or (path.is_dir() and (path / \"config.py\").exists()):\n        adapter = \"dreamdojo\"\n    else:\n        adapter = \"dreamdojo\"\n    card = ModelCard(adapter=adapter, description=f\"local checkpoint: {path}\")\n    return ResolvedRef(card=card, name=path.name, variant=variant, ckpt_path=str(path))\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve._looks_like_path","title":"_looks_like_path","text":"<pre><code>_looks_like_path(ref: str) -&gt; bool\n</code></pre> Source code in <code>worldkernels/bootstrap/resolve.py</code> <pre><code>def _looks_like_path(ref: str) -&gt; bool:\n    if ref.startswith((\"./\", \"../\", \"/\", \"~\")):\n        return True\n    p = Path(ref).expanduser()\n    return p.exists()\n</code></pre>"},{"location":"api/worldkernels/bootstrap/resolve/#worldkernels.bootstrap.resolve._strip_hf_url","title":"_strip_hf_url","text":"<pre><code>_strip_hf_url(ref: str) -&gt; str | None\n</code></pre> Source code in <code>worldkernels/bootstrap/resolve.py</code> <pre><code>def _strip_hf_url(ref: str) -&gt; str | None:\n    prefixes = (\"https://huggingface.co/\", \"http://huggingface.co/\", \"hf://\")\n    for pre in prefixes:\n        if ref.startswith(pre):\n            tail = ref[len(pre) :].split(\"?\")[0].split(\"#\")[0].rstrip(\"/\")\n            return tail\n    return None\n</code></pre>"},{"location":"api/worldkernels/bootstrap/weights/","title":"weights","text":""},{"location":"api/worldkernels/bootstrap/weights/#worldkernels.bootstrap.weights","title":"worldkernels.bootstrap.weights","text":"<p>Checkpoint provisioning: HF snapshot_download or local path passthrough.</p>"},{"location":"api/worldkernels/bootstrap/weights/#worldkernels.bootstrap.weights.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/bootstrap/weights/#worldkernels.bootstrap.weights.provision_weights","title":"provision_weights","text":"<pre><code>provision_weights(card: 'ModelCard', variant: str | None = None, ckpt_path: str | None = None, progress: 'ProgressController | None' = None, allow_fetch: bool = True) -&gt; str | None\n</code></pre> Source code in <code>worldkernels/bootstrap/weights.py</code> <pre><code>def provision_weights(\n    card: \"ModelCard\",\n    variant: str | None = None,\n    ckpt_path: str | None = None,\n    progress: \"ProgressController | None\" = None,\n    allow_fetch: bool = True,\n) -&gt; str | None:\n    if ckpt_path is not None:\n        p = Path(ckpt_path).expanduser()\n        if not p.exists():\n            raise FileNotFoundError(f\"checkpoint path does not exist: {p}\")\n        if progress is not None:\n            progress.event(\"weights\", \"done\", f\"local: {p}\")\n        return str(p)\n\n    if card.hf_repo is None and card.weights_provider is None:\n        if progress is not None:\n            progress.event(\"weights\", \"skipped\", \"no hf repo declared\")\n        return None\n\n    source = card.hf_repo or card.weights_provider\n    if not allow_fetch or os.environ.get(\"WORLDKERNELS_NO_AUTO_INSTALL\"):\n        raise FetchDisabledError(\n            f\"missing weights for {source}\",\n            f\"run `worldkernels pull {card.hf_repo or ''}`\".strip() + \" or pass --ckpt-path\",\n        )\n\n    if progress is not None:\n        progress.event(\"weights\", \"running\", f\"{source} \u00b7 {variant or ''}\".strip(\" \u00b7\"))\n\n    from worldkernels.bootstrap.hf import enable_fast_hf_transfer\n\n    enable_fast_hf_transfer()\n\n    if card.weights_provider is not None:\n        local_path = _load_weights_provider(card.weights_provider)(variant)\n        if progress is not None:\n            progress.event(\"weights\", \"done\", str(source))\n        return local_path\n\n    assert card.hf_repo is not None\n\n    try:\n        from huggingface_hub import snapshot_download\n        from huggingface_hub.errors import GatedRepoError, RepositoryNotFoundError\n    except ImportError as exc:\n        raise RuntimeError(\"huggingface_hub is required\") from exc\n\n    allow_patterns = card.allow_patterns\n    if variant and card.variant_pattern:\n        allow_patterns = [p.format(variant=variant) for p in card.variant_pattern]\n\n    try:\n        local_dir = snapshot_download(\n            card.hf_repo,\n            allow_patterns=allow_patterns,\n            repo_type=\"model\",\n        )\n    except GatedRepoError as exc:\n        raise AuthRequiredError(card.hf_repo) from exc\n    except RepositoryNotFoundError as exc:\n        raise RuntimeError(f\"HF repo not found: {card.hf_repo}\") from exc\n\n    if progress is not None:\n        progress.event(\"weights\", \"done\", f\"{card.hf_repo}\")\n    return local_dir\n</code></pre>"},{"location":"api/worldkernels/bootstrap/weights/#worldkernels.bootstrap.weights._load_weights_provider","title":"_load_weights_provider","text":"<pre><code>_load_weights_provider(spec: str) -&gt; Callable[[str | None], str]\n</code></pre> Source code in <code>worldkernels/bootstrap/weights.py</code> <pre><code>def _load_weights_provider(spec: str) -&gt; Callable[[str | None], str]:\n    import importlib\n\n    module_path, _, func_name = spec.partition(\":\")\n    return getattr(importlib.import_module(module_path), func_name)\n</code></pre>"},{"location":"api/worldkernels/cli/","title":"cli","text":""},{"location":"api/worldkernels/cli/#worldkernels.cli","title":"worldkernels.cli","text":"<p>WorldKernels CLI package.</p> <p>Modules:</p> Name Description <code>__main__</code> <code>bench</code> <p>Back-compat shim: handlers moved to worldkernels.cli.commands.bench.</p> <code>collect_env</code> <p>Back-compat shim: handler moved to worldkernels.cli.commands.collect_env.</p> <code>commands</code> <p>CLI command handlers.</p> <code>config_args</code> <p>Dataclass-driven engine flag groups + explicit-override collection.</p> <code>config_cmd</code> <p>Back-compat shim: handler moved to worldkernels.cli.commands.config_cmd.</p> <code>main</code> <p>WorldKernels CLI: positional verbs on an owned argparse layer.</p> <code>model</code> <p>Back-compat shim: handlers moved to worldkernels.cli.commands.model.</p> <code>parser</code> <p>Owned argparse layer: help search, flag aliasing, dotted JSON args, human ints.</p> <code>plugins</code> <p>Back-compat shim: handler moved to worldkernels.cli.commands.plugins.</p> <code>pull</code> <p>Back-compat shim: handlers moved to worldkernels.cli.commands.pull.</p> <code>run</code> <p>Back-compat shim: handler moved to worldkernels.cli.commands.run.</p> <code>serve</code> <p>Back-compat shim: handler moved to worldkernels.cli.commands.serve.</p>"},{"location":"api/worldkernels/cli/bench/","title":"bench","text":""},{"location":"api/worldkernels/cli/bench/#worldkernels.cli.bench","title":"worldkernels.cli.bench","text":"<p>Back-compat shim: handlers moved to worldkernels.cli.commands.bench.</p>"},{"location":"api/worldkernels/cli/collect_env/","title":"collect_env","text":""},{"location":"api/worldkernels/cli/collect_env/#worldkernels.cli.collect_env","title":"worldkernels.cli.collect_env","text":"<p>Back-compat shim: handler moved to worldkernels.cli.commands.collect_env.</p>"},{"location":"api/worldkernels/cli/commands/","title":"commands","text":""},{"location":"api/worldkernels/cli/commands/#worldkernels.cli.commands","title":"worldkernels.cli.commands","text":"<p>CLI command handlers.</p> <p>Modules:</p> Name Description <code>bench</code> <p>Benchmark command implementations.</p> <code>collect_env</code> <p>Collect environment info: GPU, deps, plugins, hub, local cache, isolated envs.</p> <code>config_cmd</code> <p><code>worldkernels config-show</code> \u2014 print the resolved runtime config + source of each flag.</p> <code>model</code> <p>Model inspection command implementation.</p> <code>plugins</code> <p>List entry_points plugins discovered for worldkernels.</p> <code>pull</code> <p>Pull / models / rm command implementations.</p> <code>run</code> <p>Headless run: bootstrap model, step N times, optionally save frames/video.</p> <code>serve</code> <p>Serve command: bootstrap a model and start FastAPI.</p>"},{"location":"api/worldkernels/cli/commands/bench/","title":"bench","text":""},{"location":"api/worldkernels/cli/commands/bench/#worldkernels.cli.commands.bench","title":"worldkernels.cli.commands.bench","text":"<p>Benchmark command implementations.</p>"},{"location":"api/worldkernels/cli/commands/bench/#worldkernels.cli.commands.bench.bench_env","title":"bench_env","text":"<pre><code>bench_env(world: str, device: str, max_sessions: int = 1, height: int = 64, width: int = 64, num_sessions: int = 1, profile: str | None = None, overrides: dict | None = None, config_file: str | None = None) -&gt; Generator[tuple[WorldEngine, list[Session]], None, None]\n</code></pre> <p>Shared setup/teardown for benchmark commands.</p> Source code in <code>worldkernels/cli/commands/bench.py</code> <pre><code>@contextmanager\ndef bench_env(\n    world: str,\n    device: str,\n    max_sessions: int = 1,\n    height: int = 64,\n    width: int = 64,\n    num_sessions: int = 1,\n    profile: str | None = None,\n    overrides: dict | None = None,\n    config_file: str | None = None,\n) -&gt; Generator[tuple[WorldEngine, list[Session]], None, None]:\n    r\"\"\"Shared setup/teardown for benchmark commands.\"\"\"\n    from worldkernels.config import resolve_runtime_config\n\n    runtime_config, _ = resolve_runtime_config(\n        profile=profile, cli_overrides=overrides, config_file=config_file\n    )\n    wk = WorldEngine(runtime_config, device=device, max_sessions=max_sessions)\n    wk.load_model(world)\n    world_key = world.split(\"/\")[-1]\n    config = WorldConfig(height=height, width=width, frames_per_step=1)\n    sessions = [wk.create_session(world_key, config=config, seed=i) for i in range(num_sessions)]\n    try:\n        yield wk, sessions\n    finally:\n        for sess in sessions:\n            sess.close()\n        wk.shutdown()\n</code></pre>"},{"location":"api/worldkernels/cli/commands/bench/#worldkernels.cli.commands.bench.run_latency","title":"run_latency","text":"<pre><code>run_latency(world: str, steps: int, height: int, width: int, device: str, profile: str | None = None, overrides: dict | None = None, config_file: str | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/bench.py</code> <pre><code>def run_latency(\n    world: str,\n    steps: int,\n    height: int,\n    width: int,\n    device: str,\n    profile: str | None = None,\n    overrides: dict | None = None,\n    config_file: str | None = None,\n) -&gt; None:\n    with bench_env(\n        world,\n        device,\n        height=height,\n        width=width,\n        profile=profile,\n        overrides=overrides,\n        config_file=config_file,\n    ) as (_, sessions):\n        session = sessions[0]\n        latencies: list[float] = []\n        with ui.StepProgress(steps, f\"latency \u00b7 {world}\", ui.resolve_output_mode()) as sp:\n            for _ in range(steps):\n                t0 = time.perf_counter()\n                session.step(Action(\"null\", {}), modalities=[\"frames\"])\n                dt = (time.perf_counter() - t0) * 1000.0\n                latencies.append(dt)\n                sp.advance(f\"{dt:.2f}ms\")\n\n        series = list(latencies)\n        latencies.sort()\n        n = len(latencies)\n        ui.rule(f\"latency \u00b7 {world} \u00b7 {device}\")\n        t = ui.table(\"metric\", \"value\")\n        t.add_row(\"mean\", f\"{sum(latencies) / n:.2f} ms\")\n        t.add_row(\"p50\", f\"{latencies[n // 2]:.2f} ms\")\n        t.add_row(\"p99\", f\"{latencies[int(n * 0.99)]:.2f} ms\")\n        t.add_row(\"min\", f\"{latencies[0]:.2f} ms\")\n        t.add_row(\"max\", f\"{latencies[-1]:.2f} ms\")\n        ui.print_table(t)\n        ui.metric_spark(\"per-step\", series)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/bench/#worldkernels.cli.commands.bench.run_throughput","title":"run_throughput","text":"<pre><code>run_throughput(world: str, num_sessions: int, steps: int, height: int, width: int, device: str, profile: str | None = None, overrides: dict | None = None, config_file: str | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/bench.py</code> <pre><code>def run_throughput(\n    world: str,\n    num_sessions: int,\n    steps: int,\n    height: int,\n    width: int,\n    device: str,\n    profile: str | None = None,\n    overrides: dict | None = None,\n    config_file: str | None = None,\n) -&gt; None:\n    with bench_env(\n        world,\n        device,\n        max_sessions=num_sessions,\n        height=height,\n        width=width,\n        num_sessions=num_sessions,\n        profile=profile,\n        overrides=overrides,\n        config_file=config_file,\n    ) as (_, sessions):\n        deltas: list[float] = []\n        with ui.StepProgress(\n            steps, f\"throughput \u00b7 {world} \u00b7 {num_sessions}sess\", ui.resolve_output_mode()\n        ) as sp:\n            t0 = time.perf_counter()\n            last = t0\n            for _ in range(steps):\n                for sess in sessions:\n                    sess.step(Action(\"null\", {}), modalities=[\"frames\"])\n                now = time.perf_counter()\n                deltas.append((now - last) * 1000.0)\n                last = now\n                sp.advance()\n            elapsed = time.perf_counter() - t0\n\n        total_steps = steps * num_sessions\n        ui.rule(f\"throughput \u00b7 {world} \u00b7 {num_sessions} sessions \u00b7 {device}\")\n        ui.field(\"total steps\", total_steps)\n        ui.field(\"wall time\", f\"{elapsed:.2f} s\")\n        ui.field(\"throughput\", f\"{total_steps / elapsed:.1f} steps/s\")\n        ui.metric_spark(\"step time\", deltas)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/bench/#worldkernels.cli.commands.bench.run_vram","title":"run_vram","text":"<pre><code>run_vram(world: str, device: str = 'cuda', resolutions: str = '256x256,480x848,720x1280') -&gt; None\n</code></pre> <p>Profile VRAM across resolutions.</p> Source code in <code>worldkernels/cli/commands/bench.py</code> <pre><code>def run_vram(\n    world: str,\n    device: str = \"cuda\",\n    resolutions: str = \"256x256,480x848,720x1280\",\n) -&gt; None:\n    r\"\"\"Profile VRAM across resolutions.\"\"\"\n    import torch\n\n    from worldkernels.core.config import WorldConfig\n    from worldkernels.worlds.hub import resolve_model\n    from worldkernels.worlds.registry import get_world_class\n\n    adapter_name, merged_kwargs = resolve_model(world)\n    cls = get_world_class(adapter_name)\n    dtype = torch.bfloat16 if device != \"cpu\" else torch.float32\n    instance = cls(**merged_kwargs)\n    instance.initialize(device=device, dtype=dtype)\n\n    pairs = []\n    for res in resolutions.split(\",\"):\n        parts = res.strip().split(\"x\")\n        pairs.append((int(parts[0]), int(parts[1])))\n\n    ui.rule(f\"vram \u00b7 {world} \u00b7 {device}\")\n    t = ui.table(\"resolution\", \"VRAM (MB)\")\n    with ui.working(\"estimating\", verbs=[\"Estimating\", \"Profiling\"]):\n        for h, w in pairs:\n            cfg = WorldConfig(height=h, width=w)\n            vram = instance.profile_vram(cfg)\n            t.add_row(f\"{h}x{w}\", f\"{vram:.1f}\")\n    ui.print_table(t)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/bench/#worldkernels.cli.commands.bench.run_startup","title":"run_startup","text":"<pre><code>run_startup(world: str, device: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/bench.py</code> <pre><code>def run_startup(world: str, device: str) -&gt; None:\n    with ui.working(f\"loading {world}\", verbs=[\"Resolving\", \"Loading\", \"Warming\"]):\n        t0 = time.perf_counter()\n        wk = WorldEngine(device=device)\n        t_engine = time.perf_counter() - t0\n\n        t1 = time.perf_counter()\n        wk.load_model(world)\n        t_load = time.perf_counter() - t1\n\n    ui.rule(f\"startup \u00b7 {world} \u00b7 {device}\")\n    ui.field(\"engine init\", f\"{t_engine * 1000:.1f} ms\")\n    ui.field(\"load+warmup\", f\"{t_load * 1000:.1f} ms\")\n    ui.field(\"total\", f\"{(t_engine + t_load) * 1000:.1f} ms\")\n\n    wk.shutdown()\n</code></pre>"},{"location":"api/worldkernels/cli/commands/bench/#worldkernels.cli.commands.bench.run_profile","title":"run_profile","text":"<pre><code>run_profile(world: str, steps: int = 10, height: int = 64, width: int = 64, device: str = 'cuda', output: str = 'wk_profile') -&gt; None\n</code></pre> <p>Run steps under torch.profiler and emit a Chrome/Nsight trace.</p> Source code in <code>worldkernels/cli/commands/bench.py</code> <pre><code>def run_profile(\n    world: str,\n    steps: int = 10,\n    height: int = 64,\n    width: int = 64,\n    device: str = \"cuda\",\n    output: str = \"wk_profile\",\n) -&gt; None:\n    r\"\"\"Run steps under torch.profiler and emit a Chrome/Nsight trace.\"\"\"\n    from pathlib import Path\n\n    import torch\n\n    with bench_env(world, device, height=height, width=width) as (_, sessions):\n        session = sessions[0]\n\n        with torch.profiler.profile(\n            activities=[\n                torch.profiler.ProfilerActivity.CPU,\n                *(\n                    [torch.profiler.ProfilerActivity.CUDA]\n                    if device != \"cpu\" and torch.cuda.is_available()\n                    else []\n                ),\n            ],\n            record_shapes=True,\n            with_stack=True,\n        ) as prof:\n            for _ in range(steps):\n                session.step(Action(\"null\", {}), modalities=[\"frames\"])\n\n    trace_path = Path(f\"{output}.json\")\n    prof.export_chrome_trace(str(trace_path))\n    ui.ok(f\"Trace written to {trace_path}\")\n    ui.info(\"open in chrome://tracing or Perfetto UI\")\n\n    print()\n    print(prof.key_averages().table(sort_by=\"cpu_time_total\", row_limit=20))\n</code></pre>"},{"location":"api/worldkernels/cli/commands/collect_env/","title":"collect_env","text":""},{"location":"api/worldkernels/cli/commands/collect_env/#worldkernels.cli.commands.collect_env","title":"worldkernels.cli.commands.collect_env","text":"<p>Collect environment info: GPU, deps, plugins, hub, local cache, isolated envs.</p> <p>Prints what's useful when filing a bug report or sanity-checking a new install.</p>"},{"location":"api/worldkernels/cli/commands/collect_env/#worldkernels.cli.commands.collect_env._PY_MIN","title":"_PY_MIN  <code>module-attribute</code>","text":"<pre><code>_PY_MIN = (3, 10)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/collect_env/#worldkernels.cli.commands.collect_env._PY_MAX_EXCL","title":"_PY_MAX_EXCL  <code>module-attribute</code>","text":"<pre><code>_PY_MAX_EXCL = (3, 14)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/collect_env/#worldkernels.cli.commands.collect_env.run_collect_env","title":"run_collect_env","text":"<pre><code>run_collect_env() -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/collect_env.py</code> <pre><code>def run_collect_env() -&gt; None:\n    ui.rule(\"worldkernels collect-env\")\n\n    _check_python_version()\n\n    ui.rule(\"python &amp; libs\")\n    _check_import(\"torch\", required=True)\n    _check_import(\"huggingface_hub\", required=True)\n    _check_import(\"fastapi\", required=True)\n    _check_import(\"rich\", required=False)\n    _check_import(\"transformers\", required=False)\n    _check_import(\"diffusers\", required=False)\n    _check_import(\"flash_attn\", required=False)\n\n    ui.rule(\"platform\")\n    try:\n        from worldkernels.platforms import current_platform\n        from worldkernels.utils.device_info import detect_target_device\n\n        platform = current_platform()\n        ui.field(\"target device\", detect_target_device())\n        ui.field(\"platform\", platform.name)\n        ui.field(\"device count\", platform.device_count())\n        ui.field(\"attention backend\", platform.default_attention_backend())\n        ui.field(\"default dtype\", str(platform.default_dtype()))\n    except Exception as exc:\n        ui.err(f\"platform probe failed: {exc}\")\n\n    ui.rule(\"gpu\")\n    try:\n        import torch\n\n        if torch.cuda.is_available():\n            for i in range(torch.cuda.device_count()):\n                name = torch.cuda.get_device_name(i)\n                cap = torch.cuda.get_device_capability(i)\n                free, total = torch.cuda.mem_get_info(i)\n                gb_free = free / (1024**3)\n                gb_total = total / (1024**3)\n                ui.field(\n                    f\"cuda:{i}\",\n                    f\"{name}  sm_{cap[0]}{cap[1]}  {gb_free:.1f}/{gb_total:.1f} GB free\",\n                    value_style=\"green\",\n                )\n        else:\n            ui.info(\"no cuda available\")\n    except Exception as exc:\n        ui.err(f\"torch check failed: {exc}\")\n\n    ui.rule(\"tools\")\n    for tool in (\"git\", \"uv\"):\n        found = shutil.which(tool)\n        ui.field(\n            tool,\n            \"found\" if found else \"MISSING\",\n            value_style=\"green\" if found else \"red bold\",\n        )\n\n    ui.rule(\"hub \u00b7 registered models\")\n    try:\n        from worldkernels.worlds.hub import list_models\n\n        t = ui.table(\"model\", \"adapter\")\n        for name, card in sorted(list_models().items()):\n            t.add_row(name, card.adapter)\n        ui.print_table(t)\n    except Exception as exc:\n        ui.err(f\"hub load failed: {exc}\")\n\n    ui.rule(\"local cache\")\n    try:\n        from worldkernels.bootstrap import cache\n\n        manifests = cache.list_manifests()\n        ui.field(\"home\", cache.home())\n        ui.field(\"cached models\", len(manifests))\n        for m in manifests:\n            v = f\":{m.variant}\" if m.variant else \"\"\n            ui.line(f\"    {m.model_id}{v}\")\n    except Exception as exc:\n        ui.err(f\"cache scan failed: {exc}\")\n\n    ui.rule(\"environment variables \u00b7 WK_* registry\")\n    try:\n        import os\n\n        from worldkernels import envs as wk_env_registry\n\n        set_vars = wk_env_registry.set_wk_vars()\n        for name, value in sorted(set_vars.items()):\n            ui.field(name, value, value_style=\"green\")\n        for name in wk_env_registry.EXTERNAL_VARIABLES:\n            if name in os.environ:\n                shown = \"***\" if name == \"HF_TOKEN\" else os.environ[name]\n                ui.field(name, shown)\n        if not set_vars:\n            ui.info(\"(no WK_* variables set)\")\n    except Exception as exc:\n        ui.err(f\"env registry failed: {exc}\")\n\n    ui.rule(\"runtime config \u00b7 defaults, WK_* env applied\")\n    try:\n        from worldkernels.config.profiles import resolve_runtime_config\n        from worldkernels.config.runtime import ALL_TOGGLE_FIELDS\n\n        cfg, sources = resolve_runtime_config()\n        for f in ALL_TOGGLE_FIELDS:\n            src = sources.get(f, \"default\")\n            tag = \"\" if src == \"default\" else f\"  ({src})\"\n            ui.field(f, f\"{getattr(cfg, f)}{tag}\")\n    except Exception as exc:\n        ui.err(f\"runtime config load failed: {exc}\")\n\n    ui.rule(\"isolated envs\")\n    try:\n        from worldkernels.bootstrap import cache\n        from worldkernels.runtime import envs\n\n        envs_root = envs.envs_dir()\n        if envs_root.exists():\n            children = [p for p in envs_root.iterdir() if p.is_dir()]\n            ui.field(\"root\", envs_root)\n            ui.field(\"count\", len(children))\n            for env_dir in children:\n                size_mb = cache.directory_size_bytes(env_dir) / (1024 * 1024)\n                ui.line(f\"    {env_dir.name}  {size_mb:.0f} MB\")\n        else:\n            ui.info(\"(none)\")\n    except Exception as exc:\n        ui.err(f\"env scan failed: {exc}\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/collect_env/#worldkernels.cli.commands.collect_env._check_python_version","title":"_check_python_version","text":"<pre><code>_check_python_version() -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/collect_env.py</code> <pre><code>def _check_python_version() -&gt; None:\n    cur = sys.version_info[:2]\n    py = f\"{cur[0]}.{cur[1]}.{sys.version_info[2]}\"\n    supported_range = f\"&gt;={_PY_MIN[0]}.{_PY_MIN[1]},&lt;{_PY_MAX_EXCL[0]}.{_PY_MAX_EXCL[1]}\"\n    if cur &lt; _PY_MIN:\n        ui.err(\n            f\"Python {py}  too old; worldkernels supports {supported_range}. \"\n            \"Recreate the venv with `uv venv --python 3.12`.\"\n        )\n    elif cur &gt;= _PY_MAX_EXCL:\n        ui.err(\n            f\"Python {py}  ahead of supported range {supported_range}. \"\n            \"Many cosmos/diffusion deps lack cp\"\n            f\"{cur[0]}{cur[1]} wheels; expect source-build failures. \"\n            \"Recreate the venv with `uv venv --python 3.12`.\"\n        )\n    else:\n        ui.field(\"Python\", f\"{py}  (supported {supported_range})\", value_style=\"green\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/collect_env/#worldkernels.cli.commands.collect_env._check_import","title":"_check_import","text":"<pre><code>_check_import(name: str, required: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/collect_env.py</code> <pre><code>def _check_import(name: str, required: bool = False) -&gt; None:\n    try:\n        mod = importlib.import_module(name)\n        ver = getattr(mod, \"__version__\", \"?\")\n        ui.field(name, ver, value_style=\"green\")\n    except ImportError:\n        if required:\n            ui.field(name, \"MISSING\", value_style=\"red bold\")\n        else:\n            ui.field(name, \"(optional, not installed)\", value_style=\"dim\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/config_cmd/","title":"config_cmd","text":""},{"location":"api/worldkernels/cli/commands/config_cmd/#worldkernels.cli.commands.config_cmd","title":"worldkernels.cli.commands.config_cmd","text":"<p><code>worldkernels config-show</code> \u2014 print the resolved runtime config + source of each flag.</p>"},{"location":"api/worldkernels/cli/commands/config_cmd/#worldkernels.cli.commands.config_cmd._config_rows","title":"_config_rows","text":"<pre><code>_config_rows(cfg: 'RuntimeConfig') -&gt; tuple[list[tuple[str, Any]], list[tuple[str, Any]]]\n</code></pre> Source code in <code>worldkernels/cli/commands/config_cmd.py</code> <pre><code>def _config_rows(cfg: \"RuntimeConfig\") -&gt; tuple[list[tuple[str, Any]], list[tuple[str, Any]]]:\n    from dataclasses import fields\n\n    from worldkernels.config.profiles import NESTED_CONFIGS\n    from worldkernels.config.runtime import ALL_TOGGLE_FIELDS\n\n    flat = [(f, getattr(cfg, f)) for f in (\"device\", \"max_sessions\", *ALL_TOGGLE_FIELDS)]\n    nested = [\n        (f\"{group}.{f.name}\", getattr(getattr(cfg, group), f.name))\n        for group in NESTED_CONFIGS\n        for f in fields(getattr(cfg, group))\n    ]\n    return flat, nested\n</code></pre>"},{"location":"api/worldkernels/cli/commands/config_cmd/#worldkernels.cli.commands.config_cmd.print_runtime_config","title":"print_runtime_config","text":"<pre><code>print_runtime_config(cfg: 'RuntimeConfig', sources: dict[str, str], title: str = 'runtime config') -&gt; None\n</code></pre> <p>Render flat + nested config tables with per-field source attribution.</p> Source code in <code>worldkernels/cli/commands/config_cmd.py</code> <pre><code>def print_runtime_config(\n    cfg: \"RuntimeConfig\", sources: dict[str, str], title: str = \"runtime config\"\n) -&gt; None:\n    r\"\"\"Render flat + nested config tables with per-field source attribution.\"\"\"\n    from worldkernels import ui\n\n    flat, nested = _config_rows(cfg)\n\n    def add_rows(t: Any, rows: list[tuple[str, Any]]) -&gt; None:\n        for name, value in rows:\n            src = sources.get(name, \"default\")\n            t.add_row(name, str(value), \"\" if src == \"default\" else f\"[green]{src}[/green]\")\n\n    ui.rule(title)\n    t = ui.table(\"flag\", \"value\", \"source\")\n    add_rows(t, flat)\n    ui.print_table(t)\n\n    ui.rule(\"nested\")\n    t = ui.table(\"field\", \"value\", \"source\")\n    add_rows(t, nested)\n    ui.print_table(t)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/config_cmd/#worldkernels.cli.commands.config_cmd.run_config_show","title":"run_config_show","text":"<pre><code>run_config_show(profile: str | None = None, as_json: bool = False, config_file: str | None = None, overrides: dict | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/config_cmd.py</code> <pre><code>def run_config_show(\n    profile: str | None = None,\n    as_json: bool = False,\n    config_file: str | None = None,\n    overrides: dict | None = None,\n) -&gt; None:\n    from worldkernels.config.profiles import resolve_runtime_config\n\n    cfg, sources = resolve_runtime_config(\n        profile=profile, cli_overrides=overrides, config_file=config_file\n    )\n\n    if as_json:\n        import json\n\n        flat, nested = _config_rows(cfg)\n        data = {\n            name: {\"value\": value, \"source\": sources.get(name, \"default\")}\n            for name, value in (*flat, *nested)\n        }\n        print(json.dumps(data, indent=2))\n        return\n\n    print_runtime_config(cfg, sources, title=f\"runtime config \u00b7 {profile or 'default'}\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/model/","title":"model","text":""},{"location":"api/worldkernels/cli/commands/model/#worldkernels.cli.commands.model","title":"worldkernels.cli.commands.model","text":"<p>Model inspection command implementation.</p>"},{"location":"api/worldkernels/cli/commands/model/#worldkernels.cli.commands.model.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/model/#worldkernels.cli.commands.model.run_inspect","title":"run_inspect","text":"<pre><code>run_inspect(model_id: str, device: str = 'cpu', config_json: str | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/model.py</code> <pre><code>def run_inspect(model_id: str, device: str = \"cpu\", config_json: str | None = None) -&gt; None:\n    from worldkernels.core.config import WorldConfig\n    from worldkernels.worlds.hub import resolve_model\n    from worldkernels.worlds.registry import get_world_class\n\n    adapter_name, _kwargs = resolve_model(model_id)\n    cls = get_world_class(adapter_name)\n\n    ui.rule(f\"Model: {model_id}\")\n    ui.field(\"Class\", f\"{cls.__module__}.{cls.__qualname__}\")\n\n    mode = getattr(cls, \"transition_mode\", None)\n    if mode is not None:\n        ui.field(\"Transition mode\", mode.value if hasattr(mode, \"value\") else mode)\n    ui.field(\"Supports streaming\", getattr(cls, \"supports_streaming\", False))\n    ui.field(\"Supports KV cache\", getattr(cls, \"supports_kv_cache\", False))\n\n    stage_modes = getattr(cls, \"stage_exec_modes\", {})\n    if stage_modes:\n        ui.rule(\"Stage execution modes\")\n        t = ui.table(\"stage\", \"mode\")\n        for stage, exec_mode in stage_modes.items():\n            s = stage.value if hasattr(stage, \"value\") else str(stage)\n            m = exec_mode.value if hasattr(exec_mode, \"value\") else str(exec_mode)\n            t.add_row(s, m)\n        ui.print_table(t)\n\n    default_config = getattr(cls, \"default_config\", None)\n    if default_config is not None:\n        ui.rule(\"Default config:\")\n        for k, v in vars(default_config).items():\n            if not k.startswith(\"_\"):\n                ui.field(f\"  {k}\", v)\n\n    if config_json:\n        cfg = WorldConfig(**json.loads(config_json))\n    else:\n        cfg = default_config or WorldConfig()\n\n    try:\n        import torch\n\n        world = cls()\n        world.initialize(device=device, dtype=torch.float32 if device == \"cpu\" else torch.bfloat16)\n        vram = world.profile_vram(cfg)\n        ui.rule()\n        ui.ok(\n            f\"VRAM estimate: {vram:.1f} MB  (config: {cfg.height}x{cfg.width}, \"\n            f\"steps={cfg.num_inference_steps}, frames_per_step={cfg.frames_per_step})\"\n        )\n    except Exception as exc:\n        log.debug(\"Could not estimate VRAM: %s\", exc)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/plugins/","title":"plugins","text":""},{"location":"api/worldkernels/cli/commands/plugins/#worldkernels.cli.commands.plugins","title":"worldkernels.cli.commands.plugins","text":"<p>List entry_points plugins discovered for worldkernels.</p>"},{"location":"api/worldkernels/cli/commands/plugins/#worldkernels.cli.commands.plugins.run_list","title":"run_list","text":"<pre><code>run_list() -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/plugins.py</code> <pre><code>def run_list() -&gt; None:\n    from importlib.metadata import entry_points\n\n    from worldkernels import ui\n\n    eps = entry_points()\n    groups = (\"worldkernels.worlds\", \"worldkernels.pipelines\")\n\n    for group in groups:\n        items = list(eps.select(group=group))\n        ui.rule(group)\n        if not items:\n            ui.info(\"(none)\")\n            continue\n        t = ui.table(\"name\", \"value\")\n        for ep in items:\n            t.add_row(ep.name, ep.value)\n        ui.print_table(t)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/pull/","title":"pull","text":""},{"location":"api/worldkernels/cli/commands/pull/#worldkernels.cli.commands.pull","title":"worldkernels.cli.commands.pull","text":"<p>Pull / models / rm command implementations.</p>"},{"location":"api/worldkernels/cli/commands/pull/#worldkernels.cli.commands.pull.run_pull","title":"run_pull","text":"<pre><code>run_pull(model: str, variant: str | None = None, ckpt_path: str | None = None, quiet: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/pull.py</code> <pre><code>def run_pull(\n    model: str,\n    variant: str | None = None,\n    ckpt_path: str | None = None,\n    quiet: bool = False,\n) -&gt; None:\n    from worldkernels.bootstrap import ProgressController, prepare\n    from worldkernels.bootstrap.progress import cli_mode\n\n    if quiet:\n        os.environ[\"WORLDKERNELS_QUIET\"] = \"1\"\n\n    with ProgressController(mode=cli_mode()) as progress:\n        prepared = prepare(model, variant=variant, ckpt_path=ckpt_path, progress=progress)\n        progress.finalize(success=True, summary=f\"cached {prepared.alias}\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/pull/#worldkernels.cli.commands.pull.run_models","title":"run_models","text":"<pre><code>run_models(show_all: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/pull.py</code> <pre><code>def run_models(show_all: bool = False) -&gt; None:\n    from worldkernels.bootstrap import cache\n\n    if show_all:\n        from worldkernels.worlds.hub import list_models\n\n        hub = list_models()\n        ui.rule(\"hub models\")\n        t = ui.table(\"model\", \"description\", \"isolation\")\n        for name, card in sorted(hub.items()):\n            t.add_row(name, card.description or \"\", card.isolation)\n        ui.print_table(t)\n        return\n\n    manifests = cache.list_manifests()\n    if not manifests:\n        acc = ui.style(\"accent\")\n        ui.info(\"no models cached locally\")\n        ui.line(\n            f\"  pull one with [{acc}]worldkernels pull &lt;model&gt;[/] \"\n            f\"or list the hub with [{acc}]worldkernels models --all[/]\"\n        )\n        return\n\n    from worldkernels.worlds.hub import get_model_card\n\n    ui.rule(f\"local models \u00b7 {cache.home()}\")\n    t = ui.table(\"model\", \"adapter\", \"size\", \"isolation\")\n    for m in sorted(manifests, key=lambda x: x.model_id):\n        variant_str = f\":{m.variant}\" if m.variant else \"\"\n        size_str = \"\"\n        if m.hf_repo:\n            sz = cache.hf_cache_size_bytes(m.hf_repo)\n            if sz:\n                size_str = _human(sz)\n        tier = getattr(get_model_card(m.model_id), \"isolation\", \"\")\n        t.add_row(f\"{m.model_id}{variant_str}\", m.adapter, size_str, tier)\n    ui.print_table(t)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/pull/#worldkernels.cli.commands.pull.run_rm","title":"run_rm","text":"<pre><code>run_rm(model: str, variant: str | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/pull.py</code> <pre><code>def run_rm(model: str, variant: str | None = None) -&gt; None:\n    from huggingface_hub import scan_cache_dir\n\n    from worldkernels.bootstrap import cache\n    from worldkernels.runtime import envs\n    from worldkernels.worlds.hub import get_model_card\n\n    removed_manifest = cache.remove_manifest(model, variant)\n    removed_env = envs.remove_env(model)\n\n    card = get_model_card(model)\n    hf_repo = card.hf_repo if card and card.hf_repo else (model if \"/\" in model else None)\n\n    if hf_repo:\n        try:\n            cache_info = scan_cache_dir()\n            for repo in cache_info.repos:\n                if repo.repo_id == hf_repo:\n                    for rev in repo.revisions:\n                        cache_info.delete_revisions(rev.commit_hash).execute()\n                        ui.ok(f\"removed {hf_repo} ({rev.commit_hash[:12]})\")\n        except Exception as exc:\n            ui.err(f\"could not scan HF cache: {exc}\")\n\n    if removed_manifest:\n        ui.ok(f\"removed manifest: {model}{':' + variant if variant else ''}\")\n    if removed_env:\n        ui.ok(f\"removed isolated env: {model}\")\n    if not (removed_manifest or removed_env or hf_repo):\n        ui.info(f\"nothing to remove for '{model}'\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/pull/#worldkernels.cli.commands.pull._human","title":"_human","text":"<pre><code>_human(b: int) -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/commands/pull.py</code> <pre><code>def _human(b: int) -&gt; str:\n    size = float(b)\n    for unit in (\"B\", \"KB\", \"MB\", \"GB\", \"TB\"):\n        if size &lt; 1024:\n            return f\"{size:.1f}{unit}\"\n        size /= 1024\n    return f\"{size:.1f}PB\"\n</code></pre>"},{"location":"api/worldkernels/cli/commands/run/","title":"run","text":""},{"location":"api/worldkernels/cli/commands/run/#worldkernels.cli.commands.run","title":"worldkernels.cli.commands.run","text":"<p>Headless run: bootstrap model, step N times, optionally save frames/video.</p>"},{"location":"api/worldkernels/cli/commands/run/#worldkernels.cli.commands.run.run_session","title":"run_session","text":"<pre><code>run_session(model: str, steps: int = 10, action_type: str = 'null', height: int = 480, width: int = 848, device: str = 'cuda', seed: int = 0, output_dir: str | None = None, output_format: str = 'frames', fps: int = 24, video_codec: str = 'libx264', modalities: str = 'frames', decode: bool = True, prompt: str | None = None, image: str | None = None, actions: str | None = None, variant: str | None = None, ckpt_path: str | None = None, model_kwargs: dict[str, Any] | None = None, allow_fetch: bool = True, quiet: bool = False, profile: str | None = None, overrides: dict | None = None, config_file: str | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/run.py</code> <pre><code>def run_session(\n    model: str,\n    steps: int = 10,\n    action_type: str = \"null\",\n    height: int = 480,\n    width: int = 848,\n    device: str = \"cuda\",\n    seed: int = 0,\n    output_dir: str | None = None,\n    output_format: str = \"frames\",\n    fps: int = 24,\n    video_codec: str = \"libx264\",\n    modalities: str = \"frames\",\n    decode: bool = True,\n    prompt: str | None = None,\n    image: str | None = None,\n    actions: str | None = None,\n    variant: str | None = None,\n    ckpt_path: str | None = None,\n    model_kwargs: dict[str, Any] | None = None,\n    allow_fetch: bool = True,\n    quiet: bool = False,\n    profile: str | None = None,\n    overrides: dict | None = None,\n    config_file: str | None = None,\n) -&gt; None:\n    from worldkernels import Action, WorldConfig, WorldEngine\n    from worldkernels.bootstrap import ProgressController\n    from worldkernels.bootstrap.progress import cli_mode\n    from worldkernels.config import resolve_runtime_config\n\n    if quiet:\n        os.environ[\"WORLDKERNELS_QUIET\"] = \"1\"\n\n    valid_formats = (\"frames\", \"video\", \"both\")\n    if output_format not in valid_formats:\n        raise ValueError(f\"--output-format must be one of {valid_formats}, got '{output_format}'\")\n\n    runtime_config, _ = resolve_runtime_config(\n        profile=profile, cli_overrides=overrides, config_file=config_file\n    )\n    wk = WorldEngine(runtime_config, device=device)\n    with ProgressController(mode=cli_mode()) as progress:\n        wk.load_model(\n            model,\n            variant=variant,\n            ckpt_path=ckpt_path,\n            progress=progress,\n            allow_fetch=allow_fetch,\n            **(model_kwargs or {}),\n        )\n        progress.finalize(success=True, summary=f\"model={model}\")\n\n    world_key = next(iter(wk.list_worlds()))\n    mods = [m.strip() for m in modalities.split(\",\")]\n    cfg = WorldConfig(height=height, width=width, initial_prompt=prompt or \"\", initial_image=image)\n    session = wk.create_session(world_key, config=cfg, seed=seed)\n\n    out = Path(output_dir) if output_dir else None\n    if out:\n        out.mkdir(parents=True, exist_ok=True)\n\n    save_frames = output_format in (\"frames\", \"both\")\n    save_video = output_format in (\"video\", \"both\")\n\n    ui.rule(f\"run \u00b7 {world_key}\")\n    ui.line(f\"Running {steps} steps on '{world_key}' ({height}x{width}, device={device})\")\n\n    frame_counter = 0\n    collected_frames: list[Any] = []\n\n    from worldkernels.core.action import load_action_chunks\n\n    action_chunks = load_action_chunks(actions, steps) if actions else None\n    if action_chunks is not None:\n        ui.line(\n            f\"Driving with real action chunks from {actions} ({len(action_chunks)} of size \"\n            f\"{action_chunks[0].shape})\"\n        )\n\n    mode = ui.resolve_output_mode()\n    with ui.StepProgress(steps, f\"run \u00b7 {world_key}\", mode) as sp:\n        for i in range(steps):\n            if action_chunks is not None:\n                chunk = action_chunks[min(i, len(action_chunks) - 1)]\n                act = Action(\"continuous\", {\"joints\": chunk})\n            else:\n                act = Action(action_type, {})\n            obs = session.step(act, modalities=mods, decode=decode)\n\n            saved = 0\n            if out and obs.frames is not None:\n                if save_frames:\n                    saved = _save_frames(obs.frames, out, height, width, frame_counter)\n                    frame_counter += saved\n                if save_video:\n                    collected_frames.extend(_raw_to_arrays(obs.frames, height, width))\n\n            step_fps = 1000.0 / obs.generation_time_ms if obs.generation_time_ms else 0.0\n            sp.advance(f\"{obs.generation_time_ms:.1f}ms \u00b7 {step_fps:.0f}fps \u00b7 {saved}f\")\n            if mode == ui.OutputMode.PLAIN:\n                extra = f\"  -&gt; saved {saved} frames\" if saved else \"\"\n                ui.line(f\"  step {i:4d}  time={obs.generation_time_ms:.1f}ms{extra}\")\n\n    if out and save_video and collected_frames:\n        video_path = out / \"output.mp4\"\n        _save_video(collected_frames, video_path, fps, video_codec)\n        ui.ok(f\"Video saved to {video_path} ({len(collected_frames)} frames, {fps} fps)\")\n\n    session.close()\n    wk.shutdown()\n    ui.ok(f\"Done. {steps} steps, {frame_counter} frames saved to {out or '(no output dir)'}.\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/run/#worldkernels.cli.commands.run._save_frames","title":"_save_frames","text":"<pre><code>_save_frames(frames: Any, out_dir: Path, height: int, width: int, start_idx: int) -&gt; int\n</code></pre> Source code in <code>worldkernels/cli/commands/run.py</code> <pre><code>def _save_frames(frames: Any, out_dir: Path, height: int, width: int, start_idx: int) -&gt; int:\n    from PIL import Image\n\n    saved = 0\n    if isinstance(frames, list):\n        for j, raw in enumerate(frames):\n            path = out_dir / f\"frame_{start_idx + j:05d}.png\"\n            if isinstance(raw, (bytes, bytearray)):\n                img = Image.frombytes(\"RGB\", (width, height), raw)\n                img.save(path)\n                saved += 1\n    return saved\n</code></pre>"},{"location":"api/worldkernels/cli/commands/run/#worldkernels.cli.commands.run._raw_to_arrays","title":"_raw_to_arrays","text":"<pre><code>_raw_to_arrays(frames: Any, height: int, width: int) -&gt; list\n</code></pre> Source code in <code>worldkernels/cli/commands/run.py</code> <pre><code>def _raw_to_arrays(frames: Any, height: int, width: int) -&gt; list:\n    import numpy as np\n\n    arrays = []\n    if isinstance(frames, list):\n        for raw in frames:\n            if isinstance(raw, (bytes, bytearray)):\n                arr = np.frombuffer(raw, dtype=np.uint8).reshape(height, width, 3)\n                arrays.append(arr)\n    return arrays\n</code></pre>"},{"location":"api/worldkernels/cli/commands/run/#worldkernels.cli.commands.run._save_video","title":"_save_video","text":"<pre><code>_save_video(frames: list, path: Path, fps: int, codec: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/run.py</code> <pre><code>def _save_video(frames: list, path: Path, fps: int, codec: str) -&gt; None:\n    import imageio.v3 as iio\n    import numpy as np\n\n    stack = np.stack(frames)\n    iio.imwrite(str(path), stack, fps=fps, codec=codec)\n</code></pre>"},{"location":"api/worldkernels/cli/commands/serve/","title":"serve","text":""},{"location":"api/worldkernels/cli/commands/serve/#worldkernels.cli.commands.serve","title":"worldkernels.cli.commands.serve","text":"<p>Serve command: bootstrap a model and start FastAPI.</p>"},{"location":"api/worldkernels/cli/commands/serve/#worldkernels.cli.commands.serve.run_serve","title":"run_serve","text":"<pre><code>run_serve(host: str, port: int, max_sessions: int, api_key: str | None, device: str, model: str | None = None, variant: str | None = None, ckpt_path: str | None = None, model_kwargs: dict[str, Any] | None = None, allow_fetch: bool = True, quiet: bool = False, profile: str | None = None, overrides: dict | None = None, config_file: str | None = None, allowed_origins: list[str] | None = None, allowed_methods: list[str] | None = None, allowed_headers: list[str] | None = None, allow_credentials: bool = False, ssl_keyfile: str | None = None, ssl_certfile: str | None = None, uvicorn_log_level: str = 'info', disable_access_log: bool = False, root_path: str = '') -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/serve.py</code> <pre><code>def run_serve(\n    host: str,\n    port: int,\n    max_sessions: int,\n    api_key: str | None,\n    device: str,\n    model: str | None = None,\n    variant: str | None = None,\n    ckpt_path: str | None = None,\n    model_kwargs: dict[str, Any] | None = None,\n    allow_fetch: bool = True,\n    quiet: bool = False,\n    profile: str | None = None,\n    overrides: dict | None = None,\n    config_file: str | None = None,\n    allowed_origins: list[str] | None = None,\n    allowed_methods: list[str] | None = None,\n    allowed_headers: list[str] | None = None,\n    allow_credentials: bool = False,\n    ssl_keyfile: str | None = None,\n    ssl_certfile: str | None = None,\n    uvicorn_log_level: str = \"info\",\n    disable_access_log: bool = False,\n    root_path: str = \"\",\n) -&gt; None:\n    import uvicorn\n\n    from worldkernels import ui\n    from worldkernels.bootstrap import ProgressController\n    from worldkernels.bootstrap.progress import cli_mode\n    from worldkernels.config import resolve_runtime_config\n    from worldkernels.core.config import ServerConfig\n    from worldkernels.serving.server import create_app\n\n    if quiet:\n        os.environ[\"WORLDKERNELS_QUIET\"] = \"1\"\n\n    runtime_config, sources = resolve_runtime_config(\n        profile=profile, cli_overrides=overrides, config_file=config_file\n    )\n    cfg = ServerConfig(\n        host=host,\n        port=port,\n        max_sessions=max_sessions,\n        api_key=api_key,\n        allowed_origins=allowed_origins or [\"*\"],\n        allowed_methods=allowed_methods or [\"*\"],\n        allowed_headers=allowed_headers or [\"*\"],\n        allow_credentials=allow_credentials,\n        ssl_keyfile=ssl_keyfile,\n        ssl_certfile=ssl_certfile,\n        uvicorn_log_level=uvicorn_log_level,  # type: ignore[arg-type]\n        disable_access_log=disable_access_log,\n        root_path=root_path,\n    )\n    app = create_app(cfg, device=device, runtime_config=runtime_config)\n\n    if model is not None:\n        from worldkernels.engine import WorldEngine\n\n        engine: WorldEngine = app.state.engine\n        with ProgressController(mode=cli_mode()) as progress:\n            engine.load_model(\n                model,\n                variant=variant,\n                ckpt_path=ckpt_path,\n                progress=progress,\n                allow_fetch=allow_fetch,\n                **(model_kwargs or {}),\n            )\n            progress.finalize(success=True, summary=f\"model={model}\")\n\n    print_startup(app, cfg, runtime_config, sources, model=model, device=device)\n\n    uvicorn_kwargs: dict[str, Any] = {\n        \"log_level\": cfg.uvicorn_log_level,\n        \"access_log\": not cfg.disable_access_log,\n        \"ssl_keyfile\": cfg.ssl_keyfile,\n        \"ssl_certfile\": cfg.ssl_certfile,\n    }\n    try:\n        if ui.resolve_output_mode() == ui.OutputMode.AUTO and ui.capabilities().is_terminal:\n            _serve_with_dashboard(app, host, port, uvicorn_kwargs)\n        else:\n            uvicorn.run(app, host=host, port=port, **uvicorn_kwargs)\n    finally:\n        engine = app.state.engine\n        ui.info(f\"[shutdown] closing {len(engine.list_sessions())} session(s)\")\n        engine.shutdown()\n        ui.info(\"[shutdown] engine released\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/serve/#worldkernels.cli.commands.serve.base_url","title":"base_url","text":"<pre><code>base_url(cfg: Any) -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/commands/serve.py</code> <pre><code>def base_url(cfg: Any) -&gt; str:\n    scheme = \"https\" if cfg.ssl_certfile else \"http\"\n    return f\"{scheme}://{cfg.host}:{cfg.port}{cfg.root_path}\"\n</code></pre>"},{"location":"api/worldkernels/cli/commands/serve/#worldkernels.cli.commands.serve.enumerate_routes","title":"enumerate_routes","text":"<pre><code>enumerate_routes(app: Any) -&gt; list[tuple[str, list[str]]]\n</code></pre> <p><code>(path, methods)</code> per mounted route; websocket routes report <code>WEBSOCKET</code>.</p> <p>Descends into included routers, which newer FastAPI keeps as a nested object instead of flattening into <code>app.routes</code>.</p> Source code in <code>worldkernels/cli/commands/serve.py</code> <pre><code>def enumerate_routes(app: Any) -&gt; list[tuple[str, list[str]]]:\n    r\"\"\"``(path, methods)`` per mounted route; websocket routes report ``WEBSOCKET``.\n\n    Descends into included routers, which newer FastAPI keeps as a nested object\n    instead of flattening into ``app.routes``.\n    \"\"\"\n    out: list[tuple[str, list[str]]] = []\n    _collect_routes(getattr(app, \"routes\", []), out)\n    return out\n</code></pre>"},{"location":"api/worldkernels/cli/commands/serve/#worldkernels.cli.commands.serve._collect_routes","title":"_collect_routes","text":"<pre><code>_collect_routes(routes: Any, out: list[tuple[str, list[str]]]) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/serve.py</code> <pre><code>def _collect_routes(routes: Any, out: list[tuple[str, list[str]]]) -&gt; None:\n    for route in routes:\n        resolved = getattr(route, \"effective_route_contexts\", None)\n        if callable(resolved):\n            _collect_routes(list(resolved()), out)\n            continue\n        nested = getattr(route, \"routes\", None) or getattr(\n            getattr(route, \"original_router\", None), \"routes\", None\n        )\n        if nested:\n            _collect_routes(nested, out)\n            continue\n        path = getattr(route, \"path\", None)\n        methods = getattr(route, \"methods\", None)\n        if not path:\n            inner = getattr(route, \"original_route\", None) or getattr(\n                route, \"starlette_route\", None\n            )\n            path = getattr(inner, \"path\", None)\n            methods = methods or getattr(inner, \"methods\", None)\n        if not path:\n            continue\n        out.append((path, sorted(methods) if methods else [\"WEBSOCKET\"]))\n</code></pre>"},{"location":"api/worldkernels/cli/commands/serve/#worldkernels.cli.commands.serve.print_startup","title":"print_startup","text":"<pre><code>print_startup(app: Any, cfg: Any, runtime_config: Any, sources: dict[str, str], model: str | None, device: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/commands/serve.py</code> <pre><code>def print_startup(\n    app: Any,\n    cfg: Any,\n    runtime_config: Any,\n    sources: dict[str, str],\n    model: str | None,\n    device: str,\n) -&gt; None:\n    from worldkernels import __version__, ui\n    from worldkernels.cli.commands.config_cmd import print_runtime_config\n\n    url = base_url(cfg)\n    ui.rule(f\"worldkernels v{__version__} \u00b7 serving {model or '(no model preloaded)'}\")\n    ui.field(\"device\", device)\n    ui.field(\"endpoint\", url)\n    if cfg.api_key:\n        ui.field(\"auth\", \"bearer token required\")\n    print_runtime_config(runtime_config, sources)\n    ui.line(\"Available routes are:\")\n    for path, methods in enumerate_routes(app):\n        ui.line(f\"  Route: {path}, Methods: {', '.join(methods)}\")\n    ui.field(\"health\", f\"{url}/health\")\n    ui.field(\"metrics\", f\"{url}/metrics\")\n    ui.field(\"docs\", f\"{url}/docs\")\n</code></pre>"},{"location":"api/worldkernels/cli/commands/serve/#worldkernels.cli.commands.serve._serve_with_dashboard","title":"_serve_with_dashboard","text":"<pre><code>_serve_with_dashboard(app: Any, host: str, port: int, uvicorn_kwargs: dict[str, Any]) -&gt; None\n</code></pre> <p>Run uvicorn in a worker thread and a live metrics panel on the main thread.</p> Source code in <code>worldkernels/cli/commands/serve.py</code> <pre><code>def _serve_with_dashboard(app: Any, host: str, port: int, uvicorn_kwargs: dict[str, Any]) -&gt; None:\n    r\"\"\"Run uvicorn in a worker thread and a live metrics panel on the main thread.\"\"\"\n    import threading\n    import time\n\n    import uvicorn\n    from rich.live import Live\n\n    from worldkernels.runtime import metrics\n    from worldkernels.ui.console import console\n\n    engine = app.state.engine\n    server = uvicorn.Server(\n        uvicorn.Config(app, host=host, port=port, **{**uvicorn_kwargs, \"log_level\": \"warning\"})\n    )\n    thread = threading.Thread(target=server.run, daemon=True)\n    thread.start()\n\n    t0 = time.perf_counter()\n    sample = {\"t\": t0, \"steps\": metrics.get_steps_total(), \"rate\": 0.0}\n\n    def _vram() -&gt; str:\n        try:\n            import torch\n\n            if torch.cuda.is_available():\n                free, total = torch.cuda.mem_get_info()\n                metrics.set_vram_bytes(float(total - free))\n                return f\"{(total - free) / 1e9:.1f} / {total / 1e9:.0f} GB\"\n        except Exception:\n            pass\n        return \"n/a\"\n\n    def render() -&gt; Any:\n        from rich.panel import Panel\n        from rich.table import Table\n\n        from worldkernels.ui.theme import accent\n\n        now = time.perf_counter()\n        el = now - t0\n        steps = metrics.get_steps_total()\n        dt = now - sample[\"t\"]\n        if dt &gt; 0:\n            inst = (steps - sample[\"steps\"]) / dt\n            sample[\"rate\"] = inst if sample[\"rate\"] == 0.0 else 0.5 * sample[\"rate\"] + 0.5 * inst\n            sample[\"t\"], sample[\"steps\"] = now, steps\n        sids = engine.list_sessions()\n\n        grid = Table.grid(padding=(0, 3))\n        grid.add_row(\"endpoint\", f\"http://{host}:{port}\")\n        grid.add_row(\"uptime\", f\"{int(el) // 60:02d}:{int(el) % 60:02d}\")\n        grid.add_row(\"sessions\", str(len(sids)))\n        grid.add_row(\"steps\", f\"{int(steps)} total  \u00b7  {sample['rate']:.2f}/s\")\n        grid.add_row(\"vram\", _vram())\n        for sid in sids[:6]:\n            s = engine.get_session(sid)\n            if s is not None:\n                grid.add_row(f\"  {sid[:12]}\", f\"step {s.step_index} \u00b7 {s.status.value}\")\n        return Panel(grid, title=\"worldkernels \u00b7 serving\", border_style=accent(), expand=False)\n\n    try:\n        with Live(\n            render(), console=console(), screen=True, refresh_per_second=4, transient=False\n        ) as live:\n            while thread.is_alive():\n                live.update(render())\n                time.sleep(0.5)\n    except KeyboardInterrupt:\n        server.should_exit = True\n        thread.join(timeout=5)\n</code></pre>"},{"location":"api/worldkernels/cli/config_args/","title":"config_args","text":""},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args","title":"worldkernels.cli.config_args","text":"<p>Dataclass-driven engine flag groups + explicit-override collection.</p>"},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ENGINE_GROUPS', 'add_config_group', 'add_engine_args', 'collect_overrides']\n</code></pre>"},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args.ENGINE_GROUPS","title":"ENGINE_GROUPS  <code>module-attribute</code>","text":"<pre><code>ENGINE_GROUPS: tuple[tuple[str, type, str], ...] = (('RuntimeConfig', RuntimeConfig, ''), ('ParallelConfig', ParallelConfig, 'parallel'), ('CacheConfig', CacheConfig, 'cache'), ('SchedulerConfig', SchedulerConfig, 'scheduler'))\n</code></pre>"},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args._unwrap_optional","title":"_unwrap_optional","text":"<pre><code>_unwrap_optional(hint: Any) -&gt; Any\n</code></pre> Source code in <code>worldkernels/cli/config_args.py</code> <pre><code>def _unwrap_optional(hint: Any) -&gt; Any:\n    if typing.get_origin(hint) in (typing.Union, types.UnionType):\n        args = [a for a in typing.get_args(hint) if a is not type(None)]\n        if len(args) == 1:\n            return args[0]\n    return hint\n</code></pre>"},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args._iter_specs","title":"_iter_specs","text":"<pre><code>_iter_specs(cls: type, prefix: str) -&gt; Iterator[tuple[Field, str, Any, Any]]\n</code></pre> Source code in <code>worldkernels/cli/config_args.py</code> <pre><code>def _iter_specs(cls: type, prefix: str) -&gt; Iterator[tuple[Field, str, Any, Any]]:\n    hints = typing.get_type_hints(cls)\n    for f in fields(cls):\n        hint = _unwrap_optional(hints[f.name])\n        if f.name in CLI_OWNED_FIELDS or is_dataclass(hint):\n            continue\n        default = f.default if f.default_factory is MISSING else f.default_factory()\n        yield f, (f\"{prefix}.{f.name}\" if prefix else f.name), hint, default\n</code></pre>"},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args.add_config_group","title":"add_config_group","text":"<pre><code>add_config_group(parser: ArgumentParser, cls: type, title: str, prefix: str = '') -&gt; _ArgumentGroup\n</code></pre> <p>Add one argparse group per dataclass, one flag per field.</p> <p>Bools become <code>--flag/--no-flag</code>, <code>Literal</code> fields get <code>choices</code>, and every flag defaults to <code>argparse.SUPPRESS</code> so only explicitly-passed values appear in the namespace (under dotted dests for nested configs).</p> Source code in <code>worldkernels/cli/config_args.py</code> <pre><code>def add_config_group(\n    parser: argparse.ArgumentParser, cls: type, title: str, prefix: str = \"\"\n) -&gt; argparse._ArgumentGroup:\n    r\"\"\"Add one argparse group per dataclass, one flag per field.\n\n    Bools become ``--flag/--no-flag``, ``Literal`` fields get ``choices``, and\n    every flag defaults to ``argparse.SUPPRESS`` so only explicitly-passed\n    values appear in the namespace (under dotted dests for nested configs).\n    \"\"\"\n    group = parser.add_argument_group(title)\n    for f, dest, hint, default in _iter_specs(cls, prefix):\n        kwargs: dict[str, Any] = {\n            \"dest\": dest,\n            \"default\": argparse.SUPPRESS,\n            \"help\": f\"(default: {default})\",\n        }\n        if hint is bool:\n            kwargs[\"action\"] = argparse.BooleanOptionalAction\n        elif typing.get_origin(hint) is Literal:\n            kwargs[\"choices\"] = typing.get_args(hint)\n        else:\n            kwargs[\"metavar\"] = f.name.upper()\n            if hint in (int, float):\n                kwargs[\"type\"] = hint\n        group.add_argument(f\"--{f.name.replace('_', '-')}\", **kwargs)\n    return group\n</code></pre>"},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args.add_engine_args","title":"add_engine_args","text":"<pre><code>add_engine_args(parser: ArgumentParser) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/config_args.py</code> <pre><code>def add_engine_args(parser: argparse.ArgumentParser) -&gt; None:\n    for title, cls, prefix in ENGINE_GROUPS:\n        add_config_group(parser, cls, title, prefix=prefix)\n</code></pre>"},{"location":"api/worldkernels/cli/config_args/#worldkernels.cli.config_args.collect_overrides","title":"collect_overrides","text":"<pre><code>collect_overrides(args: Namespace) -&gt; dict[str, Any]\n</code></pre> <p>Collect explicitly-passed engine flags as <code>{field: value}</code> with dotted keys for nested configs, ready for <code>resolve_runtime_config</code>.</p> Source code in <code>worldkernels/cli/config_args.py</code> <pre><code>def collect_overrides(args: argparse.Namespace) -&gt; dict[str, Any]:\n    r\"\"\"Collect explicitly-passed engine flags as ``{field: value}`` with dotted\n    keys for nested configs, ready for ``resolve_runtime_config``.\"\"\"\n    out: dict[str, Any] = {}\n    for _, cls, prefix in ENGINE_GROUPS:\n        for _, dest, _, _ in _iter_specs(cls, prefix):\n            if hasattr(args, dest):\n                out[dest] = getattr(args, dest)\n    return out\n</code></pre>"},{"location":"api/worldkernels/cli/config_cmd/","title":"config_cmd","text":""},{"location":"api/worldkernels/cli/config_cmd/#worldkernels.cli.config_cmd","title":"worldkernels.cli.config_cmd","text":"<p>Back-compat shim: handler moved to worldkernels.cli.commands.config_cmd.</p>"},{"location":"api/worldkernels/cli/main/","title":"main","text":""},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main","title":"worldkernels.cli.main","text":"<p>WorldKernels CLI: positional verbs on an owned argparse layer.</p>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._DESCRIPTION","title":"_DESCRIPTION  <code>module-attribute</code>","text":"<pre><code>_DESCRIPTION = 'worldkernels: GPU-first world model simulation engine'\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._MODEL_HELP","title":"_MODEL_HELP  <code>module-attribute</code>","text":"<pre><code>_MODEL_HELP = 'Model: short alias, HF repo id, HF URL, or local checkpoint path.'\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._SET_HELP","title":"_SET_HELP  <code>module-attribute</code>","text":"<pre><code>_SET_HELP = 'Comma-separated runtime overrides (e.g. teacache=off,attention_backend=sdpa).'\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._BOOL_TRUE","title":"_BOOL_TRUE  <code>module-attribute</code>","text":"<pre><code>_BOOL_TRUE = {'1', 'true', 'yes', 'on'}\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._BOOL_FALSE","title":"_BOOL_FALSE  <code>module-attribute</code>","text":"<pre><code>_BOOL_FALSE = {'0', 'false', 'no', 'off'}\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._COMMAND_MODE","title":"_COMMAND_MODE  <code>module-attribute</code>","text":"<pre><code>_COMMAND_MODE: dict[str, str] = {'serve': 'serve', 'run': 'generate', 'bench': 'measure', 'pull': 'fetch', 'models': 'fetch', 'rm': 'danger', 'inspect': 'read', 'config-show': 'read', 'collect-env': 'read', 'plugins': 'read'}\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._NO_FRONTEND_INJECTION","title":"_NO_FRONTEND_INJECTION  <code>module-attribute</code>","text":"<pre><code>_NO_FRONTEND_INJECTION = frozenset({'config-show'})\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._parse_set","title":"_parse_set","text":"<pre><code>_parse_set(value: str | None) -&gt; dict | None\n</code></pre> <p>Parse a <code>--set k=v,k=v</code> string into a RuntimeConfig overrides dict.</p> <p>Bools accept <code>on</code>/<code>off</code>/<code>true</code>/<code>false</code>/<code>1</code>/<code>0</code>; enums pass through as strings. Validation happens in <code>resolve_runtime_config</code>.</p> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _parse_set(value: str | None) -&gt; dict | None:\n    r\"\"\"Parse a ``--set k=v,k=v`` string into a RuntimeConfig overrides dict.\n\n    Bools accept ``on``/``off``/``true``/``false``/``1``/``0``; enums pass through\n    as strings. Validation happens in ``resolve_runtime_config``.\n    \"\"\"\n    if not value:\n        return None\n    out: dict = {}\n    for pair in value.split(\",\"):\n        if not pair.strip():\n            continue\n        if \"=\" not in pair:\n            raise ValueError(f\"--set entry must be key=value, got {pair!r}\")\n        key, val = pair.split(\"=\", 1)\n        key, val = key.strip(), val.strip()\n        lv = val.lower()\n        if lv in _BOOL_TRUE:\n            out[key] = True\n        elif lv in _BOOL_FALSE:\n            out[key] = False\n        else:\n            out[key] = _coerce_number(val)\n    return out\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._coerce_number","title":"_coerce_number","text":"<pre><code>_coerce_number(value: str) -&gt; Any\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _coerce_number(value: str) -&gt; Any:\n    for cast in (int, float):\n        try:\n            return cast(value)\n        except ValueError:\n            continue\n    return value\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._extra_kwargs","title":"_extra_kwargs","text":"<pre><code>_extra_kwargs(num_inference_steps: int | None, guidance_scale: float | None, text_encoder: str | None = None) -&gt; dict\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _extra_kwargs(\n    num_inference_steps: int | None,\n    guidance_scale: float | None,\n    text_encoder: str | None = None,\n) -&gt; dict:\n    out: dict = {}\n    if num_inference_steps is not None:\n        out[\"num_inference_steps\"] = num_inference_steps\n    if guidance_scale is not None:\n        out[\"guidance_scale\"] = guidance_scale\n    if text_encoder is not None:\n        out[\"text_encoder\"] = text_encoder\n    return out\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._version","title":"_version","text":"<pre><code>_version() -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _version() -&gt; str:\n    from worldkernels import __version__\n\n    return f\"worldkernels {__version__}\"\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._global_parent","title":"_global_parent","text":"<pre><code>_global_parent() -&gt; FlexibleArgumentParser\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _global_parent() -&gt; FlexibleArgumentParser:\n    parent = FlexibleArgumentParser(add_help=False)\n    g = parent.add_argument_group(\"Global\")\n    g.add_argument(\n        \"-q\",\n        \"--quiet\",\n        action=\"store_true\",\n        default=argparse.SUPPRESS,\n        help=\"Suppress non-essential output.\",\n    )\n    g.add_argument(\n        \"-v\",\n        \"--verbose\",\n        action=\"count\",\n        default=argparse.SUPPRESS,\n        help=\"Increase verbosity (-vv for debug).\",\n    )\n    g.add_argument(\"--debug\", action=\"store_true\", default=argparse.SUPPRESS, help=\"Debug logging.\")\n    g.add_argument(\n        \"--output\",\n        choices=(\"auto\", \"plain\", \"json\"),\n        default=argparse.SUPPRESS,\n        help=\"Output rendering mode.\",\n    )\n    return parent\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._add_model_args","title":"_add_model_args","text":"<pre><code>_add_model_args(p: ArgumentParser) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _add_model_args(p: argparse.ArgumentParser) -&gt; None:\n    g = p.add_argument_group(\"Model\")\n    g.add_argument(\"--variant\", default=None, help=\"Model variant (e.g. 2b_gr1).\")\n    g.add_argument(\"--ckpt-path\", default=None, help=\"Local checkpoint path override.\")\n    g.add_argument(\n        \"--no-fetch\",\n        action=\"store_true\",\n        help=\"Fail instead of downloading missing deps/weights.\",\n    )\n    g.add_argument(\"--num-inference-steps\", type=int, default=None)\n    g.add_argument(\"--guidance-scale\", type=float, default=None)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._add_config_args","title":"_add_config_args","text":"<pre><code>_add_config_args(p: ArgumentParser) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _add_config_args(p: argparse.ArgumentParser) -&gt; None:\n    g = p.add_argument_group(\"Config\")\n    g.add_argument(\"--profile\", default=None, help=\"Named config profile (baseline, fast, ...).\")\n    g.add_argument(\n        \"--config\",\n        default=None,\n        metavar=\"YAML\",\n        help=\"YAML config file; env (WK_*) and explicit CLI flags win over its values.\",\n    )\n    g.add_argument(\"--set\", dest=\"set_overrides\", metavar=\"K=V[,K=V...]\", help=_SET_HELP)\n    add_engine_args(p)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._runtime_overrides","title":"_runtime_overrides","text":"<pre><code>_runtime_overrides(args: Namespace) -&gt; dict | None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _runtime_overrides(args: argparse.Namespace) -&gt; dict | None:\n    merged = {**(_parse_set(args.set_overrides) or {}), **collect_overrides(args)}\n    return merged or None\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._csv_list","title":"_csv_list","text":"<pre><code>_csv_list(value: str) -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _csv_list(value: str) -&gt; list[str]:\n    return [v.strip() for v in value.split(\",\") if v.strip()]\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_serve","title":"_dispatch_serve","text":"<pre><code>_dispatch_serve(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_serve(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.serve import run_serve\n\n    run_serve(\n        host=args.host,\n        port=args.port,\n        max_sessions=args.max_sessions,\n        api_key=args.api_key,\n        device=args.device,\n        model=args.model,\n        variant=args.variant,\n        ckpt_path=args.ckpt_path,\n        model_kwargs=_extra_kwargs(args.num_inference_steps, args.guidance_scale),\n        allow_fetch=not args.no_fetch,\n        quiet=args.quiet,\n        profile=args.profile,\n        overrides=_runtime_overrides(args),\n        config_file=args.config,\n        allowed_origins=_csv_list(args.allowed_origins),\n        allowed_methods=_csv_list(args.allowed_methods),\n        allowed_headers=_csv_list(args.allowed_headers),\n        allow_credentials=args.allow_credentials,\n        ssl_keyfile=args.ssl_keyfile,\n        ssl_certfile=args.ssl_certfile,\n        uvicorn_log_level=args.uvicorn_log_level,\n        disable_access_log=args.disable_access_log,\n        root_path=args.root_path,\n    )\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_run","title":"_dispatch_run","text":"<pre><code>_dispatch_run(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_run(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.run import run_session\n\n    run_session(\n        model=args.model,\n        steps=args.steps,\n        action_type=args.action_type,\n        height=args.height,\n        width=args.width,\n        device=args.device,\n        seed=args.seed,\n        output_dir=args.output_dir,\n        output_format=args.output_format,\n        fps=args.fps,\n        video_codec=args.video_codec,\n        modalities=args.modalities,\n        decode=args.decode,\n        prompt=args.prompt,\n        image=args.image,\n        actions=args.actions,\n        variant=args.variant,\n        ckpt_path=args.ckpt_path,\n        model_kwargs=_extra_kwargs(\n            args.num_inference_steps, args.guidance_scale, args.text_encoder\n        ),\n        allow_fetch=not args.no_fetch,\n        quiet=args.quiet,\n        profile=args.profile,\n        overrides=_runtime_overrides(args),\n        config_file=args.config,\n    )\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_pull","title":"_dispatch_pull","text":"<pre><code>_dispatch_pull(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_pull(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.pull import run_pull\n\n    run_pull(args.model, variant=args.variant, ckpt_path=args.ckpt_path, quiet=args.quiet)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_models","title":"_dispatch_models","text":"<pre><code>_dispatch_models(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_models(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.pull import run_models\n\n    run_models(show_all=args.all)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_rm","title":"_dispatch_rm","text":"<pre><code>_dispatch_rm(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_rm(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.pull import run_rm\n\n    run_rm(args.model, variant=args.variant)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_collect_env","title":"_dispatch_collect_env","text":"<pre><code>_dispatch_collect_env(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_collect_env(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.collect_env import run_collect_env\n\n    run_collect_env()\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_config_show","title":"_dispatch_config_show","text":"<pre><code>_dispatch_config_show(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_config_show(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.config_cmd import run_config_show\n\n    run_config_show(\n        args.profile,\n        args.json,\n        config_file=args.config,\n        overrides=_runtime_overrides(args),\n    )\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_inspect","title":"_dispatch_inspect","text":"<pre><code>_dispatch_inspect(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_inspect(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.model import run_inspect\n\n    run_inspect(args.model, args.device, args.config_json)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_plugins","title":"_dispatch_plugins","text":"<pre><code>_dispatch_plugins(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_plugins(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.plugins import run_list\n\n    run_list()\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_bench_latency","title":"_dispatch_bench_latency","text":"<pre><code>_dispatch_bench_latency(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_bench_latency(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.bench import run_latency\n\n    run_latency(\n        args.world,\n        args.steps,\n        args.height,\n        args.width,\n        args.device,\n        profile=args.profile,\n        overrides=_runtime_overrides(args),\n        config_file=args.config,\n    )\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_bench_throughput","title":"_dispatch_bench_throughput","text":"<pre><code>_dispatch_bench_throughput(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_bench_throughput(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.bench import run_throughput\n\n    run_throughput(\n        args.world,\n        args.sessions,\n        args.steps,\n        args.height,\n        args.width,\n        args.device,\n        profile=args.profile,\n        overrides=_runtime_overrides(args),\n        config_file=args.config,\n    )\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_bench_startup","title":"_dispatch_bench_startup","text":"<pre><code>_dispatch_bench_startup(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_bench_startup(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.bench import run_startup\n\n    run_startup(args.world, args.device)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_bench_vram","title":"_dispatch_bench_vram","text":"<pre><code>_dispatch_bench_vram(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_bench_vram(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.bench import run_vram\n\n    run_vram(args.world, args.device, args.resolutions)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._dispatch_bench_profile","title":"_dispatch_bench_profile","text":"<pre><code>_dispatch_bench_profile(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _dispatch_bench_profile(args: argparse.Namespace) -&gt; None:\n    from worldkernels.cli.commands.bench import run_profile\n\n    run_profile(args.world, args.steps, args.height, args.width, args.device, args.trace)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._add_bench_common","title":"_add_bench_common","text":"<pre><code>_add_bench_common(p: ArgumentParser, device: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _add_bench_common(p: argparse.ArgumentParser, device: str) -&gt; None:\n    p.add_argument(\"--world\", default=\"dummy\", help=\"World model to benchmark.\")\n    p.add_argument(\"--device\", default=device)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._add_bench_shape","title":"_add_bench_shape","text":"<pre><code>_add_bench_shape(p: ArgumentParser, steps: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _add_bench_shape(p: argparse.ArgumentParser, steps: int) -&gt; None:\n    p.add_argument(\"--steps\", type=int, default=steps)\n    p.add_argument(\"--height\", type=int, default=64)\n    p.add_argument(\"--width\", type=int, default=64)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main.build_parser","title":"build_parser","text":"<pre><code>build_parser() -&gt; FlexibleArgumentParser\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def build_parser() -&gt; FlexibleArgumentParser:\n    parent = _global_parent()\n    parser = FlexibleArgumentParser(\n        prog=\"worldkernels\",\n        description=_DESCRIPTION,\n        parents=[parent],\n        epilog=WK_SUBCMD_EPILOG,\n    )\n    parser.set_defaults(dispatch=None)\n    parser.add_argument(\"-V\", \"--version\", action=\"version\", version=_version())\n    sub = parser.add_subparsers(dest=\"command\", metavar=\"&lt;command&gt;\")\n\n    def add_verb(name: str, help_text: str, dispatch: Any) -&gt; FlexibleArgumentParser:\n        p = sub.add_parser(\n            name,\n            help=help_text,\n            description=help_text,\n            parents=[parent],\n            epilog=WK_SUBCMD_EPILOG,\n        )\n        p.set_defaults(dispatch=dispatch)\n        return p\n\n    serve = add_verb(\"serve\", \"Start the WorldKernels HTTP/WebSocket server.\", _dispatch_serve)\n    serve.add_argument(\"model\", nargs=\"?\", default=None, help=f\"Model to pre-load. {_MODEL_HELP}\")\n    frontend = serve.add_argument_group(\"Frontend\")\n    frontend.add_argument(\"--host\", default=\"0.0.0.0\", help=\"Bind address.\")\n    frontend.add_argument(\"--port\", type=int, default=8000, help=\"Bind port.\")\n    frontend.add_argument(\"--max-sessions\", type=int, default=4, help=\"Concurrent session cap.\")\n    frontend.add_argument(\"-k\", \"--api-key\", default=None, help=\"Require this bearer token.\")\n    frontend.add_argument(\"--allowed-origins\", default=\"*\", help=\"Comma-separated CORS origins.\")\n    frontend.add_argument(\"--allowed-methods\", default=\"*\", help=\"Comma-separated CORS methods.\")\n    frontend.add_argument(\"--allowed-headers\", default=\"*\", help=\"Comma-separated CORS headers.\")\n    frontend.add_argument(\n        \"--allow-credentials\", action=\"store_true\", help=\"Allow CORS credentials.\"\n    )\n    frontend.add_argument(\"--ssl-keyfile\", default=None, help=\"TLS key file; enables https.\")\n    frontend.add_argument(\"--ssl-certfile\", default=None, help=\"TLS certificate file.\")\n    frontend.add_argument(\n        \"--uvicorn-log-level\",\n        choices=(\"debug\", \"info\", \"warning\", \"error\", \"critical\", \"trace\"),\n        default=\"info\",\n        help=\"uvicorn log level.\",\n    )\n    frontend.add_argument(\n        \"--disable-access-log\", action=\"store_true\", help=\"Disable uvicorn access log.\"\n    )\n    frontend.add_argument(\"--root-path\", default=\"\", help=\"ASGI root_path when behind a proxy.\")\n    serve.add_argument(\"--device\", default=\"cuda\")\n    _add_model_args(serve)\n    _add_config_args(serve)\n\n    run = add_verb(\n        \"run\",\n        \"Headless run: bootstrap a model, step N times, optionally save frames/video.\",\n        _dispatch_run,\n    )\n    run.add_argument(\"model\", nargs=\"?\", default=\"dummy\", help=_MODEL_HELP)\n    run.add_argument(\"--steps\", type=int, default=10, help=\"Number of world steps.\")\n    run.add_argument(\"--action-type\", default=\"null\")\n    run.add_argument(\"--height\", type=int, default=480)\n    run.add_argument(\"--width\", type=int, default=848)\n    run.add_argument(\"--device\", default=\"cuda\")\n    run.add_argument(\"--seed\", type=int, default=0)\n    run.add_argument(\"-o\", \"--output-dir\", default=None, help=\"Directory for saved outputs.\")\n    run.add_argument(\"--output-format\", choices=(\"frames\", \"video\", \"both\"), default=\"frames\")\n    run.add_argument(\"--fps\", type=int, default=24)\n    run.add_argument(\"--video-codec\", default=\"libx264\")\n    run.add_argument(\"--modalities\", default=\"frames\", help=\"Comma-separated modalities.\")\n    run.add_argument(\"--decode\", action=argparse.BooleanOptionalAction, default=True)\n    run.add_argument(\"--prompt\", default=None, help=\"Initial text prompt.\")\n    run.add_argument(\"-i\", \"--image\", default=None, help=\"Initial image path.\")\n    run.add_argument(\n        \"--actions\",\n        default=None,\n        help=\"Path to a .npy of GR-1 action chunks ([T,384] or [N,12,384]) to drive an \"\n        \"action-conditioned rollout (see examples/gr1_prepare.py). Default: null actions.\",\n    )\n    run.add_argument(\n        \"--text-encoder\",\n        choices=(\"auto\", \"off\", \"on\"),\n        default=None,\n        help=\"'on' loads the reason1-7B encoder (CPU-offloaded) for real prompt conditioning; \"\n        \"default off uses neutral text and relies on image + action conditioning.\",\n    )\n    _add_model_args(run)\n    _add_config_args(run)\n\n    pull = add_verb(\n        \"pull\",\n        \"Pre-fetch a model: install deps, clone packages, download weights. No server.\",\n        _dispatch_pull,\n    )\n    pull.add_argument(\"model\", nargs=\"?\", default=\"dummy\", help=_MODEL_HELP)\n    pull.add_argument(\"--variant\", default=None, help=\"Model variant (e.g. 2b_gr1).\")\n    pull.add_argument(\"--ckpt-path\", default=None, help=\"Local checkpoint path override.\")\n\n    models = add_verb(\n        \"models\",\n        \"List models: locally cached by default, or the full hub with --all.\",\n        _dispatch_models,\n    )\n    models.add_argument(\"--all\", action=\"store_true\", help=\"Show the full hub catalog.\")\n\n    rm = add_verb(\"rm\", \"Remove a model from the local cache (weights + manifest).\", _dispatch_rm)\n    rm.add_argument(\"model\", help=_MODEL_HELP)\n    rm.add_argument(\"--variant\", default=None, help=\"Only remove this variant.\")\n\n    add_verb(\n        \"collect-env\",\n        \"Collect environment info: GPU, deps, plugins, hub, local cache, isolated envs.\",\n        _dispatch_collect_env,\n    )\n\n    config_show = add_verb(\n        \"config-show\",\n        \"Show the resolved runtime config (component toggles) and each flag's source.\",\n        _dispatch_config_show,\n    )\n    config_show.add_argument(\"--json\", action=\"store_true\", help=\"Emit JSON.\")\n    _add_config_args(config_show)\n\n    inspect = add_verb(\n        \"inspect\",\n        \"Show model metadata: transition mode, VRAM estimate, capabilities.\",\n        _dispatch_inspect,\n    )\n    inspect.add_argument(\"model\", nargs=\"?\", default=\"dummy\", help=_MODEL_HELP)\n    inspect.add_argument(\"--device\", default=\"cpu\")\n    inspect.add_argument(\"--config-json\", default=None, help=\"WorldConfig overrides as JSON.\")\n\n    bench = add_verb(\n        \"bench\", \"Benchmarks: latency, throughput, startup, vram, profile.\", lambda a: None\n    )\n    bsub = bench.add_subparsers(dest=\"bench_command\", metavar=\"&lt;benchmark&gt;\", required=True)\n\n    def add_bench(name: str, help_text: str, dispatch: Any) -&gt; FlexibleArgumentParser:\n        p = bsub.add_parser(\n            name,\n            help=help_text,\n            description=help_text,\n            parents=[parent],\n            epilog=WK_SUBCMD_EPILOG,\n        )\n        p.set_defaults(dispatch=dispatch)\n        return p\n\n    latency = add_bench(\"latency\", \"Measure single-session step latency.\", _dispatch_bench_latency)\n    _add_bench_common(latency, \"cpu\")\n    _add_bench_shape(latency, 100)\n    _add_config_args(latency)\n\n    throughput = add_bench(\n        \"throughput\", \"Measure multi-session concurrent throughput.\", _dispatch_bench_throughput\n    )\n    _add_bench_common(throughput, \"cpu\")\n    _add_bench_shape(throughput, 50)\n    throughput.add_argument(\"--sessions\", type=int, default=4)\n    _add_config_args(throughput)\n\n    startup = add_bench(\"startup\", \"Measure model load + warmup time.\", _dispatch_bench_startup)\n    _add_bench_common(startup, \"cpu\")\n\n    vram = add_bench(\"vram\", \"Profile VRAM estimates across resolutions.\", _dispatch_bench_vram)\n    _add_bench_common(vram, \"cuda\")\n    vram.add_argument(\"--resolutions\", default=\"256x256,480x848,720x1280\")\n\n    profile = add_bench(\n        \"profile\", \"Run steps under torch.profiler and emit a trace file.\", _dispatch_bench_profile\n    )\n    _add_bench_common(profile, \"cuda\")\n    _add_bench_shape(profile, 10)\n    profile.add_argument(\"--trace\", default=\"wk_profile\", help=\"Chrome trace output basename.\")\n\n    add_verb(\"plugins\", \"List discovered entry_point plugins.\", _dispatch_plugins)\n\n    return parser\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._sub_choices","title":"_sub_choices","text":"<pre><code>_sub_choices(parser: ArgumentParser) -&gt; dict[str, ArgumentParser]\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _sub_choices(parser: argparse.ArgumentParser) -&gt; dict[str, argparse.ArgumentParser]:\n    for action in parser._actions:\n        if isinstance(action, argparse._SubParsersAction):\n            return dict(action.choices)\n    return {}\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._frontend_tokens","title":"_frontend_tokens","text":"<pre><code>_frontend_tokens(frontend: dict[str, Any]) -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _frontend_tokens(frontend: dict[str, Any]) -&gt; list[str]:\n    out: list[str] = []\n    for key, value in frontend.items():\n        name = str(key).replace(\"_\", \"-\").lstrip(\"-\")\n        if isinstance(value, bool):\n            out.append(f\"--{name}\" if value else f\"--no-{name}\")\n        elif isinstance(value, (list, dict)):\n            raise ValueError(f\"config file key {key!r}: nested values are engine-config only\")\n        else:\n            out.extend((f\"--{name}\", str(value)))\n    return out\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._expand_config_arg","title":"_expand_config_arg","text":"<pre><code>_expand_config_arg(parser: ArgumentParser, argv: list[str]) -&gt; list[str]\n</code></pre> <p>Insert a <code>--config</code> file's frontend keys as argv tokens.</p> <p>Tokens land right after the (sub)command so explicit CLI flags always win; engine keys stay in the file and reach <code>resolve_runtime_config</code> as the config-file layer via the <code>--config</code> flag itself. <code>config-show</code> only resolves the engine layer, so frontend keys are not injected there.</p> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _expand_config_arg(parser: argparse.ArgumentParser, argv: list[str]) -&gt; list[str]:\n    r\"\"\"Insert a ``--config`` file's frontend keys as argv tokens.\n\n    Tokens land right after the (sub)command so explicit CLI flags always win;\n    engine keys stay in the file and reach ``resolve_runtime_config`` as the\n    config-file layer via the ``--config`` flag itself. ``config-show`` only\n    resolves the engine layer, so frontend keys are not injected there.\n    \"\"\"\n    path: str | None = None\n    for i, tok in enumerate(argv):\n        if tok == \"--config\" and i + 1 &lt; len(argv):\n            path = argv[i + 1]\n            break\n        if tok.startswith(\"--config=\"):\n            path = tok.split(\"=\", 1)[1]\n            break\n    if path is None:\n        return argv\n\n    from worldkernels.config.profiles import split_config_file\n\n    frontend, _ = split_config_file(path)\n    if not frontend:\n        return argv\n    insert_at = 0\n    choices = _sub_choices(parser)\n    for i, tok in enumerate(argv):\n        if tok in choices:\n            if tok in _NO_FRONTEND_INJECTION:\n                return argv\n            insert_at = i + 1\n            choices = _sub_choices(choices[tok])\n            if not choices:\n                break\n    return argv[:insert_at] + _frontend_tokens(frontend) + argv[insert_at:]\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main._configure_ui","title":"_configure_ui","text":"<pre><code>_configure_ui(args: Namespace) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def _configure_ui(args: argparse.Namespace) -&gt; None:\n    from worldkernels.ui import configure_logging, set_mode\n    from worldkernels.ui.verbosity import OutputMode, Verbosity, set_output_mode, set_verbosity\n\n    for name, default in ((\"quiet\", False), (\"verbose\", 0), (\"debug\", False), (\"output\", \"auto\")):\n        if not hasattr(args, name):\n            setattr(args, name, default)\n    if args.quiet:\n        verbosity = Verbosity.QUIET\n    elif args.debug or (args.verbose or 0) &gt;= 2:\n        verbosity = Verbosity.DEBUG\n    elif args.verbose == 1:\n        verbosity = Verbosity.VERBOSE\n    else:\n        verbosity = Verbosity.NORMAL\n    set_verbosity(verbosity)\n    set_output_mode(OutputMode(args.output))\n    set_mode(_COMMAND_MODE.get(getattr(args, \"command\", None) or \"\", \"serve\"))\n    configure_logging(verbosity)\n</code></pre>"},{"location":"api/worldkernels/cli/main/#worldkernels.cli.main.app","title":"app","text":"<pre><code>app(argv: list[str] | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/main.py</code> <pre><code>def app(argv: list[str] | None = None) -&gt; None:\n    parser = build_parser()\n    argv = list(sys.argv[1:] if argv is None else argv)\n    args = parser.parse_args(_expand_config_arg(parser, argv))\n    _configure_ui(args)\n    if args.dispatch is None:\n        from worldkernels import ui\n\n        ui.banner()\n        parser.print_help()\n        return\n    args.dispatch(args)\n</code></pre>"},{"location":"api/worldkernels/cli/model/","title":"model","text":""},{"location":"api/worldkernels/cli/model/#worldkernels.cli.model","title":"worldkernels.cli.model","text":"<p>Back-compat shim: handlers moved to worldkernels.cli.commands.model.</p>"},{"location":"api/worldkernels/cli/parser/","title":"parser","text":""},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser","title":"worldkernels.cli.parser","text":"<p>Owned argparse layer: help search, flag aliasing, dotted JSON args, human ints.</p>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['FlexibleArgumentParser', 'SortedHelpFormatter', 'human_readable_int', 'WK_SUBCMD_EPILOG']\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.WK_SUBCMD_EPILOG","title":"WK_SUBCMD_EPILOG  <code>module-attribute</code>","text":"<pre><code>WK_SUBCMD_EPILOG = 'Use `--help=all` to show every option, `--help=&lt;group&gt;` for one config group (e.g. `--help=CacheConfig`), or `--help=&lt;keyword&gt;` to search options by name.'\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser._DECIMAL_SUFFIXES","title":"_DECIMAL_SUFFIXES  <code>module-attribute</code>","text":"<pre><code>_DECIMAL_SUFFIXES = {'k': 10 ** 3, 'm': 10 ** 6, 'g': 10 ** 9, 't': 10 ** 12}\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser._BINARY_SUFFIXES","title":"_BINARY_SUFFIXES  <code>module-attribute</code>","text":"<pre><code>_BINARY_SUFFIXES = {'K': 2 ** 10, 'M': 2 ** 20, 'G': 2 ** 30, 'T': 2 ** 40}\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser._HUMAN_INT_RE","title":"_HUMAN_INT_RE  <code>module-attribute</code>","text":"<pre><code>_HUMAN_INT_RE = re.compile('(\\\\d+(?:\\\\.\\\\d+)?)([kmgtKMGT]?)')\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.SortedHelpFormatter","title":"SortedHelpFormatter","text":"<p>               Bases: <code>ArgumentDefaultsHelpFormatter</code></p> <p>Sorts options alphabetically and always shows defaults.</p> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>class SortedHelpFormatter(argparse.ArgumentDefaultsHelpFormatter):\n    r\"\"\"Sorts options alphabetically and always shows defaults.\"\"\"\n\n    def add_arguments(self, actions: Iterable[argparse.Action]) -&gt; None:\n        actions = list(actions)\n        for action in actions:\n            if (\n                not action.help\n                and action.option_strings\n                and action.default not in (None, argparse.SUPPRESS)\n            ):\n                action.help = \"(default: %(default)s)\"\n        super().add_arguments(sorted(actions, key=lambda a: tuple(a.option_strings) or (\"\",)))\n\n    def _get_help_string(self, action: argparse.Action) -&gt; str:\n        text = action.help or \"\"\n        if (\n            \"%(default)\" not in text\n            and action.default is not argparse.SUPPRESS\n            and action.default is not None\n            and action.option_strings\n        ):\n            text = f\"{text} (default: %(default)s)\".strip()\n        return text\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.SortedHelpFormatter.add_arguments","title":"add_arguments","text":"<pre><code>add_arguments(actions: Iterable[Action]) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def add_arguments(self, actions: Iterable[argparse.Action]) -&gt; None:\n    actions = list(actions)\n    for action in actions:\n        if (\n            not action.help\n            and action.option_strings\n            and action.default not in (None, argparse.SUPPRESS)\n        ):\n            action.help = \"(default: %(default)s)\"\n    super().add_arguments(sorted(actions, key=lambda a: tuple(a.option_strings) or (\"\",)))\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.SortedHelpFormatter._get_help_string","title":"_get_help_string","text":"<pre><code>_get_help_string(action: Action) -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _get_help_string(self, action: argparse.Action) -&gt; str:\n    text = action.help or \"\"\n    if (\n        \"%(default)\" not in text\n        and action.default is not argparse.SUPPRESS\n        and action.default is not None\n        and action.option_strings\n    ):\n        text = f\"{text} (default: %(default)s)\".strip()\n    return text\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser","title":"FlexibleArgumentParser","text":"<p>               Bases: <code>ArgumentParser</code></p> <p>ArgumentParser with <code>--help=&lt;group|keyword|all&gt;</code>, <code>--under_score</code> aliasing, dotted JSON args (<code>--x.y v</code>, <code>--x.y+ v</code>), and one-time deprecation warnings.</p> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>class FlexibleArgumentParser(argparse.ArgumentParser):\n    r\"\"\"ArgumentParser with ``--help=&lt;group|keyword|all&gt;``, ``--under_score`` aliasing,\n    dotted JSON args (``--x.y v``, ``--x.y+ v``), and one-time deprecation warnings.\n    \"\"\"\n\n    _warned_deprecated: set[str] = set()\n\n    def __init__(self, *args: Any, **kwargs: Any) -&gt; None:\n        kwargs.setdefault(\"formatter_class\", SortedHelpFormatter)\n        super().__init__(*args, **kwargs)\n        self._search_keyword: str | None = None\n        self._deprecated_options: set[str] = set()\n\n    def add_argument(self, *args: Any, **kwargs: Any) -&gt; argparse.Action:\n        deprecated = kwargs.pop(\"deprecated\", False)\n        action = super().add_argument(*args, **kwargs)\n        if deprecated:\n            self._deprecated_options.update(action.option_strings)\n        return action\n\n    def parse_known_args(  # type: ignore[override]\n        self,\n        args: Sequence[str] | None = None,\n        namespace: argparse.Namespace | None = None,\n    ) -&gt; tuple[argparse.Namespace, list[str]]:\n        argv = list(sys.argv[1:] if args is None else args)\n        argv = self._preprocess(argv)\n        self._warn_deprecated(argv)\n        return super().parse_known_args(argv, namespace)\n\n    def _subcommand_choices(self) -&gt; set[str]:\n        for action in self._actions:\n            if type(action).__name__ == \"_SubParsersAction\":\n                return set(action.choices or ())\n        return set()\n\n    def _preprocess(self, argv: list[str]) -&gt; list[str]:\n        choices = self._subcommand_choices()\n        owns_help = True\n        out: list[str] = []\n        for tok in argv:\n            if owns_help and tok in choices:\n                owns_help = False\n            if tok.startswith(\"--help=\") and owns_help:\n                self._search_keyword = tok.split(\"=\", 1)[1].strip().lower()\n                tok = \"--help\"\n            elif tok.startswith(\"--\") and len(tok) &gt; 2:\n                key, eq, val = tok.partition(\"=\")\n                tok = _normalize_option(key) + eq + val\n            out.append(tok)\n        return self._repack_dotted(out)\n\n    def _repack_dotted(self, argv: list[str]) -&gt; list[str]:\n        merged: dict[str, dict[str, Any]] = {}\n        out: list[str] = []\n        i = 0\n        while i &lt; len(argv):\n            tok = argv[i]\n            key, eq, inline = tok.partition(\"=\")\n            if tok.startswith(\"--\") and \".\" in key:\n                if eq:\n                    value, i = inline, i + 1\n                elif i + 1 &lt; len(argv):\n                    value, i = argv[i + 1], i + 2\n                else:\n                    self.error(f\"missing value for {tok}\")\n                root, _, path = key.partition(\".\")\n                append = path.endswith(\"+\")\n                _assign_nested(\n                    merged.setdefault(root, {}),\n                    path.rstrip(\"+\").split(\".\"),\n                    _coerce_value(value),\n                    append,\n                )\n            else:\n                out.append(tok)\n                i += 1\n        for root, obj in merged.items():\n            out.extend((root, json.dumps(obj)))\n        return out\n\n    def _warn_deprecated(self, argv: list[str]) -&gt; None:\n        for tok in argv:\n            opt = tok.partition(\"=\")[0]\n            if opt in self._deprecated_options and opt not in self._warned_deprecated:\n                FlexibleArgumentParser._warned_deprecated.add(opt)\n                log.warning(\"argument '%s' is deprecated\", opt.lstrip(\"-\"))\n\n    def format_help(self) -&gt; str:\n        keyword = self._search_keyword\n        if keyword is None:\n            return super().format_help()\n        if keyword == \"all\":\n            return self._format_groups(self._action_groups)\n        norm = keyword.replace(\"-\", \"\").replace(\"_\", \"\")\n        for group in self._action_groups:\n            title = (group.title or \"\").replace(\"-\", \"\").replace(\"_\", \"\").lower()\n            if title == norm and group._group_actions:\n                return self._format_groups([group])\n        matches = [\n            a for a in self._actions if any(keyword in opt.lower() for opt in a.option_strings)\n        ]\n        if matches:\n            return self._format_matches(keyword, matches)\n        return (\n            f\"No group or option matches {keyword!r}. \"\n            \"Use '--help' for a summary or '--help=all' for every option.\\n\"\n        )\n\n    def _format_groups(self, groups: Sequence[Any]) -&gt; str:\n        formatter = self._get_formatter()\n        actions = [a for g in groups for a in g._group_actions]\n        formatter.add_usage(self.usage, actions, self._mutually_exclusive_groups)\n        formatter.add_text(self.description)\n        for group in groups:\n            formatter.start_section(group.title)\n            formatter.add_text(group.description)\n            formatter.add_arguments(group._group_actions)\n            formatter.end_section()\n        formatter.add_text(self.epilog)\n        return formatter.format_help()\n\n    def _format_matches(self, keyword: str, matches: list[argparse.Action]) -&gt; str:\n        formatter = self._get_formatter()\n        formatter.add_usage(self.usage, matches, [])\n        formatter.start_section(f\"options matching {keyword!r}\")\n        formatter.add_arguments(matches)\n        formatter.end_section()\n        return formatter.format_help()\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._warned_deprecated","title":"_warned_deprecated  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_warned_deprecated: set[str] = set()\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._search_keyword","title":"_search_keyword  <code>instance-attribute</code>","text":"<pre><code>_search_keyword: str | None = None\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._deprecated_options","title":"_deprecated_options  <code>instance-attribute</code>","text":"<pre><code>_deprecated_options: set[str] = set()\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser.__init__","title":"__init__","text":"<pre><code>__init__(*args: Any, **kwargs: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def __init__(self, *args: Any, **kwargs: Any) -&gt; None:\n    kwargs.setdefault(\"formatter_class\", SortedHelpFormatter)\n    super().__init__(*args, **kwargs)\n    self._search_keyword: str | None = None\n    self._deprecated_options: set[str] = set()\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser.add_argument","title":"add_argument","text":"<pre><code>add_argument(*args: Any, **kwargs: Any) -&gt; Action\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def add_argument(self, *args: Any, **kwargs: Any) -&gt; argparse.Action:\n    deprecated = kwargs.pop(\"deprecated\", False)\n    action = super().add_argument(*args, **kwargs)\n    if deprecated:\n        self._deprecated_options.update(action.option_strings)\n    return action\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser.parse_known_args","title":"parse_known_args","text":"<pre><code>parse_known_args(args: Sequence[str] | None = None, namespace: Namespace | None = None) -&gt; tuple[Namespace, list[str]]\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def parse_known_args(  # type: ignore[override]\n    self,\n    args: Sequence[str] | None = None,\n    namespace: argparse.Namespace | None = None,\n) -&gt; tuple[argparse.Namespace, list[str]]:\n    argv = list(sys.argv[1:] if args is None else args)\n    argv = self._preprocess(argv)\n    self._warn_deprecated(argv)\n    return super().parse_known_args(argv, namespace)\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._subcommand_choices","title":"_subcommand_choices","text":"<pre><code>_subcommand_choices() -&gt; set[str]\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _subcommand_choices(self) -&gt; set[str]:\n    for action in self._actions:\n        if type(action).__name__ == \"_SubParsersAction\":\n            return set(action.choices or ())\n    return set()\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._preprocess","title":"_preprocess","text":"<pre><code>_preprocess(argv: list[str]) -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _preprocess(self, argv: list[str]) -&gt; list[str]:\n    choices = self._subcommand_choices()\n    owns_help = True\n    out: list[str] = []\n    for tok in argv:\n        if owns_help and tok in choices:\n            owns_help = False\n        if tok.startswith(\"--help=\") and owns_help:\n            self._search_keyword = tok.split(\"=\", 1)[1].strip().lower()\n            tok = \"--help\"\n        elif tok.startswith(\"--\") and len(tok) &gt; 2:\n            key, eq, val = tok.partition(\"=\")\n            tok = _normalize_option(key) + eq + val\n        out.append(tok)\n    return self._repack_dotted(out)\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._repack_dotted","title":"_repack_dotted","text":"<pre><code>_repack_dotted(argv: list[str]) -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _repack_dotted(self, argv: list[str]) -&gt; list[str]:\n    merged: dict[str, dict[str, Any]] = {}\n    out: list[str] = []\n    i = 0\n    while i &lt; len(argv):\n        tok = argv[i]\n        key, eq, inline = tok.partition(\"=\")\n        if tok.startswith(\"--\") and \".\" in key:\n            if eq:\n                value, i = inline, i + 1\n            elif i + 1 &lt; len(argv):\n                value, i = argv[i + 1], i + 2\n            else:\n                self.error(f\"missing value for {tok}\")\n            root, _, path = key.partition(\".\")\n            append = path.endswith(\"+\")\n            _assign_nested(\n                merged.setdefault(root, {}),\n                path.rstrip(\"+\").split(\".\"),\n                _coerce_value(value),\n                append,\n            )\n        else:\n            out.append(tok)\n            i += 1\n    for root, obj in merged.items():\n        out.extend((root, json.dumps(obj)))\n    return out\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._warn_deprecated","title":"_warn_deprecated","text":"<pre><code>_warn_deprecated(argv: list[str]) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _warn_deprecated(self, argv: list[str]) -&gt; None:\n    for tok in argv:\n        opt = tok.partition(\"=\")[0]\n        if opt in self._deprecated_options and opt not in self._warned_deprecated:\n            FlexibleArgumentParser._warned_deprecated.add(opt)\n            log.warning(\"argument '%s' is deprecated\", opt.lstrip(\"-\"))\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser.format_help","title":"format_help","text":"<pre><code>format_help() -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def format_help(self) -&gt; str:\n    keyword = self._search_keyword\n    if keyword is None:\n        return super().format_help()\n    if keyword == \"all\":\n        return self._format_groups(self._action_groups)\n    norm = keyword.replace(\"-\", \"\").replace(\"_\", \"\")\n    for group in self._action_groups:\n        title = (group.title or \"\").replace(\"-\", \"\").replace(\"_\", \"\").lower()\n        if title == norm and group._group_actions:\n            return self._format_groups([group])\n    matches = [\n        a for a in self._actions if any(keyword in opt.lower() for opt in a.option_strings)\n    ]\n    if matches:\n        return self._format_matches(keyword, matches)\n    return (\n        f\"No group or option matches {keyword!r}. \"\n        \"Use '--help' for a summary or '--help=all' for every option.\\n\"\n    )\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._format_groups","title":"_format_groups","text":"<pre><code>_format_groups(groups: Sequence[Any]) -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _format_groups(self, groups: Sequence[Any]) -&gt; str:\n    formatter = self._get_formatter()\n    actions = [a for g in groups for a in g._group_actions]\n    formatter.add_usage(self.usage, actions, self._mutually_exclusive_groups)\n    formatter.add_text(self.description)\n    for group in groups:\n        formatter.start_section(group.title)\n        formatter.add_text(group.description)\n        formatter.add_arguments(group._group_actions)\n        formatter.end_section()\n    formatter.add_text(self.epilog)\n    return formatter.format_help()\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.FlexibleArgumentParser._format_matches","title":"_format_matches","text":"<pre><code>_format_matches(keyword: str, matches: list[Action]) -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _format_matches(self, keyword: str, matches: list[argparse.Action]) -&gt; str:\n    formatter = self._get_formatter()\n    formatter.add_usage(self.usage, matches, [])\n    formatter.start_section(f\"options matching {keyword!r}\")\n    formatter.add_arguments(matches)\n    formatter.end_section()\n    return formatter.format_help()\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.human_readable_int","title":"human_readable_int","text":"<pre><code>human_readable_int(value: str) -&gt; int\n</code></pre> <p>Parse ints with suffixes: lowercase decimal (<code>25.6k</code>), uppercase binary (<code>16K</code>).</p> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def human_readable_int(value: str) -&gt; int:\n    r\"\"\"Parse ints with suffixes: lowercase decimal (``25.6k``), uppercase binary (``16K``).\"\"\"\n    value = value.strip()\n    match = _HUMAN_INT_RE.fullmatch(value)\n    if not match:\n        raise argparse.ArgumentTypeError(f\"invalid integer {value!r}\")\n    number, suffix = match.group(1), match.group(2)\n    if suffix in _DECIMAL_SUFFIXES:\n        return int(float(number) * _DECIMAL_SUFFIXES[suffix])\n    if \".\" in number:\n        raise argparse.ArgumentTypeError(\n            f\"decimals are only allowed with decimal suffixes (k/m/g/t), got {value!r}\"\n        )\n    if suffix in _BINARY_SUFFIXES:\n        return int(number) * _BINARY_SUFFIXES[suffix]\n    return int(number)\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser.check_port","title":"check_port","text":"<pre><code>check_port(value: str) -&gt; int\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def check_port(value: str) -&gt; int:\n    port = int(value)\n    if not 1024 &lt;= port &lt;= 65535:\n        raise argparse.ArgumentTypeError(f\"port must be in [1024, 65535], got {port}\")\n    return port\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser._coerce_value","title":"_coerce_value","text":"<pre><code>_coerce_value(value: str) -&gt; Any\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _coerce_value(value: str) -&gt; Any:\n    try:\n        return json.loads(value)\n    except json.JSONDecodeError:\n        pass\n    try:\n        return human_readable_int(value)\n    except argparse.ArgumentTypeError:\n        return value\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser._assign_nested","title":"_assign_nested","text":"<pre><code>_assign_nested(target: dict[str, Any], keys: list[str], value: Any, append: bool) -&gt; None\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _assign_nested(target: dict[str, Any], keys: list[str], value: Any, append: bool) -&gt; None:\n    for key in keys[:-1]:\n        node = target.setdefault(key, {})\n        if not isinstance(node, dict):\n            log.warning(\"overwriting non-dict value at %r with a dict\", key)\n            node = target[key] = {}\n        target = node\n    leaf = keys[-1]\n    if append:\n        existing = target.setdefault(leaf, [])\n        if not isinstance(existing, list):\n            existing = target[leaf] = [existing]\n        existing.append(value)\n    else:\n        if leaf in target:\n            log.warning(\"duplicate key %r, keeping the last value\", leaf)\n        target[leaf] = value\n</code></pre>"},{"location":"api/worldkernels/cli/parser/#worldkernels.cli.parser._normalize_option","title":"_normalize_option","text":"<pre><code>_normalize_option(key: str) -&gt; str\n</code></pre> Source code in <code>worldkernels/cli/parser.py</code> <pre><code>def _normalize_option(key: str) -&gt; str:\n    head, dot, tail = key.partition(\".\")\n    return head.replace(\"_\", \"-\") + dot + tail\n</code></pre>"},{"location":"api/worldkernels/cli/plugins/","title":"plugins","text":""},{"location":"api/worldkernels/cli/plugins/#worldkernels.cli.plugins","title":"worldkernels.cli.plugins","text":"<p>Back-compat shim: handler moved to worldkernels.cli.commands.plugins.</p>"},{"location":"api/worldkernels/cli/pull/","title":"pull","text":""},{"location":"api/worldkernels/cli/pull/#worldkernels.cli.pull","title":"worldkernels.cli.pull","text":"<p>Back-compat shim: handlers moved to worldkernels.cli.commands.pull.</p>"},{"location":"api/worldkernels/cli/run/","title":"run","text":""},{"location":"api/worldkernels/cli/run/#worldkernels.cli.run","title":"worldkernels.cli.run","text":"<p>Back-compat shim: handler moved to worldkernels.cli.commands.run.</p>"},{"location":"api/worldkernels/cli/serve/","title":"serve","text":""},{"location":"api/worldkernels/cli/serve/#worldkernels.cli.serve","title":"worldkernels.cli.serve","text":"<p>Back-compat shim: handler moved to worldkernels.cli.commands.serve.</p>"},{"location":"api/worldkernels/config/","title":"config","text":""},{"location":"api/worldkernels/config/#worldkernels.config","title":"worldkernels.config","text":"<p>Configuration dataclasses for worldkernels.</p> <p>Import-light (no torch); safe on the CLI cold path.</p> <p>Modules:</p> Name Description <code>active</code> <p>Process-level active RuntimeConfig.</p> <code>cache_config</code> <p>Memory and cache configuration for the runtime.</p> <code>engine_config</code> <p>Engine configuration.</p> <code>parallel_config</code> <p>Parallelism configuration for distributed execution.</p> <code>profiles</code> <p>Ablation profiles + precedence resolver for RuntimeConfig.</p> <code>runtime</code> <p>RuntimeConfig \u2014 the single source of truth for component toggles.</p> <code>scheduler_config</code> <p>Scheduler configuration: batching, admission, preemption.</p> <code>server_config</code> <p>HTTP/WebSocket server configuration.</p> <code>world_config</code> <p>Per-session world-model generation parameters.</p>"},{"location":"api/worldkernels/config/#worldkernels.config.EngineConfig","title":"EngineConfig  <code>module-attribute</code>","text":"<pre><code>EngineConfig = RuntimeConfig\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.PROFILES","title":"PROFILES  <code>module-attribute</code>","text":"<pre><code>PROFILES: dict[str, dict[str, Any]] = {'baseline': {'torch_compile': False, 'cuda_graphs': False, 'continuous_batching': False, 'iteration_batching': False, 'teacache': False, 'trajectory_cache': False, 'kv_cache_paged': False, 'latent_pool': False, 'offload_idle': False, 'attention_backend': 'sdpa'}, 'default': {}, 'fast': {'teacache': True}, 'production': {'teacache': True, 'quantization': 'int8'}}\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RuntimeConfig', 'SessionOverrides', 'EngineConfig', 'ParallelConfig', 'CacheConfig', 'SchedulerConfig', 'WorldConfig', 'ServerConfig', 'PROFILES', 'resolve_runtime_config', 'profile_config']\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig","title":"CacheConfig  <code>dataclass</code>","text":"<p>Block-based memory and caching policy.</p> <p>Parameters:</p> Name Type Description Default <code>block_frames</code> <code>int</code> <p>Latent frames per fixed-size memory block.</p> <code>16</code> <code>gpu_memory_fraction</code> <code>float</code> <p>Fraction of free VRAM the block allocator claims.</p> <code>0.8</code> <code>enable_kv_paging</code> <code>bool</code> <p>Page the KV cache for causal world models.</p> <code>True</code> <code>enable_trajectory_cache</code> <code>bool</code> <p>Share latent blocks across sessions with a common action-history prefix (radix-tree prefix cache).</p> <code>True</code> <code>offload_to_cpu</code> <code>bool</code> <p>Allow paused-session state to spill to pinned host memory.</p> <code>True</code> <code>offload_to_nvme</code> <code>bool</code> <p>Allow cold state to spill to an NVMe-backed mmap.</p> <code>False</code> <code>nvme_path</code> <code>str | None</code> <p>Directory for NVMe spill files (required if <code>offload_to_nvme</code>).</p> <code>None</code> Source code in <code>worldkernels/config/cache_config.py</code> <pre><code>@dataclass\nclass CacheConfig:\n    r\"\"\"Block-based memory and caching policy.\n\n    Args:\n        block_frames: Latent frames per fixed-size memory block.\n        gpu_memory_fraction: Fraction of free VRAM the block allocator claims.\n        enable_kv_paging: Page the KV cache for causal world models.\n        enable_trajectory_cache: Share latent blocks across sessions with a\n            common action-history prefix (radix-tree prefix cache).\n        offload_to_cpu: Allow paused-session state to spill to pinned host memory.\n        offload_to_nvme: Allow cold state to spill to an NVMe-backed mmap.\n        nvme_path: Directory for NVMe spill files (required if ``offload_to_nvme``).\n    \"\"\"\n\n    block_frames: int = 16\n    gpu_memory_fraction: float = 0.8\n    enable_kv_paging: bool = True\n    enable_trajectory_cache: bool = True\n    offload_to_cpu: bool = True\n    offload_to_nvme: bool = False\n    nvme_path: str | None = None\n\n    def __post_init__(self) -&gt; None:\n        if not 0.0 &lt; self.gpu_memory_fraction &lt;= 1.0:\n            raise ValueError(\n                f\"gpu_memory_fraction must be in (0, 1], got {self.gpu_memory_fraction}\"\n            )\n        if self.offload_to_nvme and self.nvme_path is None:\n            raise ValueError(\"nvme_path is required when offload_to_nvme is True\")\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.block_frames","title":"block_frames  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>block_frames: int = 16\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.gpu_memory_fraction","title":"gpu_memory_fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gpu_memory_fraction: float = 0.8\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.enable_kv_paging","title":"enable_kv_paging  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_kv_paging: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.enable_trajectory_cache","title":"enable_trajectory_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_trajectory_cache: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.offload_to_cpu","title":"offload_to_cpu  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_to_cpu: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.offload_to_nvme","title":"offload_to_nvme  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_to_nvme: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.nvme_path","title":"nvme_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>nvme_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/config/cache_config.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    if not 0.0 &lt; self.gpu_memory_fraction &lt;= 1.0:\n        raise ValueError(\n            f\"gpu_memory_fraction must be in (0, 1], got {self.gpu_memory_fraction}\"\n        )\n    if self.offload_to_nvme and self.nvme_path is None:\n        raise ValueError(\"nvme_path is required when offload_to_nvme is True\")\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.CacheConfig.__init__","title":"__init__","text":"<pre><code>__init__(block_frames: int = 16, gpu_memory_fraction: float = 0.8, enable_kv_paging: bool = True, enable_trajectory_cache: bool = True, offload_to_cpu: bool = True, offload_to_nvme: bool = False, nvme_path: str | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig","title":"ParallelConfig  <code>dataclass</code>","text":"<p>Multi-GPU parallelism degrees.</p> <p>The total rank count is the product of every degree, \\( W = \\text{TP} \\cdot \\text{DP} \\cdot \\text{PP} \\cdot \\text{SP} \\cdot \\text{CFG} \\), where \\( \\text{SP} = \\text{ulysses} \\cdot \\text{ring} \\). Single-GPU execution is the all-ones special case.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_parallel_size</code> <code>int</code> <p>Shards model weights across ranks.</p> <code>1</code> <code>data_parallel_size</code> <code>int</code> <p>Replicates the model across rank groups.</p> <code>1</code> <code>pipeline_parallel_size</code> <code>int</code> <p>Splits layers across rank stages.</p> <code>1</code> <code>ulysses_degree</code> <code>int</code> <p>Ulysses sequence-parallel degree (all-to-all on Q/K/V).</p> <code>1</code> <code>ring_degree</code> <code>int</code> <p>Ring sequence-parallel degree (ring attention).</p> <code>1</code> <code>cfg_parallel_size</code> <code>int</code> <p>1, or 2 to split positive/negative guidance.</p> <code>1</code> Source code in <code>worldkernels/config/parallel_config.py</code> <pre><code>@dataclass\nclass ParallelConfig:\n    r\"\"\"Multi-GPU parallelism degrees.\n\n    The total rank count is the product of every degree,\n    \\( W = \\text{TP} \\cdot \\text{DP} \\cdot \\text{PP} \\cdot \\text{SP} \\cdot \\text{CFG} \\),\n    where \\( \\text{SP} = \\text{ulysses} \\cdot \\text{ring} \\). Single-GPU\n    execution is the all-ones special case.\n\n    Args:\n        tensor_parallel_size: Shards model weights across ranks.\n        data_parallel_size: Replicates the model across rank groups.\n        pipeline_parallel_size: Splits layers across rank stages.\n        ulysses_degree: Ulysses sequence-parallel degree (all-to-all on Q/K/V).\n        ring_degree: Ring sequence-parallel degree (ring attention).\n        cfg_parallel_size: 1, or 2 to split positive/negative guidance.\n    \"\"\"\n\n    tensor_parallel_size: int = 1\n    data_parallel_size: int = 1\n    pipeline_parallel_size: int = 1\n    ulysses_degree: int = 1\n    ring_degree: int = 1\n    cfg_parallel_size: int = 1\n\n    def __post_init__(self) -&gt; None:\n        degrees = {\n            \"tensor_parallel_size\": self.tensor_parallel_size,\n            \"data_parallel_size\": self.data_parallel_size,\n            \"pipeline_parallel_size\": self.pipeline_parallel_size,\n            \"ulysses_degree\": self.ulysses_degree,\n            \"ring_degree\": self.ring_degree,\n        }\n        for name, value in degrees.items():\n            if value &lt; 1:\n                raise ValueError(f\"{name} must be &gt;= 1, got {value}\")\n        if self.cfg_parallel_size not in (1, 2):\n            raise ValueError(f\"cfg_parallel_size must be 1 or 2, got {self.cfg_parallel_size}\")\n\n    @property\n    def sequence_parallel_size(self) -&gt; int:\n        return self.ulysses_degree * self.ring_degree\n\n    @property\n    def world_size(self) -&gt; int:\n        return (\n            self.tensor_parallel_size\n            * self.data_parallel_size\n            * self.pipeline_parallel_size\n            * self.sequence_parallel_size\n            * self.cfg_parallel_size\n        )\n\n    @property\n    def is_distributed(self) -&gt; bool:\n        return self.world_size &gt; 1\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.tensor_parallel_size","title":"tensor_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tensor_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.data_parallel_size","title":"data_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>data_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.pipeline_parallel_size","title":"pipeline_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pipeline_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.ulysses_degree","title":"ulysses_degree  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ulysses_degree: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.ring_degree","title":"ring_degree  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ring_degree: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.cfg_parallel_size","title":"cfg_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cfg_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.sequence_parallel_size","title":"sequence_parallel_size  <code>property</code>","text":"<pre><code>sequence_parallel_size: int\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.world_size","title":"world_size  <code>property</code>","text":"<pre><code>world_size: int\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.is_distributed","title":"is_distributed  <code>property</code>","text":"<pre><code>is_distributed: bool\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/config/parallel_config.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    degrees = {\n        \"tensor_parallel_size\": self.tensor_parallel_size,\n        \"data_parallel_size\": self.data_parallel_size,\n        \"pipeline_parallel_size\": self.pipeline_parallel_size,\n        \"ulysses_degree\": self.ulysses_degree,\n        \"ring_degree\": self.ring_degree,\n    }\n    for name, value in degrees.items():\n        if value &lt; 1:\n            raise ValueError(f\"{name} must be &gt;= 1, got {value}\")\n    if self.cfg_parallel_size not in (1, 2):\n        raise ValueError(f\"cfg_parallel_size must be 1 or 2, got {self.cfg_parallel_size}\")\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ParallelConfig.__init__","title":"__init__","text":"<pre><code>__init__(tensor_parallel_size: int = 1, data_parallel_size: int = 1, pipeline_parallel_size: int = 1, ulysses_degree: int = 1, ring_degree: int = 1, cfg_parallel_size: int = 1) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig","title":"RuntimeConfig  <code>dataclass</code>","text":"<p>Process-wide engine configuration and component switch panel.</p> <p>Every optimization is a field here. Defaults preserve current behavior. Flags split into bake-time (device/dtype/quantization/torch_compile/ cuda_graphs \u2014 fixed at init or warmup) and runtime (caching, batching, placement, attention \u2014 some flip per-session via <code>SessionOverrides</code>).</p> Source code in <code>worldkernels/config/runtime.py</code> <pre><code>@dataclass\nclass RuntimeConfig:\n    r\"\"\"Process-wide engine configuration and component switch panel.\n\n    Every optimization is a field here. Defaults preserve current behavior.\n    Flags split into *bake-time* (device/dtype/quantization/torch_compile/\n    cuda_graphs \u2014 fixed at init or warmup) and *runtime* (caching, batching,\n    placement, attention \u2014 some flip per-session via `SessionOverrides`).\n    \"\"\"\n\n    device: str = \"cuda\"\n    dtype: Literal[\"auto\", \"bf16\", \"fp16\", \"fp32\"] = \"auto\"\n    quantization: Literal[\"none\", \"int8\", \"int4\"] = \"none\"\n\n    torch_compile: bool = True\n    cuda_graphs: bool = True\n\n    continuous_batching: bool = True\n    iteration_batching: bool = True\n\n    teacache: bool = False\n    trajectory_cache: bool = True\n    kv_cache_paged: bool = True\n    latent_pool: bool = True\n\n    offload_idle: bool = True\n\n    attention_backend: Literal[\"auto\", \"flash\", \"sdpa\"] = \"auto\"\n\n    isolation: Literal[\"auto\", \"shared\", \"isolated\"] = \"auto\"\n\n    max_sessions: int = 4\n\n    parallel: ParallelConfig = field(default_factory=ParallelConfig)\n    cache: CacheConfig = field(default_factory=CacheConfig)\n    scheduler: SchedulerConfig = field(default_factory=SchedulerConfig)\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.device","title":"device  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>device: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.dtype","title":"dtype  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dtype: Literal['auto', 'bf16', 'fp16', 'fp32'] = 'auto'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.quantization","title":"quantization  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>quantization: Literal['none', 'int8', 'int4'] = 'none'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.torch_compile","title":"torch_compile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>torch_compile: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.cuda_graphs","title":"cuda_graphs  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cuda_graphs: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.continuous_batching","title":"continuous_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>continuous_batching: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.iteration_batching","title":"iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>iteration_batching: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.teacache","title":"teacache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>teacache: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.trajectory_cache","title":"trajectory_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>trajectory_cache: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.kv_cache_paged","title":"kv_cache_paged  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kv_cache_paged: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.latent_pool","title":"latent_pool  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>latent_pool: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.offload_idle","title":"offload_idle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_idle: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.attention_backend","title":"attention_backend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_backend: Literal['auto', 'flash', 'sdpa'] = 'auto'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.isolation","title":"isolation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>isolation: Literal['auto', 'shared', 'isolated'] = 'auto'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.max_sessions","title":"max_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_sessions: int = 4\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.parallel","title":"parallel  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>parallel: ParallelConfig = field(default_factory=ParallelConfig)\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.cache","title":"cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cache: CacheConfig = field(default_factory=CacheConfig)\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.scheduler","title":"scheduler  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>scheduler: SchedulerConfig = field(default_factory=SchedulerConfig)\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.RuntimeConfig.__init__","title":"__init__","text":"<pre><code>__init__(device: str = 'cuda', dtype: Literal['auto', 'bf16', 'fp16', 'fp32'] = 'auto', quantization: Literal['none', 'int8', 'int4'] = 'none', torch_compile: bool = True, cuda_graphs: bool = True, continuous_batching: bool = True, iteration_batching: bool = True, teacache: bool = False, trajectory_cache: bool = True, kv_cache_paged: bool = True, latent_pool: bool = True, offload_idle: bool = True, attention_backend: Literal['auto', 'flash', 'sdpa'] = 'auto', isolation: Literal['auto', 'shared', 'isolated'] = 'auto', max_sessions: int = 4, parallel: ParallelConfig = ParallelConfig(), cache: CacheConfig = CacheConfig(), scheduler: SchedulerConfig = SchedulerConfig()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SessionOverrides","title":"SessionOverrides  <code>dataclass</code>","text":"<p>Per-session toggle overrides \u2014 only the subset safe to flip without a warmup/load rebake. <code>None</code> means \"inherit the engine value\".</p> Source code in <code>worldkernels/config/runtime.py</code> <pre><code>@dataclass\nclass SessionOverrides:\n    r\"\"\"Per-session toggle overrides \u2014 only the subset safe to flip without a\n    warmup/load rebake. ``None`` means \"inherit the engine value\".\"\"\"\n\n    teacache: bool | None = None\n    trajectory_cache: bool | None = None\n    iteration_batching: bool | None = None\n    offload_idle: bool | None = None\n    attention_backend: Literal[\"auto\", \"flash\", \"sdpa\"] | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SessionOverrides.teacache","title":"teacache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>teacache: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SessionOverrides.trajectory_cache","title":"trajectory_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>trajectory_cache: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SessionOverrides.iteration_batching","title":"iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>iteration_batching: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SessionOverrides.offload_idle","title":"offload_idle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_idle: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SessionOverrides.attention_backend","title":"attention_backend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_backend: Literal['auto', 'flash', 'sdpa'] | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SessionOverrides.__init__","title":"__init__","text":"<pre><code>__init__(teacache: bool | None = None, trajectory_cache: bool | None = None, iteration_batching: bool | None = None, offload_idle: bool | None = None, attention_backend: Literal['auto', 'flash', 'sdpa'] | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig","title":"SchedulerConfig  <code>dataclass</code>","text":"<p>Continuous-batching scheduler policy.</p> <p>Parameters:</p> Name Type Description Default <code>max_batch_size</code> <code>int</code> <p>Maximum sessions in one batched forward pass.</p> <code>8</code> <code>max_concurrent_sessions</code> <code>int</code> <p>Admission cap on live sessions.</p> <code>16</code> <code>policy</code> <code>Literal['fcfs', 'priority']</code> <p>Queue ordering \u2014 first-come or priority-weighted.</p> <code>'fcfs'</code> <code>preemption_mode</code> <code>Literal['swap', 'recompute']</code> <p>Reclaim VRAM by swapping state to host, or by dropping it and recomputing from a checkpoint.</p> <code>'swap'</code> <code>enable_iteration_batching</code> <code>bool</code> <p>Let sessions join a batch mid-denoise via <code>WorldModel.transition_iter()</code>.</p> <code>True</code> <code>admission_headroom_mb</code> <code>float</code> <p>VRAM kept free above the profiled per-session cost before a new session is admitted.</p> <code>512.0</code> Source code in <code>worldkernels/config/scheduler_config.py</code> <pre><code>@dataclass\nclass SchedulerConfig:\n    r\"\"\"Continuous-batching scheduler policy.\n\n    Args:\n        max_batch_size: Maximum sessions in one batched forward pass.\n        max_concurrent_sessions: Admission cap on live sessions.\n        policy: Queue ordering \u2014 first-come or priority-weighted.\n        preemption_mode: Reclaim VRAM by swapping state to host, or by\n            dropping it and recomputing from a checkpoint.\n        enable_iteration_batching: Let sessions join a batch mid-denoise via\n            ``WorldModel.transition_iter()``.\n        admission_headroom_mb: VRAM kept free above the profiled per-session\n            cost before a new session is admitted.\n    \"\"\"\n\n    max_batch_size: int = 8\n    max_concurrent_sessions: int = 16\n    policy: Literal[\"fcfs\", \"priority\"] = \"fcfs\"\n    preemption_mode: Literal[\"swap\", \"recompute\"] = \"swap\"\n    enable_iteration_batching: bool = True\n    admission_headroom_mb: float = 512.0\n\n    def __post_init__(self) -&gt; None:\n        if self.max_batch_size &lt; 1:\n            raise ValueError(f\"max_batch_size must be &gt;= 1, got {self.max_batch_size}\")\n        if self.max_concurrent_sessions &lt; 1:\n            raise ValueError(\n                f\"max_concurrent_sessions must be &gt;= 1, got {self.max_concurrent_sessions}\"\n            )\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.max_batch_size","title":"max_batch_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_batch_size: int = 8\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.max_concurrent_sessions","title":"max_concurrent_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_concurrent_sessions: int = 16\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.policy","title":"policy  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>policy: Literal['fcfs', 'priority'] = 'fcfs'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.preemption_mode","title":"preemption_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>preemption_mode: Literal['swap', 'recompute'] = 'swap'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.enable_iteration_batching","title":"enable_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_iteration_batching: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.admission_headroom_mb","title":"admission_headroom_mb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>admission_headroom_mb: float = 512.0\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/config/scheduler_config.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    if self.max_batch_size &lt; 1:\n        raise ValueError(f\"max_batch_size must be &gt;= 1, got {self.max_batch_size}\")\n    if self.max_concurrent_sessions &lt; 1:\n        raise ValueError(\n            f\"max_concurrent_sessions must be &gt;= 1, got {self.max_concurrent_sessions}\"\n        )\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.SchedulerConfig.__init__","title":"__init__","text":"<pre><code>__init__(max_batch_size: int = 8, max_concurrent_sessions: int = 16, policy: Literal['fcfs', 'priority'] = 'fcfs', preemption_mode: Literal['swap', 'recompute'] = 'swap', enable_iteration_batching: bool = True, admission_headroom_mb: float = 512.0) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig","title":"ServerConfig  <code>dataclass</code>","text":"<p>Configuration for the serving layer.</p> Source code in <code>worldkernels/config/server_config.py</code> <pre><code>@dataclass\nclass ServerConfig:\n    r\"\"\"Configuration for the serving layer.\"\"\"\n\n    host: str = \"0.0.0.0\"\n    port: int = 8000\n    max_sessions: int = 4\n    api_key: str | None = None\n    allowed_origins: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_methods: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_headers: list[str] = field(default_factory=lambda: [\"*\"])\n    allow_credentials: bool = False\n    ssl_keyfile: str | None = None\n    ssl_certfile: str | None = None\n    uvicorn_log_level: Literal[\"critical\", \"error\", \"warning\", \"info\", \"debug\", \"trace\"] = \"info\"\n    disable_access_log: bool = False\n    root_path: str = \"\"\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.host","title":"host  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>host: str = '0.0.0.0'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.port","title":"port  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>port: int = 8000\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.max_sessions","title":"max_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_sessions: int = 4\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.api_key","title":"api_key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>api_key: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.allowed_origins","title":"allowed_origins  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_origins: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.allowed_methods","title":"allowed_methods  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_methods: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.allowed_headers","title":"allowed_headers  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_headers: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.allow_credentials","title":"allow_credentials  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allow_credentials: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.ssl_keyfile","title":"ssl_keyfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_keyfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.ssl_certfile","title":"ssl_certfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_certfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.uvicorn_log_level","title":"uvicorn_log_level  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.disable_access_log","title":"disable_access_log  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>disable_access_log: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.root_path","title":"root_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>root_path: str = ''\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.ServerConfig.__init__","title":"__init__","text":"<pre><code>__init__(host: str = '0.0.0.0', port: int = 8000, max_sessions: int = 4, api_key: str | None = None, allowed_origins: list[str] = (lambda: ['*'])(), allowed_methods: list[str] = (lambda: ['*'])(), allowed_headers: list[str] = (lambda: ['*'])(), allow_credentials: bool = False, ssl_keyfile: str | None = None, ssl_certfile: str | None = None, uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info', disable_access_log: bool = False, root_path: str = '') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig","title":"WorldConfig  <code>dataclass</code>","text":"<p>Per-session generation parameters.</p> <p>For bidirectional models, <code>frames_per_step</code> is the full chunk size. For causal models, <code>context_window</code> and <code>chunk_overlap</code> control streaming and KV cache behavior.</p> Source code in <code>worldkernels/config/world_config.py</code> <pre><code>@dataclass\nclass WorldConfig:\n    r\"\"\"Per-session generation parameters.\n\n    For bidirectional models, ``frames_per_step`` is the full chunk size.\n    For causal models, ``context_window`` and ``chunk_overlap`` control\n    streaming and KV cache behavior.\n    \"\"\"\n\n    height: int = 480\n    width: int = 848\n    fps: int = 24\n\n    num_inference_steps: int = 4\n    guidance_scale: float = 1.0\n\n    frames_per_step: int = 8\n    chunk_overlap: int = 0\n\n    context_window: int = 0\n    attention_sink_tokens: int = 0\n\n    initial_prompt: str | None = None\n    initial_image: str | None = None\n\n    max_vram_gb: float | None = None\n    precision: Literal[\"bf16\", \"fp16\", \"fp32\"] = \"bf16\"\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.height","title":"height  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>height: int = 480\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.width","title":"width  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>width: int = 848\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.fps","title":"fps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fps: int = 24\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.num_inference_steps","title":"num_inference_steps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_inference_steps: int = 4\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.guidance_scale","title":"guidance_scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>guidance_scale: float = 1.0\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.frames_per_step","title":"frames_per_step  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frames_per_step: int = 8\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.chunk_overlap","title":"chunk_overlap  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>chunk_overlap: int = 0\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.context_window","title":"context_window  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>context_window: int = 0\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.attention_sink_tokens","title":"attention_sink_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_sink_tokens: int = 0\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.initial_prompt","title":"initial_prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_prompt: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.initial_image","title":"initial_image  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_image: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.max_vram_gb","title":"max_vram_gb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_vram_gb: float | None = None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.precision","title":"precision  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16'\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.WorldConfig.__init__","title":"__init__","text":"<pre><code>__init__(height: int = 480, width: int = 848, fps: int = 24, num_inference_steps: int = 4, guidance_scale: float = 1.0, frames_per_step: int = 8, chunk_overlap: int = 0, context_window: int = 0, attention_sink_tokens: int = 0, initial_prompt: str | None = None, initial_image: str | None = None, max_vram_gb: float | None = None, precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.profile_config","title":"profile_config","text":"<pre><code>profile_config(name: str) -&gt; RuntimeConfig\n</code></pre> <p>Materialize a profile into a RuntimeConfig (defaults + profile overrides).</p> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def profile_config(name: str) -&gt; RuntimeConfig:\n    r\"\"\"Materialize a profile into a RuntimeConfig (defaults + profile overrides).\"\"\"\n    cfg, _ = resolve_runtime_config(profile=name)\n    return cfg\n</code></pre>"},{"location":"api/worldkernels/config/#worldkernels.config.resolve_runtime_config","title":"resolve_runtime_config","text":"<pre><code>resolve_runtime_config(profile: str | None = None, cli_overrides: dict[str, Any] | None = None, env: 'os._Environ[str] | dict[str, str] | None' = None, config_file: str | Path | None = None) -&gt; tuple[RuntimeConfig, dict[str, str]]\n</code></pre> <p>Resolve a RuntimeConfig under the precedence chain.</p> <p>Parameters:</p> Name Type Description Default <code>profile</code> <code>str | None</code> <p>Named profile from <code>PROFILES</code> (e.g. <code>\"baseline\"</code>).</p> <code>None</code> <code>cli_overrides</code> <code>dict[str, Any] | None</code> <p><code>{field: value}</code> from CLI flags; nested fields use dotted keys (<code>\"parallel.tensor_parallel_size\"</code>). <code>None</code> values are ignored; unknown fields raise <code>ValueError</code>.</p> <code>None</code> <code>env</code> <code>'os._Environ[str] | dict[str, str] | None'</code> <p>Environment mapping (defaults to <code>os.environ</code>).</p> <code>None</code> <code>config_file</code> <code>str | Path | None</code> <p>YAML file whose engine keys form the config-file layer (frontend keys are ignored here; see <code>split_config_file</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>RuntimeConfig</code> <p><code>(config, sources)</code> where <code>sources[field]</code> is one of <code>\"default\"</code>,</p> <code>dict[str, str]</code> <p><code>\"profile:&lt;name&gt;\"</code>, <code>\"config:&lt;path&gt;\"</code>, <code>\"env:&lt;VAR&gt;\"</code>,</p> <code>tuple[RuntimeConfig, dict[str, str]]</code> <p><code>\"cli:--&lt;flag&gt;\"</code>. Nested fields appear under their dotted keys.</p> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def resolve_runtime_config(\n    profile: str | None = None,\n    cli_overrides: dict[str, Any] | None = None,\n    env: \"os._Environ[str] | dict[str, str] | None\" = None,\n    config_file: str | Path | None = None,\n) -&gt; tuple[RuntimeConfig, dict[str, str]]:\n    r\"\"\"Resolve a RuntimeConfig under the precedence chain.\n\n    Args:\n        profile: Named profile from `PROFILES` (e.g. ``\"baseline\"``).\n        cli_overrides: ``{field: value}`` from CLI flags; nested fields use\n            dotted keys (``\"parallel.tensor_parallel_size\"``). ``None`` values\n            are ignored; unknown fields raise ``ValueError``.\n        env: Environment mapping (defaults to ``os.environ``).\n        config_file: YAML file whose engine keys form the config-file layer\n            (frontend keys are ignored here; see ``split_config_file``).\n\n    Returns:\n        ``(config, sources)`` where ``sources[field]`` is one of ``\"default\"``,\n        ``\"profile:&lt;name&gt;\"``, ``\"config:&lt;path&gt;\"``, ``\"env:&lt;VAR&gt;\"``,\n        ``\"cli:--&lt;flag&gt;\"``. Nested fields appear under their dotted keys.\n    \"\"\"\n    env = os.environ if env is None else env\n\n    cfg = RuntimeConfig()\n    sources: dict[str, str] = {f: \"default\" for f in ALL_TOGGLE_FIELDS}\n\n    if profile:\n        if profile not in PROFILES:\n            raise ValueError(f\"unknown profile {profile!r}; choices: {sorted(PROFILES)}\")\n        for field_name, value in PROFILES[profile].items():\n            _set_field(cfg, sources, field_name, value, f\"profile:{profile}\")\n\n    if config_file is not None:\n        _, engine = split_config_file(config_file)\n        for field_name, value in _flatten_engine(engine).items():\n            _set_field(cfg, sources, field_name, value, f\"config:{config_file}\")\n\n    for field_name, value, var in _env_overrides(env):\n        setattr(cfg, field_name, value)\n        sources[field_name] = f\"env:{var}\"\n\n    for field_name, value in (cli_overrides or {}).items():\n        if value is None:\n            continue\n        flag = field_name.rpartition(\".\")[2].replace(\"_\", \"-\")\n        _set_field(cfg, sources, field_name, value, f\"cli:--{flag}\")\n\n    for name in NESTED_CONFIGS:\n        getattr(cfg, name).__post_init__()\n    return cfg, sources\n</code></pre>"},{"location":"api/worldkernels/config/active/","title":"active","text":""},{"location":"api/worldkernels/config/active/#worldkernels.config.active","title":"worldkernels.config.active","text":"<p>Process-level active RuntimeConfig.</p> <p>Bake-time gates (torch_compile, quantization, cuda_graphs at warmup) read the active config here instead of threading it through every world/pipeline signature. The engine sets it at construction. Step-time, per-session toggles ride <code>ForwardContext</code> instead (set per request).</p> <p>One engine per process is the common case; a second engine overwrites the active config (documented limitation \u2014 per-session overrides cover the multi-config-in-one-process case at step time).</p>"},{"location":"api/worldkernels/config/active/#worldkernels.config.active._ACTIVE","title":"_ACTIVE  <code>module-attribute</code>","text":"<pre><code>_ACTIVE: 'RuntimeConfig | None' = None\n</code></pre>"},{"location":"api/worldkernels/config/active/#worldkernels.config.active.set_active_config","title":"set_active_config","text":"<pre><code>set_active_config(config: 'RuntimeConfig') -&gt; None\n</code></pre> Source code in <code>worldkernels/config/active.py</code> <pre><code>def set_active_config(config: \"RuntimeConfig\") -&gt; None:\n    global _ACTIVE\n    _ACTIVE = config\n</code></pre>"},{"location":"api/worldkernels/config/active/#worldkernels.config.active.get_active_config","title":"get_active_config","text":"<pre><code>get_active_config() -&gt; 'RuntimeConfig'\n</code></pre> Source code in <code>worldkernels/config/active.py</code> <pre><code>def get_active_config() -&gt; \"RuntimeConfig\":\n    global _ACTIVE\n    if _ACTIVE is None:\n        from worldkernels.config.runtime import RuntimeConfig\n\n        _ACTIVE = RuntimeConfig()\n    return _ACTIVE\n</code></pre>"},{"location":"api/worldkernels/config/active/#worldkernels.config.active.clear_active_config","title":"clear_active_config","text":"<pre><code>clear_active_config() -&gt; None\n</code></pre> Source code in <code>worldkernels/config/active.py</code> <pre><code>def clear_active_config() -&gt; None:\n    global _ACTIVE\n    _ACTIVE = None\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/","title":"cache_config","text":""},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config","title":"worldkernels.config.cache_config","text":"<p>Memory and cache configuration for the runtime.</p>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['CacheConfig']\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig","title":"CacheConfig  <code>dataclass</code>","text":"<p>Block-based memory and caching policy.</p> <p>Parameters:</p> Name Type Description Default <code>block_frames</code> <code>int</code> <p>Latent frames per fixed-size memory block.</p> <code>16</code> <code>gpu_memory_fraction</code> <code>float</code> <p>Fraction of free VRAM the block allocator claims.</p> <code>0.8</code> <code>enable_kv_paging</code> <code>bool</code> <p>Page the KV cache for causal world models.</p> <code>True</code> <code>enable_trajectory_cache</code> <code>bool</code> <p>Share latent blocks across sessions with a common action-history prefix (radix-tree prefix cache).</p> <code>True</code> <code>offload_to_cpu</code> <code>bool</code> <p>Allow paused-session state to spill to pinned host memory.</p> <code>True</code> <code>offload_to_nvme</code> <code>bool</code> <p>Allow cold state to spill to an NVMe-backed mmap.</p> <code>False</code> <code>nvme_path</code> <code>str | None</code> <p>Directory for NVMe spill files (required if <code>offload_to_nvme</code>).</p> <code>None</code> Source code in <code>worldkernels/config/cache_config.py</code> <pre><code>@dataclass\nclass CacheConfig:\n    r\"\"\"Block-based memory and caching policy.\n\n    Args:\n        block_frames: Latent frames per fixed-size memory block.\n        gpu_memory_fraction: Fraction of free VRAM the block allocator claims.\n        enable_kv_paging: Page the KV cache for causal world models.\n        enable_trajectory_cache: Share latent blocks across sessions with a\n            common action-history prefix (radix-tree prefix cache).\n        offload_to_cpu: Allow paused-session state to spill to pinned host memory.\n        offload_to_nvme: Allow cold state to spill to an NVMe-backed mmap.\n        nvme_path: Directory for NVMe spill files (required if ``offload_to_nvme``).\n    \"\"\"\n\n    block_frames: int = 16\n    gpu_memory_fraction: float = 0.8\n    enable_kv_paging: bool = True\n    enable_trajectory_cache: bool = True\n    offload_to_cpu: bool = True\n    offload_to_nvme: bool = False\n    nvme_path: str | None = None\n\n    def __post_init__(self) -&gt; None:\n        if not 0.0 &lt; self.gpu_memory_fraction &lt;= 1.0:\n            raise ValueError(\n                f\"gpu_memory_fraction must be in (0, 1], got {self.gpu_memory_fraction}\"\n            )\n        if self.offload_to_nvme and self.nvme_path is None:\n            raise ValueError(\"nvme_path is required when offload_to_nvme is True\")\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.block_frames","title":"block_frames  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>block_frames: int = 16\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.gpu_memory_fraction","title":"gpu_memory_fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gpu_memory_fraction: float = 0.8\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.enable_kv_paging","title":"enable_kv_paging  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_kv_paging: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.enable_trajectory_cache","title":"enable_trajectory_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_trajectory_cache: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.offload_to_cpu","title":"offload_to_cpu  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_to_cpu: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.offload_to_nvme","title":"offload_to_nvme  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_to_nvme: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.nvme_path","title":"nvme_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>nvme_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.__init__","title":"__init__","text":"<pre><code>__init__(block_frames: int = 16, gpu_memory_fraction: float = 0.8, enable_kv_paging: bool = True, enable_trajectory_cache: bool = True, offload_to_cpu: bool = True, offload_to_nvme: bool = False, nvme_path: str | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/cache_config/#worldkernels.config.cache_config.CacheConfig.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/config/cache_config.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    if not 0.0 &lt; self.gpu_memory_fraction &lt;= 1.0:\n        raise ValueError(\n            f\"gpu_memory_fraction must be in (0, 1], got {self.gpu_memory_fraction}\"\n        )\n    if self.offload_to_nvme and self.nvme_path is None:\n        raise ValueError(\"nvme_path is required when offload_to_nvme is True\")\n</code></pre>"},{"location":"api/worldkernels/config/engine_config/","title":"engine_config","text":""},{"location":"api/worldkernels/config/engine_config/#worldkernels.config.engine_config","title":"worldkernels.config.engine_config","text":"<p>Engine configuration.</p> <p><code>EngineConfig</code> is retained as a deprecated alias for <code>RuntimeConfig</code>, which is now the single source of truth for engine settings + component toggles.</p>"},{"location":"api/worldkernels/config/engine_config/#worldkernels.config.engine_config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['EngineConfig']\n</code></pre>"},{"location":"api/worldkernels/config/engine_config/#worldkernels.config.engine_config.EngineConfig","title":"EngineConfig  <code>module-attribute</code>","text":"<pre><code>EngineConfig = RuntimeConfig\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/","title":"parallel_config","text":""},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config","title":"worldkernels.config.parallel_config","text":"<p>Parallelism configuration for distributed execution.</p>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ParallelConfig']\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig","title":"ParallelConfig  <code>dataclass</code>","text":"<p>Multi-GPU parallelism degrees.</p> <p>The total rank count is the product of every degree, \\( W = \\text{TP} \\cdot \\text{DP} \\cdot \\text{PP} \\cdot \\text{SP} \\cdot \\text{CFG} \\), where \\( \\text{SP} = \\text{ulysses} \\cdot \\text{ring} \\). Single-GPU execution is the all-ones special case.</p> <p>Parameters:</p> Name Type Description Default <code>tensor_parallel_size</code> <code>int</code> <p>Shards model weights across ranks.</p> <code>1</code> <code>data_parallel_size</code> <code>int</code> <p>Replicates the model across rank groups.</p> <code>1</code> <code>pipeline_parallel_size</code> <code>int</code> <p>Splits layers across rank stages.</p> <code>1</code> <code>ulysses_degree</code> <code>int</code> <p>Ulysses sequence-parallel degree (all-to-all on Q/K/V).</p> <code>1</code> <code>ring_degree</code> <code>int</code> <p>Ring sequence-parallel degree (ring attention).</p> <code>1</code> <code>cfg_parallel_size</code> <code>int</code> <p>1, or 2 to split positive/negative guidance.</p> <code>1</code> Source code in <code>worldkernels/config/parallel_config.py</code> <pre><code>@dataclass\nclass ParallelConfig:\n    r\"\"\"Multi-GPU parallelism degrees.\n\n    The total rank count is the product of every degree,\n    \\( W = \\text{TP} \\cdot \\text{DP} \\cdot \\text{PP} \\cdot \\text{SP} \\cdot \\text{CFG} \\),\n    where \\( \\text{SP} = \\text{ulysses} \\cdot \\text{ring} \\). Single-GPU\n    execution is the all-ones special case.\n\n    Args:\n        tensor_parallel_size: Shards model weights across ranks.\n        data_parallel_size: Replicates the model across rank groups.\n        pipeline_parallel_size: Splits layers across rank stages.\n        ulysses_degree: Ulysses sequence-parallel degree (all-to-all on Q/K/V).\n        ring_degree: Ring sequence-parallel degree (ring attention).\n        cfg_parallel_size: 1, or 2 to split positive/negative guidance.\n    \"\"\"\n\n    tensor_parallel_size: int = 1\n    data_parallel_size: int = 1\n    pipeline_parallel_size: int = 1\n    ulysses_degree: int = 1\n    ring_degree: int = 1\n    cfg_parallel_size: int = 1\n\n    def __post_init__(self) -&gt; None:\n        degrees = {\n            \"tensor_parallel_size\": self.tensor_parallel_size,\n            \"data_parallel_size\": self.data_parallel_size,\n            \"pipeline_parallel_size\": self.pipeline_parallel_size,\n            \"ulysses_degree\": self.ulysses_degree,\n            \"ring_degree\": self.ring_degree,\n        }\n        for name, value in degrees.items():\n            if value &lt; 1:\n                raise ValueError(f\"{name} must be &gt;= 1, got {value}\")\n        if self.cfg_parallel_size not in (1, 2):\n            raise ValueError(f\"cfg_parallel_size must be 1 or 2, got {self.cfg_parallel_size}\")\n\n    @property\n    def sequence_parallel_size(self) -&gt; int:\n        return self.ulysses_degree * self.ring_degree\n\n    @property\n    def world_size(self) -&gt; int:\n        return (\n            self.tensor_parallel_size\n            * self.data_parallel_size\n            * self.pipeline_parallel_size\n            * self.sequence_parallel_size\n            * self.cfg_parallel_size\n        )\n\n    @property\n    def is_distributed(self) -&gt; bool:\n        return self.world_size &gt; 1\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.tensor_parallel_size","title":"tensor_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tensor_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.data_parallel_size","title":"data_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>data_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.pipeline_parallel_size","title":"pipeline_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pipeline_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.ulysses_degree","title":"ulysses_degree  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ulysses_degree: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.ring_degree","title":"ring_degree  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ring_degree: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.cfg_parallel_size","title":"cfg_parallel_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cfg_parallel_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.sequence_parallel_size","title":"sequence_parallel_size  <code>property</code>","text":"<pre><code>sequence_parallel_size: int\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.world_size","title":"world_size  <code>property</code>","text":"<pre><code>world_size: int\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.is_distributed","title":"is_distributed  <code>property</code>","text":"<pre><code>is_distributed: bool\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.__init__","title":"__init__","text":"<pre><code>__init__(tensor_parallel_size: int = 1, data_parallel_size: int = 1, pipeline_parallel_size: int = 1, ulysses_degree: int = 1, ring_degree: int = 1, cfg_parallel_size: int = 1) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/parallel_config/#worldkernels.config.parallel_config.ParallelConfig.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/config/parallel_config.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    degrees = {\n        \"tensor_parallel_size\": self.tensor_parallel_size,\n        \"data_parallel_size\": self.data_parallel_size,\n        \"pipeline_parallel_size\": self.pipeline_parallel_size,\n        \"ulysses_degree\": self.ulysses_degree,\n        \"ring_degree\": self.ring_degree,\n    }\n    for name, value in degrees.items():\n        if value &lt; 1:\n            raise ValueError(f\"{name} must be &gt;= 1, got {value}\")\n    if self.cfg_parallel_size not in (1, 2):\n        raise ValueError(f\"cfg_parallel_size must be 1 or 2, got {self.cfg_parallel_size}\")\n</code></pre>"},{"location":"api/worldkernels/config/profiles/","title":"profiles","text":""},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles","title":"worldkernels.config.profiles","text":"<p>Ablation profiles + precedence resolver for RuntimeConfig.</p> <p>Precedence (lowest to highest): defaults &lt; profile &lt; config file &lt; env (WK_*) &lt; CLI. <code>resolve_runtime_config</code> returns the resolved config plus a per-field source map so <code>worldkernels config-show</code> can attribute every value.</p>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['PROFILES', 'NESTED_CONFIGS', 'CLI_OWNED_FIELDS', 'resolve_runtime_config', 'profile_config', 'split_config_file']\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles.PROFILES","title":"PROFILES  <code>module-attribute</code>","text":"<pre><code>PROFILES: dict[str, dict[str, Any]] = {'baseline': {'torch_compile': False, 'cuda_graphs': False, 'continuous_batching': False, 'iteration_batching': False, 'teacache': False, 'trajectory_cache': False, 'kv_cache_paged': False, 'latent_pool': False, 'offload_idle': False, 'attention_backend': 'sdpa'}, 'default': {}, 'fast': {'teacache': True}, 'production': {'teacache': True, 'quantization': 'int8'}}\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles.NESTED_CONFIGS","title":"NESTED_CONFIGS  <code>module-attribute</code>","text":"<pre><code>NESTED_CONFIGS: dict[str, type] = {'parallel': ParallelConfig, 'cache': CacheConfig, 'scheduler': SchedulerConfig}\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles.CLI_OWNED_FIELDS","title":"CLI_OWNED_FIELDS  <code>module-attribute</code>","text":"<pre><code>CLI_OWNED_FIELDS: tuple[str, ...] = ('device', 'max_sessions')\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._FLAT_FIELDS","title":"_FLAT_FIELDS  <code>module-attribute</code>","text":"<pre><code>_FLAT_FIELDS = {(f.name) for f in (fields(RuntimeConfig))} - set(NESTED_CONFIGS)\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._NESTED_FIELDS","title":"_NESTED_FIELDS  <code>module-attribute</code>","text":"<pre><code>_NESTED_FIELDS = {name: {(f.name) for f in (fields(cls))} for name, cls in (NESTED_CONFIGS.items())}\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._TRUE","title":"_TRUE  <code>module-attribute</code>","text":"<pre><code>_TRUE = {'1', 'true', 'yes', 'on'}\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._FALSE","title":"_FALSE  <code>module-attribute</code>","text":"<pre><code>_FALSE = {'0', 'false', 'no', 'off'}\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles.profile_config","title":"profile_config","text":"<pre><code>profile_config(name: str) -&gt; RuntimeConfig\n</code></pre> <p>Materialize a profile into a RuntimeConfig (defaults + profile overrides).</p> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def profile_config(name: str) -&gt; RuntimeConfig:\n    r\"\"\"Materialize a profile into a RuntimeConfig (defaults + profile overrides).\"\"\"\n    cfg, _ = resolve_runtime_config(profile=name)\n    return cfg\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles.split_config_file","title":"split_config_file","text":"<pre><code>split_config_file(path: str | Path) -&gt; tuple[dict[str, Any], dict[str, Any]]\n</code></pre> <p>Split a YAML config file into <code>(frontend, engine)</code> mappings.</p> <p>Engine keys are RuntimeConfig fields: flat names, dotted paths, or nested mappings under <code>parallel</code> / <code>cache</code> / <code>scheduler</code>. Everything else (host, port, device, ...) is a frontend CLI flag for the caller to apply.</p> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def split_config_file(path: str | Path) -&gt; tuple[dict[str, Any], dict[str, Any]]:\n    r\"\"\"Split a YAML config file into ``(frontend, engine)`` mappings.\n\n    Engine keys are RuntimeConfig fields: flat names, dotted paths, or nested\n    mappings under ``parallel`` / ``cache`` / ``scheduler``. Everything else\n    (host, port, device, ...) is a frontend CLI flag for the caller to apply.\n    \"\"\"\n    import yaml\n\n    data = yaml.safe_load(Path(path).read_text()) or {}\n    if not isinstance(data, dict):\n        raise ValueError(f\"config file {str(path)!r} must contain a mapping\")\n    frontend: dict[str, Any] = {}\n    engine: dict[str, Any] = {}\n    for key, value in data.items():\n        norm = str(key).replace(\"-\", \"_\")\n        root = norm.partition(\".\")[0]\n        if norm in CLI_OWNED_FIELDS or (root not in _FLAT_FIELDS and root not in NESTED_CONFIGS):\n            frontend[str(key)] = value\n        else:\n            engine[norm] = value\n    return frontend, engine\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles.resolve_runtime_config","title":"resolve_runtime_config","text":"<pre><code>resolve_runtime_config(profile: str | None = None, cli_overrides: dict[str, Any] | None = None, env: 'os._Environ[str] | dict[str, str] | None' = None, config_file: str | Path | None = None) -&gt; tuple[RuntimeConfig, dict[str, str]]\n</code></pre> <p>Resolve a RuntimeConfig under the precedence chain.</p> <p>Parameters:</p> Name Type Description Default <code>profile</code> <code>str | None</code> <p>Named profile from <code>PROFILES</code> (e.g. <code>\"baseline\"</code>).</p> <code>None</code> <code>cli_overrides</code> <code>dict[str, Any] | None</code> <p><code>{field: value}</code> from CLI flags; nested fields use dotted keys (<code>\"parallel.tensor_parallel_size\"</code>). <code>None</code> values are ignored; unknown fields raise <code>ValueError</code>.</p> <code>None</code> <code>env</code> <code>'os._Environ[str] | dict[str, str] | None'</code> <p>Environment mapping (defaults to <code>os.environ</code>).</p> <code>None</code> <code>config_file</code> <code>str | Path | None</code> <p>YAML file whose engine keys form the config-file layer (frontend keys are ignored here; see <code>split_config_file</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>RuntimeConfig</code> <p><code>(config, sources)</code> where <code>sources[field]</code> is one of <code>\"default\"</code>,</p> <code>dict[str, str]</code> <p><code>\"profile:&lt;name&gt;\"</code>, <code>\"config:&lt;path&gt;\"</code>, <code>\"env:&lt;VAR&gt;\"</code>,</p> <code>tuple[RuntimeConfig, dict[str, str]]</code> <p><code>\"cli:--&lt;flag&gt;\"</code>. Nested fields appear under their dotted keys.</p> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def resolve_runtime_config(\n    profile: str | None = None,\n    cli_overrides: dict[str, Any] | None = None,\n    env: \"os._Environ[str] | dict[str, str] | None\" = None,\n    config_file: str | Path | None = None,\n) -&gt; tuple[RuntimeConfig, dict[str, str]]:\n    r\"\"\"Resolve a RuntimeConfig under the precedence chain.\n\n    Args:\n        profile: Named profile from `PROFILES` (e.g. ``\"baseline\"``).\n        cli_overrides: ``{field: value}`` from CLI flags; nested fields use\n            dotted keys (``\"parallel.tensor_parallel_size\"``). ``None`` values\n            are ignored; unknown fields raise ``ValueError``.\n        env: Environment mapping (defaults to ``os.environ``).\n        config_file: YAML file whose engine keys form the config-file layer\n            (frontend keys are ignored here; see ``split_config_file``).\n\n    Returns:\n        ``(config, sources)`` where ``sources[field]`` is one of ``\"default\"``,\n        ``\"profile:&lt;name&gt;\"``, ``\"config:&lt;path&gt;\"``, ``\"env:&lt;VAR&gt;\"``,\n        ``\"cli:--&lt;flag&gt;\"``. Nested fields appear under their dotted keys.\n    \"\"\"\n    env = os.environ if env is None else env\n\n    cfg = RuntimeConfig()\n    sources: dict[str, str] = {f: \"default\" for f in ALL_TOGGLE_FIELDS}\n\n    if profile:\n        if profile not in PROFILES:\n            raise ValueError(f\"unknown profile {profile!r}; choices: {sorted(PROFILES)}\")\n        for field_name, value in PROFILES[profile].items():\n            _set_field(cfg, sources, field_name, value, f\"profile:{profile}\")\n\n    if config_file is not None:\n        _, engine = split_config_file(config_file)\n        for field_name, value in _flatten_engine(engine).items():\n            _set_field(cfg, sources, field_name, value, f\"config:{config_file}\")\n\n    for field_name, value, var in _env_overrides(env):\n        setattr(cfg, field_name, value)\n        sources[field_name] = f\"env:{var}\"\n\n    for field_name, value in (cli_overrides or {}).items():\n        if value is None:\n            continue\n        flag = field_name.rpartition(\".\")[2].replace(\"_\", \"-\")\n        _set_field(cfg, sources, field_name, value, f\"cli:--{flag}\")\n\n    for name in NESTED_CONFIGS:\n        getattr(cfg, name).__post_init__()\n    return cfg, sources\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._set_field","title":"_set_field","text":"<pre><code>_set_field(cfg: RuntimeConfig, sources: dict[str, str], name: str, value: Any, source: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def _set_field(\n    cfg: RuntimeConfig, sources: dict[str, str], name: str, value: Any, source: str\n) -&gt; None:\n    root, dot, leaf = name.partition(\".\")\n    if dot:\n        if root not in NESTED_CONFIGS or leaf not in _NESTED_FIELDS[root]:\n            raise ValueError(f\"unknown config field {name!r} (from {source})\")\n        setattr(getattr(cfg, root), leaf, value)\n    else:\n        if root not in _FLAT_FIELDS:\n            raise ValueError(f\"unknown config field {name!r} (from {source})\")\n        allowed = TOGGLE_ENUM_FIELDS.get(root)\n        if allowed is not None and value not in allowed:\n            raise ValueError(\n                f\"invalid value {value!r} for {root!r}; choices: {allowed} (from {source})\"\n            )\n        setattr(cfg, root, value)\n    sources[name] = source\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._flatten_engine","title":"_flatten_engine","text":"<pre><code>_flatten_engine(engine: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def _flatten_engine(engine: dict[str, Any]) -&gt; dict[str, Any]:\n    out: dict[str, Any] = {}\n    for key, value in engine.items():\n        if key in NESTED_CONFIGS and isinstance(value, dict):\n            out.update({f\"{key}.{leaf}\": v for leaf, v in value.items()})\n        else:\n            out[key] = value\n    return out\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._env_overrides","title":"_env_overrides","text":"<pre><code>_env_overrides(env) -&gt; list[tuple[str, Any, str]]\n</code></pre> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def _env_overrides(env) -&gt; list[tuple[str, Any, str]]:\n    out: list[tuple[str, Any, str]] = []\n\n    disable = env.get(\"WK_DISABLE\", \"\")\n    for name in _split_csv(disable):\n        if name in TOGGLE_BOOL_FIELDS:\n            out.append((name, False, \"WK_DISABLE\"))\n\n    enable = env.get(\"WK_ENABLE\", \"\")\n    for name in _split_csv(enable):\n        if name in TOGGLE_BOOL_FIELDS:\n            out.append((name, True, \"WK_ENABLE\"))\n\n    for name in TOGGLE_BOOL_FIELDS:\n        var = f\"WK_{name.upper()}\"\n        if var in env:\n            out.append((name, _coerce_bool(env[var]), var))\n\n    for name, allowed in TOGGLE_ENUM_FIELDS.items():\n        var = f\"WK_{name.upper()}\"\n        if var in env:\n            val = env[var].strip().lower()\n            if val in allowed:\n                out.append((name, val, var))\n\n    return out\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._split_csv","title":"_split_csv","text":"<pre><code>_split_csv(value: str) -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def _split_csv(value: str) -&gt; list[str]:\n    return [v.strip() for v in value.split(\",\") if v.strip()]\n</code></pre>"},{"location":"api/worldkernels/config/profiles/#worldkernels.config.profiles._coerce_bool","title":"_coerce_bool","text":"<pre><code>_coerce_bool(value: str) -&gt; bool\n</code></pre> Source code in <code>worldkernels/config/profiles.py</code> <pre><code>def _coerce_bool(value: str) -&gt; bool:\n    v = value.strip().lower()\n    if v in _TRUE:\n        return True\n    if v in _FALSE:\n        return False\n    return bool(v)\n</code></pre>"},{"location":"api/worldkernels/config/runtime/","title":"runtime","text":""},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime","title":"worldkernels.config.runtime","text":"<p>RuntimeConfig \u2014 the single source of truth for component toggles.</p> <p>A flat \"switch panel\": one field per worldkernels component, so benchmarks, ablations, and demos can flip any subsystem on or off. Structured (non-toggle) parameters stay in the nested <code>ParallelConfig</code> / <code>CacheConfig</code> / <code>SchedulerConfig</code>.</p> <p>Import-light (no torch) \u2014 safe on the CLI cold path.</p>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RuntimeConfig', 'SessionOverrides', 'TOGGLE_BOOL_FIELDS', 'TOGGLE_ENUM_FIELDS', 'ALL_TOGGLE_FIELDS', 'SESSION_OVERRIDE_FIELDS']\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.TOGGLE_BOOL_FIELDS","title":"TOGGLE_BOOL_FIELDS  <code>module-attribute</code>","text":"<pre><code>TOGGLE_BOOL_FIELDS: tuple[str, ...] = ('torch_compile', 'cuda_graphs', 'continuous_batching', 'iteration_batching', 'teacache', 'trajectory_cache', 'kv_cache_paged', 'latent_pool', 'offload_idle')\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.TOGGLE_ENUM_FIELDS","title":"TOGGLE_ENUM_FIELDS  <code>module-attribute</code>","text":"<pre><code>TOGGLE_ENUM_FIELDS: dict[str, tuple[str, ...]] = {'dtype': ('auto', 'bf16', 'fp16', 'fp32'), 'quantization': ('none', 'int8', 'int4'), 'attention_backend': ('auto', 'flash', 'sdpa'), 'isolation': ('auto', 'shared', 'isolated')}\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.ALL_TOGGLE_FIELDS","title":"ALL_TOGGLE_FIELDS  <code>module-attribute</code>","text":"<pre><code>ALL_TOGGLE_FIELDS: tuple[str, ...] = TOGGLE_BOOL_FIELDS + tuple(TOGGLE_ENUM_FIELDS)\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SESSION_OVERRIDE_FIELDS","title":"SESSION_OVERRIDE_FIELDS  <code>module-attribute</code>","text":"<pre><code>SESSION_OVERRIDE_FIELDS: tuple[str, ...] = ('teacache', 'trajectory_cache', 'iteration_batching', 'offload_idle', 'attention_backend')\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig","title":"RuntimeConfig  <code>dataclass</code>","text":"<p>Process-wide engine configuration and component switch panel.</p> <p>Every optimization is a field here. Defaults preserve current behavior. Flags split into bake-time (device/dtype/quantization/torch_compile/ cuda_graphs \u2014 fixed at init or warmup) and runtime (caching, batching, placement, attention \u2014 some flip per-session via <code>SessionOverrides</code>).</p> Source code in <code>worldkernels/config/runtime.py</code> <pre><code>@dataclass\nclass RuntimeConfig:\n    r\"\"\"Process-wide engine configuration and component switch panel.\n\n    Every optimization is a field here. Defaults preserve current behavior.\n    Flags split into *bake-time* (device/dtype/quantization/torch_compile/\n    cuda_graphs \u2014 fixed at init or warmup) and *runtime* (caching, batching,\n    placement, attention \u2014 some flip per-session via `SessionOverrides`).\n    \"\"\"\n\n    device: str = \"cuda\"\n    dtype: Literal[\"auto\", \"bf16\", \"fp16\", \"fp32\"] = \"auto\"\n    quantization: Literal[\"none\", \"int8\", \"int4\"] = \"none\"\n\n    torch_compile: bool = True\n    cuda_graphs: bool = True\n\n    continuous_batching: bool = True\n    iteration_batching: bool = True\n\n    teacache: bool = False\n    trajectory_cache: bool = True\n    kv_cache_paged: bool = True\n    latent_pool: bool = True\n\n    offload_idle: bool = True\n\n    attention_backend: Literal[\"auto\", \"flash\", \"sdpa\"] = \"auto\"\n\n    isolation: Literal[\"auto\", \"shared\", \"isolated\"] = \"auto\"\n\n    max_sessions: int = 4\n\n    parallel: ParallelConfig = field(default_factory=ParallelConfig)\n    cache: CacheConfig = field(default_factory=CacheConfig)\n    scheduler: SchedulerConfig = field(default_factory=SchedulerConfig)\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.device","title":"device  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>device: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.dtype","title":"dtype  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dtype: Literal['auto', 'bf16', 'fp16', 'fp32'] = 'auto'\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.quantization","title":"quantization  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>quantization: Literal['none', 'int8', 'int4'] = 'none'\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.torch_compile","title":"torch_compile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>torch_compile: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.cuda_graphs","title":"cuda_graphs  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cuda_graphs: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.continuous_batching","title":"continuous_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>continuous_batching: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.iteration_batching","title":"iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>iteration_batching: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.teacache","title":"teacache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>teacache: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.trajectory_cache","title":"trajectory_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>trajectory_cache: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.kv_cache_paged","title":"kv_cache_paged  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kv_cache_paged: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.latent_pool","title":"latent_pool  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>latent_pool: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.offload_idle","title":"offload_idle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_idle: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.attention_backend","title":"attention_backend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_backend: Literal['auto', 'flash', 'sdpa'] = 'auto'\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.isolation","title":"isolation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>isolation: Literal['auto', 'shared', 'isolated'] = 'auto'\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.max_sessions","title":"max_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_sessions: int = 4\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.parallel","title":"parallel  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>parallel: ParallelConfig = field(default_factory=ParallelConfig)\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.cache","title":"cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cache: CacheConfig = field(default_factory=CacheConfig)\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.scheduler","title":"scheduler  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>scheduler: SchedulerConfig = field(default_factory=SchedulerConfig)\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.RuntimeConfig.__init__","title":"__init__","text":"<pre><code>__init__(device: str = 'cuda', dtype: Literal['auto', 'bf16', 'fp16', 'fp32'] = 'auto', quantization: Literal['none', 'int8', 'int4'] = 'none', torch_compile: bool = True, cuda_graphs: bool = True, continuous_batching: bool = True, iteration_batching: bool = True, teacache: bool = False, trajectory_cache: bool = True, kv_cache_paged: bool = True, latent_pool: bool = True, offload_idle: bool = True, attention_backend: Literal['auto', 'flash', 'sdpa'] = 'auto', isolation: Literal['auto', 'shared', 'isolated'] = 'auto', max_sessions: int = 4, parallel: ParallelConfig = ParallelConfig(), cache: CacheConfig = CacheConfig(), scheduler: SchedulerConfig = SchedulerConfig()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SessionOverrides","title":"SessionOverrides  <code>dataclass</code>","text":"<p>Per-session toggle overrides \u2014 only the subset safe to flip without a warmup/load rebake. <code>None</code> means \"inherit the engine value\".</p> Source code in <code>worldkernels/config/runtime.py</code> <pre><code>@dataclass\nclass SessionOverrides:\n    r\"\"\"Per-session toggle overrides \u2014 only the subset safe to flip without a\n    warmup/load rebake. ``None`` means \"inherit the engine value\".\"\"\"\n\n    teacache: bool | None = None\n    trajectory_cache: bool | None = None\n    iteration_batching: bool | None = None\n    offload_idle: bool | None = None\n    attention_backend: Literal[\"auto\", \"flash\", \"sdpa\"] | None = None\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SessionOverrides.teacache","title":"teacache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>teacache: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SessionOverrides.trajectory_cache","title":"trajectory_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>trajectory_cache: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SessionOverrides.iteration_batching","title":"iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>iteration_batching: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SessionOverrides.offload_idle","title":"offload_idle  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>offload_idle: bool | None = None\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SessionOverrides.attention_backend","title":"attention_backend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_backend: Literal['auto', 'flash', 'sdpa'] | None = None\n</code></pre>"},{"location":"api/worldkernels/config/runtime/#worldkernels.config.runtime.SessionOverrides.__init__","title":"__init__","text":"<pre><code>__init__(teacache: bool | None = None, trajectory_cache: bool | None = None, iteration_batching: bool | None = None, offload_idle: bool | None = None, attention_backend: Literal['auto', 'flash', 'sdpa'] | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/","title":"scheduler_config","text":""},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config","title":"worldkernels.config.scheduler_config","text":"<p>Scheduler configuration: batching, admission, preemption.</p>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['SchedulerConfig']\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig","title":"SchedulerConfig  <code>dataclass</code>","text":"<p>Continuous-batching scheduler policy.</p> <p>Parameters:</p> Name Type Description Default <code>max_batch_size</code> <code>int</code> <p>Maximum sessions in one batched forward pass.</p> <code>8</code> <code>max_concurrent_sessions</code> <code>int</code> <p>Admission cap on live sessions.</p> <code>16</code> <code>policy</code> <code>Literal['fcfs', 'priority']</code> <p>Queue ordering \u2014 first-come or priority-weighted.</p> <code>'fcfs'</code> <code>preemption_mode</code> <code>Literal['swap', 'recompute']</code> <p>Reclaim VRAM by swapping state to host, or by dropping it and recomputing from a checkpoint.</p> <code>'swap'</code> <code>enable_iteration_batching</code> <code>bool</code> <p>Let sessions join a batch mid-denoise via <code>WorldModel.transition_iter()</code>.</p> <code>True</code> <code>admission_headroom_mb</code> <code>float</code> <p>VRAM kept free above the profiled per-session cost before a new session is admitted.</p> <code>512.0</code> Source code in <code>worldkernels/config/scheduler_config.py</code> <pre><code>@dataclass\nclass SchedulerConfig:\n    r\"\"\"Continuous-batching scheduler policy.\n\n    Args:\n        max_batch_size: Maximum sessions in one batched forward pass.\n        max_concurrent_sessions: Admission cap on live sessions.\n        policy: Queue ordering \u2014 first-come or priority-weighted.\n        preemption_mode: Reclaim VRAM by swapping state to host, or by\n            dropping it and recomputing from a checkpoint.\n        enable_iteration_batching: Let sessions join a batch mid-denoise via\n            ``WorldModel.transition_iter()``.\n        admission_headroom_mb: VRAM kept free above the profiled per-session\n            cost before a new session is admitted.\n    \"\"\"\n\n    max_batch_size: int = 8\n    max_concurrent_sessions: int = 16\n    policy: Literal[\"fcfs\", \"priority\"] = \"fcfs\"\n    preemption_mode: Literal[\"swap\", \"recompute\"] = \"swap\"\n    enable_iteration_batching: bool = True\n    admission_headroom_mb: float = 512.0\n\n    def __post_init__(self) -&gt; None:\n        if self.max_batch_size &lt; 1:\n            raise ValueError(f\"max_batch_size must be &gt;= 1, got {self.max_batch_size}\")\n        if self.max_concurrent_sessions &lt; 1:\n            raise ValueError(\n                f\"max_concurrent_sessions must be &gt;= 1, got {self.max_concurrent_sessions}\"\n            )\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.max_batch_size","title":"max_batch_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_batch_size: int = 8\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.max_concurrent_sessions","title":"max_concurrent_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_concurrent_sessions: int = 16\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.policy","title":"policy  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>policy: Literal['fcfs', 'priority'] = 'fcfs'\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.preemption_mode","title":"preemption_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>preemption_mode: Literal['swap', 'recompute'] = 'swap'\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.enable_iteration_batching","title":"enable_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enable_iteration_batching: bool = True\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.admission_headroom_mb","title":"admission_headroom_mb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>admission_headroom_mb: float = 512.0\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.__init__","title":"__init__","text":"<pre><code>__init__(max_batch_size: int = 8, max_concurrent_sessions: int = 16, policy: Literal['fcfs', 'priority'] = 'fcfs', preemption_mode: Literal['swap', 'recompute'] = 'swap', enable_iteration_batching: bool = True, admission_headroom_mb: float = 512.0) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/scheduler_config/#worldkernels.config.scheduler_config.SchedulerConfig.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/config/scheduler_config.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    if self.max_batch_size &lt; 1:\n        raise ValueError(f\"max_batch_size must be &gt;= 1, got {self.max_batch_size}\")\n    if self.max_concurrent_sessions &lt; 1:\n        raise ValueError(\n            f\"max_concurrent_sessions must be &gt;= 1, got {self.max_concurrent_sessions}\"\n        )\n</code></pre>"},{"location":"api/worldkernels/config/server_config/","title":"server_config","text":""},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config","title":"worldkernels.config.server_config","text":"<p>HTTP/WebSocket server configuration.</p>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ServerConfig']\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig","title":"ServerConfig  <code>dataclass</code>","text":"<p>Configuration for the serving layer.</p> Source code in <code>worldkernels/config/server_config.py</code> <pre><code>@dataclass\nclass ServerConfig:\n    r\"\"\"Configuration for the serving layer.\"\"\"\n\n    host: str = \"0.0.0.0\"\n    port: int = 8000\n    max_sessions: int = 4\n    api_key: str | None = None\n    allowed_origins: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_methods: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_headers: list[str] = field(default_factory=lambda: [\"*\"])\n    allow_credentials: bool = False\n    ssl_keyfile: str | None = None\n    ssl_certfile: str | None = None\n    uvicorn_log_level: Literal[\"critical\", \"error\", \"warning\", \"info\", \"debug\", \"trace\"] = \"info\"\n    disable_access_log: bool = False\n    root_path: str = \"\"\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.host","title":"host  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>host: str = '0.0.0.0'\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.port","title":"port  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>port: int = 8000\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.max_sessions","title":"max_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_sessions: int = 4\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.api_key","title":"api_key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>api_key: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.allowed_origins","title":"allowed_origins  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_origins: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.allowed_methods","title":"allowed_methods  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_methods: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.allowed_headers","title":"allowed_headers  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_headers: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.allow_credentials","title":"allow_credentials  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allow_credentials: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.ssl_keyfile","title":"ssl_keyfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_keyfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.ssl_certfile","title":"ssl_certfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_certfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.uvicorn_log_level","title":"uvicorn_log_level  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info'\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.disable_access_log","title":"disable_access_log  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>disable_access_log: bool = False\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.root_path","title":"root_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>root_path: str = ''\n</code></pre>"},{"location":"api/worldkernels/config/server_config/#worldkernels.config.server_config.ServerConfig.__init__","title":"__init__","text":"<pre><code>__init__(host: str = '0.0.0.0', port: int = 8000, max_sessions: int = 4, api_key: str | None = None, allowed_origins: list[str] = (lambda: ['*'])(), allowed_methods: list[str] = (lambda: ['*'])(), allowed_headers: list[str] = (lambda: ['*'])(), allow_credentials: bool = False, ssl_keyfile: str | None = None, ssl_certfile: str | None = None, uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info', disable_access_log: bool = False, root_path: str = '') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/config/world_config/","title":"world_config","text":""},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config","title":"worldkernels.config.world_config","text":"<p>Per-session world-model generation parameters.</p>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WorldConfig']\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig","title":"WorldConfig  <code>dataclass</code>","text":"<p>Per-session generation parameters.</p> <p>For bidirectional models, <code>frames_per_step</code> is the full chunk size. For causal models, <code>context_window</code> and <code>chunk_overlap</code> control streaming and KV cache behavior.</p> Source code in <code>worldkernels/config/world_config.py</code> <pre><code>@dataclass\nclass WorldConfig:\n    r\"\"\"Per-session generation parameters.\n\n    For bidirectional models, ``frames_per_step`` is the full chunk size.\n    For causal models, ``context_window`` and ``chunk_overlap`` control\n    streaming and KV cache behavior.\n    \"\"\"\n\n    height: int = 480\n    width: int = 848\n    fps: int = 24\n\n    num_inference_steps: int = 4\n    guidance_scale: float = 1.0\n\n    frames_per_step: int = 8\n    chunk_overlap: int = 0\n\n    context_window: int = 0\n    attention_sink_tokens: int = 0\n\n    initial_prompt: str | None = None\n    initial_image: str | None = None\n\n    max_vram_gb: float | None = None\n    precision: Literal[\"bf16\", \"fp16\", \"fp32\"] = \"bf16\"\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.height","title":"height  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>height: int = 480\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.width","title":"width  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>width: int = 848\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.fps","title":"fps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fps: int = 24\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.num_inference_steps","title":"num_inference_steps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_inference_steps: int = 4\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.guidance_scale","title":"guidance_scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>guidance_scale: float = 1.0\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.frames_per_step","title":"frames_per_step  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frames_per_step: int = 8\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.chunk_overlap","title":"chunk_overlap  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>chunk_overlap: int = 0\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.context_window","title":"context_window  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>context_window: int = 0\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.attention_sink_tokens","title":"attention_sink_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_sink_tokens: int = 0\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.initial_prompt","title":"initial_prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_prompt: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.initial_image","title":"initial_image  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_image: str | None = None\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.max_vram_gb","title":"max_vram_gb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_vram_gb: float | None = None\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.precision","title":"precision  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16'\n</code></pre>"},{"location":"api/worldkernels/config/world_config/#worldkernels.config.world_config.WorldConfig.__init__","title":"__init__","text":"<pre><code>__init__(height: int = 480, width: int = 848, fps: int = 24, num_inference_steps: int = 4, guidance_scale: float = 1.0, frames_per_step: int = 8, chunk_overlap: int = 0, context_window: int = 0, attention_sink_tokens: int = 0, initial_prompt: str | None = None, initial_image: str | None = None, max_vram_gb: float | None = None, precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/","title":"core","text":""},{"location":"api/worldkernels/core/#worldkernels.core","title":"worldkernels.core","text":"<p>Core data types: session lifecycle, action, observation, error hierarchy.</p> <p>Modules:</p> Name Description <code>action</code> <p>Action schema for world model inputs.</p> <code>config</code> <p>Configuration dataclasses (moved to <code>config</code>).</p> <code>errors</code> <p>Exception hierarchy for WorldKernels.</p> <code>observation</code> <p>Observation types returned by world models.</p> <code>request</code> <p>A scheduled unit of execution work.</p> <code>session</code> <p>Session lifecycle management.</p> <code>state</code> <p>Structured world-model state.</p>"},{"location":"api/worldkernels/core/#worldkernels.core.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Action', 'CheckpointNotFoundError', 'LatentState', 'Observation', 'ServerConfig', 'Session', 'SessionLimitError', 'SessionNotFoundError', 'SessionPausedError', 'SessionStatus', 'SessionTerminatedError', 'VRAMExhaustedError', 'WorldAlreadyLoadedError', 'WorldConfig', 'WorldInitError', 'WorldKernelError', 'WorldNotFoundError']\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Action","title":"Action  <code>dataclass</code>","text":"<p>Generalized action container (control input to world).</p> Source code in <code>worldkernels/core/action.py</code> <pre><code>@dataclass\nclass Action:\n    r\"\"\"Generalized action container (control input to world).\"\"\"\n\n    action_type: str\n    payload: dict[str, Any] = field(default_factory=dict)\n    timestamp: float | None = None\n\n    def __post_init__(self) -&gt; None:\n        if not self.action_type:\n            raise ValueError(\"action_type cannot be empty\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Action.action_type","title":"action_type  <code>instance-attribute</code>","text":"<pre><code>action_type: str\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Action.payload","title":"payload  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>payload: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Action.timestamp","title":"timestamp  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timestamp: float | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Action.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/core/action.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    if not self.action_type:\n        raise ValueError(\"action_type cannot be empty\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Action.__init__","title":"__init__","text":"<pre><code>__init__(action_type: str, payload: dict[str, Any] = dict(), timestamp: float | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig","title":"ServerConfig  <code>dataclass</code>","text":"<p>Configuration for the serving layer.</p> Source code in <code>worldkernels/config/server_config.py</code> <pre><code>@dataclass\nclass ServerConfig:\n    r\"\"\"Configuration for the serving layer.\"\"\"\n\n    host: str = \"0.0.0.0\"\n    port: int = 8000\n    max_sessions: int = 4\n    api_key: str | None = None\n    allowed_origins: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_methods: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_headers: list[str] = field(default_factory=lambda: [\"*\"])\n    allow_credentials: bool = False\n    ssl_keyfile: str | None = None\n    ssl_certfile: str | None = None\n    uvicorn_log_level: Literal[\"critical\", \"error\", \"warning\", \"info\", \"debug\", \"trace\"] = \"info\"\n    disable_access_log: bool = False\n    root_path: str = \"\"\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.host","title":"host  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>host: str = '0.0.0.0'\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.port","title":"port  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>port: int = 8000\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.max_sessions","title":"max_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_sessions: int = 4\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.api_key","title":"api_key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>api_key: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.allowed_origins","title":"allowed_origins  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_origins: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.allowed_methods","title":"allowed_methods  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_methods: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.allowed_headers","title":"allowed_headers  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_headers: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.allow_credentials","title":"allow_credentials  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allow_credentials: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.ssl_keyfile","title":"ssl_keyfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_keyfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.ssl_certfile","title":"ssl_certfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_certfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.uvicorn_log_level","title":"uvicorn_log_level  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info'\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.disable_access_log","title":"disable_access_log  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>disable_access_log: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.root_path","title":"root_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>root_path: str = ''\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.ServerConfig.__init__","title":"__init__","text":"<pre><code>__init__(host: str = '0.0.0.0', port: int = 8000, max_sessions: int = 4, api_key: str | None = None, allowed_origins: list[str] = (lambda: ['*'])(), allowed_methods: list[str] = (lambda: ['*'])(), allowed_headers: list[str] = (lambda: ['*'])(), allow_credentials: bool = False, ssl_keyfile: str | None = None, ssl_certfile: str | None = None, uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info', disable_access_log: bool = False, root_path: str = '') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig","title":"WorldConfig  <code>dataclass</code>","text":"<p>Per-session generation parameters.</p> <p>For bidirectional models, <code>frames_per_step</code> is the full chunk size. For causal models, <code>context_window</code> and <code>chunk_overlap</code> control streaming and KV cache behavior.</p> Source code in <code>worldkernels/config/world_config.py</code> <pre><code>@dataclass\nclass WorldConfig:\n    r\"\"\"Per-session generation parameters.\n\n    For bidirectional models, ``frames_per_step`` is the full chunk size.\n    For causal models, ``context_window`` and ``chunk_overlap`` control\n    streaming and KV cache behavior.\n    \"\"\"\n\n    height: int = 480\n    width: int = 848\n    fps: int = 24\n\n    num_inference_steps: int = 4\n    guidance_scale: float = 1.0\n\n    frames_per_step: int = 8\n    chunk_overlap: int = 0\n\n    context_window: int = 0\n    attention_sink_tokens: int = 0\n\n    initial_prompt: str | None = None\n    initial_image: str | None = None\n\n    max_vram_gb: float | None = None\n    precision: Literal[\"bf16\", \"fp16\", \"fp32\"] = \"bf16\"\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.height","title":"height  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>height: int = 480\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.width","title":"width  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>width: int = 848\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.fps","title":"fps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fps: int = 24\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.num_inference_steps","title":"num_inference_steps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_inference_steps: int = 4\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.guidance_scale","title":"guidance_scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>guidance_scale: float = 1.0\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.frames_per_step","title":"frames_per_step  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frames_per_step: int = 8\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.chunk_overlap","title":"chunk_overlap  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>chunk_overlap: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.context_window","title":"context_window  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>context_window: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.attention_sink_tokens","title":"attention_sink_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_sink_tokens: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.initial_prompt","title":"initial_prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_prompt: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.initial_image","title":"initial_image  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_image: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.max_vram_gb","title":"max_vram_gb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_vram_gb: float | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.precision","title":"precision  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16'\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldConfig.__init__","title":"__init__","text":"<pre><code>__init__(height: int = 480, width: int = 848, fps: int = 24, num_inference_steps: int = 4, guidance_scale: float = 1.0, frames_per_step: int = 8, chunk_overlap: int = 0, context_window: int = 0, attention_sink_tokens: int = 0, initial_prompt: str | None = None, initial_image: str | None = None, max_vram_gb: float | None = None, precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.CheckpointNotFoundError","title":"CheckpointNotFoundError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class CheckpointNotFoundError(WorldKernelError):\n    def __init__(self, checkpoint_id: str, session_id: str) -&gt; None:\n        self.checkpoint_id = checkpoint_id\n        self.session_id = session_id\n        super().__init__(f\"Checkpoint '{checkpoint_id}' not found in session '{session_id}'.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.CheckpointNotFoundError.checkpoint_id","title":"checkpoint_id  <code>instance-attribute</code>","text":"<pre><code>checkpoint_id = checkpoint_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.CheckpointNotFoundError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.CheckpointNotFoundError.__init__","title":"__init__","text":"<pre><code>__init__(checkpoint_id: str, session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, checkpoint_id: str, session_id: str) -&gt; None:\n    self.checkpoint_id = checkpoint_id\n    self.session_id = session_id\n    super().__init__(f\"Checkpoint '{checkpoint_id}' not found in session '{session_id}'.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionLimitError","title":"SessionLimitError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionLimitError(WorldKernelError):\n    def __init__(self, max_sessions: int) -&gt; None:\n        self.max_sessions = max_sessions\n        super().__init__(f\"Max sessions ({max_sessions}) reached.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionLimitError.max_sessions","title":"max_sessions  <code>instance-attribute</code>","text":"<pre><code>max_sessions = max_sessions\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionLimitError.__init__","title":"__init__","text":"<pre><code>__init__(max_sessions: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, max_sessions: int) -&gt; None:\n    self.max_sessions = max_sessions\n    super().__init__(f\"Max sessions ({max_sessions}) reached.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionNotFoundError","title":"SessionNotFoundError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionNotFoundError(WorldKernelError):\n    def __init__(self, session_id: str) -&gt; None:\n        self.session_id = session_id\n        super().__init__(f\"Session '{session_id}' not found.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionNotFoundError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionNotFoundError.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, session_id: str) -&gt; None:\n    self.session_id = session_id\n    super().__init__(f\"Session '{session_id}' not found.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionPausedError","title":"SessionPausedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionPausedError(WorldKernelError):\n    def __init__(self, session_id: str) -&gt; None:\n        self.session_id = session_id\n        super().__init__(f\"Session '{session_id}' is paused. Call resume() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionPausedError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionPausedError.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, session_id: str) -&gt; None:\n    self.session_id = session_id\n    super().__init__(f\"Session '{session_id}' is paused. Call resume() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionTerminatedError","title":"SessionTerminatedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionTerminatedError(WorldKernelError):\n    def __init__(self, session_id: str) -&gt; None:\n        self.session_id = session_id\n        super().__init__(f\"Session '{session_id}' is terminated.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionTerminatedError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionTerminatedError.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, session_id: str) -&gt; None:\n    self.session_id = session_id\n    super().__init__(f\"Session '{session_id}' is terminated.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.VRAMExhaustedError","title":"VRAMExhaustedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class VRAMExhaustedError(WorldKernelError):\n    def __init__(self, required_mb: float, available_mb: float) -&gt; None:\n        self.required_mb = required_mb\n        self.available_mb = available_mb\n        super().__init__(\n            f\"Not enough VRAM: need {required_mb:.0f} MB, only {available_mb:.0f} MB available.\"\n        )\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.VRAMExhaustedError.required_mb","title":"required_mb  <code>instance-attribute</code>","text":"<pre><code>required_mb = required_mb\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.VRAMExhaustedError.available_mb","title":"available_mb  <code>instance-attribute</code>","text":"<pre><code>available_mb = available_mb\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.VRAMExhaustedError.__init__","title":"__init__","text":"<pre><code>__init__(required_mb: float, available_mb: float) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, required_mb: float, available_mb: float) -&gt; None:\n    self.required_mb = required_mb\n    self.available_mb = available_mb\n    super().__init__(\n        f\"Not enough VRAM: need {required_mb:.0f} MB, only {available_mb:.0f} MB available.\"\n    )\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldAlreadyLoadedError","title":"WorldAlreadyLoadedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldAlreadyLoadedError(WorldKernelError):\n    def __init__(self, name: str) -&gt; None:\n        self.name = name\n        super().__init__(f\"World '{name}' is already loaded.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldAlreadyLoadedError.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name = name\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldAlreadyLoadedError.__init__","title":"__init__","text":"<pre><code>__init__(name: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, name: str) -&gt; None:\n    self.name = name\n    super().__init__(f\"World '{name}' is already loaded.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldInitError","title":"WorldInitError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldInitError(WorldKernelError):\n    def __init__(self, world_id: str, reason: str) -&gt; None:\n        self.world_id = world_id\n        self.reason = reason\n        super().__init__(f\"Failed to initialize world '{world_id}': {reason}\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldInitError.world_id","title":"world_id  <code>instance-attribute</code>","text":"<pre><code>world_id = world_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldInitError.reason","title":"reason  <code>instance-attribute</code>","text":"<pre><code>reason = reason\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldInitError.__init__","title":"__init__","text":"<pre><code>__init__(world_id: str, reason: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, world_id: str, reason: str) -&gt; None:\n    self.world_id = world_id\n    self.reason = reason\n    super().__init__(f\"Failed to initialize world '{world_id}': {reason}\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldKernelError","title":"WorldKernelError","text":"<p>               Bases: <code>Exception</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldKernelError(Exception):\n    pass\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldNotFoundError","title":"WorldNotFoundError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldNotFoundError(WorldKernelError):\n    def __init__(self, world_id: str) -&gt; None:\n        self.world_id = world_id\n        super().__init__(f\"World '{world_id}' not loaded. Call load_model() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldNotFoundError.world_id","title":"world_id  <code>instance-attribute</code>","text":"<pre><code>world_id = world_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.WorldNotFoundError.__init__","title":"__init__","text":"<pre><code>__init__(world_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, world_id: str) -&gt; None:\n    self.world_id = world_id\n    super().__init__(f\"World '{world_id}' not loaded. Call load_model() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation","title":"Observation  <code>dataclass</code>","text":"<p>Multi-modal observation output from a world model step.</p> Source code in <code>worldkernels/core/observation.py</code> <pre><code>@dataclass\nclass Observation:\n    r\"\"\"Multi-modal observation output from a world model step.\"\"\"\n\n    step_index: int\n    generation_time_ms: float\n    frames: Any | None = None\n    latent: Any | None = None\n    audio: Any | None = None\n    depth: Any | None = None\n    segmentation: Any | None = None\n    structured: dict[str, Any] | None = field(default_factory=dict)\n    stage_timing: StageTiming | None = None\n    decode_skipped: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.step_index","title":"step_index  <code>instance-attribute</code>","text":"<pre><code>step_index: int\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.generation_time_ms","title":"generation_time_ms  <code>instance-attribute</code>","text":"<pre><code>generation_time_ms: float\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.frames","title":"frames  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frames: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.latent","title":"latent  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>latent: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.audio","title":"audio  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>audio: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.depth","title":"depth  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>depth: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.segmentation","title":"segmentation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>segmentation: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.structured","title":"structured  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>structured: dict[str, Any] | None = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.stage_timing","title":"stage_timing  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_timing: StageTiming | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.decode_skipped","title":"decode_skipped  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>decode_skipped: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Observation.__init__","title":"__init__","text":"<pre><code>__init__(step_index: int, generation_time_ms: float, frames: Any | None = None, latent: Any | None = None, audio: Any | None = None, depth: Any | None = None, segmentation: Any | None = None, structured: dict[str, Any] | None = dict(), stage_timing: StageTiming | None = None, decode_skipped: bool = False) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.LatentState","title":"LatentState  <code>dataclass</code>","text":"<p>Opaque tensor bundle representing the current world state.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>@dataclass\nclass LatentState:\n    r\"\"\"Opaque tensor bundle representing the current world state.\"\"\"\n\n    data: Any = None\n    device: str = \"cpu\"\n\n    @property\n    def nbytes(self) -&gt; int:\n        r\"\"\"Memory footprint in bytes.\"\"\"\n        if self.data is None:\n            return 0\n        if hasattr(self.data, \"nelement\") and hasattr(self.data, \"element_size\"):\n            return self.data.nelement() * self.data.element_size()\n        return 0\n\n    def clone(self) -&gt; LatentState:\n        r\"\"\"Deep-copy this state, decoupling from the original tensor.\"\"\"\n        if self.data is None:\n            return LatentState(data=None, device=self.device)\n        data_copy = self.data.clone() if hasattr(self.data, \"clone\") else copy.deepcopy(self.data)\n        return LatentState(data=data_copy, device=self.device)\n\n    def to(self, device: str) -&gt; LatentState:\n        r\"\"\"Move state to a different device (e.g. GPU offload to CPU).\"\"\"\n        if self.data is None or self.device == device:\n            return LatentState(data=self.data, device=device)\n        moved = self.data.to(device) if hasattr(self.data, \"to\") else self.data\n        return LatentState(data=moved, device=device)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.LatentState.data","title":"data  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>data: Any = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.LatentState.device","title":"device  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>device: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.LatentState.nbytes","title":"nbytes  <code>property</code>","text":"<pre><code>nbytes: int\n</code></pre> <p>Memory footprint in bytes.</p>"},{"location":"api/worldkernels/core/#worldkernels.core.LatentState.clone","title":"clone","text":"<pre><code>clone() -&gt; LatentState\n</code></pre> <p>Deep-copy this state, decoupling from the original tensor.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def clone(self) -&gt; LatentState:\n    r\"\"\"Deep-copy this state, decoupling from the original tensor.\"\"\"\n    if self.data is None:\n        return LatentState(data=None, device=self.device)\n    data_copy = self.data.clone() if hasattr(self.data, \"clone\") else copy.deepcopy(self.data)\n    return LatentState(data=data_copy, device=self.device)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.LatentState.to","title":"to","text":"<pre><code>to(device: str) -&gt; LatentState\n</code></pre> <p>Move state to a different device (e.g. GPU offload to CPU).</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def to(self, device: str) -&gt; LatentState:\n    r\"\"\"Move state to a different device (e.g. GPU offload to CPU).\"\"\"\n    if self.data is None or self.device == device:\n        return LatentState(data=self.data, device=device)\n    moved = self.data.to(device) if hasattr(self.data, \"to\") else self.data\n    return LatentState(data=moved, device=device)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.LatentState.__init__","title":"__init__","text":"<pre><code>__init__(data: Any = None, device: str = 'cpu') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session","title":"Session  <code>dataclass</code>","text":"<p>A stateful simulation session bound to a world model.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>@dataclass\nclass Session:\n    r\"\"\"A stateful simulation session bound to a world model.\"\"\"\n\n    session_id: str = field(default_factory=lambda: f\"sess_{uuid.uuid4().hex[:12]}\")\n    world_id: str = \"\"\n    config: WorldConfig | None = None\n\n    # Latent state (kept on GPU or offloaded)\n    state: LatentState = field(default_factory=LatentState)\n    step_index: int = 0\n\n    # Provenance\n    seed: int = 0\n    parent_session_id: str | None = None\n\n    # Lifecycle\n    created_at: datetime = field(default_factory=datetime.now)\n    last_active_at: datetime = field(default_factory=datetime.now)\n    status: SessionStatus = SessionStatus.ACTIVE\n\n    # Checkpoints: checkpoint_id -&gt; LatentState snapshot\n    _checkpoints: dict[str, LatentState] = field(default_factory=dict, repr=False)\n\n    # Runtime references (injected by WorldEngine, not serialized)\n    _world: WorldModel | None = field(default=None, repr=False)\n    _scheduler: Scheduler | None = field(default=None, repr=False)\n\n    # Per-session runtime overrides (SESSION_OVERRIDE_FIELDS subset)\n    overrides: dict | None = None\n\n    # ---- core hot path ---------------------------------------------------\n\n    def step(\n        self,\n        action: Action,\n        modalities: list[str] | None = None,\n        decode: bool = True,\n    ) -&gt; Observation:\n        r\"\"\"Execute one simulation step through the three-stage pipeline.\n\n        Args:\n            action: An Action instance describing the control input.\n            modalities: Which observation modalities to decode.\n                Defaults to ``[\"frames\"]``.\n            decode: If False, skip observation decoding (stage 3).\n\n        Returns:\n            An Observation with the requested modalities populated.\n        \"\"\"\n        from worldkernels.core.errors import SessionPausedError, SessionTerminatedError\n\n        if self.status == SessionStatus.TERMINATED:\n            raise SessionTerminatedError(self.session_id)\n        if self.status == SessionStatus.PAUSED:\n            raise SessionPausedError(self.session_id)\n\n        if self._world is None or self._scheduler is None:\n            raise RuntimeError(\n                \"Session not bound to a world model. \"\n                \"Create sessions via WorldEngine.create_session().\"\n            )\n\n        if modalities is None:\n            modalities = [\"frames\"]\n\n        new_state, obs = self._scheduler.step(\n            world=self._world,\n            state=self.state,\n            action=action,\n            modalities=modalities,\n            step_index=self.step_index,\n            decode=decode,\n            overrides=self.overrides,\n        )\n\n        self.state = new_state\n        self.step_index += 1\n        self.last_active_at = datetime.now()\n        return obs\n\n    # ---- checkpoint / branch / restore -----------------------------------\n\n    def checkpoint(self) -&gt; str:\n        r\"\"\"Snapshot current latent state, return checkpoint ID.\"\"\"\n        ckpt_id = f\"ckpt_{uuid.uuid4().hex[:8]}\"\n        self._checkpoints[ckpt_id] = self.state.clone()\n        return ckpt_id\n\n    def restore(self, checkpoint_id: str) -&gt; None:\n        r\"\"\"Restore session to a previously saved checkpoint.\"\"\"\n        if checkpoint_id not in self._checkpoints:\n            from worldkernels.core.errors import CheckpointNotFoundError\n\n            raise CheckpointNotFoundError(checkpoint_id, self.session_id)\n        self.state = self._checkpoints[checkpoint_id].clone()\n\n    def branch(self) -&gt; Session:\n        r\"\"\"Clone this session into a new independent session.\"\"\"\n        return Session(\n            world_id=self.world_id,\n            config=self.config,\n            state=self.state.clone(),\n            step_index=self.step_index,\n            seed=self.seed,\n            parent_session_id=self.session_id,\n            _world=self._world,\n            _scheduler=self._scheduler,\n        )\n\n    def close(self) -&gt; None:\n        self.status = SessionStatus.TERMINATED\n        self.state = LatentState()\n        self._checkpoints.clear()\n\n    def pause(self) -&gt; None:\n        r\"\"\"Pause this session (ACTIVE -&gt; PAUSED).\"\"\"\n        from worldkernels.core.errors import SessionTerminatedError\n\n        if self.status == SessionStatus.TERMINATED:\n            raise SessionTerminatedError(self.session_id)\n        self.status = SessionStatus.PAUSED\n\n    def resume(self) -&gt; None:\n        r\"\"\"Resume a paused session (PAUSED -&gt; ACTIVE).\"\"\"\n        from worldkernels.core.errors import SessionTerminatedError\n\n        if self.status == SessionStatus.TERMINATED:\n            raise SessionTerminatedError(self.session_id)\n        self.status = SessionStatus.ACTIVE\n        self.last_active_at = datetime.now()\n\n    @property\n    def id(self) -&gt; str:\n        return self.session_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.session_id","title":"session_id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>session_id: str = field(default_factory=lambda: f'sess_{uuid.uuid4().hex[:12]}')\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.world_id","title":"world_id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>world_id: str = ''\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.config","title":"config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>state: LatentState = field(default_factory=LatentState)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.step_index","title":"step_index  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>step_index: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>seed: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.parent_session_id","title":"parent_session_id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>parent_session_id: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.created_at","title":"created_at  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>created_at: datetime = field(default_factory=datetime.now)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.last_active_at","title":"last_active_at  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>last_active_at: datetime = field(default_factory=datetime.now)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.status","title":"status  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>status: SessionStatus = SessionStatus.ACTIVE\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session._checkpoints","title":"_checkpoints  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_checkpoints: dict[str, LatentState] = field(default_factory=dict, repr=False)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session._world","title":"_world  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_world: WorldModel | None = field(default=None, repr=False)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session._scheduler","title":"_scheduler  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_scheduler: Scheduler | None = field(default=None, repr=False)\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.overrides","title":"overrides  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>overrides: dict | None = None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.step","title":"step","text":"<pre><code>step(action: Action, modalities: list[str] | None = None, decode: bool = True) -&gt; Observation\n</code></pre> <p>Execute one simulation step through the three-stage pipeline.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Action</code> <p>An Action instance describing the control input.</p> required <code>modalities</code> <code>list[str] | None</code> <p>Which observation modalities to decode. Defaults to <code>[\"frames\"]</code>.</p> <code>None</code> <code>decode</code> <code>bool</code> <p>If False, skip observation decoding (stage 3).</p> <code>True</code> <p>Returns:</p> Type Description <code>Observation</code> <p>An Observation with the requested modalities populated.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def step(\n    self,\n    action: Action,\n    modalities: list[str] | None = None,\n    decode: bool = True,\n) -&gt; Observation:\n    r\"\"\"Execute one simulation step through the three-stage pipeline.\n\n    Args:\n        action: An Action instance describing the control input.\n        modalities: Which observation modalities to decode.\n            Defaults to ``[\"frames\"]``.\n        decode: If False, skip observation decoding (stage 3).\n\n    Returns:\n        An Observation with the requested modalities populated.\n    \"\"\"\n    from worldkernels.core.errors import SessionPausedError, SessionTerminatedError\n\n    if self.status == SessionStatus.TERMINATED:\n        raise SessionTerminatedError(self.session_id)\n    if self.status == SessionStatus.PAUSED:\n        raise SessionPausedError(self.session_id)\n\n    if self._world is None or self._scheduler is None:\n        raise RuntimeError(\n            \"Session not bound to a world model. \"\n            \"Create sessions via WorldEngine.create_session().\"\n        )\n\n    if modalities is None:\n        modalities = [\"frames\"]\n\n    new_state, obs = self._scheduler.step(\n        world=self._world,\n        state=self.state,\n        action=action,\n        modalities=modalities,\n        step_index=self.step_index,\n        decode=decode,\n        overrides=self.overrides,\n    )\n\n    self.state = new_state\n    self.step_index += 1\n    self.last_active_at = datetime.now()\n    return obs\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.checkpoint","title":"checkpoint","text":"<pre><code>checkpoint() -&gt; str\n</code></pre> <p>Snapshot current latent state, return checkpoint ID.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def checkpoint(self) -&gt; str:\n    r\"\"\"Snapshot current latent state, return checkpoint ID.\"\"\"\n    ckpt_id = f\"ckpt_{uuid.uuid4().hex[:8]}\"\n    self._checkpoints[ckpt_id] = self.state.clone()\n    return ckpt_id\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.restore","title":"restore","text":"<pre><code>restore(checkpoint_id: str) -&gt; None\n</code></pre> <p>Restore session to a previously saved checkpoint.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def restore(self, checkpoint_id: str) -&gt; None:\n    r\"\"\"Restore session to a previously saved checkpoint.\"\"\"\n    if checkpoint_id not in self._checkpoints:\n        from worldkernels.core.errors import CheckpointNotFoundError\n\n        raise CheckpointNotFoundError(checkpoint_id, self.session_id)\n    self.state = self._checkpoints[checkpoint_id].clone()\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.branch","title":"branch","text":"<pre><code>branch() -&gt; Session\n</code></pre> <p>Clone this session into a new independent session.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def branch(self) -&gt; Session:\n    r\"\"\"Clone this session into a new independent session.\"\"\"\n    return Session(\n        world_id=self.world_id,\n        config=self.config,\n        state=self.state.clone(),\n        step_index=self.step_index,\n        seed=self.seed,\n        parent_session_id=self.session_id,\n        _world=self._world,\n        _scheduler=self._scheduler,\n    )\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> Source code in <code>worldkernels/core/session.py</code> <pre><code>def close(self) -&gt; None:\n    self.status = SessionStatus.TERMINATED\n    self.state = LatentState()\n    self._checkpoints.clear()\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.pause","title":"pause","text":"<pre><code>pause() -&gt; None\n</code></pre> <p>Pause this session (ACTIVE -&gt; PAUSED).</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def pause(self) -&gt; None:\n    r\"\"\"Pause this session (ACTIVE -&gt; PAUSED).\"\"\"\n    from worldkernels.core.errors import SessionTerminatedError\n\n    if self.status == SessionStatus.TERMINATED:\n        raise SessionTerminatedError(self.session_id)\n    self.status = SessionStatus.PAUSED\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.resume","title":"resume","text":"<pre><code>resume() -&gt; None\n</code></pre> <p>Resume a paused session (PAUSED -&gt; ACTIVE).</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def resume(self) -&gt; None:\n    r\"\"\"Resume a paused session (PAUSED -&gt; ACTIVE).\"\"\"\n    from worldkernels.core.errors import SessionTerminatedError\n\n    if self.status == SessionStatus.TERMINATED:\n        raise SessionTerminatedError(self.session_id)\n    self.status = SessionStatus.ACTIVE\n    self.last_active_at = datetime.now()\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.Session.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str = (lambda: f'sess_{hex[:12]}')(), world_id: str = '', config: WorldConfig | None = None, state: LatentState = LatentState(), step_index: int = 0, seed: int = 0, parent_session_id: str | None = None, created_at: datetime = now(), last_active_at: datetime = now(), status: SessionStatus = ACTIVE, _checkpoints: dict[str, LatentState] = dict(), _world: WorldModel | None = None, _scheduler: Scheduler | None = None, overrides: dict | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionStatus","title":"SessionStatus","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> Source code in <code>worldkernels/core/session.py</code> <pre><code>class SessionStatus(str, Enum):\n    ACTIVE = \"active\"\n    PAUSED = \"paused\"\n    TERMINATED = \"terminated\"\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionStatus.ACTIVE","title":"ACTIVE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ACTIVE = 'active'\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionStatus.PAUSED","title":"PAUSED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PAUSED = 'paused'\n</code></pre>"},{"location":"api/worldkernels/core/#worldkernels.core.SessionStatus.TERMINATED","title":"TERMINATED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>TERMINATED = 'terminated'\n</code></pre>"},{"location":"api/worldkernels/core/action/","title":"action","text":""},{"location":"api/worldkernels/core/action/#worldkernels.core.action","title":"worldkernels.core.action","text":"<p>Action schema for world model inputs.</p>"},{"location":"api/worldkernels/core/action/#worldkernels.core.action.Action","title":"Action  <code>dataclass</code>","text":"<p>Generalized action container (control input to world).</p> Source code in <code>worldkernels/core/action.py</code> <pre><code>@dataclass\nclass Action:\n    r\"\"\"Generalized action container (control input to world).\"\"\"\n\n    action_type: str\n    payload: dict[str, Any] = field(default_factory=dict)\n    timestamp: float | None = None\n\n    def __post_init__(self) -&gt; None:\n        if not self.action_type:\n            raise ValueError(\"action_type cannot be empty\")\n</code></pre>"},{"location":"api/worldkernels/core/action/#worldkernels.core.action.Action.action_type","title":"action_type  <code>instance-attribute</code>","text":"<pre><code>action_type: str\n</code></pre>"},{"location":"api/worldkernels/core/action/#worldkernels.core.action.Action.payload","title":"payload  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>payload: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/core/action/#worldkernels.core.action.Action.timestamp","title":"timestamp  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timestamp: float | None = None\n</code></pre>"},{"location":"api/worldkernels/core/action/#worldkernels.core.action.Action.__init__","title":"__init__","text":"<pre><code>__init__(action_type: str, payload: dict[str, Any] = dict(), timestamp: float | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/action/#worldkernels.core.action.Action.__post_init__","title":"__post_init__","text":"<pre><code>__post_init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/core/action.py</code> <pre><code>def __post_init__(self) -&gt; None:\n    if not self.action_type:\n        raise ValueError(\"action_type cannot be empty\")\n</code></pre>"},{"location":"api/worldkernels/core/action/#worldkernels.core.action.load_action_chunks","title":"load_action_chunks","text":"<pre><code>load_action_chunks(path: str, steps: int, chunk_size: int = 12) -&gt; list[Any]\n</code></pre> <p>Load an action <code>.npy</code> into per-step chunks of <code>[chunk_size, action_dim]</code>.</p> <p>Accepts <code>[T, action_dim]</code> (split into <code>chunk_size</code>-frame chunks) or pre-chunked <code>[num_chunks, chunk_size, action_dim]</code>. DreamDojo GR-1 expects <code>action_dim=384</code>.</p> Source code in <code>worldkernels/core/action.py</code> <pre><code>def load_action_chunks(path: str, steps: int, chunk_size: int = 12) -&gt; list[Any]:\n    r\"\"\"Load an action ``.npy`` into per-step chunks of ``[chunk_size, action_dim]``.\n\n    Accepts ``[T, action_dim]`` (split into ``chunk_size``-frame chunks) or pre-chunked\n    ``[num_chunks, chunk_size, action_dim]``. DreamDojo GR-1 expects ``action_dim=384``.\n    \"\"\"\n    from pathlib import Path\n\n    import numpy as np\n\n    arr = np.load(Path(path).expanduser())\n    if arr.ndim == 3:\n        chunks = [arr[i] for i in range(arr.shape[0])]\n    elif arr.ndim == 2:\n        chunks = [arr[i : i + chunk_size] for i in range(0, len(arr), chunk_size)]\n    else:\n        raise ValueError(f\"actions must be 2D [T,D] or 3D [N,chunk,D], got shape {arr.shape}\")\n    return chunks[:steps] if steps &lt; len(chunks) else chunks\n</code></pre>"},{"location":"api/worldkernels/core/config/","title":"config","text":""},{"location":"api/worldkernels/core/config/#worldkernels.core.config","title":"worldkernels.core.config","text":"<p>Configuration dataclasses (moved to <code>config</code>).</p> <p>This module re-exports during the architecture restructure; importers are migrated to <code>config</code> and this file is then removed.</p>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WorldConfig', 'ServerConfig']\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig","title":"ServerConfig  <code>dataclass</code>","text":"<p>Configuration for the serving layer.</p> Source code in <code>worldkernels/config/server_config.py</code> <pre><code>@dataclass\nclass ServerConfig:\n    r\"\"\"Configuration for the serving layer.\"\"\"\n\n    host: str = \"0.0.0.0\"\n    port: int = 8000\n    max_sessions: int = 4\n    api_key: str | None = None\n    allowed_origins: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_methods: list[str] = field(default_factory=lambda: [\"*\"])\n    allowed_headers: list[str] = field(default_factory=lambda: [\"*\"])\n    allow_credentials: bool = False\n    ssl_keyfile: str | None = None\n    ssl_certfile: str | None = None\n    uvicorn_log_level: Literal[\"critical\", \"error\", \"warning\", \"info\", \"debug\", \"trace\"] = \"info\"\n    disable_access_log: bool = False\n    root_path: str = \"\"\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.host","title":"host  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>host: str = '0.0.0.0'\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.port","title":"port  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>port: int = 8000\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.max_sessions","title":"max_sessions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_sessions: int = 4\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.api_key","title":"api_key  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>api_key: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.allowed_origins","title":"allowed_origins  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_origins: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.allowed_methods","title":"allowed_methods  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_methods: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.allowed_headers","title":"allowed_headers  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allowed_headers: list[str] = field(default_factory=lambda: ['*'])\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.allow_credentials","title":"allow_credentials  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allow_credentials: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.ssl_keyfile","title":"ssl_keyfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_keyfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.ssl_certfile","title":"ssl_certfile  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ssl_certfile: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.uvicorn_log_level","title":"uvicorn_log_level  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info'\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.disable_access_log","title":"disable_access_log  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>disable_access_log: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.root_path","title":"root_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>root_path: str = ''\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.ServerConfig.__init__","title":"__init__","text":"<pre><code>__init__(host: str = '0.0.0.0', port: int = 8000, max_sessions: int = 4, api_key: str | None = None, allowed_origins: list[str] = (lambda: ['*'])(), allowed_methods: list[str] = (lambda: ['*'])(), allowed_headers: list[str] = (lambda: ['*'])(), allow_credentials: bool = False, ssl_keyfile: str | None = None, ssl_certfile: str | None = None, uvicorn_log_level: Literal['critical', 'error', 'warning', 'info', 'debug', 'trace'] = 'info', disable_access_log: bool = False, root_path: str = '') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig","title":"WorldConfig  <code>dataclass</code>","text":"<p>Per-session generation parameters.</p> <p>For bidirectional models, <code>frames_per_step</code> is the full chunk size. For causal models, <code>context_window</code> and <code>chunk_overlap</code> control streaming and KV cache behavior.</p> Source code in <code>worldkernels/config/world_config.py</code> <pre><code>@dataclass\nclass WorldConfig:\n    r\"\"\"Per-session generation parameters.\n\n    For bidirectional models, ``frames_per_step`` is the full chunk size.\n    For causal models, ``context_window`` and ``chunk_overlap`` control\n    streaming and KV cache behavior.\n    \"\"\"\n\n    height: int = 480\n    width: int = 848\n    fps: int = 24\n\n    num_inference_steps: int = 4\n    guidance_scale: float = 1.0\n\n    frames_per_step: int = 8\n    chunk_overlap: int = 0\n\n    context_window: int = 0\n    attention_sink_tokens: int = 0\n\n    initial_prompt: str | None = None\n    initial_image: str | None = None\n\n    max_vram_gb: float | None = None\n    precision: Literal[\"bf16\", \"fp16\", \"fp32\"] = \"bf16\"\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.height","title":"height  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>height: int = 480\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.width","title":"width  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>width: int = 848\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.fps","title":"fps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fps: int = 24\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.num_inference_steps","title":"num_inference_steps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_inference_steps: int = 4\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.guidance_scale","title":"guidance_scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>guidance_scale: float = 1.0\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.frames_per_step","title":"frames_per_step  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frames_per_step: int = 8\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.chunk_overlap","title":"chunk_overlap  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>chunk_overlap: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.context_window","title":"context_window  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>context_window: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.attention_sink_tokens","title":"attention_sink_tokens  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_sink_tokens: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.initial_prompt","title":"initial_prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_prompt: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.initial_image","title":"initial_image  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_image: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.max_vram_gb","title":"max_vram_gb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_vram_gb: float | None = None\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.precision","title":"precision  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16'\n</code></pre>"},{"location":"api/worldkernels/core/config/#worldkernels.core.config.WorldConfig.__init__","title":"__init__","text":"<pre><code>__init__(height: int = 480, width: int = 848, fps: int = 24, num_inference_steps: int = 4, guidance_scale: float = 1.0, frames_per_step: int = 8, chunk_overlap: int = 0, context_window: int = 0, attention_sink_tokens: int = 0, initial_prompt: str | None = None, initial_image: str | None = None, max_vram_gb: float | None = None, precision: Literal['bf16', 'fp16', 'fp32'] = 'bf16') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/errors/","title":"errors","text":""},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors","title":"worldkernels.core.errors","text":"<p>Exception hierarchy for WorldKernels.</p>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldKernelError","title":"WorldKernelError","text":"<p>               Bases: <code>Exception</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldKernelError(Exception):\n    pass\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldNotFoundError","title":"WorldNotFoundError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldNotFoundError(WorldKernelError):\n    def __init__(self, world_id: str) -&gt; None:\n        self.world_id = world_id\n        super().__init__(f\"World '{world_id}' not loaded. Call load_model() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldNotFoundError.world_id","title":"world_id  <code>instance-attribute</code>","text":"<pre><code>world_id = world_id\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldNotFoundError.__init__","title":"__init__","text":"<pre><code>__init__(world_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, world_id: str) -&gt; None:\n    self.world_id = world_id\n    super().__init__(f\"World '{world_id}' not loaded. Call load_model() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionLimitError","title":"SessionLimitError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionLimitError(WorldKernelError):\n    def __init__(self, max_sessions: int) -&gt; None:\n        self.max_sessions = max_sessions\n        super().__init__(f\"Max sessions ({max_sessions}) reached.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionLimitError.max_sessions","title":"max_sessions  <code>instance-attribute</code>","text":"<pre><code>max_sessions = max_sessions\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionLimitError.__init__","title":"__init__","text":"<pre><code>__init__(max_sessions: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, max_sessions: int) -&gt; None:\n    self.max_sessions = max_sessions\n    super().__init__(f\"Max sessions ({max_sessions}) reached.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionNotFoundError","title":"SessionNotFoundError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionNotFoundError(WorldKernelError):\n    def __init__(self, session_id: str) -&gt; None:\n        self.session_id = session_id\n        super().__init__(f\"Session '{session_id}' not found.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionNotFoundError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionNotFoundError.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, session_id: str) -&gt; None:\n    self.session_id = session_id\n    super().__init__(f\"Session '{session_id}' not found.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionTerminatedError","title":"SessionTerminatedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionTerminatedError(WorldKernelError):\n    def __init__(self, session_id: str) -&gt; None:\n        self.session_id = session_id\n        super().__init__(f\"Session '{session_id}' is terminated.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionTerminatedError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionTerminatedError.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, session_id: str) -&gt; None:\n    self.session_id = session_id\n    super().__init__(f\"Session '{session_id}' is terminated.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.VRAMExhaustedError","title":"VRAMExhaustedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class VRAMExhaustedError(WorldKernelError):\n    def __init__(self, required_mb: float, available_mb: float) -&gt; None:\n        self.required_mb = required_mb\n        self.available_mb = available_mb\n        super().__init__(\n            f\"Not enough VRAM: need {required_mb:.0f} MB, only {available_mb:.0f} MB available.\"\n        )\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.VRAMExhaustedError.required_mb","title":"required_mb  <code>instance-attribute</code>","text":"<pre><code>required_mb = required_mb\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.VRAMExhaustedError.available_mb","title":"available_mb  <code>instance-attribute</code>","text":"<pre><code>available_mb = available_mb\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.VRAMExhaustedError.__init__","title":"__init__","text":"<pre><code>__init__(required_mb: float, available_mb: float) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, required_mb: float, available_mb: float) -&gt; None:\n    self.required_mb = required_mb\n    self.available_mb = available_mb\n    super().__init__(\n        f\"Not enough VRAM: need {required_mb:.0f} MB, only {available_mb:.0f} MB available.\"\n    )\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldInitError","title":"WorldInitError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldInitError(WorldKernelError):\n    def __init__(self, world_id: str, reason: str) -&gt; None:\n        self.world_id = world_id\n        self.reason = reason\n        super().__init__(f\"Failed to initialize world '{world_id}': {reason}\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldInitError.world_id","title":"world_id  <code>instance-attribute</code>","text":"<pre><code>world_id = world_id\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldInitError.reason","title":"reason  <code>instance-attribute</code>","text":"<pre><code>reason = reason\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldInitError.__init__","title":"__init__","text":"<pre><code>__init__(world_id: str, reason: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, world_id: str, reason: str) -&gt; None:\n    self.world_id = world_id\n    self.reason = reason\n    super().__init__(f\"Failed to initialize world '{world_id}': {reason}\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldAlreadyLoadedError","title":"WorldAlreadyLoadedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class WorldAlreadyLoadedError(WorldKernelError):\n    def __init__(self, name: str) -&gt; None:\n        self.name = name\n        super().__init__(f\"World '{name}' is already loaded.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldAlreadyLoadedError.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name = name\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.WorldAlreadyLoadedError.__init__","title":"__init__","text":"<pre><code>__init__(name: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, name: str) -&gt; None:\n    self.name = name\n    super().__init__(f\"World '{name}' is already loaded.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.CheckpointNotFoundError","title":"CheckpointNotFoundError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class CheckpointNotFoundError(WorldKernelError):\n    def __init__(self, checkpoint_id: str, session_id: str) -&gt; None:\n        self.checkpoint_id = checkpoint_id\n        self.session_id = session_id\n        super().__init__(f\"Checkpoint '{checkpoint_id}' not found in session '{session_id}'.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.CheckpointNotFoundError.checkpoint_id","title":"checkpoint_id  <code>instance-attribute</code>","text":"<pre><code>checkpoint_id = checkpoint_id\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.CheckpointNotFoundError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.CheckpointNotFoundError.__init__","title":"__init__","text":"<pre><code>__init__(checkpoint_id: str, session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, checkpoint_id: str, session_id: str) -&gt; None:\n    self.checkpoint_id = checkpoint_id\n    self.session_id = session_id\n    super().__init__(f\"Checkpoint '{checkpoint_id}' not found in session '{session_id}'.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionPausedError","title":"SessionPausedError","text":"<p>               Bases: <code>WorldKernelError</code></p> Source code in <code>worldkernels/core/errors.py</code> <pre><code>class SessionPausedError(WorldKernelError):\n    def __init__(self, session_id: str) -&gt; None:\n        self.session_id = session_id\n        super().__init__(f\"Session '{session_id}' is paused. Call resume() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionPausedError.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id = session_id\n</code></pre>"},{"location":"api/worldkernels/core/errors/#worldkernels.core.errors.SessionPausedError.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/errors.py</code> <pre><code>def __init__(self, session_id: str) -&gt; None:\n    self.session_id = session_id\n    super().__init__(f\"Session '{session_id}' is paused. Call resume() first.\")\n</code></pre>"},{"location":"api/worldkernels/core/observation/","title":"observation","text":""},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation","title":"worldkernels.core.observation","text":"<p>Observation types returned by world models.</p>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation","title":"Observation  <code>dataclass</code>","text":"<p>Multi-modal observation output from a world model step.</p> Source code in <code>worldkernels/core/observation.py</code> <pre><code>@dataclass\nclass Observation:\n    r\"\"\"Multi-modal observation output from a world model step.\"\"\"\n\n    step_index: int\n    generation_time_ms: float\n    frames: Any | None = None\n    latent: Any | None = None\n    audio: Any | None = None\n    depth: Any | None = None\n    segmentation: Any | None = None\n    structured: dict[str, Any] | None = field(default_factory=dict)\n    stage_timing: StageTiming | None = None\n    decode_skipped: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.step_index","title":"step_index  <code>instance-attribute</code>","text":"<pre><code>step_index: int\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.generation_time_ms","title":"generation_time_ms  <code>instance-attribute</code>","text":"<pre><code>generation_time_ms: float\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.frames","title":"frames  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frames: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.latent","title":"latent  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>latent: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.audio","title":"audio  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>audio: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.depth","title":"depth  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>depth: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.segmentation","title":"segmentation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>segmentation: Any | None = None\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.structured","title":"structured  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>structured: dict[str, Any] | None = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.stage_timing","title":"stage_timing  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_timing: StageTiming | None = None\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.decode_skipped","title":"decode_skipped  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>decode_skipped: bool = False\n</code></pre>"},{"location":"api/worldkernels/core/observation/#worldkernels.core.observation.Observation.__init__","title":"__init__","text":"<pre><code>__init__(step_index: int, generation_time_ms: float, frames: Any | None = None, latent: Any | None = None, audio: Any | None = None, depth: Any | None = None, segmentation: Any | None = None, structured: dict[str, Any] | None = dict(), stage_timing: StageTiming | None = None, decode_skipped: bool = False) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/request/","title":"request","text":""},{"location":"api/worldkernels/core/request/#worldkernels.core.request","title":"worldkernels.core.request","text":"<p>A scheduled unit of execution work.</p>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['StepRequest']\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest","title":"StepRequest  <code>dataclass</code>","text":"<p>One simulation step queued for the scheduler.</p> <p>Carries everything the worker needs to advance one session by a step. Transient: the persistent <code>Session</code> state lives in the engine's session registry; the request only references it.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>Owning session (empty for engine-internal calls).</p> required <code>world</code> <code>WorldModel</code> <p>The world model to run.</p> required <code>state</code> <code>LatentState</code> <p>Current latent state.</p> required <code>action</code> <code>Action</code> <p>Action to apply.</p> required <code>modalities</code> <code>list[str]</code> <p>Observation modalities to decode.</p> <code>(lambda: ['frames'])()</code> <code>step_index</code> <code>int</code> <p>Monotonic step counter.</p> <code>0</code> <code>decode</code> <code>bool</code> <p>If False, skip the decode stage.</p> <code>True</code> Source code in <code>worldkernels/core/request.py</code> <pre><code>@dataclass\nclass StepRequest:\n    r\"\"\"One simulation step queued for the scheduler.\n\n    Carries everything the worker needs to advance one session by a step.\n    Transient: the persistent ``Session`` state lives in the engine's session\n    registry; the request only references it.\n\n    Args:\n        session_id: Owning session (empty for engine-internal calls).\n        world: The world model to run.\n        state: Current latent state.\n        action: Action to apply.\n        modalities: Observation modalities to decode.\n        step_index: Monotonic step counter.\n        decode: If False, skip the decode stage.\n    \"\"\"\n\n    session_id: str\n    world: WorldModel\n    state: LatentState\n    action: Action\n    modalities: list[str] = field(default_factory=lambda: [\"frames\"])\n    step_index: int = 0\n    decode: bool = True\n    overrides: dict | None = None\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id: str\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.world","title":"world  <code>instance-attribute</code>","text":"<pre><code>world: WorldModel\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.state","title":"state  <code>instance-attribute</code>","text":"<pre><code>state: LatentState\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.action","title":"action  <code>instance-attribute</code>","text":"<pre><code>action: Action\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.modalities","title":"modalities  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>modalities: list[str] = field(default_factory=lambda: ['frames'])\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.step_index","title":"step_index  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>step_index: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.decode","title":"decode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>decode: bool = True\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.overrides","title":"overrides  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>overrides: dict | None = None\n</code></pre>"},{"location":"api/worldkernels/core/request/#worldkernels.core.request.StepRequest.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str, world: WorldModel, state: LatentState, action: Action, modalities: list[str] = (lambda: ['frames'])(), step_index: int = 0, decode: bool = True, overrides: dict | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/session/","title":"session","text":""},{"location":"api/worldkernels/core/session/#worldkernels.core.session","title":"worldkernels.core.session","text":"<p>Session lifecycle management.</p>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.SessionStatus","title":"SessionStatus","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> Source code in <code>worldkernels/core/session.py</code> <pre><code>class SessionStatus(str, Enum):\n    ACTIVE = \"active\"\n    PAUSED = \"paused\"\n    TERMINATED = \"terminated\"\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.SessionStatus.ACTIVE","title":"ACTIVE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ACTIVE = 'active'\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.SessionStatus.PAUSED","title":"PAUSED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PAUSED = 'paused'\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.SessionStatus.TERMINATED","title":"TERMINATED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>TERMINATED = 'terminated'\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.LatentState","title":"LatentState  <code>dataclass</code>","text":"<p>Opaque tensor bundle representing the current world state.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>@dataclass\nclass LatentState:\n    r\"\"\"Opaque tensor bundle representing the current world state.\"\"\"\n\n    data: Any = None\n    device: str = \"cpu\"\n\n    @property\n    def nbytes(self) -&gt; int:\n        r\"\"\"Memory footprint in bytes.\"\"\"\n        if self.data is None:\n            return 0\n        if hasattr(self.data, \"nelement\") and hasattr(self.data, \"element_size\"):\n            return self.data.nelement() * self.data.element_size()\n        return 0\n\n    def clone(self) -&gt; LatentState:\n        r\"\"\"Deep-copy this state, decoupling from the original tensor.\"\"\"\n        if self.data is None:\n            return LatentState(data=None, device=self.device)\n        data_copy = self.data.clone() if hasattr(self.data, \"clone\") else copy.deepcopy(self.data)\n        return LatentState(data=data_copy, device=self.device)\n\n    def to(self, device: str) -&gt; LatentState:\n        r\"\"\"Move state to a different device (e.g. GPU offload to CPU).\"\"\"\n        if self.data is None or self.device == device:\n            return LatentState(data=self.data, device=device)\n        moved = self.data.to(device) if hasattr(self.data, \"to\") else self.data\n        return LatentState(data=moved, device=device)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.LatentState.data","title":"data  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>data: Any = None\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.LatentState.device","title":"device  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>device: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.LatentState.nbytes","title":"nbytes  <code>property</code>","text":"<pre><code>nbytes: int\n</code></pre> <p>Memory footprint in bytes.</p>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.LatentState.__init__","title":"__init__","text":"<pre><code>__init__(data: Any = None, device: str = 'cpu') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.LatentState.clone","title":"clone","text":"<pre><code>clone() -&gt; LatentState\n</code></pre> <p>Deep-copy this state, decoupling from the original tensor.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def clone(self) -&gt; LatentState:\n    r\"\"\"Deep-copy this state, decoupling from the original tensor.\"\"\"\n    if self.data is None:\n        return LatentState(data=None, device=self.device)\n    data_copy = self.data.clone() if hasattr(self.data, \"clone\") else copy.deepcopy(self.data)\n    return LatentState(data=data_copy, device=self.device)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.LatentState.to","title":"to","text":"<pre><code>to(device: str) -&gt; LatentState\n</code></pre> <p>Move state to a different device (e.g. GPU offload to CPU).</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def to(self, device: str) -&gt; LatentState:\n    r\"\"\"Move state to a different device (e.g. GPU offload to CPU).\"\"\"\n    if self.data is None or self.device == device:\n        return LatentState(data=self.data, device=device)\n    moved = self.data.to(device) if hasattr(self.data, \"to\") else self.data\n    return LatentState(data=moved, device=device)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session","title":"Session  <code>dataclass</code>","text":"<p>A stateful simulation session bound to a world model.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>@dataclass\nclass Session:\n    r\"\"\"A stateful simulation session bound to a world model.\"\"\"\n\n    session_id: str = field(default_factory=lambda: f\"sess_{uuid.uuid4().hex[:12]}\")\n    world_id: str = \"\"\n    config: WorldConfig | None = None\n\n    # Latent state (kept on GPU or offloaded)\n    state: LatentState = field(default_factory=LatentState)\n    step_index: int = 0\n\n    # Provenance\n    seed: int = 0\n    parent_session_id: str | None = None\n\n    # Lifecycle\n    created_at: datetime = field(default_factory=datetime.now)\n    last_active_at: datetime = field(default_factory=datetime.now)\n    status: SessionStatus = SessionStatus.ACTIVE\n\n    # Checkpoints: checkpoint_id -&gt; LatentState snapshot\n    _checkpoints: dict[str, LatentState] = field(default_factory=dict, repr=False)\n\n    # Runtime references (injected by WorldEngine, not serialized)\n    _world: WorldModel | None = field(default=None, repr=False)\n    _scheduler: Scheduler | None = field(default=None, repr=False)\n\n    # Per-session runtime overrides (SESSION_OVERRIDE_FIELDS subset)\n    overrides: dict | None = None\n\n    # ---- core hot path ---------------------------------------------------\n\n    def step(\n        self,\n        action: Action,\n        modalities: list[str] | None = None,\n        decode: bool = True,\n    ) -&gt; Observation:\n        r\"\"\"Execute one simulation step through the three-stage pipeline.\n\n        Args:\n            action: An Action instance describing the control input.\n            modalities: Which observation modalities to decode.\n                Defaults to ``[\"frames\"]``.\n            decode: If False, skip observation decoding (stage 3).\n\n        Returns:\n            An Observation with the requested modalities populated.\n        \"\"\"\n        from worldkernels.core.errors import SessionPausedError, SessionTerminatedError\n\n        if self.status == SessionStatus.TERMINATED:\n            raise SessionTerminatedError(self.session_id)\n        if self.status == SessionStatus.PAUSED:\n            raise SessionPausedError(self.session_id)\n\n        if self._world is None or self._scheduler is None:\n            raise RuntimeError(\n                \"Session not bound to a world model. \"\n                \"Create sessions via WorldEngine.create_session().\"\n            )\n\n        if modalities is None:\n            modalities = [\"frames\"]\n\n        new_state, obs = self._scheduler.step(\n            world=self._world,\n            state=self.state,\n            action=action,\n            modalities=modalities,\n            step_index=self.step_index,\n            decode=decode,\n            overrides=self.overrides,\n        )\n\n        self.state = new_state\n        self.step_index += 1\n        self.last_active_at = datetime.now()\n        return obs\n\n    # ---- checkpoint / branch / restore -----------------------------------\n\n    def checkpoint(self) -&gt; str:\n        r\"\"\"Snapshot current latent state, return checkpoint ID.\"\"\"\n        ckpt_id = f\"ckpt_{uuid.uuid4().hex[:8]}\"\n        self._checkpoints[ckpt_id] = self.state.clone()\n        return ckpt_id\n\n    def restore(self, checkpoint_id: str) -&gt; None:\n        r\"\"\"Restore session to a previously saved checkpoint.\"\"\"\n        if checkpoint_id not in self._checkpoints:\n            from worldkernels.core.errors import CheckpointNotFoundError\n\n            raise CheckpointNotFoundError(checkpoint_id, self.session_id)\n        self.state = self._checkpoints[checkpoint_id].clone()\n\n    def branch(self) -&gt; Session:\n        r\"\"\"Clone this session into a new independent session.\"\"\"\n        return Session(\n            world_id=self.world_id,\n            config=self.config,\n            state=self.state.clone(),\n            step_index=self.step_index,\n            seed=self.seed,\n            parent_session_id=self.session_id,\n            _world=self._world,\n            _scheduler=self._scheduler,\n        )\n\n    def close(self) -&gt; None:\n        self.status = SessionStatus.TERMINATED\n        self.state = LatentState()\n        self._checkpoints.clear()\n\n    def pause(self) -&gt; None:\n        r\"\"\"Pause this session (ACTIVE -&gt; PAUSED).\"\"\"\n        from worldkernels.core.errors import SessionTerminatedError\n\n        if self.status == SessionStatus.TERMINATED:\n            raise SessionTerminatedError(self.session_id)\n        self.status = SessionStatus.PAUSED\n\n    def resume(self) -&gt; None:\n        r\"\"\"Resume a paused session (PAUSED -&gt; ACTIVE).\"\"\"\n        from worldkernels.core.errors import SessionTerminatedError\n\n        if self.status == SessionStatus.TERMINATED:\n            raise SessionTerminatedError(self.session_id)\n        self.status = SessionStatus.ACTIVE\n        self.last_active_at = datetime.now()\n\n    @property\n    def id(self) -&gt; str:\n        return self.session_id\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.session_id","title":"session_id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>session_id: str = field(default_factory=lambda: f'sess_{uuid.uuid4().hex[:12]}')\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.world_id","title":"world_id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>world_id: str = ''\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.config","title":"config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.state","title":"state  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>state: LatentState = field(default_factory=LatentState)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.step_index","title":"step_index  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>step_index: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>seed: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.parent_session_id","title":"parent_session_id  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>parent_session_id: str | None = None\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.created_at","title":"created_at  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>created_at: datetime = field(default_factory=datetime.now)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.last_active_at","title":"last_active_at  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>last_active_at: datetime = field(default_factory=datetime.now)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.status","title":"status  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>status: SessionStatus = SessionStatus.ACTIVE\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session._checkpoints","title":"_checkpoints  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_checkpoints: dict[str, LatentState] = field(default_factory=dict, repr=False)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session._world","title":"_world  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_world: WorldModel | None = field(default=None, repr=False)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session._scheduler","title":"_scheduler  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_scheduler: Scheduler | None = field(default=None, repr=False)\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.overrides","title":"overrides  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>overrides: dict | None = None\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.id","title":"id  <code>property</code>","text":"<pre><code>id: str\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str = (lambda: f'sess_{hex[:12]}')(), world_id: str = '', config: WorldConfig | None = None, state: LatentState = LatentState(), step_index: int = 0, seed: int = 0, parent_session_id: str | None = None, created_at: datetime = now(), last_active_at: datetime = now(), status: SessionStatus = ACTIVE, _checkpoints: dict[str, LatentState] = dict(), _world: WorldModel | None = None, _scheduler: Scheduler | None = None, overrides: dict | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.step","title":"step","text":"<pre><code>step(action: Action, modalities: list[str] | None = None, decode: bool = True) -&gt; Observation\n</code></pre> <p>Execute one simulation step through the three-stage pipeline.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Action</code> <p>An Action instance describing the control input.</p> required <code>modalities</code> <code>list[str] | None</code> <p>Which observation modalities to decode. Defaults to <code>[\"frames\"]</code>.</p> <code>None</code> <code>decode</code> <code>bool</code> <p>If False, skip observation decoding (stage 3).</p> <code>True</code> <p>Returns:</p> Type Description <code>Observation</code> <p>An Observation with the requested modalities populated.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def step(\n    self,\n    action: Action,\n    modalities: list[str] | None = None,\n    decode: bool = True,\n) -&gt; Observation:\n    r\"\"\"Execute one simulation step through the three-stage pipeline.\n\n    Args:\n        action: An Action instance describing the control input.\n        modalities: Which observation modalities to decode.\n            Defaults to ``[\"frames\"]``.\n        decode: If False, skip observation decoding (stage 3).\n\n    Returns:\n        An Observation with the requested modalities populated.\n    \"\"\"\n    from worldkernels.core.errors import SessionPausedError, SessionTerminatedError\n\n    if self.status == SessionStatus.TERMINATED:\n        raise SessionTerminatedError(self.session_id)\n    if self.status == SessionStatus.PAUSED:\n        raise SessionPausedError(self.session_id)\n\n    if self._world is None or self._scheduler is None:\n        raise RuntimeError(\n            \"Session not bound to a world model. \"\n            \"Create sessions via WorldEngine.create_session().\"\n        )\n\n    if modalities is None:\n        modalities = [\"frames\"]\n\n    new_state, obs = self._scheduler.step(\n        world=self._world,\n        state=self.state,\n        action=action,\n        modalities=modalities,\n        step_index=self.step_index,\n        decode=decode,\n        overrides=self.overrides,\n    )\n\n    self.state = new_state\n    self.step_index += 1\n    self.last_active_at = datetime.now()\n    return obs\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.checkpoint","title":"checkpoint","text":"<pre><code>checkpoint() -&gt; str\n</code></pre> <p>Snapshot current latent state, return checkpoint ID.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def checkpoint(self) -&gt; str:\n    r\"\"\"Snapshot current latent state, return checkpoint ID.\"\"\"\n    ckpt_id = f\"ckpt_{uuid.uuid4().hex[:8]}\"\n    self._checkpoints[ckpt_id] = self.state.clone()\n    return ckpt_id\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.restore","title":"restore","text":"<pre><code>restore(checkpoint_id: str) -&gt; None\n</code></pre> <p>Restore session to a previously saved checkpoint.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def restore(self, checkpoint_id: str) -&gt; None:\n    r\"\"\"Restore session to a previously saved checkpoint.\"\"\"\n    if checkpoint_id not in self._checkpoints:\n        from worldkernels.core.errors import CheckpointNotFoundError\n\n        raise CheckpointNotFoundError(checkpoint_id, self.session_id)\n    self.state = self._checkpoints[checkpoint_id].clone()\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.branch","title":"branch","text":"<pre><code>branch() -&gt; Session\n</code></pre> <p>Clone this session into a new independent session.</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def branch(self) -&gt; Session:\n    r\"\"\"Clone this session into a new independent session.\"\"\"\n    return Session(\n        world_id=self.world_id,\n        config=self.config,\n        state=self.state.clone(),\n        step_index=self.step_index,\n        seed=self.seed,\n        parent_session_id=self.session_id,\n        _world=self._world,\n        _scheduler=self._scheduler,\n    )\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> Source code in <code>worldkernels/core/session.py</code> <pre><code>def close(self) -&gt; None:\n    self.status = SessionStatus.TERMINATED\n    self.state = LatentState()\n    self._checkpoints.clear()\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.pause","title":"pause","text":"<pre><code>pause() -&gt; None\n</code></pre> <p>Pause this session (ACTIVE -&gt; PAUSED).</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def pause(self) -&gt; None:\n    r\"\"\"Pause this session (ACTIVE -&gt; PAUSED).\"\"\"\n    from worldkernels.core.errors import SessionTerminatedError\n\n    if self.status == SessionStatus.TERMINATED:\n        raise SessionTerminatedError(self.session_id)\n    self.status = SessionStatus.PAUSED\n</code></pre>"},{"location":"api/worldkernels/core/session/#worldkernels.core.session.Session.resume","title":"resume","text":"<pre><code>resume() -&gt; None\n</code></pre> <p>Resume a paused session (PAUSED -&gt; ACTIVE).</p> Source code in <code>worldkernels/core/session.py</code> <pre><code>def resume(self) -&gt; None:\n    r\"\"\"Resume a paused session (PAUSED -&gt; ACTIVE).\"\"\"\n    from worldkernels.core.errors import SessionTerminatedError\n\n    if self.status == SessionStatus.TERMINATED:\n        raise SessionTerminatedError(self.session_id)\n    self.status = SessionStatus.ACTIVE\n    self.last_active_at = datetime.now()\n</code></pre>"},{"location":"api/worldkernels/core/state/","title":"state","text":""},{"location":"api/worldkernels/core/state/#worldkernels.core.state","title":"worldkernels.core.state","text":"<p>Structured world-model state.</p> <p>Replaces the opaque <code>LatentState.data: Any</code> with a typed container that supports cross-session batching, block-paged latent storage, and KV caching for causal world models, while keeping a tensor-only <code>extras</code> escape hatch for model-specific state.</p> <p><code>BlockHandle</code> and <code>KVHandle</code> are handles: the world state holds a reference to storage owned by the runtime, not raw tensors. During the restructure they are pass-through wrappers; the block-paged implementations land with the memory subsystem.</p>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['BlockHandle', 'KVHandle', 'ConditioningBundle', 'WorldStateMeta', 'WorldState']\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.BlockHandle","title":"BlockHandle","text":"<p>Handle to block-paged latent storage.</p> <p>Pass-through wrapper over a single tensor for now; the block-table implementation backed by the runtime block allocator replaces the body without changing this interface.</p> Source code in <code>worldkernels/core/state.py</code> <pre><code>class BlockHandle:\n    r\"\"\"Handle to block-paged latent storage.\n\n    Pass-through wrapper over a single tensor for now; the block-table\n    implementation backed by the runtime block allocator replaces the body\n    without changing this interface.\n    \"\"\"\n\n    __slots__ = (\"tensor\",)\n\n    def __init__(self, tensor: \"torch.Tensor\") -&gt; None:\n        self.tensor = tensor\n\n    def clone(self) -&gt; \"BlockHandle\":\n        return BlockHandle(self.tensor.clone())\n\n    def to(self, device: Any) -&gt; \"BlockHandle\":\n        return BlockHandle(self.tensor.to(device))\n\n    @property\n    def nbytes(self) -&gt; int:\n        return self.tensor.nelement() * self.tensor.element_size()\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.BlockHandle.__slots__","title":"__slots__  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>__slots__ = ('tensor',)\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.BlockHandle.tensor","title":"tensor  <code>instance-attribute</code>","text":"<pre><code>tensor = tensor\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.BlockHandle.nbytes","title":"nbytes  <code>property</code>","text":"<pre><code>nbytes: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.BlockHandle.__init__","title":"__init__","text":"<pre><code>__init__(tensor: 'torch.Tensor') -&gt; None\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def __init__(self, tensor: \"torch.Tensor\") -&gt; None:\n    self.tensor = tensor\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.BlockHandle.clone","title":"clone","text":"<pre><code>clone() -&gt; 'BlockHandle'\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def clone(self) -&gt; \"BlockHandle\":\n    return BlockHandle(self.tensor.clone())\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.BlockHandle.to","title":"to","text":"<pre><code>to(device: Any) -&gt; 'BlockHandle'\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def to(self, device: Any) -&gt; \"BlockHandle\":\n    return BlockHandle(self.tensor.to(device))\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.KVHandle","title":"KVHandle","text":"<p>Handle to a paged KV cache for causal world models.</p> <p>Pass-through wrapper for now; <code>None</code> on bidirectional worlds, which pay no KV-cache cost.</p> Source code in <code>worldkernels/core/state.py</code> <pre><code>class KVHandle:\n    r\"\"\"Handle to a paged KV cache for causal world models.\n\n    Pass-through wrapper for now; ``None`` on bidirectional worlds, which pay\n    no KV-cache cost.\n    \"\"\"\n\n    __slots__ = (\"layers\",)\n\n    def __init__(self, layers: list[\"torch.Tensor\"] | None = None) -&gt; None:\n        self.layers = layers or []\n\n    def clone(self) -&gt; \"KVHandle\":\n        return KVHandle([t.clone() for t in self.layers])\n\n    def to(self, device: Any) -&gt; \"KVHandle\":\n        return KVHandle([t.to(device) for t in self.layers])\n\n    @property\n    def nbytes(self) -&gt; int:\n        return sum(t.nelement() * t.element_size() for t in self.layers)\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.KVHandle.__slots__","title":"__slots__  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>__slots__ = ('layers',)\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.KVHandle.layers","title":"layers  <code>instance-attribute</code>","text":"<pre><code>layers = layers or []\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.KVHandle.nbytes","title":"nbytes  <code>property</code>","text":"<pre><code>nbytes: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.KVHandle.__init__","title":"__init__","text":"<pre><code>__init__(layers: list['torch.Tensor'] | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def __init__(self, layers: list[\"torch.Tensor\"] | None = None) -&gt; None:\n    self.layers = layers or []\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.KVHandle.clone","title":"clone","text":"<pre><code>clone() -&gt; 'KVHandle'\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def clone(self) -&gt; \"KVHandle\":\n    return KVHandle([t.clone() for t in self.layers])\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.KVHandle.to","title":"to","text":"<pre><code>to(device: Any) -&gt; 'KVHandle'\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def to(self, device: Any) -&gt; \"KVHandle\":\n    return KVHandle([t.to(device) for t in self.layers])\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle","title":"ConditioningBundle  <code>dataclass</code>","text":"<p>Conditioning signals common to video world models.</p> <p>Parameters:</p> Name Type Description Default <code>text_emb</code> <code>'torch.Tensor | None'</code> <p>Encoded prompt embedding.</p> <code>None</code> <code>neg_text_emb</code> <code>'torch.Tensor | None'</code> <p>Encoded negative-prompt embedding (for CFG).</p> <code>None</code> <code>image_cond</code> <code>'torch.Tensor | None'</code> <p>Encoded image-conditioning tensor (the rollout's last frame).</p> <code>None</code> Source code in <code>worldkernels/core/state.py</code> <pre><code>@dataclass\nclass ConditioningBundle:\n    r\"\"\"Conditioning signals common to video world models.\n\n    Args:\n        text_emb: Encoded prompt embedding.\n        neg_text_emb: Encoded negative-prompt embedding (for CFG).\n        image_cond: Encoded image-conditioning tensor (the rollout's last frame).\n    \"\"\"\n\n    text_emb: \"torch.Tensor | None\" = None\n    neg_text_emb: \"torch.Tensor | None\" = None\n    image_cond: \"torch.Tensor | None\" = None\n\n    def clone(self) -&gt; \"ConditioningBundle\":\n        return ConditioningBundle(\n            text_emb=None if self.text_emb is None else self.text_emb.clone(),\n            neg_text_emb=None if self.neg_text_emb is None else self.neg_text_emb.clone(),\n            image_cond=None if self.image_cond is None else self.image_cond.clone(),\n        )\n\n    def to(self, device: Any) -&gt; \"ConditioningBundle\":\n        return ConditioningBundle(\n            text_emb=None if self.text_emb is None else self.text_emb.to(device),\n            neg_text_emb=None if self.neg_text_emb is None else self.neg_text_emb.to(device),\n            image_cond=None if self.image_cond is None else self.image_cond.to(device),\n        )\n\n    @property\n    def nbytes(self) -&gt; int:\n        total = 0\n        for t in (self.text_emb, self.neg_text_emb, self.image_cond):\n            if t is not None:\n                total += t.nelement() * t.element_size()\n        return total\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle.text_emb","title":"text_emb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>text_emb: 'torch.Tensor | None' = None\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle.neg_text_emb","title":"neg_text_emb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>neg_text_emb: 'torch.Tensor | None' = None\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle.image_cond","title":"image_cond  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>image_cond: 'torch.Tensor | None' = None\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle.nbytes","title":"nbytes  <code>property</code>","text":"<pre><code>nbytes: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle.__init__","title":"__init__","text":"<pre><code>__init__(text_emb: 'torch.Tensor | None' = None, neg_text_emb: 'torch.Tensor | None' = None, image_cond: 'torch.Tensor | None' = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle.clone","title":"clone","text":"<pre><code>clone() -&gt; 'ConditioningBundle'\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def clone(self) -&gt; \"ConditioningBundle\":\n    return ConditioningBundle(\n        text_emb=None if self.text_emb is None else self.text_emb.clone(),\n        neg_text_emb=None if self.neg_text_emb is None else self.neg_text_emb.clone(),\n        image_cond=None if self.image_cond is None else self.image_cond.clone(),\n    )\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.ConditioningBundle.to","title":"to","text":"<pre><code>to(device: Any) -&gt; 'ConditioningBundle'\n</code></pre> Source code in <code>worldkernels/core/state.py</code> <pre><code>def to(self, device: Any) -&gt; \"ConditioningBundle\":\n    return ConditioningBundle(\n        text_emb=None if self.text_emb is None else self.text_emb.to(device),\n        neg_text_emb=None if self.neg_text_emb is None else self.neg_text_emb.to(device),\n        image_cond=None if self.image_cond is None else self.image_cond.to(device),\n    )\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldStateMeta","title":"WorldStateMeta  <code>dataclass</code>","text":"<p>Shape and regime metadata; the scheduler reads it to bucket sessions.</p> Source code in <code>worldkernels/core/state.py</code> <pre><code>@dataclass\nclass WorldStateMeta:\n    r\"\"\"Shape and regime metadata; the scheduler reads it to bucket sessions.\"\"\"\n\n    height: int\n    width: int\n    dtype: str\n    transition_mode: str\n    frames_per_step: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldStateMeta.height","title":"height  <code>instance-attribute</code>","text":"<pre><code>height: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldStateMeta.width","title":"width  <code>instance-attribute</code>","text":"<pre><code>width: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldStateMeta.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: str\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldStateMeta.transition_mode","title":"transition_mode  <code>instance-attribute</code>","text":"<pre><code>transition_mode: str\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldStateMeta.frames_per_step","title":"frames_per_step  <code>instance-attribute</code>","text":"<pre><code>frames_per_step: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldStateMeta.__init__","title":"__init__","text":"<pre><code>__init__(height: int, width: int, dtype: str, transition_mode: str, frames_per_step: int) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState","title":"WorldState  <code>dataclass</code>","text":"<p>Structured per-session world-model state.</p> <p>Parameters:</p> Name Type Description Default <code>latent</code> <code>BlockHandle</code> <p>Handle to the current latent (block-paged storage).</p> required <code>conditioning</code> <code>ConditioningBundle</code> <p>Conditioning signals for the next transition.</p> required <code>meta</code> <code>WorldStateMeta</code> <p>Shape and regime metadata.</p> required <code>kv_cache</code> <code>KVHandle | None</code> <p>Paged KV cache for causal worlds; <code>None</code> for bidirectional.</p> <code>None</code> <code>step_index</code> <code>int</code> <p>Monotonic simulation step counter.</p> <code>0</code> <code>extras</code> <code>dict[str, 'torch.Tensor']</code> <p>Model-specific tensors that do not fit the typed fields.</p> <code>dict()</code> Source code in <code>worldkernels/core/state.py</code> <pre><code>@dataclass\nclass WorldState:\n    r\"\"\"Structured per-session world-model state.\n\n    Args:\n        latent: Handle to the current latent (block-paged storage).\n        conditioning: Conditioning signals for the next transition.\n        meta: Shape and regime metadata.\n        kv_cache: Paged KV cache for causal worlds; ``None`` for bidirectional.\n        step_index: Monotonic simulation step counter.\n        extras: Model-specific tensors that do not fit the typed fields.\n    \"\"\"\n\n    latent: BlockHandle\n    conditioning: ConditioningBundle\n    meta: WorldStateMeta\n    kv_cache: KVHandle | None = None\n    step_index: int = 0\n    extras: dict[str, \"torch.Tensor\"] = field(default_factory=dict)\n\n    def clone(self) -&gt; \"WorldState\":\n        r\"\"\"Copy-on-write clone for session branching.\"\"\"\n        return WorldState(\n            latent=self.latent.clone(),\n            conditioning=self.conditioning.clone(),\n            meta=self.meta,\n            kv_cache=None if self.kv_cache is None else self.kv_cache.clone(),\n            step_index=self.step_index,\n            extras={k: v.clone() for k, v in self.extras.items()},\n        )\n\n    def to(self, device: Any) -&gt; \"WorldState\":\n        r\"\"\"Move all tensors to ``device`` (or an offload tier).\"\"\"\n        return WorldState(\n            latent=self.latent.to(device),\n            conditioning=self.conditioning.to(device),\n            meta=self.meta,\n            kv_cache=None if self.kv_cache is None else self.kv_cache.to(device),\n            step_index=self.step_index,\n            extras={k: v.to(device) for k, v in self.extras.items()},\n        )\n\n    @property\n    def nbytes(self) -&gt; int:\n        total = self.latent.nbytes + self.conditioning.nbytes\n        if self.kv_cache is not None:\n            total += self.kv_cache.nbytes\n        total += sum(v.nelement() * v.element_size() for v in self.extras.values())\n        return total\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.latent","title":"latent  <code>instance-attribute</code>","text":"<pre><code>latent: BlockHandle\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.conditioning","title":"conditioning  <code>instance-attribute</code>","text":"<pre><code>conditioning: ConditioningBundle\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.meta","title":"meta  <code>instance-attribute</code>","text":"<pre><code>meta: WorldStateMeta\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.kv_cache","title":"kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kv_cache: KVHandle | None = None\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.step_index","title":"step_index  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>step_index: int = 0\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.extras","title":"extras  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>extras: dict[str, 'torch.Tensor'] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.nbytes","title":"nbytes  <code>property</code>","text":"<pre><code>nbytes: int\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.__init__","title":"__init__","text":"<pre><code>__init__(latent: BlockHandle, conditioning: ConditioningBundle, meta: WorldStateMeta, kv_cache: KVHandle | None = None, step_index: int = 0, extras: dict[str, 'torch.Tensor'] = dict()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.clone","title":"clone","text":"<pre><code>clone() -&gt; 'WorldState'\n</code></pre> <p>Copy-on-write clone for session branching.</p> Source code in <code>worldkernels/core/state.py</code> <pre><code>def clone(self) -&gt; \"WorldState\":\n    r\"\"\"Copy-on-write clone for session branching.\"\"\"\n    return WorldState(\n        latent=self.latent.clone(),\n        conditioning=self.conditioning.clone(),\n        meta=self.meta,\n        kv_cache=None if self.kv_cache is None else self.kv_cache.clone(),\n        step_index=self.step_index,\n        extras={k: v.clone() for k, v in self.extras.items()},\n    )\n</code></pre>"},{"location":"api/worldkernels/core/state/#worldkernels.core.state.WorldState.to","title":"to","text":"<pre><code>to(device: Any) -&gt; 'WorldState'\n</code></pre> <p>Move all tensors to <code>device</code> (or an offload tier).</p> Source code in <code>worldkernels/core/state.py</code> <pre><code>def to(self, device: Any) -&gt; \"WorldState\":\n    r\"\"\"Move all tensors to ``device`` (or an offload tier).\"\"\"\n    return WorldState(\n        latent=self.latent.to(device),\n        conditioning=self.conditioning.to(device),\n        meta=self.meta,\n        kv_cache=None if self.kv_cache is None else self.kv_cache.to(device),\n        step_index=self.step_index,\n        extras={k: v.to(device) for k, v in self.extras.items()},\n    )\n</code></pre>"},{"location":"api/worldkernels/distributed/","title":"distributed","text":""},{"location":"api/worldkernels/distributed/#worldkernels.distributed","title":"worldkernels.distributed","text":"<p>Distributed-execution primitives: parallel state, collectives, CFG/sequence parallelism.</p> <p>Parallel-state queries are torch-free and eagerly exported; the collective and parallel-attention helpers import torch and are resolved lazily.</p> <p>Modules:</p> Name Description <code>cfg_parallel</code> <p>Classifier-free-guidance parallelism.</p> <code>communication</code> <p>Collective communication wrappers.</p> <code>parallel_state</code> <p>Distributed parallel-state registry.</p> <code>sequence_parallel</code> <p>Sequence-parallel primitives: Ulysses all-to-all and ring rotation.</p>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['init_distributed', 'destroy_distributed', 'is_initialized', 'get_parallel_config', 'get_global_rank', 'get_world_size', 'get_tensor_parallel_rank', 'get_tensor_parallel_world_size', 'get_tensor_parallel_group', 'get_sequence_parallel_rank', 'get_sequence_parallel_world_size', 'get_sequence_parallel_group', 'get_cfg_parallel_rank', 'get_cfg_parallel_world_size', 'get_cfg_parallel_group', 'tensor_parallel_all_reduce', 'tensor_parallel_all_gather', 'CFGParallelMixin', 'ulysses_all_to_all', 'ring_rotate']\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'tensor_parallel_all_reduce': ('worldkernels.distributed.communication', 'tensor_parallel_all_reduce'), 'tensor_parallel_all_gather': ('worldkernels.distributed.communication', 'tensor_parallel_all_gather'), 'CFGParallelMixin': ('worldkernels.distributed.cfg_parallel', 'CFGParallelMixin'), 'ulysses_all_to_all': ('worldkernels.distributed.sequence_parallel', 'ulysses_all_to_all'), 'ring_rotate': ('worldkernels.distributed.sequence_parallel', 'ring_rotate')}\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.destroy_distributed","title":"destroy_distributed","text":"<pre><code>destroy_distributed() -&gt; None\n</code></pre> <p>Tear down process groups and reset to single-rank identity.</p> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def destroy_distributed() -&gt; None:\n    r\"\"\"Tear down process groups and reset to single-rank identity.\"\"\"\n    if _STATE.world_size &gt; 1:\n        import torch.distributed as dist\n\n        if dist.is_initialized():\n            dist.destroy_process_group()\n    _STATE.__dict__.update(_ParallelState().__dict__)\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_cfg_parallel_group","title":"get_cfg_parallel_group","text":"<pre><code>get_cfg_parallel_group() -&gt; Any\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_cfg_parallel_group() -&gt; Any:\n    return _STATE.cfg.group\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_cfg_parallel_rank","title":"get_cfg_parallel_rank","text":"<pre><code>get_cfg_parallel_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_cfg_parallel_rank() -&gt; int:\n    return _STATE.cfg.rank\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_cfg_parallel_world_size","title":"get_cfg_parallel_world_size","text":"<pre><code>get_cfg_parallel_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_cfg_parallel_world_size() -&gt; int:\n    return _STATE.cfg.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_global_rank","title":"get_global_rank","text":"<pre><code>get_global_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_global_rank() -&gt; int:\n    return _STATE.global_rank\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_parallel_config","title":"get_parallel_config","text":"<pre><code>get_parallel_config() -&gt; ParallelConfig\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_parallel_config() -&gt; ParallelConfig:\n    return _STATE.config\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_sequence_parallel_group","title":"get_sequence_parallel_group","text":"<pre><code>get_sequence_parallel_group() -&gt; Any\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_sequence_parallel_group() -&gt; Any:\n    return _STATE.sequence.group\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_sequence_parallel_rank","title":"get_sequence_parallel_rank","text":"<pre><code>get_sequence_parallel_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_sequence_parallel_rank() -&gt; int:\n    return _STATE.sequence.rank\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_sequence_parallel_world_size","title":"get_sequence_parallel_world_size","text":"<pre><code>get_sequence_parallel_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_sequence_parallel_world_size() -&gt; int:\n    return _STATE.sequence.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_tensor_parallel_group","title":"get_tensor_parallel_group","text":"<pre><code>get_tensor_parallel_group() -&gt; Any\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_tensor_parallel_group() -&gt; Any:\n    return _STATE.tensor.group\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_tensor_parallel_rank","title":"get_tensor_parallel_rank","text":"<pre><code>get_tensor_parallel_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_tensor_parallel_rank() -&gt; int:\n    return _STATE.tensor.rank\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_tensor_parallel_world_size","title":"get_tensor_parallel_world_size","text":"<pre><code>get_tensor_parallel_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_tensor_parallel_world_size() -&gt; int:\n    return _STATE.tensor.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.get_world_size","title":"get_world_size","text":"<pre><code>get_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_world_size() -&gt; int:\n    return _STATE.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.init_distributed","title":"init_distributed","text":"<pre><code>init_distributed(config: ParallelConfig, *, backend: str = 'nccl') -&gt; None\n</code></pre> <p>Initialize process groups for the parallel <code>config</code>.</p> <p>Single-rank configs initialize identity groups without touching <code>torch.distributed</code>. Multi-rank configs initialize the process group (rank and world size read from the <code>RANK</code> / <code>WORLD_SIZE</code> environment) and build the per-axis sub-groups.</p> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def init_distributed(\n    config: ParallelConfig,\n    *,\n    backend: str = \"nccl\",\n) -&gt; None:\n    r\"\"\"Initialize process groups for the parallel ``config``.\n\n    Single-rank configs initialize identity groups without touching\n    ``torch.distributed``. Multi-rank configs initialize the process group\n    (rank and world size read from the ``RANK`` / ``WORLD_SIZE`` environment)\n    and build the per-axis sub-groups.\n    \"\"\"\n    _STATE.config = config\n    _STATE.world_size = config.world_size\n\n    if config.world_size == 1:\n        _STATE.global_rank = 0\n        _STATE.tensor = _GroupInfo()\n        _STATE.sequence = _GroupInfo()\n        _STATE.cfg = _GroupInfo()\n        _STATE.initialized = True\n        return\n\n    import torch.distributed as dist\n\n    if not dist.is_initialized():\n        dist.init_process_group(backend=backend)\n    global_rank = int(os.environ.get(\"RANK\", dist.get_rank()))\n    if dist.get_world_size() != config.world_size:\n        raise ValueError(\n            f\"launched world size {dist.get_world_size()} != config world size {config.world_size}\"\n        )\n    _STATE.global_rank = global_rank\n\n    tp = config.tensor_parallel_size\n    sp = config.sequence_parallel_size\n    cfg = config.cfg_parallel_size\n\n    _STATE.tensor = _build_axis(global_rank, inner=1, axis=tp, label=\"tp\")\n    _STATE.cfg = _build_axis(global_rank, inner=tp, axis=cfg, label=\"cfg\")\n    _STATE.sequence = _build_axis(global_rank, inner=tp * cfg, axis=sp, label=\"sp\")\n    _STATE.initialized = True\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.is_initialized","title":"is_initialized","text":"<pre><code>is_initialized() -&gt; bool\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def is_initialized() -&gt; bool:\n    return _STATE.initialized\n</code></pre>"},{"location":"api/worldkernels/distributed/#worldkernels.distributed.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/distributed/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/distributed/cfg_parallel/","title":"cfg_parallel","text":""},{"location":"api/worldkernels/distributed/cfg_parallel/#worldkernels.distributed.cfg_parallel","title":"worldkernels.distributed.cfg_parallel","text":"<p>Classifier-free-guidance parallelism.</p> <p>A diffusion step under CFG runs the transformer twice \u2014 once on the conditional input, once on the unconditional. With <code>cfg_parallel_size == 2</code> the two forwards run on separate ranks and their outputs are gathered, halving the per-step latency of the guided forward. With size 1 both run locally.</p>"},{"location":"api/worldkernels/distributed/cfg_parallel/#worldkernels.distributed.cfg_parallel.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['CFGParallelMixin']\n</code></pre>"},{"location":"api/worldkernels/distributed/cfg_parallel/#worldkernels.distributed.cfg_parallel.CFGParallelMixin","title":"CFGParallelMixin","text":"<p>Mixin giving a pipeline a CFG-parallel guided forward.</p> <p>The host pipeline calls <code>cfg_parallel_forward()</code> with a model callable and the conditional / unconditional keyword arguments.</p> Source code in <code>worldkernels/distributed/cfg_parallel.py</code> <pre><code>class CFGParallelMixin:\n    r\"\"\"Mixin giving a pipeline a CFG-parallel guided forward.\n\n    The host pipeline calls `cfg_parallel_forward()` with a model callable\n    and the conditional / unconditional keyword arguments.\n    \"\"\"\n\n    @staticmethod\n    def cfg_parallel_enabled() -&gt; bool:\n        return get_cfg_parallel_world_size() == 2\n\n    @staticmethod\n    def cfg_parallel_forward(\n        model: Callable[..., torch.Tensor],\n        cond_kwargs: dict,\n        uncond_kwargs: dict,\n        guidance_scale: float,\n    ) -&gt; torch.Tensor:\n        r\"\"\"Run the guided forward, splitting cond/uncond across CFG ranks.\n\n        Returns the guided prediction\n        \\( \\epsilon_u + s\\,(\\epsilon_c - \\epsilon_u) \\) for guidance scale \\(s\\).\n        \"\"\"\n        if get_cfg_parallel_world_size() == 1:\n            cond = model(**cond_kwargs)\n            uncond = model(**uncond_kwargs)\n            return uncond + guidance_scale * (cond - uncond)\n\n        import torch.distributed as dist\n\n        local = model(**(cond_kwargs if get_cfg_parallel_rank() == 0 else uncond_kwargs))\n        gathered = [torch.empty_like(local), torch.empty_like(local)]\n        dist.all_gather(gathered, local.contiguous(), group=get_cfg_parallel_group())\n        cond, uncond = gathered[0], gathered[1]\n        return uncond + guidance_scale * (cond - uncond)\n</code></pre>"},{"location":"api/worldkernels/distributed/cfg_parallel/#worldkernels.distributed.cfg_parallel.CFGParallelMixin.cfg_parallel_enabled","title":"cfg_parallel_enabled  <code>staticmethod</code>","text":"<pre><code>cfg_parallel_enabled() -&gt; bool\n</code></pre> Source code in <code>worldkernels/distributed/cfg_parallel.py</code> <pre><code>@staticmethod\ndef cfg_parallel_enabled() -&gt; bool:\n    return get_cfg_parallel_world_size() == 2\n</code></pre>"},{"location":"api/worldkernels/distributed/cfg_parallel/#worldkernels.distributed.cfg_parallel.CFGParallelMixin.cfg_parallel_forward","title":"cfg_parallel_forward  <code>staticmethod</code>","text":"<pre><code>cfg_parallel_forward(model: Callable[..., Tensor], cond_kwargs: dict, uncond_kwargs: dict, guidance_scale: float) -&gt; Tensor\n</code></pre> <p>Run the guided forward, splitting cond/uncond across CFG ranks.</p> <p>Returns the guided prediction \\( \\epsilon_u + s\\,(\\epsilon_c - \\epsilon_u) \\) for guidance scale \\(s\\).</p> Source code in <code>worldkernels/distributed/cfg_parallel.py</code> <pre><code>@staticmethod\ndef cfg_parallel_forward(\n    model: Callable[..., torch.Tensor],\n    cond_kwargs: dict,\n    uncond_kwargs: dict,\n    guidance_scale: float,\n) -&gt; torch.Tensor:\n    r\"\"\"Run the guided forward, splitting cond/uncond across CFG ranks.\n\n    Returns the guided prediction\n    \\( \\epsilon_u + s\\,(\\epsilon_c - \\epsilon_u) \\) for guidance scale \\(s\\).\n    \"\"\"\n    if get_cfg_parallel_world_size() == 1:\n        cond = model(**cond_kwargs)\n        uncond = model(**uncond_kwargs)\n        return uncond + guidance_scale * (cond - uncond)\n\n    import torch.distributed as dist\n\n    local = model(**(cond_kwargs if get_cfg_parallel_rank() == 0 else uncond_kwargs))\n    gathered = [torch.empty_like(local), torch.empty_like(local)]\n    dist.all_gather(gathered, local.contiguous(), group=get_cfg_parallel_group())\n    cond, uncond = gathered[0], gathered[1]\n    return uncond + guidance_scale * (cond - uncond)\n</code></pre>"},{"location":"api/worldkernels/distributed/communication/","title":"communication","text":""},{"location":"api/worldkernels/distributed/communication/#worldkernels.distributed.communication","title":"worldkernels.distributed.communication","text":"<p>Collective communication wrappers.</p> <p>Every collective is an identity no-op when its axis has world size 1, so single-GPU code paths carry no distributed overhead and need no guards at the call site.</p>"},{"location":"api/worldkernels/distributed/communication/#worldkernels.distributed.communication.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['tensor_parallel_all_reduce', 'tensor_parallel_all_gather']\n</code></pre>"},{"location":"api/worldkernels/distributed/communication/#worldkernels.distributed.communication.tensor_parallel_all_reduce","title":"tensor_parallel_all_reduce","text":"<pre><code>tensor_parallel_all_reduce(tensor: Tensor) -&gt; Tensor\n</code></pre> <p>Sum <code>tensor</code> across the tensor-parallel group, in place.</p> Source code in <code>worldkernels/distributed/communication.py</code> <pre><code>def tensor_parallel_all_reduce(tensor: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Sum ``tensor`` across the tensor-parallel group, in place.\"\"\"\n    if get_tensor_parallel_world_size() == 1:\n        return tensor\n    import torch.distributed as dist\n\n    dist.all_reduce(tensor, group=get_tensor_parallel_group())\n    return tensor\n</code></pre>"},{"location":"api/worldkernels/distributed/communication/#worldkernels.distributed.communication.tensor_parallel_all_gather","title":"tensor_parallel_all_gather","text":"<pre><code>tensor_parallel_all_gather(tensor: Tensor, dim: int = -1) -&gt; Tensor\n</code></pre> <p>Gather <code>tensor</code> across the tensor-parallel group, concatenating on <code>dim</code>.</p> Source code in <code>worldkernels/distributed/communication.py</code> <pre><code>def tensor_parallel_all_gather(tensor: torch.Tensor, dim: int = -1) -&gt; torch.Tensor:\n    r\"\"\"Gather ``tensor`` across the tensor-parallel group, concatenating on ``dim``.\"\"\"\n    world_size = get_tensor_parallel_world_size()\n    if world_size == 1:\n        return tensor\n    import torch.distributed as dist\n\n    shards = [torch.empty_like(tensor) for _ in range(world_size)]\n    dist.all_gather(shards, tensor.contiguous(), group=get_tensor_parallel_group())\n    return torch.cat(shards, dim=dim)\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/","title":"parallel_state","text":""},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state","title":"worldkernels.distributed.parallel_state","text":"<p>Distributed parallel-state registry.</p> <p>Holds each rank's position within every parallelism axis (tensor, sequence, CFG). The world is the product mesh</p> \\[ W = \\text{DP} \\cdot \\text{PP} \\cdot \\text{SP} \\cdot \\text{CFG} \\cdot \\text{TP} \\] <p>with TP innermost (adjacent global ranks share a TP group). Single-rank execution is the all-identity special case: every getter returns rank 0 / world size 1 / group <code>None</code>, and no <code>torch.distributed</code> call is made.</p>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['init_distributed', 'destroy_distributed', 'is_initialized', 'get_parallel_config', 'get_global_rank', 'get_world_size', 'get_tensor_parallel_rank', 'get_tensor_parallel_world_size', 'get_tensor_parallel_group', 'get_sequence_parallel_rank', 'get_sequence_parallel_world_size', 'get_sequence_parallel_group', 'get_cfg_parallel_rank', 'get_cfg_parallel_world_size', 'get_cfg_parallel_group']\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._STATE","title":"_STATE  <code>module-attribute</code>","text":"<pre><code>_STATE = _ParallelState()\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._GroupInfo","title":"_GroupInfo  <code>dataclass</code>","text":"<p>This rank's coordinates within one parallelism axis.</p> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>@dataclass\nclass _GroupInfo:\n    r\"\"\"This rank's coordinates within one parallelism axis.\"\"\"\n\n    rank: int = 0\n    world_size: int = 1\n    ranks: list[int] = field(default_factory=lambda: [0])\n    group: Any = None\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._GroupInfo.rank","title":"rank  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rank: int = 0\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._GroupInfo.world_size","title":"world_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>world_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._GroupInfo.ranks","title":"ranks  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ranks: list[int] = field(default_factory=lambda: [0])\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._GroupInfo.group","title":"group  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>group: Any = None\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._GroupInfo.__init__","title":"__init__","text":"<pre><code>__init__(rank: int = 0, world_size: int = 1, ranks: list[int] = (lambda: [0])(), group: Any = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState","title":"_ParallelState  <code>dataclass</code>","text":"Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>@dataclass\nclass _ParallelState:\n    config: ParallelConfig = field(default_factory=ParallelConfig)\n    global_rank: int = 0\n    world_size: int = 1\n    tensor: _GroupInfo = field(default_factory=_GroupInfo)\n    sequence: _GroupInfo = field(default_factory=_GroupInfo)\n    cfg: _GroupInfo = field(default_factory=_GroupInfo)\n    initialized: bool = False\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.config","title":"config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>config: ParallelConfig = field(default_factory=ParallelConfig)\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.global_rank","title":"global_rank  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>global_rank: int = 0\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.world_size","title":"world_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>world_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.tensor","title":"tensor  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>tensor: _GroupInfo = field(default_factory=_GroupInfo)\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.sequence","title":"sequence  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>sequence: _GroupInfo = field(default_factory=_GroupInfo)\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.cfg","title":"cfg  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cfg: _GroupInfo = field(default_factory=_GroupInfo)\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.initialized","title":"initialized  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initialized: bool = False\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._ParallelState.__init__","title":"__init__","text":"<pre><code>__init__(config: ParallelConfig = ParallelConfig(), global_rank: int = 0, world_size: int = 1, tensor: _GroupInfo = _GroupInfo(), sequence: _GroupInfo = _GroupInfo(), cfg: _GroupInfo = _GroupInfo(), initialized: bool = False) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.init_distributed","title":"init_distributed","text":"<pre><code>init_distributed(config: ParallelConfig, *, backend: str = 'nccl') -&gt; None\n</code></pre> <p>Initialize process groups for the parallel <code>config</code>.</p> <p>Single-rank configs initialize identity groups without touching <code>torch.distributed</code>. Multi-rank configs initialize the process group (rank and world size read from the <code>RANK</code> / <code>WORLD_SIZE</code> environment) and build the per-axis sub-groups.</p> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def init_distributed(\n    config: ParallelConfig,\n    *,\n    backend: str = \"nccl\",\n) -&gt; None:\n    r\"\"\"Initialize process groups for the parallel ``config``.\n\n    Single-rank configs initialize identity groups without touching\n    ``torch.distributed``. Multi-rank configs initialize the process group\n    (rank and world size read from the ``RANK`` / ``WORLD_SIZE`` environment)\n    and build the per-axis sub-groups.\n    \"\"\"\n    _STATE.config = config\n    _STATE.world_size = config.world_size\n\n    if config.world_size == 1:\n        _STATE.global_rank = 0\n        _STATE.tensor = _GroupInfo()\n        _STATE.sequence = _GroupInfo()\n        _STATE.cfg = _GroupInfo()\n        _STATE.initialized = True\n        return\n\n    import torch.distributed as dist\n\n    if not dist.is_initialized():\n        dist.init_process_group(backend=backend)\n    global_rank = int(os.environ.get(\"RANK\", dist.get_rank()))\n    if dist.get_world_size() != config.world_size:\n        raise ValueError(\n            f\"launched world size {dist.get_world_size()} != config world size {config.world_size}\"\n        )\n    _STATE.global_rank = global_rank\n\n    tp = config.tensor_parallel_size\n    sp = config.sequence_parallel_size\n    cfg = config.cfg_parallel_size\n\n    _STATE.tensor = _build_axis(global_rank, inner=1, axis=tp, label=\"tp\")\n    _STATE.cfg = _build_axis(global_rank, inner=tp, axis=cfg, label=\"cfg\")\n    _STATE.sequence = _build_axis(global_rank, inner=tp * cfg, axis=sp, label=\"sp\")\n    _STATE.initialized = True\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state._build_axis","title":"_build_axis","text":"<pre><code>_build_axis(global_rank: int, *, inner: int, axis: int, label: str) -&gt; _GroupInfo\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def _build_axis(global_rank: int, *, inner: int, axis: int, label: str) -&gt; _GroupInfo:\n    import torch.distributed as dist\n\n    outer = _STATE.world_size // (inner * axis)\n    info = _GroupInfo(world_size=axis)\n    for o in range(outer):\n        for i in range(inner):\n            seed = o * inner * axis + i\n            ranks = [seed + a * inner for a in range(axis)]\n            group = dist.new_group(ranks) if axis &gt; 1 else None\n            if global_rank in ranks:\n                info.ranks = ranks\n                info.rank = ranks.index(global_rank)\n                info.group = group\n    return info\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.destroy_distributed","title":"destroy_distributed","text":"<pre><code>destroy_distributed() -&gt; None\n</code></pre> <p>Tear down process groups and reset to single-rank identity.</p> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def destroy_distributed() -&gt; None:\n    r\"\"\"Tear down process groups and reset to single-rank identity.\"\"\"\n    if _STATE.world_size &gt; 1:\n        import torch.distributed as dist\n\n        if dist.is_initialized():\n            dist.destroy_process_group()\n    _STATE.__dict__.update(_ParallelState().__dict__)\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.is_initialized","title":"is_initialized","text":"<pre><code>is_initialized() -&gt; bool\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def is_initialized() -&gt; bool:\n    return _STATE.initialized\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_parallel_config","title":"get_parallel_config","text":"<pre><code>get_parallel_config() -&gt; ParallelConfig\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_parallel_config() -&gt; ParallelConfig:\n    return _STATE.config\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_global_rank","title":"get_global_rank","text":"<pre><code>get_global_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_global_rank() -&gt; int:\n    return _STATE.global_rank\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_world_size","title":"get_world_size","text":"<pre><code>get_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_world_size() -&gt; int:\n    return _STATE.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_tensor_parallel_rank","title":"get_tensor_parallel_rank","text":"<pre><code>get_tensor_parallel_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_tensor_parallel_rank() -&gt; int:\n    return _STATE.tensor.rank\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_tensor_parallel_world_size","title":"get_tensor_parallel_world_size","text":"<pre><code>get_tensor_parallel_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_tensor_parallel_world_size() -&gt; int:\n    return _STATE.tensor.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_tensor_parallel_group","title":"get_tensor_parallel_group","text":"<pre><code>get_tensor_parallel_group() -&gt; Any\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_tensor_parallel_group() -&gt; Any:\n    return _STATE.tensor.group\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_sequence_parallel_rank","title":"get_sequence_parallel_rank","text":"<pre><code>get_sequence_parallel_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_sequence_parallel_rank() -&gt; int:\n    return _STATE.sequence.rank\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_sequence_parallel_world_size","title":"get_sequence_parallel_world_size","text":"<pre><code>get_sequence_parallel_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_sequence_parallel_world_size() -&gt; int:\n    return _STATE.sequence.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_sequence_parallel_group","title":"get_sequence_parallel_group","text":"<pre><code>get_sequence_parallel_group() -&gt; Any\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_sequence_parallel_group() -&gt; Any:\n    return _STATE.sequence.group\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_cfg_parallel_rank","title":"get_cfg_parallel_rank","text":"<pre><code>get_cfg_parallel_rank() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_cfg_parallel_rank() -&gt; int:\n    return _STATE.cfg.rank\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_cfg_parallel_world_size","title":"get_cfg_parallel_world_size","text":"<pre><code>get_cfg_parallel_world_size() -&gt; int\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_cfg_parallel_world_size() -&gt; int:\n    return _STATE.cfg.world_size\n</code></pre>"},{"location":"api/worldkernels/distributed/parallel_state/#worldkernels.distributed.parallel_state.get_cfg_parallel_group","title":"get_cfg_parallel_group","text":"<pre><code>get_cfg_parallel_group() -&gt; Any\n</code></pre> Source code in <code>worldkernels/distributed/parallel_state.py</code> <pre><code>def get_cfg_parallel_group() -&gt; Any:\n    return _STATE.cfg.group\n</code></pre>"},{"location":"api/worldkernels/distributed/sequence_parallel/","title":"sequence_parallel","text":""},{"location":"api/worldkernels/distributed/sequence_parallel/#worldkernels.distributed.sequence_parallel","title":"worldkernels.distributed.sequence_parallel","text":"<p>Sequence-parallel primitives: Ulysses all-to-all and ring rotation.</p> <p>Modules:</p> Name Description <code>ring</code> <p>Ring sequence parallelism.</p> <code>ulysses</code> <p>Ulysses sequence parallelism.</p>"},{"location":"api/worldkernels/distributed/sequence_parallel/#worldkernels.distributed.sequence_parallel.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ulysses_all_to_all', 'ring_rotate']\n</code></pre>"},{"location":"api/worldkernels/distributed/sequence_parallel/#worldkernels.distributed.sequence_parallel.ring_rotate","title":"ring_rotate","text":"<pre><code>ring_rotate(tensor: Tensor) -&gt; Tensor\n</code></pre> <p>Send <code>tensor</code> to the next rank and return the tensor from the previous one.</p> <p>One hop of the ring: rank \\(r\\) sends to \\(r{+}1\\) and receives from \\(r{-}1\\) (modulo the ring size). Identity when sequence-parallel world size is 1.</p> Source code in <code>worldkernels/distributed/sequence_parallel/ring.py</code> <pre><code>def ring_rotate(tensor: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Send ``tensor`` to the next rank and return the tensor from the previous one.\n\n    One hop of the ring: rank \\(r\\) sends to \\(r{+}1\\) and receives from\n    \\(r{-}1\\) (modulo the ring size). Identity when sequence-parallel world\n    size is 1.\n    \"\"\"\n    world_size = get_sequence_parallel_world_size()\n    if world_size == 1:\n        return tensor\n    import torch.distributed as dist\n\n    group = get_sequence_parallel_group()\n    rank = get_sequence_parallel_rank()\n    ranks = dist.get_process_group_ranks(group)\n    send_to = ranks[(rank + 1) % world_size]\n    recv_from = ranks[(rank - 1) % world_size]\n\n    recv = torch.empty_like(tensor)\n    ops = [\n        dist.P2POp(dist.isend, tensor.contiguous(), send_to, group),\n        dist.P2POp(dist.irecv, recv, recv_from, group),\n    ]\n    for work in dist.batch_isend_irecv(ops):\n        work.wait()\n    return recv\n</code></pre>"},{"location":"api/worldkernels/distributed/sequence_parallel/#worldkernels.distributed.sequence_parallel.ulysses_all_to_all","title":"ulysses_all_to_all","text":"<pre><code>ulysses_all_to_all(x: Tensor, *, scatter_dim: int, gather_dim: int) -&gt; Tensor\n</code></pre> <p>All-to-all redistribution across the sequence-parallel group.</p> <p>Splits <code>x</code> into equal chunks along <code>scatter_dim</code>, exchanges one chunk with every rank, and concatenates the received chunks along <code>gather_dim</code>. Identity when sequence-parallel world size is 1.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Tensor sharded along <code>scatter_dim</code>.</p> required <code>scatter_dim</code> <code>int</code> <p>Dimension to split and send.</p> required <code>gather_dim</code> <code>int</code> <p>Dimension to concatenate received chunks on.</p> required Source code in <code>worldkernels/distributed/sequence_parallel/ulysses.py</code> <pre><code>def ulysses_all_to_all(\n    x: torch.Tensor,\n    *,\n    scatter_dim: int,\n    gather_dim: int,\n) -&gt; torch.Tensor:\n    r\"\"\"All-to-all redistribution across the sequence-parallel group.\n\n    Splits ``x`` into equal chunks along ``scatter_dim``, exchanges one chunk\n    with every rank, and concatenates the received chunks along ``gather_dim``.\n    Identity when sequence-parallel world size is 1.\n\n    Args:\n        x: Tensor sharded along ``scatter_dim``.\n        scatter_dim: Dimension to split and send.\n        gather_dim: Dimension to concatenate received chunks on.\n    \"\"\"\n    world_size = get_sequence_parallel_world_size()\n    if world_size == 1:\n        return x\n    import torch.distributed as dist\n\n    if x.shape[scatter_dim] % world_size != 0:\n        raise ValueError(\n            f\"scatter dim {scatter_dim} (size {x.shape[scatter_dim]}) not divisible \"\n            f\"by sequence-parallel world size {world_size}\"\n        )\n    send = [c.contiguous() for c in x.chunk(world_size, dim=scatter_dim)]\n    recv = [torch.empty_like(send[0]) for _ in range(world_size)]\n    dist.all_to_all(recv, send, group=get_sequence_parallel_group())\n    return torch.cat(recv, dim=gather_dim)\n</code></pre>"},{"location":"api/worldkernels/distributed/sequence_parallel/ring/","title":"ring","text":""},{"location":"api/worldkernels/distributed/sequence_parallel/ring/#worldkernels.distributed.sequence_parallel.ring","title":"worldkernels.distributed.sequence_parallel.ring","text":"<p>Ring sequence parallelism.</p> <p>Ring attention keeps the sequence sharded across ranks and circulates each rank's key/value block around a ring, accumulating attention with an online softmax so no rank ever materializes the full sequence. <code>ring_rotate()</code> is the communication primitive \u2014 one hop around the ring; the online-softmax accumulation loop lives in the attention backend that calls it.</p>"},{"location":"api/worldkernels/distributed/sequence_parallel/ring/#worldkernels.distributed.sequence_parallel.ring.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ring_rotate']\n</code></pre>"},{"location":"api/worldkernels/distributed/sequence_parallel/ring/#worldkernels.distributed.sequence_parallel.ring.ring_rotate","title":"ring_rotate","text":"<pre><code>ring_rotate(tensor: Tensor) -&gt; Tensor\n</code></pre> <p>Send <code>tensor</code> to the next rank and return the tensor from the previous one.</p> <p>One hop of the ring: rank \\(r\\) sends to \\(r{+}1\\) and receives from \\(r{-}1\\) (modulo the ring size). Identity when sequence-parallel world size is 1.</p> Source code in <code>worldkernels/distributed/sequence_parallel/ring.py</code> <pre><code>def ring_rotate(tensor: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Send ``tensor`` to the next rank and return the tensor from the previous one.\n\n    One hop of the ring: rank \\(r\\) sends to \\(r{+}1\\) and receives from\n    \\(r{-}1\\) (modulo the ring size). Identity when sequence-parallel world\n    size is 1.\n    \"\"\"\n    world_size = get_sequence_parallel_world_size()\n    if world_size == 1:\n        return tensor\n    import torch.distributed as dist\n\n    group = get_sequence_parallel_group()\n    rank = get_sequence_parallel_rank()\n    ranks = dist.get_process_group_ranks(group)\n    send_to = ranks[(rank + 1) % world_size]\n    recv_from = ranks[(rank - 1) % world_size]\n\n    recv = torch.empty_like(tensor)\n    ops = [\n        dist.P2POp(dist.isend, tensor.contiguous(), send_to, group),\n        dist.P2POp(dist.irecv, recv, recv_from, group),\n    ]\n    for work in dist.batch_isend_irecv(ops):\n        work.wait()\n    return recv\n</code></pre>"},{"location":"api/worldkernels/distributed/sequence_parallel/ulysses/","title":"ulysses","text":""},{"location":"api/worldkernels/distributed/sequence_parallel/ulysses/#worldkernels.distributed.sequence_parallel.ulysses","title":"worldkernels.distributed.sequence_parallel.ulysses","text":"<p>Ulysses sequence parallelism.</p> <p>Ulysses shards the sequence dimension across ranks for the linear layers and, inside attention, transposes that sharding onto the head dimension with an all-to-all so each rank computes full-sequence attention for a slice of the heads. Two all-to-alls bracket the attention call: sequence-sharded -&gt; head-sharded before, head-sharded -&gt; sequence-sharded after.</p>"},{"location":"api/worldkernels/distributed/sequence_parallel/ulysses/#worldkernels.distributed.sequence_parallel.ulysses.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ulysses_all_to_all']\n</code></pre>"},{"location":"api/worldkernels/distributed/sequence_parallel/ulysses/#worldkernels.distributed.sequence_parallel.ulysses.ulysses_all_to_all","title":"ulysses_all_to_all","text":"<pre><code>ulysses_all_to_all(x: Tensor, *, scatter_dim: int, gather_dim: int) -&gt; Tensor\n</code></pre> <p>All-to-all redistribution across the sequence-parallel group.</p> <p>Splits <code>x</code> into equal chunks along <code>scatter_dim</code>, exchanges one chunk with every rank, and concatenates the received chunks along <code>gather_dim</code>. Identity when sequence-parallel world size is 1.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>Tensor</code> <p>Tensor sharded along <code>scatter_dim</code>.</p> required <code>scatter_dim</code> <code>int</code> <p>Dimension to split and send.</p> required <code>gather_dim</code> <code>int</code> <p>Dimension to concatenate received chunks on.</p> required Source code in <code>worldkernels/distributed/sequence_parallel/ulysses.py</code> <pre><code>def ulysses_all_to_all(\n    x: torch.Tensor,\n    *,\n    scatter_dim: int,\n    gather_dim: int,\n) -&gt; torch.Tensor:\n    r\"\"\"All-to-all redistribution across the sequence-parallel group.\n\n    Splits ``x`` into equal chunks along ``scatter_dim``, exchanges one chunk\n    with every rank, and concatenates the received chunks along ``gather_dim``.\n    Identity when sequence-parallel world size is 1.\n\n    Args:\n        x: Tensor sharded along ``scatter_dim``.\n        scatter_dim: Dimension to split and send.\n        gather_dim: Dimension to concatenate received chunks on.\n    \"\"\"\n    world_size = get_sequence_parallel_world_size()\n    if world_size == 1:\n        return x\n    import torch.distributed as dist\n\n    if x.shape[scatter_dim] % world_size != 0:\n        raise ValueError(\n            f\"scatter dim {scatter_dim} (size {x.shape[scatter_dim]}) not divisible \"\n            f\"by sequence-parallel world size {world_size}\"\n        )\n    send = [c.contiguous() for c in x.chunk(world_size, dim=scatter_dim)]\n    recv = [torch.empty_like(send[0]) for _ in range(world_size)]\n    dist.all_to_all(recv, send, group=get_sequence_parallel_group())\n    return torch.cat(recv, dim=gather_dim)\n</code></pre>"},{"location":"api/worldkernels/engine/","title":"engine","text":""},{"location":"api/worldkernels/engine/#worldkernels.engine","title":"worldkernels.engine","text":"<p>Engine layer: request intake, model lifecycle, session registry.</p> <p>Modules:</p> Name Description <code>async_engine</code> <p>Async engine \u2014 the continuous-batching serving front-end.</p> <code>world_engine</code> <p>WorldEngine \u2014 the request-and-lifecycle layer.</p>"},{"location":"api/worldkernels/engine/#worldkernels.engine.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WorldEngine', 'AsyncEngine']\n</code></pre>"},{"location":"api/worldkernels/engine/#worldkernels.engine._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'WorldEngine': ('worldkernels.engine.world_engine', 'WorldEngine'), 'AsyncEngine': ('worldkernels.engine.async_engine', 'AsyncEngine')}\n</code></pre>"},{"location":"api/worldkernels/engine/#worldkernels.engine.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/engine/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/","title":"async_engine","text":""},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine","title":"worldkernels.engine.async_engine","text":"<p>Async engine \u2014 the continuous-batching serving front-end.</p> <p>Wraps a <code>WorldEngine</code> with an async API and a background drain loop. A <code>step</code> call enqueues a <code>StepRequest</code> and awaits a future; the drain loop wakes every <code>batch_window</code> and dispatches all queued requests as compatibility-group batches through the scheduler. Concurrent requests for the same world thus share a batched forward pass \u2014 genuine continuous batching.</p>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['AsyncEngine']\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine","title":"AsyncEngine","text":"<p>Async, continuous-batching wrapper around a <code>WorldEngine</code>.</p> <p>Parameters:</p> Name Type Description Default <code>engine</code> <code>'WorldEngine'</code> <p>The synchronous engine to drive.</p> required <code>batch_window</code> <code>float</code> <p>Seconds to accumulate requests before a drain; the micro-batching window.</p> <code>0.005</code> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>class AsyncEngine:\n    r\"\"\"Async, continuous-batching wrapper around a `WorldEngine`.\n\n    Args:\n        engine: The synchronous engine to drive.\n        batch_window: Seconds to accumulate requests before a drain; the\n            micro-batching window.\n    \"\"\"\n\n    def __init__(self, engine: \"WorldEngine\", *, batch_window: float = 0.005) -&gt; None:\n        self.engine = engine\n        self.batch_window = batch_window\n        _cfg = getattr(engine, \"config\", None)\n        self._continuous_batching = getattr(_cfg, \"continuous_batching\", True)\n        self._offload_idle = getattr(_cfg, \"offload_idle\", False)\n        self._compute_device = getattr(engine, \"device\", \"cuda\")\n        self._pending: dict[str, asyncio.Future] = {}\n        self._drain_task: asyncio.Task | None = None\n        self._closed = False\n        self.offload_count = 0\n        self.restore_count = 0\n\n    async def load_model(self, model_id: str, **kwargs: Any) -&gt; None:\n        await asyncio.to_thread(self.engine.load_model, model_id, **kwargs)\n\n    async def unload_model(self, name: str) -&gt; None:\n        await asyncio.to_thread(self.engine.unload_model, name)\n\n    async def create_session(\n        self,\n        world: str,\n        config: \"WorldConfig | None\" = None,\n        seed: int | None = None,\n        overrides: dict | None = None,\n    ) -&gt; \"Session\":\n        session = await asyncio.to_thread(\n            self.engine.create_session, world, config, seed, overrides\n        )\n        metrics.set_active_sessions(len(self.engine.list_sessions()))\n        return session\n\n    async def close_session(self, session_id: str) -&gt; None:\n        await asyncio.to_thread(self.engine.close_session, session_id)\n        metrics.set_active_sessions(len(self.engine.list_sessions()))\n\n    async def step(\n        self,\n        session_id: str,\n        action: \"Action\",\n        modalities: list[str] | None = None,\n        decode: bool = True,\n    ) -&gt; \"Observation\":\n        r\"\"\"Execute one step, batched with other concurrent steps.\"\"\"\n        from worldkernels.core.request import StepRequest\n        from worldkernels.core.session import SessionStatus\n\n        session = self.engine.get_session(session_id)\n        if session is None:\n            raise SessionNotFoundError(session_id)\n        if session.status is SessionStatus.TERMINATED:\n            raise SessionTerminatedError(session_id)\n        if session.status is SessionStatus.PAUSED:\n            raise SessionPausedError(session_id)\n        if session_id in self._pending:\n            raise RuntimeError(f\"a step is already in flight for session {session_id!r}\")\n\n        if self._offload_idle and session.state.device != self._compute_device:\n            session.state = session.state.to(self._compute_device)\n            self.restore_count += 1\n\n        self._ensure_drain_loop()\n        future: asyncio.Future = asyncio.get_running_loop().create_future()\n        self._pending[session_id] = future\n        self.engine._scheduler.add_request(\n            StepRequest(\n                session_id=session_id,\n                world=session._world,  # type: ignore[arg-type]\n                state=session.state,\n                action=action,\n                modalities=modalities or [\"frames\"],\n                step_index=session.step_index,\n                decode=decode,\n            )\n        )\n        return await future\n\n    async def shutdown(self) -&gt; None:\n        self._closed = True\n        if self._drain_task is not None:\n            self._drain_task.cancel()\n        for future in self._pending.values():\n            if not future.done():\n                future.cancel()\n        self._pending.clear()\n        await asyncio.to_thread(self.engine.shutdown)\n\n    def _ensure_drain_loop(self) -&gt; None:\n        if self._drain_task is None or self._drain_task.done():\n            self._drain_task = asyncio.create_task(self._drain_loop())\n\n    async def _drain_loop(self) -&gt; None:\n        scheduler = self.engine._scheduler\n        wait = self.batch_window if self._continuous_batching else 0.0\n        cap = None if self._continuous_batching else 1\n        while not self._closed:\n            await asyncio.sleep(wait)\n            batch = scheduler.pending\n            if not batch:\n                continue\n            try:\n                results = await asyncio.to_thread(scheduler.run_scheduled, cap)\n            except Exception as exc:  # noqa: BLE001 - surface compute errors to callers\n                self._fail_pending(exc)\n                continue\n            metrics.observe_batch(batch)\n            for session_id, (new_state, obs) in results.items():\n                self._resolve(session_id, new_state, obs)\n            if self._offload_idle:\n                self._offload_non_active(active=set(results.keys()))\n\n    def _fail_pending(self, exc: BaseException) -&gt; None:\n        r\"\"\"Fail every awaiting step with ``exc`` rather than hang on a dead batch.\"\"\"\n        for future in self._pending.values():\n            if not future.done():\n                future.set_exception(exc)\n        self._pending.clear()\n\n    def _offload_non_active(self, active: set[str]) -&gt; None:\n        for sid, session in self.engine._sessions.items():\n            if sid in active:\n                continue\n            ov = session.overrides or {}\n            if ov.get(\"offload_idle\") is False:\n                continue\n            if session.state.device != \"cpu\":\n                session.state = session.state.to(\"cpu\")\n                self.offload_count += 1\n\n    def _resolve(self, session_id: str, new_state: Any, obs: \"Observation\") -&gt; None:\n        session = self.engine.get_session(session_id)\n        if session is not None:\n            session.state = new_state\n            session.step_index += 1\n            session.last_active_at = datetime.now()\n        frames = len(obs.frames) if obs.frames is not None else 0\n        metrics.observe_step(obs.generation_time_ms / 1000.0, frames=frames)\n        future = self._pending.pop(session_id, None)\n        if future is not None and not future.done():\n            future.set_result(obs)\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.engine","title":"engine  <code>instance-attribute</code>","text":"<pre><code>engine = engine\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.batch_window","title":"batch_window  <code>instance-attribute</code>","text":"<pre><code>batch_window = batch_window\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._continuous_batching","title":"_continuous_batching  <code>instance-attribute</code>","text":"<pre><code>_continuous_batching = getattr(_cfg, 'continuous_batching', True)\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._offload_idle","title":"_offload_idle  <code>instance-attribute</code>","text":"<pre><code>_offload_idle = getattr(_cfg, 'offload_idle', False)\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._compute_device","title":"_compute_device  <code>instance-attribute</code>","text":"<pre><code>_compute_device = getattr(engine, 'device', 'cuda')\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._pending","title":"_pending  <code>instance-attribute</code>","text":"<pre><code>_pending: dict[str, Future] = {}\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._drain_task","title":"_drain_task  <code>instance-attribute</code>","text":"<pre><code>_drain_task: Task | None = None\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._closed","title":"_closed  <code>instance-attribute</code>","text":"<pre><code>_closed = False\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.offload_count","title":"offload_count  <code>instance-attribute</code>","text":"<pre><code>offload_count = 0\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.restore_count","title":"restore_count  <code>instance-attribute</code>","text":"<pre><code>restore_count = 0\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.__init__","title":"__init__","text":"<pre><code>__init__(engine: 'WorldEngine', *, batch_window: float = 0.005) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>def __init__(self, engine: \"WorldEngine\", *, batch_window: float = 0.005) -&gt; None:\n    self.engine = engine\n    self.batch_window = batch_window\n    _cfg = getattr(engine, \"config\", None)\n    self._continuous_batching = getattr(_cfg, \"continuous_batching\", True)\n    self._offload_idle = getattr(_cfg, \"offload_idle\", False)\n    self._compute_device = getattr(engine, \"device\", \"cuda\")\n    self._pending: dict[str, asyncio.Future] = {}\n    self._drain_task: asyncio.Task | None = None\n    self._closed = False\n    self.offload_count = 0\n    self.restore_count = 0\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.load_model","title":"load_model  <code>async</code>","text":"<pre><code>load_model(model_id: str, **kwargs: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>async def load_model(self, model_id: str, **kwargs: Any) -&gt; None:\n    await asyncio.to_thread(self.engine.load_model, model_id, **kwargs)\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.unload_model","title":"unload_model  <code>async</code>","text":"<pre><code>unload_model(name: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>async def unload_model(self, name: str) -&gt; None:\n    await asyncio.to_thread(self.engine.unload_model, name)\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.create_session","title":"create_session  <code>async</code>","text":"<pre><code>create_session(world: str, config: 'WorldConfig | None' = None, seed: int | None = None, overrides: dict | None = None) -&gt; 'Session'\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>async def create_session(\n    self,\n    world: str,\n    config: \"WorldConfig | None\" = None,\n    seed: int | None = None,\n    overrides: dict | None = None,\n) -&gt; \"Session\":\n    session = await asyncio.to_thread(\n        self.engine.create_session, world, config, seed, overrides\n    )\n    metrics.set_active_sessions(len(self.engine.list_sessions()))\n    return session\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.close_session","title":"close_session  <code>async</code>","text":"<pre><code>close_session(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>async def close_session(self, session_id: str) -&gt; None:\n    await asyncio.to_thread(self.engine.close_session, session_id)\n    metrics.set_active_sessions(len(self.engine.list_sessions()))\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.step","title":"step  <code>async</code>","text":"<pre><code>step(session_id: str, action: 'Action', modalities: list[str] | None = None, decode: bool = True) -&gt; 'Observation'\n</code></pre> <p>Execute one step, batched with other concurrent steps.</p> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>async def step(\n    self,\n    session_id: str,\n    action: \"Action\",\n    modalities: list[str] | None = None,\n    decode: bool = True,\n) -&gt; \"Observation\":\n    r\"\"\"Execute one step, batched with other concurrent steps.\"\"\"\n    from worldkernels.core.request import StepRequest\n    from worldkernels.core.session import SessionStatus\n\n    session = self.engine.get_session(session_id)\n    if session is None:\n        raise SessionNotFoundError(session_id)\n    if session.status is SessionStatus.TERMINATED:\n        raise SessionTerminatedError(session_id)\n    if session.status is SessionStatus.PAUSED:\n        raise SessionPausedError(session_id)\n    if session_id in self._pending:\n        raise RuntimeError(f\"a step is already in flight for session {session_id!r}\")\n\n    if self._offload_idle and session.state.device != self._compute_device:\n        session.state = session.state.to(self._compute_device)\n        self.restore_count += 1\n\n    self._ensure_drain_loop()\n    future: asyncio.Future = asyncio.get_running_loop().create_future()\n    self._pending[session_id] = future\n    self.engine._scheduler.add_request(\n        StepRequest(\n            session_id=session_id,\n            world=session._world,  # type: ignore[arg-type]\n            state=session.state,\n            action=action,\n            modalities=modalities or [\"frames\"],\n            step_index=session.step_index,\n            decode=decode,\n        )\n    )\n    return await future\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine.shutdown","title":"shutdown  <code>async</code>","text":"<pre><code>shutdown() -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>async def shutdown(self) -&gt; None:\n    self._closed = True\n    if self._drain_task is not None:\n        self._drain_task.cancel()\n    for future in self._pending.values():\n        if not future.done():\n            future.cancel()\n    self._pending.clear()\n    await asyncio.to_thread(self.engine.shutdown)\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._ensure_drain_loop","title":"_ensure_drain_loop","text":"<pre><code>_ensure_drain_loop() -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>def _ensure_drain_loop(self) -&gt; None:\n    if self._drain_task is None or self._drain_task.done():\n        self._drain_task = asyncio.create_task(self._drain_loop())\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._drain_loop","title":"_drain_loop  <code>async</code>","text":"<pre><code>_drain_loop() -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>async def _drain_loop(self) -&gt; None:\n    scheduler = self.engine._scheduler\n    wait = self.batch_window if self._continuous_batching else 0.0\n    cap = None if self._continuous_batching else 1\n    while not self._closed:\n        await asyncio.sleep(wait)\n        batch = scheduler.pending\n        if not batch:\n            continue\n        try:\n            results = await asyncio.to_thread(scheduler.run_scheduled, cap)\n        except Exception as exc:  # noqa: BLE001 - surface compute errors to callers\n            self._fail_pending(exc)\n            continue\n        metrics.observe_batch(batch)\n        for session_id, (new_state, obs) in results.items():\n            self._resolve(session_id, new_state, obs)\n        if self._offload_idle:\n            self._offload_non_active(active=set(results.keys()))\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._fail_pending","title":"_fail_pending","text":"<pre><code>_fail_pending(exc: BaseException) -&gt; None\n</code></pre> <p>Fail every awaiting step with <code>exc</code> rather than hang on a dead batch.</p> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>def _fail_pending(self, exc: BaseException) -&gt; None:\n    r\"\"\"Fail every awaiting step with ``exc`` rather than hang on a dead batch.\"\"\"\n    for future in self._pending.values():\n        if not future.done():\n            future.set_exception(exc)\n    self._pending.clear()\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._offload_non_active","title":"_offload_non_active","text":"<pre><code>_offload_non_active(active: set[str]) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>def _offload_non_active(self, active: set[str]) -&gt; None:\n    for sid, session in self.engine._sessions.items():\n        if sid in active:\n            continue\n        ov = session.overrides or {}\n        if ov.get(\"offload_idle\") is False:\n            continue\n        if session.state.device != \"cpu\":\n            session.state = session.state.to(\"cpu\")\n            self.offload_count += 1\n</code></pre>"},{"location":"api/worldkernels/engine/async_engine/#worldkernels.engine.async_engine.AsyncEngine._resolve","title":"_resolve","text":"<pre><code>_resolve(session_id: str, new_state: Any, obs: 'Observation') -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/async_engine.py</code> <pre><code>def _resolve(self, session_id: str, new_state: Any, obs: \"Observation\") -&gt; None:\n    session = self.engine.get_session(session_id)\n    if session is not None:\n        session.state = new_state\n        session.step_index += 1\n        session.last_active_at = datetime.now()\n    frames = len(obs.frames) if obs.frames is not None else 0\n    metrics.observe_step(obs.generation_time_ms / 1000.0, frames=frames)\n    future = self._pending.pop(session_id, None)\n    if future is not None and not future.done():\n        future.set_result(obs)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/","title":"world_engine","text":""},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine","title":"worldkernels.engine.world_engine","text":"<p>WorldEngine \u2014 the request-and-lifecycle layer.</p> <p>Owns the world registry and session registry, resolves and loads models, and creates sessions. Execution is delegated to a <code>Scheduler</code>, which dispatches to a <code>Worker</code>; the engine itself runs no model compute.</p>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine","title":"WorldEngine","text":"<p>GPU-first world-model simulation engine.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>str | None</code> <p>Target device (<code>\"cuda\"</code>, <code>\"cpu\"</code>, or <code>\"cuda:N\"</code>).</p> <code>None</code> <code>max_sessions</code> <code>int | None</code> <p>Maximum concurrent sessions.</p> <code>None</code> <code>offload_idle</code> <code>bool | None</code> <p>Whether to offload idle session state.</p> <code>None</code> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>class WorldEngine:\n    r\"\"\"GPU-first world-model simulation engine.\n\n    Args:\n        device: Target device (``\"cuda\"``, ``\"cpu\"``, or ``\"cuda:N\"``).\n        max_sessions: Maximum concurrent sessions.\n        offload_idle: Whether to offload idle session state.\n    \"\"\"\n\n    def __init__(\n        self,\n        config: \"RuntimeConfig | str | None\" = None,\n        *,\n        device: str | None = None,\n        max_sessions: int | None = None,\n        offload_idle: bool | None = None,\n    ) -&gt; None:\n        r\"\"\"Construct the engine from a `RuntimeConfig` (or profile name).\n\n        Args:\n            config: A `RuntimeConfig`, a profile name (``\"baseline\"`` etc.), or\n                ``None`` for resolved defaults (honoring ``WK_*`` env vars).\n            device: Back-compat override of ``config.device``.\n            max_sessions: Back-compat override of ``config.max_sessions``.\n            offload_idle: Back-compat override of ``config.offload_idle``.\n        \"\"\"\n        from worldkernels.config.active import set_active_config\n        from worldkernels.config.profiles import resolve_runtime_config\n        from worldkernels.config.runtime import RuntimeConfig\n\n        if isinstance(config, RuntimeConfig):\n            cfg = config\n        elif isinstance(config, str):\n            cfg, _ = resolve_runtime_config(profile=config)\n        else:\n            cfg, _ = resolve_runtime_config()\n\n        if device is not None:\n            cfg.device = device\n        if max_sessions is not None:\n            cfg.max_sessions = max_sessions\n        if offload_idle is not None:\n            cfg.offload_idle = offload_idle\n\n        self.config = cfg\n        set_active_config(cfg)\n\n        self.device = cfg.device\n        self.max_sessions = cfg.max_sessions\n        self.offload_idle = cfg.offload_idle\n        self.dtype = _resolve_dtype(cfg.dtype, cfg.device)\n\n        self._worlds: dict[str, WorldModel] = {}\n        self._sessions: dict[str, Session] = {}\n        self._tiers: dict[str, str] = {}\n        self._cards: dict[str, Any] = {}\n        self.trajectory_cache = None\n        if cfg.trajectory_cache:\n            from worldkernels.runtime.memory.trajectory_cache import TrajectoryCache\n\n            self.trajectory_cache = TrajectoryCache()\n\n        from worldkernels.scheduler import Scheduler\n        from worldkernels.worker import Worker\n\n        self._worker = Worker(\n            device=self.device, dtype=self.dtype, parallel_config=cfg.parallel, config=cfg\n        )\n        self._scheduler = Scheduler(self._worker, config=cfg.scheduler)\n\n        log.info(\n            \"WorldEngine initialized: device=%s, dtype=%s, max_sessions=%d\",\n            self.device,\n            self.dtype,\n            self.max_sessions,\n        )\n\n    def load_model(\n        self,\n        model_id: str,\n        alias: str | None = None,\n        variant: str | None = None,\n        ckpt_path: str | None = None,\n        progress: Any = None,\n        allow_fetch: bool = True,\n        trust_remote_code: bool = False,\n        **kwargs: Any,\n    ) -&gt; None:\n        r\"\"\"Load a world model from a hub alias, HF repo id, or local checkpoint path.\n\n        Drives the resolver, which decides whether the model can share the\n        current env or needs an isolated subprocess. Shared models go\n        through `worldkernels.bootstrap.prepare` and instantiate locally; isolated\n        models materialize a per-model uv venv and are accessed via `RemoteWorld`.\n\n        Args:\n            model_id: Short alias, HF repo id, HF URL, or local path.\n            alias: Override the in-engine key (defaults to a slug of ``model_id``).\n            variant: Pick a variant from the model card.\n            ckpt_path: Bypass HF download with a local checkpoint file.\n            progress: Optional `ProgressController` (CLI/HTTP pass theirs through).\n            allow_fetch: If ``False``, errors instead of installing/cloning/downloading.\n            trust_remote_code: Allow custom code from HF Hub (reserved).\n            **kwargs: Forwarded to the world constructor (overrides card defaults).\n        \"\"\"\n        from worldkernels.bootstrap.errors import ModelNotFoundError as _ModelNotFoundError\n        from worldkernels.bootstrap.resolve import resolve as _resolve_ref\n        from worldkernels.config import WorldConfig as WC\n        from worldkernels.runtime.resolver import IsolatedPlan, resolve_install_plan\n\n        try:\n            resolved = _resolve_ref(model_id, variant=variant, ckpt_path=ckpt_path)\n        except _ModelNotFoundError as exc:\n            raise WorldNotFoundError(model_id) from exc\n        card = resolved.card\n        if card.isolation == \"auto\" and self.config.isolation != \"auto\":\n            import dataclasses\n\n            card = dataclasses.replace(card, isolation=self.config.isolation)\n            resolved = dataclasses.replace(resolved, card=card)\n        plan = resolve_install_plan(card, list(self._cards.values()))\n\n        if isinstance(plan, IsolatedPlan):\n            world, key, tier = self._load_isolated(\n                model_id, resolved, plan, alias, progress, allow_fetch, kwargs\n            )\n        else:\n            world, key, tier = self._load_shared(\n                model_id, alias, variant, ckpt_path, progress, allow_fetch, kwargs\n            )\n\n        if key in self._worlds:\n            try:\n                world.close()  # type: ignore[attr-defined]\n            except Exception:\n                pass\n            raise WorldAlreadyLoadedError(key)\n\n        if getattr(world, \"supports_kv_cache\", False) and self.config.kv_cache_paged:\n            self._worker.runner.ensure_kv_cache(\n                frames_per_block=self.config.cache.block_frames,\n            )\n\n        if self.config.quantization != \"none\":\n            target = getattr(world, \"_quant_target\", None)\n            if target is not None:\n                from worldkernels.runtime.quantization.registry import QuantizationRegistry\n\n                QuantizationRegistry().apply(target, self.config.quantization)\n\n        try:\n            world.warmup(getattr(world, \"default_config\", None) or WC())\n        except Exception as exc:\n            raise WorldInitError(model_id, f\"warmup failed: {exc}\") from exc\n\n        self._worlds[key] = world\n        self._tiers[key] = tier\n        self._cards[key] = card\n        _emit_tier_metric(key, tier)\n        _emit_worker_count(self._tiers)\n        log.info(\"Loaded world: %s (tier=%s, class=%s)\", key, tier, type(world).__name__)\n\n    def _load_shared(\n        self,\n        model_id: str,\n        alias: str | None,\n        variant: str | None,\n        ckpt_path: str | None,\n        progress: Any,\n        allow_fetch: bool,\n        kwargs: dict[str, Any],\n    ) -&gt; tuple[Any, str, str]:\n        from worldkernels.bootstrap import prepare\n        from worldkernels.worlds.registry import get_world_class\n\n        prepared = prepare(\n            model_id,\n            variant=variant,\n            ckpt_path=ckpt_path,\n            progress=progress,\n            allow_fetch=allow_fetch,\n            **kwargs,\n        )\n        key = alias or prepared.alias\n        try:\n            world_cls = get_world_class(prepared.adapter)\n        except KeyError as exc:\n            raise WorldNotFoundError(model_id) from exc\n\n        world = world_cls(**prepared.kwargs)\n        try:\n            world.initialize(device=self.device, dtype=self.dtype)\n        except Exception as exc:\n            raise WorldInitError(model_id, str(exc)) from exc\n        return world, key, \"shared\"\n\n    def _load_isolated(\n        self,\n        model_id: str,\n        resolved: Any,\n        plan: Any,\n        alias: str | None,\n        progress: Any,\n        allow_fetch: bool,\n        kwargs: dict[str, Any],\n    ) -&gt; tuple[Any, str, str]:\n        from worldkernels.runtime import envs\n        from worldkernels.worlds.remote import RemoteWorld\n\n        if progress is not None:\n            progress.event(\"isolating\", \"running\", f\"reason: {plan.reason}\")\n\n        requirements = list(plan.requirements)\n        if \"worldkernels\" not in \" \".join(requirements):\n            requirements.append(\"worldkernels\")\n\n        envs.materialize_env(\n            model_id,\n            requirements,\n            device=self.device,\n            progress=progress,\n            allow_fetch=allow_fetch,\n        )\n\n        ctor_kwargs: dict[str, Any] = dict(resolved.card.default_kwargs)\n        if resolved.variant is not None:\n            ctor_kwargs[\"variant\"] = resolved.variant\n        if resolved.ckpt_path is not None:\n            ctor_kwargs[\"ckpt_path\"] = resolved.ckpt_path\n        ctor_kwargs.update(kwargs)\n\n        key = alias or self._alias_for(model_id, resolved.variant)\n        world = RemoteWorld(\n            model_id=model_id,\n            adapter=resolved.card.adapter,\n            ctor_kwargs=ctor_kwargs,\n        )\n        try:\n            world.initialize(device=self.device, dtype=self.dtype)\n        except Exception as exc:\n            try:\n                world.close()\n            finally:\n                raise WorldInitError(model_id, str(exc)) from exc\n        return world, key, \"isolated\"\n\n    @staticmethod\n    def _alias_for(model_id: str, variant: str | None) -&gt; str:\n        base = model_id.split(\"/\")[-1]\n        return f\"{base}:{variant}\" if variant else base\n\n    def create_session(\n        self,\n        world: str,\n        config: WorldConfig | None = None,\n        seed: int | None = None,\n        overrides: dict | Any = None,\n    ) -&gt; Session:\n        r\"\"\"Create a new simulation session bound to a loaded world model.\n\n        Args:\n            overrides: `SessionOverrides` or ``{flag: value}`` \u2014 flips a subset\n                of toggles for this session only (`SESSION_OVERRIDE_FIELDS`).\n                Bake-time flags (torch_compile, dtype, quantization,\n                cuda_graphs, kv_cache_paged, latent_pool) are ignored with a\n                ``log.warning`` since they can't change after init/warmup.\n        \"\"\"\n        from worldkernels.config import WorldConfig as WC\n        from worldkernels.config.runtime import SESSION_OVERRIDE_FIELDS\n        from worldkernels.core.session import Session\n\n        if world not in self._worlds:\n            raise WorldNotFoundError(world)\n        if len(self._sessions) &gt;= self.max_sessions:\n            raise SessionLimitError(self.max_sessions)\n\n        ov = _normalize_overrides(overrides)\n        if ov is not None:\n            for k in list(ov):\n                if k not in SESSION_OVERRIDE_FIELDS:\n                    log.warning(\n                        \"ignoring session override %r: not in the per-session-safe subset\",\n                        k,\n                    )\n                    ov.pop(k)\n\n        cfg = config or WC()\n        actual_seed = seed if seed is not None else 0\n        world_instance = self._worlds[world]\n\n        vram_mb = world_instance.profile_vram(cfg)\n        if self.device.startswith(\"cuda\") and torch.cuda.is_available():\n            free_mb = torch.cuda.mem_get_info()[0] / (1024 * 1024)\n            if vram_mb &gt; free_mb:\n                raise VRAMExhaustedError(required_mb=vram_mb, available_mb=free_mb)\n\n        initial_state = world_instance.create_initial_state(cfg, actual_seed)\n        session = Session(\n            world_id=world,\n            config=cfg,\n            state=initial_state,\n            seed=actual_seed,\n            _world=world_instance,\n            _scheduler=self._scheduler,\n            overrides=ov,\n        )\n        self._sessions[session.session_id] = session\n        log.info(\"Created session: %s (world=%s)\", session.session_id, world)\n        return session\n\n    def get_session(self, session_id: str) -&gt; Session | None:\n        return self._sessions.get(session_id)\n\n    def close_session(self, session_id: str) -&gt; None:\n        session = self._sessions.pop(session_id, None)\n        if session is not None:\n            session.close()\n\n    def list_sessions(self) -&gt; list[str]:\n        return list(self._sessions.keys())\n\n    def list_worlds(self) -&gt; list[str]:\n        return list(self._worlds.keys())\n\n    def unload_model(self, name: str) -&gt; None:\n        r\"\"\"Unload a world model and close all its sessions.\"\"\"\n        if name not in self._worlds:\n            raise WorldNotFoundError(name)\n        for sid in [s for s, sess in self._sessions.items() if sess.world_id == name]:\n            self.close_session(sid)\n        world = self._worlds.pop(name)\n        self._tiers.pop(name, None)\n        self._cards.pop(name, None)\n        close = getattr(world, \"close\", None)\n        if callable(close):\n            try:\n                close()\n            except Exception as exc:\n                log.warning(\"error closing world %r: %s\", name, exc)\n        _emit_worker_count(self._tiers)\n        log.info(\"Unloaded world: %s\", name)\n\n    def get_tier(self, name: str) -&gt; str | None:\n        r\"\"\"Return the isolation tier (``\"shared\"`` / ``\"isolated\"``) for a loaded model.\"\"\"\n        return self._tiers.get(name)\n\n    def lookup_trajectory_prefix(self, action_hashes: list[str]):\n        r\"\"\"Return the longest cached prefix of ``action_hashes``, or ``None`` if disabled.\"\"\"\n        if self.trajectory_cache is None:\n            return None\n        return self.trajectory_cache.match(action_hashes)\n\n    def cache_trajectory(self, action_hashes: list[str], block_ids: list[int]) -&gt; bool:\n        r\"\"\"Insert an action-history \u2192 block-id mapping; ``False`` if cache is off.\"\"\"\n        if self.trajectory_cache is None:\n            return False\n        self.trajectory_cache.insert(action_hashes, block_ids)\n        return True\n\n    def list_tiers(self) -&gt; dict[str, str]:\n        return dict(self._tiers)\n\n    def shutdown(self) -&gt; None:\n        for session in self._sessions.values():\n            session.close()\n        self._sessions.clear()\n        for name, world in list(self._worlds.items()):\n            close = getattr(world, \"close\", None)\n            if callable(close):\n                try:\n                    close()\n                except Exception as exc:\n                    log.warning(\"error closing world %r during shutdown: %s\", name, exc)\n        self._worlds.clear()\n        self._tiers.clear()\n        self._cards.clear()\n        log.info(\"WorldEngine shut down.\")\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = cfg\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = cfg.device\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.max_sessions","title":"max_sessions  <code>instance-attribute</code>","text":"<pre><code>max_sessions = cfg.max_sessions\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.offload_idle","title":"offload_idle  <code>instance-attribute</code>","text":"<pre><code>offload_idle = cfg.offload_idle\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype = _resolve_dtype(cfg.dtype, cfg.device)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._worlds","title":"_worlds  <code>instance-attribute</code>","text":"<pre><code>_worlds: dict[str, WorldModel] = {}\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._sessions","title":"_sessions  <code>instance-attribute</code>","text":"<pre><code>_sessions: dict[str, Session] = {}\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._tiers","title":"_tiers  <code>instance-attribute</code>","text":"<pre><code>_tiers: dict[str, str] = {}\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._cards","title":"_cards  <code>instance-attribute</code>","text":"<pre><code>_cards: dict[str, Any] = {}\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.trajectory_cache","title":"trajectory_cache  <code>instance-attribute</code>","text":"<pre><code>trajectory_cache = None\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._worker","title":"_worker  <code>instance-attribute</code>","text":"<pre><code>_worker = Worker(device=self.device, dtype=self.dtype, parallel_config=cfg.parallel, config=cfg)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._scheduler","title":"_scheduler  <code>instance-attribute</code>","text":"<pre><code>_scheduler = Scheduler(self._worker, config=cfg.scheduler)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.__init__","title":"__init__","text":"<pre><code>__init__(config: 'RuntimeConfig | str | None' = None, *, device: str | None = None, max_sessions: int | None = None, offload_idle: bool | None = None) -&gt; None\n</code></pre> <p>Construct the engine from a <code>RuntimeConfig</code> (or profile name).</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>'RuntimeConfig | str | None'</code> <p>A <code>RuntimeConfig</code>, a profile name (<code>\"baseline\"</code> etc.), or <code>None</code> for resolved defaults (honoring <code>WK_*</code> env vars).</p> <code>None</code> <code>device</code> <code>str | None</code> <p>Back-compat override of <code>config.device</code>.</p> <code>None</code> <code>max_sessions</code> <code>int | None</code> <p>Back-compat override of <code>config.max_sessions</code>.</p> <code>None</code> <code>offload_idle</code> <code>bool | None</code> <p>Back-compat override of <code>config.offload_idle</code>.</p> <code>None</code> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def __init__(\n    self,\n    config: \"RuntimeConfig | str | None\" = None,\n    *,\n    device: str | None = None,\n    max_sessions: int | None = None,\n    offload_idle: bool | None = None,\n) -&gt; None:\n    r\"\"\"Construct the engine from a `RuntimeConfig` (or profile name).\n\n    Args:\n        config: A `RuntimeConfig`, a profile name (``\"baseline\"`` etc.), or\n            ``None`` for resolved defaults (honoring ``WK_*`` env vars).\n        device: Back-compat override of ``config.device``.\n        max_sessions: Back-compat override of ``config.max_sessions``.\n        offload_idle: Back-compat override of ``config.offload_idle``.\n    \"\"\"\n    from worldkernels.config.active import set_active_config\n    from worldkernels.config.profiles import resolve_runtime_config\n    from worldkernels.config.runtime import RuntimeConfig\n\n    if isinstance(config, RuntimeConfig):\n        cfg = config\n    elif isinstance(config, str):\n        cfg, _ = resolve_runtime_config(profile=config)\n    else:\n        cfg, _ = resolve_runtime_config()\n\n    if device is not None:\n        cfg.device = device\n    if max_sessions is not None:\n        cfg.max_sessions = max_sessions\n    if offload_idle is not None:\n        cfg.offload_idle = offload_idle\n\n    self.config = cfg\n    set_active_config(cfg)\n\n    self.device = cfg.device\n    self.max_sessions = cfg.max_sessions\n    self.offload_idle = cfg.offload_idle\n    self.dtype = _resolve_dtype(cfg.dtype, cfg.device)\n\n    self._worlds: dict[str, WorldModel] = {}\n    self._sessions: dict[str, Session] = {}\n    self._tiers: dict[str, str] = {}\n    self._cards: dict[str, Any] = {}\n    self.trajectory_cache = None\n    if cfg.trajectory_cache:\n        from worldkernels.runtime.memory.trajectory_cache import TrajectoryCache\n\n        self.trajectory_cache = TrajectoryCache()\n\n    from worldkernels.scheduler import Scheduler\n    from worldkernels.worker import Worker\n\n    self._worker = Worker(\n        device=self.device, dtype=self.dtype, parallel_config=cfg.parallel, config=cfg\n    )\n    self._scheduler = Scheduler(self._worker, config=cfg.scheduler)\n\n    log.info(\n        \"WorldEngine initialized: device=%s, dtype=%s, max_sessions=%d\",\n        self.device,\n        self.dtype,\n        self.max_sessions,\n    )\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.load_model","title":"load_model","text":"<pre><code>load_model(model_id: str, alias: str | None = None, variant: str | None = None, ckpt_path: str | None = None, progress: Any = None, allow_fetch: bool = True, trust_remote_code: bool = False, **kwargs: Any) -&gt; None\n</code></pre> <p>Load a world model from a hub alias, HF repo id, or local checkpoint path.</p> <p>Drives the resolver, which decides whether the model can share the current env or needs an isolated subprocess. Shared models go through <code>worldkernels.bootstrap.prepare</code> and instantiate locally; isolated models materialize a per-model uv venv and are accessed via <code>RemoteWorld</code>.</p> <p>Parameters:</p> Name Type Description Default <code>model_id</code> <code>str</code> <p>Short alias, HF repo id, HF URL, or local path.</p> required <code>alias</code> <code>str | None</code> <p>Override the in-engine key (defaults to a slug of <code>model_id</code>).</p> <code>None</code> <code>variant</code> <code>str | None</code> <p>Pick a variant from the model card.</p> <code>None</code> <code>ckpt_path</code> <code>str | None</code> <p>Bypass HF download with a local checkpoint file.</p> <code>None</code> <code>progress</code> <code>Any</code> <p>Optional <code>ProgressController</code> (CLI/HTTP pass theirs through).</p> <code>None</code> <code>allow_fetch</code> <code>bool</code> <p>If <code>False</code>, errors instead of installing/cloning/downloading.</p> <code>True</code> <code>trust_remote_code</code> <code>bool</code> <p>Allow custom code from HF Hub (reserved).</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Forwarded to the world constructor (overrides card defaults).</p> <code>{}</code> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def load_model(\n    self,\n    model_id: str,\n    alias: str | None = None,\n    variant: str | None = None,\n    ckpt_path: str | None = None,\n    progress: Any = None,\n    allow_fetch: bool = True,\n    trust_remote_code: bool = False,\n    **kwargs: Any,\n) -&gt; None:\n    r\"\"\"Load a world model from a hub alias, HF repo id, or local checkpoint path.\n\n    Drives the resolver, which decides whether the model can share the\n    current env or needs an isolated subprocess. Shared models go\n    through `worldkernels.bootstrap.prepare` and instantiate locally; isolated\n    models materialize a per-model uv venv and are accessed via `RemoteWorld`.\n\n    Args:\n        model_id: Short alias, HF repo id, HF URL, or local path.\n        alias: Override the in-engine key (defaults to a slug of ``model_id``).\n        variant: Pick a variant from the model card.\n        ckpt_path: Bypass HF download with a local checkpoint file.\n        progress: Optional `ProgressController` (CLI/HTTP pass theirs through).\n        allow_fetch: If ``False``, errors instead of installing/cloning/downloading.\n        trust_remote_code: Allow custom code from HF Hub (reserved).\n        **kwargs: Forwarded to the world constructor (overrides card defaults).\n    \"\"\"\n    from worldkernels.bootstrap.errors import ModelNotFoundError as _ModelNotFoundError\n    from worldkernels.bootstrap.resolve import resolve as _resolve_ref\n    from worldkernels.config import WorldConfig as WC\n    from worldkernels.runtime.resolver import IsolatedPlan, resolve_install_plan\n\n    try:\n        resolved = _resolve_ref(model_id, variant=variant, ckpt_path=ckpt_path)\n    except _ModelNotFoundError as exc:\n        raise WorldNotFoundError(model_id) from exc\n    card = resolved.card\n    if card.isolation == \"auto\" and self.config.isolation != \"auto\":\n        import dataclasses\n\n        card = dataclasses.replace(card, isolation=self.config.isolation)\n        resolved = dataclasses.replace(resolved, card=card)\n    plan = resolve_install_plan(card, list(self._cards.values()))\n\n    if isinstance(plan, IsolatedPlan):\n        world, key, tier = self._load_isolated(\n            model_id, resolved, plan, alias, progress, allow_fetch, kwargs\n        )\n    else:\n        world, key, tier = self._load_shared(\n            model_id, alias, variant, ckpt_path, progress, allow_fetch, kwargs\n        )\n\n    if key in self._worlds:\n        try:\n            world.close()  # type: ignore[attr-defined]\n        except Exception:\n            pass\n        raise WorldAlreadyLoadedError(key)\n\n    if getattr(world, \"supports_kv_cache\", False) and self.config.kv_cache_paged:\n        self._worker.runner.ensure_kv_cache(\n            frames_per_block=self.config.cache.block_frames,\n        )\n\n    if self.config.quantization != \"none\":\n        target = getattr(world, \"_quant_target\", None)\n        if target is not None:\n            from worldkernels.runtime.quantization.registry import QuantizationRegistry\n\n            QuantizationRegistry().apply(target, self.config.quantization)\n\n    try:\n        world.warmup(getattr(world, \"default_config\", None) or WC())\n    except Exception as exc:\n        raise WorldInitError(model_id, f\"warmup failed: {exc}\") from exc\n\n    self._worlds[key] = world\n    self._tiers[key] = tier\n    self._cards[key] = card\n    _emit_tier_metric(key, tier)\n    _emit_worker_count(self._tiers)\n    log.info(\"Loaded world: %s (tier=%s, class=%s)\", key, tier, type(world).__name__)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._load_shared","title":"_load_shared","text":"<pre><code>_load_shared(model_id: str, alias: str | None, variant: str | None, ckpt_path: str | None, progress: Any, allow_fetch: bool, kwargs: dict[str, Any]) -&gt; tuple[Any, str, str]\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def _load_shared(\n    self,\n    model_id: str,\n    alias: str | None,\n    variant: str | None,\n    ckpt_path: str | None,\n    progress: Any,\n    allow_fetch: bool,\n    kwargs: dict[str, Any],\n) -&gt; tuple[Any, str, str]:\n    from worldkernels.bootstrap import prepare\n    from worldkernels.worlds.registry import get_world_class\n\n    prepared = prepare(\n        model_id,\n        variant=variant,\n        ckpt_path=ckpt_path,\n        progress=progress,\n        allow_fetch=allow_fetch,\n        **kwargs,\n    )\n    key = alias or prepared.alias\n    try:\n        world_cls = get_world_class(prepared.adapter)\n    except KeyError as exc:\n        raise WorldNotFoundError(model_id) from exc\n\n    world = world_cls(**prepared.kwargs)\n    try:\n        world.initialize(device=self.device, dtype=self.dtype)\n    except Exception as exc:\n        raise WorldInitError(model_id, str(exc)) from exc\n    return world, key, \"shared\"\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._load_isolated","title":"_load_isolated","text":"<pre><code>_load_isolated(model_id: str, resolved: Any, plan: Any, alias: str | None, progress: Any, allow_fetch: bool, kwargs: dict[str, Any]) -&gt; tuple[Any, str, str]\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def _load_isolated(\n    self,\n    model_id: str,\n    resolved: Any,\n    plan: Any,\n    alias: str | None,\n    progress: Any,\n    allow_fetch: bool,\n    kwargs: dict[str, Any],\n) -&gt; tuple[Any, str, str]:\n    from worldkernels.runtime import envs\n    from worldkernels.worlds.remote import RemoteWorld\n\n    if progress is not None:\n        progress.event(\"isolating\", \"running\", f\"reason: {plan.reason}\")\n\n    requirements = list(plan.requirements)\n    if \"worldkernels\" not in \" \".join(requirements):\n        requirements.append(\"worldkernels\")\n\n    envs.materialize_env(\n        model_id,\n        requirements,\n        device=self.device,\n        progress=progress,\n        allow_fetch=allow_fetch,\n    )\n\n    ctor_kwargs: dict[str, Any] = dict(resolved.card.default_kwargs)\n    if resolved.variant is not None:\n        ctor_kwargs[\"variant\"] = resolved.variant\n    if resolved.ckpt_path is not None:\n        ctor_kwargs[\"ckpt_path\"] = resolved.ckpt_path\n    ctor_kwargs.update(kwargs)\n\n    key = alias or self._alias_for(model_id, resolved.variant)\n    world = RemoteWorld(\n        model_id=model_id,\n        adapter=resolved.card.adapter,\n        ctor_kwargs=ctor_kwargs,\n    )\n    try:\n        world.initialize(device=self.device, dtype=self.dtype)\n    except Exception as exc:\n        try:\n            world.close()\n        finally:\n            raise WorldInitError(model_id, str(exc)) from exc\n    return world, key, \"isolated\"\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine._alias_for","title":"_alias_for  <code>staticmethod</code>","text":"<pre><code>_alias_for(model_id: str, variant: str | None) -&gt; str\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>@staticmethod\ndef _alias_for(model_id: str, variant: str | None) -&gt; str:\n    base = model_id.split(\"/\")[-1]\n    return f\"{base}:{variant}\" if variant else base\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.create_session","title":"create_session","text":"<pre><code>create_session(world: str, config: WorldConfig | None = None, seed: int | None = None, overrides: dict | Any = None) -&gt; Session\n</code></pre> <p>Create a new simulation session bound to a loaded world model.</p> <p>Parameters:</p> Name Type Description Default <code>overrides</code> <code>dict | Any</code> <p><code>SessionOverrides</code> or <code>{flag: value}</code> \u2014 flips a subset of toggles for this session only (<code>SESSION_OVERRIDE_FIELDS</code>). Bake-time flags (torch_compile, dtype, quantization, cuda_graphs, kv_cache_paged, latent_pool) are ignored with a <code>log.warning</code> since they can't change after init/warmup.</p> <code>None</code> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def create_session(\n    self,\n    world: str,\n    config: WorldConfig | None = None,\n    seed: int | None = None,\n    overrides: dict | Any = None,\n) -&gt; Session:\n    r\"\"\"Create a new simulation session bound to a loaded world model.\n\n    Args:\n        overrides: `SessionOverrides` or ``{flag: value}`` \u2014 flips a subset\n            of toggles for this session only (`SESSION_OVERRIDE_FIELDS`).\n            Bake-time flags (torch_compile, dtype, quantization,\n            cuda_graphs, kv_cache_paged, latent_pool) are ignored with a\n            ``log.warning`` since they can't change after init/warmup.\n    \"\"\"\n    from worldkernels.config import WorldConfig as WC\n    from worldkernels.config.runtime import SESSION_OVERRIDE_FIELDS\n    from worldkernels.core.session import Session\n\n    if world not in self._worlds:\n        raise WorldNotFoundError(world)\n    if len(self._sessions) &gt;= self.max_sessions:\n        raise SessionLimitError(self.max_sessions)\n\n    ov = _normalize_overrides(overrides)\n    if ov is not None:\n        for k in list(ov):\n            if k not in SESSION_OVERRIDE_FIELDS:\n                log.warning(\n                    \"ignoring session override %r: not in the per-session-safe subset\",\n                    k,\n                )\n                ov.pop(k)\n\n    cfg = config or WC()\n    actual_seed = seed if seed is not None else 0\n    world_instance = self._worlds[world]\n\n    vram_mb = world_instance.profile_vram(cfg)\n    if self.device.startswith(\"cuda\") and torch.cuda.is_available():\n        free_mb = torch.cuda.mem_get_info()[0] / (1024 * 1024)\n        if vram_mb &gt; free_mb:\n            raise VRAMExhaustedError(required_mb=vram_mb, available_mb=free_mb)\n\n    initial_state = world_instance.create_initial_state(cfg, actual_seed)\n    session = Session(\n        world_id=world,\n        config=cfg,\n        state=initial_state,\n        seed=actual_seed,\n        _world=world_instance,\n        _scheduler=self._scheduler,\n        overrides=ov,\n    )\n    self._sessions[session.session_id] = session\n    log.info(\"Created session: %s (world=%s)\", session.session_id, world)\n    return session\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.get_session","title":"get_session","text":"<pre><code>get_session(session_id: str) -&gt; Session | None\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def get_session(self, session_id: str) -&gt; Session | None:\n    return self._sessions.get(session_id)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.close_session","title":"close_session","text":"<pre><code>close_session(session_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def close_session(self, session_id: str) -&gt; None:\n    session = self._sessions.pop(session_id, None)\n    if session is not None:\n        session.close()\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.list_sessions","title":"list_sessions","text":"<pre><code>list_sessions() -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def list_sessions(self) -&gt; list[str]:\n    return list(self._sessions.keys())\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.list_worlds","title":"list_worlds","text":"<pre><code>list_worlds() -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def list_worlds(self) -&gt; list[str]:\n    return list(self._worlds.keys())\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.unload_model","title":"unload_model","text":"<pre><code>unload_model(name: str) -&gt; None\n</code></pre> <p>Unload a world model and close all its sessions.</p> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def unload_model(self, name: str) -&gt; None:\n    r\"\"\"Unload a world model and close all its sessions.\"\"\"\n    if name not in self._worlds:\n        raise WorldNotFoundError(name)\n    for sid in [s for s, sess in self._sessions.items() if sess.world_id == name]:\n        self.close_session(sid)\n    world = self._worlds.pop(name)\n    self._tiers.pop(name, None)\n    self._cards.pop(name, None)\n    close = getattr(world, \"close\", None)\n    if callable(close):\n        try:\n            close()\n        except Exception as exc:\n            log.warning(\"error closing world %r: %s\", name, exc)\n    _emit_worker_count(self._tiers)\n    log.info(\"Unloaded world: %s\", name)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.get_tier","title":"get_tier","text":"<pre><code>get_tier(name: str) -&gt; str | None\n</code></pre> <p>Return the isolation tier (<code>\"shared\"</code> / <code>\"isolated\"</code>) for a loaded model.</p> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def get_tier(self, name: str) -&gt; str | None:\n    r\"\"\"Return the isolation tier (``\"shared\"`` / ``\"isolated\"``) for a loaded model.\"\"\"\n    return self._tiers.get(name)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.lookup_trajectory_prefix","title":"lookup_trajectory_prefix","text":"<pre><code>lookup_trajectory_prefix(action_hashes: list[str])\n</code></pre> <p>Return the longest cached prefix of <code>action_hashes</code>, or <code>None</code> if disabled.</p> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def lookup_trajectory_prefix(self, action_hashes: list[str]):\n    r\"\"\"Return the longest cached prefix of ``action_hashes``, or ``None`` if disabled.\"\"\"\n    if self.trajectory_cache is None:\n        return None\n    return self.trajectory_cache.match(action_hashes)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.cache_trajectory","title":"cache_trajectory","text":"<pre><code>cache_trajectory(action_hashes: list[str], block_ids: list[int]) -&gt; bool\n</code></pre> <p>Insert an action-history \u2192 block-id mapping; <code>False</code> if cache is off.</p> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def cache_trajectory(self, action_hashes: list[str], block_ids: list[int]) -&gt; bool:\n    r\"\"\"Insert an action-history \u2192 block-id mapping; ``False`` if cache is off.\"\"\"\n    if self.trajectory_cache is None:\n        return False\n    self.trajectory_cache.insert(action_hashes, block_ids)\n    return True\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.list_tiers","title":"list_tiers","text":"<pre><code>list_tiers() -&gt; dict[str, str]\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def list_tiers(self) -&gt; dict[str, str]:\n    return dict(self._tiers)\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine.WorldEngine.shutdown","title":"shutdown","text":"<pre><code>shutdown() -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def shutdown(self) -&gt; None:\n    for session in self._sessions.values():\n        session.close()\n    self._sessions.clear()\n    for name, world in list(self._worlds.items()):\n        close = getattr(world, \"close\", None)\n        if callable(close):\n            try:\n                close()\n            except Exception as exc:\n                log.warning(\"error closing world %r during shutdown: %s\", name, exc)\n    self._worlds.clear()\n    self._tiers.clear()\n    self._cards.clear()\n    log.info(\"WorldEngine shut down.\")\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine._default_dtype","title":"_default_dtype","text":"<pre><code>_default_dtype(device: str) -&gt; dtype\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def _default_dtype(device: str) -&gt; torch.dtype:\n    if device == \"cpu\":\n        return torch.float32\n    if torch.cuda.is_available():\n        if torch.cuda.get_device_capability() &gt;= (8, 0):\n            return torch.bfloat16\n        return torch.float16\n    return torch.float32\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine._resolve_dtype","title":"_resolve_dtype","text":"<pre><code>_resolve_dtype(dtype: str, device: str) -&gt; dtype\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def _resolve_dtype(dtype: str, device: str) -&gt; torch.dtype:\n    if dtype == \"auto\":\n        return _default_dtype(device)\n    return {\"bf16\": torch.bfloat16, \"fp16\": torch.float16, \"fp32\": torch.float32}[dtype]\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine._normalize_overrides","title":"_normalize_overrides","text":"<pre><code>_normalize_overrides(value) -&gt; dict | None\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def _normalize_overrides(value) -&gt; dict | None:\n    if value is None:\n        return None\n    if isinstance(value, dict):\n        return {k: v for k, v in value.items() if v is not None}\n    import dataclasses\n\n    if dataclasses.is_dataclass(value) and not isinstance(value, type):\n        return {k: v for k, v in dataclasses.asdict(value).items() if v is not None}\n    raise TypeError(\n        f\"overrides must be dict, SessionOverrides, or None; got {type(value).__name__}\"\n    )\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine._emit_tier_metric","title":"_emit_tier_metric","text":"<pre><code>_emit_tier_metric(model_id: str, tier: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def _emit_tier_metric(model_id: str, tier: str) -&gt; None:\n    try:\n        from worldkernels.runtime import metrics\n\n        metrics.set_isolation_tier(model_id, 0 if tier == \"shared\" else 1)\n    except Exception:\n        pass\n</code></pre>"},{"location":"api/worldkernels/engine/world_engine/#worldkernels.engine.world_engine._emit_worker_count","title":"_emit_worker_count","text":"<pre><code>_emit_worker_count(tiers: dict[str, str]) -&gt; None\n</code></pre> Source code in <code>worldkernels/engine/world_engine.py</code> <pre><code>def _emit_worker_count(tiers: dict[str, str]) -&gt; None:\n    try:\n        from worldkernels.runtime import metrics\n\n        metrics.set_worker_processes(sum(1 for t in tiers.values() if t == \"isolated\"))\n    except Exception:\n        pass\n</code></pre>"},{"location":"api/worldkernels/envs/","title":"envs","text":""},{"location":"api/worldkernels/envs/#worldkernels.envs","title":"worldkernels.envs","text":"<p>Central registry of WorldKernels environment variables.</p>"},{"location":"api/worldkernels/envs/#worldkernels.envs.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['environment_variables', 'EXTERNAL_VARIABLES', 'set_wk_vars']\n</code></pre>"},{"location":"api/worldkernels/envs/#worldkernels.envs.environment_variables","title":"environment_variables  <code>module-attribute</code>","text":"<pre><code>environment_variables: dict[str, Callable[[], Any]] = {'WK_DISABLE': _read('WK_DISABLE', ''), 'WK_ENABLE': _read('WK_ENABLE', ''), 'WK_TARGET_DEVICE': _read('WK_TARGET_DEVICE'), 'WORLDKERNELS_QUIET': _read('WORLDKERNELS_QUIET'), None: {f'WK_{name.upper()}': (_read(f'WK_{name.upper()}')) for name in ALL_TOGGLE_FIELDS}}\n</code></pre>"},{"location":"api/worldkernels/envs/#worldkernels.envs.EXTERNAL_VARIABLES","title":"EXTERNAL_VARIABLES  <code>module-attribute</code>","text":"<pre><code>EXTERNAL_VARIABLES: tuple[str, ...] = ('NO_COLOR', 'CI', 'HF_TOKEN', 'HF_HOME', 'HF_HUB_OFFLINE')\n</code></pre>"},{"location":"api/worldkernels/envs/#worldkernels.envs._read","title":"_read","text":"<pre><code>_read(name: str, default: str | None = None) -&gt; Callable[[], str | None]\n</code></pre> Source code in <code>worldkernels/envs.py</code> <pre><code>def _read(name: str, default: str | None = None) -&gt; Callable[[], str | None]:\n    return lambda: os.environ.get(name, default)\n</code></pre>"},{"location":"api/worldkernels/envs/#worldkernels.envs.set_wk_vars","title":"set_wk_vars","text":"<pre><code>set_wk_vars() -&gt; dict[str, str]\n</code></pre> <p>Registry variables currently present in the environment.</p> Source code in <code>worldkernels/envs.py</code> <pre><code>def set_wk_vars() -&gt; dict[str, str]:\n    r\"\"\"Registry variables currently present in the environment.\"\"\"\n    return {name: os.environ[name] for name in environment_variables if name in os.environ}\n</code></pre>"},{"location":"api/worldkernels/envs/#worldkernels.envs.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str) -&gt; Any\n</code></pre> Source code in <code>worldkernels/envs.py</code> <pre><code>def __getattr__(name: str) -&gt; Any:\n    if name in environment_variables:\n        return environment_variables[name]()\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/integrations/","title":"integrations","text":""},{"location":"api/worldkernels/integrations/#worldkernels.integrations","title":"worldkernels.integrations","text":"<p>Modules:</p> Name Description <code>comfyui</code>"},{"location":"api/worldkernels/integrations/comfyui/","title":"comfyui","text":""},{"location":"api/worldkernels/integrations/comfyui/#worldkernels.integrations.comfyui","title":"worldkernels.integrations.comfyui","text":""},{"location":"api/worldkernels/models/","title":"models","text":""},{"location":"api/worldkernels/models/#worldkernels.models","title":"worldkernels.models","text":"<p>Native video-diffusion model families and their pipelines.</p> <p>One subpackage per model family (<code>wan/</code>, ...), a shared <code>schedulers/</code> package, and a <code>registry</code> mapping family names to pipeline classes. Adding a model is a new subpackage plus one <code>registry</code> row.</p> <p>The <code>registry</code> module is import-light (no torch); pipeline modules are not and are resolved lazily.</p> <p>Modules:</p> Name Description <code>base</code> <p>Video-generator interface.</p> <code>components</code> <p>Shared weight-bearing nn.Modules for native video-diffusion models.</p> <code>dreamdojo</code> <p>DreamDojo model family: native action-conditioned DiT, tokenizer, checkpoints.</p> <code>registry</code> <p>Registry of video-diffusion pipeline classes, keyed by model family.</p> <code>schedulers</code> <p>Schedulers for video-diffusion pipelines.</p> <code>wan</code> <p>Wan-family video-diffusion pipelines (diffusers-backed).</p>"},{"location":"api/worldkernels/models/#worldkernels.models.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['get_pipeline_class', 'list_pipelines', 'register_pipeline']\n</code></pre>"},{"location":"api/worldkernels/models/#worldkernels.models.get_pipeline_class","title":"get_pipeline_class","text":"<pre><code>get_pipeline_class(name: str) -&gt; type\n</code></pre> <p>Resolve a pipeline family name to its class, importing the module lazily.</p> Source code in <code>worldkernels/models/registry.py</code> <pre><code>def get_pipeline_class(name: str) -&gt; type:\n    r\"\"\"Resolve a pipeline family name to its class, importing the module lazily.\"\"\"\n    if name not in _PIPELINES:\n        available = \", \".join(sorted(_PIPELINES)) or \"(none)\"\n        raise KeyError(f\"Unknown pipeline {name!r}. Available: {available}\")\n    module_path, class_name = _PIPELINES[name]\n    return getattr(importlib.import_module(module_path), class_name)\n</code></pre>"},{"location":"api/worldkernels/models/#worldkernels.models.list_pipelines","title":"list_pipelines","text":"<pre><code>list_pipelines() -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/models/registry.py</code> <pre><code>def list_pipelines() -&gt; list[str]:\n    return sorted(_PIPELINES)\n</code></pre>"},{"location":"api/worldkernels/models/#worldkernels.models.register_pipeline","title":"register_pipeline","text":"<pre><code>register_pipeline(name: str, module: str, class_name: str) -&gt; None\n</code></pre> <p>Register a pipeline family <code>name</code> resolving to <code>module:class_name</code>.</p> Source code in <code>worldkernels/models/registry.py</code> <pre><code>def register_pipeline(name: str, module: str, class_name: str) -&gt; None:\n    r\"\"\"Register a pipeline family ``name`` resolving to ``module:class_name``.\"\"\"\n    _PIPELINES[name] = (module, class_name)\n</code></pre>"},{"location":"api/worldkernels/models/base/","title":"base","text":""},{"location":"api/worldkernels/models/base/#worldkernels.models.base","title":"worldkernels.models.base","text":"<p>Video-generator interface.</p> <p>A <code>VideoGenerator</code> is a pipeline: it produces a video clip from conditioning in one forward pass and holds no per-session state. It is the substrate a <code>GeneratorWorld</code> wraps to present a one-shot generator as an interactive world model.</p> <p>Conditioning objects are generator-specific (e.g. <code>WanLatent</code>); they are opaque to the world layer, which only moves them between generator calls.</p>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['GenerationResult', 'VideoGenerator']\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.GenerationResult","title":"GenerationResult  <code>dataclass</code>","text":"<p>Output of one generator forward pass.</p> <p>Parameters:</p> Name Type Description Default <code>latent</code> <code>'torch.Tensor'</code> <p>Denoised latent in the model's latent space.</p> required <code>video</code> <code>'torch.Tensor'</code> <p>Decoded clip of shape <code>(1, 3, frames, H, W)</code> in <code>[-1, 1]</code>.</p> required Source code in <code>worldkernels/models/base.py</code> <pre><code>@dataclass\nclass GenerationResult:\n    r\"\"\"Output of one generator forward pass.\n\n    Args:\n        latent: Denoised latent in the model's latent space.\n        video: Decoded clip of shape ``(1, 3, frames, H, W)`` in ``[-1, 1]``.\n    \"\"\"\n\n    latent: \"torch.Tensor\"\n    video: \"torch.Tensor\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.GenerationResult.latent","title":"latent  <code>instance-attribute</code>","text":"<pre><code>latent: 'torch.Tensor'\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.GenerationResult.video","title":"video  <code>instance-attribute</code>","text":"<pre><code>video: 'torch.Tensor'\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.GenerationResult.__init__","title":"__init__","text":"<pre><code>__init__(latent: 'torch.Tensor', video: 'torch.Tensor') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator","title":"VideoGenerator","text":"<p>               Bases: <code>ABC</code></p> <p>A stateless video-generation pipeline.</p> <p>Subclasses compose components (VAE, transformer, text encoder) and own a single forward pass. The conditioning type is the subclass's own; the world layer treats it opaquely.</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>class VideoGenerator(ABC):\n    r\"\"\"A stateless video-generation pipeline.\n\n    Subclasses compose components (VAE, transformer, text encoder) and own a\n    single forward pass. The conditioning type is the subclass's own; the\n    world layer treats it opaquely.\n    \"\"\"\n\n    @abstractmethod\n    def load(self, device: str, dtype: \"torch.dtype\") -&gt; None:\n        r\"\"\"Load weights and move components to ``device``.\"\"\"\n\n    @abstractmethod\n    def encode_prompt(self, prompt: str) -&gt; \"torch.Tensor\":\n        r\"\"\"Encode a text prompt into a conditioning embedding.\"\"\"\n\n    @abstractmethod\n    def initial_conditioning(\n        self,\n        *,\n        prompt: str,\n        negative_prompt: str,\n        image: Any | None,\n        height: int,\n        width: int,\n        frames_per_step: int,\n        seed: int,\n    ) -&gt; Any:\n        r\"\"\"Build the conditioning for the first generation step.\"\"\"\n\n    @abstractmethod\n    def apply_prompt(self, conditioning: Any, prompt_embeds: \"torch.Tensor\") -&gt; Any:\n        r\"\"\"Return conditioning with its text embedding replaced (mid-rollout action).\"\"\"\n\n    @abstractmethod\n    def generate(\n        self,\n        conditioning: Any,\n        *,\n        num_steps: int,\n        guidance: float,\n        num_frames: int,\n        seed: int,\n    ) -&gt; GenerationResult:\n        r\"\"\"Run one generation forward pass.\"\"\"\n\n    @abstractmethod\n    def advance(\n        self,\n        conditioning: Any,\n        next_image: \"torch.Tensor\",\n    ) -&gt; Any:\n        r\"\"\"Build the next-step conditioning from the previous one and a rollout frame.\"\"\"\n\n    @abstractmethod\n    def decode(self, latent: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        r\"\"\"VAE-decode a latent to a video tensor in ``[-1, 1]``.\"\"\"\n\n    @abstractmethod\n    def profile_vram(self, *, height: int, width: int, num_frames: int) -&gt; float:\n        r\"\"\"Estimate per-session VRAM (MB).\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.load","title":"load  <code>abstractmethod</code>","text":"<pre><code>load(device: str, dtype: 'torch.dtype') -&gt; None\n</code></pre> <p>Load weights and move components to <code>device</code>.</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef load(self, device: str, dtype: \"torch.dtype\") -&gt; None:\n    r\"\"\"Load weights and move components to ``device``.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.encode_prompt","title":"encode_prompt  <code>abstractmethod</code>","text":"<pre><code>encode_prompt(prompt: str) -&gt; 'torch.Tensor'\n</code></pre> <p>Encode a text prompt into a conditioning embedding.</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef encode_prompt(self, prompt: str) -&gt; \"torch.Tensor\":\n    r\"\"\"Encode a text prompt into a conditioning embedding.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.initial_conditioning","title":"initial_conditioning  <code>abstractmethod</code>","text":"<pre><code>initial_conditioning(*, prompt: str, negative_prompt: str, image: Any | None, height: int, width: int, frames_per_step: int, seed: int) -&gt; Any\n</code></pre> <p>Build the conditioning for the first generation step.</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef initial_conditioning(\n    self,\n    *,\n    prompt: str,\n    negative_prompt: str,\n    image: Any | None,\n    height: int,\n    width: int,\n    frames_per_step: int,\n    seed: int,\n) -&gt; Any:\n    r\"\"\"Build the conditioning for the first generation step.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.apply_prompt","title":"apply_prompt  <code>abstractmethod</code>","text":"<pre><code>apply_prompt(conditioning: Any, prompt_embeds: 'torch.Tensor') -&gt; Any\n</code></pre> <p>Return conditioning with its text embedding replaced (mid-rollout action).</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef apply_prompt(self, conditioning: Any, prompt_embeds: \"torch.Tensor\") -&gt; Any:\n    r\"\"\"Return conditioning with its text embedding replaced (mid-rollout action).\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.generate","title":"generate  <code>abstractmethod</code>","text":"<pre><code>generate(conditioning: Any, *, num_steps: int, guidance: float, num_frames: int, seed: int) -&gt; GenerationResult\n</code></pre> <p>Run one generation forward pass.</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef generate(\n    self,\n    conditioning: Any,\n    *,\n    num_steps: int,\n    guidance: float,\n    num_frames: int,\n    seed: int,\n) -&gt; GenerationResult:\n    r\"\"\"Run one generation forward pass.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.advance","title":"advance  <code>abstractmethod</code>","text":"<pre><code>advance(conditioning: Any, next_image: 'torch.Tensor') -&gt; Any\n</code></pre> <p>Build the next-step conditioning from the previous one and a rollout frame.</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef advance(\n    self,\n    conditioning: Any,\n    next_image: \"torch.Tensor\",\n) -&gt; Any:\n    r\"\"\"Build the next-step conditioning from the previous one and a rollout frame.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.decode","title":"decode  <code>abstractmethod</code>","text":"<pre><code>decode(latent: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> <p>VAE-decode a latent to a video tensor in <code>[-1, 1]</code>.</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef decode(self, latent: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    r\"\"\"VAE-decode a latent to a video tensor in ``[-1, 1]``.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/base/#worldkernels.models.base.VideoGenerator.profile_vram","title":"profile_vram  <code>abstractmethod</code>","text":"<pre><code>profile_vram(*, height: int, width: int, num_frames: int) -&gt; float\n</code></pre> <p>Estimate per-session VRAM (MB).</p> Source code in <code>worldkernels/models/base.py</code> <pre><code>@abstractmethod\ndef profile_vram(self, *, height: int, width: int, num_frames: int) -&gt; float:\n    r\"\"\"Estimate per-session VRAM (MB).\"\"\"\n</code></pre>"},{"location":"api/worldkernels/models/components/","title":"components","text":""},{"location":"api/worldkernels/models/components/#worldkernels.models.components","title":"worldkernels.models.components","text":"<p>Shared weight-bearing nn.Modules for native video-diffusion models.</p> <p>Modules:</p> Name Description <code>attention</code> <p>Multi-head attention with QK RMSNorm, RoPE, and a frame-granular KV cache.</p> <code>dit</code> <p>adaLN-modulated video DiT blocks and the assembled backbone.</p> <code>embeddings</code> <p>Timestep, patch, and rotary position embeddings for native video DiTs.</p> <code>normalization</code> <p>Normalization layers for native video-diffusion models.</p>"},{"location":"api/worldkernels/models/components/#worldkernels.models.components.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RMSNorm', 'SinusoidalTimesteps', 'TimestepEmbedding', 'PatchEmbed', 'VideoRoPE3D', 'apply_rotary_emb', 'Attention', 'FrameKVCache', 'FeedForward', 'DiTBlock', 'FinalLayer', 'VideoDiT']\n</code></pre>"},{"location":"api/worldkernels/models/components/#worldkernels.models.components._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'RMSNorm': ('worldkernels.models.components.normalization', 'RMSNorm'), 'SinusoidalTimesteps': ('worldkernels.models.components.embeddings', 'SinusoidalTimesteps'), 'TimestepEmbedding': ('worldkernels.models.components.embeddings', 'TimestepEmbedding'), 'PatchEmbed': ('worldkernels.models.components.embeddings', 'PatchEmbed'), 'VideoRoPE3D': ('worldkernels.models.components.embeddings', 'VideoRoPE3D'), 'apply_rotary_emb': ('worldkernels.models.components.embeddings', 'apply_rotary_emb'), 'Attention': ('worldkernels.models.components.attention', 'Attention'), 'FrameKVCache': ('worldkernels.models.components.attention', 'FrameKVCache'), 'FeedForward': ('worldkernels.models.components.dit', 'FeedForward'), 'DiTBlock': ('worldkernels.models.components.dit', 'DiTBlock'), 'FinalLayer': ('worldkernels.models.components.dit', 'FinalLayer'), 'VideoDiT': ('worldkernels.models.components.dit', 'VideoDiT')}\n</code></pre>"},{"location":"api/worldkernels/models/components/#worldkernels.models.components.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/models/components/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/","title":"attention","text":""},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention","title":"worldkernels.models.components.attention","text":"<p>Multi-head attention with QK RMSNorm, RoPE, and a frame-granular KV cache.</p>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Attention', 'FrameKVCache']\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache","title":"FrameKVCache","text":"<p>Rolling per-frame K/V cache over preallocated tensors.</p> <p>Entries are stored per latent frame with shape <code>(B, HW, heads, head_dim)</code> inside <code>(max_frames, B, HW, heads, head_dim)</code> buffers. Reads gather a rolling window of at most <code>window</code> frames preceding the current index and concatenate the current frame's K/V.</p> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>class FrameKVCache:\n    r\"\"\"Rolling per-frame K/V cache over preallocated tensors.\n\n    Entries are stored per latent frame with shape ``(B, HW, heads, head_dim)``\n    inside ``(max_frames, B, HW, heads, head_dim)`` buffers. Reads gather a\n    rolling window of at most ``window`` frames preceding the current index and\n    concatenate the current frame's K/V.\n    \"\"\"\n\n    def __init__(self, k: torch.Tensor, v: torch.Tensor, window: int | None = None) -&gt; None:\n        self.k = k\n        self.v = v\n        self.window = window\n\n    @classmethod\n    def allocate(\n        cls,\n        *,\n        max_frames: int,\n        batch_size: int,\n        tokens_per_frame: int,\n        num_heads: int,\n        head_dim: int,\n        device: torch.device | str,\n        dtype: torch.dtype,\n        window: int | None = None,\n    ) -&gt; \"FrameKVCache\":\n        shape = (max_frames, batch_size, tokens_per_frame, num_heads, head_dim)\n        return cls(\n            torch.zeros(shape, device=device, dtype=dtype),\n            torch.zeros(shape, device=device, dtype=dtype),\n            window=window,\n        )\n\n    def store(self, frame_idx: int, k: torch.Tensor, v: torch.Tensor) -&gt; None:\n        self.k[frame_idx] = k\n        self.v[frame_idx] = v\n\n    def gather(\n        self, frame_idx: int, k: torch.Tensor, v: torch.Tensor\n    ) -&gt; tuple[torch.Tensor, torch.Tensor]:\n        r\"\"\"Concatenate the rolling window of cached frames with the current K/V.\"\"\"\n        if frame_idx == 0:\n            return k, v\n        start = max(0, frame_idx - self.window) if self.window is not None else 0\n        B = k.shape[0]\n        cached_k = self.k[start:frame_idx].transpose(0, 1).reshape(B, -1, *k.shape[-2:])\n        cached_v = self.v[start:frame_idx].transpose(0, 1).reshape(B, -1, *v.shape[-2:])\n        return torch.cat([cached_k, k], dim=1), torch.cat([cached_v, v], dim=1)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache.k","title":"k  <code>instance-attribute</code>","text":"<pre><code>k = k\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache.v","title":"v  <code>instance-attribute</code>","text":"<pre><code>v = v\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache.window","title":"window  <code>instance-attribute</code>","text":"<pre><code>window = window\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache.__init__","title":"__init__","text":"<pre><code>__init__(k: Tensor, v: Tensor, window: int | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>def __init__(self, k: torch.Tensor, v: torch.Tensor, window: int | None = None) -&gt; None:\n    self.k = k\n    self.v = v\n    self.window = window\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache.allocate","title":"allocate  <code>classmethod</code>","text":"<pre><code>allocate(*, max_frames: int, batch_size: int, tokens_per_frame: int, num_heads: int, head_dim: int, device: device | str, dtype: dtype, window: int | None = None) -&gt; 'FrameKVCache'\n</code></pre> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>@classmethod\ndef allocate(\n    cls,\n    *,\n    max_frames: int,\n    batch_size: int,\n    tokens_per_frame: int,\n    num_heads: int,\n    head_dim: int,\n    device: torch.device | str,\n    dtype: torch.dtype,\n    window: int | None = None,\n) -&gt; \"FrameKVCache\":\n    shape = (max_frames, batch_size, tokens_per_frame, num_heads, head_dim)\n    return cls(\n        torch.zeros(shape, device=device, dtype=dtype),\n        torch.zeros(shape, device=device, dtype=dtype),\n        window=window,\n    )\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache.store","title":"store","text":"<pre><code>store(frame_idx: int, k: Tensor, v: Tensor) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>def store(self, frame_idx: int, k: torch.Tensor, v: torch.Tensor) -&gt; None:\n    self.k[frame_idx] = k\n    self.v[frame_idx] = v\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.FrameKVCache.gather","title":"gather","text":"<pre><code>gather(frame_idx: int, k: Tensor, v: Tensor) -&gt; tuple[Tensor, Tensor]\n</code></pre> <p>Concatenate the rolling window of cached frames with the current K/V.</p> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>def gather(\n    self, frame_idx: int, k: torch.Tensor, v: torch.Tensor\n) -&gt; tuple[torch.Tensor, torch.Tensor]:\n    r\"\"\"Concatenate the rolling window of cached frames with the current K/V.\"\"\"\n    if frame_idx == 0:\n        return k, v\n    start = max(0, frame_idx - self.window) if self.window is not None else 0\n    B = k.shape[0]\n    cached_k = self.k[start:frame_idx].transpose(0, 1).reshape(B, -1, *k.shape[-2:])\n    cached_v = self.v[start:frame_idx].transpose(0, 1).reshape(B, -1, *v.shape[-2:])\n    return torch.cat([cached_k, k], dim=1), torch.cat([cached_v, v], dim=1)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention","title":"Attention","text":"<p>               Bases: <code>Module</code></p> <p>Self- or cross-attention with per-head QK RMSNorm and optional RoPE.</p> <p>Self-attention mode (<code>context_dim=None</code>) applies rotary embeddings to Q/K, in float32 when <code>use_wan_fp32_strategy</code> is set, and supports the frame-granular KV cache for causal streaming. The attention op routes through the runtime attention-backend selector.</p> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>class Attention(nn.Module):\n    r\"\"\"Self- or cross-attention with per-head QK RMSNorm and optional RoPE.\n\n    Self-attention mode (``context_dim=None``) applies rotary embeddings to\n    Q/K, in float32 when ``use_wan_fp32_strategy`` is set, and supports the\n    frame-granular KV cache for causal streaming. The attention op routes\n    through the runtime attention-backend selector.\n    \"\"\"\n\n    def __init__(\n        self,\n        query_dim: int,\n        context_dim: int | None = None,\n        n_heads: int = 8,\n        head_dim: int = 64,\n        use_wan_fp32_strategy: bool = False,\n    ) -&gt; None:\n        super().__init__()\n        self.is_selfattn = context_dim is None\n        context_dim = query_dim if context_dim is None else context_dim\n        inner_dim = head_dim * n_heads\n\n        self.n_heads = n_heads\n        self.head_dim = head_dim\n        self.use_wan_fp32_strategy = use_wan_fp32_strategy\n\n        self.q_proj = nn.Linear(query_dim, inner_dim, bias=False)\n        self.q_norm = RMSNorm(head_dim, eps=1e-6)\n        self.k_proj = nn.Linear(context_dim, inner_dim, bias=False)\n        self.k_norm = RMSNorm(head_dim, eps=1e-6)\n        self.v_proj = nn.Linear(context_dim, inner_dim, bias=False)\n        self.output_proj = nn.Linear(inner_dim, query_dim, bias=False)\n\n    def forward(\n        self,\n        x: torch.Tensor,\n        context: torch.Tensor | None = None,\n        rope_emb: torch.Tensor | None = None,\n        kv_cache: FrameKVCache | None = None,\n        frame_idx: int = 0,\n        store_kv: bool = False,\n    ) -&gt; torch.Tensor:\n        q = self.q_proj(x)\n        kv_input = x if context is None else context\n        k = self.k_proj(kv_input)\n        v = self.v_proj(kv_input)\n        q, k, v = (\n            rearrange(t, \"b s (h d) -&gt; b s h d\", h=self.n_heads, d=self.head_dim) for t in (q, k, v)\n        )\n\n        q = self.q_norm(q)\n        k = self.k_norm(k)\n        if self.is_selfattn and rope_emb is not None:\n            original_dtype = q.dtype\n            if self.use_wan_fp32_strategy:\n                q = q.to(torch.float32)\n                k = k.to(torch.float32)\n            q = apply_rotary_emb(q, rope_emb)\n            k = apply_rotary_emb(k, rope_emb)\n            if self.use_wan_fp32_strategy:\n                q = q.to(original_dtype)\n                k = k.to(original_dtype)\n\n        if kv_cache is not None:\n            if store_kv:\n                kv_cache.store(frame_idx, k, v)\n            k, v = kv_cache.gather(frame_idx, k, v)\n\n        from worldkernels.runtime.attention.selector import select_attention_backend\n\n        out = select_attention_backend().forward(q, k, v)\n        return self.output_proj(rearrange(out, \"b s h d -&gt; b s (h d)\"))\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.is_selfattn","title":"is_selfattn  <code>instance-attribute</code>","text":"<pre><code>is_selfattn = context_dim is None\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.n_heads","title":"n_heads  <code>instance-attribute</code>","text":"<pre><code>n_heads = n_heads\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.head_dim","title":"head_dim  <code>instance-attribute</code>","text":"<pre><code>head_dim = head_dim\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.use_wan_fp32_strategy","title":"use_wan_fp32_strategy  <code>instance-attribute</code>","text":"<pre><code>use_wan_fp32_strategy = use_wan_fp32_strategy\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.q_proj","title":"q_proj  <code>instance-attribute</code>","text":"<pre><code>q_proj = nn.Linear(query_dim, inner_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.q_norm","title":"q_norm  <code>instance-attribute</code>","text":"<pre><code>q_norm = RMSNorm(head_dim, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.k_proj","title":"k_proj  <code>instance-attribute</code>","text":"<pre><code>k_proj = nn.Linear(context_dim, inner_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.k_norm","title":"k_norm  <code>instance-attribute</code>","text":"<pre><code>k_norm = RMSNorm(head_dim, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.v_proj","title":"v_proj  <code>instance-attribute</code>","text":"<pre><code>v_proj = nn.Linear(context_dim, inner_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.output_proj","title":"output_proj  <code>instance-attribute</code>","text":"<pre><code>output_proj = nn.Linear(inner_dim, query_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.__init__","title":"__init__","text":"<pre><code>__init__(query_dim: int, context_dim: int | None = None, n_heads: int = 8, head_dim: int = 64, use_wan_fp32_strategy: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>def __init__(\n    self,\n    query_dim: int,\n    context_dim: int | None = None,\n    n_heads: int = 8,\n    head_dim: int = 64,\n    use_wan_fp32_strategy: bool = False,\n) -&gt; None:\n    super().__init__()\n    self.is_selfattn = context_dim is None\n    context_dim = query_dim if context_dim is None else context_dim\n    inner_dim = head_dim * n_heads\n\n    self.n_heads = n_heads\n    self.head_dim = head_dim\n    self.use_wan_fp32_strategy = use_wan_fp32_strategy\n\n    self.q_proj = nn.Linear(query_dim, inner_dim, bias=False)\n    self.q_norm = RMSNorm(head_dim, eps=1e-6)\n    self.k_proj = nn.Linear(context_dim, inner_dim, bias=False)\n    self.k_norm = RMSNorm(head_dim, eps=1e-6)\n    self.v_proj = nn.Linear(context_dim, inner_dim, bias=False)\n    self.output_proj = nn.Linear(inner_dim, query_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/attention/#worldkernels.models.components.attention.Attention.forward","title":"forward","text":"<pre><code>forward(x: Tensor, context: Tensor | None = None, rope_emb: Tensor | None = None, kv_cache: FrameKVCache | None = None, frame_idx: int = 0, store_kv: bool = False) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/attention.py</code> <pre><code>def forward(\n    self,\n    x: torch.Tensor,\n    context: torch.Tensor | None = None,\n    rope_emb: torch.Tensor | None = None,\n    kv_cache: FrameKVCache | None = None,\n    frame_idx: int = 0,\n    store_kv: bool = False,\n) -&gt; torch.Tensor:\n    q = self.q_proj(x)\n    kv_input = x if context is None else context\n    k = self.k_proj(kv_input)\n    v = self.v_proj(kv_input)\n    q, k, v = (\n        rearrange(t, \"b s (h d) -&gt; b s h d\", h=self.n_heads, d=self.head_dim) for t in (q, k, v)\n    )\n\n    q = self.q_norm(q)\n    k = self.k_norm(k)\n    if self.is_selfattn and rope_emb is not None:\n        original_dtype = q.dtype\n        if self.use_wan_fp32_strategy:\n            q = q.to(torch.float32)\n            k = k.to(torch.float32)\n        q = apply_rotary_emb(q, rope_emb)\n        k = apply_rotary_emb(k, rope_emb)\n        if self.use_wan_fp32_strategy:\n            q = q.to(original_dtype)\n            k = k.to(original_dtype)\n\n    if kv_cache is not None:\n        if store_kv:\n            kv_cache.store(frame_idx, k, v)\n        k, v = kv_cache.gather(frame_idx, k, v)\n\n    from worldkernels.runtime.attention.selector import select_attention_backend\n\n    out = select_attention_backend().forward(q, k, v)\n    return self.output_proj(rearrange(out, \"b s h d -&gt; b s (h d)\"))\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/","title":"dit","text":""},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit","title":"worldkernels.models.components.dit","text":"<p>adaLN-modulated video DiT blocks and the assembled backbone.</p>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['FeedForward', 'DiTBlock', 'FinalLayer', 'VideoDiT']\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FeedForward","title":"FeedForward","text":"<p>               Bases: <code>Module</code></p> <p>GPT2-style two-layer MLP with GELU, no biases.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>class FeedForward(nn.Module):\n    r\"\"\"GPT2-style two-layer MLP with GELU, no biases.\"\"\"\n\n    def __init__(self, d_model: int, d_ff: int) -&gt; None:\n        super().__init__()\n        self.activation = nn.GELU()\n        self.layer1 = nn.Linear(d_model, d_ff, bias=False)\n        self.layer2 = nn.Linear(d_ff, d_model, bias=False)\n\n    def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n        return self.layer2(self.activation(self.layer1(x)))\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FeedForward.activation","title":"activation  <code>instance-attribute</code>","text":"<pre><code>activation = nn.GELU()\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FeedForward.layer1","title":"layer1  <code>instance-attribute</code>","text":"<pre><code>layer1 = nn.Linear(d_model, d_ff, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FeedForward.layer2","title":"layer2  <code>instance-attribute</code>","text":"<pre><code>layer2 = nn.Linear(d_ff, d_model, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FeedForward.__init__","title":"__init__","text":"<pre><code>__init__(d_model: int, d_ff: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def __init__(self, d_model: int, d_ff: int) -&gt; None:\n    super().__init__()\n    self.activation = nn.GELU()\n    self.layer1 = nn.Linear(d_model, d_ff, bias=False)\n    self.layer2 = nn.Linear(d_ff, d_model, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FeedForward.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n    return self.layer2(self.activation(self.layer1(x)))\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock","title":"DiTBlock","text":"<p>               Bases: <code>Module</code></p> <p>Self-attention, cross-attention, and MLP, each with adaLN(-LoRA) modulation.</p> <p>Modulation parameters are computed in float32 under the Wan strategy; the modulated streams run in the residual dtype. Residual update expressions keep the reference operand order for bit-comparable accumulation.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>class DiTBlock(nn.Module):\n    r\"\"\"Self-attention, cross-attention, and MLP, each with adaLN(-LoRA) modulation.\n\n    Modulation parameters are computed in float32 under the Wan strategy; the\n    modulated streams run in the residual dtype. Residual update expressions\n    keep the reference operand order for bit-comparable accumulation.\n    \"\"\"\n\n    def __init__(\n        self,\n        x_dim: int,\n        context_dim: int,\n        num_heads: int,\n        mlp_ratio: float = 4.0,\n        use_adaln_lora: bool = False,\n        adaln_lora_dim: int = 256,\n        use_wan_fp32_strategy: bool = False,\n    ) -&gt; None:\n        super().__init__()\n        self.use_adaln_lora = use_adaln_lora\n        self.use_wan_fp32_strategy = use_wan_fp32_strategy\n\n        self.layer_norm_self_attn = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-6)\n        self.self_attn = Attention(\n            x_dim, None, num_heads, x_dim // num_heads, use_wan_fp32_strategy=use_wan_fp32_strategy\n        )\n        self.layer_norm_cross_attn = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-6)\n        self.cross_attn = Attention(x_dim, context_dim, num_heads, x_dim // num_heads)\n        self.layer_norm_mlp = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-6)\n        self.mlp = FeedForward(x_dim, int(x_dim * mlp_ratio))\n\n        self.adaln_modulation_self_attn = _adaln_modulation(\n            x_dim, 3, use_adaln_lora, adaln_lora_dim\n        )\n        self.adaln_modulation_cross_attn = _adaln_modulation(\n            x_dim, 3, use_adaln_lora, adaln_lora_dim\n        )\n        self.adaln_modulation_mlp = _adaln_modulation(x_dim, 3, use_adaln_lora, adaln_lora_dim)\n\n    def forward(\n        self,\n        x_B_T_H_W_D: torch.Tensor,\n        emb_B_T_D: torch.Tensor,\n        crossattn_emb: torch.Tensor,\n        rope_emb_L_1_1_D: torch.Tensor | None = None,\n        adaln_lora_B_T_3D: torch.Tensor | None = None,\n        kv_cache: FrameKVCache | None = None,\n        frame_idx: int = 0,\n        store_kv: bool = False,\n    ) -&gt; torch.Tensor:\n        with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n            if self.use_adaln_lora:\n                assert adaln_lora_B_T_3D is not None\n                mod_self = (self.adaln_modulation_self_attn(emb_B_T_D) + adaln_lora_B_T_3D).chunk(\n                    3, dim=-1\n                )\n                mod_cross = (self.adaln_modulation_cross_attn(emb_B_T_D) + adaln_lora_B_T_3D).chunk(\n                    3, dim=-1\n                )\n                mod_mlp = (self.adaln_modulation_mlp(emb_B_T_D) + adaln_lora_B_T_3D).chunk(\n                    3, dim=-1\n                )\n            else:\n                mod_self = self.adaln_modulation_self_attn(emb_B_T_D).chunk(3, dim=-1)\n                mod_cross = self.adaln_modulation_cross_attn(emb_B_T_D).chunk(3, dim=-1)\n                mod_mlp = self.adaln_modulation_mlp(emb_B_T_D).chunk(3, dim=-1)\n\n        def _expand(t: torch.Tensor) -&gt; torch.Tensor:\n            return rearrange(t, \"b t d -&gt; b t 1 1 d\").type_as(x_B_T_H_W_D)\n\n        shift_sa, scale_sa, gate_sa = (_expand(t) for t in mod_self)\n        shift_ca, scale_ca, gate_ca = (_expand(t) for t in mod_cross)\n        shift_mlp, scale_mlp, gate_mlp = (_expand(t) for t in mod_mlp)\n\n        B, T, H, W, D = x_B_T_H_W_D.shape\n\n        normalized = _modulate(x_B_T_H_W_D, self.layer_norm_self_attn, scale_sa, shift_sa)\n        result = rearrange(\n            self.self_attn(\n                rearrange(normalized, \"b t h w d -&gt; b (t h w) d\"),\n                None,\n                rope_emb=rope_emb_L_1_1_D,\n                kv_cache=kv_cache,\n                frame_idx=frame_idx,\n                store_kv=store_kv,\n            ),\n            \"b (t h w) d -&gt; b t h w d\",\n            t=T,\n            h=H,\n            w=W,\n        )\n        x_B_T_H_W_D = x_B_T_H_W_D + gate_sa * result\n\n        normalized = _modulate(x_B_T_H_W_D, self.layer_norm_cross_attn, scale_ca, shift_ca)\n        result = rearrange(\n            self.cross_attn(rearrange(normalized, \"b t h w d -&gt; b (t h w) d\"), crossattn_emb),\n            \"b (t h w) d -&gt; b t h w d\",\n            t=T,\n            h=H,\n            w=W,\n        )\n        x_B_T_H_W_D = result * gate_ca + x_B_T_H_W_D\n\n        normalized = _modulate(x_B_T_H_W_D, self.layer_norm_mlp, scale_mlp, shift_mlp)\n        x_B_T_H_W_D = x_B_T_H_W_D + gate_mlp * self.mlp(normalized)\n        return x_B_T_H_W_D\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.use_adaln_lora","title":"use_adaln_lora  <code>instance-attribute</code>","text":"<pre><code>use_adaln_lora = use_adaln_lora\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.use_wan_fp32_strategy","title":"use_wan_fp32_strategy  <code>instance-attribute</code>","text":"<pre><code>use_wan_fp32_strategy = use_wan_fp32_strategy\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.layer_norm_self_attn","title":"layer_norm_self_attn  <code>instance-attribute</code>","text":"<pre><code>layer_norm_self_attn = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.self_attn","title":"self_attn  <code>instance-attribute</code>","text":"<pre><code>self_attn = Attention(x_dim, None, num_heads, x_dim // num_heads, use_wan_fp32_strategy=use_wan_fp32_strategy)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.layer_norm_cross_attn","title":"layer_norm_cross_attn  <code>instance-attribute</code>","text":"<pre><code>layer_norm_cross_attn = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.cross_attn","title":"cross_attn  <code>instance-attribute</code>","text":"<pre><code>cross_attn = Attention(x_dim, context_dim, num_heads, x_dim // num_heads)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.layer_norm_mlp","title":"layer_norm_mlp  <code>instance-attribute</code>","text":"<pre><code>layer_norm_mlp = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.mlp","title":"mlp  <code>instance-attribute</code>","text":"<pre><code>mlp = FeedForward(x_dim, int(x_dim * mlp_ratio))\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.adaln_modulation_self_attn","title":"adaln_modulation_self_attn  <code>instance-attribute</code>","text":"<pre><code>adaln_modulation_self_attn = _adaln_modulation(x_dim, 3, use_adaln_lora, adaln_lora_dim)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.adaln_modulation_cross_attn","title":"adaln_modulation_cross_attn  <code>instance-attribute</code>","text":"<pre><code>adaln_modulation_cross_attn = _adaln_modulation(x_dim, 3, use_adaln_lora, adaln_lora_dim)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.adaln_modulation_mlp","title":"adaln_modulation_mlp  <code>instance-attribute</code>","text":"<pre><code>adaln_modulation_mlp = _adaln_modulation(x_dim, 3, use_adaln_lora, adaln_lora_dim)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.__init__","title":"__init__","text":"<pre><code>__init__(x_dim: int, context_dim: int, num_heads: int, mlp_ratio: float = 4.0, use_adaln_lora: bool = False, adaln_lora_dim: int = 256, use_wan_fp32_strategy: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def __init__(\n    self,\n    x_dim: int,\n    context_dim: int,\n    num_heads: int,\n    mlp_ratio: float = 4.0,\n    use_adaln_lora: bool = False,\n    adaln_lora_dim: int = 256,\n    use_wan_fp32_strategy: bool = False,\n) -&gt; None:\n    super().__init__()\n    self.use_adaln_lora = use_adaln_lora\n    self.use_wan_fp32_strategy = use_wan_fp32_strategy\n\n    self.layer_norm_self_attn = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-6)\n    self.self_attn = Attention(\n        x_dim, None, num_heads, x_dim // num_heads, use_wan_fp32_strategy=use_wan_fp32_strategy\n    )\n    self.layer_norm_cross_attn = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-6)\n    self.cross_attn = Attention(x_dim, context_dim, num_heads, x_dim // num_heads)\n    self.layer_norm_mlp = nn.LayerNorm(x_dim, elementwise_affine=False, eps=1e-6)\n    self.mlp = FeedForward(x_dim, int(x_dim * mlp_ratio))\n\n    self.adaln_modulation_self_attn = _adaln_modulation(\n        x_dim, 3, use_adaln_lora, adaln_lora_dim\n    )\n    self.adaln_modulation_cross_attn = _adaln_modulation(\n        x_dim, 3, use_adaln_lora, adaln_lora_dim\n    )\n    self.adaln_modulation_mlp = _adaln_modulation(x_dim, 3, use_adaln_lora, adaln_lora_dim)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.DiTBlock.forward","title":"forward","text":"<pre><code>forward(x_B_T_H_W_D: Tensor, emb_B_T_D: Tensor, crossattn_emb: Tensor, rope_emb_L_1_1_D: Tensor | None = None, adaln_lora_B_T_3D: Tensor | None = None, kv_cache: FrameKVCache | None = None, frame_idx: int = 0, store_kv: bool = False) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def forward(\n    self,\n    x_B_T_H_W_D: torch.Tensor,\n    emb_B_T_D: torch.Tensor,\n    crossattn_emb: torch.Tensor,\n    rope_emb_L_1_1_D: torch.Tensor | None = None,\n    adaln_lora_B_T_3D: torch.Tensor | None = None,\n    kv_cache: FrameKVCache | None = None,\n    frame_idx: int = 0,\n    store_kv: bool = False,\n) -&gt; torch.Tensor:\n    with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n        if self.use_adaln_lora:\n            assert adaln_lora_B_T_3D is not None\n            mod_self = (self.adaln_modulation_self_attn(emb_B_T_D) + adaln_lora_B_T_3D).chunk(\n                3, dim=-1\n            )\n            mod_cross = (self.adaln_modulation_cross_attn(emb_B_T_D) + adaln_lora_B_T_3D).chunk(\n                3, dim=-1\n            )\n            mod_mlp = (self.adaln_modulation_mlp(emb_B_T_D) + adaln_lora_B_T_3D).chunk(\n                3, dim=-1\n            )\n        else:\n            mod_self = self.adaln_modulation_self_attn(emb_B_T_D).chunk(3, dim=-1)\n            mod_cross = self.adaln_modulation_cross_attn(emb_B_T_D).chunk(3, dim=-1)\n            mod_mlp = self.adaln_modulation_mlp(emb_B_T_D).chunk(3, dim=-1)\n\n    def _expand(t: torch.Tensor) -&gt; torch.Tensor:\n        return rearrange(t, \"b t d -&gt; b t 1 1 d\").type_as(x_B_T_H_W_D)\n\n    shift_sa, scale_sa, gate_sa = (_expand(t) for t in mod_self)\n    shift_ca, scale_ca, gate_ca = (_expand(t) for t in mod_cross)\n    shift_mlp, scale_mlp, gate_mlp = (_expand(t) for t in mod_mlp)\n\n    B, T, H, W, D = x_B_T_H_W_D.shape\n\n    normalized = _modulate(x_B_T_H_W_D, self.layer_norm_self_attn, scale_sa, shift_sa)\n    result = rearrange(\n        self.self_attn(\n            rearrange(normalized, \"b t h w d -&gt; b (t h w) d\"),\n            None,\n            rope_emb=rope_emb_L_1_1_D,\n            kv_cache=kv_cache,\n            frame_idx=frame_idx,\n            store_kv=store_kv,\n        ),\n        \"b (t h w) d -&gt; b t h w d\",\n        t=T,\n        h=H,\n        w=W,\n    )\n    x_B_T_H_W_D = x_B_T_H_W_D + gate_sa * result\n\n    normalized = _modulate(x_B_T_H_W_D, self.layer_norm_cross_attn, scale_ca, shift_ca)\n    result = rearrange(\n        self.cross_attn(rearrange(normalized, \"b t h w d -&gt; b (t h w) d\"), crossattn_emb),\n        \"b (t h w) d -&gt; b t h w d\",\n        t=T,\n        h=H,\n        w=W,\n    )\n    x_B_T_H_W_D = result * gate_ca + x_B_T_H_W_D\n\n    normalized = _modulate(x_B_T_H_W_D, self.layer_norm_mlp, scale_mlp, shift_mlp)\n    x_B_T_H_W_D = x_B_T_H_W_D + gate_mlp * self.mlp(normalized)\n    return x_B_T_H_W_D\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer","title":"FinalLayer","text":"<p>               Bases: <code>Module</code></p> <p>adaLN-modulated projection from model width to patch pixels.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>class FinalLayer(nn.Module):\n    r\"\"\"adaLN-modulated projection from model width to patch pixels.\"\"\"\n\n    def __init__(\n        self,\n        hidden_size: int,\n        spatial_patch_size: int,\n        temporal_patch_size: int,\n        out_channels: int,\n        use_adaln_lora: bool = False,\n        adaln_lora_dim: int = 256,\n        use_wan_fp32_strategy: bool = False,\n    ) -&gt; None:\n        super().__init__()\n        self.hidden_size = hidden_size\n        self.use_adaln_lora = use_adaln_lora\n        self.use_wan_fp32_strategy = use_wan_fp32_strategy\n        self.layer_norm = nn.LayerNorm(hidden_size, elementwise_affine=False, eps=1e-6)\n        self.linear = nn.Linear(\n            hidden_size,\n            spatial_patch_size * spatial_patch_size * temporal_patch_size * out_channels,\n            bias=False,\n        )\n        self.adaln_modulation = _adaln_modulation(hidden_size, 2, use_adaln_lora, adaln_lora_dim)\n\n    def forward(\n        self,\n        x_B_T_H_W_D: torch.Tensor,\n        emb_B_T_D: torch.Tensor,\n        adaln_lora_B_T_3D: torch.Tensor | None = None,\n    ) -&gt; torch.Tensor:\n        if self.use_wan_fp32_strategy:\n            assert emb_B_T_D.dtype == torch.float32\n        with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n            if self.use_adaln_lora:\n                assert adaln_lora_B_T_3D is not None\n                shift, scale = (\n                    self.adaln_modulation(emb_B_T_D)\n                    + adaln_lora_B_T_3D[:, :, : 2 * self.hidden_size]\n                ).chunk(2, dim=-1)\n            else:\n                shift, scale = self.adaln_modulation(emb_B_T_D).chunk(2, dim=-1)\n            shift = rearrange(shift, \"b t d -&gt; b t 1 1 d\")\n            scale = rearrange(scale, \"b t d -&gt; b t 1 1 d\")\n            x_B_T_H_W_D = _modulate(x_B_T_H_W_D, self.layer_norm, scale, shift)\n            x_B_T_H_W_O = self.linear(x_B_T_H_W_D)\n        return x_B_T_H_W_O\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.hidden_size","title":"hidden_size  <code>instance-attribute</code>","text":"<pre><code>hidden_size = hidden_size\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.use_adaln_lora","title":"use_adaln_lora  <code>instance-attribute</code>","text":"<pre><code>use_adaln_lora = use_adaln_lora\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.use_wan_fp32_strategy","title":"use_wan_fp32_strategy  <code>instance-attribute</code>","text":"<pre><code>use_wan_fp32_strategy = use_wan_fp32_strategy\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.layer_norm","title":"layer_norm  <code>instance-attribute</code>","text":"<pre><code>layer_norm = nn.LayerNorm(hidden_size, elementwise_affine=False, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.linear","title":"linear  <code>instance-attribute</code>","text":"<pre><code>linear = nn.Linear(hidden_size, spatial_patch_size * spatial_patch_size * temporal_patch_size * out_channels, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.adaln_modulation","title":"adaln_modulation  <code>instance-attribute</code>","text":"<pre><code>adaln_modulation = _adaln_modulation(hidden_size, 2, use_adaln_lora, adaln_lora_dim)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.__init__","title":"__init__","text":"<pre><code>__init__(hidden_size: int, spatial_patch_size: int, temporal_patch_size: int, out_channels: int, use_adaln_lora: bool = False, adaln_lora_dim: int = 256, use_wan_fp32_strategy: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def __init__(\n    self,\n    hidden_size: int,\n    spatial_patch_size: int,\n    temporal_patch_size: int,\n    out_channels: int,\n    use_adaln_lora: bool = False,\n    adaln_lora_dim: int = 256,\n    use_wan_fp32_strategy: bool = False,\n) -&gt; None:\n    super().__init__()\n    self.hidden_size = hidden_size\n    self.use_adaln_lora = use_adaln_lora\n    self.use_wan_fp32_strategy = use_wan_fp32_strategy\n    self.layer_norm = nn.LayerNorm(hidden_size, elementwise_affine=False, eps=1e-6)\n    self.linear = nn.Linear(\n        hidden_size,\n        spatial_patch_size * spatial_patch_size * temporal_patch_size * out_channels,\n        bias=False,\n    )\n    self.adaln_modulation = _adaln_modulation(hidden_size, 2, use_adaln_lora, adaln_lora_dim)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.FinalLayer.forward","title":"forward","text":"<pre><code>forward(x_B_T_H_W_D: Tensor, emb_B_T_D: Tensor, adaln_lora_B_T_3D: Tensor | None = None) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def forward(\n    self,\n    x_B_T_H_W_D: torch.Tensor,\n    emb_B_T_D: torch.Tensor,\n    adaln_lora_B_T_3D: torch.Tensor | None = None,\n) -&gt; torch.Tensor:\n    if self.use_wan_fp32_strategy:\n        assert emb_B_T_D.dtype == torch.float32\n    with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n        if self.use_adaln_lora:\n            assert adaln_lora_B_T_3D is not None\n            shift, scale = (\n                self.adaln_modulation(emb_B_T_D)\n                + adaln_lora_B_T_3D[:, :, : 2 * self.hidden_size]\n            ).chunk(2, dim=-1)\n        else:\n            shift, scale = self.adaln_modulation(emb_B_T_D).chunk(2, dim=-1)\n        shift = rearrange(shift, \"b t d -&gt; b t 1 1 d\")\n        scale = rearrange(scale, \"b t d -&gt; b t 1 1 d\")\n        x_B_T_H_W_D = _modulate(x_B_T_H_W_D, self.layer_norm, scale, shift)\n        x_B_T_H_W_O = self.linear(x_B_T_H_W_D)\n    return x_B_T_H_W_O\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT","title":"VideoDiT","text":"<p>               Bases: <code>Module</code></p> <p>adaLN-LoRA video diffusion transformer over patchified latents.</p> <p>The Cosmos-Predict2.5 backbone: patch embed with an appended padding-mask channel, 3D RoPE with NTK extrapolation, sinusoidal timesteps with a shared adaLN-LoRA stream, and a stack of <code>DiTBlock</code>. Subclasses add conditioning (actions, condition-mask channels) and causal per-frame execution.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>class VideoDiT(nn.Module):\n    r\"\"\"adaLN-LoRA video diffusion transformer over patchified latents.\n\n    The Cosmos-Predict2.5 backbone: patch embed with an appended padding-mask\n    channel, 3D RoPE with NTK extrapolation, sinusoidal timesteps with a shared\n    adaLN-LoRA stream, and a stack of `DiTBlock`. Subclasses add conditioning\n    (actions, condition-mask channels) and causal per-frame execution.\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        max_img_h: int,\n        max_img_w: int,\n        max_frames: int,\n        in_channels: int,\n        out_channels: int,\n        patch_spatial: int = 2,\n        patch_temporal: int = 1,\n        concat_padding_mask: bool = True,\n        model_channels: int = 2048,\n        num_blocks: int = 28,\n        num_heads: int = 16,\n        mlp_ratio: float = 4.0,\n        crossattn_emb_channels: int = 1024,\n        use_crossattn_projection: bool = False,\n        crossattn_proj_in_channels: int = 1024,\n        use_adaln_lora: bool = True,\n        adaln_lora_dim: int = 256,\n        rope_h_extrapolation_ratio: float = 1.0,\n        rope_w_extrapolation_ratio: float = 1.0,\n        rope_t_extrapolation_ratio: float = 1.0,\n        use_wan_fp32_strategy: bool = False,\n    ) -&gt; None:\n        super().__init__()\n        self.in_channels = in_channels\n        self.out_channels = out_channels\n        self.patch_spatial = patch_spatial\n        self.patch_temporal = patch_temporal\n        self.model_channels = model_channels\n        self.num_heads = num_heads\n        self.concat_padding_mask = concat_padding_mask\n        self.use_adaln_lora = use_adaln_lora\n        self.use_crossattn_projection = use_crossattn_projection\n        self.use_wan_fp32_strategy = use_wan_fp32_strategy\n\n        self.x_embedder = PatchEmbed(\n            spatial_patch_size=patch_spatial,\n            temporal_patch_size=patch_temporal,\n            in_channels=in_channels + 1 if concat_padding_mask else in_channels,\n            out_channels=model_channels,\n        )\n        self.pos_embedder = VideoRoPE3D(\n            head_dim=model_channels // num_heads,\n            len_h=max_img_h // patch_spatial,\n            len_w=max_img_w // patch_spatial,\n            len_t=max_frames // patch_temporal,\n            h_extrapolation_ratio=rope_h_extrapolation_ratio,\n            w_extrapolation_ratio=rope_w_extrapolation_ratio,\n            t_extrapolation_ratio=rope_t_extrapolation_ratio,\n        )\n        self.t_embedder = nn.Sequential(\n            SinusoidalTimesteps(model_channels),\n            TimestepEmbedding(model_channels, model_channels, use_adaln_lora=use_adaln_lora),\n        )\n        self.t_embedding_norm = RMSNorm(model_channels, eps=1e-6)\n\n        self.blocks = nn.ModuleList(\n            DiTBlock(\n                x_dim=model_channels,\n                context_dim=crossattn_emb_channels,\n                num_heads=num_heads,\n                mlp_ratio=mlp_ratio,\n                use_adaln_lora=use_adaln_lora,\n                adaln_lora_dim=adaln_lora_dim,\n                use_wan_fp32_strategy=use_wan_fp32_strategy,\n            )\n            for _ in range(num_blocks)\n        )\n        self.final_layer = FinalLayer(\n            hidden_size=model_channels,\n            spatial_patch_size=patch_spatial,\n            temporal_patch_size=patch_temporal,\n            out_channels=out_channels,\n            use_adaln_lora=use_adaln_lora,\n            adaln_lora_dim=adaln_lora_dim,\n            use_wan_fp32_strategy=use_wan_fp32_strategy,\n        )\n\n        if use_crossattn_projection:\n            self.crossattn_proj = nn.Sequential(\n                nn.Linear(crossattn_proj_in_channels, crossattn_emb_channels, bias=True),\n                nn.GELU(),\n            )\n\n    def prepare_embedded_sequence(\n        self,\n        x_B_C_T_H_W: torch.Tensor,\n        padding_mask: torch.Tensor | None = None,\n    ) -&gt; tuple[torch.Tensor, torch.Tensor]:\n        r\"\"\"Patchify with the padding-mask channel appended; return tokens and RoPE table.\"\"\"\n        if self.concat_padding_mask:\n            assert padding_mask is not None\n            if padding_mask.shape[-2:] != x_B_C_T_H_W.shape[-2:]:\n                padding_mask = F.interpolate(\n                    padding_mask, size=x_B_C_T_H_W.shape[-2:], mode=\"nearest\"\n                )\n            x_B_C_T_H_W = torch.cat(\n                [\n                    x_B_C_T_H_W,\n                    padding_mask.unsqueeze(1).repeat(1, 1, x_B_C_T_H_W.shape[2], 1, 1),\n                ],\n                dim=1,\n            )\n        x_B_T_H_W_D = self.x_embedder(x_B_C_T_H_W)\n        _, T, H, W, _ = x_B_T_H_W_D.shape\n        rope_emb_L_1_1_D = self.pos_embedder(T, H, W)\n        return x_B_T_H_W_D, rope_emb_L_1_1_D\n\n    def compute_time_embeddings(\n        self, timesteps_B_T: torch.Tensor\n    ) -&gt; tuple[torch.Tensor, torch.Tensor | None]:\n        r\"\"\"Timestep embedding and adaLN-LoRA stream, float32 under the Wan strategy.\"\"\"\n        with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n            if timesteps_B_T.ndim == 1:\n                timesteps_B_T = timesteps_B_T.unsqueeze(1)\n            t_embedding_B_T_D, adaln_lora_B_T_3D = self.t_embedder(timesteps_B_T)\n            t_embedding_B_T_D = self.t_embedding_norm(t_embedding_B_T_D)\n        return t_embedding_B_T_D, adaln_lora_B_T_3D\n\n    def project_context(self, crossattn_emb: torch.Tensor) -&gt; torch.Tensor:\n        if self.use_crossattn_projection:\n            return self.crossattn_proj(crossattn_emb)\n        return crossattn_emb\n\n    def unpatchify(self, x_B_T_H_W_M: torch.Tensor) -&gt; torch.Tensor:\n        return rearrange(\n            x_B_T_H_W_M,\n            \"B T H W (p1 p2 t C) -&gt; B C (T t) (H p1) (W p2)\",\n            p1=self.patch_spatial,\n            p2=self.patch_spatial,\n            t=self.patch_temporal,\n        )\n\n    def forward(\n        self,\n        x_B_C_T_H_W: torch.Tensor,\n        timesteps_B_T: torch.Tensor,\n        crossattn_emb: torch.Tensor,\n        padding_mask: torch.Tensor | None = None,\n    ) -&gt; torch.Tensor:\n        x_B_T_H_W_D, rope_emb_L_1_1_D = self.prepare_embedded_sequence(\n            x_B_C_T_H_W, padding_mask=padding_mask\n        )\n        crossattn_emb = self.project_context(crossattn_emb)\n        t_embedding_B_T_D, adaln_lora_B_T_3D = self.compute_time_embeddings(timesteps_B_T)\n\n        for block in self.blocks:\n            x_B_T_H_W_D = block(\n                x_B_T_H_W_D,\n                t_embedding_B_T_D,\n                crossattn_emb,\n                rope_emb_L_1_1_D=rope_emb_L_1_1_D,\n                adaln_lora_B_T_3D=adaln_lora_B_T_3D,\n            )\n\n        x_B_T_H_W_O = self.final_layer(\n            x_B_T_H_W_D, t_embedding_B_T_D, adaln_lora_B_T_3D=adaln_lora_B_T_3D\n        )\n        return self.unpatchify(x_B_T_H_W_O)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.in_channels","title":"in_channels  <code>instance-attribute</code>","text":"<pre><code>in_channels = in_channels\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.out_channels","title":"out_channels  <code>instance-attribute</code>","text":"<pre><code>out_channels = out_channels\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.patch_spatial","title":"patch_spatial  <code>instance-attribute</code>","text":"<pre><code>patch_spatial = patch_spatial\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.patch_temporal","title":"patch_temporal  <code>instance-attribute</code>","text":"<pre><code>patch_temporal = patch_temporal\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.model_channels","title":"model_channels  <code>instance-attribute</code>","text":"<pre><code>model_channels = model_channels\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.num_heads","title":"num_heads  <code>instance-attribute</code>","text":"<pre><code>num_heads = num_heads\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.concat_padding_mask","title":"concat_padding_mask  <code>instance-attribute</code>","text":"<pre><code>concat_padding_mask = concat_padding_mask\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.use_adaln_lora","title":"use_adaln_lora  <code>instance-attribute</code>","text":"<pre><code>use_adaln_lora = use_adaln_lora\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.use_crossattn_projection","title":"use_crossattn_projection  <code>instance-attribute</code>","text":"<pre><code>use_crossattn_projection = use_crossattn_projection\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.use_wan_fp32_strategy","title":"use_wan_fp32_strategy  <code>instance-attribute</code>","text":"<pre><code>use_wan_fp32_strategy = use_wan_fp32_strategy\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.x_embedder","title":"x_embedder  <code>instance-attribute</code>","text":"<pre><code>x_embedder = PatchEmbed(spatial_patch_size=patch_spatial, temporal_patch_size=patch_temporal, in_channels=in_channels + 1 if concat_padding_mask else in_channels, out_channels=model_channels)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.pos_embedder","title":"pos_embedder  <code>instance-attribute</code>","text":"<pre><code>pos_embedder = VideoRoPE3D(head_dim=model_channels // num_heads, len_h=max_img_h // patch_spatial, len_w=max_img_w // patch_spatial, len_t=max_frames // patch_temporal, h_extrapolation_ratio=rope_h_extrapolation_ratio, w_extrapolation_ratio=rope_w_extrapolation_ratio, t_extrapolation_ratio=rope_t_extrapolation_ratio)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.t_embedder","title":"t_embedder  <code>instance-attribute</code>","text":"<pre><code>t_embedder = nn.Sequential(SinusoidalTimesteps(model_channels), TimestepEmbedding(model_channels, model_channels, use_adaln_lora=use_adaln_lora))\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.t_embedding_norm","title":"t_embedding_norm  <code>instance-attribute</code>","text":"<pre><code>t_embedding_norm = RMSNorm(model_channels, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.blocks","title":"blocks  <code>instance-attribute</code>","text":"<pre><code>blocks = nn.ModuleList((DiTBlock(x_dim=model_channels, context_dim=crossattn_emb_channels, num_heads=num_heads, mlp_ratio=mlp_ratio, use_adaln_lora=use_adaln_lora, adaln_lora_dim=adaln_lora_dim, use_wan_fp32_strategy=use_wan_fp32_strategy)) for _ in (range(num_blocks)))\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.final_layer","title":"final_layer  <code>instance-attribute</code>","text":"<pre><code>final_layer = FinalLayer(hidden_size=model_channels, spatial_patch_size=patch_spatial, temporal_patch_size=patch_temporal, out_channels=out_channels, use_adaln_lora=use_adaln_lora, adaln_lora_dim=adaln_lora_dim, use_wan_fp32_strategy=use_wan_fp32_strategy)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.crossattn_proj","title":"crossattn_proj  <code>instance-attribute</code>","text":"<pre><code>crossattn_proj = nn.Sequential(nn.Linear(crossattn_proj_in_channels, crossattn_emb_channels, bias=True), nn.GELU())\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.__init__","title":"__init__","text":"<pre><code>__init__(*, max_img_h: int, max_img_w: int, max_frames: int, in_channels: int, out_channels: int, patch_spatial: int = 2, patch_temporal: int = 1, concat_padding_mask: bool = True, model_channels: int = 2048, num_blocks: int = 28, num_heads: int = 16, mlp_ratio: float = 4.0, crossattn_emb_channels: int = 1024, use_crossattn_projection: bool = False, crossattn_proj_in_channels: int = 1024, use_adaln_lora: bool = True, adaln_lora_dim: int = 256, rope_h_extrapolation_ratio: float = 1.0, rope_w_extrapolation_ratio: float = 1.0, rope_t_extrapolation_ratio: float = 1.0, use_wan_fp32_strategy: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def __init__(\n    self,\n    *,\n    max_img_h: int,\n    max_img_w: int,\n    max_frames: int,\n    in_channels: int,\n    out_channels: int,\n    patch_spatial: int = 2,\n    patch_temporal: int = 1,\n    concat_padding_mask: bool = True,\n    model_channels: int = 2048,\n    num_blocks: int = 28,\n    num_heads: int = 16,\n    mlp_ratio: float = 4.0,\n    crossattn_emb_channels: int = 1024,\n    use_crossattn_projection: bool = False,\n    crossattn_proj_in_channels: int = 1024,\n    use_adaln_lora: bool = True,\n    adaln_lora_dim: int = 256,\n    rope_h_extrapolation_ratio: float = 1.0,\n    rope_w_extrapolation_ratio: float = 1.0,\n    rope_t_extrapolation_ratio: float = 1.0,\n    use_wan_fp32_strategy: bool = False,\n) -&gt; None:\n    super().__init__()\n    self.in_channels = in_channels\n    self.out_channels = out_channels\n    self.patch_spatial = patch_spatial\n    self.patch_temporal = patch_temporal\n    self.model_channels = model_channels\n    self.num_heads = num_heads\n    self.concat_padding_mask = concat_padding_mask\n    self.use_adaln_lora = use_adaln_lora\n    self.use_crossattn_projection = use_crossattn_projection\n    self.use_wan_fp32_strategy = use_wan_fp32_strategy\n\n    self.x_embedder = PatchEmbed(\n        spatial_patch_size=patch_spatial,\n        temporal_patch_size=patch_temporal,\n        in_channels=in_channels + 1 if concat_padding_mask else in_channels,\n        out_channels=model_channels,\n    )\n    self.pos_embedder = VideoRoPE3D(\n        head_dim=model_channels // num_heads,\n        len_h=max_img_h // patch_spatial,\n        len_w=max_img_w // patch_spatial,\n        len_t=max_frames // patch_temporal,\n        h_extrapolation_ratio=rope_h_extrapolation_ratio,\n        w_extrapolation_ratio=rope_w_extrapolation_ratio,\n        t_extrapolation_ratio=rope_t_extrapolation_ratio,\n    )\n    self.t_embedder = nn.Sequential(\n        SinusoidalTimesteps(model_channels),\n        TimestepEmbedding(model_channels, model_channels, use_adaln_lora=use_adaln_lora),\n    )\n    self.t_embedding_norm = RMSNorm(model_channels, eps=1e-6)\n\n    self.blocks = nn.ModuleList(\n        DiTBlock(\n            x_dim=model_channels,\n            context_dim=crossattn_emb_channels,\n            num_heads=num_heads,\n            mlp_ratio=mlp_ratio,\n            use_adaln_lora=use_adaln_lora,\n            adaln_lora_dim=adaln_lora_dim,\n            use_wan_fp32_strategy=use_wan_fp32_strategy,\n        )\n        for _ in range(num_blocks)\n    )\n    self.final_layer = FinalLayer(\n        hidden_size=model_channels,\n        spatial_patch_size=patch_spatial,\n        temporal_patch_size=patch_temporal,\n        out_channels=out_channels,\n        use_adaln_lora=use_adaln_lora,\n        adaln_lora_dim=adaln_lora_dim,\n        use_wan_fp32_strategy=use_wan_fp32_strategy,\n    )\n\n    if use_crossattn_projection:\n        self.crossattn_proj = nn.Sequential(\n            nn.Linear(crossattn_proj_in_channels, crossattn_emb_channels, bias=True),\n            nn.GELU(),\n        )\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.prepare_embedded_sequence","title":"prepare_embedded_sequence","text":"<pre><code>prepare_embedded_sequence(x_B_C_T_H_W: Tensor, padding_mask: Tensor | None = None) -&gt; tuple[Tensor, Tensor]\n</code></pre> <p>Patchify with the padding-mask channel appended; return tokens and RoPE table.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def prepare_embedded_sequence(\n    self,\n    x_B_C_T_H_W: torch.Tensor,\n    padding_mask: torch.Tensor | None = None,\n) -&gt; tuple[torch.Tensor, torch.Tensor]:\n    r\"\"\"Patchify with the padding-mask channel appended; return tokens and RoPE table.\"\"\"\n    if self.concat_padding_mask:\n        assert padding_mask is not None\n        if padding_mask.shape[-2:] != x_B_C_T_H_W.shape[-2:]:\n            padding_mask = F.interpolate(\n                padding_mask, size=x_B_C_T_H_W.shape[-2:], mode=\"nearest\"\n            )\n        x_B_C_T_H_W = torch.cat(\n            [\n                x_B_C_T_H_W,\n                padding_mask.unsqueeze(1).repeat(1, 1, x_B_C_T_H_W.shape[2], 1, 1),\n            ],\n            dim=1,\n        )\n    x_B_T_H_W_D = self.x_embedder(x_B_C_T_H_W)\n    _, T, H, W, _ = x_B_T_H_W_D.shape\n    rope_emb_L_1_1_D = self.pos_embedder(T, H, W)\n    return x_B_T_H_W_D, rope_emb_L_1_1_D\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.compute_time_embeddings","title":"compute_time_embeddings","text":"<pre><code>compute_time_embeddings(timesteps_B_T: Tensor) -&gt; tuple[Tensor, Tensor | None]\n</code></pre> <p>Timestep embedding and adaLN-LoRA stream, float32 under the Wan strategy.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def compute_time_embeddings(\n    self, timesteps_B_T: torch.Tensor\n) -&gt; tuple[torch.Tensor, torch.Tensor | None]:\n    r\"\"\"Timestep embedding and adaLN-LoRA stream, float32 under the Wan strategy.\"\"\"\n    with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n        if timesteps_B_T.ndim == 1:\n            timesteps_B_T = timesteps_B_T.unsqueeze(1)\n        t_embedding_B_T_D, adaln_lora_B_T_3D = self.t_embedder(timesteps_B_T)\n        t_embedding_B_T_D = self.t_embedding_norm(t_embedding_B_T_D)\n    return t_embedding_B_T_D, adaln_lora_B_T_3D\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.project_context","title":"project_context","text":"<pre><code>project_context(crossattn_emb: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def project_context(self, crossattn_emb: torch.Tensor) -&gt; torch.Tensor:\n    if self.use_crossattn_projection:\n        return self.crossattn_proj(crossattn_emb)\n    return crossattn_emb\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.unpatchify","title":"unpatchify","text":"<pre><code>unpatchify(x_B_T_H_W_M: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def unpatchify(self, x_B_T_H_W_M: torch.Tensor) -&gt; torch.Tensor:\n    return rearrange(\n        x_B_T_H_W_M,\n        \"B T H W (p1 p2 t C) -&gt; B C (T t) (H p1) (W p2)\",\n        p1=self.patch_spatial,\n        p2=self.patch_spatial,\n        t=self.patch_temporal,\n    )\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit.VideoDiT.forward","title":"forward","text":"<pre><code>forward(x_B_C_T_H_W: Tensor, timesteps_B_T: Tensor, crossattn_emb: Tensor, padding_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def forward(\n    self,\n    x_B_C_T_H_W: torch.Tensor,\n    timesteps_B_T: torch.Tensor,\n    crossattn_emb: torch.Tensor,\n    padding_mask: torch.Tensor | None = None,\n) -&gt; torch.Tensor:\n    x_B_T_H_W_D, rope_emb_L_1_1_D = self.prepare_embedded_sequence(\n        x_B_C_T_H_W, padding_mask=padding_mask\n    )\n    crossattn_emb = self.project_context(crossattn_emb)\n    t_embedding_B_T_D, adaln_lora_B_T_3D = self.compute_time_embeddings(timesteps_B_T)\n\n    for block in self.blocks:\n        x_B_T_H_W_D = block(\n            x_B_T_H_W_D,\n            t_embedding_B_T_D,\n            crossattn_emb,\n            rope_emb_L_1_1_D=rope_emb_L_1_1_D,\n            adaln_lora_B_T_3D=adaln_lora_B_T_3D,\n        )\n\n    x_B_T_H_W_O = self.final_layer(\n        x_B_T_H_W_D, t_embedding_B_T_D, adaln_lora_B_T_3D=adaln_lora_B_T_3D\n    )\n    return self.unpatchify(x_B_T_H_W_O)\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit._adaln_modulation","title":"_adaln_modulation","text":"<pre><code>_adaln_modulation(x_dim: int, n_chunks: int, use_adaln_lora: bool, lora_dim: int) -&gt; Module\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def _adaln_modulation(x_dim: int, n_chunks: int, use_adaln_lora: bool, lora_dim: int) -&gt; nn.Module:\n    if use_adaln_lora:\n        return nn.Sequential(\n            nn.SiLU(),\n            nn.Linear(x_dim, lora_dim, bias=False),\n            nn.Linear(lora_dim, n_chunks * x_dim, bias=False),\n        )\n    return nn.Sequential(nn.SiLU(), nn.Linear(x_dim, n_chunks * x_dim, bias=False))\n</code></pre>"},{"location":"api/worldkernels/models/components/dit/#worldkernels.models.components.dit._modulate","title":"_modulate","text":"<pre><code>_modulate(x: Tensor, norm: Module, scale: Tensor, shift: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def _modulate(\n    x: torch.Tensor, norm: nn.Module, scale: torch.Tensor, shift: torch.Tensor\n) -&gt; torch.Tensor:\n    return norm(x) * (1 + scale) + shift\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/","title":"embeddings","text":""},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings","title":"worldkernels.models.components.embeddings","text":"<p>Timestep, patch, and rotary position embeddings for native video DiTs.</p>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['SinusoidalTimesteps', 'TimestepEmbedding', 'PatchEmbed', 'VideoRoPE3D', 'apply_rotary_emb']\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.SinusoidalTimesteps","title":"SinusoidalTimesteps","text":"<p>               Bases: <code>Module</code></p> <p>Sinusoidal timestep features, <code>[cos | sin]</code> ordered, computed in float32.</p> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>class SinusoidalTimesteps(nn.Module):\n    r\"\"\"Sinusoidal timestep features, ``[cos | sin]`` ordered, computed in float32.\"\"\"\n\n    def __init__(self, num_channels: int) -&gt; None:\n        super().__init__()\n        self.num_channels = num_channels\n\n    def forward(self, timesteps_B_T: torch.Tensor) -&gt; torch.Tensor:\n        assert timesteps_B_T.ndim == 2, f\"expected 2D input, got {timesteps_B_T.ndim}\"\n        in_dtype = timesteps_B_T.dtype\n        timesteps = timesteps_B_T.flatten().float()\n        half_dim = self.num_channels // 2\n        exponent = -math.log(10000) * torch.arange(\n            half_dim, dtype=torch.float32, device=timesteps.device\n        )\n        emb = timesteps[:, None] * torch.exp(exponent / half_dim)[None, :]\n        emb = torch.cat([torch.cos(emb), torch.sin(emb)], dim=-1)\n        return rearrange(\n            emb.to(dtype=in_dtype),\n            \"(b t) d -&gt; b t d\",\n            b=timesteps_B_T.shape[0],\n            t=timesteps_B_T.shape[1],\n        )\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.SinusoidalTimesteps.num_channels","title":"num_channels  <code>instance-attribute</code>","text":"<pre><code>num_channels = num_channels\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.SinusoidalTimesteps.__init__","title":"__init__","text":"<pre><code>__init__(num_channels: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def __init__(self, num_channels: int) -&gt; None:\n    super().__init__()\n    self.num_channels = num_channels\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.SinusoidalTimesteps.forward","title":"forward","text":"<pre><code>forward(timesteps_B_T: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def forward(self, timesteps_B_T: torch.Tensor) -&gt; torch.Tensor:\n    assert timesteps_B_T.ndim == 2, f\"expected 2D input, got {timesteps_B_T.ndim}\"\n    in_dtype = timesteps_B_T.dtype\n    timesteps = timesteps_B_T.flatten().float()\n    half_dim = self.num_channels // 2\n    exponent = -math.log(10000) * torch.arange(\n        half_dim, dtype=torch.float32, device=timesteps.device\n    )\n    emb = timesteps[:, None] * torch.exp(exponent / half_dim)[None, :]\n    emb = torch.cat([torch.cos(emb), torch.sin(emb)], dim=-1)\n    return rearrange(\n        emb.to(dtype=in_dtype),\n        \"(b t) d -&gt; b t d\",\n        b=timesteps_B_T.shape[0],\n        t=timesteps_B_T.shape[1],\n    )\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.TimestepEmbedding","title":"TimestepEmbedding","text":"<p>               Bases: <code>Module</code></p> <p>Two-layer timestep MLP; in adaLN-LoRA mode the MLP output is the LoRA stream.</p> <p>Returns:</p> Type Description <p><code>(emb_B_T_D, adaln_lora_B_T_3D)</code>. With adaLN-LoRA the embedding</p> <p>passed to the blocks is the raw sinusoidal input and the MLP produces</p> <p>the shared 3D LoRA modulation; without it the MLP output is the</p> <p>embedding and the LoRA stream is <code>None</code>.</p> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>class TimestepEmbedding(nn.Module):\n    r\"\"\"Two-layer timestep MLP; in adaLN-LoRA mode the MLP output is the LoRA stream.\n\n    Returns:\n        ``(emb_B_T_D, adaln_lora_B_T_3D)``. With adaLN-LoRA the embedding\n        passed to the blocks is the raw sinusoidal input and the MLP produces\n        the shared 3D LoRA modulation; without it the MLP output is the\n        embedding and the LoRA stream is ``None``.\n    \"\"\"\n\n    def __init__(self, in_features: int, out_features: int, use_adaln_lora: bool = False) -&gt; None:\n        super().__init__()\n        self.linear_1 = nn.Linear(in_features, out_features, bias=not use_adaln_lora)\n        self.activation = nn.SiLU()\n        self.use_adaln_lora = use_adaln_lora\n        out_dim = 3 * out_features if use_adaln_lora else out_features\n        self.linear_2 = nn.Linear(out_features, out_dim, bias=False)\n\n    def forward(self, sample: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor | None]:\n        emb = self.linear_2(self.activation(self.linear_1(sample)))\n        if self.use_adaln_lora:\n            return sample, emb\n        return emb, None\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.TimestepEmbedding.linear_1","title":"linear_1  <code>instance-attribute</code>","text":"<pre><code>linear_1 = nn.Linear(in_features, out_features, bias=not use_adaln_lora)\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.TimestepEmbedding.activation","title":"activation  <code>instance-attribute</code>","text":"<pre><code>activation = nn.SiLU()\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.TimestepEmbedding.use_adaln_lora","title":"use_adaln_lora  <code>instance-attribute</code>","text":"<pre><code>use_adaln_lora = use_adaln_lora\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.TimestepEmbedding.linear_2","title":"linear_2  <code>instance-attribute</code>","text":"<pre><code>linear_2 = nn.Linear(out_features, out_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.TimestepEmbedding.__init__","title":"__init__","text":"<pre><code>__init__(in_features: int, out_features: int, use_adaln_lora: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def __init__(self, in_features: int, out_features: int, use_adaln_lora: bool = False) -&gt; None:\n    super().__init__()\n    self.linear_1 = nn.Linear(in_features, out_features, bias=not use_adaln_lora)\n    self.activation = nn.SiLU()\n    self.use_adaln_lora = use_adaln_lora\n    out_dim = 3 * out_features if use_adaln_lora else out_features\n    self.linear_2 = nn.Linear(out_features, out_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.TimestepEmbedding.forward","title":"forward","text":"<pre><code>forward(sample: Tensor) -&gt; tuple[Tensor, Tensor | None]\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def forward(self, sample: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor | None]:\n    emb = self.linear_2(self.activation(self.linear_1(sample)))\n    if self.use_adaln_lora:\n        return sample, emb\n    return emb, None\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.PatchEmbed","title":"PatchEmbed","text":"<p>               Bases: <code>Module</code></p> <p>Space-time patchify followed by a linear projection to model width.</p> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>class PatchEmbed(nn.Module):\n    r\"\"\"Space-time patchify followed by a linear projection to model width.\"\"\"\n\n    def __init__(\n        self,\n        spatial_patch_size: int,\n        temporal_patch_size: int,\n        in_channels: int,\n        out_channels: int,\n    ) -&gt; None:\n        super().__init__()\n        self.spatial_patch_size = spatial_patch_size\n        self.temporal_patch_size = temporal_patch_size\n        self.proj = nn.Sequential(\n            Rearrange(\n                \"b c (t r) (h m) (w n) -&gt; b t h w (c r m n)\",\n                r=temporal_patch_size,\n                m=spatial_patch_size,\n                n=spatial_patch_size,\n            ),\n            nn.Linear(\n                in_channels * spatial_patch_size * spatial_patch_size * temporal_patch_size,\n                out_channels,\n                bias=False,\n            ),\n        )\n\n    def forward(self, x_B_C_T_H_W: torch.Tensor) -&gt; torch.Tensor:\n        assert x_B_C_T_H_W.dim() == 5\n        _, _, T, H, W = x_B_C_T_H_W.shape\n        assert (\n            H % self.spatial_patch_size == 0\n            and W % self.spatial_patch_size == 0\n            and T % self.temporal_patch_size == 0\n        ), f\"input {(T, H, W)} not divisible by patch size\"\n        return self.proj(x_B_C_T_H_W)\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.PatchEmbed.spatial_patch_size","title":"spatial_patch_size  <code>instance-attribute</code>","text":"<pre><code>spatial_patch_size = spatial_patch_size\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.PatchEmbed.temporal_patch_size","title":"temporal_patch_size  <code>instance-attribute</code>","text":"<pre><code>temporal_patch_size = temporal_patch_size\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.PatchEmbed.proj","title":"proj  <code>instance-attribute</code>","text":"<pre><code>proj = nn.Sequential(Rearrange('b c (t r) (h m) (w n) -&gt; b t h w (c r m n)', r=temporal_patch_size, m=spatial_patch_size, n=spatial_patch_size), nn.Linear(in_channels * spatial_patch_size * spatial_patch_size * temporal_patch_size, out_channels, bias=False))\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.PatchEmbed.__init__","title":"__init__","text":"<pre><code>__init__(spatial_patch_size: int, temporal_patch_size: int, in_channels: int, out_channels: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def __init__(\n    self,\n    spatial_patch_size: int,\n    temporal_patch_size: int,\n    in_channels: int,\n    out_channels: int,\n) -&gt; None:\n    super().__init__()\n    self.spatial_patch_size = spatial_patch_size\n    self.temporal_patch_size = temporal_patch_size\n    self.proj = nn.Sequential(\n        Rearrange(\n            \"b c (t r) (h m) (w n) -&gt; b t h w (c r m n)\",\n            r=temporal_patch_size,\n            m=spatial_patch_size,\n            n=spatial_patch_size,\n        ),\n        nn.Linear(\n            in_channels * spatial_patch_size * spatial_patch_size * temporal_patch_size,\n            out_channels,\n            bias=False,\n        ),\n    )\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.PatchEmbed.forward","title":"forward","text":"<pre><code>forward(x_B_C_T_H_W: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def forward(self, x_B_C_T_H_W: torch.Tensor) -&gt; torch.Tensor:\n    assert x_B_C_T_H_W.dim() == 5\n    _, _, T, H, W = x_B_C_T_H_W.shape\n    assert (\n        H % self.spatial_patch_size == 0\n        and W % self.spatial_patch_size == 0\n        and T % self.temporal_patch_size == 0\n    ), f\"input {(T, H, W)} not divisible by patch size\"\n    return self.proj(x_B_C_T_H_W)\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D","title":"VideoRoPE3D","text":"<p>               Bases: <code>Module</code></p> <p>3D rotary position embedding over (t, h, w) with NTK-aware extrapolation.</p> <p>The head dimension is split <code>dim_h = dim_w = 2 * (d // 6)</code> and <code>dim_t = d - 2 dim_h</code>; each axis gets its own frequency ladder with base \\(\\theta = 10^4 \\cdot r^{d_a/(d_a-2)}\\) for extrapolation ratio \\(r\\). Emits a duplicated-frequency table of shape <code>(T*H*W, 1, 1, d)</code> in float32 for the half-rotation form of <code>apply_rotary_emb</code>.</p> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>class VideoRoPE3D(nn.Module):\n    r\"\"\"3D rotary position embedding over (t, h, w) with NTK-aware extrapolation.\n\n    The head dimension is split ``dim_h = dim_w = 2 * (d // 6)`` and\n    ``dim_t = d - 2 dim_h``; each axis gets its own frequency ladder with base\n    \\(\\theta = 10^4 \\cdot r^{d_a/(d_a-2)}\\) for extrapolation ratio \\(r\\).\n    Emits a duplicated-frequency table of shape ``(T*H*W, 1, 1, d)`` in float32\n    for the half-rotation form of `apply_rotary_emb`.\n    \"\"\"\n\n    seq: torch.Tensor\n    dim_spatial_range: torch.Tensor\n    dim_temporal_range: torch.Tensor\n\n    def __init__(\n        self,\n        *,\n        head_dim: int,\n        len_h: int,\n        len_w: int,\n        len_t: int,\n        h_extrapolation_ratio: float = 1.0,\n        w_extrapolation_ratio: float = 1.0,\n        t_extrapolation_ratio: float = 1.0,\n    ) -&gt; None:\n        super().__init__()\n        self.max_h = len_h\n        self.max_w = len_w\n        self.max_t = len_t\n\n        dim_h = head_dim // 6 * 2\n        dim_w = dim_h\n        dim_t = head_dim - 2 * dim_h\n        assert head_dim == dim_h + dim_w + dim_t, (\n            f\"bad head_dim: {head_dim} != {dim_h} + {dim_w} + {dim_t}\"\n        )\n\n        self.register_buffer(\n            \"seq\", torch.arange(max(len_h, len_w, len_t), dtype=torch.float), persistent=True\n        )\n        self.register_buffer(\n            \"dim_spatial_range\",\n            torch.arange(0, dim_h, 2)[: (dim_h // 2)].float() / dim_h,\n            persistent=True,\n        )\n        self.register_buffer(\n            \"dim_temporal_range\",\n            torch.arange(0, dim_t, 2)[: (dim_t // 2)].float() / dim_t,\n            persistent=True,\n        )\n\n        self.h_ntk_factor = h_extrapolation_ratio ** (dim_h / (dim_h - 2))\n        self.w_ntk_factor = w_extrapolation_ratio ** (dim_w / (dim_w - 2))\n        self.t_ntk_factor = t_extrapolation_ratio ** (dim_t / (dim_t - 2))\n\n    def _freqs(self) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]:\n        h_theta = 10000.0 * self.h_ntk_factor\n        w_theta = 10000.0 * self.w_ntk_factor\n        t_theta = 10000.0 * self.t_ntk_factor\n        h_freqs = 1.0 / (h_theta ** self.dim_spatial_range.float())\n        w_freqs = 1.0 / (w_theta ** self.dim_spatial_range.float())\n        t_freqs = 1.0 / (t_theta ** self.dim_temporal_range.float())\n        return t_freqs, h_freqs, w_freqs\n\n    def forward(self, T: int, H: int, W: int) -&gt; torch.Tensor:\n        r\"\"\"Full-grid table for a ``(T, H, W)`` token volume, shape ``(T*H*W, 1, 1, d)``.\"\"\"\n        assert H &lt;= self.max_h and W &lt;= self.max_w, (\n            f\"({H}, {W}) exceeds maximum ({self.max_h}, {self.max_w})\"\n        )\n        t_freqs, h_freqs, w_freqs = self._freqs()\n        half_emb_t = torch.outer(self.seq[:T], t_freqs)\n        half_emb_h = torch.outer(self.seq[:H], h_freqs)\n        half_emb_w = torch.outer(self.seq[:W], w_freqs)\n        em_T_H_W_D = torch.cat(\n            [\n                repeat(half_emb_t, \"t d -&gt; t h w d\", h=H, w=W),\n                repeat(half_emb_h, \"h d -&gt; t h w d\", t=T, w=W),\n                repeat(half_emb_w, \"w d -&gt; t h w d\", t=T, h=H),\n            ]\n            * 2,\n            dim=-1,\n        )\n        return rearrange(em_T_H_W_D, \"t h w d -&gt; (t h w) 1 1 d\").float()\n\n    def forward_frame(self, frame_idx: int, H: int, W: int) -&gt; torch.Tensor:\n        r\"\"\"Table for the single latent frame at absolute index ``frame_idx``.\"\"\"\n        assert frame_idx &lt; self.max_t, f\"frame_idx {frame_idx} exceeds maximum {self.max_t}\"\n        t_freqs, h_freqs, w_freqs = self._freqs()\n        half_emb_t = torch.outer(self.seq[frame_idx : frame_idx + 1], t_freqs)\n        half_emb_h = torch.outer(self.seq[:H], h_freqs)\n        half_emb_w = torch.outer(self.seq[:W], w_freqs)\n        em_T_H_W_D = torch.cat(\n            [\n                repeat(half_emb_t, \"t d -&gt; t h w d\", h=H, w=W),\n                repeat(half_emb_h, \"h d -&gt; t h w d\", t=1, w=W),\n                repeat(half_emb_w, \"w d -&gt; t h w d\", t=1, h=H),\n            ]\n            * 2,\n            dim=-1,\n        )\n        return rearrange(em_T_H_W_D, \"t h w d -&gt; (t h w) 1 1 d\").float()\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.seq","title":"seq  <code>instance-attribute</code>","text":"<pre><code>seq: Tensor\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.dim_spatial_range","title":"dim_spatial_range  <code>instance-attribute</code>","text":"<pre><code>dim_spatial_range: Tensor\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.dim_temporal_range","title":"dim_temporal_range  <code>instance-attribute</code>","text":"<pre><code>dim_temporal_range: Tensor\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.max_h","title":"max_h  <code>instance-attribute</code>","text":"<pre><code>max_h = len_h\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.max_w","title":"max_w  <code>instance-attribute</code>","text":"<pre><code>max_w = len_w\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.max_t","title":"max_t  <code>instance-attribute</code>","text":"<pre><code>max_t = len_t\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.h_ntk_factor","title":"h_ntk_factor  <code>instance-attribute</code>","text":"<pre><code>h_ntk_factor = h_extrapolation_ratio ** (dim_h / (dim_h - 2))\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.w_ntk_factor","title":"w_ntk_factor  <code>instance-attribute</code>","text":"<pre><code>w_ntk_factor = w_extrapolation_ratio ** (dim_w / (dim_w - 2))\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.t_ntk_factor","title":"t_ntk_factor  <code>instance-attribute</code>","text":"<pre><code>t_ntk_factor = t_extrapolation_ratio ** (dim_t / (dim_t - 2))\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.__init__","title":"__init__","text":"<pre><code>__init__(*, head_dim: int, len_h: int, len_w: int, len_t: int, h_extrapolation_ratio: float = 1.0, w_extrapolation_ratio: float = 1.0, t_extrapolation_ratio: float = 1.0) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def __init__(\n    self,\n    *,\n    head_dim: int,\n    len_h: int,\n    len_w: int,\n    len_t: int,\n    h_extrapolation_ratio: float = 1.0,\n    w_extrapolation_ratio: float = 1.0,\n    t_extrapolation_ratio: float = 1.0,\n) -&gt; None:\n    super().__init__()\n    self.max_h = len_h\n    self.max_w = len_w\n    self.max_t = len_t\n\n    dim_h = head_dim // 6 * 2\n    dim_w = dim_h\n    dim_t = head_dim - 2 * dim_h\n    assert head_dim == dim_h + dim_w + dim_t, (\n        f\"bad head_dim: {head_dim} != {dim_h} + {dim_w} + {dim_t}\"\n    )\n\n    self.register_buffer(\n        \"seq\", torch.arange(max(len_h, len_w, len_t), dtype=torch.float), persistent=True\n    )\n    self.register_buffer(\n        \"dim_spatial_range\",\n        torch.arange(0, dim_h, 2)[: (dim_h // 2)].float() / dim_h,\n        persistent=True,\n    )\n    self.register_buffer(\n        \"dim_temporal_range\",\n        torch.arange(0, dim_t, 2)[: (dim_t // 2)].float() / dim_t,\n        persistent=True,\n    )\n\n    self.h_ntk_factor = h_extrapolation_ratio ** (dim_h / (dim_h - 2))\n    self.w_ntk_factor = w_extrapolation_ratio ** (dim_w / (dim_w - 2))\n    self.t_ntk_factor = t_extrapolation_ratio ** (dim_t / (dim_t - 2))\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D._freqs","title":"_freqs","text":"<pre><code>_freqs() -&gt; tuple[Tensor, Tensor, Tensor]\n</code></pre> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def _freqs(self) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]:\n    h_theta = 10000.0 * self.h_ntk_factor\n    w_theta = 10000.0 * self.w_ntk_factor\n    t_theta = 10000.0 * self.t_ntk_factor\n    h_freqs = 1.0 / (h_theta ** self.dim_spatial_range.float())\n    w_freqs = 1.0 / (w_theta ** self.dim_spatial_range.float())\n    t_freqs = 1.0 / (t_theta ** self.dim_temporal_range.float())\n    return t_freqs, h_freqs, w_freqs\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.forward","title":"forward","text":"<pre><code>forward(T: int, H: int, W: int) -&gt; Tensor\n</code></pre> <p>Full-grid table for a <code>(T, H, W)</code> token volume, shape <code>(T*H*W, 1, 1, d)</code>.</p> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def forward(self, T: int, H: int, W: int) -&gt; torch.Tensor:\n    r\"\"\"Full-grid table for a ``(T, H, W)`` token volume, shape ``(T*H*W, 1, 1, d)``.\"\"\"\n    assert H &lt;= self.max_h and W &lt;= self.max_w, (\n        f\"({H}, {W}) exceeds maximum ({self.max_h}, {self.max_w})\"\n    )\n    t_freqs, h_freqs, w_freqs = self._freqs()\n    half_emb_t = torch.outer(self.seq[:T], t_freqs)\n    half_emb_h = torch.outer(self.seq[:H], h_freqs)\n    half_emb_w = torch.outer(self.seq[:W], w_freqs)\n    em_T_H_W_D = torch.cat(\n        [\n            repeat(half_emb_t, \"t d -&gt; t h w d\", h=H, w=W),\n            repeat(half_emb_h, \"h d -&gt; t h w d\", t=T, w=W),\n            repeat(half_emb_w, \"w d -&gt; t h w d\", t=T, h=H),\n        ]\n        * 2,\n        dim=-1,\n    )\n    return rearrange(em_T_H_W_D, \"t h w d -&gt; (t h w) 1 1 d\").float()\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.VideoRoPE3D.forward_frame","title":"forward_frame","text":"<pre><code>forward_frame(frame_idx: int, H: int, W: int) -&gt; Tensor\n</code></pre> <p>Table for the single latent frame at absolute index <code>frame_idx</code>.</p> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def forward_frame(self, frame_idx: int, H: int, W: int) -&gt; torch.Tensor:\n    r\"\"\"Table for the single latent frame at absolute index ``frame_idx``.\"\"\"\n    assert frame_idx &lt; self.max_t, f\"frame_idx {frame_idx} exceeds maximum {self.max_t}\"\n    t_freqs, h_freqs, w_freqs = self._freqs()\n    half_emb_t = torch.outer(self.seq[frame_idx : frame_idx + 1], t_freqs)\n    half_emb_h = torch.outer(self.seq[:H], h_freqs)\n    half_emb_w = torch.outer(self.seq[:W], w_freqs)\n    em_T_H_W_D = torch.cat(\n        [\n            repeat(half_emb_t, \"t d -&gt; t h w d\", h=H, w=W),\n            repeat(half_emb_h, \"h d -&gt; t h w d\", t=1, w=W),\n            repeat(half_emb_w, \"w d -&gt; t h w d\", t=1, h=H),\n        ]\n        * 2,\n        dim=-1,\n    )\n    return rearrange(em_T_H_W_D, \"t h w d -&gt; (t h w) 1 1 d\").float()\n</code></pre>"},{"location":"api/worldkernels/models/components/embeddings/#worldkernels.models.components.embeddings.apply_rotary_emb","title":"apply_rotary_emb","text":"<pre><code>apply_rotary_emb(t: Tensor, freqs: Tensor) -&gt; Tensor\n</code></pre> <p>Half-rotation rotary embedding on <code>(B, S, H, D)</code> tensors.</p> <p><code>freqs</code> is a duplicated-frequency table <code>(S, 1, 1, rot_dim)</code>; cos/sin are taken in the input dtype and the rotation uses the NeoX half split.</p> Source code in <code>worldkernels/models/components/embeddings.py</code> <pre><code>def apply_rotary_emb(t: torch.Tensor, freqs: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Half-rotation rotary embedding on ``(B, S, H, D)`` tensors.\n\n    ``freqs`` is a duplicated-frequency table ``(S, 1, 1, rot_dim)``; cos/sin\n    are taken in the input dtype and the rotation uses the NeoX half split.\n    \"\"\"\n    if freqs.ndim == 4 and freqs.shape[1] == 1:\n        freqs = freqs.transpose(0, 1)\n    cos_ = freqs.cos().to(t.dtype)\n    sin_ = freqs.sin().to(t.dtype)\n    rot_dim = cos_.shape[-1]\n    half = rot_dim // 2\n    t_rot = t[..., :rot_dim]\n    t_pass = t[..., rot_dim:]\n    x1, x2 = t_rot[..., :half], t_rot[..., half:]\n    cos_h = cos_[..., :half]\n    sin_h = sin_[..., :half]\n    o1 = x1 * cos_h - x2 * sin_h\n    o2 = x2 * cos_h + x1 * sin_h\n    return torch.cat([o1, o2, t_pass], dim=-1)\n</code></pre>"},{"location":"api/worldkernels/models/components/normalization/","title":"normalization","text":""},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization","title":"worldkernels.models.components.normalization","text":"<p>Normalization layers for native video-diffusion models.</p>"},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RMSNorm']\n</code></pre>"},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization.RMSNorm","title":"RMSNorm","text":"<p>               Bases: <code>Module</code></p> <p>Root-mean-square norm computed in float32, cast back to the input dtype.</p> \\[\\text{RMSNorm}(x) = \\frac{x}{\\sqrt{\\overline{x^2} + \\epsilon}} \\cdot w\\] Source code in <code>worldkernels/models/components/normalization.py</code> <pre><code>class RMSNorm(nn.Module):\n    r\"\"\"Root-mean-square norm computed in float32, cast back to the input dtype.\n\n    $$\\text{RMSNorm}(x) = \\frac{x}{\\sqrt{\\overline{x^2} + \\epsilon}} \\cdot w$$\n    \"\"\"\n\n    def __init__(self, dim: int, eps: float = 1e-6) -&gt; None:\n        super().__init__()\n        self.eps = eps\n        self.weight = nn.Parameter(torch.ones(dim))\n\n    def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n        norm = torch.rsqrt(x.float().pow(2).mean(-1, keepdim=True) + self.eps)\n        return (x.float() * norm).to(x.dtype) * self.weight\n\n    def reset_parameters(self) -&gt; None:\n        nn.init.ones_(self.weight)\n</code></pre>"},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization.RMSNorm.eps","title":"eps  <code>instance-attribute</code>","text":"<pre><code>eps = eps\n</code></pre>"},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization.RMSNorm.weight","title":"weight  <code>instance-attribute</code>","text":"<pre><code>weight = nn.Parameter(torch.ones(dim))\n</code></pre>"},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization.RMSNorm.__init__","title":"__init__","text":"<pre><code>__init__(dim: int, eps: float = 1e-06) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/normalization.py</code> <pre><code>def __init__(self, dim: int, eps: float = 1e-6) -&gt; None:\n    super().__init__()\n    self.eps = eps\n    self.weight = nn.Parameter(torch.ones(dim))\n</code></pre>"},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization.RMSNorm.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/normalization.py</code> <pre><code>def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n    norm = torch.rsqrt(x.float().pow(2).mean(-1, keepdim=True) + self.eps)\n    return (x.float() * norm).to(x.dtype) * self.weight\n</code></pre>"},{"location":"api/worldkernels/models/components/normalization/#worldkernels.models.components.normalization.RMSNorm.reset_parameters","title":"reset_parameters","text":"<pre><code>reset_parameters() -&gt; None\n</code></pre> Source code in <code>worldkernels/models/components/normalization.py</code> <pre><code>def reset_parameters(self) -&gt; None:\n    nn.init.ones_(self.weight)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/","title":"dreamdojo","text":""},{"location":"api/worldkernels/models/dreamdojo/#worldkernels.models.dreamdojo","title":"worldkernels.models.dreamdojo","text":"<p>DreamDojo model family: native action-conditioned DiT, tokenizer, checkpoints.</p> <p>Modules:</p> Name Description <code>checkpoint</code> <p>DreamDojo checkpoint download, DCP\u2192.pt conversion, and native net loading.</p> <code>lam</code> <p>Latent action encoder: continuous proxy actions from consecutive frame pairs.</p> <code>net</code> <p>Action-conditioned video DiT for DreamDojo (teacher clip + causal student paths).</p> <code>pipeline</code> <p>Native DreamDojo inference pipeline: action-conditioned rectified-flow video2world.</p> <code>tokenizer</code> <p>Wan2.1 video tokenizer for DreamDojo, matching the reference bf16 numerics.</p>"},{"location":"api/worldkernels/models/dreamdojo/#worldkernels.models.dreamdojo.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ActionVideoDiT', 'NET_2B', 'NET_14B', 'VideoTokenizer', 'download_dreamdojo_checkpoint', 'LatentActionEncoder', 'load_net', 'remap_state_dict']\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/#worldkernels.models.dreamdojo._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'ActionVideoDiT': ('worldkernels.models.dreamdojo.net', 'ActionVideoDiT'), 'NET_2B': ('worldkernels.models.dreamdojo.net', 'NET_2B'), 'NET_14B': ('worldkernels.models.dreamdojo.net', 'NET_14B'), 'VideoTokenizer': ('worldkernels.models.dreamdojo.tokenizer', 'VideoTokenizer'), 'download_dreamdojo_checkpoint': ('worldkernels.models.dreamdojo.checkpoint', 'download_dreamdojo_checkpoint'), 'LatentActionEncoder': ('worldkernels.models.dreamdojo.lam', 'LatentActionEncoder'), 'load_net': ('worldkernels.models.dreamdojo.checkpoint', 'load_net'), 'remap_state_dict': ('worldkernels.models.dreamdojo.checkpoint', 'remap_state_dict')}\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/#worldkernels.models.dreamdojo.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/","title":"checkpoint","text":""},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint","title":"worldkernels.models.dreamdojo.checkpoint","text":"<p>DreamDojo checkpoint download, DCP\u2192.pt conversion, and native net loading.</p>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.HF_REPO","title":"HF_REPO  <code>module-attribute</code>","text":"<pre><code>HF_REPO = 'nvidia/DreamDojo'\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.TEXT_EMB_REPO","title":"TEXT_EMB_REPO  <code>module-attribute</code>","text":"<pre><code>TEXT_EMB_REPO = 'nvidia/Cosmos-Predict2.5-2B'\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.TEXT_EMB_FILE","title":"TEXT_EMB_FILE  <code>module-attribute</code>","text":"<pre><code>TEXT_EMB_FILE = 'robot/action-cond/cr1_empty_string_text_embeddings.pt'\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.CKPT_DIRS","title":"CKPT_DIRS  <code>module-attribute</code>","text":"<pre><code>CKPT_DIRS = {'2b_pretrain': '2B_pretrain', '2b_gr1': '2B_GR1_post-train', '2b_agibot': '2B_AgiBot_post-train', '2b_g1': '2B_G1_post-train', '2b_yam': '2B_YAM_post-train', '14b_pretrain': '14B_pretrain', '14b_gr1': '14B_GR1_post-train', '14b_agibot': '14B_AgiBot_post-train', '14b_g1': '14B_G1_post-train', '14b_yam': '14B_YAM_post-train'}\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.provision_dreamdojo_weights","title":"provision_dreamdojo_weights","text":"<pre><code>provision_dreamdojo_weights(variant: str | None = None) -&gt; str\n</code></pre> <p>ModelCard <code>weights_provider</code> entry: map a variant to its checkpoint dir.</p> <p>The DreamDojo repo is an ~859 GB monorepo (every 2B/14B variant, each with model + optimizer + scheduler + trainer shards). A blanket <code>snapshot_download</code> would mirror all of it, so the card routes provisioning here, which fetches only the requested variant's latest model shards (~13 GB for 2B).</p> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def provision_dreamdojo_weights(variant: str | None = None) -&gt; str:\n    r\"\"\"ModelCard ``weights_provider`` entry: map a variant to its checkpoint dir.\n\n    The DreamDojo repo is an ~859 GB monorepo (every 2B/14B variant, each with\n    model + optimizer + scheduler + trainer shards). A blanket ``snapshot_download``\n    would mirror all of it, so the card routes provisioning here, which fetches only\n    the requested variant's latest model shards (~13 GB for 2B).\n    \"\"\"\n    return download_dreamdojo_checkpoint(CKPT_DIRS.get(variant or \"2b_pretrain\", \"2B_pretrain\"))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.download_dreamdojo_checkpoint","title":"download_dreamdojo_checkpoint","text":"<pre><code>download_dreamdojo_checkpoint(ckpt_dir_name: str = '2B_pretrain') -&gt; str\n</code></pre> <p>Download a DreamDojo checkpoint (DCP format) and convert to a bf16 <code>.pt</code>.</p> <p>DreamDojo ships only DCP training shards (no ready-made inference file). The latest iter's <code>model/</code> directory is ~12.9 GB: <code>net_ema.*</code> (the EMA, fp32, ~8.6 GB) + <code>net.*</code> (the model, ~4.3 GB), FSDP-sharded uniformly across all 8 <code>.distcp</code> files so none can be skipped. We fetch those shards into a scratch dir, keep only the EMA weights converted to bf16 (~4.3 GB), then delete the scratch shards so the cached footprint is the converted file alone, not ~17 GB.</p> <p>Two-stage fetch: stage 1 grabs <code>latest_checkpoint.txt</code> (a few bytes); stage 2 grabs only <code>{ckpt_dir}/{latest_iter}/model/*</code> (the sibling <code>optim/</code> Adam-moment shards are deliberately excluded).</p> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def download_dreamdojo_checkpoint(ckpt_dir_name: str = \"2B_pretrain\") -&gt; str:\n    r\"\"\"Download a DreamDojo checkpoint (DCP format) and convert to a bf16 ``.pt``.\n\n    DreamDojo ships only DCP training shards (no ready-made inference file). The latest\n    iter's ``model/`` directory is ~12.9 GB: ``net_ema.*`` (the EMA, fp32, ~8.6 GB) +\n    ``net.*`` (the model, ~4.3 GB), FSDP-sharded uniformly across all 8 ``.distcp`` files\n    so none can be skipped. We fetch those shards into a scratch dir, keep only the EMA\n    weights converted to bf16 (~4.3 GB), then **delete the scratch shards** so the cached\n    footprint is the converted file alone, not ~17 GB.\n\n    Two-stage fetch: stage 1 grabs ``latest_checkpoint.txt`` (a few bytes); stage 2 grabs\n    only ``{ckpt_dir}/{latest_iter}/model/*`` (the sibling ``optim/`` Adam-moment shards\n    are deliberately excluded).\n    \"\"\"\n    import shutil\n\n    from huggingface_hub import hf_hub_download, snapshot_download\n\n    from worldkernels.bootstrap import cache\n    from worldkernels.bootstrap.hf import enable_fast_hf_transfer\n\n    enable_fast_hf_transfer()\n\n    latest_file_local = hf_hub_download(\n        HF_REPO,\n        filename=f\"{ckpt_dir_name}/latest_checkpoint.txt\",\n        repo_type=\"model\",\n    )\n    iter_name = Path(latest_file_local).read_text().strip()\n    if not iter_name or \"/\" in iter_name or iter_name.startswith(\".\"):\n        raise RuntimeError(\n            f\"DreamDojo {ckpt_dir_name}: invalid latest iter {iter_name!r} in latest_checkpoint.txt\"\n        )\n\n    out_dir = cache.home() / \"dreamdojo\" / ckpt_dir_name / iter_name\n    pt_path = out_dir / \"model_ema_bf16.pt\"\n    if pt_path.exists():\n        return str(pt_path)\n\n    log.info(\n        \"DreamDojo %s: latest iteration is %r \u2014 fetching only that iter's model shards\",\n        ckpt_dir_name,\n        iter_name,\n    )\n    work_dir = out_dir / \"_dcp\"\n    snapshot_download(\n        HF_REPO,\n        allow_patterns=[f\"{ckpt_dir_name}/{iter_name}/model/*\"],\n        repo_type=\"model\",\n        local_dir=str(work_dir),\n    )\n    model_dir = work_dir / ckpt_dir_name / iter_name / \"model\"\n    if not model_dir.is_dir() or not any(model_dir.glob(\"*.distcp\")):\n        raise RuntimeError(\n            f\"DreamDojo {ckpt_dir_name}/{iter_name}: no model shards under {model_dir} \"\n            \"(repo layout changed?)\"\n        )\n\n    log.info(\"Converting DCP checkpoint to bf16 EMA .pt at %s\", pt_path)\n    import torch\n\n    out_dir.mkdir(parents=True, exist_ok=True)\n    state_dict = _consolidate_dcp(model_dir, work_dir)\n    ema_bf16 = {}\n    for key, value in state_dict.items():\n        if key.startswith(\"net_ema.\"):\n            new_key = \"net.\" + key[len(\"net_ema.\") :]\n            if isinstance(value, torch.Tensor) and value.dtype == torch.float32:\n                value = value.bfloat16()\n            ema_bf16[new_key] = value\n    if not ema_bf16:\n        raise RuntimeError(\n            f\"DreamDojo {ckpt_dir_name}/{iter_name}: checkpoint has no 'net_ema.*' weights\"\n        )\n    torch.save(ema_bf16, pt_path)\n    shutil.rmtree(work_dir, ignore_errors=True)\n    log.info(\n        \"Saved EMA bf16 checkpoint (%.1f GB) and pruned raw DCP shards: %s\",\n        pt_path.stat().st_size / 1e9,\n        pt_path,\n    )\n    return str(pt_path)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint._consolidate_dcp","title":"_consolidate_dcp","text":"<pre><code>_consolidate_dcp(model_dir: Path, scratch_dir: Path) -&gt; dict\n</code></pre> <p>DCP shards \u2192 a consolidated CPU state dict (via a transient <code>.pt</code> in <code>scratch_dir</code>).</p> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def _consolidate_dcp(model_dir: Path, scratch_dir: Path) -&gt; dict:\n    r\"\"\"DCP shards \u2192 a consolidated CPU state dict (via a transient ``.pt`` in ``scratch_dir``).\"\"\"\n    import torch\n    from torch.distributed.checkpoint.format_utils import dcp_to_torch_save\n\n    full_pt = scratch_dir / \"model.pt\"\n    try:\n        dcp_to_torch_save(str(model_dir), str(full_pt))\n        return torch.load(full_pt, map_location=\"cpu\", weights_only=False)\n    finally:\n        full_pt.unlink(missing_ok=True)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.download_lam_checkpoint","title":"download_lam_checkpoint","text":"<pre><code>download_lam_checkpoint() -&gt; str\n</code></pre> <p>Local path to the latent action model checkpoint (<code>LAM_400k.ckpt</code>).</p> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def download_lam_checkpoint() -&gt; str:\n    r\"\"\"Local path to the latent action model checkpoint (``LAM_400k.ckpt``).\"\"\"\n    from huggingface_hub import hf_hub_download\n\n    from worldkernels.bootstrap.hf import enable_fast_hf_transfer\n\n    enable_fast_hf_transfer()\n    return hf_hub_download(HF_REPO, filename=\"LAM_400k.ckpt\", repo_type=\"model\")\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.download_text_embedding","title":"download_text_embedding","text":"<pre><code>download_text_embedding() -&gt; str\n</code></pre> <p>Local path to the precomputed CR1 empty-string text embedding.</p> <p>The action-conditioned model was trained and is evaluated with the real (non-zero) embedding of the empty prompt; this precomputed <code>.pt</code> supplies that in-distribution neutral conditioning without the 7B text encoder. A locally computed copy in the worldkernels cache (see <code>compute_text_embedding</code>) takes precedence over the gated HF download.</p> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def download_text_embedding() -&gt; str:\n    r\"\"\"Local path to the precomputed CR1 empty-string text embedding.\n\n    The action-conditioned model was trained and is evaluated with the real\n    (non-zero) embedding of the empty prompt; this precomputed ``.pt`` supplies\n    that in-distribution neutral conditioning without the 7B text encoder. A\n    locally computed copy in the worldkernels cache (see\n    ``compute_text_embedding``) takes precedence over the gated HF download.\n    \"\"\"\n    from worldkernels.bootstrap import cache\n\n    local = cache.home() / \"dreamdojo\" / \"cr1_empty_string_text_embeddings.pt\"\n    if local.exists():\n        return str(local)\n\n    from huggingface_hub import hf_hub_download\n\n    from worldkernels.bootstrap.hf import enable_fast_hf_transfer\n\n    enable_fast_hf_transfer()\n    try:\n        return hf_hub_download(TEXT_EMB_REPO, filename=TEXT_EMB_FILE, repo_type=\"model\")\n    except Exception as err:\n        raise RuntimeError(\n            f\"Cannot fetch the CR1 empty-string text embedding from the gated repo \"\n            f\"{TEXT_EMB_REPO!r} (accept its license and set a valid HF_TOKEN), or run \"\n            f\"compute_text_embedding() once to derive it locally from \"\n            f\"nvidia/Cosmos-Reason1-7B.\"\n        ) from err\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.compute_text_embedding","title":"compute_text_embedding","text":"<pre><code>compute_text_embedding() -&gt; str\n</code></pre> <p>Derive the CR1 empty-string embedding locally; returns the cached <code>.pt</code> path.</p> <p>Reproduces the reference recipe: the Cosmos-Reason1-7B chat template around the empty prompt, padded to 512 tokens, hidden states of all 28 layers each mean-normalized per token and concatenated to 100352 channels.</p> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def compute_text_embedding() -&gt; str:\n    r\"\"\"Derive the CR1 empty-string embedding locally; returns the cached ``.pt`` path.\n\n    Reproduces the reference recipe: the Cosmos-Reason1-7B chat template around\n    the empty prompt, padded to 512 tokens, hidden states of all 28 layers each\n    mean-normalized per token and concatenated to 100352 channels.\n    \"\"\"\n    prompt = \"\"\n    import torch\n    from transformers import AutoTokenizer, Qwen2_5_VLForConditionalGeneration\n\n    from worldkernels.bootstrap import cache\n\n    repo = \"nvidia/Cosmos-Reason1-7B\"\n    tok = AutoTokenizer.from_pretrained(repo)\n    system_text = \"You are a helpful assistant who will provide prompts to an image generator.\"\n    conversations: list[dict] = [\n        {\"role\": \"system\", \"content\": [{\"type\": \"text\", \"text\": system_text}]},\n        {\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": prompt}]},\n    ]\n    text = tok.apply_chat_template(conversations, tokenize=False, add_generation_prompt=False)\n    ids = tok(text, add_special_tokens=False)[\"input_ids\"][:512]\n    ids = ids + [tok.pad_token_id] * (512 - len(ids))\n    device = \"cuda\" if torch.cuda.is_available() else \"cpu\"\n    input_ids = torch.LongTensor(ids).unsqueeze(0).to(device)\n\n    encoder = Qwen2_5_VLForConditionalGeneration.from_pretrained(  # type: ignore[arg-type]\n        repo, torch_dtype=torch.bfloat16\n    )\n    model = encoder.to(device).eval()  # type: ignore[arg-type]\n    with torch.no_grad():\n        outputs = model(input_ids, output_hidden_states=True)\n    hidden_states = outputs.hidden_states\n\n    def _mean_normalize(t: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        return (t - t.mean(dim=-1, keepdim=True)) / (t.std(dim=-1, keepdim=True) + 1e-8)\n\n    emb = torch.cat([_mean_normalize(h) for h in hidden_states[1:]], dim=-1)\n    del model\n    if device == \"cuda\":\n        torch.cuda.empty_cache()\n\n    out_path = cache.home() / \"dreamdojo\" / \"cr1_empty_string_text_embeddings.pt\"\n    out_path.parent.mkdir(parents=True, exist_ok=True)\n    torch.save(emb.squeeze(0).float().cpu(), out_path)\n    return str(out_path)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.remap_state_dict","title":"remap_state_dict","text":"<pre><code>remap_state_dict(state_dict: dict[str, 'torch.Tensor']) -&gt; dict[str, 'torch.Tensor']\n</code></pre> <p>Converted-checkpoint keys \u2192 native <code>ActionVideoDiT</code> namespace.</p> <p>Strips the <code>net.</code> prefix and drops TransformerEngine <code>*_extra_state</code> blobs and <code>accum_*</code> training counters. Applies unchanged to teacher and distilled-student checkpoints (shared namespace).</p> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def remap_state_dict(state_dict: dict[str, \"torch.Tensor\"]) -&gt; dict[str, \"torch.Tensor\"]:\n    r\"\"\"Converted-checkpoint keys \u2192 native `ActionVideoDiT` namespace.\n\n    Strips the ``net.`` prefix and drops TransformerEngine ``*_extra_state``\n    blobs and ``accum_*`` training counters. Applies unchanged to teacher and\n    distilled-student checkpoints (shared namespace).\n    \"\"\"\n    out = {}\n    for key, value in state_dict.items():\n        if key.endswith(\"_extra_state\"):\n            continue\n        key = key.removeprefix(\"net.\")\n        if key.startswith(\"accum_\"):\n            continue\n        out[key] = value\n    return out\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/checkpoint/#worldkernels.models.dreamdojo.checkpoint.load_net","title":"load_net","text":"<pre><code>load_net(variant: str, *, ckpt_path: str | None = None, device: str | 'torch.device' = 'cuda', dtype: 'torch.dtype | None' = None) -&gt; 'ActionVideoDiT'\n</code></pre> <p>Build the native DreamDojo DiT for <code>variant</code> and load its weights.</p> <p>Parameters:</p> Name Type Description Default <code>variant</code> <code>str</code> <p>A <code>CKPT_DIRS</code> key (e.g. <code>\"2b_gr1\"</code>); the size prefix picks the net preset.</p> required <code>ckpt_path</code> <code>str | None</code> <p>Optional converted <code>.pt</code> path (e.g. a distilled student checkpoint); defaults to downloading the released teacher weights.</p> <code>None</code> Source code in <code>worldkernels/models/dreamdojo/checkpoint.py</code> <pre><code>def load_net(\n    variant: str,\n    *,\n    ckpt_path: str | None = None,\n    device: str | \"torch.device\" = \"cuda\",\n    dtype: \"torch.dtype | None\" = None,\n) -&gt; \"ActionVideoDiT\":\n    r\"\"\"Build the native DreamDojo DiT for ``variant`` and load its weights.\n\n    Args:\n        variant: A `CKPT_DIRS` key (e.g. ``\"2b_gr1\"``); the size prefix picks\n            the net preset.\n        ckpt_path: Optional converted ``.pt`` path (e.g. a distilled student\n            checkpoint); defaults to downloading the released teacher weights.\n    \"\"\"\n    import torch\n\n    from worldkernels.models.dreamdojo.net import NET_2B, NET_14B, ActionVideoDiT\n\n    if variant not in CKPT_DIRS:\n        raise ValueError(f\"unknown DreamDojo variant {variant!r}; known: {sorted(CKPT_DIRS)}\")\n    preset = NET_14B if variant.startswith(\"14b\") else NET_2B\n    with torch.device(\"meta\"):\n        net = ActionVideoDiT(**preset)\n\n    path = ckpt_path or download_dreamdojo_checkpoint(CKPT_DIRS[variant])\n    state_dict = remap_state_dict(torch.load(path, map_location=\"cpu\", weights_only=True))\n    net.load_state_dict(state_dict, assign=True)\n    net = net.to(device=device, dtype=dtype or torch.bfloat16)\n    return net.eval().requires_grad_(False)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/","title":"lam","text":""},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam","title":"worldkernels.models.dreamdojo.lam","text":"<p>Latent action encoder: continuous proxy actions from consecutive frame pairs.</p>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['LatentActionEncoder', 'LAM_INPUT_HEIGHT', 'LAM_INPUT_WIDTH', 'LATENT_ACTION_DIM']\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LAM_INPUT_HEIGHT","title":"LAM_INPUT_HEIGHT  <code>module-attribute</code>","text":"<pre><code>LAM_INPUT_HEIGHT = 240\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LAM_INPUT_WIDTH","title":"LAM_INPUT_WIDTH  <code>module-attribute</code>","text":"<pre><code>LAM_INPUT_WIDTH = 320\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LATENT_ACTION_DIM","title":"LATENT_ACTION_DIM  <code>module-attribute</code>","text":"<pre><code>LATENT_ACTION_DIM = 32\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LATENT_ACTION_SLICE","title":"LATENT_ACTION_SLICE  <code>module-attribute</code>","text":"<pre><code>LATENT_ACTION_SLICE = slice(352, 384)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._Rotary","title":"_Rotary","text":"<p>               Bases: <code>Module</code></p> <p>Interleaved-pair rotary embedding over the temporal token axis.</p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>class _Rotary(nn.Module):\n    r\"\"\"Interleaved-pair rotary embedding over the temporal token axis.\"\"\"\n\n    def __init__(self, dim: int, theta: float = 10000.0) -&gt; None:\n        super().__init__()\n        freqs = 1.0 / (theta ** (torch.arange(0, dim, 2)[: dim // 2].float() / dim))\n        self.freqs = nn.Parameter(freqs, requires_grad=False)\n\n    def rotate(self, t: torch.Tensor) -&gt; torch.Tensor:\n        seq = torch.arange(t.shape[-2], device=t.device, dtype=t.dtype)\n        freqs = torch.einsum(\"n,f-&gt;nf\", seq.to(self.freqs.dtype), self.freqs)\n        freqs = freqs.repeat_interleave(2, dim=-1)\n        return t * freqs.cos() + _rotate_interleaved(t) * freqs.sin()\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._Rotary.freqs","title":"freqs  <code>instance-attribute</code>","text":"<pre><code>freqs = nn.Parameter(freqs, requires_grad=False)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._Rotary.__init__","title":"__init__","text":"<pre><code>__init__(dim: int, theta: float = 10000.0) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def __init__(self, dim: int, theta: float = 10000.0) -&gt; None:\n    super().__init__()\n    freqs = 1.0 / (theta ** (torch.arange(0, dim, 2)[: dim // 2].float() / dim))\n    self.freqs = nn.Parameter(freqs, requires_grad=False)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._Rotary.rotate","title":"rotate","text":"<pre><code>rotate(t: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def rotate(self, t: torch.Tensor) -&gt; torch.Tensor:\n    seq = torch.arange(t.shape[-2], device=t.device, dtype=t.dtype)\n    freqs = torch.einsum(\"n,f-&gt;nf\", seq.to(self.freqs.dtype), self.freqs)\n    freqs = freqs.repeat_interleave(2, dim=-1)\n    return t * freqs.cos() + _rotate_interleaved(t) * freqs.sin()\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention","title":"_SelfAttention","text":"<p>               Bases: <code>Module</code></p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>class _SelfAttention(nn.Module):\n    def __init__(self, model_dim: int, num_heads: int, rot_emb: bool = False) -&gt; None:\n        super().__init__()\n        head_dim = model_dim // num_heads\n        self.scale = head_dim**-0.5\n        self.heads = num_heads\n        self.to_q = nn.Linear(model_dim, model_dim, bias=False)\n        self.to_k = nn.Linear(model_dim, model_dim, bias=False)\n        self.to_v = nn.Linear(model_dim, model_dim, bias=False)\n        self.to_out = nn.Sequential(nn.Linear(model_dim, model_dim), nn.Dropout(0.0))\n        if rot_emb:\n            self.rotary_embedding = _Rotary(head_dim)\n\n    def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n        q, k, v = self.to_q(x), self.to_k(x), self.to_v(x)\n        q, k, v = (rearrange(t, \"b n (h d) -&gt; b h n d\", h=self.heads) for t in (q, k, v))\n        if hasattr(self, \"rotary_embedding\"):\n            q = self.rotary_embedding.rotate(q).contiguous()\n            k = self.rotary_embedding.rotate(k).contiguous()\n        attn = torch.softmax(q @ k.transpose(-2, -1) * self.scale, dim=-1)\n        out = rearrange(attn @ v, \"b h n d -&gt; b n (h d)\")\n        return self.to_out(out)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.scale","title":"scale  <code>instance-attribute</code>","text":"<pre><code>scale = head_dim ** -0.5\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.heads","title":"heads  <code>instance-attribute</code>","text":"<pre><code>heads = num_heads\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.to_q","title":"to_q  <code>instance-attribute</code>","text":"<pre><code>to_q = nn.Linear(model_dim, model_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.to_k","title":"to_k  <code>instance-attribute</code>","text":"<pre><code>to_k = nn.Linear(model_dim, model_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.to_v","title":"to_v  <code>instance-attribute</code>","text":"<pre><code>to_v = nn.Linear(model_dim, model_dim, bias=False)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.to_out","title":"to_out  <code>instance-attribute</code>","text":"<pre><code>to_out = nn.Sequential(nn.Linear(model_dim, model_dim), nn.Dropout(0.0))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.rotary_embedding","title":"rotary_embedding  <code>instance-attribute</code>","text":"<pre><code>rotary_embedding = _Rotary(head_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.__init__","title":"__init__","text":"<pre><code>__init__(model_dim: int, num_heads: int, rot_emb: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def __init__(self, model_dim: int, num_heads: int, rot_emb: bool = False) -&gt; None:\n    super().__init__()\n    head_dim = model_dim // num_heads\n    self.scale = head_dim**-0.5\n    self.heads = num_heads\n    self.to_q = nn.Linear(model_dim, model_dim, bias=False)\n    self.to_k = nn.Linear(model_dim, model_dim, bias=False)\n    self.to_v = nn.Linear(model_dim, model_dim, bias=False)\n    self.to_out = nn.Sequential(nn.Linear(model_dim, model_dim), nn.Dropout(0.0))\n    if rot_emb:\n        self.rotary_embedding = _Rotary(head_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SelfAttention.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n    q, k, v = self.to_q(x), self.to_k(x), self.to_v(x)\n    q, k, v = (rearrange(t, \"b n (h d) -&gt; b h n d\", h=self.heads) for t in (q, k, v))\n    if hasattr(self, \"rotary_embedding\"):\n        q = self.rotary_embedding.rotate(q).contiguous()\n        k = self.rotary_embedding.rotate(k).contiguous()\n    attn = torch.softmax(q @ k.transpose(-2, -1) * self.scale, dim=-1)\n    out = rearrange(attn @ v, \"b h n d -&gt; b n (h d)\")\n    return self.to_out(out)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock","title":"_SpatioTemporalBlock","text":"<p>               Bases: <code>Module</code></p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>class _SpatioTemporalBlock(nn.Module):\n    def __init__(self, model_dim: int, num_heads: int) -&gt; None:\n        super().__init__()\n        self.spatial_attn = _SelfAttention(model_dim, num_heads)\n        self.temporal_attn = _SelfAttention(model_dim, num_heads, rot_emb=True)\n        self.ffn = nn.Sequential(\n            nn.Linear(model_dim, model_dim * 4),\n            nn.GELU(),\n            nn.Dropout(0.0),\n            nn.Linear(model_dim * 4, model_dim),\n        )\n        self.norm1 = nn.LayerNorm(model_dim)\n        self.norm2 = nn.LayerNorm(model_dim)\n        self.norm3 = nn.LayerNorm(model_dim)\n\n    def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n        t_len, s_len = x.shape[1:3]\n        x = rearrange(x, \"b t s e -&gt; (b t) s e\")\n        x = x + self.spatial_attn(self.norm1(x))\n        x = rearrange(x, \"(b t) s e -&gt; b t s e\", t=t_len)\n\n        x = rearrange(x, \"b t s e -&gt; (b s) t e\")\n        x = x + self.temporal_attn(self.norm2(x))\n        x = rearrange(x, \"(b s) t e -&gt; b t s e\", s=s_len)\n\n        return x + self.ffn(self.norm3(x))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.spatial_attn","title":"spatial_attn  <code>instance-attribute</code>","text":"<pre><code>spatial_attn = _SelfAttention(model_dim, num_heads)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.temporal_attn","title":"temporal_attn  <code>instance-attribute</code>","text":"<pre><code>temporal_attn = _SelfAttention(model_dim, num_heads, rot_emb=True)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.ffn","title":"ffn  <code>instance-attribute</code>","text":"<pre><code>ffn = nn.Sequential(nn.Linear(model_dim, model_dim * 4), nn.GELU(), nn.Dropout(0.0), nn.Linear(model_dim * 4, model_dim))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.norm1","title":"norm1  <code>instance-attribute</code>","text":"<pre><code>norm1 = nn.LayerNorm(model_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.norm2","title":"norm2  <code>instance-attribute</code>","text":"<pre><code>norm2 = nn.LayerNorm(model_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.norm3","title":"norm3  <code>instance-attribute</code>","text":"<pre><code>norm3 = nn.LayerNorm(model_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.__init__","title":"__init__","text":"<pre><code>__init__(model_dim: int, num_heads: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def __init__(self, model_dim: int, num_heads: int) -&gt; None:\n    super().__init__()\n    self.spatial_attn = _SelfAttention(model_dim, num_heads)\n    self.temporal_attn = _SelfAttention(model_dim, num_heads, rot_emb=True)\n    self.ffn = nn.Sequential(\n        nn.Linear(model_dim, model_dim * 4),\n        nn.GELU(),\n        nn.Dropout(0.0),\n        nn.Linear(model_dim * 4, model_dim),\n    )\n    self.norm1 = nn.LayerNorm(model_dim)\n    self.norm2 = nn.LayerNorm(model_dim)\n    self.norm3 = nn.LayerNorm(model_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalBlock.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n    t_len, s_len = x.shape[1:3]\n    x = rearrange(x, \"b t s e -&gt; (b t) s e\")\n    x = x + self.spatial_attn(self.norm1(x))\n    x = rearrange(x, \"(b t) s e -&gt; b t s e\", t=t_len)\n\n    x = rearrange(x, \"b t s e -&gt; (b s) t e\")\n    x = x + self.temporal_attn(self.norm2(x))\n    x = rearrange(x, \"(b s) t e -&gt; b t s e\", s=s_len)\n\n    return x + self.ffn(self.norm3(x))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalTransformer","title":"_SpatioTemporalTransformer","text":"<p>               Bases: <code>Module</code></p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>class _SpatioTemporalTransformer(nn.Module):\n    pe: torch.Tensor\n\n    def __init__(\n        self, in_dim: int, model_dim: int, out_dim: int, num_blocks: int, num_heads: int\n    ) -&gt; None:\n        super().__init__()\n        self.ffn = nn.Sequential(\n            nn.LayerNorm(in_dim), nn.Linear(in_dim, model_dim), nn.LayerNorm(model_dim)\n        )\n        pe = torch.zeros(5000, model_dim)\n        position = torch.arange(0, 5000).float().unsqueeze(1)\n        div_term = torch.exp(\n            torch.arange(0, model_dim, 2).float() * -(math.log(10000.0) / model_dim)\n        )\n        pe[:, 0::2] = torch.sin(position * div_term)\n        pe[:, 1::2] = torch.cos(position * div_term)\n        self.register_buffer(\"pe\", pe, persistent=False)\n        self.transformer_blocks = nn.ModuleList(\n            _SpatioTemporalBlock(model_dim, num_heads) for _ in range(num_blocks)\n        )\n        self.out = nn.Linear(model_dim, out_dim)\n\n    def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n        x = self.ffn(x)\n        x = x + self.pe[: x.shape[2]]\n        for block in self.transformer_blocks:\n            x = block(x)\n        return self.out(x)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalTransformer.pe","title":"pe  <code>instance-attribute</code>","text":"<pre><code>pe: Tensor\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalTransformer.ffn","title":"ffn  <code>instance-attribute</code>","text":"<pre><code>ffn = nn.Sequential(nn.LayerNorm(in_dim), nn.Linear(in_dim, model_dim), nn.LayerNorm(model_dim))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalTransformer.transformer_blocks","title":"transformer_blocks  <code>instance-attribute</code>","text":"<pre><code>transformer_blocks = nn.ModuleList((_SpatioTemporalBlock(model_dim, num_heads)) for _ in (range(num_blocks)))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalTransformer.out","title":"out  <code>instance-attribute</code>","text":"<pre><code>out = nn.Linear(model_dim, out_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalTransformer.__init__","title":"__init__","text":"<pre><code>__init__(in_dim: int, model_dim: int, out_dim: int, num_blocks: int, num_heads: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def __init__(\n    self, in_dim: int, model_dim: int, out_dim: int, num_blocks: int, num_heads: int\n) -&gt; None:\n    super().__init__()\n    self.ffn = nn.Sequential(\n        nn.LayerNorm(in_dim), nn.Linear(in_dim, model_dim), nn.LayerNorm(model_dim)\n    )\n    pe = torch.zeros(5000, model_dim)\n    position = torch.arange(0, 5000).float().unsqueeze(1)\n    div_term = torch.exp(\n        torch.arange(0, model_dim, 2).float() * -(math.log(10000.0) / model_dim)\n    )\n    pe[:, 0::2] = torch.sin(position * div_term)\n    pe[:, 1::2] = torch.cos(position * div_term)\n    self.register_buffer(\"pe\", pe, persistent=False)\n    self.transformer_blocks = nn.ModuleList(\n        _SpatioTemporalBlock(model_dim, num_heads) for _ in range(num_blocks)\n    )\n    self.out = nn.Linear(model_dim, out_dim)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._SpatioTemporalTransformer.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n    x = self.ffn(x)\n    x = x + self.pe[: x.shape[2]]\n    for block in self.transformer_blocks:\n        x = block(x)\n    return self.out(x)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder","title":"LatentActionEncoder","text":"<p>               Bases: <code>Module</code></p> <p>Encoder half of the DreamDojo latent action VAE (frame pair \u2192 32-dim action).</p> <p>A learned action-prompt token is prepended to each frame's 16x16 patch tokens; a 24-block spatiotemporal transformer attends across the pair and the second frame's prompt token is projected to the posterior mean, the continuous latent action occupying slice <code>[352:384)</code> of the universal action vector.</p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>class LatentActionEncoder(nn.Module):\n    r\"\"\"Encoder half of the DreamDojo latent action VAE (frame pair \u2192 32-dim action).\n\n    A learned action-prompt token is prepended to each frame's 16x16 patch\n    tokens; a 24-block spatiotemporal transformer attends across the pair and\n    the second frame's prompt token is projected to the posterior mean, the\n    continuous latent action occupying slice ``[352:384)`` of the universal\n    action vector.\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        in_dim: int = 3,\n        model_dim: int = 1024,\n        latent_dim: int = LATENT_ACTION_DIM,\n        patch_size: int = 16,\n        enc_blocks: int = 24,\n        num_heads: int = 16,\n    ) -&gt; None:\n        super().__init__()\n        self.patch_size = patch_size\n        self.model_dim = model_dim\n        self.latent_dim = latent_dim\n        patch_token_dim = in_dim * patch_size**2\n        self.action_prompt = nn.Parameter(torch.empty(1, 1, 1, patch_token_dim))\n        self.encoder = _SpatioTemporalTransformer(\n            in_dim=patch_token_dim,\n            model_dim=model_dim,\n            out_dim=model_dim,\n            num_blocks=enc_blocks,\n            num_heads=num_heads,\n        )\n        self.fc = nn.Linear(model_dim, latent_dim * 2)\n\n    @classmethod\n    def from_checkpoint(\n        cls, ckpt_path: str, device: \"str | torch.device\" = \"cuda\"\n    ) -&gt; \"LatentActionEncoder\":\n        r\"\"\"Load the encoder subset of a LAM Lightning checkpoint (``lam.`` prefix).\"\"\"\n        encoder = cls()\n        state = torch.load(ckpt_path, map_location=\"cpu\", weights_only=False)\n        state = state.get(\"state_dict\", state)\n        own = encoder.state_dict()\n        subset = {\n            k.removeprefix(\"lam.\"): v for k, v in state.items() if k.removeprefix(\"lam.\") in own\n        }\n        missing = set(own) - set(subset)\n        assert not missing, f\"LAM checkpoint missing encoder keys: {sorted(missing)[:5]}\"\n        encoder.load_state_dict(subset, strict=True)\n        return encoder.to(device).eval().requires_grad_(False)\n\n    def encode_pairs(self, frame_pairs: torch.Tensor) -&gt; torch.Tensor:\n        r\"\"\"Posterior means for ``(P, 2, H, W, 3)`` frame pairs in \\([0, 1]\\) \u2192 ``(P, 32)``.\"\"\"\n        P, T, H, W, _ = frame_pairs.shape\n        assert T == 2\n        size = self.patch_size\n        frame_pairs = frame_pairs[:, :, : H - (H % size), : W - (W % size), :]\n        patches = rearrange(\n            frame_pairs, \"b t (hn hp) (wn wp) c -&gt; b t (hn wn) (hp wp c)\", hp=size, wp=size\n        )\n        padded = torch.cat([self.action_prompt.expand(P, T, -1, -1), patches], dim=2)\n        z = self.encoder(padded)[:, 1:, 0].reshape(P, self.model_dim)\n        z_mu, _ = torch.chunk(self.fc(z), 2, dim=1)\n        return z_mu\n\n    def encode_video(self, frames_uint8: torch.Tensor) -&gt; torch.Tensor:\n        r\"\"\"Latent actions for consecutive pairs of ``(T, 3, H, W)`` uint8 frames \u2192 ``(T-1, 32)``.\n\n        Frames are bilinearly resized to the LAM's native 240x320 as in the\n        reference data pipeline.\n        \"\"\"\n        import torch.nn.functional as F\n\n        frames = frames_uint8.float() / 255.0\n        if frames.shape[-2:] != (LAM_INPUT_HEIGHT, LAM_INPUT_WIDTH):\n            frames = F.interpolate(\n                frames, size=(LAM_INPUT_HEIGHT, LAM_INPUT_WIDTH), mode=\"bilinear\"\n            )\n        frames = frames.permute(0, 2, 3, 1)\n        pairs = torch.stack([frames[:-1], frames[1:]], dim=1)\n        device = next(self.parameters()).device\n        with torch.no_grad():\n            return self.encode_pairs(pairs.to(device))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.patch_size","title":"patch_size  <code>instance-attribute</code>","text":"<pre><code>patch_size = patch_size\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.model_dim","title":"model_dim  <code>instance-attribute</code>","text":"<pre><code>model_dim = model_dim\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.latent_dim","title":"latent_dim  <code>instance-attribute</code>","text":"<pre><code>latent_dim = latent_dim\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.action_prompt","title":"action_prompt  <code>instance-attribute</code>","text":"<pre><code>action_prompt = nn.Parameter(torch.empty(1, 1, 1, patch_token_dim))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.encoder","title":"encoder  <code>instance-attribute</code>","text":"<pre><code>encoder = _SpatioTemporalTransformer(in_dim=patch_token_dim, model_dim=model_dim, out_dim=model_dim, num_blocks=enc_blocks, num_heads=num_heads)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.fc","title":"fc  <code>instance-attribute</code>","text":"<pre><code>fc = nn.Linear(model_dim, latent_dim * 2)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.__init__","title":"__init__","text":"<pre><code>__init__(*, in_dim: int = 3, model_dim: int = 1024, latent_dim: int = LATENT_ACTION_DIM, patch_size: int = 16, enc_blocks: int = 24, num_heads: int = 16) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def __init__(\n    self,\n    *,\n    in_dim: int = 3,\n    model_dim: int = 1024,\n    latent_dim: int = LATENT_ACTION_DIM,\n    patch_size: int = 16,\n    enc_blocks: int = 24,\n    num_heads: int = 16,\n) -&gt; None:\n    super().__init__()\n    self.patch_size = patch_size\n    self.model_dim = model_dim\n    self.latent_dim = latent_dim\n    patch_token_dim = in_dim * patch_size**2\n    self.action_prompt = nn.Parameter(torch.empty(1, 1, 1, patch_token_dim))\n    self.encoder = _SpatioTemporalTransformer(\n        in_dim=patch_token_dim,\n        model_dim=model_dim,\n        out_dim=model_dim,\n        num_blocks=enc_blocks,\n        num_heads=num_heads,\n    )\n    self.fc = nn.Linear(model_dim, latent_dim * 2)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.from_checkpoint","title":"from_checkpoint  <code>classmethod</code>","text":"<pre><code>from_checkpoint(ckpt_path: str, device: 'str | torch.device' = 'cuda') -&gt; 'LatentActionEncoder'\n</code></pre> <p>Load the encoder subset of a LAM Lightning checkpoint (<code>lam.</code> prefix).</p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>@classmethod\ndef from_checkpoint(\n    cls, ckpt_path: str, device: \"str | torch.device\" = \"cuda\"\n) -&gt; \"LatentActionEncoder\":\n    r\"\"\"Load the encoder subset of a LAM Lightning checkpoint (``lam.`` prefix).\"\"\"\n    encoder = cls()\n    state = torch.load(ckpt_path, map_location=\"cpu\", weights_only=False)\n    state = state.get(\"state_dict\", state)\n    own = encoder.state_dict()\n    subset = {\n        k.removeprefix(\"lam.\"): v for k, v in state.items() if k.removeprefix(\"lam.\") in own\n    }\n    missing = set(own) - set(subset)\n    assert not missing, f\"LAM checkpoint missing encoder keys: {sorted(missing)[:5]}\"\n    encoder.load_state_dict(subset, strict=True)\n    return encoder.to(device).eval().requires_grad_(False)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.encode_pairs","title":"encode_pairs","text":"<pre><code>encode_pairs(frame_pairs: Tensor) -&gt; Tensor\n</code></pre> <p>Posterior means for <code>(P, 2, H, W, 3)</code> frame pairs in \\([0, 1]\\) \u2192 <code>(P, 32)</code>.</p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def encode_pairs(self, frame_pairs: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Posterior means for ``(P, 2, H, W, 3)`` frame pairs in \\([0, 1]\\) \u2192 ``(P, 32)``.\"\"\"\n    P, T, H, W, _ = frame_pairs.shape\n    assert T == 2\n    size = self.patch_size\n    frame_pairs = frame_pairs[:, :, : H - (H % size), : W - (W % size), :]\n    patches = rearrange(\n        frame_pairs, \"b t (hn hp) (wn wp) c -&gt; b t (hn wn) (hp wp c)\", hp=size, wp=size\n    )\n    padded = torch.cat([self.action_prompt.expand(P, T, -1, -1), patches], dim=2)\n    z = self.encoder(padded)[:, 1:, 0].reshape(P, self.model_dim)\n    z_mu, _ = torch.chunk(self.fc(z), 2, dim=1)\n    return z_mu\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam.LatentActionEncoder.encode_video","title":"encode_video","text":"<pre><code>encode_video(frames_uint8: Tensor) -&gt; Tensor\n</code></pre> <p>Latent actions for consecutive pairs of <code>(T, 3, H, W)</code> uint8 frames \u2192 <code>(T-1, 32)</code>.</p> <p>Frames are bilinearly resized to the LAM's native 240x320 as in the reference data pipeline.</p> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def encode_video(self, frames_uint8: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Latent actions for consecutive pairs of ``(T, 3, H, W)`` uint8 frames \u2192 ``(T-1, 32)``.\n\n    Frames are bilinearly resized to the LAM's native 240x320 as in the\n    reference data pipeline.\n    \"\"\"\n    import torch.nn.functional as F\n\n    frames = frames_uint8.float() / 255.0\n    if frames.shape[-2:] != (LAM_INPUT_HEIGHT, LAM_INPUT_WIDTH):\n        frames = F.interpolate(\n            frames, size=(LAM_INPUT_HEIGHT, LAM_INPUT_WIDTH), mode=\"bilinear\"\n        )\n    frames = frames.permute(0, 2, 3, 1)\n    pairs = torch.stack([frames[:-1], frames[1:]], dim=1)\n    device = next(self.parameters()).device\n    with torch.no_grad():\n        return self.encode_pairs(pairs.to(device))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/lam/#worldkernels.models.dreamdojo.lam._rotate_interleaved","title":"_rotate_interleaved","text":"<pre><code>_rotate_interleaved(x: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/lam.py</code> <pre><code>def _rotate_interleaved(x: torch.Tensor) -&gt; torch.Tensor:\n    x = rearrange(x, \"... (d r) -&gt; ... d r\", r=2)\n    x1, x2 = x.unbind(dim=-1)\n    return rearrange(torch.stack((-x2, x1), dim=-1), \"... d r -&gt; ... (d r)\")\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/","title":"net","text":""},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net","title":"worldkernels.models.dreamdojo.net","text":"<p>Action-conditioned video DiT for DreamDojo (teacher clip + causal student paths).</p>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ActionEmbedder', 'ActionVideoDiT', 'NET_2B', 'NET_14B']\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.NET_2B","title":"NET_2B  <code>module-attribute</code>","text":"<pre><code>NET_2B: dict[str, Any] = dict(max_img_h=240, max_img_w=240, max_frames=128, in_channels=16, out_channels=16, patch_spatial=2, patch_temporal=1, concat_padding_mask=True, model_channels=2048, num_blocks=28, num_heads=16, use_adaln_lora=True, adaln_lora_dim=256, rope_h_extrapolation_ratio=3.0, rope_w_extrapolation_ratio=3.0, rope_t_extrapolation_ratio=1.0, use_wan_fp32_strategy=True, use_crossattn_projection=True, crossattn_proj_in_channels=100352, crossattn_emb_channels=1024)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.NET_14B","title":"NET_14B  <code>module-attribute</code>","text":"<pre><code>NET_14B: dict[str, Any] = {None: NET_2B, 'model_channels': 5120, 'num_heads': 40, 'num_blocks': 36}\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionEmbedder","title":"ActionEmbedder","text":"<p>               Bases: <code>Module</code></p> <p>Two-layer GELU(tanh) MLP projecting flattened per-frame action chunks.</p> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>class ActionEmbedder(nn.Module):\n    r\"\"\"Two-layer GELU(tanh) MLP projecting flattened per-frame action chunks.\"\"\"\n\n    def __init__(self, in_features: int, hidden_features: int, out_features: int) -&gt; None:\n        super().__init__()\n        self.fc1 = nn.Linear(in_features, hidden_features)\n        self.activation = nn.GELU(approximate=\"tanh\")\n        self.fc2 = nn.Linear(hidden_features, out_features)\n\n    def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n        return self.fc2(self.activation(self.fc1(x)))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionEmbedder.fc1","title":"fc1  <code>instance-attribute</code>","text":"<pre><code>fc1 = nn.Linear(in_features, hidden_features)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionEmbedder.activation","title":"activation  <code>instance-attribute</code>","text":"<pre><code>activation = nn.GELU(approximate='tanh')\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionEmbedder.fc2","title":"fc2  <code>instance-attribute</code>","text":"<pre><code>fc2 = nn.Linear(hidden_features, out_features)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionEmbedder.__init__","title":"__init__","text":"<pre><code>__init__(in_features: int, hidden_features: int, out_features: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>def __init__(self, in_features: int, hidden_features: int, out_features: int) -&gt; None:\n    super().__init__()\n    self.fc1 = nn.Linear(in_features, hidden_features)\n    self.activation = nn.GELU(approximate=\"tanh\")\n    self.fc2 = nn.Linear(hidden_features, out_features)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionEmbedder.forward","title":"forward","text":"<pre><code>forward(x: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>def forward(self, x: torch.Tensor) -&gt; torch.Tensor:\n    return self.fc2(self.activation(self.fc1(x)))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT","title":"ActionVideoDiT","text":"<p>               Bases: <code>VideoDiT</code></p> <p>DreamDojo DiT: per-latent-frame action chunks injected into the adaLN stream.</p> <p>Actions arrive as <code>(B, T_a, action_dim)</code> with <code>T_a = num_action_per_latent_frame * generated_frames</code>; each latent frame's chunk is flattened and projected by two MLPs added to the timestep embedding and the adaLN-LoRA stream (zero contribution for the conditioning frame). An extra input channel carries the condition-video mask. <code>forward</code> runs the bidirectional teacher clip; <code>forward_frame</code> runs one latent frame causally against a per-block <code>FrameKVCache</code> with absolute-position RoPE.</p> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>class ActionVideoDiT(VideoDiT):\n    r\"\"\"DreamDojo DiT: per-latent-frame action chunks injected into the adaLN stream.\n\n    Actions arrive as ``(B, T_a, action_dim)`` with\n    ``T_a = num_action_per_latent_frame * generated_frames``; each latent\n    frame's chunk is flattened and projected by two MLPs added to the timestep\n    embedding and the adaLN-LoRA stream (zero contribution for the conditioning\n    frame). An extra input channel carries the condition-video mask. `forward`\n    runs the bidirectional teacher clip; `forward_frame` runs one latent frame\n    causally against a per-block `FrameKVCache` with absolute-position RoPE.\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        action_dim: int = 384,\n        num_action_per_latent_frame: int = 4,\n        hidden_dim_in_action_embedder: int | None = None,\n        timestep_scale: float = 0.001,\n        in_channels: int = 16,\n        **kwargs: Any,\n    ) -&gt; None:\n        super().__init__(in_channels=in_channels + 1, **kwargs)\n        self.action_dim = action_dim\n        self.num_action_per_latent_frame = num_action_per_latent_frame\n        self.timestep_scale = timestep_scale\n        hidden = hidden_dim_in_action_embedder or self.model_channels * 4\n        self.action_embedder_B_D = ActionEmbedder(\n            action_dim * num_action_per_latent_frame, hidden, self.model_channels\n        )\n        self.action_embedder_B_3D = ActionEmbedder(\n            action_dim * num_action_per_latent_frame, hidden, 3 * self.model_channels\n        )\n\n    def _embed_action_chunks(self, action: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor]:\n        r\"\"\"Project per-latent-frame action chunks, zero-padded for the conditioning frame.\"\"\"\n        num_actions = action.shape[1]\n        action = rearrange(\n            action, \"b (t a) d -&gt; b t (a d)\", t=num_actions // self.num_action_per_latent_frame\n        )\n        action_emb_B_D = self.action_embedder_B_D(action)\n        action_emb_B_3D = self.action_embedder_B_3D(action)\n        zero_pad_B_D = torch.zeros_like(action_emb_B_D[:, :1, :])\n        zero_pad_B_3D = torch.zeros_like(action_emb_B_3D[:, :1, :])\n        return (\n            torch.cat([zero_pad_B_D, action_emb_B_D], dim=1),\n            torch.cat([zero_pad_B_3D, action_emb_B_3D], dim=1),\n        )\n\n    def forward(  # type: ignore[override]\n        self,\n        x_B_C_T_H_W: torch.Tensor,\n        timesteps_B_T: torch.Tensor,\n        crossattn_emb: torch.Tensor,\n        condition_video_input_mask_B_C_T_H_W: torch.Tensor | None = None,\n        action: torch.Tensor | None = None,\n        padding_mask: torch.Tensor | None = None,\n    ) -&gt; torch.Tensor:\n        assert condition_video_input_mask_B_C_T_H_W is not None\n        assert action is not None\n        x_B_C_T_H_W = torch.cat(\n            [x_B_C_T_H_W, condition_video_input_mask_B_C_T_H_W.type_as(x_B_C_T_H_W)], dim=1\n        )\n        timesteps_B_T = timesteps_B_T * self.timestep_scale\n        action_emb_B_D, action_emb_B_3D = self._embed_action_chunks(action)\n\n        x_B_T_H_W_D, rope_emb_L_1_1_D = self.prepare_embedded_sequence(\n            x_B_C_T_H_W, padding_mask=padding_mask\n        )\n        crossattn_emb = self.project_context(crossattn_emb)\n\n        with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n            if timesteps_B_T.ndim == 1:\n                timesteps_B_T = timesteps_B_T.unsqueeze(1)\n            t_embedding_B_T_D, adaln_lora_B_T_3D = self.t_embedder(timesteps_B_T)\n            t_embedding_B_T_D = t_embedding_B_T_D + action_emb_B_D\n            adaln_lora_B_T_3D = adaln_lora_B_T_3D + action_emb_B_3D\n            t_embedding_B_T_D = self.t_embedding_norm(t_embedding_B_T_D)\n\n        for block in self.blocks:\n            x_B_T_H_W_D = block(\n                x_B_T_H_W_D,\n                t_embedding_B_T_D,\n                crossattn_emb,\n                rope_emb_L_1_1_D=rope_emb_L_1_1_D,\n                adaln_lora_B_T_3D=adaln_lora_B_T_3D,\n            )\n\n        x_B_T_H_W_O = self.final_layer(\n            x_B_T_H_W_D, t_embedding_B_T_D, adaln_lora_B_T_3D=adaln_lora_B_T_3D\n        )\n        return self.unpatchify(x_B_T_H_W_O)\n\n    def forward_frame(\n        self,\n        x_B_C_1_H_W: torch.Tensor,\n        frame_idx: int,\n        timesteps_B_1: torch.Tensor,\n        crossattn_emb: torch.Tensor,\n        condition_video_input_mask_B_C_1_H_W: torch.Tensor,\n        action: torch.Tensor | None = None,\n        kv_caches: list[FrameKVCache] | None = None,\n        store_kv: bool = False,\n        padding_mask: torch.Tensor | None = None,\n    ) -&gt; torch.Tensor:\n        r\"\"\"Denoise one latent frame at absolute ``frame_idx`` against cached context.\n\n        Args:\n            action: The single frame's action chunk\n                ``(B, num_action_per_latent_frame, action_dim)``; ``None`` for\n                the conditioning frame (zero action contribution).\n            kv_caches: One `FrameKVCache` per block; read under a rolling\n                window, written only when ``store_kv`` is set.\n        \"\"\"\n        x_B_C_1_H_W = torch.cat(\n            [x_B_C_1_H_W, condition_video_input_mask_B_C_1_H_W.type_as(x_B_C_1_H_W)], dim=1\n        )\n        timesteps_B_1 = timesteps_B_1 * self.timestep_scale\n\n        with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n            if timesteps_B_1.ndim == 1:\n                timesteps_B_1 = timesteps_B_1.unsqueeze(1)\n            t_embedding_B_T_D, adaln_lora_B_T_3D = self.t_embedder(timesteps_B_1)\n            if action is not None:\n                action_flat = rearrange(action, \"b t d -&gt; b 1 (t d)\")\n                t_embedding_B_T_D = t_embedding_B_T_D + self.action_embedder_B_D(action_flat)\n                adaln_lora_B_T_3D = adaln_lora_B_T_3D + self.action_embedder_B_3D(action_flat)\n            t_embedding_B_T_D = self.t_embedding_norm(t_embedding_B_T_D)\n            if self.use_wan_fp32_strategy:\n                t_embedding_B_T_D = t_embedding_B_T_D.float()\n\n        x_B_T_H_W_D, _ = self.prepare_embedded_sequence(x_B_C_1_H_W, padding_mask=padding_mask)\n        _, T, H, W, _ = x_B_T_H_W_D.shape\n        assert T == 1, \"forward_frame expects a single latent frame\"\n        crossattn_emb = self.project_context(crossattn_emb)\n        rope_emb_L_1_1_D = self.pos_embedder.forward_frame(frame_idx, H, W)\n\n        for i, block in enumerate(self.blocks):\n            x_B_T_H_W_D = block(\n                x_B_T_H_W_D,\n                t_embedding_B_T_D,\n                crossattn_emb,\n                rope_emb_L_1_1_D=rope_emb_L_1_1_D,\n                adaln_lora_B_T_3D=adaln_lora_B_T_3D,\n                kv_cache=kv_caches[i] if kv_caches is not None else None,\n                frame_idx=frame_idx,\n                store_kv=store_kv,\n            )\n\n        x_B_T_H_W_O = self.final_layer(\n            x_B_T_H_W_D, t_embedding_B_T_D, adaln_lora_B_T_3D=adaln_lora_B_T_3D\n        )\n        return self.unpatchify(x_B_T_H_W_O)\n\n    def allocate_kv_caches(\n        self,\n        *,\n        max_frames: int,\n        batch_size: int,\n        latent_h: int,\n        latent_w: int,\n        window: int | None,\n        device: torch.device | str,\n        dtype: torch.dtype,\n    ) -&gt; list[FrameKVCache]:\n        r\"\"\"One rolling KV cache per block sized for ``latent_h x latent_w`` frames.\"\"\"\n        tokens = (latent_h // self.patch_spatial) * (latent_w // self.patch_spatial)\n        head_dim = self.model_channels // self.num_heads\n        return [\n            FrameKVCache.allocate(\n                max_frames=max_frames,\n                batch_size=batch_size,\n                tokens_per_frame=tokens,\n                num_heads=self.num_heads,\n                head_dim=head_dim,\n                device=device,\n                dtype=dtype,\n                window=window,\n            )\n            for _ in range(len(self.blocks))\n        ]\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.action_dim","title":"action_dim  <code>instance-attribute</code>","text":"<pre><code>action_dim = action_dim\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.num_action_per_latent_frame","title":"num_action_per_latent_frame  <code>instance-attribute</code>","text":"<pre><code>num_action_per_latent_frame = num_action_per_latent_frame\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.timestep_scale","title":"timestep_scale  <code>instance-attribute</code>","text":"<pre><code>timestep_scale = timestep_scale\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.action_embedder_B_D","title":"action_embedder_B_D  <code>instance-attribute</code>","text":"<pre><code>action_embedder_B_D = ActionEmbedder(action_dim * num_action_per_latent_frame, hidden, self.model_channels)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.action_embedder_B_3D","title":"action_embedder_B_3D  <code>instance-attribute</code>","text":"<pre><code>action_embedder_B_3D = ActionEmbedder(action_dim * num_action_per_latent_frame, hidden, 3 * self.model_channels)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.in_channels","title":"in_channels  <code>instance-attribute</code>","text":"<pre><code>in_channels = in_channels\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.out_channels","title":"out_channels  <code>instance-attribute</code>","text":"<pre><code>out_channels = out_channels\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.patch_spatial","title":"patch_spatial  <code>instance-attribute</code>","text":"<pre><code>patch_spatial = patch_spatial\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.patch_temporal","title":"patch_temporal  <code>instance-attribute</code>","text":"<pre><code>patch_temporal = patch_temporal\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.model_channels","title":"model_channels  <code>instance-attribute</code>","text":"<pre><code>model_channels = model_channels\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.num_heads","title":"num_heads  <code>instance-attribute</code>","text":"<pre><code>num_heads = num_heads\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.concat_padding_mask","title":"concat_padding_mask  <code>instance-attribute</code>","text":"<pre><code>concat_padding_mask = concat_padding_mask\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.use_adaln_lora","title":"use_adaln_lora  <code>instance-attribute</code>","text":"<pre><code>use_adaln_lora = use_adaln_lora\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.use_crossattn_projection","title":"use_crossattn_projection  <code>instance-attribute</code>","text":"<pre><code>use_crossattn_projection = use_crossattn_projection\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.use_wan_fp32_strategy","title":"use_wan_fp32_strategy  <code>instance-attribute</code>","text":"<pre><code>use_wan_fp32_strategy = use_wan_fp32_strategy\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.x_embedder","title":"x_embedder  <code>instance-attribute</code>","text":"<pre><code>x_embedder = PatchEmbed(spatial_patch_size=patch_spatial, temporal_patch_size=patch_temporal, in_channels=in_channels + 1 if concat_padding_mask else in_channels, out_channels=model_channels)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.pos_embedder","title":"pos_embedder  <code>instance-attribute</code>","text":"<pre><code>pos_embedder = VideoRoPE3D(head_dim=model_channels // num_heads, len_h=max_img_h // patch_spatial, len_w=max_img_w // patch_spatial, len_t=max_frames // patch_temporal, h_extrapolation_ratio=rope_h_extrapolation_ratio, w_extrapolation_ratio=rope_w_extrapolation_ratio, t_extrapolation_ratio=rope_t_extrapolation_ratio)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.t_embedder","title":"t_embedder  <code>instance-attribute</code>","text":"<pre><code>t_embedder = nn.Sequential(SinusoidalTimesteps(model_channels), TimestepEmbedding(model_channels, model_channels, use_adaln_lora=use_adaln_lora))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.t_embedding_norm","title":"t_embedding_norm  <code>instance-attribute</code>","text":"<pre><code>t_embedding_norm = RMSNorm(model_channels, eps=1e-06)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.blocks","title":"blocks  <code>instance-attribute</code>","text":"<pre><code>blocks = nn.ModuleList((DiTBlock(x_dim=model_channels, context_dim=crossattn_emb_channels, num_heads=num_heads, mlp_ratio=mlp_ratio, use_adaln_lora=use_adaln_lora, adaln_lora_dim=adaln_lora_dim, use_wan_fp32_strategy=use_wan_fp32_strategy)) for _ in (range(num_blocks)))\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.final_layer","title":"final_layer  <code>instance-attribute</code>","text":"<pre><code>final_layer = FinalLayer(hidden_size=model_channels, spatial_patch_size=patch_spatial, temporal_patch_size=patch_temporal, out_channels=out_channels, use_adaln_lora=use_adaln_lora, adaln_lora_dim=adaln_lora_dim, use_wan_fp32_strategy=use_wan_fp32_strategy)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.crossattn_proj","title":"crossattn_proj  <code>instance-attribute</code>","text":"<pre><code>crossattn_proj = nn.Sequential(nn.Linear(crossattn_proj_in_channels, crossattn_emb_channels, bias=True), nn.GELU())\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.__init__","title":"__init__","text":"<pre><code>__init__(*, action_dim: int = 384, num_action_per_latent_frame: int = 4, hidden_dim_in_action_embedder: int | None = None, timestep_scale: float = 0.001, in_channels: int = 16, **kwargs: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>def __init__(\n    self,\n    *,\n    action_dim: int = 384,\n    num_action_per_latent_frame: int = 4,\n    hidden_dim_in_action_embedder: int | None = None,\n    timestep_scale: float = 0.001,\n    in_channels: int = 16,\n    **kwargs: Any,\n) -&gt; None:\n    super().__init__(in_channels=in_channels + 1, **kwargs)\n    self.action_dim = action_dim\n    self.num_action_per_latent_frame = num_action_per_latent_frame\n    self.timestep_scale = timestep_scale\n    hidden = hidden_dim_in_action_embedder or self.model_channels * 4\n    self.action_embedder_B_D = ActionEmbedder(\n        action_dim * num_action_per_latent_frame, hidden, self.model_channels\n    )\n    self.action_embedder_B_3D = ActionEmbedder(\n        action_dim * num_action_per_latent_frame, hidden, 3 * self.model_channels\n    )\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT._embed_action_chunks","title":"_embed_action_chunks","text":"<pre><code>_embed_action_chunks(action: Tensor) -&gt; tuple[Tensor, Tensor]\n</code></pre> <p>Project per-latent-frame action chunks, zero-padded for the conditioning frame.</p> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>def _embed_action_chunks(self, action: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor]:\n    r\"\"\"Project per-latent-frame action chunks, zero-padded for the conditioning frame.\"\"\"\n    num_actions = action.shape[1]\n    action = rearrange(\n        action, \"b (t a) d -&gt; b t (a d)\", t=num_actions // self.num_action_per_latent_frame\n    )\n    action_emb_B_D = self.action_embedder_B_D(action)\n    action_emb_B_3D = self.action_embedder_B_3D(action)\n    zero_pad_B_D = torch.zeros_like(action_emb_B_D[:, :1, :])\n    zero_pad_B_3D = torch.zeros_like(action_emb_B_3D[:, :1, :])\n    return (\n        torch.cat([zero_pad_B_D, action_emb_B_D], dim=1),\n        torch.cat([zero_pad_B_3D, action_emb_B_3D], dim=1),\n    )\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.forward","title":"forward","text":"<pre><code>forward(x_B_C_T_H_W: Tensor, timesteps_B_T: Tensor, crossattn_emb: Tensor, condition_video_input_mask_B_C_T_H_W: Tensor | None = None, action: Tensor | None = None, padding_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>def forward(  # type: ignore[override]\n    self,\n    x_B_C_T_H_W: torch.Tensor,\n    timesteps_B_T: torch.Tensor,\n    crossattn_emb: torch.Tensor,\n    condition_video_input_mask_B_C_T_H_W: torch.Tensor | None = None,\n    action: torch.Tensor | None = None,\n    padding_mask: torch.Tensor | None = None,\n) -&gt; torch.Tensor:\n    assert condition_video_input_mask_B_C_T_H_W is not None\n    assert action is not None\n    x_B_C_T_H_W = torch.cat(\n        [x_B_C_T_H_W, condition_video_input_mask_B_C_T_H_W.type_as(x_B_C_T_H_W)], dim=1\n    )\n    timesteps_B_T = timesteps_B_T * self.timestep_scale\n    action_emb_B_D, action_emb_B_3D = self._embed_action_chunks(action)\n\n    x_B_T_H_W_D, rope_emb_L_1_1_D = self.prepare_embedded_sequence(\n        x_B_C_T_H_W, padding_mask=padding_mask\n    )\n    crossattn_emb = self.project_context(crossattn_emb)\n\n    with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n        if timesteps_B_T.ndim == 1:\n            timesteps_B_T = timesteps_B_T.unsqueeze(1)\n        t_embedding_B_T_D, adaln_lora_B_T_3D = self.t_embedder(timesteps_B_T)\n        t_embedding_B_T_D = t_embedding_B_T_D + action_emb_B_D\n        adaln_lora_B_T_3D = adaln_lora_B_T_3D + action_emb_B_3D\n        t_embedding_B_T_D = self.t_embedding_norm(t_embedding_B_T_D)\n\n    for block in self.blocks:\n        x_B_T_H_W_D = block(\n            x_B_T_H_W_D,\n            t_embedding_B_T_D,\n            crossattn_emb,\n            rope_emb_L_1_1_D=rope_emb_L_1_1_D,\n            adaln_lora_B_T_3D=adaln_lora_B_T_3D,\n        )\n\n    x_B_T_H_W_O = self.final_layer(\n        x_B_T_H_W_D, t_embedding_B_T_D, adaln_lora_B_T_3D=adaln_lora_B_T_3D\n    )\n    return self.unpatchify(x_B_T_H_W_O)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.forward_frame","title":"forward_frame","text":"<pre><code>forward_frame(x_B_C_1_H_W: Tensor, frame_idx: int, timesteps_B_1: Tensor, crossattn_emb: Tensor, condition_video_input_mask_B_C_1_H_W: Tensor, action: Tensor | None = None, kv_caches: list[FrameKVCache] | None = None, store_kv: bool = False, padding_mask: Tensor | None = None) -&gt; Tensor\n</code></pre> <p>Denoise one latent frame at absolute <code>frame_idx</code> against cached context.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Tensor | None</code> <p>The single frame's action chunk <code>(B, num_action_per_latent_frame, action_dim)</code>; <code>None</code> for the conditioning frame (zero action contribution).</p> <code>None</code> <code>kv_caches</code> <code>list[FrameKVCache] | None</code> <p>One <code>FrameKVCache</code> per block; read under a rolling window, written only when <code>store_kv</code> is set.</p> <code>None</code> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>def forward_frame(\n    self,\n    x_B_C_1_H_W: torch.Tensor,\n    frame_idx: int,\n    timesteps_B_1: torch.Tensor,\n    crossattn_emb: torch.Tensor,\n    condition_video_input_mask_B_C_1_H_W: torch.Tensor,\n    action: torch.Tensor | None = None,\n    kv_caches: list[FrameKVCache] | None = None,\n    store_kv: bool = False,\n    padding_mask: torch.Tensor | None = None,\n) -&gt; torch.Tensor:\n    r\"\"\"Denoise one latent frame at absolute ``frame_idx`` against cached context.\n\n    Args:\n        action: The single frame's action chunk\n            ``(B, num_action_per_latent_frame, action_dim)``; ``None`` for\n            the conditioning frame (zero action contribution).\n        kv_caches: One `FrameKVCache` per block; read under a rolling\n            window, written only when ``store_kv`` is set.\n    \"\"\"\n    x_B_C_1_H_W = torch.cat(\n        [x_B_C_1_H_W, condition_video_input_mask_B_C_1_H_W.type_as(x_B_C_1_H_W)], dim=1\n    )\n    timesteps_B_1 = timesteps_B_1 * self.timestep_scale\n\n    with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n        if timesteps_B_1.ndim == 1:\n            timesteps_B_1 = timesteps_B_1.unsqueeze(1)\n        t_embedding_B_T_D, adaln_lora_B_T_3D = self.t_embedder(timesteps_B_1)\n        if action is not None:\n            action_flat = rearrange(action, \"b t d -&gt; b 1 (t d)\")\n            t_embedding_B_T_D = t_embedding_B_T_D + self.action_embedder_B_D(action_flat)\n            adaln_lora_B_T_3D = adaln_lora_B_T_3D + self.action_embedder_B_3D(action_flat)\n        t_embedding_B_T_D = self.t_embedding_norm(t_embedding_B_T_D)\n        if self.use_wan_fp32_strategy:\n            t_embedding_B_T_D = t_embedding_B_T_D.float()\n\n    x_B_T_H_W_D, _ = self.prepare_embedded_sequence(x_B_C_1_H_W, padding_mask=padding_mask)\n    _, T, H, W, _ = x_B_T_H_W_D.shape\n    assert T == 1, \"forward_frame expects a single latent frame\"\n    crossattn_emb = self.project_context(crossattn_emb)\n    rope_emb_L_1_1_D = self.pos_embedder.forward_frame(frame_idx, H, W)\n\n    for i, block in enumerate(self.blocks):\n        x_B_T_H_W_D = block(\n            x_B_T_H_W_D,\n            t_embedding_B_T_D,\n            crossattn_emb,\n            rope_emb_L_1_1_D=rope_emb_L_1_1_D,\n            adaln_lora_B_T_3D=adaln_lora_B_T_3D,\n            kv_cache=kv_caches[i] if kv_caches is not None else None,\n            frame_idx=frame_idx,\n            store_kv=store_kv,\n        )\n\n    x_B_T_H_W_O = self.final_layer(\n        x_B_T_H_W_D, t_embedding_B_T_D, adaln_lora_B_T_3D=adaln_lora_B_T_3D\n    )\n    return self.unpatchify(x_B_T_H_W_O)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.allocate_kv_caches","title":"allocate_kv_caches","text":"<pre><code>allocate_kv_caches(*, max_frames: int, batch_size: int, latent_h: int, latent_w: int, window: int | None, device: device | str, dtype: dtype) -&gt; list[FrameKVCache]\n</code></pre> <p>One rolling KV cache per block sized for <code>latent_h x latent_w</code> frames.</p> Source code in <code>worldkernels/models/dreamdojo/net.py</code> <pre><code>def allocate_kv_caches(\n    self,\n    *,\n    max_frames: int,\n    batch_size: int,\n    latent_h: int,\n    latent_w: int,\n    window: int | None,\n    device: torch.device | str,\n    dtype: torch.dtype,\n) -&gt; list[FrameKVCache]:\n    r\"\"\"One rolling KV cache per block sized for ``latent_h x latent_w`` frames.\"\"\"\n    tokens = (latent_h // self.patch_spatial) * (latent_w // self.patch_spatial)\n    head_dim = self.model_channels // self.num_heads\n    return [\n        FrameKVCache.allocate(\n            max_frames=max_frames,\n            batch_size=batch_size,\n            tokens_per_frame=tokens,\n            num_heads=self.num_heads,\n            head_dim=head_dim,\n            device=device,\n            dtype=dtype,\n            window=window,\n        )\n        for _ in range(len(self.blocks))\n    ]\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.prepare_embedded_sequence","title":"prepare_embedded_sequence","text":"<pre><code>prepare_embedded_sequence(x_B_C_T_H_W: Tensor, padding_mask: Tensor | None = None) -&gt; tuple[Tensor, Tensor]\n</code></pre> <p>Patchify with the padding-mask channel appended; return tokens and RoPE table.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def prepare_embedded_sequence(\n    self,\n    x_B_C_T_H_W: torch.Tensor,\n    padding_mask: torch.Tensor | None = None,\n) -&gt; tuple[torch.Tensor, torch.Tensor]:\n    r\"\"\"Patchify with the padding-mask channel appended; return tokens and RoPE table.\"\"\"\n    if self.concat_padding_mask:\n        assert padding_mask is not None\n        if padding_mask.shape[-2:] != x_B_C_T_H_W.shape[-2:]:\n            padding_mask = F.interpolate(\n                padding_mask, size=x_B_C_T_H_W.shape[-2:], mode=\"nearest\"\n            )\n        x_B_C_T_H_W = torch.cat(\n            [\n                x_B_C_T_H_W,\n                padding_mask.unsqueeze(1).repeat(1, 1, x_B_C_T_H_W.shape[2], 1, 1),\n            ],\n            dim=1,\n        )\n    x_B_T_H_W_D = self.x_embedder(x_B_C_T_H_W)\n    _, T, H, W, _ = x_B_T_H_W_D.shape\n    rope_emb_L_1_1_D = self.pos_embedder(T, H, W)\n    return x_B_T_H_W_D, rope_emb_L_1_1_D\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.compute_time_embeddings","title":"compute_time_embeddings","text":"<pre><code>compute_time_embeddings(timesteps_B_T: Tensor) -&gt; tuple[Tensor, Tensor | None]\n</code></pre> <p>Timestep embedding and adaLN-LoRA stream, float32 under the Wan strategy.</p> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def compute_time_embeddings(\n    self, timesteps_B_T: torch.Tensor\n) -&gt; tuple[torch.Tensor, torch.Tensor | None]:\n    r\"\"\"Timestep embedding and adaLN-LoRA stream, float32 under the Wan strategy.\"\"\"\n    with amp.autocast(\"cuda\", enabled=self.use_wan_fp32_strategy, dtype=torch.float32):\n        if timesteps_B_T.ndim == 1:\n            timesteps_B_T = timesteps_B_T.unsqueeze(1)\n        t_embedding_B_T_D, adaln_lora_B_T_3D = self.t_embedder(timesteps_B_T)\n        t_embedding_B_T_D = self.t_embedding_norm(t_embedding_B_T_D)\n    return t_embedding_B_T_D, adaln_lora_B_T_3D\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.project_context","title":"project_context","text":"<pre><code>project_context(crossattn_emb: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def project_context(self, crossattn_emb: torch.Tensor) -&gt; torch.Tensor:\n    if self.use_crossattn_projection:\n        return self.crossattn_proj(crossattn_emb)\n    return crossattn_emb\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/net/#worldkernels.models.dreamdojo.net.ActionVideoDiT.unpatchify","title":"unpatchify","text":"<pre><code>unpatchify(x_B_T_H_W_M: Tensor) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/components/dit.py</code> <pre><code>def unpatchify(self, x_B_T_H_W_M: torch.Tensor) -&gt; torch.Tensor:\n    return rearrange(\n        x_B_T_H_W_M,\n        \"B T H W (p1 p2 t C) -&gt; B C (T t) (H p1) (W p2)\",\n        p1=self.patch_spatial,\n        p2=self.patch_spatial,\n        t=self.patch_temporal,\n    )\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/","title":"pipeline","text":""},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline","title":"worldkernels.models.dreamdojo.pipeline","text":"<p>Native DreamDojo inference pipeline: action-conditioned rectified-flow video2world.</p>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['DreamDojoPipeline', 'seeded_noise']\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.FRAMES_PER_CLIP","title":"FRAMES_PER_CLIP  <code>module-attribute</code>","text":"<pre><code>FRAMES_PER_CLIP = 13\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.NATIVE_HEIGHT","title":"NATIVE_HEIGHT  <code>module-attribute</code>","text":"<pre><code>NATIVE_HEIGHT = 480\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.NATIVE_WIDTH","title":"NATIVE_WIDTH  <code>module-attribute</code>","text":"<pre><code>NATIVE_WIDTH = 640\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline","title":"DreamDojoPipeline","text":"<p>One 13-frame clip per call: encode conditioning, denoise with UniPC, decode.</p> <p>The clip forward mirrors the reference exactly: frame 0 (or a longer pixel prefix) is the conditioning, the latent condition mask replaces both the solver input and the predicted velocity on conditioned frames, the fixed empty-prompt CR1 embedding is the cross-attention context, and sampling runs the owned flow-matching UniPC solver in float32 around a bf16 net.</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>class DreamDojoPipeline:\n    r\"\"\"One 13-frame clip per call: encode conditioning, denoise with UniPC, decode.\n\n    The clip forward mirrors the reference exactly: frame 0 (or a longer pixel\n    prefix) is the conditioning, the latent condition mask replaces both the\n    solver input and the predicted velocity on conditioned frames, the fixed\n    empty-prompt CR1 embedding is the cross-attention context, and sampling\n    runs the owned flow-matching UniPC solver in float32 around a bf16 net.\n    \"\"\"\n\n    def __init__(\n        self,\n        variant: str = \"2b_gr1\",\n        *,\n        num_steps: int = 35,\n        guidance: float = 0.0,\n        flow_shift: float = 5.0,\n    ) -&gt; None:\n        self.variant = variant\n        self.num_steps = num_steps\n        self.guidance = guidance\n        self.flow_shift = flow_shift\n        self.net: ActionVideoDiT | None = None\n        self.tokenizer: VideoTokenizer | None = None\n        self.text_emb: torch.Tensor | None = None\n        self.device: str | torch.device = \"cuda\"\n        self.dtype: torch.dtype | None = None\n\n    def load(\n        self,\n        device: \"str | torch.device\",\n        dtype: \"torch.dtype | None\" = None,\n        ckpt_path: str | None = None,\n    ) -&gt; \"DreamDojoPipeline\":\n        import torch\n\n        from worldkernels.models.dreamdojo.checkpoint import (\n            download_text_embedding,\n            load_net,\n        )\n        from worldkernels.models.dreamdojo.tokenizer import VideoTokenizer\n\n        dtype = dtype or torch.bfloat16\n        self.device = device\n        self.dtype = dtype\n        self.net = load_net(self.variant, ckpt_path=ckpt_path, device=device, dtype=dtype)\n        self.tokenizer = VideoTokenizer().load(device)\n\n        emb = torch.load(download_text_embedding(), map_location=\"cpu\", weights_only=True)\n        if isinstance(emb, (list, tuple)):\n            emb = emb[0]\n        if emb.dim() == 2:\n            emb = emb.unsqueeze(0)\n        self.text_emb = emb.to(device=device, dtype=dtype)\n        log.info(\n            \"DreamDojoPipeline(%s) loaded on %s (%.1f GB VRAM)\",\n            self.variant,\n            device,\n            torch.cuda.memory_allocated() / 1e9 if torch.cuda.is_available() else 0.0,\n        )\n        return self\n\n    @property\n    def num_action_per_latent_frame(self) -&gt; int:\n        assert self.net is not None\n        return self.net.num_action_per_latent_frame\n\n    def encode_frames(self, frames_uint8: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        r\"\"\"Encode ``(B, 3, T, H, W)`` uint8 frames to normalized latents (bf16).\"\"\"\n\n        assert self.tokenizer is not None and self.dtype is not None\n        video = frames_uint8.to(device=self.device, dtype=self.dtype) / 127.5 - 1.0\n        return self.tokenizer.encode(video).to(self.dtype)\n\n    def decode_latent(self, latent: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        r\"\"\"Decode latents to video in \\([-1, 1]\\) float32.\"\"\"\n        assert self.tokenizer is not None\n        return self.tokenizer.decode(latent.to(self.dtype)).float()\n\n    @staticmethod\n    def to_uint8(video: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        import torch\n\n        return ((video.float() + 1.0) * 0.5).clamp(0.0, 1.0).mul(255.0).to(torch.uint8)\n\n    def generate_clip(\n        self,\n        cond_frames_uint8: \"torch.Tensor\",\n        action: \"torch.Tensor\",\n        *,\n        num_steps: int | None = None,\n        guidance: float | None = None,\n        seed: int = 1,\n    ) -&gt; tuple[\"torch.Tensor\", \"torch.Tensor\"]:\n        r\"\"\"Generate one clip conditioned on a pixel prefix and a 12-action chunk.\n\n        Args:\n            cond_frames_uint8: ``(B, 3, T_c, H, W)`` uint8 conditioning prefix,\n                ``T_c = 4k + 1``.\n            action: ``(B, 12, action_dim)`` relative-action chunk.\n            seed: Noise seed (architecture-invariant generator).\n\n        Returns:\n            ``(latent, video)``: final latents ``(B, 16, 4, H/8, W/8)`` float32\n            and decoded video ``(B, 3, 13, H, W)`` in \\([-1, 1]\\) float32.\n        \"\"\"\n        import torch\n\n        from worldkernels.models.schedulers.flow_unipc import FlowUniPCMultistepScheduler\n\n        assert self.net is not None and self.dtype is not None and self.text_emb is not None\n        num_steps = num_steps if num_steps is not None else self.num_steps\n        guidance = guidance if guidance is not None else self.guidance\n\n        B, _, t_cond, H, W = cond_frames_uint8.shape\n        video = torch.zeros(\n            B, 3, FRAMES_PER_CLIP, H, W, dtype=torch.uint8, device=cond_frames_uint8.device\n        )\n        video[:, :, :t_cond] = cond_frames_uint8\n        x0 = self.encode_frames(video)\n\n        _, C, t_lat, h, w = x0.shape\n        num_cond_latent = 1 + (t_cond - 1) // 4 if cond_frames_uint8.any() else 0\n        mask = torch.zeros(B, 1, t_lat, h, w, dtype=self.dtype, device=self.device)\n        mask[:, :, :num_cond_latent] = 1.0\n        mask_c = mask.repeat(1, C, 1, 1, 1)\n        padding_mask = torch.zeros(B, 1, h, w, dtype=self.dtype, device=self.device)\n        action = action.to(device=self.device, dtype=self.dtype)\n        text_emb = self.text_emb.expand(B, -1, -1)\n\n        noise = seeded_noise((B, C, t_lat, h, w), seed, self.device)\n        scheduler = FlowUniPCMultistepScheduler(num_train_timesteps=1000, shift=1.0)\n        scheduler.set_timesteps(num_steps, device=self.device, shift=self.flow_shift)\n\n        net = self.net\n        x0_f = x0.float()\n        mask_f = mask_c.float()\n        latents = noise\n\n        def denoise(xt: torch.Tensor, t: torch.Tensor, ctx: torch.Tensor) -&gt; torch.Tensor:\n            xt_in = x0_f * mask_f + xt * (1 - mask_f)\n            with torch.no_grad():\n                v = net(\n                    xt_in.to(self.dtype),\n                    t.expand(B, 1),\n                    ctx,\n                    condition_video_input_mask_B_C_T_H_W=mask,\n                    action=action,\n                    padding_mask=padding_mask,\n                ).float()\n            return (noise - x0_f) * mask_f + v * (1 - mask_f)\n\n        for t in scheduler.timesteps:\n            t_b = t.reshape(1, 1)\n            cond_v = denoise(latents, t_b, text_emb)\n            if guidance &gt; 0:\n                uncond_v = denoise(latents, t_b, torch.zeros_like(text_emb))\n                velocity = cond_v + guidance * (cond_v - uncond_v)\n            else:\n                velocity = cond_v\n            latents = scheduler.step(velocity, t, latents, return_dict=False)[0]\n\n        return latents, self.decode_latent(latents)\n\n    def generate_stream_latents(\n        self,\n        context_latents: \"torch.Tensor\",\n        window_actions: \"torch.Tensor\",\n        noise: \"torch.Tensor\",\n        *,\n        student_steps: int = 4,\n        window: int = 3,\n    ) -&gt; \"torch.Tensor\":\n        r\"\"\"Causal few-step generation over one latent window (distilled student).\n\n        Mirrors the reference Self-Forcing rollout: clean context frames are\n        prefilled into per-block KV caches with a single \\(t=0\\) forward each,\n        then every remaining frame runs ``student_steps`` TrigFlow denoise\n        passes (KV read-only) against the rolling window followed by one clean\n        forward that stores its K/V. Actions are absolute per latent\n        transition: frame \\(f\\) consumes ``window_actions[(f-1)A:(f)A]``.\n\n        Args:\n            context_latents: ``(B, 16, S, h, w)`` clean context, ``S &gt;= 1``.\n            window_actions: ``(B, (T-1)*A, action_dim)`` transitions for the\n                whole window.\n            noise: ``(B, 16, T, h, w)`` unit noise; frames ``S..T-1`` are\n                generated.\n            window: Rolling KV window in latent frames.\n\n        Returns:\n            ``(B, 16, T, h, w)`` latents in the net dtype.\n        \"\"\"\n        import torch\n\n        from worldkernels.models.schedulers.trigflow import (\n            TRIGFLOW_TIMES_4STEP,\n            trigflow_scaling,\n            trigflow_step,\n        )\n\n        assert self.net is not None and self.dtype is not None and self.text_emb is not None\n        net = self.net\n        A = net.num_action_per_latent_frame\n        B, C, T, h, w = noise.shape\n        S = context_latents.shape[2]\n        assert 1 &lt;= student_steps &lt;= len(TRIGFLOW_TIMES_4STEP)\n        assert window_actions.shape[1] == (T - 1) * A\n\n        noise = noise.to(device=self.device, dtype=self.dtype)\n        actions = window_actions.to(device=self.device, dtype=self.dtype)\n        text_emb = self.text_emb.expand(B, -1, -1)\n        zero_mask = torch.zeros(B, 1, 1, h, w, dtype=self.dtype, device=self.device)\n        padding_mask = torch.zeros(B, 1, h, w, dtype=self.dtype, device=self.device)\n        t_zero = torch.zeros(B, 1, dtype=self.dtype, device=self.device)\n\n        output = torch.zeros(B, C, T, h, w, dtype=self.dtype, device=self.device)\n        output[:, :, :S] = context_latents.to(device=self.device, dtype=self.dtype)\n\n        caches = net.allocate_kv_caches(\n            max_frames=T,\n            batch_size=B,\n            latent_h=h,\n            latent_w=w,\n            window=window,\n            device=self.device,\n            dtype=self.dtype,\n        )\n\n        def frame_action(f: int) -&gt; torch.Tensor | None:\n            return actions[:, (f - 1) * A : f * A] if f &gt; 0 else None\n\n        def store_frame(latent_frame: torch.Tensor, f: int) -&gt; None:\n            with torch.no_grad():\n                net.forward_frame(\n                    latent_frame,\n                    f,\n                    t_zero,\n                    text_emb,\n                    condition_video_input_mask_B_C_1_H_W=zero_mask,\n                    action=frame_action(f),\n                    kv_caches=caches,\n                    store_kv=True,\n                    padding_mask=padding_mask,\n                )\n\n        for f in range(S):\n            store_frame(output[:, :, f : f + 1], f)\n\n        t_steps = TRIGFLOW_TIMES_4STEP\n        for f in range(S, T):\n            frame_noise = noise[:, :, f : f + 1]\n            frame_seq = frame_noise\n            x0_pred = frame_seq\n            for s_idx in range(student_steps):\n                t = torch.full((B, 1), t_steps[s_idx], dtype=self.dtype, device=self.device)\n                c_skip, c_out, c_in, c_noise = trigflow_scaling(t)\n                c_bc = c_in.view(B, 1, 1, 1, 1)\n                with torch.no_grad():\n                    net_out = net.forward_frame(\n                        (frame_seq * c_bc).to(self.dtype),\n                        f,\n                        c_noise,\n                        text_emb,\n                        condition_video_input_mask_B_C_1_H_W=zero_mask,\n                        action=frame_action(f),\n                        kv_caches=caches,\n                        store_kv=False,\n                        padding_mask=padding_mask,\n                    )\n                net_out = net_out.float().to(frame_seq.dtype)\n                x0_pred = (\n                    c_skip.view(B, 1, 1, 1, 1) * frame_seq + c_out.view(B, 1, 1, 1, 1) * net_out\n                )\n                if s_idx &lt; student_steps - 1:\n                    t_next = torch.tensor(t_steps[s_idx + 1], dtype=self.dtype, device=self.device)\n                    frame_seq = trigflow_step(x0_pred, frame_noise, t_next)\n            output[:, :, f : f + 1] = x0_pred\n            if f &lt; T - 1:\n                store_frame(x0_pred, f)\n\n        return output\n\n    def stream_chunk(\n        self,\n        cond_frames_uint8: \"torch.Tensor\",\n        window_actions: \"torch.Tensor\",\n        *,\n        student_steps: int = 4,\n        window: int = 3,\n        new_frames: int = 3,\n        seed: int = 1,\n    ) -&gt; tuple[\"torch.Tensor\", \"torch.Tensor\"]:\n        r\"\"\"One causal streaming chunk from a pixel-context prefix.\n\n        Encodes the prefix (``T_c = 4k+1`` pixels, ``k+1`` clean latent\n        frames), rolls the student forward ``new_frames`` latent frames, and\n        decodes. ``window_actions`` covers every transition in the window,\n        context and new: ``(B, (k + new_frames) * 4, action_dim)``.\n\n        Returns:\n            ``(latents, new_video)``: full-window latents and the newly\n            generated pixels ``(B, 3, new_frames*4, H, W)`` in \\([-1, 1]\\).\n        \"\"\"\n        import torch\n\n        B, _, t_cond, H, W = cond_frames_uint8.shape\n        start_idx = 1 + (t_cond - 1) // 4\n        T = start_idx + new_frames\n        total_px = (T - 1) * 4 + 1\n\n        video = torch.zeros(\n            B, 3, total_px, H, W, dtype=torch.uint8, device=cond_frames_uint8.device\n        )\n        video[:, :, :t_cond] = cond_frames_uint8\n        x0 = self.encode_frames(video)\n        context = x0[:, :, :start_idx]\n\n        noise = seeded_noise((B, 16, T, H // 8, W // 8), seed, self.device)\n        latents = self.generate_stream_latents(\n            context, window_actions, noise, student_steps=student_steps, window=window\n        )\n        decoded = self.decode_latent(latents)\n        return latents, decoded[:, :, (start_idx - 1) * 4 + 1 :]\n\n    def profile_vram(self, *, height: int, width: int, num_frames: int) -&gt; float:\n        r\"\"\"Rough VRAM estimate in MB for a session at the given geometry.\"\"\"\n        del num_frames\n        latent = 16 * 4 * (height // 8) * (width // 8) * 2\n        weights = 4.3e9 if self.variant.startswith(\"2b\") else 28e9\n        vae = 0.3e9\n        return (weights + vae + latent * 50) / 1e6\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.variant","title":"variant  <code>instance-attribute</code>","text":"<pre><code>variant = variant\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.num_steps","title":"num_steps  <code>instance-attribute</code>","text":"<pre><code>num_steps = num_steps\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.guidance","title":"guidance  <code>instance-attribute</code>","text":"<pre><code>guidance = guidance\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.flow_shift","title":"flow_shift  <code>instance-attribute</code>","text":"<pre><code>flow_shift = flow_shift\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.net","title":"net  <code>instance-attribute</code>","text":"<pre><code>net: ActionVideoDiT | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.tokenizer","title":"tokenizer  <code>instance-attribute</code>","text":"<pre><code>tokenizer: VideoTokenizer | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.text_emb","title":"text_emb  <code>instance-attribute</code>","text":"<pre><code>text_emb: Tensor | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str | device = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: dtype | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.num_action_per_latent_frame","title":"num_action_per_latent_frame  <code>property</code>","text":"<pre><code>num_action_per_latent_frame: int\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.__init__","title":"__init__","text":"<pre><code>__init__(variant: str = '2b_gr1', *, num_steps: int = 35, guidance: float = 0.0, flow_shift: float = 5.0) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def __init__(\n    self,\n    variant: str = \"2b_gr1\",\n    *,\n    num_steps: int = 35,\n    guidance: float = 0.0,\n    flow_shift: float = 5.0,\n) -&gt; None:\n    self.variant = variant\n    self.num_steps = num_steps\n    self.guidance = guidance\n    self.flow_shift = flow_shift\n    self.net: ActionVideoDiT | None = None\n    self.tokenizer: VideoTokenizer | None = None\n    self.text_emb: torch.Tensor | None = None\n    self.device: str | torch.device = \"cuda\"\n    self.dtype: torch.dtype | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.load","title":"load","text":"<pre><code>load(device: 'str | torch.device', dtype: 'torch.dtype | None' = None, ckpt_path: str | None = None) -&gt; 'DreamDojoPipeline'\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def load(\n    self,\n    device: \"str | torch.device\",\n    dtype: \"torch.dtype | None\" = None,\n    ckpt_path: str | None = None,\n) -&gt; \"DreamDojoPipeline\":\n    import torch\n\n    from worldkernels.models.dreamdojo.checkpoint import (\n        download_text_embedding,\n        load_net,\n    )\n    from worldkernels.models.dreamdojo.tokenizer import VideoTokenizer\n\n    dtype = dtype or torch.bfloat16\n    self.device = device\n    self.dtype = dtype\n    self.net = load_net(self.variant, ckpt_path=ckpt_path, device=device, dtype=dtype)\n    self.tokenizer = VideoTokenizer().load(device)\n\n    emb = torch.load(download_text_embedding(), map_location=\"cpu\", weights_only=True)\n    if isinstance(emb, (list, tuple)):\n        emb = emb[0]\n    if emb.dim() == 2:\n        emb = emb.unsqueeze(0)\n    self.text_emb = emb.to(device=device, dtype=dtype)\n    log.info(\n        \"DreamDojoPipeline(%s) loaded on %s (%.1f GB VRAM)\",\n        self.variant,\n        device,\n        torch.cuda.memory_allocated() / 1e9 if torch.cuda.is_available() else 0.0,\n    )\n    return self\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.encode_frames","title":"encode_frames","text":"<pre><code>encode_frames(frames_uint8: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> <p>Encode <code>(B, 3, T, H, W)</code> uint8 frames to normalized latents (bf16).</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def encode_frames(self, frames_uint8: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    r\"\"\"Encode ``(B, 3, T, H, W)`` uint8 frames to normalized latents (bf16).\"\"\"\n\n    assert self.tokenizer is not None and self.dtype is not None\n    video = frames_uint8.to(device=self.device, dtype=self.dtype) / 127.5 - 1.0\n    return self.tokenizer.encode(video).to(self.dtype)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.decode_latent","title":"decode_latent","text":"<pre><code>decode_latent(latent: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> <p>Decode latents to video in \\([-1, 1]\\) float32.</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def decode_latent(self, latent: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    r\"\"\"Decode latents to video in \\([-1, 1]\\) float32.\"\"\"\n    assert self.tokenizer is not None\n    return self.tokenizer.decode(latent.to(self.dtype)).float()\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.to_uint8","title":"to_uint8  <code>staticmethod</code>","text":"<pre><code>to_uint8(video: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>@staticmethod\ndef to_uint8(video: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    import torch\n\n    return ((video.float() + 1.0) * 0.5).clamp(0.0, 1.0).mul(255.0).to(torch.uint8)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.generate_clip","title":"generate_clip","text":"<pre><code>generate_clip(cond_frames_uint8: 'torch.Tensor', action: 'torch.Tensor', *, num_steps: int | None = None, guidance: float | None = None, seed: int = 1) -&gt; tuple['torch.Tensor', 'torch.Tensor']\n</code></pre> <p>Generate one clip conditioned on a pixel prefix and a 12-action chunk.</p> <p>Parameters:</p> Name Type Description Default <code>cond_frames_uint8</code> <code>'torch.Tensor'</code> <p><code>(B, 3, T_c, H, W)</code> uint8 conditioning prefix, <code>T_c = 4k + 1</code>.</p> required <code>action</code> <code>'torch.Tensor'</code> <p><code>(B, 12, action_dim)</code> relative-action chunk.</p> required <code>seed</code> <code>int</code> <p>Noise seed (architecture-invariant generator).</p> <code>1</code> <p>Returns:</p> Type Description <code>'torch.Tensor'</code> <p><code>(latent, video)</code>: final latents <code>(B, 16, 4, H/8, W/8)</code> float32</p> <code>'torch.Tensor'</code> <p>and decoded video <code>(B, 3, 13, H, W)</code> in \\([-1, 1]\\) float32.</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def generate_clip(\n    self,\n    cond_frames_uint8: \"torch.Tensor\",\n    action: \"torch.Tensor\",\n    *,\n    num_steps: int | None = None,\n    guidance: float | None = None,\n    seed: int = 1,\n) -&gt; tuple[\"torch.Tensor\", \"torch.Tensor\"]:\n    r\"\"\"Generate one clip conditioned on a pixel prefix and a 12-action chunk.\n\n    Args:\n        cond_frames_uint8: ``(B, 3, T_c, H, W)`` uint8 conditioning prefix,\n            ``T_c = 4k + 1``.\n        action: ``(B, 12, action_dim)`` relative-action chunk.\n        seed: Noise seed (architecture-invariant generator).\n\n    Returns:\n        ``(latent, video)``: final latents ``(B, 16, 4, H/8, W/8)`` float32\n        and decoded video ``(B, 3, 13, H, W)`` in \\([-1, 1]\\) float32.\n    \"\"\"\n    import torch\n\n    from worldkernels.models.schedulers.flow_unipc import FlowUniPCMultistepScheduler\n\n    assert self.net is not None and self.dtype is not None and self.text_emb is not None\n    num_steps = num_steps if num_steps is not None else self.num_steps\n    guidance = guidance if guidance is not None else self.guidance\n\n    B, _, t_cond, H, W = cond_frames_uint8.shape\n    video = torch.zeros(\n        B, 3, FRAMES_PER_CLIP, H, W, dtype=torch.uint8, device=cond_frames_uint8.device\n    )\n    video[:, :, :t_cond] = cond_frames_uint8\n    x0 = self.encode_frames(video)\n\n    _, C, t_lat, h, w = x0.shape\n    num_cond_latent = 1 + (t_cond - 1) // 4 if cond_frames_uint8.any() else 0\n    mask = torch.zeros(B, 1, t_lat, h, w, dtype=self.dtype, device=self.device)\n    mask[:, :, :num_cond_latent] = 1.0\n    mask_c = mask.repeat(1, C, 1, 1, 1)\n    padding_mask = torch.zeros(B, 1, h, w, dtype=self.dtype, device=self.device)\n    action = action.to(device=self.device, dtype=self.dtype)\n    text_emb = self.text_emb.expand(B, -1, -1)\n\n    noise = seeded_noise((B, C, t_lat, h, w), seed, self.device)\n    scheduler = FlowUniPCMultistepScheduler(num_train_timesteps=1000, shift=1.0)\n    scheduler.set_timesteps(num_steps, device=self.device, shift=self.flow_shift)\n\n    net = self.net\n    x0_f = x0.float()\n    mask_f = mask_c.float()\n    latents = noise\n\n    def denoise(xt: torch.Tensor, t: torch.Tensor, ctx: torch.Tensor) -&gt; torch.Tensor:\n        xt_in = x0_f * mask_f + xt * (1 - mask_f)\n        with torch.no_grad():\n            v = net(\n                xt_in.to(self.dtype),\n                t.expand(B, 1),\n                ctx,\n                condition_video_input_mask_B_C_T_H_W=mask,\n                action=action,\n                padding_mask=padding_mask,\n            ).float()\n        return (noise - x0_f) * mask_f + v * (1 - mask_f)\n\n    for t in scheduler.timesteps:\n        t_b = t.reshape(1, 1)\n        cond_v = denoise(latents, t_b, text_emb)\n        if guidance &gt; 0:\n            uncond_v = denoise(latents, t_b, torch.zeros_like(text_emb))\n            velocity = cond_v + guidance * (cond_v - uncond_v)\n        else:\n            velocity = cond_v\n        latents = scheduler.step(velocity, t, latents, return_dict=False)[0]\n\n    return latents, self.decode_latent(latents)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.generate_stream_latents","title":"generate_stream_latents","text":"<pre><code>generate_stream_latents(context_latents: 'torch.Tensor', window_actions: 'torch.Tensor', noise: 'torch.Tensor', *, student_steps: int = 4, window: int = 3) -&gt; 'torch.Tensor'\n</code></pre> <p>Causal few-step generation over one latent window (distilled student).</p> <p>Mirrors the reference Self-Forcing rollout: clean context frames are prefilled into per-block KV caches with a single \\(t=0\\) forward each, then every remaining frame runs <code>student_steps</code> TrigFlow denoise passes (KV read-only) against the rolling window followed by one clean forward that stores its K/V. Actions are absolute per latent transition: frame \\(f\\) consumes <code>window_actions[(f-1)A:(f)A]</code>.</p> <p>Parameters:</p> Name Type Description Default <code>context_latents</code> <code>'torch.Tensor'</code> <p><code>(B, 16, S, h, w)</code> clean context, <code>S &gt;= 1</code>.</p> required <code>window_actions</code> <code>'torch.Tensor'</code> <p><code>(B, (T-1)*A, action_dim)</code> transitions for the whole window.</p> required <code>noise</code> <code>'torch.Tensor'</code> <p><code>(B, 16, T, h, w)</code> unit noise; frames <code>S..T-1</code> are generated.</p> required <code>window</code> <code>int</code> <p>Rolling KV window in latent frames.</p> <code>3</code> <p>Returns:</p> Type Description <code>'torch.Tensor'</code> <p><code>(B, 16, T, h, w)</code> latents in the net dtype.</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def generate_stream_latents(\n    self,\n    context_latents: \"torch.Tensor\",\n    window_actions: \"torch.Tensor\",\n    noise: \"torch.Tensor\",\n    *,\n    student_steps: int = 4,\n    window: int = 3,\n) -&gt; \"torch.Tensor\":\n    r\"\"\"Causal few-step generation over one latent window (distilled student).\n\n    Mirrors the reference Self-Forcing rollout: clean context frames are\n    prefilled into per-block KV caches with a single \\(t=0\\) forward each,\n    then every remaining frame runs ``student_steps`` TrigFlow denoise\n    passes (KV read-only) against the rolling window followed by one clean\n    forward that stores its K/V. Actions are absolute per latent\n    transition: frame \\(f\\) consumes ``window_actions[(f-1)A:(f)A]``.\n\n    Args:\n        context_latents: ``(B, 16, S, h, w)`` clean context, ``S &gt;= 1``.\n        window_actions: ``(B, (T-1)*A, action_dim)`` transitions for the\n            whole window.\n        noise: ``(B, 16, T, h, w)`` unit noise; frames ``S..T-1`` are\n            generated.\n        window: Rolling KV window in latent frames.\n\n    Returns:\n        ``(B, 16, T, h, w)`` latents in the net dtype.\n    \"\"\"\n    import torch\n\n    from worldkernels.models.schedulers.trigflow import (\n        TRIGFLOW_TIMES_4STEP,\n        trigflow_scaling,\n        trigflow_step,\n    )\n\n    assert self.net is not None and self.dtype is not None and self.text_emb is not None\n    net = self.net\n    A = net.num_action_per_latent_frame\n    B, C, T, h, w = noise.shape\n    S = context_latents.shape[2]\n    assert 1 &lt;= student_steps &lt;= len(TRIGFLOW_TIMES_4STEP)\n    assert window_actions.shape[1] == (T - 1) * A\n\n    noise = noise.to(device=self.device, dtype=self.dtype)\n    actions = window_actions.to(device=self.device, dtype=self.dtype)\n    text_emb = self.text_emb.expand(B, -1, -1)\n    zero_mask = torch.zeros(B, 1, 1, h, w, dtype=self.dtype, device=self.device)\n    padding_mask = torch.zeros(B, 1, h, w, dtype=self.dtype, device=self.device)\n    t_zero = torch.zeros(B, 1, dtype=self.dtype, device=self.device)\n\n    output = torch.zeros(B, C, T, h, w, dtype=self.dtype, device=self.device)\n    output[:, :, :S] = context_latents.to(device=self.device, dtype=self.dtype)\n\n    caches = net.allocate_kv_caches(\n        max_frames=T,\n        batch_size=B,\n        latent_h=h,\n        latent_w=w,\n        window=window,\n        device=self.device,\n        dtype=self.dtype,\n    )\n\n    def frame_action(f: int) -&gt; torch.Tensor | None:\n        return actions[:, (f - 1) * A : f * A] if f &gt; 0 else None\n\n    def store_frame(latent_frame: torch.Tensor, f: int) -&gt; None:\n        with torch.no_grad():\n            net.forward_frame(\n                latent_frame,\n                f,\n                t_zero,\n                text_emb,\n                condition_video_input_mask_B_C_1_H_W=zero_mask,\n                action=frame_action(f),\n                kv_caches=caches,\n                store_kv=True,\n                padding_mask=padding_mask,\n            )\n\n    for f in range(S):\n        store_frame(output[:, :, f : f + 1], f)\n\n    t_steps = TRIGFLOW_TIMES_4STEP\n    for f in range(S, T):\n        frame_noise = noise[:, :, f : f + 1]\n        frame_seq = frame_noise\n        x0_pred = frame_seq\n        for s_idx in range(student_steps):\n            t = torch.full((B, 1), t_steps[s_idx], dtype=self.dtype, device=self.device)\n            c_skip, c_out, c_in, c_noise = trigflow_scaling(t)\n            c_bc = c_in.view(B, 1, 1, 1, 1)\n            with torch.no_grad():\n                net_out = net.forward_frame(\n                    (frame_seq * c_bc).to(self.dtype),\n                    f,\n                    c_noise,\n                    text_emb,\n                    condition_video_input_mask_B_C_1_H_W=zero_mask,\n                    action=frame_action(f),\n                    kv_caches=caches,\n                    store_kv=False,\n                    padding_mask=padding_mask,\n                )\n            net_out = net_out.float().to(frame_seq.dtype)\n            x0_pred = (\n                c_skip.view(B, 1, 1, 1, 1) * frame_seq + c_out.view(B, 1, 1, 1, 1) * net_out\n            )\n            if s_idx &lt; student_steps - 1:\n                t_next = torch.tensor(t_steps[s_idx + 1], dtype=self.dtype, device=self.device)\n                frame_seq = trigflow_step(x0_pred, frame_noise, t_next)\n        output[:, :, f : f + 1] = x0_pred\n        if f &lt; T - 1:\n            store_frame(x0_pred, f)\n\n    return output\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.stream_chunk","title":"stream_chunk","text":"<pre><code>stream_chunk(cond_frames_uint8: 'torch.Tensor', window_actions: 'torch.Tensor', *, student_steps: int = 4, window: int = 3, new_frames: int = 3, seed: int = 1) -&gt; tuple['torch.Tensor', 'torch.Tensor']\n</code></pre> <p>One causal streaming chunk from a pixel-context prefix.</p> <p>Encodes the prefix (<code>T_c = 4k+1</code> pixels, <code>k+1</code> clean latent frames), rolls the student forward <code>new_frames</code> latent frames, and decodes. <code>window_actions</code> covers every transition in the window, context and new: <code>(B, (k + new_frames) * 4, action_dim)</code>.</p> <p>Returns:</p> Type Description <code>'torch.Tensor'</code> <p><code>(latents, new_video)</code>: full-window latents and the newly</p> <code>'torch.Tensor'</code> <p>generated pixels <code>(B, 3, new_frames*4, H, W)</code> in \\([-1, 1]\\).</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def stream_chunk(\n    self,\n    cond_frames_uint8: \"torch.Tensor\",\n    window_actions: \"torch.Tensor\",\n    *,\n    student_steps: int = 4,\n    window: int = 3,\n    new_frames: int = 3,\n    seed: int = 1,\n) -&gt; tuple[\"torch.Tensor\", \"torch.Tensor\"]:\n    r\"\"\"One causal streaming chunk from a pixel-context prefix.\n\n    Encodes the prefix (``T_c = 4k+1`` pixels, ``k+1`` clean latent\n    frames), rolls the student forward ``new_frames`` latent frames, and\n    decodes. ``window_actions`` covers every transition in the window,\n    context and new: ``(B, (k + new_frames) * 4, action_dim)``.\n\n    Returns:\n        ``(latents, new_video)``: full-window latents and the newly\n        generated pixels ``(B, 3, new_frames*4, H, W)`` in \\([-1, 1]\\).\n    \"\"\"\n    import torch\n\n    B, _, t_cond, H, W = cond_frames_uint8.shape\n    start_idx = 1 + (t_cond - 1) // 4\n    T = start_idx + new_frames\n    total_px = (T - 1) * 4 + 1\n\n    video = torch.zeros(\n        B, 3, total_px, H, W, dtype=torch.uint8, device=cond_frames_uint8.device\n    )\n    video[:, :, :t_cond] = cond_frames_uint8\n    x0 = self.encode_frames(video)\n    context = x0[:, :, :start_idx]\n\n    noise = seeded_noise((B, 16, T, H // 8, W // 8), seed, self.device)\n    latents = self.generate_stream_latents(\n        context, window_actions, noise, student_steps=student_steps, window=window\n    )\n    decoded = self.decode_latent(latents)\n    return latents, decoded[:, :, (start_idx - 1) * 4 + 1 :]\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.DreamDojoPipeline.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(*, height: int, width: int, num_frames: int) -&gt; float\n</code></pre> <p>Rough VRAM estimate in MB for a session at the given geometry.</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def profile_vram(self, *, height: int, width: int, num_frames: int) -&gt; float:\n    r\"\"\"Rough VRAM estimate in MB for a session at the given geometry.\"\"\"\n    del num_frames\n    latent = 16 * 4 * (height // 8) * (width // 8) * 2\n    weights = 4.3e9 if self.variant.startswith(\"2b\") else 28e9\n    vae = 0.3e9\n    return (weights + vae + latent * 50) / 1e6\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/pipeline/#worldkernels.models.dreamdojo.pipeline.seeded_noise","title":"seeded_noise","text":"<pre><code>seeded_noise(shape: tuple[int, ...], seed: int, device: 'str | torch.device') -&gt; 'torch.Tensor'\n</code></pre> <p>Architecture-invariant unit Gaussian, identical to the reference generator.</p> Source code in <code>worldkernels/models/dreamdojo/pipeline.py</code> <pre><code>def seeded_noise(shape: tuple[int, ...], seed: int, device: \"str | torch.device\") -&gt; \"torch.Tensor\":\n    r\"\"\"Architecture-invariant unit Gaussian, identical to the reference generator.\"\"\"\n    import numpy as np\n    import torch\n\n    array = np.random.RandomState(seed).standard_normal(shape).astype(np.float32)\n    return torch.from_numpy(array).to(device=device)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/","title":"tokenizer","text":""},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer","title":"worldkernels.models.dreamdojo.tokenizer","text":"<p>Wan2.1 video tokenizer for DreamDojo, matching the reference bf16 numerics.</p>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['VideoTokenizer']\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.WAN_VAE_REPO","title":"WAN_VAE_REPO  <code>module-attribute</code>","text":"<pre><code>WAN_VAE_REPO = 'Wan-AI/Wan2.1-T2V-1.3B-Diffusers'\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer","title":"VideoTokenizer","text":"<p>Deterministic Wan2.1 VAE encode/decode with the standard latent normalization.</p> <p>Runs the VAE fully in bfloat16 (weights and activations) as the reference stack does. <code>encode</code> takes video in \\([-1, 1]\\), returns the posterior mean normalized as \\((\\mu - m) \\cdot s^{-1}\\) with the canonical Wan 16-channel statistics; <code>decode</code> inverts the normalization and returns video in \\([-1, 1]\\). Input dtypes are preserved on output.</p> Source code in <code>worldkernels/models/dreamdojo/tokenizer.py</code> <pre><code>class VideoTokenizer:\n    r\"\"\"Deterministic Wan2.1 VAE encode/decode with the standard latent normalization.\n\n    Runs the VAE fully in bfloat16 (weights and activations) as the reference\n    stack does. ``encode`` takes video in \\([-1, 1]\\), returns the posterior\n    mean normalized as \\((\\mu - m) \\cdot s^{-1}\\) with the canonical Wan\n    16-channel statistics; ``decode`` inverts the normalization and returns\n    video in \\([-1, 1]\\). Input dtypes are preserved on output.\n    \"\"\"\n\n    spatial_compression_factor = 8\n    temporal_compression_factor = 4\n    latent_channels = 16\n\n    def __init__(self, repo: str = WAN_VAE_REPO) -&gt; None:\n        self.repo = repo\n        self._vae = None\n        self._mean: torch.Tensor | None = None\n        self._inv_std: torch.Tensor | None = None\n\n    def load(self, device: str | \"torch.device\") -&gt; \"VideoTokenizer\":\n        import torch\n        from diffusers import AutoencoderKLWan\n\n        vae = AutoencoderKLWan.from_pretrained(\n            self.repo, subfolder=\"vae\", torch_dtype=torch.bfloat16\n        )\n        vae = vae.to(device).eval().requires_grad_(False)\n        self._vae = vae\n        self._mean = torch.tensor(\n            vae.config.latents_mean, dtype=torch.bfloat16, device=device\n        ).view(1, self.latent_channels, 1, 1, 1)\n        self._inv_std = 1.0 / torch.tensor(\n            vae.config.latents_std, dtype=torch.bfloat16, device=device\n        ).view(1, self.latent_channels, 1, 1, 1)\n        return self\n\n    def encode(self, video_B_C_T_H_W: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        import torch\n\n        assert self._vae is not None, \"call load() first\"\n        in_dtype = video_B_C_T_H_W.dtype\n        with torch.no_grad():\n            mu = self._vae.encode(video_B_C_T_H_W.to(torch.bfloat16)).latent_dist.mode()\n            mu = (mu - self._mean) * self._inv_std\n        return mu.to(in_dtype)\n\n    def decode(self, latent_B_C_T_H_W: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        import torch\n\n        assert self._vae is not None, \"call load() first\"\n        in_dtype = latent_B_C_T_H_W.dtype\n        with torch.no_grad():\n            z = latent_B_C_T_H_W.to(torch.bfloat16) / self._inv_std + self._mean\n            video = self._vae.decode(z).sample\n        return video.to(in_dtype)\n\n    def get_latent_num_frames(self, num_pixel_frames: int) -&gt; int:\n        return 1 + (num_pixel_frames - 1) // self.temporal_compression_factor\n\n    def get_pixel_num_frames(self, num_latent_frames: int) -&gt; int:\n        return (num_latent_frames - 1) * self.temporal_compression_factor + 1\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.spatial_compression_factor","title":"spatial_compression_factor  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>spatial_compression_factor = 8\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.temporal_compression_factor","title":"temporal_compression_factor  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>temporal_compression_factor = 4\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.latent_channels","title":"latent_channels  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>latent_channels = 16\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.repo","title":"repo  <code>instance-attribute</code>","text":"<pre><code>repo = repo\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer._vae","title":"_vae  <code>instance-attribute</code>","text":"<pre><code>_vae = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer._mean","title":"_mean  <code>instance-attribute</code>","text":"<pre><code>_mean: Tensor | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer._inv_std","title":"_inv_std  <code>instance-attribute</code>","text":"<pre><code>_inv_std: Tensor | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.__init__","title":"__init__","text":"<pre><code>__init__(repo: str = WAN_VAE_REPO) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/tokenizer.py</code> <pre><code>def __init__(self, repo: str = WAN_VAE_REPO) -&gt; None:\n    self.repo = repo\n    self._vae = None\n    self._mean: torch.Tensor | None = None\n    self._inv_std: torch.Tensor | None = None\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.load","title":"load","text":"<pre><code>load(device: str | 'torch.device') -&gt; 'VideoTokenizer'\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/tokenizer.py</code> <pre><code>def load(self, device: str | \"torch.device\") -&gt; \"VideoTokenizer\":\n    import torch\n    from diffusers import AutoencoderKLWan\n\n    vae = AutoencoderKLWan.from_pretrained(\n        self.repo, subfolder=\"vae\", torch_dtype=torch.bfloat16\n    )\n    vae = vae.to(device).eval().requires_grad_(False)\n    self._vae = vae\n    self._mean = torch.tensor(\n        vae.config.latents_mean, dtype=torch.bfloat16, device=device\n    ).view(1, self.latent_channels, 1, 1, 1)\n    self._inv_std = 1.0 / torch.tensor(\n        vae.config.latents_std, dtype=torch.bfloat16, device=device\n    ).view(1, self.latent_channels, 1, 1, 1)\n    return self\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.encode","title":"encode","text":"<pre><code>encode(video_B_C_T_H_W: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/tokenizer.py</code> <pre><code>def encode(self, video_B_C_T_H_W: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    import torch\n\n    assert self._vae is not None, \"call load() first\"\n    in_dtype = video_B_C_T_H_W.dtype\n    with torch.no_grad():\n        mu = self._vae.encode(video_B_C_T_H_W.to(torch.bfloat16)).latent_dist.mode()\n        mu = (mu - self._mean) * self._inv_std\n    return mu.to(in_dtype)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.decode","title":"decode","text":"<pre><code>decode(latent_B_C_T_H_W: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/tokenizer.py</code> <pre><code>def decode(self, latent_B_C_T_H_W: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    import torch\n\n    assert self._vae is not None, \"call load() first\"\n    in_dtype = latent_B_C_T_H_W.dtype\n    with torch.no_grad():\n        z = latent_B_C_T_H_W.to(torch.bfloat16) / self._inv_std + self._mean\n        video = self._vae.decode(z).sample\n    return video.to(in_dtype)\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.get_latent_num_frames","title":"get_latent_num_frames","text":"<pre><code>get_latent_num_frames(num_pixel_frames: int) -&gt; int\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/tokenizer.py</code> <pre><code>def get_latent_num_frames(self, num_pixel_frames: int) -&gt; int:\n    return 1 + (num_pixel_frames - 1) // self.temporal_compression_factor\n</code></pre>"},{"location":"api/worldkernels/models/dreamdojo/tokenizer/#worldkernels.models.dreamdojo.tokenizer.VideoTokenizer.get_pixel_num_frames","title":"get_pixel_num_frames","text":"<pre><code>get_pixel_num_frames(num_latent_frames: int) -&gt; int\n</code></pre> Source code in <code>worldkernels/models/dreamdojo/tokenizer.py</code> <pre><code>def get_pixel_num_frames(self, num_latent_frames: int) -&gt; int:\n    return (num_latent_frames - 1) * self.temporal_compression_factor + 1\n</code></pre>"},{"location":"api/worldkernels/models/registry/","title":"registry","text":""},{"location":"api/worldkernels/models/registry/#worldkernels.models.registry","title":"worldkernels.models.registry","text":"<p>Registry of video-diffusion pipeline classes, keyed by model family.</p> <p>Adding a model family: drop a <code>pipeline_*.py</code> under <code>models/&lt;family&gt;/</code> that defines the pipeline class, then add one row to <code>_PIPELINES</code>. Resolution is lazy, so importing this registry never imports torch or diffusers.</p>"},{"location":"api/worldkernels/models/registry/#worldkernels.models.registry.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['get_pipeline_class', 'list_pipelines', 'register_pipeline']\n</code></pre>"},{"location":"api/worldkernels/models/registry/#worldkernels.models.registry._PIPELINES","title":"_PIPELINES  <code>module-attribute</code>","text":"<pre><code>_PIPELINES: dict[str, tuple[str, str]] = {'wan_i2v': ('worldkernels.models.wan.pipeline_wan_i2v', 'WanI2VPipeline'), 'dreamdojo': ('worldkernels.models.dreamdojo.pipeline', 'DreamDojoPipeline')}\n</code></pre>"},{"location":"api/worldkernels/models/registry/#worldkernels.models.registry.register_pipeline","title":"register_pipeline","text":"<pre><code>register_pipeline(name: str, module: str, class_name: str) -&gt; None\n</code></pre> <p>Register a pipeline family <code>name</code> resolving to <code>module:class_name</code>.</p> Source code in <code>worldkernels/models/registry.py</code> <pre><code>def register_pipeline(name: str, module: str, class_name: str) -&gt; None:\n    r\"\"\"Register a pipeline family ``name`` resolving to ``module:class_name``.\"\"\"\n    _PIPELINES[name] = (module, class_name)\n</code></pre>"},{"location":"api/worldkernels/models/registry/#worldkernels.models.registry.list_pipelines","title":"list_pipelines","text":"<pre><code>list_pipelines() -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/models/registry.py</code> <pre><code>def list_pipelines() -&gt; list[str]:\n    return sorted(_PIPELINES)\n</code></pre>"},{"location":"api/worldkernels/models/registry/#worldkernels.models.registry.get_pipeline_class","title":"get_pipeline_class","text":"<pre><code>get_pipeline_class(name: str) -&gt; type\n</code></pre> <p>Resolve a pipeline family name to its class, importing the module lazily.</p> Source code in <code>worldkernels/models/registry.py</code> <pre><code>def get_pipeline_class(name: str) -&gt; type:\n    r\"\"\"Resolve a pipeline family name to its class, importing the module lazily.\"\"\"\n    if name not in _PIPELINES:\n        available = \", \".join(sorted(_PIPELINES)) or \"(none)\"\n        raise KeyError(f\"Unknown pipeline {name!r}. Available: {available}\")\n    module_path, class_name = _PIPELINES[name]\n    return getattr(importlib.import_module(module_path), class_name)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/","title":"schedulers","text":""},{"location":"api/worldkernels/models/schedulers/#worldkernels.models.schedulers","title":"worldkernels.models.schedulers","text":"<p>Schedulers for video-diffusion pipelines.</p> <p>Modules:</p> Name Description <code>flow_unipc</code> <p>Owned flow-matching UniPC multistep solver.</p> <code>trigflow</code> <p>TrigFlow few-step sampling for Self-Forcing-distilled causal students.</p>"},{"location":"api/worldkernels/models/schedulers/#worldkernels.models.schedulers.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['FlowUniPCMultistepScheduler', 'TRIGFLOW_TIMES_4STEP', 'trigflow_time', 'trigflow_scaling', 'trigflow_step']\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/#worldkernels.models.schedulers._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'FlowUniPCMultistepScheduler': ('worldkernels.models.schedulers.flow_unipc', 'FlowUniPCMultistepScheduler'), 'TRIGFLOW_TIMES_4STEP': ('worldkernels.models.schedulers.trigflow', 'TRIGFLOW_TIMES_4STEP'), 'trigflow_time': ('worldkernels.models.schedulers.trigflow', 'trigflow_time'), 'trigflow_scaling': ('worldkernels.models.schedulers.trigflow', 'trigflow_scaling'), 'trigflow_step': ('worldkernels.models.schedulers.trigflow', 'trigflow_step')}\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/#worldkernels.models.schedulers.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/models/schedulers/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/","title":"flow_unipc","text":""},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc","title":"worldkernels.models.schedulers.flow_unipc","text":"<p>Owned flow-matching UniPC multistep solver.</p> <p>Full port of the rectified-flow UniPC solver used by Wan and Cosmos-Predict2.5 teacher models (UniPC B(h), predictor-corrector, <code>predict_x0</code> form on the flow parameterization \\(x_t = (1-\\sigma) x_0 + \\sigma \\epsilon\\), velocity prediction \\(v = \\epsilon - x_0\\)). Owning every line of the solver is what lets the native DreamDojo pipeline match golden fixtures at 1e-6.</p> <p>Unlike the upstream copy, the <code>shift</code> warp \\(\\sigma \\mapsto s\\sigma / (1 + (s-1)\\sigma)\\) is applied exactly once, in <code>set_timesteps</code>; upstream applied it a second time at construction, which is the identity for the teacher's <code>shift=1</code> construction.</p>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['FlowUniPCMultistepScheduler']\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler","title":"FlowUniPCMultistepScheduler","text":"<p>               Bases: <code>SchedulerMixin</code>, <code>ConfigMixin</code></p> <p>UniPC multistep solver for flow-matching video diffusion models.</p> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>class FlowUniPCMultistepScheduler(SchedulerMixin, ConfigMixin):  # type: ignore[misc]\n    r\"\"\"UniPC multistep solver for flow-matching video diffusion models.\"\"\"\n\n    order = 1\n\n    @register_to_config\n    def __init__(\n        self,\n        num_train_timesteps: int = 1000,\n        solver_order: int = 2,\n        shift: float = 1.0,\n        solver_type: str = \"bh2\",\n        lower_order_final: bool = True,\n        disable_corrector: list[int] | None = None,\n    ) -&gt; None:\n        if solver_type not in (\"bh1\", \"bh2\"):\n            raise NotImplementedError(f\"{solver_type} is not implemented for {self.__class__}\")\n\n        alphas = np.linspace(1, 1 / num_train_timesteps, num_train_timesteps)[::-1].copy()\n        sigmas = torch.from_numpy(1.0 - alphas).to(dtype=torch.float32)\n\n        self._num_train_timesteps = num_train_timesteps\n        self._solver_order = solver_order\n        self._shift = shift\n        self._solver_type = solver_type\n        self._lower_order_final = lower_order_final\n\n        self.num_inference_steps: int | None = None\n        self.sigmas = sigmas.to(\"cpu\")\n        self.timesteps = sigmas * num_train_timesteps\n        self.sigma_min = self.sigmas[-1].item()\n        self.sigma_max = self.sigmas[0].item()\n\n        self.model_outputs: list[torch.Tensor | None] = [None] * solver_order\n        self.timestep_list: list[int | torch.Tensor | None] = [None] * solver_order\n        self.lower_order_nums = 0\n        self.disable_corrector = list(disable_corrector) if disable_corrector else []\n        self.last_sample: torch.Tensor | None = None\n        self.this_order = solver_order\n        self._step_index: int | None = None\n        self._begin_index: int | None = None\n\n    @classmethod\n    def for_wan(\n        cls,\n        *,\n        flow_shift: float = 5.0,\n        num_train_timesteps: int = 1000,\n    ) -&gt; \"FlowUniPCMultistepScheduler\":\n        r\"\"\"Build a scheduler with Wan's flow-matching defaults.\n\n        Args:\n            flow_shift: Timestep-schedule shift \\(s\\), applied in\n                ``set_timesteps`` as \\(\\sigma \\mapsto s\\sigma/(1+(s-1)\\sigma)\\);\n                larger \\(s\\) spends more steps at high noise. Wan uses ~3.0 at\n                480p, ~5.0 at 720p.\n            num_train_timesteps: Training timestep count.\n        \"\"\"\n        return cls(num_train_timesteps=num_train_timesteps, shift=flow_shift)\n\n    @property\n    def step_index(self) -&gt; int | None:\n        return self._step_index\n\n    @property\n    def begin_index(self) -&gt; int | None:\n        return self._begin_index\n\n    def set_begin_index(self, begin_index: int = 0) -&gt; None:\n        self._begin_index = begin_index\n\n    def set_timesteps(\n        self,\n        num_inference_steps: int,\n        device: str | torch.device | None = None,\n        shift: float | None = None,\n        use_kerras_sigma: bool = False,\n    ) -&gt; None:\n        r\"\"\"Set the discrete sigma/timestep schedule for inference.\n\n        Args:\n            num_inference_steps: Number of solver steps.\n            device: Device for the integer timestep tensor.\n            shift: Schedule warp \\(s\\); defaults to the construction-time value.\n            use_kerras_sigma: Use the EDM Karras sigma ladder mapped to flow\n                sigmas via \\(\\sigma/(1+\\sigma)\\) instead of the shifted linspace.\n        \"\"\"\n        if use_kerras_sigma:\n            sigma_max, sigma_min, rho = 200, 0.01, 7\n            ramp = np.arange(num_inference_steps + 1) / num_inference_steps\n            min_inv_rho = sigma_min ** (1 / rho)\n            max_inv_rho = sigma_max ** (1 / rho)\n            sigmas = (max_inv_rho + ramp * (min_inv_rho - max_inv_rho)) ** rho\n            sigmas = sigmas / (1 + sigmas)\n        else:\n            sigmas = np.linspace(self.sigma_max, self.sigma_min, num_inference_steps + 1)[:-1]\n            if shift is None:\n                shift = self._shift\n            sigmas = shift * sigmas / (1 + (shift - 1) * sigmas)\n\n        timesteps = sigmas * self._num_train_timesteps\n        sigmas = np.concatenate([sigmas, [0.0]]).astype(np.float32)\n\n        self.sigmas = torch.from_numpy(sigmas).to(\"cpu\")\n        self.timesteps = torch.from_numpy(timesteps).to(device=device, dtype=torch.int64)\n        self.num_inference_steps = len(timesteps)\n\n        self.model_outputs = [None] * self._solver_order\n        self.timestep_list = [None] * self._solver_order\n        self.lower_order_nums = 0\n        self.last_sample = None\n        self._step_index = None\n        self._begin_index = None\n\n    def _sigma_to_alpha_sigma_t(self, sigma: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor]:\n        return 1 - sigma, sigma\n\n    def convert_model_output(\n        self, model_output: torch.Tensor, sample: torch.Tensor\n    ) -&gt; torch.Tensor:\n        r\"\"\"Convert velocity prediction to the \\(x_0\\) prediction UniPC integrates.\"\"\"\n        sigma_t = self.sigmas[self.step_index]\n        return sample - sigma_t * model_output\n\n    def multistep_uni_p_bh_update(self, sample: torch.Tensor, order: int) -&gt; torch.Tensor:\n        r\"\"\"UniP predictor update of the given order from stored model outputs.\"\"\"\n        m0 = self.model_outputs[-1]\n        assert m0 is not None\n        x = sample\n        device = sample.device\n\n        assert self._step_index is not None\n        sigma_t, sigma_s0 = self.sigmas[self._step_index + 1], self.sigmas[self._step_index]\n        alpha_t, sigma_t = self._sigma_to_alpha_sigma_t(sigma_t)\n        alpha_s0, sigma_s0 = self._sigma_to_alpha_sigma_t(sigma_s0)\n\n        lambda_t = torch.log(alpha_t) - torch.log(sigma_t)\n        lambda_s0 = torch.log(alpha_s0) - torch.log(sigma_s0)\n        h = lambda_t - lambda_s0\n\n        rks: list[torch.Tensor | float] = []\n        D1s = []\n        for i in range(1, order):\n            si = self._step_index - i\n            mi = self.model_outputs[-(i + 1)]\n            assert mi is not None\n            alpha_si, sigma_si = self._sigma_to_alpha_sigma_t(self.sigmas[si])\n            lambda_si = torch.log(alpha_si) - torch.log(sigma_si)\n            rk = (lambda_si - lambda_s0) / h\n            rks.append(rk)\n            D1s.append((mi - m0) / rk)\n        rks.append(1.0)\n        rks_t = torch.tensor(rks, device=device)\n\n        hh = -h\n        h_phi_1 = torch.expm1(hh)  # h * phi_1(h) = e^h - 1\n        h_phi_k = h_phi_1 / hh - 1\n        factorial_i = 1\n        B_h = hh if self._solver_type == \"bh1\" else torch.expm1(hh)\n\n        R = []\n        b = []\n        for i in range(1, order + 1):\n            R.append(torch.pow(rks_t, i - 1))\n            b.append(h_phi_k * factorial_i / B_h)\n            factorial_i *= i + 1\n            h_phi_k = h_phi_k / hh - 1 / factorial_i\n        R_t = torch.stack(R)\n        b_t = torch.tensor(b, device=device)\n\n        if D1s:\n            D1s_t = torch.stack(D1s, dim=1)\n            if order == 2:\n                rhos_p = torch.tensor([0.5], dtype=x.dtype, device=device)\n            else:\n                rhos_p = torch.linalg.solve(R_t[:-1, :-1], b_t[:-1]).to(device).to(x.dtype)\n            pred_res = torch.einsum(\"k,bkc...-&gt;bc...\", rhos_p, D1s_t)\n        else:\n            pred_res = torch.zeros_like(x)\n\n        x_t = sigma_t / sigma_s0 * x - alpha_t * h_phi_1 * m0 - alpha_t * B_h * pred_res\n        return x_t.to(x.dtype)\n\n    def multistep_uni_c_bh_update(\n        self,\n        this_model_output: torch.Tensor,\n        last_sample: torch.Tensor,\n        this_sample: torch.Tensor,\n        order: int,\n    ) -&gt; torch.Tensor:\n        r\"\"\"UniC corrector update using the model output at the predicted point.\"\"\"\n        m0 = self.model_outputs[-1]\n        assert m0 is not None\n        x = last_sample\n        model_t = this_model_output\n        device = this_sample.device\n\n        assert self._step_index is not None\n        sigma_t, sigma_s0 = self.sigmas[self._step_index], self.sigmas[self._step_index - 1]\n        alpha_t, sigma_t = self._sigma_to_alpha_sigma_t(sigma_t)\n        alpha_s0, sigma_s0 = self._sigma_to_alpha_sigma_t(sigma_s0)\n\n        lambda_t = torch.log(alpha_t) - torch.log(sigma_t)\n        lambda_s0 = torch.log(alpha_s0) - torch.log(sigma_s0)\n        h = lambda_t - lambda_s0\n\n        rks: list[torch.Tensor | float] = []\n        D1s = []\n        for i in range(1, order):\n            si = self._step_index - (i + 1)\n            mi = self.model_outputs[-(i + 1)]\n            assert mi is not None\n            alpha_si, sigma_si = self._sigma_to_alpha_sigma_t(self.sigmas[si])\n            lambda_si = torch.log(alpha_si) - torch.log(sigma_si)\n            rk = (lambda_si - lambda_s0) / h\n            rks.append(rk)\n            D1s.append((mi - m0) / rk)\n        rks.append(1.0)\n        rks_t = torch.tensor(rks, device=device)\n\n        hh = -h\n        h_phi_1 = torch.expm1(hh)\n        h_phi_k = h_phi_1 / hh - 1\n        factorial_i = 1\n        B_h = hh if self._solver_type == \"bh1\" else torch.expm1(hh)\n\n        R = []\n        b = []\n        for i in range(1, order + 1):\n            R.append(torch.pow(rks_t, i - 1))\n            b.append(h_phi_k * factorial_i / B_h)\n            factorial_i *= i + 1\n            h_phi_k = h_phi_k / hh - 1 / factorial_i\n        R_t = torch.stack(R)\n        b_t = torch.tensor(b, device=device)\n\n        D1s_t = torch.stack(D1s, dim=1) if D1s else None\n\n        if order == 1:\n            rhos_c = torch.tensor([0.5], dtype=x.dtype, device=device)\n        else:\n            rhos_c = torch.linalg.solve(R_t, b_t).to(device).to(x.dtype)\n\n        if D1s_t is not None:\n            corr_res = torch.einsum(\"k,bkc...-&gt;bc...\", rhos_c[:-1], D1s_t)\n        else:\n            corr_res = torch.zeros_like(x)\n        D1_t = model_t - m0\n        x_t = (\n            sigma_t / sigma_s0 * x\n            - alpha_t * h_phi_1 * m0\n            - alpha_t * B_h * (corr_res + rhos_c[-1] * D1_t)\n        )\n        return x_t.to(x.dtype)\n\n    def index_for_timestep(\n        self, timestep: int | torch.Tensor, schedule_timesteps: torch.Tensor | None = None\n    ) -&gt; int:\n        if schedule_timesteps is None:\n            schedule_timesteps = self.timesteps\n        indices = (schedule_timesteps == timestep).nonzero()\n        pos = 1 if len(indices) &gt; 1 else 0\n        return int(indices[pos].item())\n\n    def _init_step_index(self, timestep: int | torch.Tensor) -&gt; None:\n        if self._begin_index is None:\n            if isinstance(timestep, torch.Tensor):\n                timestep = timestep.to(self.timesteps.device)\n            self._step_index = self.index_for_timestep(timestep)\n        else:\n            self._step_index = self._begin_index\n\n    def step(\n        self,\n        model_output: torch.Tensor,\n        timestep: int | torch.Tensor,\n        sample: torch.Tensor,\n        return_dict: bool = True,\n        generator: torch.Generator | None = None,\n    ) -&gt; SchedulerOutput | tuple[torch.Tensor, torch.Tensor]:\n        r\"\"\"Advance the sample one solver step given the velocity prediction.\"\"\"\n        del generator\n        if self.num_inference_steps is None:\n            raise ValueError(\"run 'set_timesteps' before calling 'step'\")\n        if self._step_index is None:\n            self._init_step_index(timestep)\n        assert self._step_index is not None\n\n        use_corrector = (\n            self._step_index &gt; 0\n            and self._step_index - 1 not in self.disable_corrector\n            and self.last_sample is not None\n        )\n\n        model_output_convert = self.convert_model_output(model_output, sample=sample)\n\n        if use_corrector:\n            assert self.last_sample is not None\n            sample = self.multistep_uni_c_bh_update(\n                this_model_output=model_output_convert,\n                last_sample=self.last_sample,\n                this_sample=sample,\n                order=self.this_order,\n            )\n\n        for i in range(self._solver_order - 1):\n            self.model_outputs[i] = self.model_outputs[i + 1]\n            self.timestep_list[i] = self.timestep_list[i + 1]\n        self.model_outputs[-1] = model_output_convert\n        self.timestep_list[-1] = timestep\n\n        if self._lower_order_final:\n            this_order = min(self._solver_order, len(self.timesteps) - self._step_index)\n        else:\n            this_order = self._solver_order\n        self.this_order = min(this_order, self.lower_order_nums + 1)\n        assert self.this_order &gt; 0\n\n        self.last_sample = sample\n        prev_sample = self.multistep_uni_p_bh_update(sample=sample, order=self.this_order)\n\n        if self.lower_order_nums &lt; self._solver_order:\n            self.lower_order_nums += 1\n        self._step_index += 1\n\n        if not return_dict:\n            return (prev_sample, model_output_convert)\n        return SchedulerOutput(prev_sample=prev_sample)\n\n    def scale_model_input(self, sample: torch.Tensor, *args, **kwargs) -&gt; torch.Tensor:\n        return sample\n\n    def add_noise(\n        self,\n        original_samples: torch.Tensor,\n        noise: torch.Tensor,\n        timesteps: torch.Tensor,\n    ) -&gt; torch.Tensor:\n        r\"\"\"Forward-diffuse samples to the noise levels of ``timesteps``.\"\"\"\n        sigmas = self.sigmas.to(device=original_samples.device, dtype=original_samples.dtype)\n        schedule_timesteps = self.timesteps.to(original_samples.device)\n        timesteps = timesteps.to(original_samples.device)\n\n        if self._begin_index is None:\n            step_indices = [self.index_for_timestep(t, schedule_timesteps) for t in timesteps]\n        elif self._step_index is not None:\n            step_indices = [self._step_index] * timesteps.shape[0]\n        else:\n            step_indices = [self._begin_index] * timesteps.shape[0]\n\n        sigma = sigmas[step_indices].flatten()\n        while len(sigma.shape) &lt; len(original_samples.shape):\n            sigma = sigma.unsqueeze(-1)\n\n        alpha_t, sigma_t = self._sigma_to_alpha_sigma_t(sigma)\n        return alpha_t * original_samples + sigma_t * noise\n\n    def __len__(self) -&gt; int:\n        return int(self._num_train_timesteps)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.order","title":"order  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>order = 1\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._num_train_timesteps","title":"_num_train_timesteps  <code>instance-attribute</code>","text":"<pre><code>_num_train_timesteps = num_train_timesteps\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._solver_order","title":"_solver_order  <code>instance-attribute</code>","text":"<pre><code>_solver_order = solver_order\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._shift","title":"_shift  <code>instance-attribute</code>","text":"<pre><code>_shift = shift\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._solver_type","title":"_solver_type  <code>instance-attribute</code>","text":"<pre><code>_solver_type = solver_type\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._lower_order_final","title":"_lower_order_final  <code>instance-attribute</code>","text":"<pre><code>_lower_order_final = lower_order_final\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.num_inference_steps","title":"num_inference_steps  <code>instance-attribute</code>","text":"<pre><code>num_inference_steps: int | None = None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.sigmas","title":"sigmas  <code>instance-attribute</code>","text":"<pre><code>sigmas = sigmas.to('cpu')\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.timesteps","title":"timesteps  <code>instance-attribute</code>","text":"<pre><code>timesteps = sigmas * num_train_timesteps\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.sigma_min","title":"sigma_min  <code>instance-attribute</code>","text":"<pre><code>sigma_min = self.sigmas[-1].item()\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.sigma_max","title":"sigma_max  <code>instance-attribute</code>","text":"<pre><code>sigma_max = self.sigmas[0].item()\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.model_outputs","title":"model_outputs  <code>instance-attribute</code>","text":"<pre><code>model_outputs: list[Tensor | None] = [None] * solver_order\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.timestep_list","title":"timestep_list  <code>instance-attribute</code>","text":"<pre><code>timestep_list: list[int | Tensor | None] = [None] * solver_order\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.lower_order_nums","title":"lower_order_nums  <code>instance-attribute</code>","text":"<pre><code>lower_order_nums = 0\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.disable_corrector","title":"disable_corrector  <code>instance-attribute</code>","text":"<pre><code>disable_corrector = list(disable_corrector) if disable_corrector else []\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.last_sample","title":"last_sample  <code>instance-attribute</code>","text":"<pre><code>last_sample: Tensor | None = None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.this_order","title":"this_order  <code>instance-attribute</code>","text":"<pre><code>this_order = solver_order\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._step_index","title":"_step_index  <code>instance-attribute</code>","text":"<pre><code>_step_index: int | None = None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._begin_index","title":"_begin_index  <code>instance-attribute</code>","text":"<pre><code>_begin_index: int | None = None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.step_index","title":"step_index  <code>property</code>","text":"<pre><code>step_index: int | None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.begin_index","title":"begin_index  <code>property</code>","text":"<pre><code>begin_index: int | None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.__init__","title":"__init__","text":"<pre><code>__init__(num_train_timesteps: int = 1000, solver_order: int = 2, shift: float = 1.0, solver_type: str = 'bh2', lower_order_final: bool = True, disable_corrector: list[int] | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>@register_to_config\ndef __init__(\n    self,\n    num_train_timesteps: int = 1000,\n    solver_order: int = 2,\n    shift: float = 1.0,\n    solver_type: str = \"bh2\",\n    lower_order_final: bool = True,\n    disable_corrector: list[int] | None = None,\n) -&gt; None:\n    if solver_type not in (\"bh1\", \"bh2\"):\n        raise NotImplementedError(f\"{solver_type} is not implemented for {self.__class__}\")\n\n    alphas = np.linspace(1, 1 / num_train_timesteps, num_train_timesteps)[::-1].copy()\n    sigmas = torch.from_numpy(1.0 - alphas).to(dtype=torch.float32)\n\n    self._num_train_timesteps = num_train_timesteps\n    self._solver_order = solver_order\n    self._shift = shift\n    self._solver_type = solver_type\n    self._lower_order_final = lower_order_final\n\n    self.num_inference_steps: int | None = None\n    self.sigmas = sigmas.to(\"cpu\")\n    self.timesteps = sigmas * num_train_timesteps\n    self.sigma_min = self.sigmas[-1].item()\n    self.sigma_max = self.sigmas[0].item()\n\n    self.model_outputs: list[torch.Tensor | None] = [None] * solver_order\n    self.timestep_list: list[int | torch.Tensor | None] = [None] * solver_order\n    self.lower_order_nums = 0\n    self.disable_corrector = list(disable_corrector) if disable_corrector else []\n    self.last_sample: torch.Tensor | None = None\n    self.this_order = solver_order\n    self._step_index: int | None = None\n    self._begin_index: int | None = None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.for_wan","title":"for_wan  <code>classmethod</code>","text":"<pre><code>for_wan(*, flow_shift: float = 5.0, num_train_timesteps: int = 1000) -&gt; 'FlowUniPCMultistepScheduler'\n</code></pre> <p>Build a scheduler with Wan's flow-matching defaults.</p> <p>Parameters:</p> Name Type Description Default <code>flow_shift</code> <code>float</code> <p>Timestep-schedule shift \\(s\\), applied in <code>set_timesteps</code> as \\(\\sigma \\mapsto s\\sigma/(1+(s-1)\\sigma)\\); larger \\(s\\) spends more steps at high noise. Wan uses ~3.0 at 480p, ~5.0 at 720p.</p> <code>5.0</code> <code>num_train_timesteps</code> <code>int</code> <p>Training timestep count.</p> <code>1000</code> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>@classmethod\ndef for_wan(\n    cls,\n    *,\n    flow_shift: float = 5.0,\n    num_train_timesteps: int = 1000,\n) -&gt; \"FlowUniPCMultistepScheduler\":\n    r\"\"\"Build a scheduler with Wan's flow-matching defaults.\n\n    Args:\n        flow_shift: Timestep-schedule shift \\(s\\), applied in\n            ``set_timesteps`` as \\(\\sigma \\mapsto s\\sigma/(1+(s-1)\\sigma)\\);\n            larger \\(s\\) spends more steps at high noise. Wan uses ~3.0 at\n            480p, ~5.0 at 720p.\n        num_train_timesteps: Training timestep count.\n    \"\"\"\n    return cls(num_train_timesteps=num_train_timesteps, shift=flow_shift)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.set_begin_index","title":"set_begin_index","text":"<pre><code>set_begin_index(begin_index: int = 0) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def set_begin_index(self, begin_index: int = 0) -&gt; None:\n    self._begin_index = begin_index\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.set_timesteps","title":"set_timesteps","text":"<pre><code>set_timesteps(num_inference_steps: int, device: str | device | None = None, shift: float | None = None, use_kerras_sigma: bool = False) -&gt; None\n</code></pre> <p>Set the discrete sigma/timestep schedule for inference.</p> <p>Parameters:</p> Name Type Description Default <code>num_inference_steps</code> <code>int</code> <p>Number of solver steps.</p> required <code>device</code> <code>str | device | None</code> <p>Device for the integer timestep tensor.</p> <code>None</code> <code>shift</code> <code>float | None</code> <p>Schedule warp \\(s\\); defaults to the construction-time value.</p> <code>None</code> <code>use_kerras_sigma</code> <code>bool</code> <p>Use the EDM Karras sigma ladder mapped to flow sigmas via \\(\\sigma/(1+\\sigma)\\) instead of the shifted linspace.</p> <code>False</code> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def set_timesteps(\n    self,\n    num_inference_steps: int,\n    device: str | torch.device | None = None,\n    shift: float | None = None,\n    use_kerras_sigma: bool = False,\n) -&gt; None:\n    r\"\"\"Set the discrete sigma/timestep schedule for inference.\n\n    Args:\n        num_inference_steps: Number of solver steps.\n        device: Device for the integer timestep tensor.\n        shift: Schedule warp \\(s\\); defaults to the construction-time value.\n        use_kerras_sigma: Use the EDM Karras sigma ladder mapped to flow\n            sigmas via \\(\\sigma/(1+\\sigma)\\) instead of the shifted linspace.\n    \"\"\"\n    if use_kerras_sigma:\n        sigma_max, sigma_min, rho = 200, 0.01, 7\n        ramp = np.arange(num_inference_steps + 1) / num_inference_steps\n        min_inv_rho = sigma_min ** (1 / rho)\n        max_inv_rho = sigma_max ** (1 / rho)\n        sigmas = (max_inv_rho + ramp * (min_inv_rho - max_inv_rho)) ** rho\n        sigmas = sigmas / (1 + sigmas)\n    else:\n        sigmas = np.linspace(self.sigma_max, self.sigma_min, num_inference_steps + 1)[:-1]\n        if shift is None:\n            shift = self._shift\n        sigmas = shift * sigmas / (1 + (shift - 1) * sigmas)\n\n    timesteps = sigmas * self._num_train_timesteps\n    sigmas = np.concatenate([sigmas, [0.0]]).astype(np.float32)\n\n    self.sigmas = torch.from_numpy(sigmas).to(\"cpu\")\n    self.timesteps = torch.from_numpy(timesteps).to(device=device, dtype=torch.int64)\n    self.num_inference_steps = len(timesteps)\n\n    self.model_outputs = [None] * self._solver_order\n    self.timestep_list = [None] * self._solver_order\n    self.lower_order_nums = 0\n    self.last_sample = None\n    self._step_index = None\n    self._begin_index = None\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._sigma_to_alpha_sigma_t","title":"_sigma_to_alpha_sigma_t","text":"<pre><code>_sigma_to_alpha_sigma_t(sigma: Tensor) -&gt; tuple[Tensor, Tensor]\n</code></pre> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def _sigma_to_alpha_sigma_t(self, sigma: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor]:\n    return 1 - sigma, sigma\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.convert_model_output","title":"convert_model_output","text":"<pre><code>convert_model_output(model_output: Tensor, sample: Tensor) -&gt; Tensor\n</code></pre> <p>Convert velocity prediction to the \\(x_0\\) prediction UniPC integrates.</p> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def convert_model_output(\n    self, model_output: torch.Tensor, sample: torch.Tensor\n) -&gt; torch.Tensor:\n    r\"\"\"Convert velocity prediction to the \\(x_0\\) prediction UniPC integrates.\"\"\"\n    sigma_t = self.sigmas[self.step_index]\n    return sample - sigma_t * model_output\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.multistep_uni_p_bh_update","title":"multistep_uni_p_bh_update","text":"<pre><code>multistep_uni_p_bh_update(sample: Tensor, order: int) -&gt; Tensor\n</code></pre> <p>UniP predictor update of the given order from stored model outputs.</p> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def multistep_uni_p_bh_update(self, sample: torch.Tensor, order: int) -&gt; torch.Tensor:\n    r\"\"\"UniP predictor update of the given order from stored model outputs.\"\"\"\n    m0 = self.model_outputs[-1]\n    assert m0 is not None\n    x = sample\n    device = sample.device\n\n    assert self._step_index is not None\n    sigma_t, sigma_s0 = self.sigmas[self._step_index + 1], self.sigmas[self._step_index]\n    alpha_t, sigma_t = self._sigma_to_alpha_sigma_t(sigma_t)\n    alpha_s0, sigma_s0 = self._sigma_to_alpha_sigma_t(sigma_s0)\n\n    lambda_t = torch.log(alpha_t) - torch.log(sigma_t)\n    lambda_s0 = torch.log(alpha_s0) - torch.log(sigma_s0)\n    h = lambda_t - lambda_s0\n\n    rks: list[torch.Tensor | float] = []\n    D1s = []\n    for i in range(1, order):\n        si = self._step_index - i\n        mi = self.model_outputs[-(i + 1)]\n        assert mi is not None\n        alpha_si, sigma_si = self._sigma_to_alpha_sigma_t(self.sigmas[si])\n        lambda_si = torch.log(alpha_si) - torch.log(sigma_si)\n        rk = (lambda_si - lambda_s0) / h\n        rks.append(rk)\n        D1s.append((mi - m0) / rk)\n    rks.append(1.0)\n    rks_t = torch.tensor(rks, device=device)\n\n    hh = -h\n    h_phi_1 = torch.expm1(hh)  # h * phi_1(h) = e^h - 1\n    h_phi_k = h_phi_1 / hh - 1\n    factorial_i = 1\n    B_h = hh if self._solver_type == \"bh1\" else torch.expm1(hh)\n\n    R = []\n    b = []\n    for i in range(1, order + 1):\n        R.append(torch.pow(rks_t, i - 1))\n        b.append(h_phi_k * factorial_i / B_h)\n        factorial_i *= i + 1\n        h_phi_k = h_phi_k / hh - 1 / factorial_i\n    R_t = torch.stack(R)\n    b_t = torch.tensor(b, device=device)\n\n    if D1s:\n        D1s_t = torch.stack(D1s, dim=1)\n        if order == 2:\n            rhos_p = torch.tensor([0.5], dtype=x.dtype, device=device)\n        else:\n            rhos_p = torch.linalg.solve(R_t[:-1, :-1], b_t[:-1]).to(device).to(x.dtype)\n        pred_res = torch.einsum(\"k,bkc...-&gt;bc...\", rhos_p, D1s_t)\n    else:\n        pred_res = torch.zeros_like(x)\n\n    x_t = sigma_t / sigma_s0 * x - alpha_t * h_phi_1 * m0 - alpha_t * B_h * pred_res\n    return x_t.to(x.dtype)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.multistep_uni_c_bh_update","title":"multistep_uni_c_bh_update","text":"<pre><code>multistep_uni_c_bh_update(this_model_output: Tensor, last_sample: Tensor, this_sample: Tensor, order: int) -&gt; Tensor\n</code></pre> <p>UniC corrector update using the model output at the predicted point.</p> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def multistep_uni_c_bh_update(\n    self,\n    this_model_output: torch.Tensor,\n    last_sample: torch.Tensor,\n    this_sample: torch.Tensor,\n    order: int,\n) -&gt; torch.Tensor:\n    r\"\"\"UniC corrector update using the model output at the predicted point.\"\"\"\n    m0 = self.model_outputs[-1]\n    assert m0 is not None\n    x = last_sample\n    model_t = this_model_output\n    device = this_sample.device\n\n    assert self._step_index is not None\n    sigma_t, sigma_s0 = self.sigmas[self._step_index], self.sigmas[self._step_index - 1]\n    alpha_t, sigma_t = self._sigma_to_alpha_sigma_t(sigma_t)\n    alpha_s0, sigma_s0 = self._sigma_to_alpha_sigma_t(sigma_s0)\n\n    lambda_t = torch.log(alpha_t) - torch.log(sigma_t)\n    lambda_s0 = torch.log(alpha_s0) - torch.log(sigma_s0)\n    h = lambda_t - lambda_s0\n\n    rks: list[torch.Tensor | float] = []\n    D1s = []\n    for i in range(1, order):\n        si = self._step_index - (i + 1)\n        mi = self.model_outputs[-(i + 1)]\n        assert mi is not None\n        alpha_si, sigma_si = self._sigma_to_alpha_sigma_t(self.sigmas[si])\n        lambda_si = torch.log(alpha_si) - torch.log(sigma_si)\n        rk = (lambda_si - lambda_s0) / h\n        rks.append(rk)\n        D1s.append((mi - m0) / rk)\n    rks.append(1.0)\n    rks_t = torch.tensor(rks, device=device)\n\n    hh = -h\n    h_phi_1 = torch.expm1(hh)\n    h_phi_k = h_phi_1 / hh - 1\n    factorial_i = 1\n    B_h = hh if self._solver_type == \"bh1\" else torch.expm1(hh)\n\n    R = []\n    b = []\n    for i in range(1, order + 1):\n        R.append(torch.pow(rks_t, i - 1))\n        b.append(h_phi_k * factorial_i / B_h)\n        factorial_i *= i + 1\n        h_phi_k = h_phi_k / hh - 1 / factorial_i\n    R_t = torch.stack(R)\n    b_t = torch.tensor(b, device=device)\n\n    D1s_t = torch.stack(D1s, dim=1) if D1s else None\n\n    if order == 1:\n        rhos_c = torch.tensor([0.5], dtype=x.dtype, device=device)\n    else:\n        rhos_c = torch.linalg.solve(R_t, b_t).to(device).to(x.dtype)\n\n    if D1s_t is not None:\n        corr_res = torch.einsum(\"k,bkc...-&gt;bc...\", rhos_c[:-1], D1s_t)\n    else:\n        corr_res = torch.zeros_like(x)\n    D1_t = model_t - m0\n    x_t = (\n        sigma_t / sigma_s0 * x\n        - alpha_t * h_phi_1 * m0\n        - alpha_t * B_h * (corr_res + rhos_c[-1] * D1_t)\n    )\n    return x_t.to(x.dtype)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.index_for_timestep","title":"index_for_timestep","text":"<pre><code>index_for_timestep(timestep: int | Tensor, schedule_timesteps: Tensor | None = None) -&gt; int\n</code></pre> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def index_for_timestep(\n    self, timestep: int | torch.Tensor, schedule_timesteps: torch.Tensor | None = None\n) -&gt; int:\n    if schedule_timesteps is None:\n        schedule_timesteps = self.timesteps\n    indices = (schedule_timesteps == timestep).nonzero()\n    pos = 1 if len(indices) &gt; 1 else 0\n    return int(indices[pos].item())\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler._init_step_index","title":"_init_step_index","text":"<pre><code>_init_step_index(timestep: int | Tensor) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def _init_step_index(self, timestep: int | torch.Tensor) -&gt; None:\n    if self._begin_index is None:\n        if isinstance(timestep, torch.Tensor):\n            timestep = timestep.to(self.timesteps.device)\n        self._step_index = self.index_for_timestep(timestep)\n    else:\n        self._step_index = self._begin_index\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.step","title":"step","text":"<pre><code>step(model_output: Tensor, timestep: int | Tensor, sample: Tensor, return_dict: bool = True, generator: Generator | None = None) -&gt; SchedulerOutput | tuple[Tensor, Tensor]\n</code></pre> <p>Advance the sample one solver step given the velocity prediction.</p> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def step(\n    self,\n    model_output: torch.Tensor,\n    timestep: int | torch.Tensor,\n    sample: torch.Tensor,\n    return_dict: bool = True,\n    generator: torch.Generator | None = None,\n) -&gt; SchedulerOutput | tuple[torch.Tensor, torch.Tensor]:\n    r\"\"\"Advance the sample one solver step given the velocity prediction.\"\"\"\n    del generator\n    if self.num_inference_steps is None:\n        raise ValueError(\"run 'set_timesteps' before calling 'step'\")\n    if self._step_index is None:\n        self._init_step_index(timestep)\n    assert self._step_index is not None\n\n    use_corrector = (\n        self._step_index &gt; 0\n        and self._step_index - 1 not in self.disable_corrector\n        and self.last_sample is not None\n    )\n\n    model_output_convert = self.convert_model_output(model_output, sample=sample)\n\n    if use_corrector:\n        assert self.last_sample is not None\n        sample = self.multistep_uni_c_bh_update(\n            this_model_output=model_output_convert,\n            last_sample=self.last_sample,\n            this_sample=sample,\n            order=self.this_order,\n        )\n\n    for i in range(self._solver_order - 1):\n        self.model_outputs[i] = self.model_outputs[i + 1]\n        self.timestep_list[i] = self.timestep_list[i + 1]\n    self.model_outputs[-1] = model_output_convert\n    self.timestep_list[-1] = timestep\n\n    if self._lower_order_final:\n        this_order = min(self._solver_order, len(self.timesteps) - self._step_index)\n    else:\n        this_order = self._solver_order\n    self.this_order = min(this_order, self.lower_order_nums + 1)\n    assert self.this_order &gt; 0\n\n    self.last_sample = sample\n    prev_sample = self.multistep_uni_p_bh_update(sample=sample, order=self.this_order)\n\n    if self.lower_order_nums &lt; self._solver_order:\n        self.lower_order_nums += 1\n    self._step_index += 1\n\n    if not return_dict:\n        return (prev_sample, model_output_convert)\n    return SchedulerOutput(prev_sample=prev_sample)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.scale_model_input","title":"scale_model_input","text":"<pre><code>scale_model_input(sample: Tensor, *args, **kwargs) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def scale_model_input(self, sample: torch.Tensor, *args, **kwargs) -&gt; torch.Tensor:\n    return sample\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.add_noise","title":"add_noise","text":"<pre><code>add_noise(original_samples: Tensor, noise: Tensor, timesteps: Tensor) -&gt; Tensor\n</code></pre> <p>Forward-diffuse samples to the noise levels of <code>timesteps</code>.</p> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def add_noise(\n    self,\n    original_samples: torch.Tensor,\n    noise: torch.Tensor,\n    timesteps: torch.Tensor,\n) -&gt; torch.Tensor:\n    r\"\"\"Forward-diffuse samples to the noise levels of ``timesteps``.\"\"\"\n    sigmas = self.sigmas.to(device=original_samples.device, dtype=original_samples.dtype)\n    schedule_timesteps = self.timesteps.to(original_samples.device)\n    timesteps = timesteps.to(original_samples.device)\n\n    if self._begin_index is None:\n        step_indices = [self.index_for_timestep(t, schedule_timesteps) for t in timesteps]\n    elif self._step_index is not None:\n        step_indices = [self._step_index] * timesteps.shape[0]\n    else:\n        step_indices = [self._begin_index] * timesteps.shape[0]\n\n    sigma = sigmas[step_indices].flatten()\n    while len(sigma.shape) &lt; len(original_samples.shape):\n        sigma = sigma.unsqueeze(-1)\n\n    alpha_t, sigma_t = self._sigma_to_alpha_sigma_t(sigma)\n    return alpha_t * original_samples + sigma_t * noise\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/flow_unipc/#worldkernels.models.schedulers.flow_unipc.FlowUniPCMultistepScheduler.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> Source code in <code>worldkernels/models/schedulers/flow_unipc.py</code> <pre><code>def __len__(self) -&gt; int:\n    return int(self._num_train_timesteps)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/trigflow/","title":"trigflow","text":""},{"location":"api/worldkernels/models/schedulers/trigflow/#worldkernels.models.schedulers.trigflow","title":"worldkernels.models.schedulers.trigflow","text":"<p>TrigFlow few-step sampling for Self-Forcing-distilled causal students.</p> <p>TrigFlow parameterizes the noise level as an angle \\(t = \\arctan(\\sigma/\\sigma_d)\\) so a sample decomposes as \\(x_t = \\cos(t)\\,x_0/\\sigma_d + \\sin(t)\\,\\epsilon\\). The distilled DreamDojo student denoises each latent frame in a few steps along a fixed angle ladder, with rectified-flow preconditioning coefficients derived from the same angle.</p>"},{"location":"api/worldkernels/models/schedulers/trigflow/#worldkernels.models.schedulers.trigflow.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['TRIGFLOW_TIMES_4STEP', 'trigflow_time', 'trigflow_scaling', 'trigflow_step']\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/trigflow/#worldkernels.models.schedulers.trigflow.TRIGFLOW_TIMES_4STEP","title":"TRIGFLOW_TIMES_4STEP  <code>module-attribute</code>","text":"<pre><code>TRIGFLOW_TIMES_4STEP: tuple[float, ...] = (math.pi / 2, math.atan(15.0), math.atan(5.0), math.atan(5.0 / 3.0))\n</code></pre> <p>Student sampling angles, pure noise (\\(\\pi/2\\)) down to \\(\\arctan(5/3)\\).</p>"},{"location":"api/worldkernels/models/schedulers/trigflow/#worldkernels.models.schedulers.trigflow.trigflow_time","title":"trigflow_time","text":"<pre><code>trigflow_time(sigma: float, sigma_data: float = 1.0) -&gt; float\n</code></pre> <p>Angle for a noise level: \\(t = \\arctan(\\sigma/\\sigma_d)\\).</p> Source code in <code>worldkernels/models/schedulers/trigflow.py</code> <pre><code>def trigflow_time(sigma: float, sigma_data: float = 1.0) -&gt; float:\n    r\"\"\"Angle for a noise level: \\(t = \\arctan(\\sigma/\\sigma_d)\\).\"\"\"\n    return math.atan(sigma / sigma_data)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/trigflow/#worldkernels.models.schedulers.trigflow.trigflow_scaling","title":"trigflow_scaling","text":"<pre><code>trigflow_scaling(trigflow_t: Tensor, sigma_data: float = 1.0) -&gt; tuple[Tensor, Tensor, Tensor, Tensor]\n</code></pre> <p>Rectified-flow preconditioning coefficients for a TrigFlow angle.</p> <p>Computed in float64 and cast back, matching the reference exactly: $\\(c_\\text{skip} = c_\\text{in} = \\frac{\\sigma_d}{\\cos t + \\sigma_d \\sin t},\\quad c_\\text{out} = -\\sigma_d \\sin t \\cdot c_\\text{in} / \\sigma_d,\\quad c_\\text{noise} = \\sigma_d \\sin t \\cdot c_\\text{in} / \\sigma_d\\)$</p> <p>The denoiser evaluates \\(x_0 = c_\\text{skip} x_t + c_\\text{out}\\, F(c_\\text{in} x_t, c_\\text{noise})\\).</p> <p>Returns:</p> Type Description <code>tuple[Tensor, Tensor, Tensor, Tensor]</code> <p><code>(c_skip, c_out, c_in, c_noise)</code> in the input dtype.</p> Source code in <code>worldkernels/models/schedulers/trigflow.py</code> <pre><code>def trigflow_scaling(\n    trigflow_t: torch.Tensor, sigma_data: float = 1.0\n) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]:\n    r\"\"\"Rectified-flow preconditioning coefficients for a TrigFlow angle.\n\n    Computed in float64 and cast back, matching the reference exactly:\n    $$c_\\text{skip} = c_\\text{in} = \\frac{\\sigma_d}{\\cos t + \\sigma_d \\sin t},\\quad\n    c_\\text{out} = -\\sigma_d \\sin t \\cdot c_\\text{in} / \\sigma_d,\\quad\n    c_\\text{noise} = \\sigma_d \\sin t \\cdot c_\\text{in} / \\sigma_d$$\n\n    The denoiser evaluates \\(x_0 = c_\\text{skip} x_t + c_\\text{out}\\,\n    F(c_\\text{in} x_t, c_\\text{noise})\\).\n\n    Returns:\n        ``(c_skip, c_out, c_in, c_noise)`` in the input dtype.\n    \"\"\"\n    dtype = trigflow_t.dtype\n    t = trigflow_t.to(torch.float64)\n    denom = torch.cos(t) + sigma_data * torch.sin(t)\n    c_skip = sigma_data / denom\n    c_out = -sigma_data * torch.sin(t) / denom\n    c_in = sigma_data / denom\n    c_noise = sigma_data * torch.sin(t) / denom\n    return c_skip.to(dtype), c_out.to(dtype), c_in.to(dtype), c_noise.to(dtype)\n</code></pre>"},{"location":"api/worldkernels/models/schedulers/trigflow/#worldkernels.models.schedulers.trigflow.trigflow_step","title":"trigflow_step","text":"<pre><code>trigflow_step(x0_pred: Tensor, noise: Tensor, t_next: Tensor, sigma_data: float = 1.0) -&gt; Tensor\n</code></pre> <p>Re-noise the current \\(x_0\\) prediction to the next angle.</p> \\[x_{t'} = \\cos(t')\\,\\hat{x}_0/\\sigma_d + \\sin(t')\\,\\epsilon\\] Source code in <code>worldkernels/models/schedulers/trigflow.py</code> <pre><code>def trigflow_step(\n    x0_pred: torch.Tensor,\n    noise: torch.Tensor,\n    t_next: torch.Tensor,\n    sigma_data: float = 1.0,\n) -&gt; torch.Tensor:\n    r\"\"\"Re-noise the current \\(x_0\\) prediction to the next angle.\n\n    $$x_{t'} = \\cos(t')\\,\\hat{x}_0/\\sigma_d + \\sin(t')\\,\\epsilon$$\n    \"\"\"\n    return torch.cos(t_next) * x0_pred / sigma_data + torch.sin(t_next) * noise\n</code></pre>"},{"location":"api/worldkernels/models/wan/","title":"wan","text":""},{"location":"api/worldkernels/models/wan/#worldkernels.models.wan","title":"worldkernels.models.wan","text":"<p>Wan-family video-diffusion pipelines (diffusers-backed).</p> <p>Modules:</p> Name Description <code>pipeline_wan_i2v</code> <p>Wan image-to-video generator (diffusers-backed).</p>"},{"location":"api/worldkernels/models/wan/#worldkernels.models.wan.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WanI2VPipeline', 'WanLatent']\n</code></pre>"},{"location":"api/worldkernels/models/wan/#worldkernels.models.wan._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'WanI2VPipeline': ('worldkernels.models.wan.pipeline_wan_i2v', 'WanI2VPipeline'), 'WanLatent': ('worldkernels.models.wan.pipeline_wan_i2v', 'WanLatent')}\n</code></pre>"},{"location":"api/worldkernels/models/wan/#worldkernels.models.wan.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/models/wan/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/","title":"pipeline_wan_i2v","text":""},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v","title":"worldkernels.models.wan.pipeline_wan_i2v","text":"<p>Wan image-to-video generator (diffusers-backed).</p> <p>A <code>VideoGenerator</code>: loads the diffusers Wan components, swaps in the owned <code>FlowUniPCMultistepScheduler</code>, and produces a video clip per forward pass. Composed by <code>GeneratorWorld</code> to expose Wan as a world.</p> <p>The VAE is held in float32 (Wan's VAE is numerically sensitive); the transformer and text encoder use the requested dtype.</p>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent","title":"WanLatent","text":"<p>Conditioning for a Wan image-to-video rollout.</p> <p>Holds the conditioning frame and prompt embeddings; <code>latent</code> / <code>video</code> carry the most recent generation output so no recompute is needed.</p> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>class WanLatent:\n    r\"\"\"Conditioning for a Wan image-to-video rollout.\n\n    Holds the conditioning frame and prompt embeddings; ``latent`` / ``video``\n    carry the most recent generation output so no recompute is needed.\n    \"\"\"\n\n    __slots__ = (\"last_frame\", \"prompt_embeds\", \"negative_prompt_embeds\", \"latent\", \"video\")\n\n    def __init__(\n        self,\n        last_frame: torch.Tensor,\n        prompt_embeds: torch.Tensor,\n        negative_prompt_embeds: torch.Tensor | None = None,\n        latent: torch.Tensor | None = None,\n        video: torch.Tensor | None = None,\n    ) -&gt; None:\n        self.last_frame = last_frame\n        self.prompt_embeds = prompt_embeds\n        self.negative_prompt_embeds = negative_prompt_embeds\n        self.latent = latent\n        self.video = video\n\n    def clone(self) -&gt; \"WanLatent\":\n        return WanLatent(\n            last_frame=self.last_frame.clone(),\n            prompt_embeds=self.prompt_embeds.clone(),\n            negative_prompt_embeds=(\n                None if self.negative_prompt_embeds is None else self.negative_prompt_embeds.clone()\n            ),\n            latent=None if self.latent is None else self.latent.clone(),\n            video=None if self.video is None else self.video.clone(),\n        )\n\n    def to(self, device: Any) -&gt; \"WanLatent\":\n        return WanLatent(\n            last_frame=self.last_frame.to(device),\n            prompt_embeds=self.prompt_embeds.to(device),\n            negative_prompt_embeds=(\n                None\n                if self.negative_prompt_embeds is None\n                else self.negative_prompt_embeds.to(device)\n            ),\n            latent=None if self.latent is None else self.latent.to(device),\n            video=None if self.video is None else self.video.to(device),\n        )\n\n    @property\n    def nelement(self) -&gt; int:\n        n = self.last_frame.nelement() + self.prompt_embeds.nelement()\n        for t in (self.negative_prompt_embeds, self.latent, self.video):\n            if t is not None:\n                n += t.nelement()\n        return n\n\n    @property\n    def element_size(self) -&gt; int:\n        return self.last_frame.element_size()\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.__slots__","title":"__slots__  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>__slots__ = ('last_frame', 'prompt_embeds', 'negative_prompt_embeds', 'latent', 'video')\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.last_frame","title":"last_frame  <code>instance-attribute</code>","text":"<pre><code>last_frame = last_frame\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.prompt_embeds","title":"prompt_embeds  <code>instance-attribute</code>","text":"<pre><code>prompt_embeds = prompt_embeds\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.negative_prompt_embeds","title":"negative_prompt_embeds  <code>instance-attribute</code>","text":"<pre><code>negative_prompt_embeds = negative_prompt_embeds\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.latent","title":"latent  <code>instance-attribute</code>","text":"<pre><code>latent = latent\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.video","title":"video  <code>instance-attribute</code>","text":"<pre><code>video = video\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.nelement","title":"nelement  <code>property</code>","text":"<pre><code>nelement: int\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.element_size","title":"element_size  <code>property</code>","text":"<pre><code>element_size: int\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.__init__","title":"__init__","text":"<pre><code>__init__(last_frame: Tensor, prompt_embeds: Tensor, negative_prompt_embeds: Tensor | None = None, latent: Tensor | None = None, video: Tensor | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def __init__(\n    self,\n    last_frame: torch.Tensor,\n    prompt_embeds: torch.Tensor,\n    negative_prompt_embeds: torch.Tensor | None = None,\n    latent: torch.Tensor | None = None,\n    video: torch.Tensor | None = None,\n) -&gt; None:\n    self.last_frame = last_frame\n    self.prompt_embeds = prompt_embeds\n    self.negative_prompt_embeds = negative_prompt_embeds\n    self.latent = latent\n    self.video = video\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.clone","title":"clone","text":"<pre><code>clone() -&gt; 'WanLatent'\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def clone(self) -&gt; \"WanLatent\":\n    return WanLatent(\n        last_frame=self.last_frame.clone(),\n        prompt_embeds=self.prompt_embeds.clone(),\n        negative_prompt_embeds=(\n            None if self.negative_prompt_embeds is None else self.negative_prompt_embeds.clone()\n        ),\n        latent=None if self.latent is None else self.latent.clone(),\n        video=None if self.video is None else self.video.clone(),\n    )\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanLatent.to","title":"to","text":"<pre><code>to(device: Any) -&gt; 'WanLatent'\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def to(self, device: Any) -&gt; \"WanLatent\":\n    return WanLatent(\n        last_frame=self.last_frame.to(device),\n        prompt_embeds=self.prompt_embeds.to(device),\n        negative_prompt_embeds=(\n            None\n            if self.negative_prompt_embeds is None\n            else self.negative_prompt_embeds.to(device)\n        ),\n        latent=None if self.latent is None else self.latent.to(device),\n        video=None if self.video is None else self.video.to(device),\n    )\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline","title":"WanI2VPipeline","text":"<p>               Bases: <code>VideoGenerator</code></p> <p>Wan image-to-video generator driving diffusers components.</p> <p>Parameters:</p> Name Type Description Default <code>repo</code> <code>str</code> <p>diffusers-format Wan repo (e.g. <code>Wan-AI/Wan2.2-TI2V-5B-Diffusers</code>).</p> required <code>pipeline_class</code> <code>str</code> <p>diffusers pipeline class name to load.</p> <code>'WanImageToVideoPipeline'</code> <code>flow_shift</code> <code>float</code> <p>Flow-matching schedule shift for the scheduler.</p> <code>5.0</code> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>class WanI2VPipeline(VideoGenerator):\n    r\"\"\"Wan image-to-video generator driving diffusers components.\n\n    Args:\n        repo: diffusers-format Wan repo (e.g. ``Wan-AI/Wan2.2-TI2V-5B-Diffusers``).\n        pipeline_class: diffusers pipeline class name to load.\n        flow_shift: Flow-matching schedule shift for the scheduler.\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        repo: str,\n        pipeline_class: str = \"WanImageToVideoPipeline\",\n        flow_shift: float = 5.0,\n    ) -&gt; None:\n        self.repo = repo\n        self.pipeline_class = pipeline_class\n        self.flow_shift = flow_shift\n        self.device: str = \"cpu\"\n        self.dtype: torch.dtype = torch.float32\n        self._dp: Any = None\n        self._loaded = False\n\n    @property\n    def is_loaded(self) -&gt; bool:\n        return self._loaded\n\n    def load(self, device: str, dtype: torch.dtype) -&gt; None:\n        import diffusers\n        from diffusers import AutoencoderKLWan\n\n        from worldkernels.models.schedulers import FlowUniPCMultistepScheduler\n\n        self.device = device\n        self.dtype = dtype\n\n        log.info(\"Loading Wan components from %s\", self.repo)\n        vae = AutoencoderKLWan.from_pretrained(\n            self.repo, subfolder=\"vae\", torch_dtype=torch.float32\n        )\n        pipe_cls = getattr(diffusers, self.pipeline_class)\n        dp = pipe_cls.from_pretrained(self.repo, vae=vae, torch_dtype=dtype)\n        dp.scheduler = FlowUniPCMultistepScheduler.for_wan(flow_shift=self.flow_shift)\n        dp.to(device)\n\n        self._dp = dp\n        self._loaded = True\n        log.info(\n            \"WanI2VPipeline loaded on %s (%.1f GB VRAM)\",\n            device,\n            torch.cuda.memory_allocated() / 1e9 if torch.cuda.is_available() else 0.0,\n        )\n\n    def encode_prompt(self, prompt: str) -&gt; torch.Tensor:\n        embeds, _ = self._encode_text(prompt, None)\n        return embeds\n\n    def initial_conditioning(\n        self,\n        *,\n        prompt: str,\n        negative_prompt: str,\n        image: Any | None,\n        height: int,\n        width: int,\n        frames_per_step: int,\n        seed: int,\n    ) -&gt; WanLatent:\n        prompt_embeds, negative_prompt_embeds = self._encode_text(prompt, negative_prompt)\n        if image is not None:\n            last_frame = self._encode_image(image, height=height, width=width)\n        else:\n            last_frame = torch.zeros(1, 3, height, width, device=self.device, dtype=self.dtype)\n        return WanLatent(last_frame, prompt_embeds, negative_prompt_embeds)\n\n    def apply_prompt(self, conditioning: WanLatent, prompt_embeds: torch.Tensor) -&gt; WanLatent:\n        return WanLatent(\n            conditioning.last_frame, prompt_embeds, conditioning.negative_prompt_embeds\n        )\n\n    def generate(\n        self,\n        conditioning: WanLatent,\n        *,\n        num_steps: int,\n        guidance: float,\n        num_frames: int,\n        seed: int,\n    ) -&gt; GenerationResult:\n        assert self._dp is not None, \"Pipeline not loaded \u2014 call load() first\"\n        height, width = conditioning.last_frame.shape[-2], conditioning.last_frame.shape[-1]\n        gen_device = \"cpu\" if self.device == \"cpu\" else self.device\n        generator = torch.Generator(device=gen_device).manual_seed(seed)\n\n        with torch.no_grad():\n            result = self._dp(\n                image=self._to_pil(conditioning.last_frame),\n                prompt_embeds=conditioning.prompt_embeds,\n                negative_prompt_embeds=conditioning.negative_prompt_embeds,\n                height=height,\n                width=width,\n                num_frames=num_frames,\n                num_inference_steps=num_steps,\n                guidance_scale=guidance,\n                generator=generator,\n                output_type=\"latent\",\n            )\n        latent = result.frames if hasattr(result, \"frames\") else result[0]\n        if not torch.is_tensor(latent):\n            latent = latent[0]\n        return GenerationResult(latent=latent, video=self.decode(latent))\n\n    def advance(self, conditioning: WanLatent, next_image: torch.Tensor) -&gt; WanLatent:\n        return WanLatent(\n            next_image.to(self.dtype),\n            conditioning.prompt_embeds,\n            conditioning.negative_prompt_embeds,\n        )\n\n    def decode(self, latent: torch.Tensor) -&gt; torch.Tensor:\n        r\"\"\"VAE-decode a normalized latent to a video tensor in ``[-1, 1]``.\"\"\"\n        assert self._dp is not None, \"Pipeline not loaded \u2014 call load() first\"\n        vae = self._dp.vae\n        latent = latent.to(vae.dtype)\n        mean = torch.tensor(vae.config.latents_mean, device=latent.device, dtype=vae.dtype)\n        std = torch.tensor(vae.config.latents_std, device=latent.device, dtype=vae.dtype)\n        shape = (1, vae.config.z_dim, 1, 1, 1)\n        latent = latent * std.view(shape) + mean.view(shape)\n        with torch.no_grad():\n            return vae.decode(latent, return_dict=False)[0]\n\n    def profile_vram(self, *, height: int, width: int, num_frames: int) -&gt; float:\n        clip_bytes = 3 * height * width * num_frames * 4\n        latent_bytes = 16 * ((num_frames + 3) // 4) * (height // 8) * (width // 8) * 4\n        return (clip_bytes + latent_bytes) / (1024 * 1024) * 2.0 + 1024.0\n\n    def _encode_text(\n        self,\n        prompt: str,\n        negative_prompt: str | None,\n    ) -&gt; tuple[torch.Tensor, torch.Tensor | None]:\n        assert self._dp is not None, \"Pipeline not loaded \u2014 call load() first\"\n        prompt_embeds, negative_prompt_embeds = self._dp.encode_prompt(\n            prompt=prompt,\n            negative_prompt=negative_prompt,\n            do_classifier_free_guidance=negative_prompt is not None,\n            device=self.device,\n        )\n        return prompt_embeds, negative_prompt_embeds\n\n    def _encode_image(self, image: Any, *, height: int, width: int) -&gt; torch.Tensor:\n        import torchvision.transforms.functional as TF\n\n        if isinstance(image, str):\n            from PIL import Image\n\n            img = Image.open(image).convert(\"RGB\")\n            tensor = TF.to_tensor(img.resize((width, height)))\n        else:\n            tensor = torch.as_tensor(image, dtype=torch.float32)\n            if tensor.ndim == 3 and tensor.shape[0] != 3:\n                tensor = tensor.permute(2, 0, 1)\n            if tensor.ndim == 4:\n                tensor = tensor[0]\n        return tensor.clamp(0, 1).to(device=self.device, dtype=self.dtype).unsqueeze(0)\n\n    def _to_pil(self, last_frame: torch.Tensor) -&gt; \"Image\":\n        from PIL import Image\n\n        tensor = last_frame[0] if last_frame.ndim == 4 else last_frame\n        array = tensor.clamp(0, 1).mul(255).to(torch.uint8).permute(1, 2, 0).cpu().numpy()\n        return Image.fromarray(array)\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.repo","title":"repo  <code>instance-attribute</code>","text":"<pre><code>repo = repo\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.pipeline_class","title":"pipeline_class  <code>instance-attribute</code>","text":"<pre><code>pipeline_class = pipeline_class\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.flow_shift","title":"flow_shift  <code>instance-attribute</code>","text":"<pre><code>flow_shift = flow_shift\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: dtype = torch.float32\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline._dp","title":"_dp  <code>instance-attribute</code>","text":"<pre><code>_dp: Any = None\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline._loaded","title":"_loaded  <code>instance-attribute</code>","text":"<pre><code>_loaded = False\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.is_loaded","title":"is_loaded  <code>property</code>","text":"<pre><code>is_loaded: bool\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.__init__","title":"__init__","text":"<pre><code>__init__(*, repo: str, pipeline_class: str = 'WanImageToVideoPipeline', flow_shift: float = 5.0) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def __init__(\n    self,\n    *,\n    repo: str,\n    pipeline_class: str = \"WanImageToVideoPipeline\",\n    flow_shift: float = 5.0,\n) -&gt; None:\n    self.repo = repo\n    self.pipeline_class = pipeline_class\n    self.flow_shift = flow_shift\n    self.device: str = \"cpu\"\n    self.dtype: torch.dtype = torch.float32\n    self._dp: Any = None\n    self._loaded = False\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.load","title":"load","text":"<pre><code>load(device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def load(self, device: str, dtype: torch.dtype) -&gt; None:\n    import diffusers\n    from diffusers import AutoencoderKLWan\n\n    from worldkernels.models.schedulers import FlowUniPCMultistepScheduler\n\n    self.device = device\n    self.dtype = dtype\n\n    log.info(\"Loading Wan components from %s\", self.repo)\n    vae = AutoencoderKLWan.from_pretrained(\n        self.repo, subfolder=\"vae\", torch_dtype=torch.float32\n    )\n    pipe_cls = getattr(diffusers, self.pipeline_class)\n    dp = pipe_cls.from_pretrained(self.repo, vae=vae, torch_dtype=dtype)\n    dp.scheduler = FlowUniPCMultistepScheduler.for_wan(flow_shift=self.flow_shift)\n    dp.to(device)\n\n    self._dp = dp\n    self._loaded = True\n    log.info(\n        \"WanI2VPipeline loaded on %s (%.1f GB VRAM)\",\n        device,\n        torch.cuda.memory_allocated() / 1e9 if torch.cuda.is_available() else 0.0,\n    )\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.encode_prompt","title":"encode_prompt","text":"<pre><code>encode_prompt(prompt: str) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def encode_prompt(self, prompt: str) -&gt; torch.Tensor:\n    embeds, _ = self._encode_text(prompt, None)\n    return embeds\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.initial_conditioning","title":"initial_conditioning","text":"<pre><code>initial_conditioning(*, prompt: str, negative_prompt: str, image: Any | None, height: int, width: int, frames_per_step: int, seed: int) -&gt; WanLatent\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def initial_conditioning(\n    self,\n    *,\n    prompt: str,\n    negative_prompt: str,\n    image: Any | None,\n    height: int,\n    width: int,\n    frames_per_step: int,\n    seed: int,\n) -&gt; WanLatent:\n    prompt_embeds, negative_prompt_embeds = self._encode_text(prompt, negative_prompt)\n    if image is not None:\n        last_frame = self._encode_image(image, height=height, width=width)\n    else:\n        last_frame = torch.zeros(1, 3, height, width, device=self.device, dtype=self.dtype)\n    return WanLatent(last_frame, prompt_embeds, negative_prompt_embeds)\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.apply_prompt","title":"apply_prompt","text":"<pre><code>apply_prompt(conditioning: WanLatent, prompt_embeds: Tensor) -&gt; WanLatent\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def apply_prompt(self, conditioning: WanLatent, prompt_embeds: torch.Tensor) -&gt; WanLatent:\n    return WanLatent(\n        conditioning.last_frame, prompt_embeds, conditioning.negative_prompt_embeds\n    )\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.generate","title":"generate","text":"<pre><code>generate(conditioning: WanLatent, *, num_steps: int, guidance: float, num_frames: int, seed: int) -&gt; GenerationResult\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def generate(\n    self,\n    conditioning: WanLatent,\n    *,\n    num_steps: int,\n    guidance: float,\n    num_frames: int,\n    seed: int,\n) -&gt; GenerationResult:\n    assert self._dp is not None, \"Pipeline not loaded \u2014 call load() first\"\n    height, width = conditioning.last_frame.shape[-2], conditioning.last_frame.shape[-1]\n    gen_device = \"cpu\" if self.device == \"cpu\" else self.device\n    generator = torch.Generator(device=gen_device).manual_seed(seed)\n\n    with torch.no_grad():\n        result = self._dp(\n            image=self._to_pil(conditioning.last_frame),\n            prompt_embeds=conditioning.prompt_embeds,\n            negative_prompt_embeds=conditioning.negative_prompt_embeds,\n            height=height,\n            width=width,\n            num_frames=num_frames,\n            num_inference_steps=num_steps,\n            guidance_scale=guidance,\n            generator=generator,\n            output_type=\"latent\",\n        )\n    latent = result.frames if hasattr(result, \"frames\") else result[0]\n    if not torch.is_tensor(latent):\n        latent = latent[0]\n    return GenerationResult(latent=latent, video=self.decode(latent))\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.advance","title":"advance","text":"<pre><code>advance(conditioning: WanLatent, next_image: Tensor) -&gt; WanLatent\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def advance(self, conditioning: WanLatent, next_image: torch.Tensor) -&gt; WanLatent:\n    return WanLatent(\n        next_image.to(self.dtype),\n        conditioning.prompt_embeds,\n        conditioning.negative_prompt_embeds,\n    )\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.decode","title":"decode","text":"<pre><code>decode(latent: Tensor) -&gt; Tensor\n</code></pre> <p>VAE-decode a normalized latent to a video tensor in <code>[-1, 1]</code>.</p> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def decode(self, latent: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"VAE-decode a normalized latent to a video tensor in ``[-1, 1]``.\"\"\"\n    assert self._dp is not None, \"Pipeline not loaded \u2014 call load() first\"\n    vae = self._dp.vae\n    latent = latent.to(vae.dtype)\n    mean = torch.tensor(vae.config.latents_mean, device=latent.device, dtype=vae.dtype)\n    std = torch.tensor(vae.config.latents_std, device=latent.device, dtype=vae.dtype)\n    shape = (1, vae.config.z_dim, 1, 1, 1)\n    latent = latent * std.view(shape) + mean.view(shape)\n    with torch.no_grad():\n        return vae.decode(latent, return_dict=False)[0]\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(*, height: int, width: int, num_frames: int) -&gt; float\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def profile_vram(self, *, height: int, width: int, num_frames: int) -&gt; float:\n    clip_bytes = 3 * height * width * num_frames * 4\n    latent_bytes = 16 * ((num_frames + 3) // 4) * (height // 8) * (width // 8) * 4\n    return (clip_bytes + latent_bytes) / (1024 * 1024) * 2.0 + 1024.0\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline._encode_text","title":"_encode_text","text":"<pre><code>_encode_text(prompt: str, negative_prompt: str | None) -&gt; tuple[Tensor, Tensor | None]\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def _encode_text(\n    self,\n    prompt: str,\n    negative_prompt: str | None,\n) -&gt; tuple[torch.Tensor, torch.Tensor | None]:\n    assert self._dp is not None, \"Pipeline not loaded \u2014 call load() first\"\n    prompt_embeds, negative_prompt_embeds = self._dp.encode_prompt(\n        prompt=prompt,\n        negative_prompt=negative_prompt,\n        do_classifier_free_guidance=negative_prompt is not None,\n        device=self.device,\n    )\n    return prompt_embeds, negative_prompt_embeds\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline._encode_image","title":"_encode_image","text":"<pre><code>_encode_image(image: Any, *, height: int, width: int) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def _encode_image(self, image: Any, *, height: int, width: int) -&gt; torch.Tensor:\n    import torchvision.transforms.functional as TF\n\n    if isinstance(image, str):\n        from PIL import Image\n\n        img = Image.open(image).convert(\"RGB\")\n        tensor = TF.to_tensor(img.resize((width, height)))\n    else:\n        tensor = torch.as_tensor(image, dtype=torch.float32)\n        if tensor.ndim == 3 and tensor.shape[0] != 3:\n            tensor = tensor.permute(2, 0, 1)\n        if tensor.ndim == 4:\n            tensor = tensor[0]\n    return tensor.clamp(0, 1).to(device=self.device, dtype=self.dtype).unsqueeze(0)\n</code></pre>"},{"location":"api/worldkernels/models/wan/pipeline_wan_i2v/#worldkernels.models.wan.pipeline_wan_i2v.WanI2VPipeline._to_pil","title":"_to_pil","text":"<pre><code>_to_pil(last_frame: Tensor) -&gt; 'Image'\n</code></pre> Source code in <code>worldkernels/models/wan/pipeline_wan_i2v.py</code> <pre><code>def _to_pil(self, last_frame: torch.Tensor) -&gt; \"Image\":\n    from PIL import Image\n\n    tensor = last_frame[0] if last_frame.ndim == 4 else last_frame\n    array = tensor.clamp(0, 1).mul(255).to(torch.uint8).permute(1, 2, 0).cpu().numpy()\n    return Image.fromarray(array)\n</code></pre>"},{"location":"api/worldkernels/platforms/","title":"platforms","text":""},{"location":"api/worldkernels/platforms/#worldkernels.platforms","title":"worldkernels.platforms","text":"<p>Hardware-platform abstraction. <code>current_platform()</code> resolves the active device family.</p> <p>Modules:</p> Name Description <code>cpu</code> <p>CPU fallback platform.</p> <code>cuda</code> <p>NVIDIA CUDA platform.</p> <code>interface</code> <p>Hardware-platform abstraction.</p> <code>rocm</code> <p>AMD ROCm platform.</p>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Platform', 'CudaPlatform', 'RocmPlatform', 'CpuPlatform', 'current_platform']\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms._PLATFORMS","title":"_PLATFORMS  <code>module-attribute</code>","text":"<pre><code>_PLATFORMS: tuple[type[Platform], ...] = (CudaPlatform, RocmPlatform, CpuPlatform)\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms._active","title":"_active  <code>module-attribute</code>","text":"<pre><code>_active: Platform | None = None\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform","title":"CpuPlatform","text":"<p>               Bases: <code>Platform</code></p> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>class CpuPlatform(Platform):\n    name: ClassVar[str] = \"cpu\"\n    device_type: ClassVar[str] = \"cpu\"\n\n    @classmethod\n    def is_available(cls) -&gt; bool:\n        return True\n\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        import torch\n\n        return torch.float32\n\n    def default_attention_backend(self) -&gt; str:\n        return \"sdpa\"\n\n    def device_count(self) -&gt; int:\n        return 1\n\n    def synchronize(self) -&gt; None:\n        return None\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform.is_available","title":"is_available  <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>@classmethod\ndef is_available(cls) -&gt; bool:\n    return True\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform.default_dtype","title":"default_dtype","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def default_dtype(self) -&gt; \"torch.dtype\":\n    import torch\n\n    return torch.float32\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform.default_attention_backend","title":"default_attention_backend","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def default_attention_backend(self) -&gt; str:\n    return \"sdpa\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform.device_count","title":"device_count","text":"<pre><code>device_count() -&gt; int\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def device_count(self) -&gt; int:\n    return 1\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CpuPlatform.synchronize","title":"synchronize","text":"<pre><code>synchronize() -&gt; None\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def synchronize(self) -&gt; None:\n    return None\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform","title":"CudaPlatform","text":"<p>               Bases: <code>Platform</code></p> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>class CudaPlatform(Platform):\n    name: ClassVar[str] = \"cuda\"\n    device_type: ClassVar[str] = \"cuda\"\n\n    @classmethod\n    def is_available(cls) -&gt; bool:\n        try:\n            import torch\n        except ImportError:\n            return False\n        return torch.cuda.is_available() and not getattr(torch.version, \"hip\", None)\n\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        import torch\n\n        major = torch.cuda.get_device_capability()[0]\n        return torch.bfloat16 if major &gt;= 8 else torch.float16\n\n    def default_attention_backend(self) -&gt; str:\n        try:\n            import flash_attn  # noqa: F401\n        except ImportError:\n            return \"sdpa\"\n        return \"flash\"\n\n    def device_count(self) -&gt; int:\n        import torch\n\n        return torch.cuda.device_count()\n\n    def synchronize(self) -&gt; None:\n        import torch\n\n        torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform.is_available","title":"is_available  <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>@classmethod\ndef is_available(cls) -&gt; bool:\n    try:\n        import torch\n    except ImportError:\n        return False\n    return torch.cuda.is_available() and not getattr(torch.version, \"hip\", None)\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform.default_dtype","title":"default_dtype","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def default_dtype(self) -&gt; \"torch.dtype\":\n    import torch\n\n    major = torch.cuda.get_device_capability()[0]\n    return torch.bfloat16 if major &gt;= 8 else torch.float16\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform.default_attention_backend","title":"default_attention_backend","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def default_attention_backend(self) -&gt; str:\n    try:\n        import flash_attn  # noqa: F401\n    except ImportError:\n        return \"sdpa\"\n    return \"flash\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform.device_count","title":"device_count","text":"<pre><code>device_count() -&gt; int\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def device_count(self) -&gt; int:\n    import torch\n\n    return torch.cuda.device_count()\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.CudaPlatform.synchronize","title":"synchronize","text":"<pre><code>synchronize() -&gt; None\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def synchronize(self) -&gt; None:\n    import torch\n\n    torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform","title":"Platform","text":"<p>               Bases: <code>ABC</code></p> <p>Device-family capabilities and defaults.</p> <p>Subclasses declare <code>name</code> / <code>device_type</code> and implement the probes.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>class Platform(ABC):\n    r\"\"\"Device-family capabilities and defaults.\n\n    Subclasses declare ``name`` / ``device_type`` and implement the probes.\n    \"\"\"\n\n    name: ClassVar[str]\n    device_type: ClassVar[str]\n\n    @classmethod\n    @abstractmethod\n    def is_available(cls) -&gt; bool:\n        r\"\"\"Whether this platform is usable in the current process.\"\"\"\n\n    @abstractmethod\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        r\"\"\"Preferred compute precision for this device family.\"\"\"\n\n    @abstractmethod\n    def default_attention_backend(self) -&gt; str:\n        r\"\"\"Name of the best available attention backend.\"\"\"\n\n    @abstractmethod\n    def device_count(self) -&gt; int:\n        r\"\"\"Number of visible devices of this family.\"\"\"\n\n    @abstractmethod\n    def synchronize(self) -&gt; None:\n        r\"\"\"Block until all queued device work completes.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform.is_available","title":"is_available  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> <p>Whether this platform is usable in the current process.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@classmethod\n@abstractmethod\ndef is_available(cls) -&gt; bool:\n    r\"\"\"Whether this platform is usable in the current process.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform.default_dtype","title":"default_dtype  <code>abstractmethod</code>","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> <p>Preferred compute precision for this device family.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef default_dtype(self) -&gt; \"torch.dtype\":\n    r\"\"\"Preferred compute precision for this device family.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform.default_attention_backend","title":"default_attention_backend  <code>abstractmethod</code>","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> <p>Name of the best available attention backend.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef default_attention_backend(self) -&gt; str:\n    r\"\"\"Name of the best available attention backend.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform.device_count","title":"device_count  <code>abstractmethod</code>","text":"<pre><code>device_count() -&gt; int\n</code></pre> <p>Number of visible devices of this family.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef device_count(self) -&gt; int:\n    r\"\"\"Number of visible devices of this family.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.Platform.synchronize","title":"synchronize  <code>abstractmethod</code>","text":"<pre><code>synchronize() -&gt; None\n</code></pre> <p>Block until all queued device work completes.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef synchronize(self) -&gt; None:\n    r\"\"\"Block until all queued device work completes.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform","title":"RocmPlatform","text":"<p>               Bases: <code>Platform</code></p> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>class RocmPlatform(Platform):\n    name: ClassVar[str] = \"rocm\"\n    device_type: ClassVar[str] = \"cuda\"\n\n    @classmethod\n    def is_available(cls) -&gt; bool:\n        try:\n            import torch\n        except ImportError:\n            return False\n        return torch.cuda.is_available() and bool(getattr(torch.version, \"hip\", None))\n\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        import torch\n\n        return torch.bfloat16\n\n    def default_attention_backend(self) -&gt; str:\n        return \"sdpa\"\n\n    def device_count(self) -&gt; int:\n        import torch\n\n        return torch.cuda.device_count()\n\n    def synchronize(self) -&gt; None:\n        import torch\n\n        torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'rocm'\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform.is_available","title":"is_available  <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>@classmethod\ndef is_available(cls) -&gt; bool:\n    try:\n        import torch\n    except ImportError:\n        return False\n    return torch.cuda.is_available() and bool(getattr(torch.version, \"hip\", None))\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform.default_dtype","title":"default_dtype","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def default_dtype(self) -&gt; \"torch.dtype\":\n    import torch\n\n    return torch.bfloat16\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform.default_attention_backend","title":"default_attention_backend","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def default_attention_backend(self) -&gt; str:\n    return \"sdpa\"\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform.device_count","title":"device_count","text":"<pre><code>device_count() -&gt; int\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def device_count(self) -&gt; int:\n    import torch\n\n    return torch.cuda.device_count()\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.RocmPlatform.synchronize","title":"synchronize","text":"<pre><code>synchronize() -&gt; None\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def synchronize(self) -&gt; None:\n    import torch\n\n    torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/platforms/#worldkernels.platforms.current_platform","title":"current_platform","text":"<pre><code>current_platform() -&gt; Platform\n</code></pre> <p>Return the active platform, probing devices once and caching the result.</p> Source code in <code>worldkernels/platforms/__init__.py</code> <pre><code>def current_platform() -&gt; Platform:\n    r\"\"\"Return the active platform, probing devices once and caching the result.\"\"\"\n    global _active\n    if _active is None:\n        for platform_cls in _PLATFORMS:\n            if platform_cls.is_available():\n                _active = platform_cls()\n                break\n        else:\n            _active = CpuPlatform()\n    return _active\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/","title":"cpu","text":""},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu","title":"worldkernels.platforms.cpu","text":"<p>CPU fallback platform.</p>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['CpuPlatform']\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform","title":"CpuPlatform","text":"<p>               Bases: <code>Platform</code></p> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>class CpuPlatform(Platform):\n    name: ClassVar[str] = \"cpu\"\n    device_type: ClassVar[str] = \"cpu\"\n\n    @classmethod\n    def is_available(cls) -&gt; bool:\n        return True\n\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        import torch\n\n        return torch.float32\n\n    def default_attention_backend(self) -&gt; str:\n        return \"sdpa\"\n\n    def device_count(self) -&gt; int:\n        return 1\n\n    def synchronize(self) -&gt; None:\n        return None\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform.is_available","title":"is_available  <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>@classmethod\ndef is_available(cls) -&gt; bool:\n    return True\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform.default_dtype","title":"default_dtype","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def default_dtype(self) -&gt; \"torch.dtype\":\n    import torch\n\n    return torch.float32\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform.default_attention_backend","title":"default_attention_backend","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def default_attention_backend(self) -&gt; str:\n    return \"sdpa\"\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform.device_count","title":"device_count","text":"<pre><code>device_count() -&gt; int\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def device_count(self) -&gt; int:\n    return 1\n</code></pre>"},{"location":"api/worldkernels/platforms/cpu/#worldkernels.platforms.cpu.CpuPlatform.synchronize","title":"synchronize","text":"<pre><code>synchronize() -&gt; None\n</code></pre> Source code in <code>worldkernels/platforms/cpu.py</code> <pre><code>def synchronize(self) -&gt; None:\n    return None\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/","title":"cuda","text":""},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda","title":"worldkernels.platforms.cuda","text":"<p>NVIDIA CUDA platform.</p>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['CudaPlatform']\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform","title":"CudaPlatform","text":"<p>               Bases: <code>Platform</code></p> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>class CudaPlatform(Platform):\n    name: ClassVar[str] = \"cuda\"\n    device_type: ClassVar[str] = \"cuda\"\n\n    @classmethod\n    def is_available(cls) -&gt; bool:\n        try:\n            import torch\n        except ImportError:\n            return False\n        return torch.cuda.is_available() and not getattr(torch.version, \"hip\", None)\n\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        import torch\n\n        major = torch.cuda.get_device_capability()[0]\n        return torch.bfloat16 if major &gt;= 8 else torch.float16\n\n    def default_attention_backend(self) -&gt; str:\n        try:\n            import flash_attn  # noqa: F401\n        except ImportError:\n            return \"sdpa\"\n        return \"flash\"\n\n    def device_count(self) -&gt; int:\n        import torch\n\n        return torch.cuda.device_count()\n\n    def synchronize(self) -&gt; None:\n        import torch\n\n        torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform.is_available","title":"is_available  <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>@classmethod\ndef is_available(cls) -&gt; bool:\n    try:\n        import torch\n    except ImportError:\n        return False\n    return torch.cuda.is_available() and not getattr(torch.version, \"hip\", None)\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform.default_dtype","title":"default_dtype","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def default_dtype(self) -&gt; \"torch.dtype\":\n    import torch\n\n    major = torch.cuda.get_device_capability()[0]\n    return torch.bfloat16 if major &gt;= 8 else torch.float16\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform.default_attention_backend","title":"default_attention_backend","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def default_attention_backend(self) -&gt; str:\n    try:\n        import flash_attn  # noqa: F401\n    except ImportError:\n        return \"sdpa\"\n    return \"flash\"\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform.device_count","title":"device_count","text":"<pre><code>device_count() -&gt; int\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def device_count(self) -&gt; int:\n    import torch\n\n    return torch.cuda.device_count()\n</code></pre>"},{"location":"api/worldkernels/platforms/cuda/#worldkernels.platforms.cuda.CudaPlatform.synchronize","title":"synchronize","text":"<pre><code>synchronize() -&gt; None\n</code></pre> Source code in <code>worldkernels/platforms/cuda.py</code> <pre><code>def synchronize(self) -&gt; None:\n    import torch\n\n    torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/","title":"interface","text":""},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface","title":"worldkernels.platforms.interface","text":"<p>Hardware-platform abstraction.</p> <p>A <code>Platform</code> answers device-specific questions \u2014 default precision, default attention backend, device count, synchronization \u2014 so the rest of the engine never branches on CUDA / ROCm / CPU directly.</p>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Platform']\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform","title":"Platform","text":"<p>               Bases: <code>ABC</code></p> <p>Device-family capabilities and defaults.</p> <p>Subclasses declare <code>name</code> / <code>device_type</code> and implement the probes.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>class Platform(ABC):\n    r\"\"\"Device-family capabilities and defaults.\n\n    Subclasses declare ``name`` / ``device_type`` and implement the probes.\n    \"\"\"\n\n    name: ClassVar[str]\n    device_type: ClassVar[str]\n\n    @classmethod\n    @abstractmethod\n    def is_available(cls) -&gt; bool:\n        r\"\"\"Whether this platform is usable in the current process.\"\"\"\n\n    @abstractmethod\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        r\"\"\"Preferred compute precision for this device family.\"\"\"\n\n    @abstractmethod\n    def default_attention_backend(self) -&gt; str:\n        r\"\"\"Name of the best available attention backend.\"\"\"\n\n    @abstractmethod\n    def device_count(self) -&gt; int:\n        r\"\"\"Number of visible devices of this family.\"\"\"\n\n    @abstractmethod\n    def synchronize(self) -&gt; None:\n        r\"\"\"Block until all queued device work completes.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform.is_available","title":"is_available  <code>abstractmethod</code> <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> <p>Whether this platform is usable in the current process.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@classmethod\n@abstractmethod\ndef is_available(cls) -&gt; bool:\n    r\"\"\"Whether this platform is usable in the current process.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform.default_dtype","title":"default_dtype  <code>abstractmethod</code>","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> <p>Preferred compute precision for this device family.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef default_dtype(self) -&gt; \"torch.dtype\":\n    r\"\"\"Preferred compute precision for this device family.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform.default_attention_backend","title":"default_attention_backend  <code>abstractmethod</code>","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> <p>Name of the best available attention backend.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef default_attention_backend(self) -&gt; str:\n    r\"\"\"Name of the best available attention backend.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform.device_count","title":"device_count  <code>abstractmethod</code>","text":"<pre><code>device_count() -&gt; int\n</code></pre> <p>Number of visible devices of this family.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef device_count(self) -&gt; int:\n    r\"\"\"Number of visible devices of this family.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/interface/#worldkernels.platforms.interface.Platform.synchronize","title":"synchronize  <code>abstractmethod</code>","text":"<pre><code>synchronize() -&gt; None\n</code></pre> <p>Block until all queued device work completes.</p> Source code in <code>worldkernels/platforms/interface.py</code> <pre><code>@abstractmethod\ndef synchronize(self) -&gt; None:\n    r\"\"\"Block until all queued device work completes.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/","title":"rocm","text":""},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm","title":"worldkernels.platforms.rocm","text":"<p>AMD ROCm platform.</p>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RocmPlatform']\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform","title":"RocmPlatform","text":"<p>               Bases: <code>Platform</code></p> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>class RocmPlatform(Platform):\n    name: ClassVar[str] = \"rocm\"\n    device_type: ClassVar[str] = \"cuda\"\n\n    @classmethod\n    def is_available(cls) -&gt; bool:\n        try:\n            import torch\n        except ImportError:\n            return False\n        return torch.cuda.is_available() and bool(getattr(torch.version, \"hip\", None))\n\n    def default_dtype(self) -&gt; \"torch.dtype\":\n        import torch\n\n        return torch.bfloat16\n\n    def default_attention_backend(self) -&gt; str:\n        return \"sdpa\"\n\n    def device_count(self) -&gt; int:\n        import torch\n\n        return torch.cuda.device_count()\n\n    def synchronize(self) -&gt; None:\n        import torch\n\n        torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'rocm'\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform.device_type","title":"device_type  <code>class-attribute</code>","text":"<pre><code>device_type: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform.is_available","title":"is_available  <code>classmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>@classmethod\ndef is_available(cls) -&gt; bool:\n    try:\n        import torch\n    except ImportError:\n        return False\n    return torch.cuda.is_available() and bool(getattr(torch.version, \"hip\", None))\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform.default_dtype","title":"default_dtype","text":"<pre><code>default_dtype() -&gt; 'torch.dtype'\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def default_dtype(self) -&gt; \"torch.dtype\":\n    import torch\n\n    return torch.bfloat16\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform.default_attention_backend","title":"default_attention_backend","text":"<pre><code>default_attention_backend() -&gt; str\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def default_attention_backend(self) -&gt; str:\n    return \"sdpa\"\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform.device_count","title":"device_count","text":"<pre><code>device_count() -&gt; int\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def device_count(self) -&gt; int:\n    import torch\n\n    return torch.cuda.device_count()\n</code></pre>"},{"location":"api/worldkernels/platforms/rocm/#worldkernels.platforms.rocm.RocmPlatform.synchronize","title":"synchronize","text":"<pre><code>synchronize() -&gt; None\n</code></pre> Source code in <code>worldkernels/platforms/rocm.py</code> <pre><code>def synchronize(self) -&gt; None:\n    import torch\n\n    torch.cuda.synchronize()\n</code></pre>"},{"location":"api/worldkernels/plugins/","title":"plugins","text":""},{"location":"api/worldkernels/plugins/#worldkernels.plugins","title":"worldkernels.plugins","text":"<p>Entry-point plugin system for WorldKernels.</p>"},{"location":"api/worldkernels/runtime/","title":"runtime","text":""},{"location":"api/worldkernels/runtime/#worldkernels.runtime","title":"worldkernels.runtime","text":"<p>Modules:</p> Name Description <code>attention</code> <p>Pluggable attention backends and selection.</p> <code>backends</code> <code>cache</code> <p>Denoise-step caching backends.</p> <code>compile</code> <p>Compilation backends: regional torch.compile and CUDA graph capture.</p> <code>connectors</code> <p>Inter-stage data transport for the world model pipeline.</p> <code>envs</code> <p>Per-model uv venv materialization under <code>WORLDKERNELS_HOME/envs/</code>.</p> <code>executor</code> <p>World model executor: stage-disaggregated hot path.</p> <code>forward_context</code> <p>Thread-local forward context.</p> <code>ipc</code> <p>Tensor IPC across the engine \u2194 worker boundary.</p> <code>locks</code> <p>Per-model lockfile storage under <code>WORLDKERNELS_HOME/locks/</code>.</p> <code>memory</code> <p>Memory subsystem: block allocation, KV paging, buffer pools, offloading.</p> <code>metrics</code> <p>Prometheus metrics for the serving runtime.</p> <code>quantization</code> <p>Weight quantization registry and schemes.</p> <code>resolver</code> <p>Dependency resolver: decide shared vs isolated tier.</p> <code>rpc</code> <p>Length-prefixed msgpack RPC over a Unix domain socket.</p> <code>stages</code> <p>Stage definitions for the world model execution pipeline.</p> <code>worker</code> <p>Worker subprocess entrypoint for isolated models.</p>"},{"location":"api/worldkernels/runtime/attention/","title":"attention","text":""},{"location":"api/worldkernels/runtime/attention/#worldkernels.runtime.attention","title":"worldkernels.runtime.attention","text":"<p>Pluggable attention backends and selection.</p> <p>Modules:</p> Name Description <code>backend</code> <p>Pluggable attention backend interface.</p> <code>backends</code> <p>Attention backend implementations.</p> <code>selector</code> <p>Attention backend selection.</p>"},{"location":"api/worldkernels/runtime/attention/#worldkernels.runtime.attention.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['AttentionBackend', 'AttentionMetadata', 'SDPABackend', 'FlashAttentionBackend', 'select_attention_backend']\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/#worldkernels.runtime.attention._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'AttentionBackend': ('worldkernels.runtime.attention.backend', 'AttentionBackend'), 'AttentionMetadata': ('worldkernels.runtime.attention.backend', 'AttentionMetadata'), 'SDPABackend': ('worldkernels.runtime.attention.backends.sdpa', 'SDPABackend'), 'FlashAttentionBackend': ('worldkernels.runtime.attention.backends.flash', 'FlashAttentionBackend'), 'select_attention_backend': ('worldkernels.runtime.attention.selector', 'select_attention_backend')}\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/#worldkernels.runtime.attention.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/runtime/attention/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/","title":"backend","text":""},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend","title":"worldkernels.runtime.attention.backend","text":"<p>Pluggable attention backend interface.</p> <p>A backend computes scaled dot-product attention for query/key/value tensors in the <code>(batch, seq, heads, head_dim)</code> layout \u2014 the layout diffusers and flash-attn use. Backends are selected per hardware by <code>select_attention_backend()</code> and read from the active <code>ForwardContext</code>.</p>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['AttentionMetadata', 'AttentionBackend']\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.AttentionMetadata","title":"AttentionMetadata  <code>dataclass</code>","text":"<p>Per-forward attention state threaded via the forward context.</p> <p>Parameters:</p> Name Type Description Default <code>is_causal</code> <code>bool</code> <p>Whether the attention mask is causal (streaming worlds).</p> <code>False</code> <code>scale</code> <code>float | None</code> <p>Softmax scale; <code>None</code> uses \\(1/\\sqrt{d}\\).</p> <code>None</code> Source code in <code>worldkernels/runtime/attention/backend.py</code> <pre><code>@dataclass\nclass AttentionMetadata:\n    r\"\"\"Per-forward attention state threaded via the forward context.\n\n    Args:\n        is_causal: Whether the attention mask is causal (streaming worlds).\n        scale: Softmax scale; ``None`` uses \\(1/\\sqrt{d}\\).\n    \"\"\"\n\n    is_causal: bool = False\n    scale: float | None = None\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.AttentionMetadata.is_causal","title":"is_causal  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>is_causal: bool = False\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.AttentionMetadata.scale","title":"scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>scale: float | None = None\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.AttentionMetadata.__init__","title":"__init__","text":"<pre><code>__init__(is_causal: bool = False, scale: float | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.AttentionBackend","title":"AttentionBackend","text":"<p>               Bases: <code>ABC</code></p> <p>Computes attention over <code>(batch, seq, heads, head_dim)</code> tensors.</p> Source code in <code>worldkernels/runtime/attention/backend.py</code> <pre><code>class AttentionBackend(ABC):\n    r\"\"\"Computes attention over ``(batch, seq, heads, head_dim)`` tensors.\"\"\"\n\n    name: ClassVar[str]\n\n    @abstractmethod\n    def forward(\n        self,\n        query: \"torch.Tensor\",\n        key: \"torch.Tensor\",\n        value: \"torch.Tensor\",\n        *,\n        is_causal: bool = False,\n        scale: float | None = None,\n    ) -&gt; \"torch.Tensor\":\n        r\"\"\"Return the attention output in ``(batch, seq, heads, head_dim)`` layout.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.AttentionBackend.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backend/#worldkernels.runtime.attention.backend.AttentionBackend.forward","title":"forward  <code>abstractmethod</code>","text":"<pre><code>forward(query: 'torch.Tensor', key: 'torch.Tensor', value: 'torch.Tensor', *, is_causal: bool = False, scale: float | None = None) -&gt; 'torch.Tensor'\n</code></pre> <p>Return the attention output in <code>(batch, seq, heads, head_dim)</code> layout.</p> Source code in <code>worldkernels/runtime/attention/backend.py</code> <pre><code>@abstractmethod\ndef forward(\n    self,\n    query: \"torch.Tensor\",\n    key: \"torch.Tensor\",\n    value: \"torch.Tensor\",\n    *,\n    is_causal: bool = False,\n    scale: float | None = None,\n) -&gt; \"torch.Tensor\":\n    r\"\"\"Return the attention output in ``(batch, seq, heads, head_dim)`` layout.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/","title":"backends","text":""},{"location":"api/worldkernels/runtime/attention/backends/#worldkernels.runtime.attention.backends","title":"worldkernels.runtime.attention.backends","text":"<p>Attention backend implementations.</p> <p>Modules:</p> Name Description <code>flash</code> <p>FlashAttention backend.</p> <code>sdpa</code> <p>PyTorch scaled-dot-product-attention backend.</p>"},{"location":"api/worldkernels/runtime/attention/backends/#worldkernels.runtime.attention.backends.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['SDPABackend', 'FlashAttentionBackend']\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/#worldkernels.runtime.attention.backends._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'SDPABackend': ('worldkernels.runtime.attention.backends.sdpa', 'SDPABackend'), 'FlashAttentionBackend': ('worldkernels.runtime.attention.backends.flash', 'FlashAttentionBackend')}\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/#worldkernels.runtime.attention.backends.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/runtime/attention/backends/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/flash/","title":"flash","text":""},{"location":"api/worldkernels/runtime/attention/backends/flash/#worldkernels.runtime.attention.backends.flash","title":"worldkernels.runtime.attention.backends.flash","text":"<p>FlashAttention backend.</p> <p>Fast, low-memory attention on Ampere+ GPUs. Requires the optional <code>flash-attn</code> package; absent it, the backend stays importable and raises an actionable error only when used.</p>"},{"location":"api/worldkernels/runtime/attention/backends/flash/#worldkernels.runtime.attention.backends.flash.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['FlashAttentionBackend']\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/flash/#worldkernels.runtime.attention.backends.flash._flash_attn","title":"_flash_attn  <code>module-attribute</code>","text":"<pre><code>_flash_attn = optional_import('flash_attn', 'flash-attn')\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/flash/#worldkernels.runtime.attention.backends.flash.FlashAttentionBackend","title":"FlashAttentionBackend","text":"<p>               Bases: <code>AttentionBackend</code></p> <p>Attention via <code>flash_attn.flash_attn_func</code> (Ampere+ GPUs).</p> Source code in <code>worldkernels/runtime/attention/backends/flash.py</code> <pre><code>class FlashAttentionBackend(AttentionBackend):\n    r\"\"\"Attention via ``flash_attn.flash_attn_func`` (Ampere+ GPUs).\"\"\"\n\n    name: ClassVar[str] = \"flash\"\n\n    @staticmethod\n    def is_available() -&gt; bool:\n        from worldkernels.utils.import_utils import PlaceholderModule\n\n        return not isinstance(_flash_attn, PlaceholderModule)\n\n    def forward(\n        self,\n        query: torch.Tensor,\n        key: torch.Tensor,\n        value: torch.Tensor,\n        *,\n        is_causal: bool = False,\n        scale: float | None = None,\n    ) -&gt; torch.Tensor:\n        return _flash_attn.flash_attn_func(query, key, value, causal=is_causal, softmax_scale=scale)\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/flash/#worldkernels.runtime.attention.backends.flash.FlashAttentionBackend.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'flash'\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/flash/#worldkernels.runtime.attention.backends.flash.FlashAttentionBackend.is_available","title":"is_available  <code>staticmethod</code>","text":"<pre><code>is_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/runtime/attention/backends/flash.py</code> <pre><code>@staticmethod\ndef is_available() -&gt; bool:\n    from worldkernels.utils.import_utils import PlaceholderModule\n\n    return not isinstance(_flash_attn, PlaceholderModule)\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/flash/#worldkernels.runtime.attention.backends.flash.FlashAttentionBackend.forward","title":"forward","text":"<pre><code>forward(query: Tensor, key: Tensor, value: Tensor, *, is_causal: bool = False, scale: float | None = None) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/runtime/attention/backends/flash.py</code> <pre><code>def forward(\n    self,\n    query: torch.Tensor,\n    key: torch.Tensor,\n    value: torch.Tensor,\n    *,\n    is_causal: bool = False,\n    scale: float | None = None,\n) -&gt; torch.Tensor:\n    return _flash_attn.flash_attn_func(query, key, value, causal=is_causal, softmax_scale=scale)\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/sdpa/","title":"sdpa","text":""},{"location":"api/worldkernels/runtime/attention/backends/sdpa/#worldkernels.runtime.attention.backends.sdpa","title":"worldkernels.runtime.attention.backends.sdpa","text":"<p>PyTorch scaled-dot-product-attention backend.</p> <p>The universal fallback: works on CPU and every GPU, no extra dependency.</p>"},{"location":"api/worldkernels/runtime/attention/backends/sdpa/#worldkernels.runtime.attention.backends.sdpa.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['SDPABackend']\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/sdpa/#worldkernels.runtime.attention.backends.sdpa.SDPABackend","title":"SDPABackend","text":"<p>               Bases: <code>AttentionBackend</code></p> <p>Attention via <code>torch.nn.functional.scaled_dot_product_attention</code>.</p> Source code in <code>worldkernels/runtime/attention/backends/sdpa.py</code> <pre><code>class SDPABackend(AttentionBackend):\n    r\"\"\"Attention via ``torch.nn.functional.scaled_dot_product_attention``.\"\"\"\n\n    name: ClassVar[str] = \"sdpa\"\n\n    def forward(\n        self,\n        query: torch.Tensor,\n        key: torch.Tensor,\n        value: torch.Tensor,\n        *,\n        is_causal: bool = False,\n        scale: float | None = None,\n    ) -&gt; torch.Tensor:\n        q = query.transpose(1, 2)\n        k = key.transpose(1, 2)\n        v = value.transpose(1, 2)\n        out = F.scaled_dot_product_attention(q, k, v, is_causal=is_causal, scale=scale)\n        return out.transpose(1, 2)\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/sdpa/#worldkernels.runtime.attention.backends.sdpa.SDPABackend.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'sdpa'\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/backends/sdpa/#worldkernels.runtime.attention.backends.sdpa.SDPABackend.forward","title":"forward","text":"<pre><code>forward(query: Tensor, key: Tensor, value: Tensor, *, is_causal: bool = False, scale: float | None = None) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/runtime/attention/backends/sdpa.py</code> <pre><code>def forward(\n    self,\n    query: torch.Tensor,\n    key: torch.Tensor,\n    value: torch.Tensor,\n    *,\n    is_causal: bool = False,\n    scale: float | None = None,\n) -&gt; torch.Tensor:\n    q = query.transpose(1, 2)\n    k = key.transpose(1, 2)\n    v = value.transpose(1, 2)\n    out = F.scaled_dot_product_attention(q, k, v, is_causal=is_causal, scale=scale)\n    return out.transpose(1, 2)\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/selector/","title":"selector","text":""},{"location":"api/worldkernels/runtime/attention/selector/#worldkernels.runtime.attention.selector","title":"worldkernels.runtime.attention.selector","text":"<p>Attention backend selection.</p> <p>Resolves a backend by explicit name or by the active platform's default, falling back to SDPA when a requested accelerated backend is unavailable.</p>"},{"location":"api/worldkernels/runtime/attention/selector/#worldkernels.runtime.attention.selector.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['select_attention_backend']\n</code></pre>"},{"location":"api/worldkernels/runtime/attention/selector/#worldkernels.runtime.attention.selector.select_attention_backend","title":"select_attention_backend","text":"<pre><code>select_attention_backend(name: str | None = None) -&gt; AttentionBackend\n</code></pre> <p>Return an attention backend instance.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str | None</code> <p><code>\"sdpa\"</code> / <code>\"flash\"</code>. When <code>None</code> we first consult the active <code>ForwardContext.attention_backend</code> (set by <code>WorldRunner</code> from <code>RuntimeConfig.attention_backend</code>), then fall back to the platform default. A requested <code>\"flash\"</code> backend silently falls back to SDPA when <code>flash-attn</code> is not installed.</p> <code>None</code> Source code in <code>worldkernels/runtime/attention/selector.py</code> <pre><code>def select_attention_backend(name: str | None = None) -&gt; AttentionBackend:\n    r\"\"\"Return an attention backend instance.\n\n    Args:\n        name: ``\"sdpa\"`` / ``\"flash\"``. When ``None`` we first consult the\n            active `ForwardContext.attention_backend` (set by `WorldRunner`\n            from `RuntimeConfig.attention_backend`), then fall back to the\n            platform default. A requested ``\"flash\"`` backend silently falls\n            back to SDPA when ``flash-attn`` is not installed.\n    \"\"\"\n    from worldkernels.platforms import current_platform\n    from worldkernels.runtime.attention.backends import FlashAttentionBackend, SDPABackend\n\n    if name is None:\n        try:\n            from worldkernels.runtime.forward_context import get_forward_context\n\n            name = get_forward_context().attention_backend\n        except RuntimeError:\n            pass\n\n    resolved = name or current_platform().default_attention_backend()\n    if resolved == \"flash\" and FlashAttentionBackend.is_available():\n        return FlashAttentionBackend()\n    if resolved == \"flash\":\n        return SDPABackend()\n    if resolved == \"sdpa\":\n        return SDPABackend()\n    raise ValueError(f\"unknown attention backend {resolved!r}; available: sdpa, flash\")\n</code></pre>"},{"location":"api/worldkernels/runtime/backends/","title":"backends","text":""},{"location":"api/worldkernels/runtime/backends/#worldkernels.runtime.backends","title":"worldkernels.runtime.backends","text":"<p>Modules:</p> Name Description <code>compile</code> <code>eager</code> <p>Eager PyTorch backend (baseline).</p> <code>tensorrt</code>"},{"location":"api/worldkernels/runtime/backends/compile/","title":"compile","text":""},{"location":"api/worldkernels/runtime/backends/compile/#worldkernels.runtime.backends.compile","title":"worldkernels.runtime.backends.compile","text":""},{"location":"api/worldkernels/runtime/backends/eager/","title":"eager","text":""},{"location":"api/worldkernels/runtime/backends/eager/#worldkernels.runtime.backends.eager","title":"worldkernels.runtime.backends.eager","text":"<p>Eager PyTorch backend (baseline).</p> <p>No compilation, no CUDA graphs. Just <code>torch.no_grad()</code> with the model's native forward pass. Always available, works on CPU and any GPU.</p>"},{"location":"api/worldkernels/runtime/backends/eager/#worldkernels.runtime.backends.eager.EagerBackend","title":"EagerBackend","text":"<p>Minimal execution backend that runs model stages directly.</p> Source code in <code>worldkernels/runtime/backends/eager.py</code> <pre><code>class EagerBackend:\n    \"\"\"Minimal execution backend that runs model stages directly.\"\"\"\n\n    name = \"eager\"\n\n    def __init__(self, device: str, dtype: torch.dtype) -&gt; None:\n        self.device = device\n        self.dtype = dtype\n\n    @torch.no_grad()\n    def run(self, fn, *args, **kwargs):\n        \"\"\"Execute *fn* under ``torch.no_grad`` on the configured device.\"\"\"\n        return fn(*args, **kwargs)\n</code></pre>"},{"location":"api/worldkernels/runtime/backends/eager/#worldkernels.runtime.backends.eager.EagerBackend.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name = 'eager'\n</code></pre>"},{"location":"api/worldkernels/runtime/backends/eager/#worldkernels.runtime.backends.eager.EagerBackend.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/runtime/backends/eager/#worldkernels.runtime.backends.eager.EagerBackend.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype = dtype\n</code></pre>"},{"location":"api/worldkernels/runtime/backends/eager/#worldkernels.runtime.backends.eager.EagerBackend.__init__","title":"__init__","text":"<pre><code>__init__(device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/backends/eager.py</code> <pre><code>def __init__(self, device: str, dtype: torch.dtype) -&gt; None:\n    self.device = device\n    self.dtype = dtype\n</code></pre>"},{"location":"api/worldkernels/runtime/backends/eager/#worldkernels.runtime.backends.eager.EagerBackend.run","title":"run","text":"<pre><code>run(fn, *args, **kwargs)\n</code></pre> <p>Execute fn under <code>torch.no_grad</code> on the configured device.</p> Source code in <code>worldkernels/runtime/backends/eager.py</code> <pre><code>@torch.no_grad()\ndef run(self, fn, *args, **kwargs):\n    \"\"\"Execute *fn* under ``torch.no_grad`` on the configured device.\"\"\"\n    return fn(*args, **kwargs)\n</code></pre>"},{"location":"api/worldkernels/runtime/backends/tensorrt/","title":"tensorrt","text":""},{"location":"api/worldkernels/runtime/backends/tensorrt/#worldkernels.runtime.backends.tensorrt","title":"worldkernels.runtime.backends.tensorrt","text":""},{"location":"api/worldkernels/runtime/cache/","title":"cache","text":""},{"location":"api/worldkernels/runtime/cache/#worldkernels.runtime.cache","title":"worldkernels.runtime.cache","text":"<p>Denoise-step caching backends.</p> <p>Modules:</p> Name Description <code>teacache</code> <p>TeaCache \u2014 timestep-embedding-aware denoise-step caching.</p>"},{"location":"api/worldkernels/runtime/cache/#worldkernels.runtime.cache.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['TeaCache']\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/#worldkernels.runtime.cache._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'TeaCache': ('worldkernels.runtime.cache.teacache', 'TeaCache')}\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/#worldkernels.runtime.cache.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/runtime/cache/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/","title":"teacache","text":""},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache","title":"worldkernels.runtime.cache.teacache","text":"<p>TeaCache \u2014 timestep-embedding-aware denoise-step caching.</p> <p>Across a denoise loop the transformer's per-step modulation signal changes slowly. TeaCache accumulates the relative L1 change of that signal; while the accumulated change stays below a threshold it reports that the heavy transformer compute can be skipped and the previous step's residual reused.</p> <p>This holds only the skip decision and the cached residual; the transformer calls <code>should_compute()</code> / <code>store()</code> around its block stack.</p>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['TeaCache']\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache","title":"TeaCache","text":"<p>Relative-L1 step-skipping cache for a denoise loop.</p> <p>Parameters:</p> Name Type Description Default <code>rel_l1_threshold</code> <code>float</code> <p>Accumulated relative-L1 change at which a full recompute is forced. Larger skips more steps (faster, lower quality).</p> <code>0.15</code> Source code in <code>worldkernels/runtime/cache/teacache.py</code> <pre><code>class TeaCache:\n    r\"\"\"Relative-L1 step-skipping cache for a denoise loop.\n\n    Args:\n        rel_l1_threshold: Accumulated relative-L1 change at which a full\n            recompute is forced. Larger skips more steps (faster, lower quality).\n    \"\"\"\n\n    def __init__(self, rel_l1_threshold: float = 0.15) -&gt; None:\n        if rel_l1_threshold &lt; 0:\n            raise ValueError(f\"rel_l1_threshold must be &gt;= 0, got {rel_l1_threshold}\")\n        self.rel_l1_threshold = rel_l1_threshold\n        self._prev_modulation: \"torch.Tensor | None\" = None\n        self._cached_residual: \"torch.Tensor | None\" = None\n        self._accumulated = 0.0\n        self.hits = 0\n        self.misses = 0\n\n    def should_compute(self, modulation: \"torch.Tensor\") -&gt; bool:\n        r\"\"\"Whether the transformer must run this step, or its residual is reused.\n\n        Updates the accumulated relative-L1 distance from the previous step's\n        modulation signal.\n        \"\"\"\n        if self._prev_modulation is None or self._cached_residual is None:\n            self._prev_modulation = modulation\n            self.misses += 1\n            return True\n\n        prev = self._prev_modulation\n        rel_change = ((modulation - prev).abs().mean() / (prev.abs().mean() + 1e-8)).item()\n        self._accumulated += rel_change\n        self._prev_modulation = modulation\n\n        if self._accumulated &lt; self.rel_l1_threshold:\n            self.hits += 1\n            return False\n        self._accumulated = 0.0\n        self.misses += 1\n        return True\n\n    def store(self, residual: \"torch.Tensor\") -&gt; None:\n        r\"\"\"Cache the transformer residual from a computed step.\"\"\"\n        self._cached_residual = residual\n\n    @property\n    def cached_residual(self) -&gt; \"torch.Tensor | None\":\n        return self._cached_residual\n\n    @property\n    def hit_rate(self) -&gt; float:\n        total = self.hits + self.misses\n        return self.hits / total if total else 0.0\n\n    def reset(self) -&gt; None:\n        r\"\"\"Clear all state for a fresh denoise loop.\"\"\"\n        self._prev_modulation = None\n        self._cached_residual = None\n        self._accumulated = 0.0\n        self.hits = 0\n        self.misses = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.rel_l1_threshold","title":"rel_l1_threshold  <code>instance-attribute</code>","text":"<pre><code>rel_l1_threshold = rel_l1_threshold\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache._prev_modulation","title":"_prev_modulation  <code>instance-attribute</code>","text":"<pre><code>_prev_modulation: 'torch.Tensor | None' = None\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache._cached_residual","title":"_cached_residual  <code>instance-attribute</code>","text":"<pre><code>_cached_residual: 'torch.Tensor | None' = None\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache._accumulated","title":"_accumulated  <code>instance-attribute</code>","text":"<pre><code>_accumulated = 0.0\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.hits","title":"hits  <code>instance-attribute</code>","text":"<pre><code>hits = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.misses","title":"misses  <code>instance-attribute</code>","text":"<pre><code>misses = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.cached_residual","title":"cached_residual  <code>property</code>","text":"<pre><code>cached_residual: 'torch.Tensor | None'\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.hit_rate","title":"hit_rate  <code>property</code>","text":"<pre><code>hit_rate: float\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.__init__","title":"__init__","text":"<pre><code>__init__(rel_l1_threshold: float = 0.15) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/cache/teacache.py</code> <pre><code>def __init__(self, rel_l1_threshold: float = 0.15) -&gt; None:\n    if rel_l1_threshold &lt; 0:\n        raise ValueError(f\"rel_l1_threshold must be &gt;= 0, got {rel_l1_threshold}\")\n    self.rel_l1_threshold = rel_l1_threshold\n    self._prev_modulation: \"torch.Tensor | None\" = None\n    self._cached_residual: \"torch.Tensor | None\" = None\n    self._accumulated = 0.0\n    self.hits = 0\n    self.misses = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.should_compute","title":"should_compute","text":"<pre><code>should_compute(modulation: 'torch.Tensor') -&gt; bool\n</code></pre> <p>Whether the transformer must run this step, or its residual is reused.</p> <p>Updates the accumulated relative-L1 distance from the previous step's modulation signal.</p> Source code in <code>worldkernels/runtime/cache/teacache.py</code> <pre><code>def should_compute(self, modulation: \"torch.Tensor\") -&gt; bool:\n    r\"\"\"Whether the transformer must run this step, or its residual is reused.\n\n    Updates the accumulated relative-L1 distance from the previous step's\n    modulation signal.\n    \"\"\"\n    if self._prev_modulation is None or self._cached_residual is None:\n        self._prev_modulation = modulation\n        self.misses += 1\n        return True\n\n    prev = self._prev_modulation\n    rel_change = ((modulation - prev).abs().mean() / (prev.abs().mean() + 1e-8)).item()\n    self._accumulated += rel_change\n    self._prev_modulation = modulation\n\n    if self._accumulated &lt; self.rel_l1_threshold:\n        self.hits += 1\n        return False\n    self._accumulated = 0.0\n    self.misses += 1\n    return True\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.store","title":"store","text":"<pre><code>store(residual: 'torch.Tensor') -&gt; None\n</code></pre> <p>Cache the transformer residual from a computed step.</p> Source code in <code>worldkernels/runtime/cache/teacache.py</code> <pre><code>def store(self, residual: \"torch.Tensor\") -&gt; None:\n    r\"\"\"Cache the transformer residual from a computed step.\"\"\"\n    self._cached_residual = residual\n</code></pre>"},{"location":"api/worldkernels/runtime/cache/teacache/#worldkernels.runtime.cache.teacache.TeaCache.reset","title":"reset","text":"<pre><code>reset() -&gt; None\n</code></pre> <p>Clear all state for a fresh denoise loop.</p> Source code in <code>worldkernels/runtime/cache/teacache.py</code> <pre><code>def reset(self) -&gt; None:\n    r\"\"\"Clear all state for a fresh denoise loop.\"\"\"\n    self._prev_modulation = None\n    self._cached_residual = None\n    self._accumulated = 0.0\n    self.hits = 0\n    self.misses = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/","title":"compile","text":""},{"location":"api/worldkernels/runtime/compile/#worldkernels.runtime.compile","title":"worldkernels.runtime.compile","text":"<p>Compilation backends: regional torch.compile and CUDA graph capture.</p> <p>Modules:</p> Name Description <code>cuda_graph</code> <p>CUDA graph capture and replay.</p> <code>torch_compile</code> <p>Regional <code>torch.compile</code>.</p>"},{"location":"api/worldkernels/runtime/compile/#worldkernels.runtime.compile.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['regionally_compile', 'CUDAGraphRunner']\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/#worldkernels.runtime.compile._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'regionally_compile': ('worldkernels.runtime.compile.torch_compile', 'regionally_compile'), 'CUDAGraphRunner': ('worldkernels.runtime.compile.cuda_graph', 'CUDAGraphRunner')}\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/#worldkernels.runtime.compile.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/runtime/compile/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/","title":"cuda_graph","text":""},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph","title":"worldkernels.runtime.compile.cuda_graph","text":"<p>CUDA graph capture and replay.</p> <p>A captured graph records the GPU kernel sequence of a fixed-shape callable once, then replays it with near-zero CPU launch overhead \u2014 the largest win for the denoise loop, which runs the same transformer step tens of times. Inputs must keep identical shapes, dtypes, and devices across replays.</p>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['CUDAGraphRunner']\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner","title":"CUDAGraphRunner","text":"<p>Captures one fixed-shape callable as a CUDA graph and replays it.</p> Source code in <code>worldkernels/runtime/compile/cuda_graph.py</code> <pre><code>class CUDAGraphRunner:\n    r\"\"\"Captures one fixed-shape callable as a CUDA graph and replays it.\"\"\"\n\n    def __init__(self) -&gt; None:\n        self._graph: Any = None\n        self._static_inputs: tuple[\"torch.Tensor\", ...] = ()\n        self._static_output: Any = None\n\n    @property\n    def is_captured(self) -&gt; bool:\n        return self._graph is not None\n\n    def capture(\n        self,\n        fn: Callable[..., \"torch.Tensor\"],\n        *sample_inputs: \"torch.Tensor\",\n        warmup: int = 3,\n    ) -&gt; \"CUDAGraphRunner\":\n        r\"\"\"Warm up and capture ``fn(*sample_inputs)`` as a CUDA graph.\n\n        Args:\n            fn: The callable to capture; must take only tensor arguments.\n            sample_inputs: Representative inputs defining the captured shapes.\n            warmup: Eager iterations before capture (lets autograd/cuDNN settle).\n        \"\"\"\n        import torch\n\n        if not torch.cuda.is_available():\n            raise RuntimeError(\"CUDA graph capture requires a CUDA device\")\n\n        warmup_stream = torch.cuda.Stream()\n        warmup_stream.wait_stream(torch.cuda.current_stream())\n        with torch.cuda.stream(warmup_stream):\n            for _ in range(warmup):\n                fn(*sample_inputs)\n        torch.cuda.current_stream().wait_stream(warmup_stream)\n\n        self._static_inputs = tuple(t.clone() for t in sample_inputs)\n        self._graph = torch.cuda.CUDAGraph()\n        with torch.cuda.graph(self._graph):\n            self._static_output = fn(*self._static_inputs)\n        return self\n\n    def replay(self, *inputs: \"torch.Tensor\") -&gt; Any:\n        r\"\"\"Copy ``inputs`` into the captured buffers and replay the graph.\"\"\"\n        if self._graph is None:\n            raise RuntimeError(\"graph not captured; call capture() first\")\n        if len(inputs) != len(self._static_inputs):\n            raise ValueError(f\"expected {len(self._static_inputs)} inputs, got {len(inputs)}\")\n        for static, fresh in zip(self._static_inputs, inputs):\n            static.copy_(fresh)\n        self._graph.replay()\n        return self._static_output\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner._graph","title":"_graph  <code>instance-attribute</code>","text":"<pre><code>_graph: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner._static_inputs","title":"_static_inputs  <code>instance-attribute</code>","text":"<pre><code>_static_inputs: tuple['torch.Tensor', ...] = ()\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner._static_output","title":"_static_output  <code>instance-attribute</code>","text":"<pre><code>_static_output: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner.is_captured","title":"is_captured  <code>property</code>","text":"<pre><code>is_captured: bool\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner.__init__","title":"__init__","text":"<pre><code>__init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/compile/cuda_graph.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._graph: Any = None\n    self._static_inputs: tuple[\"torch.Tensor\", ...] = ()\n    self._static_output: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner.capture","title":"capture","text":"<pre><code>capture(fn: Callable[..., 'torch.Tensor'], *sample_inputs: 'torch.Tensor', warmup: int = 3) -&gt; 'CUDAGraphRunner'\n</code></pre> <p>Warm up and capture <code>fn(*sample_inputs)</code> as a CUDA graph.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>Callable[..., 'torch.Tensor']</code> <p>The callable to capture; must take only tensor arguments.</p> required <code>sample_inputs</code> <code>'torch.Tensor'</code> <p>Representative inputs defining the captured shapes.</p> <code>()</code> <code>warmup</code> <code>int</code> <p>Eager iterations before capture (lets autograd/cuDNN settle).</p> <code>3</code> Source code in <code>worldkernels/runtime/compile/cuda_graph.py</code> <pre><code>def capture(\n    self,\n    fn: Callable[..., \"torch.Tensor\"],\n    *sample_inputs: \"torch.Tensor\",\n    warmup: int = 3,\n) -&gt; \"CUDAGraphRunner\":\n    r\"\"\"Warm up and capture ``fn(*sample_inputs)`` as a CUDA graph.\n\n    Args:\n        fn: The callable to capture; must take only tensor arguments.\n        sample_inputs: Representative inputs defining the captured shapes.\n        warmup: Eager iterations before capture (lets autograd/cuDNN settle).\n    \"\"\"\n    import torch\n\n    if not torch.cuda.is_available():\n        raise RuntimeError(\"CUDA graph capture requires a CUDA device\")\n\n    warmup_stream = torch.cuda.Stream()\n    warmup_stream.wait_stream(torch.cuda.current_stream())\n    with torch.cuda.stream(warmup_stream):\n        for _ in range(warmup):\n            fn(*sample_inputs)\n    torch.cuda.current_stream().wait_stream(warmup_stream)\n\n    self._static_inputs = tuple(t.clone() for t in sample_inputs)\n    self._graph = torch.cuda.CUDAGraph()\n    with torch.cuda.graph(self._graph):\n        self._static_output = fn(*self._static_inputs)\n    return self\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/cuda_graph/#worldkernels.runtime.compile.cuda_graph.CUDAGraphRunner.replay","title":"replay","text":"<pre><code>replay(*inputs: 'torch.Tensor') -&gt; Any\n</code></pre> <p>Copy <code>inputs</code> into the captured buffers and replay the graph.</p> Source code in <code>worldkernels/runtime/compile/cuda_graph.py</code> <pre><code>def replay(self, *inputs: \"torch.Tensor\") -&gt; Any:\n    r\"\"\"Copy ``inputs`` into the captured buffers and replay the graph.\"\"\"\n    if self._graph is None:\n        raise RuntimeError(\"graph not captured; call capture() first\")\n    if len(inputs) != len(self._static_inputs):\n        raise ValueError(f\"expected {len(self._static_inputs)} inputs, got {len(inputs)}\")\n    for static, fresh in zip(self._static_inputs, inputs):\n        static.copy_(fresh)\n    self._graph.replay()\n    return self._static_output\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/torch_compile/","title":"torch_compile","text":""},{"location":"api/worldkernels/runtime/compile/torch_compile/#worldkernels.runtime.compile.torch_compile","title":"worldkernels.runtime.compile.torch_compile","text":"<p>Regional <code>torch.compile</code>.</p> <p>A video DiT is a stack of identical transformer blocks wrapped by one-off embedding and projection layers. Compiling only the repeated block \u2014 not the whole model \u2014 keeps compile time bounded and avoids recompiles when outer shapes vary. A module opts in by listing the attribute names of its repeated <code>nn.ModuleList</code>s in <code>_repeated_blocks</code>.</p>"},{"location":"api/worldkernels/runtime/compile/torch_compile/#worldkernels.runtime.compile.torch_compile.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['regionally_compile']\n</code></pre>"},{"location":"api/worldkernels/runtime/compile/torch_compile/#worldkernels.runtime.compile.torch_compile.regionally_compile","title":"regionally_compile","text":"<pre><code>regionally_compile(module: 'nn.Module', *, mode: str = 'reduce-overhead', repeated_block_attrs: list[str] | None = None) -&gt; 'nn.Module'\n</code></pre> <p>Compile a module's repeated blocks in place, leaving one-off ops eager.</p> <p>Parameters:</p> Name Type Description Default <code>module</code> <code>'nn.Module'</code> <p>The model to compile.</p> required <code>mode</code> <code>str</code> <p><code>torch.compile</code> mode.</p> <code>'reduce-overhead'</code> <code>repeated_block_attrs</code> <code>list[str] | None</code> <p>Attribute names of repeated <code>nn.ModuleList</code>s. Defaults to <code>module._repeated_blocks</code>; if neither is present the whole module is compiled.</p> <code>None</code> <p>Returns:</p> Type Description <code>'nn.Module'</code> <p>The same <code>module</code>, with its repeated blocks replaced by compiled ones.</p> Source code in <code>worldkernels/runtime/compile/torch_compile.py</code> <pre><code>def regionally_compile(\n    module: \"nn.Module\",\n    *,\n    mode: str = \"reduce-overhead\",\n    repeated_block_attrs: list[str] | None = None,\n) -&gt; \"nn.Module\":\n    r\"\"\"Compile a module's repeated blocks in place, leaving one-off ops eager.\n\n    Args:\n        module: The model to compile.\n        mode: ``torch.compile`` mode.\n        repeated_block_attrs: Attribute names of repeated ``nn.ModuleList``s.\n            Defaults to ``module._repeated_blocks``; if neither is present the\n            whole module is compiled.\n\n    Returns:\n        The same ``module``, with its repeated blocks replaced by compiled ones.\n    \"\"\"\n    import torch\n\n    from worldkernels.config.active import get_active_config\n\n    if not get_active_config().torch_compile:\n        return module\n\n    attrs = repeated_block_attrs or getattr(module, \"_repeated_blocks\", None)\n    if not attrs:\n        return torch.compile(module, mode=mode)  # type: ignore[return-value]\n\n    for attr in attrs:\n        block_list = getattr(module, attr, None)\n        if block_list is None:\n            continue\n        for i in range(len(block_list)):\n            block_list[i] = torch.compile(block_list[i], mode=mode)\n    return module\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/","title":"connectors","text":""},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors","title":"worldkernels.runtime.connectors","text":"<p>Inter-stage data transport for the world model pipeline.</p>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.StageConnector","title":"StageConnector","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract transport between pipeline stages.</p> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>class StageConnector(ABC):\n    r\"\"\"Abstract transport between pipeline stages.\"\"\"\n\n    @abstractmethod\n    def put(\n        self,\n        from_stage: str,\n        to_stage: str,\n        key: str,\n        data: StageOutput,\n    ) -&gt; bool:\n        r\"\"\"Store a stage output for the destination stage.\"\"\"\n\n    @abstractmethod\n    def get(\n        self,\n        from_stage: str,\n        to_stage: str,\n        key: str,\n    ) -&gt; StageOutput | None:\n        r\"\"\"Retrieve a stage output. Returns None if not yet available.\"\"\"\n\n    @abstractmethod\n    def cleanup(self, key: str) -&gt; None:\n        r\"\"\"Release all resources associated with a key.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.StageConnector.put","title":"put  <code>abstractmethod</code>","text":"<pre><code>put(from_stage: str, to_stage: str, key: str, data: StageOutput) -&gt; bool\n</code></pre> <p>Store a stage output for the destination stage.</p> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>@abstractmethod\ndef put(\n    self,\n    from_stage: str,\n    to_stage: str,\n    key: str,\n    data: StageOutput,\n) -&gt; bool:\n    r\"\"\"Store a stage output for the destination stage.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.StageConnector.get","title":"get  <code>abstractmethod</code>","text":"<pre><code>get(from_stage: str, to_stage: str, key: str) -&gt; StageOutput | None\n</code></pre> <p>Retrieve a stage output. Returns None if not yet available.</p> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>@abstractmethod\ndef get(\n    self,\n    from_stage: str,\n    to_stage: str,\n    key: str,\n) -&gt; StageOutput | None:\n    r\"\"\"Retrieve a stage output. Returns None if not yet available.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.StageConnector.cleanup","title":"cleanup  <code>abstractmethod</code>","text":"<pre><code>cleanup(key: str) -&gt; None\n</code></pre> <p>Release all resources associated with a key.</p> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>@abstractmethod\ndef cleanup(self, key: str) -&gt; None:\n    r\"\"\"Release all resources associated with a key.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.LocalConnector","title":"LocalConnector","text":"<p>               Bases: <code>StageConnector</code></p> <p>Zero-copy connector for co-located stages on the same device.</p> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>class LocalConnector(StageConnector):\n    r\"\"\"Zero-copy connector for co-located stages on the same device.\"\"\"\n\n    def __init__(self) -&gt; None:\n        self._store: dict[tuple[str, str, str], StageOutput] = {}\n\n    def put(\n        self,\n        from_stage: str,\n        to_stage: str,\n        key: str,\n        data: StageOutput,\n    ) -&gt; bool:\n        self._store[(from_stage, to_stage, key)] = data\n        return True\n\n    def get(\n        self,\n        from_stage: str,\n        to_stage: str,\n        key: str,\n    ) -&gt; StageOutput | None:\n        return self._store.pop((from_stage, to_stage, key), None)\n\n    def cleanup(self, key: str) -&gt; None:\n        to_remove = [k for k in self._store if k[2] == key]\n        for k in to_remove:\n            del self._store[k]\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.LocalConnector._store","title":"_store  <code>instance-attribute</code>","text":"<pre><code>_store: dict[tuple[str, str, str], StageOutput] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.LocalConnector.__init__","title":"__init__","text":"<pre><code>__init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._store: dict[tuple[str, str, str], StageOutput] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.LocalConnector.put","title":"put","text":"<pre><code>put(from_stage: str, to_stage: str, key: str, data: StageOutput) -&gt; bool\n</code></pre> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>def put(\n    self,\n    from_stage: str,\n    to_stage: str,\n    key: str,\n    data: StageOutput,\n) -&gt; bool:\n    self._store[(from_stage, to_stage, key)] = data\n    return True\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.LocalConnector.get","title":"get","text":"<pre><code>get(from_stage: str, to_stage: str, key: str) -&gt; StageOutput | None\n</code></pre> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>def get(\n    self,\n    from_stage: str,\n    to_stage: str,\n    key: str,\n) -&gt; StageOutput | None:\n    return self._store.pop((from_stage, to_stage, key), None)\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.LocalConnector.cleanup","title":"cleanup","text":"<pre><code>cleanup(key: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>def cleanup(self, key: str) -&gt; None:\n    to_remove = [k for k in self._store if k[2] == key]\n    for k in to_remove:\n        del self._store[k]\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.ConnectorRegistry","title":"ConnectorRegistry","text":"<p>Factory for creating stage connector instances.</p> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>class ConnectorRegistry:\n    r\"\"\"Factory for creating stage connector instances.\"\"\"\n\n    _registry: dict[str, type[StageConnector]] = {}\n\n    @classmethod\n    def register(cls, name: str, connector_cls: type[StageConnector]) -&gt; None:\n        if name in cls._registry:\n            log.warning(\"Overwriting connector registration: %s\", name)\n        cls._registry[name] = connector_cls\n\n    @classmethod\n    def create(cls, name: str, **kwargs: Any) -&gt; StageConnector:\n        if name not in cls._registry:\n            raise KeyError(f\"Unknown connector '{name}'. Available: {list(cls._registry.keys())}\")\n        return cls._registry[name](**kwargs)\n\n    @classmethod\n    def list_registered(cls) -&gt; list[str]:\n        return list(cls._registry.keys())\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.ConnectorRegistry._registry","title":"_registry  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_registry: dict[str, type[StageConnector]] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.ConnectorRegistry.register","title":"register  <code>classmethod</code>","text":"<pre><code>register(name: str, connector_cls: type[StageConnector]) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>@classmethod\ndef register(cls, name: str, connector_cls: type[StageConnector]) -&gt; None:\n    if name in cls._registry:\n        log.warning(\"Overwriting connector registration: %s\", name)\n    cls._registry[name] = connector_cls\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.ConnectorRegistry.create","title":"create  <code>classmethod</code>","text":"<pre><code>create(name: str, **kwargs: Any) -&gt; StageConnector\n</code></pre> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>@classmethod\ndef create(cls, name: str, **kwargs: Any) -&gt; StageConnector:\n    if name not in cls._registry:\n        raise KeyError(f\"Unknown connector '{name}'. Available: {list(cls._registry.keys())}\")\n    return cls._registry[name](**kwargs)\n</code></pre>"},{"location":"api/worldkernels/runtime/connectors/#worldkernels.runtime.connectors.ConnectorRegistry.list_registered","title":"list_registered  <code>classmethod</code>","text":"<pre><code>list_registered() -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/runtime/connectors.py</code> <pre><code>@classmethod\ndef list_registered(cls) -&gt; list[str]:\n    return list(cls._registry.keys())\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/","title":"envs","text":""},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs","title":"worldkernels.runtime.envs","text":"<p>Per-model uv venv materialization under <code>WORLDKERNELS_HOME/envs/</code>.</p>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['envs_dir', 'env_path', 'venv_python', 'materialize_env', 'remove_env']\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs.envs_dir","title":"envs_dir","text":"<pre><code>envs_dir() -&gt; Path\n</code></pre> Source code in <code>worldkernels/runtime/envs.py</code> <pre><code>def envs_dir() -&gt; Path:\n    p = cache.home() / \"envs\"\n    p.mkdir(parents=True, exist_ok=True)\n    return p\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs.env_path","title":"env_path","text":"<pre><code>env_path(model_id: str) -&gt; Path\n</code></pre> Source code in <code>worldkernels/runtime/envs.py</code> <pre><code>def env_path(model_id: str) -&gt; Path:\n    return envs_dir() / _slug(model_id)\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs.venv_python","title":"venv_python","text":"<pre><code>venv_python(model_id: str) -&gt; Path\n</code></pre> Source code in <code>worldkernels/runtime/envs.py</code> <pre><code>def venv_python(model_id: str) -&gt; Path:\n    base = env_path(model_id)\n    if os.name == \"nt\":\n        return base / \"Scripts\" / \"python.exe\"\n    return base / \"bin\" / \"python\"\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs.materialize_env","title":"materialize_env","text":"<pre><code>materialize_env(model_id: str, requirements: list[str], device: str, progress: 'ProgressController | None' = None, allow_fetch: bool = True) -&gt; Path\n</code></pre> <p>Create or reuse an isolated uv venv for <code>model_id</code>.</p> <p>Returns the venv root path. Idempotent: if a lock file matches the requested requirements + torch ABI + device, returns the existing path without touching the venv.</p> Source code in <code>worldkernels/runtime/envs.py</code> <pre><code>def materialize_env(\n    model_id: str,\n    requirements: list[str],\n    device: str,\n    progress: \"ProgressController | None\" = None,\n    allow_fetch: bool = True,\n) -&gt; Path:\n    r\"\"\"Create or reuse an isolated uv venv for ``model_id``.\n\n    Returns the venv root path. Idempotent: if a lock file matches the requested\n    requirements + torch ABI + device, returns the existing path without\n    touching the venv.\n    \"\"\"\n    base = env_path(model_id)\n    abi = torch_abi_tag()\n    new_hash = deps_hash(requirements, abi, device)\n\n    existing = EnvLock.read(model_id)\n    if existing is not None and existing.deps_hash == new_hash and venv_python(model_id).exists():\n        if progress is not None:\n            progress.event(\"isolating\", \"skipped\", f\"cached env: {base}\")\n        return base\n\n    if not allow_fetch or os.environ.get(\"WORLDKERNELS_NO_AUTO_INSTALL\"):\n        raise FetchDisabledError(\n            f\"missing isolated env for {model_id!r}\",\n            f\"run `worldkernels pull {model_id}` to materialize it\",\n        )\n\n    uv_bin = _resolve_uv()\n    if uv_bin is None:\n        raise RuntimeError(\n            \"uv not found; required for per-model env materialization. uv is a core \"\n            \"worldkernels dep \u2014 try `pip install --force-reinstall worldkernels` \"\n            \"(or unset WORLDKERNELS_FORCE_PIP).\"\n        )\n\n    if progress is not None:\n        progress.event(\"isolating\", \"running\", f\"creating {base} \u2026\")\n    log.info(\"Materializing per-model venv at %s\", base)\n\n    if base.exists():\n        shutil.rmtree(base)\n\n    _run([uv_bin, \"venv\", str(base)], progress=progress)\n\n    if requirements:\n        py = str(venv_python(model_id))\n        if progress is not None:\n            progress.event(\"isolating\", \"running\", f\"installing {len(requirements)} deps \u2026\")\n        _run([uv_bin, \"pip\", \"install\", \"--python\", py, *requirements], progress=progress)\n\n    lock = EnvLock(\n        model_id=model_id,\n        deps_hash=new_hash,\n        torch_abi=abi,\n        device=device,\n        requirements=requirements,\n    )\n    lock.write()\n\n    if progress is not None:\n        progress.event(\"isolating\", \"done\", f\"env ready: {base}\")\n    return base\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs.remove_env","title":"remove_env","text":"<pre><code>remove_env(model_id: str) -&gt; bool\n</code></pre> Source code in <code>worldkernels/runtime/envs.py</code> <pre><code>def remove_env(model_id: str) -&gt; bool:\n    base = env_path(model_id)\n    lock_path = EnvLock(model_id=model_id, deps_hash=\"\", torch_abi=\"\", device=\"\").path\n    removed = False\n    if base.exists():\n        shutil.rmtree(base)\n        removed = True\n    if lock_path.exists():\n        lock_path.unlink()\n        removed = True\n    return removed\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs._run","title":"_run","text":"<pre><code>_run(cmd: list[str], progress: 'ProgressController | None' = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/envs.py</code> <pre><code>def _run(cmd: list[str], progress: \"ProgressController | None\" = None) -&gt; None:\n    quiet = progress is not None and progress.mode in (\"tty\", \"quiet\", \"json\", \"sink\")\n    proc = subprocess.run(cmd, check=False, capture_output=quiet, text=True)\n    if proc.returncode != 0:\n        if quiet:\n            sys.stderr.write((proc.stdout or \"\") + \"\\n\" + (proc.stderr or \"\") + \"\\n\")\n        raise RuntimeError(f\"{' '.join(cmd[:3])} failed (exit {proc.returncode})\")\n</code></pre>"},{"location":"api/worldkernels/runtime/envs/#worldkernels.runtime.envs._slug","title":"_slug","text":"<pre><code>_slug(model_id: str) -&gt; str\n</code></pre> Source code in <code>worldkernels/runtime/envs.py</code> <pre><code>def _slug(model_id: str) -&gt; str:\n    return model_id.replace(\"/\", \"_\").replace(\":\", \"_\")\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/","title":"executor","text":""},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor","title":"worldkernels.runtime.executor","text":"<p>World model executor: stage-disaggregated hot path.</p>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor","title":"Executor","text":"<p>Runs world model stages independently or as a unified pipeline.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>str</code> <p>Target device string.</p> required <code>dtype</code> <code>dtype</code> <p>Target dtype.</p> required <code>stage_configs</code> <code>tuple[StageConfig, ...] | None</code> <p>Per-stage execution configs.</p> <code>None</code> <code>connector</code> <code>StageConnector | None</code> <p>Inter-stage transport. Defaults to <code>LocalConnector</code>.</p> <code>None</code> <code>config</code> <code>'RuntimeConfig | None'</code> <p>Runtime config (component toggles); gates cuda_graphs / iteration_batching / latent_pool / kv_cache (wired incrementally).</p> <code>None</code> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>class Executor:\n    r\"\"\"Runs world model stages independently or as a unified pipeline.\n\n    Args:\n        device: Target device string.\n        dtype: Target dtype.\n        stage_configs: Per-stage execution configs.\n        connector: Inter-stage transport. Defaults to ``LocalConnector``.\n        config: Runtime config (component toggles); gates cuda_graphs /\n            iteration_batching / latent_pool / kv_cache (wired incrementally).\n    \"\"\"\n\n    def __init__(\n        self,\n        device: str,\n        dtype: torch.dtype,\n        stage_configs: tuple[StageConfig, ...] | None = None,\n        connector: StageConnector | None = None,\n        config: \"RuntimeConfig | None\" = None,\n    ) -&gt; None:\n        self.device = device\n        self.dtype = dtype\n        self.config = config\n        self.stage_configs = {\n            cfg.stage_type: cfg for cfg in (stage_configs or DEFAULT_PIPELINE_STAGES)\n        }\n        self.connector = connector or LocalConnector()\n\n    def _stage_enabled(self, stage_type: StageType) -&gt; bool:\n        cfg = self.stage_configs.get(stage_type)\n        return cfg is None or cfg.enabled\n\n    def _iteration_batching_enabled(self) -&gt; bool:\n        try:\n            from worldkernels.runtime.forward_context import get_forward_context\n\n            return get_forward_context().iteration_batching\n        except RuntimeError:\n            return bool(self.config and self.config.iteration_batching)\n\n    # ---- per-stage execution ---------------------------------------------\n\n    @torch.no_grad()\n    def execute_encode(\n        self,\n        world: WorldModel,\n        action: Action,\n    ) -&gt; StageOutput:\n        r\"\"\"Stage 1: encode action to conditioning tensor.\"\"\"\n        t0 = time.perf_counter()\n        action_tensor = world.encode_action(action)\n        elapsed = (time.perf_counter() - t0) * 1000.0\n        return StageOutput(\n            stage_type=StageType.ENCODE,\n            data=action_tensor,\n            timing_ms=elapsed,\n        )\n\n    @torch.no_grad()\n    def execute_transition(\n        self,\n        world: WorldModel,\n        state: LatentState,\n        action_encoded: torch.Tensor,\n    ) -&gt; StageOutput:\n        r\"\"\"Stage 2: advance latent state.\n\n        Treats ``transition()`` as opaque; the world's ``transition_mode``\n        determines internal behavior (bidirectional vs causal vs hybrid).\n        When ``iteration_batching`` is on and the world supports it, drives\n        ``transition_iter`` and uses the final yielded state \u2014 opening the\n        seam for sessions to join a batch mid-denoise.\n        \"\"\"\n        t0 = time.perf_counter()\n        iter_batch_on = self._iteration_batching_enabled()\n        if iter_batch_on and getattr(world, \"supports_iteration_batching\", False):\n            new_state = state\n            for new_state in world.transition_iter(state, action_encoded):\n                pass\n        else:\n            new_state = world.transition(state, action_encoded)\n        elapsed = (time.perf_counter() - t0) * 1000.0\n        return StageOutput(\n            stage_type=StageType.TRANSITION,\n            data=new_state,\n            timing_ms=elapsed,\n            metadata={\"transition_mode\": world.transition_mode.value},\n        )\n\n    @torch.no_grad()\n    def execute_decode(\n        self,\n        world: WorldModel,\n        state: LatentState,\n        modalities: list[str],\n    ) -&gt; StageOutput:\n        r\"\"\"Stage 3: decode latent state to observation.\"\"\"\n        t0 = time.perf_counter()\n        obs = world.decode_observation(state, modalities)\n        elapsed = (time.perf_counter() - t0) * 1000.0\n        return StageOutput(\n            stage_type=StageType.DECODE,\n            data=obs,\n            timing_ms=elapsed,\n        )\n\n    # ---- unified pipeline ------------------------------------------------\n\n    @torch.no_grad()\n    def step(\n        self,\n        world: WorldModel,\n        state: LatentState,\n        action: Action,\n        modalities: list[str],\n        step_index: int,\n        decode: bool = True,\n    ) -&gt; tuple[LatentState, Observation]:\n        r\"\"\"Execute the full 3-stage pipeline for one simulation step.\n\n        Args:\n            world: The world model adapter.\n            state: Current latent state.\n            action: Action to apply.\n            modalities: Observation modalities to decode.\n            step_index: Monotonic step counter.\n            decode: If False, skip stage 3 and return latent-only observation.\n\n        Returns:\n            Tuple of (new_state, observation) with ``stage_timing`` attached.\n        \"\"\"\n        timing = StageTiming()\n        t_wall = time.perf_counter()\n\n        encode_out = self.execute_encode(world, action)\n        timing.encode_action_ms = encode_out.timing_ms\n\n        transition_out = self.execute_transition(\n            world,\n            state,\n            encode_out.data,\n        )\n        timing.transition_ms = transition_out.timing_ms\n        new_state = transition_out.data\n\n        if decode and self._stage_enabled(StageType.DECODE):\n            decode_out = self.execute_decode(world, new_state, modalities)\n            timing.decode_observation_ms = decode_out.timing_ms\n            obs = decode_out.data\n        else:\n            obs = Observation(step_index=step_index, generation_time_ms=0.0)\n            obs.decode_skipped = True\n\n        wall_ms = (time.perf_counter() - t_wall) * 1000.0\n        obs.step_index = step_index\n        obs.generation_time_ms = wall_ms\n        obs.stage_timing = timing\n\n        return new_state, obs\n\n    @torch.no_grad()\n    def execute_batched(\n        self,\n        requests: list[StepRequest],\n    ) -&gt; list[tuple[LatentState, Observation]]:\n        r\"\"\"Execute a batch of step requests.\n\n        Currently iterates one request at a time; true batched forward passes\n        over a compatibility group land with the throughput step. The list\n        signature is the seam the scheduler dispatches into.\n        \"\"\"\n        return [\n            self.step(\n                world=req.world,\n                state=req.state,\n                action=req.action,\n                modalities=req.modalities,\n                step_index=req.step_index,\n                decode=req.decode,\n            )\n            for req in requests\n        ]\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype = dtype\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.stage_configs","title":"stage_configs  <code>instance-attribute</code>","text":"<pre><code>stage_configs = {(cfg.stage_type): cfg for cfg in (stage_configs or DEFAULT_PIPELINE_STAGES)}\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.connector","title":"connector  <code>instance-attribute</code>","text":"<pre><code>connector = connector or LocalConnector()\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.__init__","title":"__init__","text":"<pre><code>__init__(device: str, dtype: dtype, stage_configs: tuple[StageConfig, ...] | None = None, connector: StageConnector | None = None, config: 'RuntimeConfig | None' = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>def __init__(\n    self,\n    device: str,\n    dtype: torch.dtype,\n    stage_configs: tuple[StageConfig, ...] | None = None,\n    connector: StageConnector | None = None,\n    config: \"RuntimeConfig | None\" = None,\n) -&gt; None:\n    self.device = device\n    self.dtype = dtype\n    self.config = config\n    self.stage_configs = {\n        cfg.stage_type: cfg for cfg in (stage_configs or DEFAULT_PIPELINE_STAGES)\n    }\n    self.connector = connector or LocalConnector()\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor._stage_enabled","title":"_stage_enabled","text":"<pre><code>_stage_enabled(stage_type: StageType) -&gt; bool\n</code></pre> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>def _stage_enabled(self, stage_type: StageType) -&gt; bool:\n    cfg = self.stage_configs.get(stage_type)\n    return cfg is None or cfg.enabled\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor._iteration_batching_enabled","title":"_iteration_batching_enabled","text":"<pre><code>_iteration_batching_enabled() -&gt; bool\n</code></pre> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>def _iteration_batching_enabled(self) -&gt; bool:\n    try:\n        from worldkernels.runtime.forward_context import get_forward_context\n\n        return get_forward_context().iteration_batching\n    except RuntimeError:\n        return bool(self.config and self.config.iteration_batching)\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.execute_encode","title":"execute_encode","text":"<pre><code>execute_encode(world: WorldModel, action: Action) -&gt; StageOutput\n</code></pre> <p>Stage 1: encode action to conditioning tensor.</p> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>@torch.no_grad()\ndef execute_encode(\n    self,\n    world: WorldModel,\n    action: Action,\n) -&gt; StageOutput:\n    r\"\"\"Stage 1: encode action to conditioning tensor.\"\"\"\n    t0 = time.perf_counter()\n    action_tensor = world.encode_action(action)\n    elapsed = (time.perf_counter() - t0) * 1000.0\n    return StageOutput(\n        stage_type=StageType.ENCODE,\n        data=action_tensor,\n        timing_ms=elapsed,\n    )\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.execute_transition","title":"execute_transition","text":"<pre><code>execute_transition(world: WorldModel, state: LatentState, action_encoded: Tensor) -&gt; StageOutput\n</code></pre> <p>Stage 2: advance latent state.</p> <p>Treats <code>transition()</code> as opaque; the world's <code>transition_mode</code> determines internal behavior (bidirectional vs causal vs hybrid). When <code>iteration_batching</code> is on and the world supports it, drives <code>transition_iter</code> and uses the final yielded state \u2014 opening the seam for sessions to join a batch mid-denoise.</p> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>@torch.no_grad()\ndef execute_transition(\n    self,\n    world: WorldModel,\n    state: LatentState,\n    action_encoded: torch.Tensor,\n) -&gt; StageOutput:\n    r\"\"\"Stage 2: advance latent state.\n\n    Treats ``transition()`` as opaque; the world's ``transition_mode``\n    determines internal behavior (bidirectional vs causal vs hybrid).\n    When ``iteration_batching`` is on and the world supports it, drives\n    ``transition_iter`` and uses the final yielded state \u2014 opening the\n    seam for sessions to join a batch mid-denoise.\n    \"\"\"\n    t0 = time.perf_counter()\n    iter_batch_on = self._iteration_batching_enabled()\n    if iter_batch_on and getattr(world, \"supports_iteration_batching\", False):\n        new_state = state\n        for new_state in world.transition_iter(state, action_encoded):\n            pass\n    else:\n        new_state = world.transition(state, action_encoded)\n    elapsed = (time.perf_counter() - t0) * 1000.0\n    return StageOutput(\n        stage_type=StageType.TRANSITION,\n        data=new_state,\n        timing_ms=elapsed,\n        metadata={\"transition_mode\": world.transition_mode.value},\n    )\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.execute_decode","title":"execute_decode","text":"<pre><code>execute_decode(world: WorldModel, state: LatentState, modalities: list[str]) -&gt; StageOutput\n</code></pre> <p>Stage 3: decode latent state to observation.</p> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>@torch.no_grad()\ndef execute_decode(\n    self,\n    world: WorldModel,\n    state: LatentState,\n    modalities: list[str],\n) -&gt; StageOutput:\n    r\"\"\"Stage 3: decode latent state to observation.\"\"\"\n    t0 = time.perf_counter()\n    obs = world.decode_observation(state, modalities)\n    elapsed = (time.perf_counter() - t0) * 1000.0\n    return StageOutput(\n        stage_type=StageType.DECODE,\n        data=obs,\n        timing_ms=elapsed,\n    )\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.step","title":"step","text":"<pre><code>step(world: WorldModel, state: LatentState, action: Action, modalities: list[str], step_index: int, decode: bool = True) -&gt; tuple[LatentState, Observation]\n</code></pre> <p>Execute the full 3-stage pipeline for one simulation step.</p> <p>Parameters:</p> Name Type Description Default <code>world</code> <code>WorldModel</code> <p>The world model adapter.</p> required <code>state</code> <code>LatentState</code> <p>Current latent state.</p> required <code>action</code> <code>Action</code> <p>Action to apply.</p> required <code>modalities</code> <code>list[str]</code> <p>Observation modalities to decode.</p> required <code>step_index</code> <code>int</code> <p>Monotonic step counter.</p> required <code>decode</code> <code>bool</code> <p>If False, skip stage 3 and return latent-only observation.</p> <code>True</code> <p>Returns:</p> Type Description <code>tuple[LatentState, Observation]</code> <p>Tuple of (new_state, observation) with <code>stage_timing</code> attached.</p> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>@torch.no_grad()\ndef step(\n    self,\n    world: WorldModel,\n    state: LatentState,\n    action: Action,\n    modalities: list[str],\n    step_index: int,\n    decode: bool = True,\n) -&gt; tuple[LatentState, Observation]:\n    r\"\"\"Execute the full 3-stage pipeline for one simulation step.\n\n    Args:\n        world: The world model adapter.\n        state: Current latent state.\n        action: Action to apply.\n        modalities: Observation modalities to decode.\n        step_index: Monotonic step counter.\n        decode: If False, skip stage 3 and return latent-only observation.\n\n    Returns:\n        Tuple of (new_state, observation) with ``stage_timing`` attached.\n    \"\"\"\n    timing = StageTiming()\n    t_wall = time.perf_counter()\n\n    encode_out = self.execute_encode(world, action)\n    timing.encode_action_ms = encode_out.timing_ms\n\n    transition_out = self.execute_transition(\n        world,\n        state,\n        encode_out.data,\n    )\n    timing.transition_ms = transition_out.timing_ms\n    new_state = transition_out.data\n\n    if decode and self._stage_enabled(StageType.DECODE):\n        decode_out = self.execute_decode(world, new_state, modalities)\n        timing.decode_observation_ms = decode_out.timing_ms\n        obs = decode_out.data\n    else:\n        obs = Observation(step_index=step_index, generation_time_ms=0.0)\n        obs.decode_skipped = True\n\n    wall_ms = (time.perf_counter() - t_wall) * 1000.0\n    obs.step_index = step_index\n    obs.generation_time_ms = wall_ms\n    obs.stage_timing = timing\n\n    return new_state, obs\n</code></pre>"},{"location":"api/worldkernels/runtime/executor/#worldkernels.runtime.executor.Executor.execute_batched","title":"execute_batched","text":"<pre><code>execute_batched(requests: list[StepRequest]) -&gt; list[tuple[LatentState, Observation]]\n</code></pre> <p>Execute a batch of step requests.</p> <p>Currently iterates one request at a time; true batched forward passes over a compatibility group land with the throughput step. The list signature is the seam the scheduler dispatches into.</p> Source code in <code>worldkernels/runtime/executor.py</code> <pre><code>@torch.no_grad()\ndef execute_batched(\n    self,\n    requests: list[StepRequest],\n) -&gt; list[tuple[LatentState, Observation]]:\n    r\"\"\"Execute a batch of step requests.\n\n    Currently iterates one request at a time; true batched forward passes\n    over a compatibility group land with the throughput step. The list\n    signature is the seam the scheduler dispatches into.\n    \"\"\"\n    return [\n        self.step(\n            world=req.world,\n            state=req.state,\n            action=req.action,\n            modalities=req.modalities,\n            step_index=req.step_index,\n            decode=req.decode,\n        )\n        for req in requests\n    ]\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/","title":"forward_context","text":""},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context","title":"worldkernels.runtime.forward_context","text":"<p>Thread-local forward context.</p> <p>Threads execution state \u2014 attention metadata, parallel-state degrees, the active cache backend, the current CUDA stream \u2014 through a model forward pass without passing it as kwargs through every layer. Components read it via <code>get_forward_context()</code>; the runner sets it via <code>set_forward_context()</code>.</p>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['ForwardContext', 'get_forward_context', 'set_forward_context']\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context._local","title":"_local  <code>module-attribute</code>","text":"<pre><code>_local = threading.local()\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext","title":"ForwardContext  <code>dataclass</code>","text":"<p>State threaded through one batched forward pass.</p> <p>Parameters:</p> Name Type Description Default <code>attn_metadata</code> <code>Any</code> <p>Backend-specific attention state (KV block tables, sequence lengths) for the current batch.</p> <code>None</code> <code>cache_backend</code> <code>Any</code> <p>Active denoise-step cache (e.g. TeaCache), or <code>None</code>.</p> <code>None</code> <code>pool</code> <code>Any</code> <p>Active per-runner buffer pool (<code>LatentPool</code>), or <code>None</code>.</p> <code>None</code> <code>attention_backend</code> <code>str | None</code> <p>Resolved attention backend name (<code>\"flash\"</code> / <code>\"sdpa\"</code>) when the runtime forces a choice; <code>None</code> lets the selector fall through to the platform default.</p> <code>None</code> <code>stream</code> <code>Any</code> <p>Current compute CUDA stream, or <code>None</code> on CPU.</p> <code>None</code> <code>sp_world_size</code> <code>int</code> <p>Sequence-parallel world size for the active forward.</p> <code>1</code> <code>sp_padding</code> <code>int</code> <p>Sequence padding added to make the length SP-divisible.</p> <code>0</code> <code>extra</code> <code>dict[str, Any]</code> <p>Backend escape hatch for additional per-forward state.</p> <code>dict()</code> Source code in <code>worldkernels/runtime/forward_context.py</code> <pre><code>@dataclass\nclass ForwardContext:\n    r\"\"\"State threaded through one batched forward pass.\n\n    Args:\n        attn_metadata: Backend-specific attention state (KV block tables,\n            sequence lengths) for the current batch.\n        cache_backend: Active denoise-step cache (e.g. TeaCache), or ``None``.\n        pool: Active per-runner buffer pool (`LatentPool`), or ``None``.\n        attention_backend: Resolved attention backend name (``\"flash\"`` /\n            ``\"sdpa\"``) when the runtime forces a choice; ``None`` lets the\n            selector fall through to the platform default.\n        stream: Current compute CUDA stream, or ``None`` on CPU.\n        sp_world_size: Sequence-parallel world size for the active forward.\n        sp_padding: Sequence padding added to make the length SP-divisible.\n        extra: Backend escape hatch for additional per-forward state.\n    \"\"\"\n\n    attn_metadata: Any = None\n    cache_backend: Any = None\n    pool: Any = None\n    attention_backend: str | None = None\n    iteration_batching: bool = True\n    stream: Any = None\n    sp_world_size: int = 1\n    sp_padding: int = 0\n    extra: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.attn_metadata","title":"attn_metadata  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attn_metadata: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.cache_backend","title":"cache_backend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>cache_backend: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.pool","title":"pool  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pool: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.attention_backend","title":"attention_backend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>attention_backend: str | None = None\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.iteration_batching","title":"iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>iteration_batching: bool = True\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.stream","title":"stream  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stream: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.sp_world_size","title":"sp_world_size  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>sp_world_size: int = 1\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.sp_padding","title":"sp_padding  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>sp_padding: int = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.extra","title":"extra  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>extra: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.ForwardContext.__init__","title":"__init__","text":"<pre><code>__init__(attn_metadata: Any = None, cache_backend: Any = None, pool: Any = None, attention_backend: str | None = None, iteration_batching: bool = True, stream: Any = None, sp_world_size: int = 1, sp_padding: int = 0, extra: dict[str, Any] = dict()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.get_forward_context","title":"get_forward_context","text":"<pre><code>get_forward_context() -&gt; ForwardContext\n</code></pre> <p>Return the active context, raising if none is set.</p> Source code in <code>worldkernels/runtime/forward_context.py</code> <pre><code>def get_forward_context() -&gt; ForwardContext:\n    r\"\"\"Return the active context, raising if none is set.\"\"\"\n    ctx = getattr(_local, \"ctx\", None)\n    if ctx is None:\n        raise RuntimeError(\n            \"no active ForwardContext; wrap model execution in set_forward_context()\"\n        )\n    return ctx\n</code></pre>"},{"location":"api/worldkernels/runtime/forward_context/#worldkernels.runtime.forward_context.set_forward_context","title":"set_forward_context","text":"<pre><code>set_forward_context(ctx: ForwardContext) -&gt; Iterator[ForwardContext]\n</code></pre> <p>Bind <code>ctx</code> as the active forward context for the calling thread.</p> Source code in <code>worldkernels/runtime/forward_context.py</code> <pre><code>@contextmanager\ndef set_forward_context(ctx: ForwardContext) -&gt; Iterator[ForwardContext]:\n    r\"\"\"Bind ``ctx`` as the active forward context for the calling thread.\"\"\"\n    previous = getattr(_local, \"ctx\", None)\n    _local.ctx = ctx\n    try:\n        yield ctx\n    finally:\n        _local.ctx = previous\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/","title":"ipc","text":""},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc","title":"worldkernels.runtime.ipc","text":"<p>Tensor IPC across the engine \u2194 worker boundary.</p> <p>Two paths: - CUDA IPC: shares the device pointer via <code>cudaIpcGetMemHandle</code>.   Zero-copy across processes on the same GPU. - Host shared memory: <code>multiprocessing.shared_memory</code> with a D2H/H2D   copy. Slower, used when CUDA IPC fails (cgroups, MIG, containers without   <code>--ipc=host</code>).</p> <p>The owner side must keep the source tensor alive until the consumer releases the handle (refcounting via the protocol header).</p>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['IpcHandle', 'share_tensor', 'recv_tensor', 'ipc_available']\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._DISABLE_CUDA_IPC","title":"_DISABLE_CUDA_IPC  <code>module-attribute</code>","text":"<pre><code>_DISABLE_CUDA_IPC: bool = False\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.IpcHandle","title":"IpcHandle  <code>dataclass</code>","text":"<p>Serializable handle for a shared tensor.</p> <p>Either <code>mode=\"cuda\"</code> with <code>ipc_payload</code> from <code>Tensor._share_cuda_()</code>, or <code>mode=\"shm\"</code> with the name of a <code>multiprocessing.shared_memory</code> block.</p> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>@dataclass\nclass IpcHandle:\n    r\"\"\"Serializable handle for a shared tensor.\n\n    Either ``mode=\"cuda\"`` with ``ipc_payload`` from ``Tensor._share_cuda_()``,\n    or ``mode=\"shm\"`` with the name of a ``multiprocessing.shared_memory`` block.\n    \"\"\"\n\n    mode: Literal[\"cuda\", \"shm\"]\n    shape: tuple[int, ...]\n    dtype: str\n    device: str\n    payload: dict[str, Any]\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.IpcHandle.mode","title":"mode  <code>instance-attribute</code>","text":"<pre><code>mode: Literal['cuda', 'shm']\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.IpcHandle.shape","title":"shape  <code>instance-attribute</code>","text":"<pre><code>shape: tuple[int, ...]\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.IpcHandle.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: str\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.IpcHandle.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.IpcHandle.payload","title":"payload  <code>instance-attribute</code>","text":"<pre><code>payload: dict[str, Any]\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.IpcHandle.__init__","title":"__init__","text":"<pre><code>__init__(mode: Literal['cuda', 'shm'], shape: tuple[int, ...], dtype: str, device: str, payload: dict[str, Any]) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.disable_cuda_ipc","title":"disable_cuda_ipc","text":"<pre><code>disable_cuda_ipc(disable: bool = True) -&gt; None\n</code></pre> <p>Force the host-shm fallback path (used by tests).</p> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def disable_cuda_ipc(disable: bool = True) -&gt; None:\n    r\"\"\"Force the host-shm fallback path (used by tests).\"\"\"\n    global _DISABLE_CUDA_IPC\n    _DISABLE_CUDA_IPC = disable\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.ipc_available","title":"ipc_available","text":"<pre><code>ipc_available() -&gt; bool\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def ipc_available() -&gt; bool:\n    if _DISABLE_CUDA_IPC:\n        return False\n    try:\n        import torch\n\n        return torch.cuda.is_available()\n    except ImportError:\n        return False\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.share_tensor","title":"share_tensor","text":"<pre><code>share_tensor(t: Any) -&gt; IpcHandle\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def share_tensor(t: Any) -&gt; IpcHandle:\n    if _DISABLE_CUDA_IPC or not _is_cuda_tensor(t):\n        return _share_via_shm(t)\n    try:\n        return _share_via_cuda(t)\n    except (RuntimeError, OSError) as exc:\n        log.warning(\"CUDA IPC failed (%s); falling back to host shared memory\", exc)\n        return _share_via_shm(t)\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc.recv_tensor","title":"recv_tensor","text":"<pre><code>recv_tensor(handle: IpcHandle) -&gt; Any\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def recv_tensor(handle: IpcHandle) -&gt; Any:\n    if handle.mode == \"cuda\":\n        return _recv_via_cuda(handle)\n    return _recv_via_shm(handle)\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._is_cuda_tensor","title":"_is_cuda_tensor","text":"<pre><code>_is_cuda_tensor(t: Any) -&gt; bool\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _is_cuda_tensor(t: Any) -&gt; bool:\n    try:\n        import torch\n\n        return isinstance(t, torch.Tensor) and t.is_cuda\n    except ImportError:\n        return False\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._share_via_cuda","title":"_share_via_cuda","text":"<pre><code>_share_via_cuda(t: Any) -&gt; IpcHandle\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _share_via_cuda(t: Any) -&gt; IpcHandle:\n    payload = t._share_cuda_()\n    return IpcHandle(\n        mode=\"cuda\",\n        shape=tuple(t.shape),\n        dtype=str(t.dtype),\n        device=str(t.device),\n        payload={\n            \"device\": payload[0],\n            \"handle\": payload[1],\n            \"size_bytes\": payload[2],\n            \"offset_bytes\": payload[3],\n            \"ref_counter_handle\": payload[4] if len(payload) &gt; 4 else None,\n            \"ref_counter_offset\": payload[5] if len(payload) &gt; 5 else None,\n            \"event_handle\": payload[6] if len(payload) &gt; 6 else None,\n            \"event_sync_required\": payload[7] if len(payload) &gt; 7 else None,\n        },\n    )\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._recv_via_cuda","title":"_recv_via_cuda","text":"<pre><code>_recv_via_cuda(handle: IpcHandle) -&gt; Any\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _recv_via_cuda(handle: IpcHandle) -&gt; Any:\n    import torch\n    from torch.multiprocessing.reductions import rebuild_cuda_tensor\n\n    p = handle.payload\n    dtype = _str_to_dtype(handle.dtype)\n    args = (\n        torch.Tensor,\n        handle.shape,\n        (1,) * len(handle.shape),\n        0,\n        dtype,\n        p[\"device\"],\n        p[\"handle\"],\n        p[\"size_bytes\"],\n        p[\"offset_bytes\"],\n        p.get(\"ref_counter_handle\"),\n        p.get(\"ref_counter_offset\"),\n        p.get(\"event_handle\"),\n        p.get(\"event_sync_required\"),\n    )\n    return rebuild_cuda_tensor(*args)  # type: ignore[call-arg]\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._share_via_shm","title":"_share_via_shm","text":"<pre><code>_share_via_shm(t: Any) -&gt; IpcHandle\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _share_via_shm(t: Any) -&gt; IpcHandle:\n    from multiprocessing import shared_memory\n\n    arr = _tensor_to_numpy(t)\n    shm = shared_memory.SharedMemory(create=True, size=max(1, arr.nbytes))\n    assert shm.buf is not None\n    shm.buf[: arr.nbytes] = arr.tobytes()\n    return IpcHandle(\n        mode=\"shm\",\n        shape=tuple(t.shape if hasattr(t, \"shape\") else arr.shape),\n        dtype=str(getattr(t, \"dtype\", arr.dtype)),\n        device=str(getattr(t, \"device\", \"cpu\")),\n        payload={\"name\": shm.name, \"size_bytes\": arr.nbytes},\n    )\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._recv_via_shm","title":"_recv_via_shm","text":"<pre><code>_recv_via_shm(handle: IpcHandle) -&gt; Any\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _recv_via_shm(handle: IpcHandle) -&gt; Any:\n    from multiprocessing import shared_memory\n\n    import numpy as np\n\n    shm = shared_memory.SharedMemory(name=handle.payload[\"name\"])\n    assert shm.buf is not None\n    dtype = _str_to_numpy_dtype(handle.dtype)\n    nbytes = handle.payload[\"size_bytes\"]\n    arr = np.frombuffer(shm.buf[:nbytes], dtype=dtype).reshape(handle.shape).copy()\n    try:\n        import torch\n\n        out = torch.from_numpy(arr)\n        target_device = handle.device\n        if target_device.startswith(\"cuda\"):\n            out = out.to(target_device)\n        return out\n    except ImportError:\n        return arr\n    finally:\n        shm.close()\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._tensor_to_numpy","title":"_tensor_to_numpy","text":"<pre><code>_tensor_to_numpy(t: Any)\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _tensor_to_numpy(t: Any):\n    try:\n        import torch\n\n        if isinstance(t, torch.Tensor):\n            return t.detach().contiguous().cpu().numpy()\n    except ImportError:\n        pass\n    import numpy as np\n\n    return np.asarray(t)\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._str_to_dtype","title":"_str_to_dtype","text":"<pre><code>_str_to_dtype(name: str)\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _str_to_dtype(name: str):\n    import torch\n\n    table = {\n        \"torch.float32\": torch.float32,\n        \"torch.float16\": torch.float16,\n        \"torch.bfloat16\": torch.bfloat16,\n        \"torch.int32\": torch.int32,\n        \"torch.int64\": torch.int64,\n        \"torch.uint8\": torch.uint8,\n        \"torch.bool\": torch.bool,\n    }\n    if name in table:\n        return table[name]\n    short = name.split(\".\")[-1]\n    return getattr(torch, short, torch.float32)\n</code></pre>"},{"location":"api/worldkernels/runtime/ipc/#worldkernels.runtime.ipc._str_to_numpy_dtype","title":"_str_to_numpy_dtype","text":"<pre><code>_str_to_numpy_dtype(name: str)\n</code></pre> Source code in <code>worldkernels/runtime/ipc.py</code> <pre><code>def _str_to_numpy_dtype(name: str):\n    import numpy as np\n\n    short = name.split(\".\")[-1]\n    table = {\n        \"float32\": np.float32,\n        \"float16\": np.float16,\n        \"bfloat16\": np.float32,\n        \"int32\": np.int32,\n        \"int64\": np.int64,\n        \"uint8\": np.uint8,\n        \"bool\": np.bool_,\n    }\n    return table.get(short, np.float32)\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/","title":"locks","text":""},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks","title":"worldkernels.runtime.locks","text":"<p>Per-model lockfile storage under <code>WORLDKERNELS_HOME/locks/</code>.</p>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock","title":"EnvLock  <code>dataclass</code>","text":"<p>Lockfile metadata for a per-model isolated venv.</p> Source code in <code>worldkernels/runtime/locks.py</code> <pre><code>@dataclass\nclass EnvLock:\n    r\"\"\"Lockfile metadata for a per-model isolated venv.\"\"\"\n\n    model_id: str\n    deps_hash: str\n    torch_abi: str\n    device: str\n    requirements: list[str] = field(default_factory=list)\n    resolved: str = \"\"\n\n    @property\n    def path(self) -&gt; Path:\n        return locks_dir() / f\"{_slug(self.model_id)}.lock.json\"\n\n    def write(self) -&gt; None:\n        self.path.write_text(json.dumps(asdict(self), indent=2))\n\n    @classmethod\n    def read(cls, model_id: str) -&gt; \"EnvLock | None\":\n        p = locks_dir() / f\"{_slug(model_id)}.lock.json\"\n        if not p.exists():\n            return None\n        return cls(**json.loads(p.read_text()))\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.model_id","title":"model_id  <code>instance-attribute</code>","text":"<pre><code>model_id: str\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.deps_hash","title":"deps_hash  <code>instance-attribute</code>","text":"<pre><code>deps_hash: str\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.torch_abi","title":"torch_abi  <code>instance-attribute</code>","text":"<pre><code>torch_abi: str\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.requirements","title":"requirements  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>requirements: list[str] = field(default_factory=list)\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.resolved","title":"resolved  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>resolved: str = ''\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.path","title":"path  <code>property</code>","text":"<pre><code>path: Path\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.__init__","title":"__init__","text":"<pre><code>__init__(model_id: str, deps_hash: str, torch_abi: str, device: str, requirements: list[str] = list(), resolved: str = '') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.write","title":"write","text":"<pre><code>write() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/locks.py</code> <pre><code>def write(self) -&gt; None:\n    self.path.write_text(json.dumps(asdict(self), indent=2))\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.EnvLock.read","title":"read  <code>classmethod</code>","text":"<pre><code>read(model_id: str) -&gt; 'EnvLock | None'\n</code></pre> Source code in <code>worldkernels/runtime/locks.py</code> <pre><code>@classmethod\ndef read(cls, model_id: str) -&gt; \"EnvLock | None\":\n    p = locks_dir() / f\"{_slug(model_id)}.lock.json\"\n    if not p.exists():\n        return None\n    return cls(**json.loads(p.read_text()))\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.locks_dir","title":"locks_dir","text":"<pre><code>locks_dir() -&gt; Path\n</code></pre> Source code in <code>worldkernels/runtime/locks.py</code> <pre><code>def locks_dir() -&gt; Path:\n    p = cache.home() / \"locks\"\n    p.mkdir(parents=True, exist_ok=True)\n    return p\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.deps_hash","title":"deps_hash","text":"<pre><code>deps_hash(deps: list[str], torch_abi: str, device: str) -&gt; str\n</code></pre> Source code in <code>worldkernels/runtime/locks.py</code> <pre><code>def deps_hash(deps: list[str], torch_abi: str, device: str) -&gt; str:\n    payload = \"\\n\".join(sorted(deps)) + f\"|{torch_abi}|{device}\"\n    return hashlib.sha256(payload.encode()).hexdigest()[:16]\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks.torch_abi_tag","title":"torch_abi_tag","text":"<pre><code>torch_abi_tag() -&gt; str\n</code></pre> <p>Return a short tag that changes whenever torch's binary ABI changes.</p> Source code in <code>worldkernels/runtime/locks.py</code> <pre><code>def torch_abi_tag() -&gt; str:\n    r\"\"\"Return a short tag that changes whenever torch's binary ABI changes.\"\"\"\n    try:\n        import torch\n\n        cuda = getattr(torch.version, \"cuda\", None) or \"nocuda\"\n        hip = getattr(torch.version, \"hip\", None) or \"nohip\"\n        return f\"{torch.__version__}-cu{cuda}-hip{hip}\"\n    except ImportError:\n        return \"no-torch\"\n</code></pre>"},{"location":"api/worldkernels/runtime/locks/#worldkernels.runtime.locks._slug","title":"_slug","text":"<pre><code>_slug(model_id: str) -&gt; str\n</code></pre> Source code in <code>worldkernels/runtime/locks.py</code> <pre><code>def _slug(model_id: str) -&gt; str:\n    return model_id.replace(\"/\", \"_\").replace(\":\", \"_\")\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/","title":"memory","text":""},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory","title":"worldkernels.runtime.memory","text":"<p>Memory subsystem: block allocation, KV paging, buffer pools, offloading.</p> <p><code>TrajectoryCache</code> is torch-free and eagerly exported; the tensor-backed managers are lazily imported.</p> <p>Modules:</p> Name Description <code>block_manager</code> <p>Fixed-size block (slab) allocator for latent and KV storage.</p> <code>kv_cache</code> <p>Paged KV cache for causal world models.</p> <code>latent_pool</code> <p>Pre-allocated tensor buffer pool.</p> <code>offload</code> <p>Three-tier session-state offloading.</p> <code>trajectory_cache</code> <p>Trajectory prefix cache.</p>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['BlockManager', 'LatentPool', 'Offloader', 'MemoryTier', 'KVCacheManager', 'TrajectoryCache', 'PrefixMatch']\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'BlockManager': ('worldkernels.runtime.memory.block_manager', 'BlockManager'), 'LatentPool': ('worldkernels.runtime.memory.latent_pool', 'LatentPool'), 'Offloader': ('worldkernels.runtime.memory.offload', 'Offloader'), 'MemoryTier': ('worldkernels.runtime.memory.offload', 'MemoryTier'), 'KVCacheManager': ('worldkernels.runtime.memory.kv_cache', 'KVCacheManager')}\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.PrefixMatch","title":"PrefixMatch  <code>dataclass</code>","text":"<p>Result of a trajectory-prefix lookup.</p> <p>Parameters:</p> Name Type Description Default <code>length</code> <code>int</code> <p>Number of leading actions matched.</p> required <code>block_ids</code> <code>list[int]</code> <p>Cached latent block ids for the matched prefix.</p> required Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>@dataclass\nclass PrefixMatch:\n    r\"\"\"Result of a trajectory-prefix lookup.\n\n    Args:\n        length: Number of leading actions matched.\n        block_ids: Cached latent block ids for the matched prefix.\n    \"\"\"\n\n    length: int\n    block_ids: list[int]\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.PrefixMatch.length","title":"length  <code>instance-attribute</code>","text":"<pre><code>length: int\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.PrefixMatch.block_ids","title":"block_ids  <code>instance-attribute</code>","text":"<pre><code>block_ids: list[int]\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.PrefixMatch.__init__","title":"__init__","text":"<pre><code>__init__(length: int, block_ids: list[int]) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache","title":"TrajectoryCache","text":"<p>Radix-tree cache mapping action-history prefixes to latent block ids.</p> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>class TrajectoryCache:\n    r\"\"\"Radix-tree cache mapping action-history prefixes to latent block ids.\"\"\"\n\n    def __init__(self) -&gt; None:\n        self._root = _TrieNode()\n        self._size = 0\n\n    @property\n    def size(self) -&gt; int:\n        r\"\"\"Number of cached trajectory steps.\"\"\"\n        return self._size\n\n    def insert(self, action_hashes: list[str], block_ids: list[int]) -&gt; None:\n        r\"\"\"Cache the per-step latent block id for an action sequence.\n\n        Args:\n            action_hashes: Content hashes of each action in order.\n            block_ids: Latent block id produced after each action; same length.\n        \"\"\"\n        if len(action_hashes) != len(block_ids):\n            raise ValueError(\"action_hashes and block_ids must have equal length\")\n        node = self._root\n        for action_hash, block_id in zip(action_hashes, block_ids):\n            child = node.children.get(action_hash)\n            if child is None:\n                child = _TrieNode()\n                node.children[action_hash] = child\n                self._size += 1\n            child.block_ids = [block_id]\n            node = child\n\n    def match(self, action_hashes: list[str]) -&gt; PrefixMatch:\n        r\"\"\"Return the longest cached prefix of ``action_hashes``.\"\"\"\n        node = self._root\n        matched: list[int] = []\n        for action_hash in action_hashes:\n            child = node.children.get(action_hash)\n            if child is None or not child.block_ids:\n                break\n            matched.append(child.block_ids[0])\n            node = child\n        return PrefixMatch(length=len(matched), block_ids=matched)\n\n    def clear(self) -&gt; None:\n        self._root = _TrieNode()\n        self._size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache._root","title":"_root  <code>instance-attribute</code>","text":"<pre><code>_root = _TrieNode()\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache._size","title":"_size  <code>instance-attribute</code>","text":"<pre><code>_size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache.size","title":"size  <code>property</code>","text":"<pre><code>size: int\n</code></pre> <p>Number of cached trajectory steps.</p>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache.__init__","title":"__init__","text":"<pre><code>__init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._root = _TrieNode()\n    self._size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache.insert","title":"insert","text":"<pre><code>insert(action_hashes: list[str], block_ids: list[int]) -&gt; None\n</code></pre> <p>Cache the per-step latent block id for an action sequence.</p> <p>Parameters:</p> Name Type Description Default <code>action_hashes</code> <code>list[str]</code> <p>Content hashes of each action in order.</p> required <code>block_ids</code> <code>list[int]</code> <p>Latent block id produced after each action; same length.</p> required Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def insert(self, action_hashes: list[str], block_ids: list[int]) -&gt; None:\n    r\"\"\"Cache the per-step latent block id for an action sequence.\n\n    Args:\n        action_hashes: Content hashes of each action in order.\n        block_ids: Latent block id produced after each action; same length.\n    \"\"\"\n    if len(action_hashes) != len(block_ids):\n        raise ValueError(\"action_hashes and block_ids must have equal length\")\n    node = self._root\n    for action_hash, block_id in zip(action_hashes, block_ids):\n        child = node.children.get(action_hash)\n        if child is None:\n            child = _TrieNode()\n            node.children[action_hash] = child\n            self._size += 1\n        child.block_ids = [block_id]\n        node = child\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache.match","title":"match","text":"<pre><code>match(action_hashes: list[str]) -&gt; PrefixMatch\n</code></pre> <p>Return the longest cached prefix of <code>action_hashes</code>.</p> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def match(self, action_hashes: list[str]) -&gt; PrefixMatch:\n    r\"\"\"Return the longest cached prefix of ``action_hashes``.\"\"\"\n    node = self._root\n    matched: list[int] = []\n    for action_hash in action_hashes:\n        child = node.children.get(action_hash)\n        if child is None or not child.block_ids:\n            break\n        matched.append(child.block_ids[0])\n        node = child\n    return PrefixMatch(length=len(matched), block_ids=matched)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.TrajectoryCache.clear","title":"clear","text":"<pre><code>clear() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def clear(self) -&gt; None:\n    self._root = _TrieNode()\n    self._size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/#worldkernels.runtime.memory.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/runtime/memory/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/","title":"block_manager","text":""},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager","title":"worldkernels.runtime.memory.block_manager","text":"<p>Fixed-size block (slab) allocator for latent and KV storage.</p> <p>Pre-allocates one contiguous slab on the device and hands out block indices, so steady-state operation never calls <code>cudaMalloc</code> / <code>cudaFree</code>. Blocks are reference-counted, which makes copy-on-write sharing (session branching, trajectory-prefix reuse) a refcount bump rather than a copy.</p>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['BlockManager']\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager","title":"BlockManager","text":"<p>Slab allocator handing out fixed-shape blocks of a pre-allocated tensor.</p> <p>Parameters:</p> Name Type Description Default <code>block_shape</code> <code>'Sequence[int]'</code> <p>Shape of one block (the per-block trailing dims).</p> required <code>num_blocks</code> <code>int</code> <p>Number of blocks in the slab.</p> required <code>device</code> <code>str</code> <p>Device the slab lives on.</p> required <code>dtype</code> <code>dtype</code> <p>Slab dtype.</p> required Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>class BlockManager:\n    r\"\"\"Slab allocator handing out fixed-shape blocks of a pre-allocated tensor.\n\n    Args:\n        block_shape: Shape of one block (the per-block trailing dims).\n        num_blocks: Number of blocks in the slab.\n        device: Device the slab lives on.\n        dtype: Slab dtype.\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        block_shape: \"Sequence[int]\",\n        num_blocks: int,\n        device: str,\n        dtype: torch.dtype,\n    ) -&gt; None:\n        if num_blocks &lt; 1:\n            raise ValueError(f\"num_blocks must be &gt;= 1, got {num_blocks}\")\n        self.block_shape = tuple(block_shape)\n        self.num_blocks = num_blocks\n        self.device = device\n        self.dtype = dtype\n        self._slab = torch.empty((num_blocks, *self.block_shape), device=device, dtype=dtype)\n        self._free: list[int] = list(reversed(range(num_blocks)))\n        self._refcount: dict[int, int] = {}\n\n    @property\n    def num_free(self) -&gt; int:\n        return len(self._free)\n\n    @property\n    def num_allocated(self) -&gt; int:\n        return self.num_blocks - len(self._free)\n\n    def allocate(self, n_blocks: int = 1) -&gt; list[int]:\n        r\"\"\"Reserve ``n_blocks`` blocks, returning their ids (refcount 1).\"\"\"\n        if n_blocks &lt; 0:\n            raise ValueError(f\"n_blocks must be &gt;= 0, got {n_blocks}\")\n        if n_blocks &gt; len(self._free):\n            raise MemoryError(\n                f\"block pool exhausted: requested {n_blocks}, {len(self._free)} free \"\n                f\"of {self.num_blocks}\"\n            )\n        ids = [self._free.pop() for _ in range(n_blocks)]\n        for block_id in ids:\n            self._refcount[block_id] = 1\n        return ids\n\n    def block(self, block_id: int) -&gt; torch.Tensor:\n        r\"\"\"Return the tensor view of block ``block_id``.\"\"\"\n        return self._slab[block_id]\n\n    def share(self, block_ids: \"Sequence[int]\") -&gt; list[int]:\n        r\"\"\"Copy-on-write share: bump refcounts and return the same ids.\"\"\"\n        for block_id in block_ids:\n            self._refcount[block_id] += 1\n        return list(block_ids)\n\n    def free(self, block_ids: \"Sequence[int]\") -&gt; None:\n        r\"\"\"Release a reference to each block; return it to the free list at zero.\"\"\"\n        for block_id in block_ids:\n            count = self._refcount.get(block_id, 0)\n            if count &lt;= 0:\n                continue\n            if count == 1:\n                del self._refcount[block_id]\n                self._free.append(block_id)\n            else:\n                self._refcount[block_id] = count - 1\n\n    def refcount(self, block_id: int) -&gt; int:\n        return self._refcount.get(block_id, 0)\n\n    def reset(self) -&gt; None:\n        r\"\"\"Release every block.\"\"\"\n        self._free = list(reversed(range(self.num_blocks)))\n        self._refcount.clear()\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.block_shape","title":"block_shape  <code>instance-attribute</code>","text":"<pre><code>block_shape = tuple(block_shape)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.num_blocks","title":"num_blocks  <code>instance-attribute</code>","text":"<pre><code>num_blocks = num_blocks\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype = dtype\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager._slab","title":"_slab  <code>instance-attribute</code>","text":"<pre><code>_slab = torch.empty((num_blocks, *(self.block_shape)), device=device, dtype=dtype)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager._free","title":"_free  <code>instance-attribute</code>","text":"<pre><code>_free: list[int] = list(reversed(range(num_blocks)))\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager._refcount","title":"_refcount  <code>instance-attribute</code>","text":"<pre><code>_refcount: dict[int, int] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.num_free","title":"num_free  <code>property</code>","text":"<pre><code>num_free: int\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.num_allocated","title":"num_allocated  <code>property</code>","text":"<pre><code>num_allocated: int\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.__init__","title":"__init__","text":"<pre><code>__init__(*, block_shape: 'Sequence[int]', num_blocks: int, device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>def __init__(\n    self,\n    *,\n    block_shape: \"Sequence[int]\",\n    num_blocks: int,\n    device: str,\n    dtype: torch.dtype,\n) -&gt; None:\n    if num_blocks &lt; 1:\n        raise ValueError(f\"num_blocks must be &gt;= 1, got {num_blocks}\")\n    self.block_shape = tuple(block_shape)\n    self.num_blocks = num_blocks\n    self.device = device\n    self.dtype = dtype\n    self._slab = torch.empty((num_blocks, *self.block_shape), device=device, dtype=dtype)\n    self._free: list[int] = list(reversed(range(num_blocks)))\n    self._refcount: dict[int, int] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.allocate","title":"allocate","text":"<pre><code>allocate(n_blocks: int = 1) -&gt; list[int]\n</code></pre> <p>Reserve <code>n_blocks</code> blocks, returning their ids (refcount 1).</p> Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>def allocate(self, n_blocks: int = 1) -&gt; list[int]:\n    r\"\"\"Reserve ``n_blocks`` blocks, returning their ids (refcount 1).\"\"\"\n    if n_blocks &lt; 0:\n        raise ValueError(f\"n_blocks must be &gt;= 0, got {n_blocks}\")\n    if n_blocks &gt; len(self._free):\n        raise MemoryError(\n            f\"block pool exhausted: requested {n_blocks}, {len(self._free)} free \"\n            f\"of {self.num_blocks}\"\n        )\n    ids = [self._free.pop() for _ in range(n_blocks)]\n    for block_id in ids:\n        self._refcount[block_id] = 1\n    return ids\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.block","title":"block","text":"<pre><code>block(block_id: int) -&gt; Tensor\n</code></pre> <p>Return the tensor view of block <code>block_id</code>.</p> Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>def block(self, block_id: int) -&gt; torch.Tensor:\n    r\"\"\"Return the tensor view of block ``block_id``.\"\"\"\n    return self._slab[block_id]\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.share","title":"share","text":"<pre><code>share(block_ids: 'Sequence[int]') -&gt; list[int]\n</code></pre> <p>Copy-on-write share: bump refcounts and return the same ids.</p> Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>def share(self, block_ids: \"Sequence[int]\") -&gt; list[int]:\n    r\"\"\"Copy-on-write share: bump refcounts and return the same ids.\"\"\"\n    for block_id in block_ids:\n        self._refcount[block_id] += 1\n    return list(block_ids)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.free","title":"free","text":"<pre><code>free(block_ids: 'Sequence[int]') -&gt; None\n</code></pre> <p>Release a reference to each block; return it to the free list at zero.</p> Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>def free(self, block_ids: \"Sequence[int]\") -&gt; None:\n    r\"\"\"Release a reference to each block; return it to the free list at zero.\"\"\"\n    for block_id in block_ids:\n        count = self._refcount.get(block_id, 0)\n        if count &lt;= 0:\n            continue\n        if count == 1:\n            del self._refcount[block_id]\n            self._free.append(block_id)\n        else:\n            self._refcount[block_id] = count - 1\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.refcount","title":"refcount","text":"<pre><code>refcount(block_id: int) -&gt; int\n</code></pre> Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>def refcount(self, block_id: int) -&gt; int:\n    return self._refcount.get(block_id, 0)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/block_manager/#worldkernels.runtime.memory.block_manager.BlockManager.reset","title":"reset","text":"<pre><code>reset() -&gt; None\n</code></pre> <p>Release every block.</p> Source code in <code>worldkernels/runtime/memory/block_manager.py</code> <pre><code>def reset(self) -&gt; None:\n    r\"\"\"Release every block.\"\"\"\n    self._free = list(reversed(range(self.num_blocks)))\n    self._refcount.clear()\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/","title":"kv_cache","text":""},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache","title":"worldkernels.runtime.memory.kv_cache","text":"<p>Paged KV cache for causal world models.</p> <p>Each session's KV cache is a chain of fixed-size blocks drawn from a <code>BlockManager</code>. Branching a session forks the block table copy-on-write: blocks are shared (a refcount bump) until one branch writes past the divergence point.</p> <p>Only causal worlds (<code>transition_mode == CAUSAL</code>) hold a KV cache; bidirectional worlds pay none of this cost.</p>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['KVCacheManager']\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager","title":"KVCacheManager","text":"<p>Per-session paged KV-cache block tables over a shared block pool.</p> <p>Parameters:</p> Name Type Description Default <code>block_manager</code> <code>'BlockManager'</code> <p>The block pool KV blocks are drawn from.</p> required Source code in <code>worldkernels/runtime/memory/kv_cache.py</code> <pre><code>class KVCacheManager:\n    r\"\"\"Per-session paged KV-cache block tables over a shared block pool.\n\n    Args:\n        block_manager: The block pool KV blocks are drawn from.\n    \"\"\"\n\n    def __init__(self, block_manager: \"BlockManager\") -&gt; None:\n        self.blocks = block_manager\n        self._tables: dict[str, list[int]] = {}\n\n    def allocate(self, session_id: str, num_blocks: int) -&gt; list[int]:\n        r\"\"\"Allocate ``num_blocks`` KV blocks for ``session_id``.\"\"\"\n        ids = self.blocks.allocate(num_blocks)\n        self._tables[session_id] = list(ids)\n        return ids\n\n    def append(self, session_id: str, num_blocks: int = 1) -&gt; list[int]:\n        r\"\"\"Grow a session's KV table by ``num_blocks`` (one decode chunk).\"\"\"\n        if session_id not in self._tables:\n            raise KeyError(f\"no KV table for session {session_id!r}\")\n        ids = self.blocks.allocate(num_blocks)\n        self._tables[session_id].extend(ids)\n        return ids\n\n    def block_table(self, session_id: str) -&gt; list[int]:\n        r\"\"\"Return the ordered block ids backing a session's KV cache.\"\"\"\n        return list(self._tables.get(session_id, []))\n\n    def fork(self, src_session_id: str, dst_session_id: str) -&gt; list[int]:\n        r\"\"\"Copy-on-write fork: ``dst`` shares ``src``'s blocks until it writes.\"\"\"\n        if src_session_id not in self._tables:\n            raise KeyError(f\"no KV table for session {src_session_id!r}\")\n        shared = self.blocks.share(self._tables[src_session_id])\n        self._tables[dst_session_id] = shared\n        return shared\n\n    def free(self, session_id: str) -&gt; None:\n        r\"\"\"Release a session's KV blocks.\"\"\"\n        table = self._tables.pop(session_id, None)\n        if table:\n            self.blocks.free(table)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager.blocks","title":"blocks  <code>instance-attribute</code>","text":"<pre><code>blocks = block_manager\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager._tables","title":"_tables  <code>instance-attribute</code>","text":"<pre><code>_tables: dict[str, list[int]] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager.__init__","title":"__init__","text":"<pre><code>__init__(block_manager: 'BlockManager') -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/kv_cache.py</code> <pre><code>def __init__(self, block_manager: \"BlockManager\") -&gt; None:\n    self.blocks = block_manager\n    self._tables: dict[str, list[int]] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager.allocate","title":"allocate","text":"<pre><code>allocate(session_id: str, num_blocks: int) -&gt; list[int]\n</code></pre> <p>Allocate <code>num_blocks</code> KV blocks for <code>session_id</code>.</p> Source code in <code>worldkernels/runtime/memory/kv_cache.py</code> <pre><code>def allocate(self, session_id: str, num_blocks: int) -&gt; list[int]:\n    r\"\"\"Allocate ``num_blocks`` KV blocks for ``session_id``.\"\"\"\n    ids = self.blocks.allocate(num_blocks)\n    self._tables[session_id] = list(ids)\n    return ids\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager.append","title":"append","text":"<pre><code>append(session_id: str, num_blocks: int = 1) -&gt; list[int]\n</code></pre> <p>Grow a session's KV table by <code>num_blocks</code> (one decode chunk).</p> Source code in <code>worldkernels/runtime/memory/kv_cache.py</code> <pre><code>def append(self, session_id: str, num_blocks: int = 1) -&gt; list[int]:\n    r\"\"\"Grow a session's KV table by ``num_blocks`` (one decode chunk).\"\"\"\n    if session_id not in self._tables:\n        raise KeyError(f\"no KV table for session {session_id!r}\")\n    ids = self.blocks.allocate(num_blocks)\n    self._tables[session_id].extend(ids)\n    return ids\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager.block_table","title":"block_table","text":"<pre><code>block_table(session_id: str) -&gt; list[int]\n</code></pre> <p>Return the ordered block ids backing a session's KV cache.</p> Source code in <code>worldkernels/runtime/memory/kv_cache.py</code> <pre><code>def block_table(self, session_id: str) -&gt; list[int]:\n    r\"\"\"Return the ordered block ids backing a session's KV cache.\"\"\"\n    return list(self._tables.get(session_id, []))\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager.fork","title":"fork","text":"<pre><code>fork(src_session_id: str, dst_session_id: str) -&gt; list[int]\n</code></pre> <p>Copy-on-write fork: <code>dst</code> shares <code>src</code>'s blocks until it writes.</p> Source code in <code>worldkernels/runtime/memory/kv_cache.py</code> <pre><code>def fork(self, src_session_id: str, dst_session_id: str) -&gt; list[int]:\n    r\"\"\"Copy-on-write fork: ``dst`` shares ``src``'s blocks until it writes.\"\"\"\n    if src_session_id not in self._tables:\n        raise KeyError(f\"no KV table for session {src_session_id!r}\")\n    shared = self.blocks.share(self._tables[src_session_id])\n    self._tables[dst_session_id] = shared\n    return shared\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/kv_cache/#worldkernels.runtime.memory.kv_cache.KVCacheManager.free","title":"free","text":"<pre><code>free(session_id: str) -&gt; None\n</code></pre> <p>Release a session's KV blocks.</p> Source code in <code>worldkernels/runtime/memory/kv_cache.py</code> <pre><code>def free(self, session_id: str) -&gt; None:\n    r\"\"\"Release a session's KV blocks.\"\"\"\n    table = self._tables.pop(session_id, None)\n    if table:\n        self.blocks.free(table)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/","title":"latent_pool","text":""},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool","title":"worldkernels.runtime.memory.latent_pool","text":"<p>Pre-allocated tensor buffer pool.</p> <p>Hot-path code (the denoise loop, frame buffers) must never call <code>torch.zeros</code> / <code>torch.empty</code>. The pool hands out reusable buffers keyed by <code>(shape, dtype)</code> and reclaims them on release.</p>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['LatentPool']\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool","title":"LatentPool","text":"<p>Buffer pool that recycles tensors by shape and dtype.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>str</code> <p>Device buffers are allocated on.</p> required Source code in <code>worldkernels/runtime/memory/latent_pool.py</code> <pre><code>class LatentPool:\n    r\"\"\"Buffer pool that recycles tensors by shape and dtype.\n\n    Args:\n        device: Device buffers are allocated on.\n    \"\"\"\n\n    def __init__(self, device: str) -&gt; None:\n        self.device = device\n        self._free: dict[tuple, list[torch.Tensor]] = {}\n        self._in_use = 0\n\n    @property\n    def num_in_use(self) -&gt; int:\n        return self._in_use\n\n    @property\n    def num_pooled(self) -&gt; int:\n        return sum(len(bufs) for bufs in self._free.values())\n\n    def acquire(self, shape: \"Sequence[int]\", dtype: torch.dtype) -&gt; torch.Tensor:\n        r\"\"\"Return a buffer of ``(shape, dtype)``, reusing a pooled one if available.\"\"\"\n        key = (tuple(shape), dtype)\n        pool = self._free.get(key)\n        self._in_use += 1\n        if pool:\n            return pool.pop()\n        return torch.empty(tuple(shape), device=self.device, dtype=dtype)\n\n    def release(self, tensor: torch.Tensor) -&gt; None:\n        r\"\"\"Return ``tensor`` to the pool for reuse.\"\"\"\n        key = (tuple(tensor.shape), tensor.dtype)\n        self._free.setdefault(key, []).append(tensor)\n        self._in_use = max(0, self._in_use - 1)\n\n    def clear(self) -&gt; None:\n        r\"\"\"Drop all pooled buffers.\"\"\"\n        self._free.clear()\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool._free","title":"_free  <code>instance-attribute</code>","text":"<pre><code>_free: dict[tuple, list[Tensor]] = {}\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool._in_use","title":"_in_use  <code>instance-attribute</code>","text":"<pre><code>_in_use = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool.num_in_use","title":"num_in_use  <code>property</code>","text":"<pre><code>num_in_use: int\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool.num_pooled","title":"num_pooled  <code>property</code>","text":"<pre><code>num_pooled: int\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool.__init__","title":"__init__","text":"<pre><code>__init__(device: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/latent_pool.py</code> <pre><code>def __init__(self, device: str) -&gt; None:\n    self.device = device\n    self._free: dict[tuple, list[torch.Tensor]] = {}\n    self._in_use = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool.acquire","title":"acquire","text":"<pre><code>acquire(shape: 'Sequence[int]', dtype: dtype) -&gt; Tensor\n</code></pre> <p>Return a buffer of <code>(shape, dtype)</code>, reusing a pooled one if available.</p> Source code in <code>worldkernels/runtime/memory/latent_pool.py</code> <pre><code>def acquire(self, shape: \"Sequence[int]\", dtype: torch.dtype) -&gt; torch.Tensor:\n    r\"\"\"Return a buffer of ``(shape, dtype)``, reusing a pooled one if available.\"\"\"\n    key = (tuple(shape), dtype)\n    pool = self._free.get(key)\n    self._in_use += 1\n    if pool:\n        return pool.pop()\n    return torch.empty(tuple(shape), device=self.device, dtype=dtype)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool.release","title":"release","text":"<pre><code>release(tensor: Tensor) -&gt; None\n</code></pre> <p>Return <code>tensor</code> to the pool for reuse.</p> Source code in <code>worldkernels/runtime/memory/latent_pool.py</code> <pre><code>def release(self, tensor: torch.Tensor) -&gt; None:\n    r\"\"\"Return ``tensor`` to the pool for reuse.\"\"\"\n    key = (tuple(tensor.shape), tensor.dtype)\n    self._free.setdefault(key, []).append(tensor)\n    self._in_use = max(0, self._in_use - 1)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/latent_pool/#worldkernels.runtime.memory.latent_pool.LatentPool.clear","title":"clear","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Drop all pooled buffers.</p> Source code in <code>worldkernels/runtime/memory/latent_pool.py</code> <pre><code>def clear(self) -&gt; None:\n    r\"\"\"Drop all pooled buffers.\"\"\"\n    self._free.clear()\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/","title":"offload","text":""},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload","title":"worldkernels.runtime.memory.offload","text":"<p>Three-tier session-state offloading.</p> <p>Paused or cold session state can spill from VRAM to pinned host memory and, later, to NVMe. Host transfers use pinned buffers and a dedicated copy stream so they overlap compute. The NVMe tier is the declared seam; host transfers are implemented.</p>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['MemoryTier', 'Offloader']\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.MemoryTier","title":"MemoryTier","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Where session state currently resides.</p> Source code in <code>worldkernels/runtime/memory/offload.py</code> <pre><code>class MemoryTier(str, Enum):\n    r\"\"\"Where session state currently resides.\"\"\"\n\n    GPU = \"gpu\"\n    HOST = \"host\"\n    NVME = \"nvme\"\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.MemoryTier.GPU","title":"GPU  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>GPU = 'gpu'\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.MemoryTier.HOST","title":"HOST  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>HOST = 'host'\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.MemoryTier.NVME","title":"NVME  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NVME = 'nvme'\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader","title":"Offloader","text":"<p>Moves tensors between VRAM and pinned host memory.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>str</code> <p>The GPU device tensors are restored to.</p> required Source code in <code>worldkernels/runtime/memory/offload.py</code> <pre><code>class Offloader:\n    r\"\"\"Moves tensors between VRAM and pinned host memory.\n\n    Args:\n        device: The GPU device tensors are restored to.\n    \"\"\"\n\n    def __init__(self, device: str) -&gt; None:\n        self.device = device\n        self._pinning = device.startswith(\"cuda\") and torch.cuda.is_available()\n        self._stream = torch.cuda.Stream() if self._pinning else None\n\n    @staticmethod\n    def tier_of(tensor: torch.Tensor) -&gt; MemoryTier:\n        return MemoryTier.GPU if tensor.device.type == \"cuda\" else MemoryTier.HOST\n\n    def to_host(self, tensor: torch.Tensor) -&gt; torch.Tensor:\n        r\"\"\"Copy ``tensor`` to (pinned) host memory.\"\"\"\n        if tensor.device.type != \"cuda\":\n            return tensor\n        host = torch.empty(tensor.shape, dtype=tensor.dtype, device=\"cpu\", pin_memory=self._pinning)\n        if self._stream is not None:\n            with torch.cuda.stream(self._stream):\n                host.copy_(tensor, non_blocking=True)\n            self._stream.synchronize()\n        else:\n            host.copy_(tensor)\n        return host\n\n    def to_device(self, tensor: torch.Tensor) -&gt; torch.Tensor:\n        r\"\"\"Restore ``tensor`` to the GPU device.\"\"\"\n        if tensor.device.type == \"cuda\":\n            return tensor\n        return tensor.to(self.device, non_blocking=self._pinning)\n\n    def offload(self, tensor: torch.Tensor, tier: MemoryTier) -&gt; torch.Tensor:\n        r\"\"\"Move ``tensor`` to ``tier``.\"\"\"\n        if tier is MemoryTier.GPU:\n            return self.to_device(tensor)\n        if tier is MemoryTier.HOST:\n            return self.to_host(tensor)\n        raise NotImplementedError(\"NVMe offload tier is not implemented yet\")\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader._pinning","title":"_pinning  <code>instance-attribute</code>","text":"<pre><code>_pinning = device.startswith('cuda') and torch.cuda.is_available()\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader._stream","title":"_stream  <code>instance-attribute</code>","text":"<pre><code>_stream = torch.cuda.Stream() if self._pinning else None\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader.__init__","title":"__init__","text":"<pre><code>__init__(device: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/offload.py</code> <pre><code>def __init__(self, device: str) -&gt; None:\n    self.device = device\n    self._pinning = device.startswith(\"cuda\") and torch.cuda.is_available()\n    self._stream = torch.cuda.Stream() if self._pinning else None\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader.tier_of","title":"tier_of  <code>staticmethod</code>","text":"<pre><code>tier_of(tensor: Tensor) -&gt; MemoryTier\n</code></pre> Source code in <code>worldkernels/runtime/memory/offload.py</code> <pre><code>@staticmethod\ndef tier_of(tensor: torch.Tensor) -&gt; MemoryTier:\n    return MemoryTier.GPU if tensor.device.type == \"cuda\" else MemoryTier.HOST\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader.to_host","title":"to_host","text":"<pre><code>to_host(tensor: Tensor) -&gt; Tensor\n</code></pre> <p>Copy <code>tensor</code> to (pinned) host memory.</p> Source code in <code>worldkernels/runtime/memory/offload.py</code> <pre><code>def to_host(self, tensor: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Copy ``tensor`` to (pinned) host memory.\"\"\"\n    if tensor.device.type != \"cuda\":\n        return tensor\n    host = torch.empty(tensor.shape, dtype=tensor.dtype, device=\"cpu\", pin_memory=self._pinning)\n    if self._stream is not None:\n        with torch.cuda.stream(self._stream):\n            host.copy_(tensor, non_blocking=True)\n        self._stream.synchronize()\n    else:\n        host.copy_(tensor)\n    return host\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader.to_device","title":"to_device","text":"<pre><code>to_device(tensor: Tensor) -&gt; Tensor\n</code></pre> <p>Restore <code>tensor</code> to the GPU device.</p> Source code in <code>worldkernels/runtime/memory/offload.py</code> <pre><code>def to_device(self, tensor: torch.Tensor) -&gt; torch.Tensor:\n    r\"\"\"Restore ``tensor`` to the GPU device.\"\"\"\n    if tensor.device.type == \"cuda\":\n        return tensor\n    return tensor.to(self.device, non_blocking=self._pinning)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/offload/#worldkernels.runtime.memory.offload.Offloader.offload","title":"offload","text":"<pre><code>offload(tensor: Tensor, tier: MemoryTier) -&gt; Tensor\n</code></pre> <p>Move <code>tensor</code> to <code>tier</code>.</p> Source code in <code>worldkernels/runtime/memory/offload.py</code> <pre><code>def offload(self, tensor: torch.Tensor, tier: MemoryTier) -&gt; torch.Tensor:\n    r\"\"\"Move ``tensor`` to ``tier``.\"\"\"\n    if tier is MemoryTier.GPU:\n        return self.to_device(tensor)\n    if tier is MemoryTier.HOST:\n        return self.to_host(tensor)\n    raise NotImplementedError(\"NVMe offload tier is not implemented yet\")\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/","title":"trajectory_cache","text":""},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache","title":"worldkernels.runtime.memory.trajectory_cache","text":"<p>Trajectory prefix cache.</p> <p>A radix tree (trie) keyed by action-history hashes. Two sessions that issue the same sequence of actions from the same initial state produce the same latents; the cache lets the second session reuse the first's latent blocks for the shared prefix instead of recomputing them.</p> <p>Block ids stored at each node reference blocks in a <code>BlockManager</code>; this module holds only the index, not tensors, so it is torch-free.</p>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['TrajectoryCache']\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache._TrieNode","title":"_TrieNode  <code>dataclass</code>","text":"Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>@dataclass\nclass _TrieNode:\n    block_ids: list[int] = field(default_factory=list)\n    children: dict[str, \"_TrieNode\"] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache._TrieNode.block_ids","title":"block_ids  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>block_ids: list[int] = field(default_factory=list)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache._TrieNode.children","title":"children  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>children: dict[str, '_TrieNode'] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache._TrieNode.__init__","title":"__init__","text":"<pre><code>__init__(block_ids: list[int] = list(), children: dict[str, '_TrieNode'] = dict()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.PrefixMatch","title":"PrefixMatch  <code>dataclass</code>","text":"<p>Result of a trajectory-prefix lookup.</p> <p>Parameters:</p> Name Type Description Default <code>length</code> <code>int</code> <p>Number of leading actions matched.</p> required <code>block_ids</code> <code>list[int]</code> <p>Cached latent block ids for the matched prefix.</p> required Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>@dataclass\nclass PrefixMatch:\n    r\"\"\"Result of a trajectory-prefix lookup.\n\n    Args:\n        length: Number of leading actions matched.\n        block_ids: Cached latent block ids for the matched prefix.\n    \"\"\"\n\n    length: int\n    block_ids: list[int]\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.PrefixMatch.length","title":"length  <code>instance-attribute</code>","text":"<pre><code>length: int\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.PrefixMatch.block_ids","title":"block_ids  <code>instance-attribute</code>","text":"<pre><code>block_ids: list[int]\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.PrefixMatch.__init__","title":"__init__","text":"<pre><code>__init__(length: int, block_ids: list[int]) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache","title":"TrajectoryCache","text":"<p>Radix-tree cache mapping action-history prefixes to latent block ids.</p> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>class TrajectoryCache:\n    r\"\"\"Radix-tree cache mapping action-history prefixes to latent block ids.\"\"\"\n\n    def __init__(self) -&gt; None:\n        self._root = _TrieNode()\n        self._size = 0\n\n    @property\n    def size(self) -&gt; int:\n        r\"\"\"Number of cached trajectory steps.\"\"\"\n        return self._size\n\n    def insert(self, action_hashes: list[str], block_ids: list[int]) -&gt; None:\n        r\"\"\"Cache the per-step latent block id for an action sequence.\n\n        Args:\n            action_hashes: Content hashes of each action in order.\n            block_ids: Latent block id produced after each action; same length.\n        \"\"\"\n        if len(action_hashes) != len(block_ids):\n            raise ValueError(\"action_hashes and block_ids must have equal length\")\n        node = self._root\n        for action_hash, block_id in zip(action_hashes, block_ids):\n            child = node.children.get(action_hash)\n            if child is None:\n                child = _TrieNode()\n                node.children[action_hash] = child\n                self._size += 1\n            child.block_ids = [block_id]\n            node = child\n\n    def match(self, action_hashes: list[str]) -&gt; PrefixMatch:\n        r\"\"\"Return the longest cached prefix of ``action_hashes``.\"\"\"\n        node = self._root\n        matched: list[int] = []\n        for action_hash in action_hashes:\n            child = node.children.get(action_hash)\n            if child is None or not child.block_ids:\n                break\n            matched.append(child.block_ids[0])\n            node = child\n        return PrefixMatch(length=len(matched), block_ids=matched)\n\n    def clear(self) -&gt; None:\n        self._root = _TrieNode()\n        self._size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache._root","title":"_root  <code>instance-attribute</code>","text":"<pre><code>_root = _TrieNode()\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache._size","title":"_size  <code>instance-attribute</code>","text":"<pre><code>_size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache.size","title":"size  <code>property</code>","text":"<pre><code>size: int\n</code></pre> <p>Number of cached trajectory steps.</p>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache.__init__","title":"__init__","text":"<pre><code>__init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._root = _TrieNode()\n    self._size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache.insert","title":"insert","text":"<pre><code>insert(action_hashes: list[str], block_ids: list[int]) -&gt; None\n</code></pre> <p>Cache the per-step latent block id for an action sequence.</p> <p>Parameters:</p> Name Type Description Default <code>action_hashes</code> <code>list[str]</code> <p>Content hashes of each action in order.</p> required <code>block_ids</code> <code>list[int]</code> <p>Latent block id produced after each action; same length.</p> required Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def insert(self, action_hashes: list[str], block_ids: list[int]) -&gt; None:\n    r\"\"\"Cache the per-step latent block id for an action sequence.\n\n    Args:\n        action_hashes: Content hashes of each action in order.\n        block_ids: Latent block id produced after each action; same length.\n    \"\"\"\n    if len(action_hashes) != len(block_ids):\n        raise ValueError(\"action_hashes and block_ids must have equal length\")\n    node = self._root\n    for action_hash, block_id in zip(action_hashes, block_ids):\n        child = node.children.get(action_hash)\n        if child is None:\n            child = _TrieNode()\n            node.children[action_hash] = child\n            self._size += 1\n        child.block_ids = [block_id]\n        node = child\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache.match","title":"match","text":"<pre><code>match(action_hashes: list[str]) -&gt; PrefixMatch\n</code></pre> <p>Return the longest cached prefix of <code>action_hashes</code>.</p> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def match(self, action_hashes: list[str]) -&gt; PrefixMatch:\n    r\"\"\"Return the longest cached prefix of ``action_hashes``.\"\"\"\n    node = self._root\n    matched: list[int] = []\n    for action_hash in action_hashes:\n        child = node.children.get(action_hash)\n        if child is None or not child.block_ids:\n            break\n        matched.append(child.block_ids[0])\n        node = child\n    return PrefixMatch(length=len(matched), block_ids=matched)\n</code></pre>"},{"location":"api/worldkernels/runtime/memory/trajectory_cache/#worldkernels.runtime.memory.trajectory_cache.TrajectoryCache.clear","title":"clear","text":"<pre><code>clear() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/memory/trajectory_cache.py</code> <pre><code>def clear(self) -&gt; None:\n    self._root = _TrieNode()\n    self._size = 0\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/","title":"metrics","text":""},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics","title":"worldkernels.runtime.metrics","text":"<p>Prometheus metrics for the serving runtime.</p> <p>Metrics live in a private <code>CollectorRegistry</code> so they never collide with a host application's default registry. The serving layer exposes them at <code>/metrics</code> via <code>render()</code>.</p>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['REGISTRY', 'CONTENT_TYPE_LATEST', 'render', 'observe_step', 'observe_batch', 'set_active_sessions', 'set_vram_bytes', 'set_cache_hit_ratio', 'set_isolation_tier', 'set_worker_processes', 'observe_worker_ipc_latency', 'inc_worker_respawns', 'get_active_sessions', 'get_vram_bytes', 'get_steps_total']\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.REGISTRY","title":"REGISTRY  <code>module-attribute</code>","text":"<pre><code>REGISTRY = CollectorRegistry()\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._STEP_LATENCY","title":"_STEP_LATENCY  <code>module-attribute</code>","text":"<pre><code>_STEP_LATENCY = Histogram('wk_step_latency_seconds', 'Wall-clock latency of one simulation step.', registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._FRAMES_TOTAL","title":"_FRAMES_TOTAL  <code>module-attribute</code>","text":"<pre><code>_FRAMES_TOTAL = Counter('wk_frames_generated_total', 'Total frames generated across all sessions.', registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._STEPS_TOTAL","title":"_STEPS_TOTAL  <code>module-attribute</code>","text":"<pre><code>_STEPS_TOTAL = Counter('wk_steps_total', 'Total simulation steps executed.', registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._BATCH_SIZE","title":"_BATCH_SIZE  <code>module-attribute</code>","text":"<pre><code>_BATCH_SIZE = Histogram('wk_batch_size', 'Number of sessions per batched forward pass.', buckets=(1, 2, 4, 8, 16, 32), registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._ACTIVE_SESSIONS","title":"_ACTIVE_SESSIONS  <code>module-attribute</code>","text":"<pre><code>_ACTIVE_SESSIONS = Gauge('wk_active_sessions', 'Number of live sessions.', registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._VRAM_BYTES","title":"_VRAM_BYTES  <code>module-attribute</code>","text":"<pre><code>_VRAM_BYTES = Gauge('wk_vram_usage_bytes', 'Device memory currently allocated.', registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._CACHE_HIT_RATIO","title":"_CACHE_HIT_RATIO  <code>module-attribute</code>","text":"<pre><code>_CACHE_HIT_RATIO = Gauge('wk_cache_hit_ratio', 'Denoise-step cache hit ratio in [0, 1].', registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._ISOLATION_TIER","title":"_ISOLATION_TIER  <code>module-attribute</code>","text":"<pre><code>_ISOLATION_TIER = Gauge('wk_model_isolation_tier', 'Per-model isolation tier (0 = shared env, 1 = isolated subprocess).', labelnames=('model_id',), registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._WORKER_PROCESSES","title":"_WORKER_PROCESSES  <code>module-attribute</code>","text":"<pre><code>_WORKER_PROCESSES = Gauge('wk_worker_processes', 'Number of isolated worker subprocesses currently live.', registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._WORKER_IPC_LATENCY","title":"_WORKER_IPC_LATENCY  <code>module-attribute</code>","text":"<pre><code>_WORKER_IPC_LATENCY = Histogram('wk_worker_ipc_latency_seconds', 'Engine \u2194 worker RPC round-trip latency.', labelnames=('model_id',), registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics._WORKER_RESPAWNS","title":"_WORKER_RESPAWNS  <code>module-attribute</code>","text":"<pre><code>_WORKER_RESPAWNS = Counter('wk_worker_respawns_total', 'Times an isolated worker was respawned after crashing.', labelnames=('model_id',), registry=REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.observe_step","title":"observe_step","text":"<pre><code>observe_step(latency_seconds: float, frames: int = 0) -&gt; None\n</code></pre> <p>Record one completed simulation step.</p> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def observe_step(latency_seconds: float, frames: int = 0) -&gt; None:\n    r\"\"\"Record one completed simulation step.\"\"\"\n    _STEP_LATENCY.observe(latency_seconds)\n    _STEPS_TOTAL.inc()\n    if frames:\n        _FRAMES_TOTAL.inc(frames)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.observe_batch","title":"observe_batch","text":"<pre><code>observe_batch(size: int) -&gt; None\n</code></pre> <p>Record the size of one batched forward pass.</p> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def observe_batch(size: int) -&gt; None:\n    r\"\"\"Record the size of one batched forward pass.\"\"\"\n    _BATCH_SIZE.observe(size)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.set_active_sessions","title":"set_active_sessions","text":"<pre><code>set_active_sessions(count: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def set_active_sessions(count: int) -&gt; None:\n    _ACTIVE_SESSIONS.set(count)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.set_vram_bytes","title":"set_vram_bytes","text":"<pre><code>set_vram_bytes(value: float) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def set_vram_bytes(value: float) -&gt; None:\n    _VRAM_BYTES.set(value)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.set_cache_hit_ratio","title":"set_cache_hit_ratio","text":"<pre><code>set_cache_hit_ratio(ratio: float) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def set_cache_hit_ratio(ratio: float) -&gt; None:\n    _CACHE_HIT_RATIO.set(ratio)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.set_isolation_tier","title":"set_isolation_tier","text":"<pre><code>set_isolation_tier(model_id: str, tier: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def set_isolation_tier(model_id: str, tier: int) -&gt; None:\n    _ISOLATION_TIER.labels(model_id=model_id).set(tier)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.set_worker_processes","title":"set_worker_processes","text":"<pre><code>set_worker_processes(count: int) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def set_worker_processes(count: int) -&gt; None:\n    _WORKER_PROCESSES.set(count)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.observe_worker_ipc_latency","title":"observe_worker_ipc_latency","text":"<pre><code>observe_worker_ipc_latency(model_id: str, seconds: float) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def observe_worker_ipc_latency(model_id: str, seconds: float) -&gt; None:\n    _WORKER_IPC_LATENCY.labels(model_id=model_id).observe(seconds)\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.inc_worker_respawns","title":"inc_worker_respawns","text":"<pre><code>inc_worker_respawns(model_id: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def inc_worker_respawns(model_id: str) -&gt; None:\n    _WORKER_RESPAWNS.labels(model_id=model_id).inc()\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.get_active_sessions","title":"get_active_sessions","text":"<pre><code>get_active_sessions() -&gt; float\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def get_active_sessions() -&gt; float:\n    return _ACTIVE_SESSIONS._value.get()\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.get_vram_bytes","title":"get_vram_bytes","text":"<pre><code>get_vram_bytes() -&gt; float\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def get_vram_bytes() -&gt; float:\n    return _VRAM_BYTES._value.get()\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.get_steps_total","title":"get_steps_total","text":"<pre><code>get_steps_total() -&gt; float\n</code></pre> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def get_steps_total() -&gt; float:\n    return _STEPS_TOTAL._value.get()\n</code></pre>"},{"location":"api/worldkernels/runtime/metrics/#worldkernels.runtime.metrics.render","title":"render","text":"<pre><code>render() -&gt; bytes\n</code></pre> <p>Render the metrics registry in Prometheus text exposition format.</p> Source code in <code>worldkernels/runtime/metrics.py</code> <pre><code>def render() -&gt; bytes:\n    r\"\"\"Render the metrics registry in Prometheus text exposition format.\"\"\"\n    return _generate_latest(REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/","title":"quantization","text":""},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization","title":"worldkernels.runtime.quantization","text":"<p>Weight quantization registry and schemes.</p> <p>Modules:</p> Name Description <code>registry</code> <p>Quantization registry.</p>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['QuantizationRegistry', 'QuantizationScheme']\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationRegistry","title":"QuantizationRegistry","text":"<p>Resolves a quantization scheme to its weight applier.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>class QuantizationRegistry:\n    r\"\"\"Resolves a quantization scheme to its weight applier.\"\"\"\n\n    def __init__(self) -&gt; None:\n        self._appliers: dict[QuantizationScheme, Callable[[\"nn.Module\"], \"nn.Module\"]] = {\n            QuantizationScheme.NONE: _apply_none,\n            QuantizationScheme.INT8: _apply_int8,\n            QuantizationScheme.INT4: _apply_int4,\n        }\n\n    def register(\n        self,\n        scheme: QuantizationScheme,\n        applier: Callable[[\"nn.Module\"], \"nn.Module\"],\n    ) -&gt; None:\n        r\"\"\"Register or override the applier for ``scheme``.\"\"\"\n        self._appliers[scheme] = applier\n\n    def apply(self, module: \"nn.Module\", scheme: QuantizationScheme | str) -&gt; \"nn.Module\":\n        r\"\"\"Quantize ``module`` in place under ``scheme`` and return it.\"\"\"\n        scheme = QuantizationScheme(scheme)\n        applier = self._appliers.get(scheme)\n        if applier is None:\n            raise ValueError(f\"no applier registered for scheme {scheme.value!r}\")\n        return applier(module)\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationRegistry._appliers","title":"_appliers  <code>instance-attribute</code>","text":"<pre><code>_appliers: dict[QuantizationScheme, Callable[['nn.Module'], 'nn.Module']] = {QuantizationScheme.NONE: _apply_none, QuantizationScheme.INT8: _apply_int8, QuantizationScheme.INT4: _apply_int4}\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationRegistry.__init__","title":"__init__","text":"<pre><code>__init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._appliers: dict[QuantizationScheme, Callable[[\"nn.Module\"], \"nn.Module\"]] = {\n        QuantizationScheme.NONE: _apply_none,\n        QuantizationScheme.INT8: _apply_int8,\n        QuantizationScheme.INT4: _apply_int4,\n    }\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationRegistry.register","title":"register","text":"<pre><code>register(scheme: QuantizationScheme, applier: Callable[['nn.Module'], 'nn.Module']) -&gt; None\n</code></pre> <p>Register or override the applier for <code>scheme</code>.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def register(\n    self,\n    scheme: QuantizationScheme,\n    applier: Callable[[\"nn.Module\"], \"nn.Module\"],\n) -&gt; None:\n    r\"\"\"Register or override the applier for ``scheme``.\"\"\"\n    self._appliers[scheme] = applier\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationRegistry.apply","title":"apply","text":"<pre><code>apply(module: 'nn.Module', scheme: QuantizationScheme | str) -&gt; 'nn.Module'\n</code></pre> <p>Quantize <code>module</code> in place under <code>scheme</code> and return it.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def apply(self, module: \"nn.Module\", scheme: QuantizationScheme | str) -&gt; \"nn.Module\":\n    r\"\"\"Quantize ``module`` in place under ``scheme`` and return it.\"\"\"\n    scheme = QuantizationScheme(scheme)\n    applier = self._appliers.get(scheme)\n    if applier is None:\n        raise ValueError(f\"no applier registered for scheme {scheme.value!r}\")\n    return applier(module)\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationScheme","title":"QuantizationScheme","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Supported weight-quantization schemes.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>class QuantizationScheme(str, Enum):\n    r\"\"\"Supported weight-quantization schemes.\"\"\"\n\n    NONE = \"none\"\n    INT8 = \"int8\"\n    INT4 = \"int4\"\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationScheme.NONE","title":"NONE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NONE = 'none'\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationScheme.INT8","title":"INT8  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>INT8 = 'int8'\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/#worldkernels.runtime.quantization.QuantizationScheme.INT4","title":"INT4  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>INT4 = 'int4'\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/","title":"registry","text":""},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry","title":"worldkernels.runtime.quantization.registry","text":"<p>Quantization registry.</p> <p>Maps a <code>QuantizationScheme</code> to an applier that quantizes a module's weights in place. <code>none</code> is identity; <code>int8</code> / <code>int4</code> weight-only quantization is delegated to <code>torchao</code> and fails with an actionable install message when that optional dependency is absent.</p>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['QuantizationScheme', 'QuantizationRegistry']\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationScheme","title":"QuantizationScheme","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Supported weight-quantization schemes.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>class QuantizationScheme(str, Enum):\n    r\"\"\"Supported weight-quantization schemes.\"\"\"\n\n    NONE = \"none\"\n    INT8 = \"int8\"\n    INT4 = \"int4\"\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationScheme.NONE","title":"NONE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NONE = 'none'\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationScheme.INT8","title":"INT8  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>INT8 = 'int8'\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationScheme.INT4","title":"INT4  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>INT4 = 'int4'\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationRegistry","title":"QuantizationRegistry","text":"<p>Resolves a quantization scheme to its weight applier.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>class QuantizationRegistry:\n    r\"\"\"Resolves a quantization scheme to its weight applier.\"\"\"\n\n    def __init__(self) -&gt; None:\n        self._appliers: dict[QuantizationScheme, Callable[[\"nn.Module\"], \"nn.Module\"]] = {\n            QuantizationScheme.NONE: _apply_none,\n            QuantizationScheme.INT8: _apply_int8,\n            QuantizationScheme.INT4: _apply_int4,\n        }\n\n    def register(\n        self,\n        scheme: QuantizationScheme,\n        applier: Callable[[\"nn.Module\"], \"nn.Module\"],\n    ) -&gt; None:\n        r\"\"\"Register or override the applier for ``scheme``.\"\"\"\n        self._appliers[scheme] = applier\n\n    def apply(self, module: \"nn.Module\", scheme: QuantizationScheme | str) -&gt; \"nn.Module\":\n        r\"\"\"Quantize ``module`` in place under ``scheme`` and return it.\"\"\"\n        scheme = QuantizationScheme(scheme)\n        applier = self._appliers.get(scheme)\n        if applier is None:\n            raise ValueError(f\"no applier registered for scheme {scheme.value!r}\")\n        return applier(module)\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationRegistry._appliers","title":"_appliers  <code>instance-attribute</code>","text":"<pre><code>_appliers: dict[QuantizationScheme, Callable[['nn.Module'], 'nn.Module']] = {QuantizationScheme.NONE: _apply_none, QuantizationScheme.INT8: _apply_int8, QuantizationScheme.INT4: _apply_int4}\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationRegistry.__init__","title":"__init__","text":"<pre><code>__init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._appliers: dict[QuantizationScheme, Callable[[\"nn.Module\"], \"nn.Module\"]] = {\n        QuantizationScheme.NONE: _apply_none,\n        QuantizationScheme.INT8: _apply_int8,\n        QuantizationScheme.INT4: _apply_int4,\n    }\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationRegistry.register","title":"register","text":"<pre><code>register(scheme: QuantizationScheme, applier: Callable[['nn.Module'], 'nn.Module']) -&gt; None\n</code></pre> <p>Register or override the applier for <code>scheme</code>.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def register(\n    self,\n    scheme: QuantizationScheme,\n    applier: Callable[[\"nn.Module\"], \"nn.Module\"],\n) -&gt; None:\n    r\"\"\"Register or override the applier for ``scheme``.\"\"\"\n    self._appliers[scheme] = applier\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry.QuantizationRegistry.apply","title":"apply","text":"<pre><code>apply(module: 'nn.Module', scheme: QuantizationScheme | str) -&gt; 'nn.Module'\n</code></pre> <p>Quantize <code>module</code> in place under <code>scheme</code> and return it.</p> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def apply(self, module: \"nn.Module\", scheme: QuantizationScheme | str) -&gt; \"nn.Module\":\n    r\"\"\"Quantize ``module`` in place under ``scheme`` and return it.\"\"\"\n    scheme = QuantizationScheme(scheme)\n    applier = self._appliers.get(scheme)\n    if applier is None:\n        raise ValueError(f\"no applier registered for scheme {scheme.value!r}\")\n    return applier(module)\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry._apply_none","title":"_apply_none","text":"<pre><code>_apply_none(module: 'nn.Module') -&gt; 'nn.Module'\n</code></pre> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def _apply_none(module: \"nn.Module\") -&gt; \"nn.Module\":\n    return module\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry._apply_int8","title":"_apply_int8","text":"<pre><code>_apply_int8(module: 'nn.Module') -&gt; 'nn.Module'\n</code></pre> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def _apply_int8(module: \"nn.Module\") -&gt; \"nn.Module\":\n    ao = optional_import(\"torchao\", \"quant\")\n    ao.quantization.quantize_(module, ao.quantization.int8_weight_only())\n    return module\n</code></pre>"},{"location":"api/worldkernels/runtime/quantization/registry/#worldkernels.runtime.quantization.registry._apply_int4","title":"_apply_int4","text":"<pre><code>_apply_int4(module: 'nn.Module') -&gt; 'nn.Module'\n</code></pre> Source code in <code>worldkernels/runtime/quantization/registry.py</code> <pre><code>def _apply_int4(module: \"nn.Module\") -&gt; \"nn.Module\":\n    ao = optional_import(\"torchao\", \"quant\")\n    ao.quantization.quantize_(module, ao.quantization.int4_weight_only())\n    return module\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/","title":"resolver","text":""},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver","title":"worldkernels.runtime.resolver","text":"<p>Dependency resolver: decide shared vs isolated tier.</p> <p>Wraps <code>uv pip compile</code> against the union of currently-loaded model constraints plus the incoming model's constraints. Pure function over constraints \u2014 no side effects on the host environment.</p>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['InstallPlan', 'SharedPlan', 'IsolatedPlan', 'IncompatibleDependenciesError', 'resolve_install_plan']\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.InstallPlan","title":"InstallPlan  <code>module-attribute</code>","text":"<pre><code>InstallPlan = SharedPlan | IsolatedPlan\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.SharedPlan","title":"SharedPlan  <code>dataclass</code>","text":"<p>Resolver verdict: pack into the shared (current) env. <code>deltas</code> lists the extra packages to install beyond what's already there.</p> Source code in <code>worldkernels/runtime/resolver.py</code> <pre><code>@dataclass(frozen=True)\nclass SharedPlan:\n    r\"\"\"Resolver verdict: pack into the shared (current) env. ``deltas`` lists\n    the extra packages to install beyond what's already there.\"\"\"\n\n    deltas: tuple[str, ...] = ()\n    resolved_requirements: str = \"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.SharedPlan.deltas","title":"deltas  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>deltas: tuple[str, ...] = ()\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.SharedPlan.resolved_requirements","title":"resolved_requirements  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>resolved_requirements: str = ''\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.SharedPlan.__init__","title":"__init__","text":"<pre><code>__init__(deltas: tuple[str, ...] = (), resolved_requirements: str = '') -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.IsolatedPlan","title":"IsolatedPlan  <code>dataclass</code>","text":"<p>Resolver verdict: materialize a dedicated venv for this model.</p> Source code in <code>worldkernels/runtime/resolver.py</code> <pre><code>@dataclass(frozen=True)\nclass IsolatedPlan:\n    r\"\"\"Resolver verdict: materialize a dedicated venv for this model.\"\"\"\n\n    reason: str\n    conflicting: tuple[str, ...] = field(default_factory=tuple)\n    requirements: tuple[str, ...] = field(default_factory=tuple)\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.IsolatedPlan.reason","title":"reason  <code>instance-attribute</code>","text":"<pre><code>reason: str\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.IsolatedPlan.conflicting","title":"conflicting  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>conflicting: tuple[str, ...] = field(default_factory=tuple)\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.IsolatedPlan.requirements","title":"requirements  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>requirements: tuple[str, ...] = field(default_factory=tuple)\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.IsolatedPlan.__init__","title":"__init__","text":"<pre><code>__init__(reason: str, conflicting: tuple[str, ...] = tuple(), requirements: tuple[str, ...] = tuple()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.IncompatibleDependenciesError","title":"IncompatibleDependenciesError","text":"<p>               Bases: <code>WorldKernelError</code></p> <p>Raised when <code>isolation='shared'</code> is forced but the resolver returns IsolatedPlan.</p> Source code in <code>worldkernels/runtime/resolver.py</code> <pre><code>class IncompatibleDependenciesError(WorldKernelError):\n    r\"\"\"Raised when ``isolation='shared'`` is forced but the resolver returns IsolatedPlan.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver.resolve_install_plan","title":"resolve_install_plan","text":"<pre><code>resolve_install_plan(incoming: 'ModelCard', loaded: list['ModelCard']) -&gt; InstallPlan\n</code></pre> <p>Decide whether <code>incoming</code> can share an env with currently-loaded models.</p> <p>Parameters:</p> Name Type Description Default <code>incoming</code> <code>'ModelCard'</code> <p>ModelCard about to be loaded.</p> required <code>loaded</code> <code>list['ModelCard']</code> <p>ModelCards currently loaded in the shared env (excludes isolated ones).</p> required Source code in <code>worldkernels/runtime/resolver.py</code> <pre><code>def resolve_install_plan(\n    incoming: \"ModelCard\",\n    loaded: list[\"ModelCard\"],\n) -&gt; InstallPlan:\n    r\"\"\"Decide whether ``incoming`` can share an env with currently-loaded models.\n\n    Args:\n        incoming: ModelCard about to be loaded.\n        loaded: ModelCards currently loaded in the shared env (excludes isolated ones).\n    \"\"\"\n    if incoming.isolation == \"isolated\":\n        return IsolatedPlan(\n            reason=\"card.isolation='isolated' forced\",\n            requirements=tuple(_card_requirements(incoming)),\n        )\n\n    requirements = _union_requirements(loaded + [incoming])\n    if not requirements:\n        return SharedPlan(deltas=tuple(_card_requirements(incoming)))\n\n    if shutil.which(\"uv\") is None:\n        log.warning(\"uv not on PATH; resolver falls back to optimistic SharedPlan\")\n        return SharedPlan(deltas=tuple(_card_requirements(incoming)))\n\n    ok, output = _compile_requirements(requirements)\n    if ok:\n        return SharedPlan(deltas=tuple(_card_requirements(incoming)), resolved_requirements=output)\n\n    if incoming.isolation == \"shared\":\n        raise IncompatibleDependenciesError(\n            f\"Cannot share env with currently-loaded models. Conflict:\\n{output}\"\n        )\n\n    return IsolatedPlan(\n        reason=output.strip().splitlines()[-1] if output.strip() else \"unsatisfiable constraints\",\n        conflicting=tuple(requirements),\n        requirements=tuple(_card_requirements(incoming)),\n    )\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver._card_requirements","title":"_card_requirements","text":"<pre><code>_card_requirements(card: 'ModelCard') -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/runtime/resolver.py</code> <pre><code>def _card_requirements(card: \"ModelCard\") -&gt; list[str]:\n    reqs: list[str] = list(card.constraints)\n    for comp in card.components:\n        reqs.extend(comp.deps)\n    return reqs\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver._union_requirements","title":"_union_requirements","text":"<pre><code>_union_requirements(cards: list['ModelCard']) -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/runtime/resolver.py</code> <pre><code>def _union_requirements(cards: list[\"ModelCard\"]) -&gt; list[str]:\n    seen: dict[str, None] = {}\n    for card in cards:\n        for req in _card_requirements(card):\n            if req not in seen:\n                seen[req] = None\n    return list(seen)\n</code></pre>"},{"location":"api/worldkernels/runtime/resolver/#worldkernels.runtime.resolver._compile_requirements","title":"_compile_requirements","text":"<pre><code>_compile_requirements(requirements: list[str]) -&gt; tuple[bool, str]\n</code></pre> <p>Run <code>uv pip compile</code> against <code>requirements</code>; return <code>(ok, stdout_or_stderr)</code>.</p> Source code in <code>worldkernels/runtime/resolver.py</code> <pre><code>def _compile_requirements(requirements: list[str]) -&gt; tuple[bool, str]:\n    r\"\"\"Run ``uv pip compile`` against ``requirements``; return ``(ok, stdout_or_stderr)``.\"\"\"\n    with tempfile.NamedTemporaryFile(\"w\", suffix=\".in\", delete=False) as f:\n        f.write(\"\\n\".join(requirements) + \"\\n\")\n        in_path = Path(f.name)\n    try:\n        proc = subprocess.run(\n            [\"uv\", \"pip\", \"compile\", \"--quiet\", str(in_path)],\n            capture_output=True,\n            text=True,\n            check=False,\n            timeout=120,\n        )\n        if proc.returncode == 0:\n            return True, proc.stdout\n        return False, proc.stderr or proc.stdout\n    except subprocess.TimeoutExpired:\n        return False, \"uv pip compile timed out\"\n    finally:\n        in_path.unlink(missing_ok=True)\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/","title":"rpc","text":""},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc","title":"worldkernels.runtime.rpc","text":"<p>Length-prefixed msgpack RPC over a Unix domain socket.</p> <p>Wire format: 4-byte big-endian length header + msgpack body. Each message is a dict with <code>id</code>, <code>op</code>, <code>args</code> (request) or <code>id</code>, <code>ok</code>, <code>result|error</code> (response). Synchronous request/response \u2014 the worker handles one call at a time per session, matching the engine's per-session step contract.</p>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RpcServer', 'RpcClient', 'RpcError', 'default_socket_path']\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._HEADER","title":"_HEADER  <code>module-attribute</code>","text":"<pre><code>_HEADER = struct.Struct('&gt;I')\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._MAX_FRAME","title":"_MAX_FRAME  <code>module-attribute</code>","text":"<pre><code>_MAX_FRAME = 1 &lt;&lt; 26\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcError","title":"RpcError","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Raised on the client side when the worker returned an error envelope.</p> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>class RpcError(RuntimeError):\n    r\"\"\"Raised on the client side when the worker returned an error envelope.\"\"\"\n\n    def __init__(self, op: str, cls: str, message: str) -&gt; None:\n        super().__init__(f\"{op}: {cls}: {message}\")\n        self.op = op\n        self.exc_cls = cls\n        self.message = message\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcError.op","title":"op  <code>instance-attribute</code>","text":"<pre><code>op = op\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcError.exc_cls","title":"exc_cls  <code>instance-attribute</code>","text":"<pre><code>exc_cls = cls\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcError.message","title":"message  <code>instance-attribute</code>","text":"<pre><code>message = message\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcError.__init__","title":"__init__","text":"<pre><code>__init__(op: str, cls: str, message: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def __init__(self, op: str, cls: str, message: str) -&gt; None:\n    super().__init__(f\"{op}: {cls}: {message}\")\n    self.op = op\n    self.exc_cls = cls\n    self.message = message\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._Frame","title":"_Frame  <code>dataclass</code>","text":"Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>@dataclass\nclass _Frame:\n    payload: dict[str, Any]\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._Frame.payload","title":"payload  <code>instance-attribute</code>","text":"<pre><code>payload: dict[str, Any]\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._Frame.__init__","title":"__init__","text":"<pre><code>__init__(payload: dict[str, Any]) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient","title":"RpcClient","text":"<p>Synchronous client. One in-flight call at a time; safe to reuse across calls.</p> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>class RpcClient:\n    r\"\"\"Synchronous client. One in-flight call at a time; safe to reuse across calls.\"\"\"\n\n    def __init__(self, socket_path: Path, connect_timeout: float = 30.0) -&gt; None:\n        self.socket_path = Path(socket_path)\n        self._sock: socket.socket | None = None\n        self._connect_timeout = connect_timeout\n\n    def connect(self) -&gt; None:\n        deadline = self._connect_timeout\n        import time as _time\n\n        end = _time.monotonic() + deadline\n        last_exc: Exception | None = None\n        while _time.monotonic() &lt; end:\n            try:\n                s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)\n                s.connect(str(self.socket_path))\n                self._sock = s\n                return\n            except (FileNotFoundError, ConnectionRefusedError) as exc:\n                last_exc = exc\n                _time.sleep(0.05)\n        raise ConnectionError(f\"could not connect to {self.socket_path}: {last_exc}\")\n\n    def call(self, op: str, **args: Any) -&gt; Any:\n        if self._sock is None:\n            self.connect()\n        assert self._sock is not None\n        req = {\"id\": uuid.uuid4().hex, \"op\": op, \"args\": args}\n        _send(self._sock, req)\n        resp = _recv(self._sock)\n        if resp.get(\"ok\"):\n            return resp.get(\"result\")\n        err = resp.get(\"error\", {})\n        raise RpcError(op, err.get(\"cls\", \"RpcError\"), err.get(\"message\", \"unknown\"))\n\n    def close(self) -&gt; None:\n        if self._sock is not None:\n            try:\n                self._sock.shutdown(socket.SHUT_RDWR)\n            except OSError:\n                pass\n            self._sock.close()\n            self._sock = None\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient.socket_path","title":"socket_path  <code>instance-attribute</code>","text":"<pre><code>socket_path = Path(socket_path)\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient._sock","title":"_sock  <code>instance-attribute</code>","text":"<pre><code>_sock: socket | None = None\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient._connect_timeout","title":"_connect_timeout  <code>instance-attribute</code>","text":"<pre><code>_connect_timeout = connect_timeout\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient.__init__","title":"__init__","text":"<pre><code>__init__(socket_path: Path, connect_timeout: float = 30.0) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def __init__(self, socket_path: Path, connect_timeout: float = 30.0) -&gt; None:\n    self.socket_path = Path(socket_path)\n    self._sock: socket.socket | None = None\n    self._connect_timeout = connect_timeout\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient.connect","title":"connect","text":"<pre><code>connect() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def connect(self) -&gt; None:\n    deadline = self._connect_timeout\n    import time as _time\n\n    end = _time.monotonic() + deadline\n    last_exc: Exception | None = None\n    while _time.monotonic() &lt; end:\n        try:\n            s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)\n            s.connect(str(self.socket_path))\n            self._sock = s\n            return\n        except (FileNotFoundError, ConnectionRefusedError) as exc:\n            last_exc = exc\n            _time.sleep(0.05)\n    raise ConnectionError(f\"could not connect to {self.socket_path}: {last_exc}\")\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient.call","title":"call","text":"<pre><code>call(op: str, **args: Any) -&gt; Any\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def call(self, op: str, **args: Any) -&gt; Any:\n    if self._sock is None:\n        self.connect()\n    assert self._sock is not None\n    req = {\"id\": uuid.uuid4().hex, \"op\": op, \"args\": args}\n    _send(self._sock, req)\n    resp = _recv(self._sock)\n    if resp.get(\"ok\"):\n        return resp.get(\"result\")\n    err = resp.get(\"error\", {})\n    raise RpcError(op, err.get(\"cls\", \"RpcError\"), err.get(\"message\", \"unknown\"))\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcClient.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def close(self) -&gt; None:\n    if self._sock is not None:\n        try:\n            self._sock.shutdown(socket.SHUT_RDWR)\n        except OSError:\n            pass\n        self._sock.close()\n        self._sock = None\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer","title":"RpcServer","text":"<p>Listens on a Unix socket, dispatches ops to a handler callable.</p> <p>Handler signature: <code>handler(op: str, args: dict) -&gt; Any</code>. The server handles one client at a time; if the engine reconnects, the next <code>accept</code> returns. Designed for the engine\u2194worker 1:1 pairing.</p> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>class RpcServer:\n    r\"\"\"Listens on a Unix socket, dispatches ops to a handler callable.\n\n    Handler signature: ``handler(op: str, args: dict) -&gt; Any``. The server\n    handles one client at a time; if the engine reconnects, the next ``accept``\n    returns. Designed for the engine\u2194worker 1:1 pairing.\n    \"\"\"\n\n    def __init__(self, socket_path: Path) -&gt; None:\n        self.socket_path = Path(socket_path)\n        self._server: socket.socket | None = None\n        self._shutdown = False\n\n    def serve(self, handler) -&gt; None:\n        if self.socket_path.exists():\n            self.socket_path.unlink()\n        self._server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)\n        self._server.bind(str(self.socket_path))\n        os.chmod(self.socket_path, 0o600)\n        self._server.listen(1)\n        log.info(\"rpc.server listening on %s\", self.socket_path)\n\n        while not self._shutdown:\n            try:\n                client, _ = self._server.accept()\n            except OSError:\n                break\n            self._serve_client(client, handler)\n\n    def _serve_client(self, client: socket.socket, handler) -&gt; None:\n        try:\n            while not self._shutdown:\n                try:\n                    req = _recv(client)\n                except (ConnectionError, EOFError):\n                    return\n                op = req.get(\"op\", \"\")\n                args = req.get(\"args\", {})\n                req_id = req.get(\"id\")\n                resp: dict[str, Any]\n                try:\n                    result = handler(op, args)\n                    resp = {\"id\": req_id, \"ok\": True, \"result\": result}\n                except Exception as exc:\n                    resp = {\n                        \"id\": req_id,\n                        \"ok\": False,\n                        \"error\": {\"cls\": type(exc).__name__, \"message\": str(exc)},\n                    }\n                _send(client, resp)\n                if op == \"close\":\n                    self._shutdown = True\n                    return\n        finally:\n            client.close()\n\n    def stop(self) -&gt; None:\n        self._shutdown = True\n        if self._server is not None:\n            try:\n                self._server.close()\n            except OSError:\n                pass\n        try:\n            self.socket_path.unlink(missing_ok=True)\n        except OSError:\n            pass\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer.socket_path","title":"socket_path  <code>instance-attribute</code>","text":"<pre><code>socket_path = Path(socket_path)\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer._server","title":"_server  <code>instance-attribute</code>","text":"<pre><code>_server: socket | None = None\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer._shutdown","title":"_shutdown  <code>instance-attribute</code>","text":"<pre><code>_shutdown = False\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer.__init__","title":"__init__","text":"<pre><code>__init__(socket_path: Path) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def __init__(self, socket_path: Path) -&gt; None:\n    self.socket_path = Path(socket_path)\n    self._server: socket.socket | None = None\n    self._shutdown = False\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer.serve","title":"serve","text":"<pre><code>serve(handler) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def serve(self, handler) -&gt; None:\n    if self.socket_path.exists():\n        self.socket_path.unlink()\n    self._server = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)\n    self._server.bind(str(self.socket_path))\n    os.chmod(self.socket_path, 0o600)\n    self._server.listen(1)\n    log.info(\"rpc.server listening on %s\", self.socket_path)\n\n    while not self._shutdown:\n        try:\n            client, _ = self._server.accept()\n        except OSError:\n            break\n        self._serve_client(client, handler)\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer._serve_client","title":"_serve_client","text":"<pre><code>_serve_client(client: socket, handler) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def _serve_client(self, client: socket.socket, handler) -&gt; None:\n    try:\n        while not self._shutdown:\n            try:\n                req = _recv(client)\n            except (ConnectionError, EOFError):\n                return\n            op = req.get(\"op\", \"\")\n            args = req.get(\"args\", {})\n            req_id = req.get(\"id\")\n            resp: dict[str, Any]\n            try:\n                result = handler(op, args)\n                resp = {\"id\": req_id, \"ok\": True, \"result\": result}\n            except Exception as exc:\n                resp = {\n                    \"id\": req_id,\n                    \"ok\": False,\n                    \"error\": {\"cls\": type(exc).__name__, \"message\": str(exc)},\n                }\n            _send(client, resp)\n            if op == \"close\":\n                self._shutdown = True\n                return\n    finally:\n        client.close()\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.RpcServer.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def stop(self) -&gt; None:\n    self._shutdown = True\n    if self._server is not None:\n        try:\n            self._server.close()\n        except OSError:\n            pass\n    try:\n        self.socket_path.unlink(missing_ok=True)\n    except OSError:\n        pass\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc.default_socket_path","title":"default_socket_path","text":"<pre><code>default_socket_path(model_id: str) -&gt; Path\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def default_socket_path(model_id: str) -&gt; Path:\n    slug = model_id.replace(\"/\", \"_\").replace(\":\", \"_\")\n    suffix = uuid.uuid4().hex[:8]\n    return Path(tempfile.gettempdir()) / f\"wk-{slug}-{suffix}.sock\"\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._send","title":"_send","text":"<pre><code>_send(sock: socket, payload: dict[str, Any]) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def _send(sock: socket.socket, payload: dict[str, Any]) -&gt; None:\n    body = msgpack.packb(payload, use_bin_type=True)\n    if len(body) &gt; _MAX_FRAME:\n        raise RpcError(\"send\", \"RpcError\", f\"frame too large: {len(body)} bytes\")\n    sock.sendall(_HEADER.pack(len(body)) + body)\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._recv_exactly","title":"_recv_exactly","text":"<pre><code>_recv_exactly(sock: socket, n: int) -&gt; bytes\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def _recv_exactly(sock: socket.socket, n: int) -&gt; bytes:\n    chunks = []\n    remaining = n\n    while remaining:\n        chunk = sock.recv(remaining)\n        if not chunk:\n            raise ConnectionError(\"peer closed during recv\")\n        chunks.append(chunk)\n        remaining -= len(chunk)\n    return b\"\".join(chunks)\n</code></pre>"},{"location":"api/worldkernels/runtime/rpc/#worldkernels.runtime.rpc._recv","title":"_recv","text":"<pre><code>_recv(sock: socket) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/rpc.py</code> <pre><code>def _recv(sock: socket.socket) -&gt; dict[str, Any]:\n    header = _recv_exactly(sock, _HEADER.size)\n    (length,) = _HEADER.unpack(header)\n    if length &gt; _MAX_FRAME:\n        raise RpcError(\"recv\", \"RpcError\", f\"frame too large: {length} bytes\")\n    body = _recv_exactly(sock, length)\n    return msgpack.unpackb(body, raw=False)\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/","title":"stages","text":""},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages","title":"worldkernels.runtime.stages","text":"<p>Stage definitions for the world model execution pipeline.</p>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.DEFAULT_PIPELINE_STAGES","title":"DEFAULT_PIPELINE_STAGES  <code>module-attribute</code>","text":"<pre><code>DEFAULT_PIPELINE_STAGES: tuple[StageConfig, ...] = (StageConfig(StageType.ENCODE, StageExecMode.SINGLE_SHOT), StageConfig(StageType.TRANSITION, StageExecMode.ITERATIVE), StageConfig(StageType.DECODE, StageExecMode.SINGLE_SHOT))\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageType","title":"StageType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Pipeline stage identity.</p> Source code in <code>worldkernels/runtime/stages.py</code> <pre><code>class StageType(str, Enum):\n    \"\"\"Pipeline stage identity.\"\"\"\n\n    ENCODE = \"encode\"\n    TRANSITION = \"transition\"\n    DECODE = \"decode\"\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageType.ENCODE","title":"ENCODE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ENCODE = 'encode'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageType.TRANSITION","title":"TRANSITION  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>TRANSITION = 'transition'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageType.DECODE","title":"DECODE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>DECODE = 'decode'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageExecMode","title":"StageExecMode","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>How a stage executes: single forward pass or multi-step loop.</p> Source code in <code>worldkernels/runtime/stages.py</code> <pre><code>class StageExecMode(str, Enum):\n    r\"\"\"How a stage executes: single forward pass or multi-step loop.\"\"\"\n\n    SINGLE_SHOT = \"single_shot\"\n    ITERATIVE = \"iterative\"\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageExecMode.SINGLE_SHOT","title":"SINGLE_SHOT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>SINGLE_SHOT = 'single_shot'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageExecMode.ITERATIVE","title":"ITERATIVE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ITERATIVE = 'iterative'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.TransitionMode","title":"TransitionMode","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Attention architecture of the transition stage.</p> <p>Determines KV caching, streaming capability, and scheduling strategy.</p> Source code in <code>worldkernels/runtime/stages.py</code> <pre><code>class TransitionMode(str, Enum):\n    r\"\"\"Attention architecture of the transition stage.\n\n    Determines KV caching, streaming capability, and scheduling strategy.\n    \"\"\"\n\n    BIDIRECTIONAL = \"bidirectional\"\n    CAUSAL = \"causal\"\n    HYBRID = \"hybrid\"\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.TransitionMode.BIDIRECTIONAL","title":"BIDIRECTIONAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BIDIRECTIONAL = 'bidirectional'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.TransitionMode.CAUSAL","title":"CAUSAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>CAUSAL = 'causal'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.TransitionMode.HYBRID","title":"HYBRID  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>HYBRID = 'hybrid'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig","title":"StageConfig  <code>dataclass</code>","text":"<p>Per-stage execution configuration.</p> <p>Parameters:</p> Name Type Description Default <code>stage_type</code> <code>StageType</code> <p>Which pipeline stage this configures.</p> required <code>exec_mode</code> <code>StageExecMode</code> <p>Single-shot or iterative execution.</p> <code>SINGLE_SHOT</code> <code>device</code> <code>str | None</code> <p>Device override (None inherits from engine).</p> <code>None</code> <code>dtype</code> <code>str | None</code> <p>Dtype override (None inherits from engine).</p> <code>None</code> <code>memory_fraction</code> <code>float</code> <p>Fraction of GPU memory budget for this stage.</p> <code>1.0</code> <code>backend</code> <code>str</code> <p>Execution backend name (\"eager\", \"compile\", \"tensorrt\").</p> <code>'eager'</code> <code>enabled</code> <code>bool</code> <p>If False, the stage is skipped entirely.</p> <code>True</code> Source code in <code>worldkernels/runtime/stages.py</code> <pre><code>@dataclass\nclass StageConfig:\n    r\"\"\"Per-stage execution configuration.\n\n    Args:\n        stage_type: Which pipeline stage this configures.\n        exec_mode: Single-shot or iterative execution.\n        device: Device override (None inherits from engine).\n        dtype: Dtype override (None inherits from engine).\n        memory_fraction: Fraction of GPU memory budget for this stage.\n        backend: Execution backend name (\"eager\", \"compile\", \"tensorrt\").\n        enabled: If False, the stage is skipped entirely.\n    \"\"\"\n\n    stage_type: StageType\n    exec_mode: StageExecMode = StageExecMode.SINGLE_SHOT\n    device: str | None = None\n    dtype: str | None = None\n    memory_fraction: float = 1.0\n    backend: str = \"eager\"\n    enabled: bool = True\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.stage_type","title":"stage_type  <code>instance-attribute</code>","text":"<pre><code>stage_type: StageType\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.exec_mode","title":"exec_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>exec_mode: StageExecMode = StageExecMode.SINGLE_SHOT\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.device","title":"device  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>device: str | None = None\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.dtype","title":"dtype  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dtype: str | None = None\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.memory_fraction","title":"memory_fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>memory_fraction: float = 1.0\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.backend","title":"backend  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>backend: str = 'eager'\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.enabled","title":"enabled  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>enabled: bool = True\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageConfig.__init__","title":"__init__","text":"<pre><code>__init__(stage_type: StageType, exec_mode: StageExecMode = SINGLE_SHOT, device: str | None = None, dtype: str | None = None, memory_fraction: float = 1.0, backend: str = 'eager', enabled: bool = True) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageOutput","title":"StageOutput  <code>dataclass</code>","text":"<p>Typed inter-stage data payload.</p> Source code in <code>worldkernels/runtime/stages.py</code> <pre><code>@dataclass\nclass StageOutput:\n    r\"\"\"Typed inter-stage data payload.\"\"\"\n\n    stage_type: StageType\n    data: Any\n    timing_ms: float = 0.0\n    metadata: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageOutput.stage_type","title":"stage_type  <code>instance-attribute</code>","text":"<pre><code>stage_type: StageType\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageOutput.data","title":"data  <code>instance-attribute</code>","text":"<pre><code>data: Any\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageOutput.timing_ms","title":"timing_ms  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>timing_ms: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageOutput.metadata","title":"metadata  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>metadata: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageOutput.__init__","title":"__init__","text":"<pre><code>__init__(stage_type: StageType, data: Any, timing_ms: float = 0.0, metadata: dict[str, Any] = dict()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageTiming","title":"StageTiming  <code>dataclass</code>","text":"<p>Per-stage timing breakdown for profiling.</p> Source code in <code>worldkernels/runtime/stages.py</code> <pre><code>@dataclass\nclass StageTiming:\n    r\"\"\"Per-stage timing breakdown for profiling.\"\"\"\n\n    encode_action_ms: float = 0.0\n    transition_ms: float = 0.0\n    decode_observation_ms: float = 0.0\n\n    @property\n    def total_ms(self) -&gt; float:\n        return self.encode_action_ms + self.transition_ms + self.decode_observation_ms\n\n    def as_dict(self) -&gt; dict[str, float]:\n        return {\n            \"encode_action_ms\": self.encode_action_ms,\n            \"transition_ms\": self.transition_ms,\n            \"decode_observation_ms\": self.decode_observation_ms,\n            \"total_ms\": self.total_ms,\n        }\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageTiming.encode_action_ms","title":"encode_action_ms  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>encode_action_ms: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageTiming.transition_ms","title":"transition_ms  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_ms: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageTiming.decode_observation_ms","title":"decode_observation_ms  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>decode_observation_ms: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageTiming.total_ms","title":"total_ms  <code>property</code>","text":"<pre><code>total_ms: float\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageTiming.__init__","title":"__init__","text":"<pre><code>__init__(encode_action_ms: float = 0.0, transition_ms: float = 0.0, decode_observation_ms: float = 0.0) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/stages/#worldkernels.runtime.stages.StageTiming.as_dict","title":"as_dict","text":"<pre><code>as_dict() -&gt; dict[str, float]\n</code></pre> Source code in <code>worldkernels/runtime/stages.py</code> <pre><code>def as_dict(self) -&gt; dict[str, float]:\n    return {\n        \"encode_action_ms\": self.encode_action_ms,\n        \"transition_ms\": self.transition_ms,\n        \"decode_observation_ms\": self.decode_observation_ms,\n        \"total_ms\": self.total_ms,\n    }\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/","title":"worker","text":""},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker","title":"worldkernels.runtime.worker","text":"<p>Worker subprocess entrypoint for isolated models.</p> <p>Invoked as <code>python -m worldkernels.runtime.worker --model {id} --socket {path} --device {cuda:N}</code>. Runs inside the per-model isolated venv (<code>runtime/envs.py</code>). Loads the world via the standard registry, listens on a Unix socket, dispatches RPC ops, and shares tensors via <code>runtime/ipc.py</code> (CUDA IPC or host shm).</p>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._DISPATCH","title":"_DISPATCH  <code>module-attribute</code>","text":"<pre><code>_DISPATCH = {'init': _handle_init, 'warmup': _handle_warmup, 'profile_vram': _handle_profile_vram, 'create_initial_state': _handle_create_initial_state, 'encode_action': _handle_encode_action, 'transition': _handle_transition, 'decode': _handle_decode, 'release': _handle_release, 'health': _handle_health, 'close': _handle_close}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.WorkerState","title":"WorkerState  <code>dataclass</code>","text":"Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>@dataclass\nclass WorkerState:\n    world: Any = None\n    device: str = \"cuda\"\n    dtype: Any = None\n    objects: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.WorkerState.world","title":"world  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>world: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.WorkerState.device","title":"device  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>device: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.WorkerState.dtype","title":"dtype  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dtype: Any = None\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.WorkerState.objects","title":"objects  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>objects: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.WorkerState.__init__","title":"__init__","text":"<pre><code>__init__(world: Any = None, device: str = 'cuda', dtype: Any = None, objects: dict[str, Any] = dict()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._new_handle","title":"_new_handle","text":"<pre><code>_new_handle() -&gt; str\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _new_handle() -&gt; str:\n    return uuid.uuid4().hex\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_init","title":"_handle_init","text":"<pre><code>_handle_init(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_init(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    from worldkernels.worlds.registry import get_world_class\n\n    adapter = args[\"adapter\"]\n    kwargs = args.get(\"kwargs\", {})\n    device = args.get(\"device\", \"cuda\")\n    dtype_str = args.get(\"dtype\", \"torch.bfloat16\")\n\n    cls = get_world_class(adapter)\n    dtype = _resolve_dtype(dtype_str)\n    world = cls(**kwargs)\n    world.initialize(device=device, dtype=dtype)\n\n    state.world = world\n    state.device = device\n    state.dtype = dtype\n    log.info(\"worker: loaded %s on %s (%s)\", adapter, device, dtype)\n    return {\"adapter\": adapter, \"device\": device, \"dtype\": str(dtype)}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_warmup","title":"_handle_warmup","text":"<pre><code>_handle_warmup(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_warmup(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    from worldkernels.config import WorldConfig\n\n    cfg = WorldConfig(**args.get(\"config\", {}))\n    state.world.warmup(cfg)\n    return {\"warmed\": True}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_profile_vram","title":"_handle_profile_vram","text":"<pre><code>_handle_profile_vram(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_profile_vram(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    from worldkernels.config import WorldConfig\n\n    cfg = WorldConfig(**args.get(\"config\", {}))\n    return {\"vram_mb\": float(state.world.profile_vram(cfg))}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_create_initial_state","title":"_handle_create_initial_state","text":"<pre><code>_handle_create_initial_state(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_create_initial_state(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    from worldkernels.config import WorldConfig\n\n    cfg = WorldConfig(**args.get(\"config\", {}))\n    seed = int(args.get(\"seed\", 0))\n    latent_state = state.world.create_initial_state(cfg, seed)\n    h = _new_handle()\n    state.objects[h] = latent_state\n    return {\"handle\": h}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_encode_action","title":"_handle_encode_action","text":"<pre><code>_handle_encode_action(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_encode_action(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    from worldkernels.core.action import Action\n\n    action = Action(action_type=args[\"action_type\"], payload=args.get(\"payload\", {}))\n    tensor = state.world.encode_action(action)\n    h = _new_handle()\n    state.objects[h] = tensor\n    return {\"handle\": h}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_transition","title":"_handle_transition","text":"<pre><code>_handle_transition(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_transition(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    state_h = args[\"state_handle\"]\n    action_h = args[\"action_handle\"]\n    latent = state.objects[state_h]\n    action = state.objects[action_h]\n    new_state = state.world.transition(latent, action)\n    new_h = _new_handle()\n    state.objects[new_h] = new_state\n    return {\"handle\": new_h}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_decode","title":"_handle_decode","text":"<pre><code>_handle_decode(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_decode(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    state_h = args[\"state_handle\"]\n    modalities = args.get(\"modalities\", [\"frames\"])\n    latent = state.objects[state_h]\n    t0 = time.perf_counter()\n    obs = state.world.decode_observation(latent, modalities)\n    elapsed_ms = (time.perf_counter() - t0) * 1000.0\n    result: dict[str, Any] = {\n        \"step_index\": obs.step_index,\n        \"generation_time_ms\": getattr(obs, \"generation_time_ms\", elapsed_ms),\n        \"decode_skipped\": getattr(obs, \"decode_skipped\", False),\n    }\n    if obs.frames is not None:\n        result[\"frames\"] = [_bytes_or_handle(f) for f in obs.frames]\n    if getattr(obs, \"latent\", None) is not None:\n        result[\"latent_ipc\"] = _to_ipc(obs.latent)\n    return result\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._bytes_or_handle","title":"_bytes_or_handle","text":"<pre><code>_bytes_or_handle(frame: Any) -&gt; Any\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _bytes_or_handle(frame: Any) -&gt; Any:\n    if isinstance(frame, (bytes, bytearray)):\n        return {\"kind\": \"bytes\", \"data\": bytes(frame)}\n    handle = ipc.share_tensor(frame)\n    return {\"kind\": \"ipc\", \"handle\": _ipc_to_dict(handle)}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._to_ipc","title":"_to_ipc","text":"<pre><code>_to_ipc(t: Any) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _to_ipc(t: Any) -&gt; dict[str, Any]:\n    return _ipc_to_dict(ipc.share_tensor(t))\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._ipc_to_dict","title":"_ipc_to_dict","text":"<pre><code>_ipc_to_dict(h: IpcHandle) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _ipc_to_dict(h: ipc.IpcHandle) -&gt; dict[str, Any]:\n    return {\n        \"mode\": h.mode,\n        \"shape\": list(h.shape),\n        \"dtype\": h.dtype,\n        \"device\": h.device,\n        \"payload\": h.payload,\n    }\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_release","title":"_handle_release","text":"<pre><code>_handle_release(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_release(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    h = args[\"handle\"]\n    state.objects.pop(h, None)\n    return {\"released\": h}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_health","title":"_handle_health","text":"<pre><code>_handle_health(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_health(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    import os\n\n    return {\n        \"ok\": True,\n        \"pid\": os.getpid(),\n        \"device\": state.device,\n        \"world_loaded\": state.world is not None,\n        \"n_objects\": len(state.objects or {}),\n    }\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._handle_close","title":"_handle_close","text":"<pre><code>_handle_close(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _handle_close(state: WorkerState, args: dict[str, Any]) -&gt; dict[str, Any]:\n    state.objects.clear()\n    state.world = None\n    return {\"closed\": True}\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker._resolve_dtype","title":"_resolve_dtype","text":"<pre><code>_resolve_dtype(name: str) -&gt; Any\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def _resolve_dtype(name: str) -&gt; Any:\n    import torch\n\n    short = name.split(\".\")[-1]\n    return getattr(torch, short, torch.float32)\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.serve","title":"serve","text":"<pre><code>serve(socket_path: Path) -&gt; None\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def serve(socket_path: Path) -&gt; None:\n    state = WorkerState()\n\n    def handler(op: str, args: dict[str, Any]) -&gt; Any:\n        fn = _DISPATCH.get(op)\n        if fn is None:\n            raise ValueError(f\"unknown op: {op}\")\n        return fn(state, args)\n\n    server = RpcServer(socket_path)\n    log.info(\"worker: serving on %s\", socket_path)\n    server.serve(handler)\n</code></pre>"},{"location":"api/worldkernels/runtime/worker/#worldkernels.runtime.worker.main","title":"main","text":"<pre><code>main() -&gt; int\n</code></pre> Source code in <code>worldkernels/runtime/worker.py</code> <pre><code>def main() -&gt; int:\n    import argparse\n\n    p = argparse.ArgumentParser(description=\"worldkernels worker subprocess\")\n    p.add_argument(\"--socket\", required=True, help=\"Unix socket path\")\n    p.add_argument(\"--log-level\", default=\"INFO\")\n    ns = p.parse_args()\n\n    logging.basicConfig(level=ns.log_level, format=\"worker[%(process)d] %(message)s\")\n    try:\n        serve(Path(ns.socket))\n        return 0\n    except KeyboardInterrupt:\n        return 0\n    except Exception:\n        log.exception(\"worker fatal\")\n        return 1\n</code></pre>"},{"location":"api/worldkernels/scheduler/","title":"scheduler","text":""},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler","title":"worldkernels.scheduler","text":"<p>Scheduler layer: continuous-batching dispatch, admission, preemption.</p> <p>Modules:</p> Name Description <code>admission</code> <p>Admission control.</p> <code>batching</code> <p>Compatibility-group batching.</p> <code>preemption</code> <p>Preemption policy.</p> <code>scheduler</code> <p>Scheduler \u2014 continuous-batching dispatch layer.</p>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Scheduler', 'CompatibilityKey', 'CompatibilityGroup', 'group_requests', 'AdmissionController', 'AdmissionDecision', 'PreemptionPolicy', 'PreemptionCandidate', 'PreemptionDecision']\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionController","title":"AdmissionController","text":"<p>Gates session creation on VRAM headroom and the concurrency cap.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>'SchedulerConfig'</code> <p>Scheduler policy (concurrency cap, headroom margin).</p> required Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>class AdmissionController:\n    r\"\"\"Gates session creation on VRAM headroom and the concurrency cap.\n\n    Args:\n        config: Scheduler policy (concurrency cap, headroom margin).\n    \"\"\"\n\n    def __init__(self, config: \"SchedulerConfig\") -&gt; None:\n        self.config = config\n\n    def check(\n        self,\n        world: \"WorldModel\",\n        world_config: \"WorldConfig\",\n        *,\n        live_sessions: int,\n        free_vram_mb: float | None,\n    ) -&gt; AdmissionDecision:\n        r\"\"\"Decide whether a new session for ``world`` may be admitted.\n\n        Args:\n            world: The world model the session will run.\n            world_config: Per-session generation parameters.\n            live_sessions: Current number of live sessions.\n            free_vram_mb: Free device VRAM, or ``None`` on CPU (skips the check).\n        \"\"\"\n        if live_sessions &gt;= self.config.max_concurrent_sessions:\n            return AdmissionDecision(\n                admitted=False,\n                reason=(\n                    f\"concurrency cap reached \"\n                    f\"({live_sessions}/{self.config.max_concurrent_sessions})\"\n                ),\n            )\n        required = world.profile_vram(world_config)\n        if free_vram_mb is None:\n            return AdmissionDecision(admitted=True, required_mb=required)\n        budget = free_vram_mb - self.config.admission_headroom_mb\n        if required &gt; budget:\n            return AdmissionDecision(\n                admitted=False,\n                reason=(\n                    f\"insufficient VRAM: need {required:.0f} MB + \"\n                    f\"{self.config.admission_headroom_mb:.0f} MB headroom, \"\n                    f\"{free_vram_mb:.0f} MB free\"\n                ),\n                required_mb=required,\n                available_mb=free_vram_mb,\n            )\n        return AdmissionDecision(admitted=True, required_mb=required, available_mb=free_vram_mb)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionController.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionController.__init__","title":"__init__","text":"<pre><code>__init__(config: 'SchedulerConfig') -&gt; None\n</code></pre> Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>def __init__(self, config: \"SchedulerConfig\") -&gt; None:\n    self.config = config\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionController.check","title":"check","text":"<pre><code>check(world: 'WorldModel', world_config: 'WorldConfig', *, live_sessions: int, free_vram_mb: float | None) -&gt; AdmissionDecision\n</code></pre> <p>Decide whether a new session for <code>world</code> may be admitted.</p> <p>Parameters:</p> Name Type Description Default <code>world</code> <code>'WorldModel'</code> <p>The world model the session will run.</p> required <code>world_config</code> <code>'WorldConfig'</code> <p>Per-session generation parameters.</p> required <code>live_sessions</code> <code>int</code> <p>Current number of live sessions.</p> required <code>free_vram_mb</code> <code>float | None</code> <p>Free device VRAM, or <code>None</code> on CPU (skips the check).</p> required Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>def check(\n    self,\n    world: \"WorldModel\",\n    world_config: \"WorldConfig\",\n    *,\n    live_sessions: int,\n    free_vram_mb: float | None,\n) -&gt; AdmissionDecision:\n    r\"\"\"Decide whether a new session for ``world`` may be admitted.\n\n    Args:\n        world: The world model the session will run.\n        world_config: Per-session generation parameters.\n        live_sessions: Current number of live sessions.\n        free_vram_mb: Free device VRAM, or ``None`` on CPU (skips the check).\n    \"\"\"\n    if live_sessions &gt;= self.config.max_concurrent_sessions:\n        return AdmissionDecision(\n            admitted=False,\n            reason=(\n                f\"concurrency cap reached \"\n                f\"({live_sessions}/{self.config.max_concurrent_sessions})\"\n            ),\n        )\n    required = world.profile_vram(world_config)\n    if free_vram_mb is None:\n        return AdmissionDecision(admitted=True, required_mb=required)\n    budget = free_vram_mb - self.config.admission_headroom_mb\n    if required &gt; budget:\n        return AdmissionDecision(\n            admitted=False,\n            reason=(\n                f\"insufficient VRAM: need {required:.0f} MB + \"\n                f\"{self.config.admission_headroom_mb:.0f} MB headroom, \"\n                f\"{free_vram_mb:.0f} MB free\"\n            ),\n            required_mb=required,\n            available_mb=free_vram_mb,\n        )\n    return AdmissionDecision(admitted=True, required_mb=required, available_mb=free_vram_mb)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionDecision","title":"AdmissionDecision  <code>dataclass</code>","text":"<p>Outcome of an admission check.</p> <p>Parameters:</p> Name Type Description Default <code>admitted</code> <code>bool</code> <p>Whether the session may be created.</p> required <code>reason</code> <code>str</code> <p>Human-readable explanation when refused.</p> <code>''</code> <code>required_mb</code> <code>float</code> <p>Profiled VRAM the session needs.</p> <code>0.0</code> <code>available_mb</code> <code>float</code> <p>Free device VRAM at decision time.</p> <code>0.0</code> Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>@dataclass\nclass AdmissionDecision:\n    r\"\"\"Outcome of an admission check.\n\n    Args:\n        admitted: Whether the session may be created.\n        reason: Human-readable explanation when refused.\n        required_mb: Profiled VRAM the session needs.\n        available_mb: Free device VRAM at decision time.\n    \"\"\"\n\n    admitted: bool\n    reason: str = \"\"\n    required_mb: float = 0.0\n    available_mb: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionDecision.admitted","title":"admitted  <code>instance-attribute</code>","text":"<pre><code>admitted: bool\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionDecision.reason","title":"reason  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>reason: str = ''\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionDecision.required_mb","title":"required_mb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>required_mb: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionDecision.available_mb","title":"available_mb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>available_mb: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.AdmissionDecision.__init__","title":"__init__","text":"<pre><code>__init__(admitted: bool, reason: str = '', required_mb: float = 0.0, available_mb: float = 0.0) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityGroup","title":"CompatibilityGroup  <code>dataclass</code>","text":"<p>A set of step requests that can share one batched forward pass.</p> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>@dataclass\nclass CompatibilityGroup:\n    r\"\"\"A set of step requests that can share one batched forward pass.\"\"\"\n\n    key: CompatibilityKey\n    requests: list[\"StepRequest\"]\n\n    @property\n    def size(self) -&gt; int:\n        return len(self.requests)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityGroup.key","title":"key  <code>instance-attribute</code>","text":"<pre><code>key: CompatibilityKey\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityGroup.requests","title":"requests  <code>instance-attribute</code>","text":"<pre><code>requests: list['StepRequest']\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityGroup.size","title":"size  <code>property</code>","text":"<pre><code>size: int\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityGroup.__init__","title":"__init__","text":"<pre><code>__init__(key: CompatibilityKey, requests: list['StepRequest']) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityKey","title":"CompatibilityKey  <code>dataclass</code>","text":"<p>Equivalence class for batching: same world instance, same regime.</p> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>@dataclass(frozen=True)\nclass CompatibilityKey:\n    r\"\"\"Equivalence class for batching: same world instance, same regime.\"\"\"\n\n    world_id: int\n    transition_mode: str\n    decode: bool\n\n    @classmethod\n    def of(cls, request: \"StepRequest\") -&gt; \"CompatibilityKey\":\n        return cls(\n            world_id=id(request.world),\n            transition_mode=request.world.transition_mode.value,\n            decode=request.decode,\n        )\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityKey.world_id","title":"world_id  <code>instance-attribute</code>","text":"<pre><code>world_id: int\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityKey.transition_mode","title":"transition_mode  <code>instance-attribute</code>","text":"<pre><code>transition_mode: str\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityKey.decode","title":"decode  <code>instance-attribute</code>","text":"<pre><code>decode: bool\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityKey.of","title":"of  <code>classmethod</code>","text":"<pre><code>of(request: 'StepRequest') -&gt; 'CompatibilityKey'\n</code></pre> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>@classmethod\ndef of(cls, request: \"StepRequest\") -&gt; \"CompatibilityKey\":\n    return cls(\n        world_id=id(request.world),\n        transition_mode=request.world.transition_mode.value,\n        decode=request.decode,\n    )\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.CompatibilityKey.__init__","title":"__init__","text":"<pre><code>__init__(world_id: int, transition_mode: str, decode: bool) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionCandidate","title":"PreemptionCandidate  <code>dataclass</code>","text":"<p>A running session considered for preemption.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The session.</p> required <code>last_active_step</code> <code>int</code> <p>Monotonic step index of its last activity.</p> required <code>priority</code> <code>int</code> <p>Higher means more important; preempt low-priority first.</p> <code>0</code> <code>state_bytes</code> <code>int</code> <p>Footprint of its latent state.</p> <code>0</code> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>@dataclass\nclass PreemptionCandidate:\n    r\"\"\"A running session considered for preemption.\n\n    Args:\n        session_id: The session.\n        last_active_step: Monotonic step index of its last activity.\n        priority: Higher means more important; preempt low-priority first.\n        state_bytes: Footprint of its latent state.\n    \"\"\"\n\n    session_id: str\n    last_active_step: int\n    priority: int = 0\n    state_bytes: int = 0\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionCandidate.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id: str\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionCandidate.last_active_step","title":"last_active_step  <code>instance-attribute</code>","text":"<pre><code>last_active_step: int\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionCandidate.priority","title":"priority  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>priority: int = 0\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionCandidate.state_bytes","title":"state_bytes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>state_bytes: int = 0\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionCandidate.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str, last_active_step: int, priority: int = 0, state_bytes: int = 0) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionDecision","title":"PreemptionDecision  <code>dataclass</code>","text":"Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>@dataclass\nclass PreemptionDecision:\n    victim_id: str\n    mode: Literal[\"swap\", \"recompute\"]\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionDecision.victim_id","title":"victim_id  <code>instance-attribute</code>","text":"<pre><code>victim_id: str\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionDecision.mode","title":"mode  <code>instance-attribute</code>","text":"<pre><code>mode: Literal['swap', 'recompute']\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionDecision.__init__","title":"__init__","text":"<pre><code>__init__(victim_id: str, mode: Literal['swap', 'recompute']) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionPolicy","title":"PreemptionPolicy","text":"<p>Selects which session to preempt and how.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>Literal['swap', 'recompute']</code> <p>Preferred reclamation mechanism.</p> <code>'swap'</code> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>class PreemptionPolicy:\n    r\"\"\"Selects which session to preempt and how.\n\n    Args:\n        mode: Preferred reclamation mechanism.\n    \"\"\"\n\n    def __init__(self, mode: Literal[\"swap\", \"recompute\"] = \"swap\") -&gt; None:\n        self.mode = mode\n\n    def select_victim(\n        self,\n        candidates: \"Sequence[PreemptionCandidate]\",\n    ) -&gt; PreemptionDecision | None:\n        r\"\"\"Pick the lowest-priority, least-recently-active session to preempt.\n\n        Returns ``None`` when there is nothing to preempt.\n        \"\"\"\n        if not candidates:\n            return None\n        victim = min(candidates, key=lambda c: (c.priority, c.last_active_step))\n        return PreemptionDecision(victim_id=victim.session_id, mode=self.mode)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionPolicy.mode","title":"mode  <code>instance-attribute</code>","text":"<pre><code>mode = mode\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionPolicy.__init__","title":"__init__","text":"<pre><code>__init__(mode: Literal['swap', 'recompute'] = 'swap') -&gt; None\n</code></pre> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>def __init__(self, mode: Literal[\"swap\", \"recompute\"] = \"swap\") -&gt; None:\n    self.mode = mode\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.PreemptionPolicy.select_victim","title":"select_victim","text":"<pre><code>select_victim(candidates: 'Sequence[PreemptionCandidate]') -&gt; PreemptionDecision | None\n</code></pre> <p>Pick the lowest-priority, least-recently-active session to preempt.</p> <p>Returns <code>None</code> when there is nothing to preempt.</p> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>def select_victim(\n    self,\n    candidates: \"Sequence[PreemptionCandidate]\",\n) -&gt; PreemptionDecision | None:\n    r\"\"\"Pick the lowest-priority, least-recently-active session to preempt.\n\n    Returns ``None`` when there is nothing to preempt.\n    \"\"\"\n    if not candidates:\n        return None\n    victim = min(candidates, key=lambda c: (c.priority, c.last_active_step))\n    return PreemptionDecision(victim_id=victim.session_id, mode=self.mode)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler","title":"Scheduler","text":"<p>Dispatches step requests to a worker, batching compatible ones.</p> <p>Parameters:</p> Name Type Description Default <code>worker</code> <code>'Worker'</code> <p>The worker that executes requests.</p> required <code>config</code> <code>SchedulerConfig | None</code> <p>Batching / admission / preemption policy.</p> <code>None</code> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>class Scheduler:\n    r\"\"\"Dispatches step requests to a worker, batching compatible ones.\n\n    Args:\n        worker: The worker that executes requests.\n        config: Batching / admission / preemption policy.\n    \"\"\"\n\n    def __init__(self, worker: \"Worker\", config: SchedulerConfig | None = None) -&gt; None:\n        self.worker = worker\n        self.config = config or SchedulerConfig()\n        self.admission = AdmissionController(self.config)\n        self.preemption = PreemptionPolicy(self.config.preemption_mode)\n        self._queue: list[StepRequest] = []\n\n    @property\n    def pending(self) -&gt; int:\n        r\"\"\"Number of requests waiting in the queue.\"\"\"\n        return len(self._queue)\n\n    def step(\n        self,\n        *,\n        world: \"WorldModel\",\n        state: \"LatentState\",\n        action: \"Action\",\n        modalities: list[str],\n        step_index: int,\n        decode: bool = True,\n        overrides: dict | None = None,\n    ) -&gt; tuple[\"LatentState\", \"Observation\"]:\n        r\"\"\"Dispatch one simulation step synchronously and return its result.\"\"\"\n        from worldkernels.core.request import StepRequest\n\n        request = StepRequest(\n            session_id=\"\",\n            world=world,\n            state=state,\n            action=action,\n            modalities=list(modalities),\n            step_index=step_index,\n            decode=decode,\n            overrides=overrides,\n        )\n        (result,) = self.worker.execute([request])\n        return result\n\n    def add_request(self, request: \"StepRequest\") -&gt; None:\n        r\"\"\"Enqueue a step request for the next scheduling round.\"\"\"\n        self._queue.append(request)\n\n    def schedule(self, max_batch_size: int | None = None) -&gt; list[CompatibilityGroup]:\n        r\"\"\"Drain the queue into compatibility-group batches.\n\n        Args:\n            max_batch_size: Override the configured cap. ``1`` collapses batching\n                (one request per group) \u2014 used when ``continuous_batching`` is off.\n        \"\"\"\n        if not self._queue:\n            return []\n        cap = max_batch_size if max_batch_size is not None else self.config.max_batch_size\n        groups = group_requests(self._queue, max_batch_size=cap)\n        self._queue.clear()\n        return groups\n\n    def run_scheduled(\n        self, max_batch_size: int | None = None\n    ) -&gt; dict[str, tuple[\"LatentState\", \"Observation\"]]:\n        r\"\"\"Schedule pending requests, dispatch each group batched, and collect results.\n\n        Returns a mapping from ``session_id`` to ``(new_state, observation)``.\n        \"\"\"\n        results: dict[str, tuple[LatentState, Observation]] = {}\n        for group in self.schedule(max_batch_size=max_batch_size):\n            outputs = self.worker.execute(group.requests)\n            for request, output in zip(group.requests, outputs):\n                results[request.session_id] = output\n        return results\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.worker","title":"worker  <code>instance-attribute</code>","text":"<pre><code>worker = worker\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config or SchedulerConfig()\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.admission","title":"admission  <code>instance-attribute</code>","text":"<pre><code>admission = AdmissionController(self.config)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.preemption","title":"preemption  <code>instance-attribute</code>","text":"<pre><code>preemption = PreemptionPolicy(self.config.preemption_mode)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler._queue","title":"_queue  <code>instance-attribute</code>","text":"<pre><code>_queue: list[StepRequest] = []\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.pending","title":"pending  <code>property</code>","text":"<pre><code>pending: int\n</code></pre> <p>Number of requests waiting in the queue.</p>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.__init__","title":"__init__","text":"<pre><code>__init__(worker: 'Worker', config: SchedulerConfig | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def __init__(self, worker: \"Worker\", config: SchedulerConfig | None = None) -&gt; None:\n    self.worker = worker\n    self.config = config or SchedulerConfig()\n    self.admission = AdmissionController(self.config)\n    self.preemption = PreemptionPolicy(self.config.preemption_mode)\n    self._queue: list[StepRequest] = []\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.step","title":"step","text":"<pre><code>step(*, world: 'WorldModel', state: 'LatentState', action: 'Action', modalities: list[str], step_index: int, decode: bool = True, overrides: dict | None = None) -&gt; tuple['LatentState', 'Observation']\n</code></pre> <p>Dispatch one simulation step synchronously and return its result.</p> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def step(\n    self,\n    *,\n    world: \"WorldModel\",\n    state: \"LatentState\",\n    action: \"Action\",\n    modalities: list[str],\n    step_index: int,\n    decode: bool = True,\n    overrides: dict | None = None,\n) -&gt; tuple[\"LatentState\", \"Observation\"]:\n    r\"\"\"Dispatch one simulation step synchronously and return its result.\"\"\"\n    from worldkernels.core.request import StepRequest\n\n    request = StepRequest(\n        session_id=\"\",\n        world=world,\n        state=state,\n        action=action,\n        modalities=list(modalities),\n        step_index=step_index,\n        decode=decode,\n        overrides=overrides,\n    )\n    (result,) = self.worker.execute([request])\n    return result\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.add_request","title":"add_request","text":"<pre><code>add_request(request: 'StepRequest') -&gt; None\n</code></pre> <p>Enqueue a step request for the next scheduling round.</p> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def add_request(self, request: \"StepRequest\") -&gt; None:\n    r\"\"\"Enqueue a step request for the next scheduling round.\"\"\"\n    self._queue.append(request)\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.schedule","title":"schedule","text":"<pre><code>schedule(max_batch_size: int | None = None) -&gt; list[CompatibilityGroup]\n</code></pre> <p>Drain the queue into compatibility-group batches.</p> <p>Parameters:</p> Name Type Description Default <code>max_batch_size</code> <code>int | None</code> <p>Override the configured cap. <code>1</code> collapses batching (one request per group) \u2014 used when <code>continuous_batching</code> is off.</p> <code>None</code> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def schedule(self, max_batch_size: int | None = None) -&gt; list[CompatibilityGroup]:\n    r\"\"\"Drain the queue into compatibility-group batches.\n\n    Args:\n        max_batch_size: Override the configured cap. ``1`` collapses batching\n            (one request per group) \u2014 used when ``continuous_batching`` is off.\n    \"\"\"\n    if not self._queue:\n        return []\n    cap = max_batch_size if max_batch_size is not None else self.config.max_batch_size\n    groups = group_requests(self._queue, max_batch_size=cap)\n    self._queue.clear()\n    return groups\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.Scheduler.run_scheduled","title":"run_scheduled","text":"<pre><code>run_scheduled(max_batch_size: int | None = None) -&gt; dict[str, tuple['LatentState', 'Observation']]\n</code></pre> <p>Schedule pending requests, dispatch each group batched, and collect results.</p> <p>Returns a mapping from <code>session_id</code> to <code>(new_state, observation)</code>.</p> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def run_scheduled(\n    self, max_batch_size: int | None = None\n) -&gt; dict[str, tuple[\"LatentState\", \"Observation\"]]:\n    r\"\"\"Schedule pending requests, dispatch each group batched, and collect results.\n\n    Returns a mapping from ``session_id`` to ``(new_state, observation)``.\n    \"\"\"\n    results: dict[str, tuple[LatentState, Observation]] = {}\n    for group in self.schedule(max_batch_size=max_batch_size):\n        outputs = self.worker.execute(group.requests)\n        for request, output in zip(group.requests, outputs):\n            results[request.session_id] = output\n    return results\n</code></pre>"},{"location":"api/worldkernels/scheduler/#worldkernels.scheduler.group_requests","title":"group_requests","text":"<pre><code>group_requests(requests: list['StepRequest'], *, max_batch_size: int) -&gt; list[CompatibilityGroup]\n</code></pre> <p>Partition <code>requests</code> into compatibility groups capped at <code>max_batch_size</code>.</p> <p>Insertion order is preserved within each group; a group that exceeds the cap is split into consecutive batches.</p> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>def group_requests(\n    requests: list[\"StepRequest\"],\n    *,\n    max_batch_size: int,\n) -&gt; list[CompatibilityGroup]:\n    r\"\"\"Partition ``requests`` into compatibility groups capped at ``max_batch_size``.\n\n    Insertion order is preserved within each group; a group that exceeds the\n    cap is split into consecutive batches.\n    \"\"\"\n    if max_batch_size &lt; 1:\n        raise ValueError(f\"max_batch_size must be &gt;= 1, got {max_batch_size}\")\n    by_key: dict[CompatibilityKey, list[StepRequest]] = {}\n    for request in requests:\n        by_key.setdefault(CompatibilityKey.of(request), []).append(request)\n\n    groups: list[CompatibilityGroup] = []\n    for key, members in by_key.items():\n        for start in range(0, len(members), max_batch_size):\n            groups.append(CompatibilityGroup(key, members[start : start + max_batch_size]))\n    return groups\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/","title":"admission","text":""},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission","title":"worldkernels.scheduler.admission","text":"<p>Admission control.</p> <p>Before a session is created the admission controller checks that its profiled VRAM cost, plus a headroom margin, fits in free device memory and that the concurrent-session cap is not exceeded.</p>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['AdmissionDecision', 'AdmissionController']\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionDecision","title":"AdmissionDecision  <code>dataclass</code>","text":"<p>Outcome of an admission check.</p> <p>Parameters:</p> Name Type Description Default <code>admitted</code> <code>bool</code> <p>Whether the session may be created.</p> required <code>reason</code> <code>str</code> <p>Human-readable explanation when refused.</p> <code>''</code> <code>required_mb</code> <code>float</code> <p>Profiled VRAM the session needs.</p> <code>0.0</code> <code>available_mb</code> <code>float</code> <p>Free device VRAM at decision time.</p> <code>0.0</code> Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>@dataclass\nclass AdmissionDecision:\n    r\"\"\"Outcome of an admission check.\n\n    Args:\n        admitted: Whether the session may be created.\n        reason: Human-readable explanation when refused.\n        required_mb: Profiled VRAM the session needs.\n        available_mb: Free device VRAM at decision time.\n    \"\"\"\n\n    admitted: bool\n    reason: str = \"\"\n    required_mb: float = 0.0\n    available_mb: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionDecision.admitted","title":"admitted  <code>instance-attribute</code>","text":"<pre><code>admitted: bool\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionDecision.reason","title":"reason  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>reason: str = ''\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionDecision.required_mb","title":"required_mb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>required_mb: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionDecision.available_mb","title":"available_mb  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>available_mb: float = 0.0\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionDecision.__init__","title":"__init__","text":"<pre><code>__init__(admitted: bool, reason: str = '', required_mb: float = 0.0, available_mb: float = 0.0) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionController","title":"AdmissionController","text":"<p>Gates session creation on VRAM headroom and the concurrency cap.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>'SchedulerConfig'</code> <p>Scheduler policy (concurrency cap, headroom margin).</p> required Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>class AdmissionController:\n    r\"\"\"Gates session creation on VRAM headroom and the concurrency cap.\n\n    Args:\n        config: Scheduler policy (concurrency cap, headroom margin).\n    \"\"\"\n\n    def __init__(self, config: \"SchedulerConfig\") -&gt; None:\n        self.config = config\n\n    def check(\n        self,\n        world: \"WorldModel\",\n        world_config: \"WorldConfig\",\n        *,\n        live_sessions: int,\n        free_vram_mb: float | None,\n    ) -&gt; AdmissionDecision:\n        r\"\"\"Decide whether a new session for ``world`` may be admitted.\n\n        Args:\n            world: The world model the session will run.\n            world_config: Per-session generation parameters.\n            live_sessions: Current number of live sessions.\n            free_vram_mb: Free device VRAM, or ``None`` on CPU (skips the check).\n        \"\"\"\n        if live_sessions &gt;= self.config.max_concurrent_sessions:\n            return AdmissionDecision(\n                admitted=False,\n                reason=(\n                    f\"concurrency cap reached \"\n                    f\"({live_sessions}/{self.config.max_concurrent_sessions})\"\n                ),\n            )\n        required = world.profile_vram(world_config)\n        if free_vram_mb is None:\n            return AdmissionDecision(admitted=True, required_mb=required)\n        budget = free_vram_mb - self.config.admission_headroom_mb\n        if required &gt; budget:\n            return AdmissionDecision(\n                admitted=False,\n                reason=(\n                    f\"insufficient VRAM: need {required:.0f} MB + \"\n                    f\"{self.config.admission_headroom_mb:.0f} MB headroom, \"\n                    f\"{free_vram_mb:.0f} MB free\"\n                ),\n                required_mb=required,\n                available_mb=free_vram_mb,\n            )\n        return AdmissionDecision(admitted=True, required_mb=required, available_mb=free_vram_mb)\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionController.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionController.__init__","title":"__init__","text":"<pre><code>__init__(config: 'SchedulerConfig') -&gt; None\n</code></pre> Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>def __init__(self, config: \"SchedulerConfig\") -&gt; None:\n    self.config = config\n</code></pre>"},{"location":"api/worldkernels/scheduler/admission/#worldkernels.scheduler.admission.AdmissionController.check","title":"check","text":"<pre><code>check(world: 'WorldModel', world_config: 'WorldConfig', *, live_sessions: int, free_vram_mb: float | None) -&gt; AdmissionDecision\n</code></pre> <p>Decide whether a new session for <code>world</code> may be admitted.</p> <p>Parameters:</p> Name Type Description Default <code>world</code> <code>'WorldModel'</code> <p>The world model the session will run.</p> required <code>world_config</code> <code>'WorldConfig'</code> <p>Per-session generation parameters.</p> required <code>live_sessions</code> <code>int</code> <p>Current number of live sessions.</p> required <code>free_vram_mb</code> <code>float | None</code> <p>Free device VRAM, or <code>None</code> on CPU (skips the check).</p> required Source code in <code>worldkernels/scheduler/admission.py</code> <pre><code>def check(\n    self,\n    world: \"WorldModel\",\n    world_config: \"WorldConfig\",\n    *,\n    live_sessions: int,\n    free_vram_mb: float | None,\n) -&gt; AdmissionDecision:\n    r\"\"\"Decide whether a new session for ``world`` may be admitted.\n\n    Args:\n        world: The world model the session will run.\n        world_config: Per-session generation parameters.\n        live_sessions: Current number of live sessions.\n        free_vram_mb: Free device VRAM, or ``None`` on CPU (skips the check).\n    \"\"\"\n    if live_sessions &gt;= self.config.max_concurrent_sessions:\n        return AdmissionDecision(\n            admitted=False,\n            reason=(\n                f\"concurrency cap reached \"\n                f\"({live_sessions}/{self.config.max_concurrent_sessions})\"\n            ),\n        )\n    required = world.profile_vram(world_config)\n    if free_vram_mb is None:\n        return AdmissionDecision(admitted=True, required_mb=required)\n    budget = free_vram_mb - self.config.admission_headroom_mb\n    if required &gt; budget:\n        return AdmissionDecision(\n            admitted=False,\n            reason=(\n                f\"insufficient VRAM: need {required:.0f} MB + \"\n                f\"{self.config.admission_headroom_mb:.0f} MB headroom, \"\n                f\"{free_vram_mb:.0f} MB free\"\n            ),\n            required_mb=required,\n            available_mb=free_vram_mb,\n        )\n    return AdmissionDecision(admitted=True, required_mb=required, available_mb=free_vram_mb)\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/","title":"batching","text":""},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching","title":"worldkernels.scheduler.batching","text":"<p>Compatibility-group batching.</p> <p>Two step requests may share a batched forward pass only if they run the same world model in the same regime. <code>CompatibilityKey</code> is the equivalence class; <code>group_requests()</code> partitions a queue into batchable groups, each capped at the scheduler's <code>max_batch_size</code>.</p>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['CompatibilityKey', 'CompatibilityGroup', 'group_requests']\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityKey","title":"CompatibilityKey  <code>dataclass</code>","text":"<p>Equivalence class for batching: same world instance, same regime.</p> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>@dataclass(frozen=True)\nclass CompatibilityKey:\n    r\"\"\"Equivalence class for batching: same world instance, same regime.\"\"\"\n\n    world_id: int\n    transition_mode: str\n    decode: bool\n\n    @classmethod\n    def of(cls, request: \"StepRequest\") -&gt; \"CompatibilityKey\":\n        return cls(\n            world_id=id(request.world),\n            transition_mode=request.world.transition_mode.value,\n            decode=request.decode,\n        )\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityKey.world_id","title":"world_id  <code>instance-attribute</code>","text":"<pre><code>world_id: int\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityKey.transition_mode","title":"transition_mode  <code>instance-attribute</code>","text":"<pre><code>transition_mode: str\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityKey.decode","title":"decode  <code>instance-attribute</code>","text":"<pre><code>decode: bool\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityKey.__init__","title":"__init__","text":"<pre><code>__init__(world_id: int, transition_mode: str, decode: bool) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityKey.of","title":"of  <code>classmethod</code>","text":"<pre><code>of(request: 'StepRequest') -&gt; 'CompatibilityKey'\n</code></pre> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>@classmethod\ndef of(cls, request: \"StepRequest\") -&gt; \"CompatibilityKey\":\n    return cls(\n        world_id=id(request.world),\n        transition_mode=request.world.transition_mode.value,\n        decode=request.decode,\n    )\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityGroup","title":"CompatibilityGroup  <code>dataclass</code>","text":"<p>A set of step requests that can share one batched forward pass.</p> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>@dataclass\nclass CompatibilityGroup:\n    r\"\"\"A set of step requests that can share one batched forward pass.\"\"\"\n\n    key: CompatibilityKey\n    requests: list[\"StepRequest\"]\n\n    @property\n    def size(self) -&gt; int:\n        return len(self.requests)\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityGroup.key","title":"key  <code>instance-attribute</code>","text":"<pre><code>key: CompatibilityKey\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityGroup.requests","title":"requests  <code>instance-attribute</code>","text":"<pre><code>requests: list['StepRequest']\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityGroup.size","title":"size  <code>property</code>","text":"<pre><code>size: int\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.CompatibilityGroup.__init__","title":"__init__","text":"<pre><code>__init__(key: CompatibilityKey, requests: list['StepRequest']) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/batching/#worldkernels.scheduler.batching.group_requests","title":"group_requests","text":"<pre><code>group_requests(requests: list['StepRequest'], *, max_batch_size: int) -&gt; list[CompatibilityGroup]\n</code></pre> <p>Partition <code>requests</code> into compatibility groups capped at <code>max_batch_size</code>.</p> <p>Insertion order is preserved within each group; a group that exceeds the cap is split into consecutive batches.</p> Source code in <code>worldkernels/scheduler/batching.py</code> <pre><code>def group_requests(\n    requests: list[\"StepRequest\"],\n    *,\n    max_batch_size: int,\n) -&gt; list[CompatibilityGroup]:\n    r\"\"\"Partition ``requests`` into compatibility groups capped at ``max_batch_size``.\n\n    Insertion order is preserved within each group; a group that exceeds the\n    cap is split into consecutive batches.\n    \"\"\"\n    if max_batch_size &lt; 1:\n        raise ValueError(f\"max_batch_size must be &gt;= 1, got {max_batch_size}\")\n    by_key: dict[CompatibilityKey, list[StepRequest]] = {}\n    for request in requests:\n        by_key.setdefault(CompatibilityKey.of(request), []).append(request)\n\n    groups: list[CompatibilityGroup] = []\n    for key, members in by_key.items():\n        for start in range(0, len(members), max_batch_size):\n            groups.append(CompatibilityGroup(key, members[start : start + max_batch_size]))\n    return groups\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/","title":"preemption","text":""},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption","title":"worldkernels.scheduler.preemption","text":"<p>Preemption policy.</p> <p>Under VRAM pressure the scheduler reclaims a running session's state. Two mechanisms: swap (copy state to host memory, restore later) and recompute (drop state, replay the action sequence from a checkpoint). The policy picks a victim and the mechanism.</p>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['PreemptionCandidate', 'PreemptionDecision', 'PreemptionPolicy']\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionCandidate","title":"PreemptionCandidate  <code>dataclass</code>","text":"<p>A running session considered for preemption.</p> <p>Parameters:</p> Name Type Description Default <code>session_id</code> <code>str</code> <p>The session.</p> required <code>last_active_step</code> <code>int</code> <p>Monotonic step index of its last activity.</p> required <code>priority</code> <code>int</code> <p>Higher means more important; preempt low-priority first.</p> <code>0</code> <code>state_bytes</code> <code>int</code> <p>Footprint of its latent state.</p> <code>0</code> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>@dataclass\nclass PreemptionCandidate:\n    r\"\"\"A running session considered for preemption.\n\n    Args:\n        session_id: The session.\n        last_active_step: Monotonic step index of its last activity.\n        priority: Higher means more important; preempt low-priority first.\n        state_bytes: Footprint of its latent state.\n    \"\"\"\n\n    session_id: str\n    last_active_step: int\n    priority: int = 0\n    state_bytes: int = 0\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionCandidate.session_id","title":"session_id  <code>instance-attribute</code>","text":"<pre><code>session_id: str\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionCandidate.last_active_step","title":"last_active_step  <code>instance-attribute</code>","text":"<pre><code>last_active_step: int\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionCandidate.priority","title":"priority  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>priority: int = 0\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionCandidate.state_bytes","title":"state_bytes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>state_bytes: int = 0\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionCandidate.__init__","title":"__init__","text":"<pre><code>__init__(session_id: str, last_active_step: int, priority: int = 0, state_bytes: int = 0) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionDecision","title":"PreemptionDecision  <code>dataclass</code>","text":"Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>@dataclass\nclass PreemptionDecision:\n    victim_id: str\n    mode: Literal[\"swap\", \"recompute\"]\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionDecision.victim_id","title":"victim_id  <code>instance-attribute</code>","text":"<pre><code>victim_id: str\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionDecision.mode","title":"mode  <code>instance-attribute</code>","text":"<pre><code>mode: Literal['swap', 'recompute']\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionDecision.__init__","title":"__init__","text":"<pre><code>__init__(victim_id: str, mode: Literal['swap', 'recompute']) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionPolicy","title":"PreemptionPolicy","text":"<p>Selects which session to preempt and how.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>Literal['swap', 'recompute']</code> <p>Preferred reclamation mechanism.</p> <code>'swap'</code> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>class PreemptionPolicy:\n    r\"\"\"Selects which session to preempt and how.\n\n    Args:\n        mode: Preferred reclamation mechanism.\n    \"\"\"\n\n    def __init__(self, mode: Literal[\"swap\", \"recompute\"] = \"swap\") -&gt; None:\n        self.mode = mode\n\n    def select_victim(\n        self,\n        candidates: \"Sequence[PreemptionCandidate]\",\n    ) -&gt; PreemptionDecision | None:\n        r\"\"\"Pick the lowest-priority, least-recently-active session to preempt.\n\n        Returns ``None`` when there is nothing to preempt.\n        \"\"\"\n        if not candidates:\n            return None\n        victim = min(candidates, key=lambda c: (c.priority, c.last_active_step))\n        return PreemptionDecision(victim_id=victim.session_id, mode=self.mode)\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionPolicy.mode","title":"mode  <code>instance-attribute</code>","text":"<pre><code>mode = mode\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionPolicy.__init__","title":"__init__","text":"<pre><code>__init__(mode: Literal['swap', 'recompute'] = 'swap') -&gt; None\n</code></pre> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>def __init__(self, mode: Literal[\"swap\", \"recompute\"] = \"swap\") -&gt; None:\n    self.mode = mode\n</code></pre>"},{"location":"api/worldkernels/scheduler/preemption/#worldkernels.scheduler.preemption.PreemptionPolicy.select_victim","title":"select_victim","text":"<pre><code>select_victim(candidates: 'Sequence[PreemptionCandidate]') -&gt; PreemptionDecision | None\n</code></pre> <p>Pick the lowest-priority, least-recently-active session to preempt.</p> <p>Returns <code>None</code> when there is nothing to preempt.</p> Source code in <code>worldkernels/scheduler/preemption.py</code> <pre><code>def select_victim(\n    self,\n    candidates: \"Sequence[PreemptionCandidate]\",\n) -&gt; PreemptionDecision | None:\n    r\"\"\"Pick the lowest-priority, least-recently-active session to preempt.\n\n    Returns ``None`` when there is nothing to preempt.\n    \"\"\"\n    if not candidates:\n        return None\n    victim = min(candidates, key=lambda c: (c.priority, c.last_active_step))\n    return PreemptionDecision(victim_id=victim.session_id, mode=self.mode)\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/","title":"scheduler","text":""},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler","title":"worldkernels.scheduler.scheduler","text":"<p>Scheduler \u2014 continuous-batching dispatch layer.</p> <p>Owns the transient <code>StepRequest</code> queue and dispatches work to a <code>Worker</code>. It does not own sessions \u2014 the engine's session registry does.</p> <p>Two entry points:</p> <ul> <li><code>step()</code> \u2014 synchronous, used by <code>Session.step</code> today: dispatch one   request and return its result.</li> <li><code>add_request()</code> / <code>run_scheduled()</code> \u2014 the queue path: many requests   are enqueued, partitioned into <code>CompatibilityGroup</code> batches, and each   group dispatched as one batched forward. The async engine drives this.</li> </ul> <p>Admission and preemption policy live here as <code>admission</code> and <code>preemption</code> for the engine to consult.</p>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Scheduler']\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler","title":"Scheduler","text":"<p>Dispatches step requests to a worker, batching compatible ones.</p> <p>Parameters:</p> Name Type Description Default <code>worker</code> <code>'Worker'</code> <p>The worker that executes requests.</p> required <code>config</code> <code>SchedulerConfig | None</code> <p>Batching / admission / preemption policy.</p> <code>None</code> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>class Scheduler:\n    r\"\"\"Dispatches step requests to a worker, batching compatible ones.\n\n    Args:\n        worker: The worker that executes requests.\n        config: Batching / admission / preemption policy.\n    \"\"\"\n\n    def __init__(self, worker: \"Worker\", config: SchedulerConfig | None = None) -&gt; None:\n        self.worker = worker\n        self.config = config or SchedulerConfig()\n        self.admission = AdmissionController(self.config)\n        self.preemption = PreemptionPolicy(self.config.preemption_mode)\n        self._queue: list[StepRequest] = []\n\n    @property\n    def pending(self) -&gt; int:\n        r\"\"\"Number of requests waiting in the queue.\"\"\"\n        return len(self._queue)\n\n    def step(\n        self,\n        *,\n        world: \"WorldModel\",\n        state: \"LatentState\",\n        action: \"Action\",\n        modalities: list[str],\n        step_index: int,\n        decode: bool = True,\n        overrides: dict | None = None,\n    ) -&gt; tuple[\"LatentState\", \"Observation\"]:\n        r\"\"\"Dispatch one simulation step synchronously and return its result.\"\"\"\n        from worldkernels.core.request import StepRequest\n\n        request = StepRequest(\n            session_id=\"\",\n            world=world,\n            state=state,\n            action=action,\n            modalities=list(modalities),\n            step_index=step_index,\n            decode=decode,\n            overrides=overrides,\n        )\n        (result,) = self.worker.execute([request])\n        return result\n\n    def add_request(self, request: \"StepRequest\") -&gt; None:\n        r\"\"\"Enqueue a step request for the next scheduling round.\"\"\"\n        self._queue.append(request)\n\n    def schedule(self, max_batch_size: int | None = None) -&gt; list[CompatibilityGroup]:\n        r\"\"\"Drain the queue into compatibility-group batches.\n\n        Args:\n            max_batch_size: Override the configured cap. ``1`` collapses batching\n                (one request per group) \u2014 used when ``continuous_batching`` is off.\n        \"\"\"\n        if not self._queue:\n            return []\n        cap = max_batch_size if max_batch_size is not None else self.config.max_batch_size\n        groups = group_requests(self._queue, max_batch_size=cap)\n        self._queue.clear()\n        return groups\n\n    def run_scheduled(\n        self, max_batch_size: int | None = None\n    ) -&gt; dict[str, tuple[\"LatentState\", \"Observation\"]]:\n        r\"\"\"Schedule pending requests, dispatch each group batched, and collect results.\n\n        Returns a mapping from ``session_id`` to ``(new_state, observation)``.\n        \"\"\"\n        results: dict[str, tuple[LatentState, Observation]] = {}\n        for group in self.schedule(max_batch_size=max_batch_size):\n            outputs = self.worker.execute(group.requests)\n            for request, output in zip(group.requests, outputs):\n                results[request.session_id] = output\n        return results\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.worker","title":"worker  <code>instance-attribute</code>","text":"<pre><code>worker = worker\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config or SchedulerConfig()\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.admission","title":"admission  <code>instance-attribute</code>","text":"<pre><code>admission = AdmissionController(self.config)\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.preemption","title":"preemption  <code>instance-attribute</code>","text":"<pre><code>preemption = PreemptionPolicy(self.config.preemption_mode)\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler._queue","title":"_queue  <code>instance-attribute</code>","text":"<pre><code>_queue: list[StepRequest] = []\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.pending","title":"pending  <code>property</code>","text":"<pre><code>pending: int\n</code></pre> <p>Number of requests waiting in the queue.</p>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.__init__","title":"__init__","text":"<pre><code>__init__(worker: 'Worker', config: SchedulerConfig | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def __init__(self, worker: \"Worker\", config: SchedulerConfig | None = None) -&gt; None:\n    self.worker = worker\n    self.config = config or SchedulerConfig()\n    self.admission = AdmissionController(self.config)\n    self.preemption = PreemptionPolicy(self.config.preemption_mode)\n    self._queue: list[StepRequest] = []\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.step","title":"step","text":"<pre><code>step(*, world: 'WorldModel', state: 'LatentState', action: 'Action', modalities: list[str], step_index: int, decode: bool = True, overrides: dict | None = None) -&gt; tuple['LatentState', 'Observation']\n</code></pre> <p>Dispatch one simulation step synchronously and return its result.</p> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def step(\n    self,\n    *,\n    world: \"WorldModel\",\n    state: \"LatentState\",\n    action: \"Action\",\n    modalities: list[str],\n    step_index: int,\n    decode: bool = True,\n    overrides: dict | None = None,\n) -&gt; tuple[\"LatentState\", \"Observation\"]:\n    r\"\"\"Dispatch one simulation step synchronously and return its result.\"\"\"\n    from worldkernels.core.request import StepRequest\n\n    request = StepRequest(\n        session_id=\"\",\n        world=world,\n        state=state,\n        action=action,\n        modalities=list(modalities),\n        step_index=step_index,\n        decode=decode,\n        overrides=overrides,\n    )\n    (result,) = self.worker.execute([request])\n    return result\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.add_request","title":"add_request","text":"<pre><code>add_request(request: 'StepRequest') -&gt; None\n</code></pre> <p>Enqueue a step request for the next scheduling round.</p> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def add_request(self, request: \"StepRequest\") -&gt; None:\n    r\"\"\"Enqueue a step request for the next scheduling round.\"\"\"\n    self._queue.append(request)\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.schedule","title":"schedule","text":"<pre><code>schedule(max_batch_size: int | None = None) -&gt; list[CompatibilityGroup]\n</code></pre> <p>Drain the queue into compatibility-group batches.</p> <p>Parameters:</p> Name Type Description Default <code>max_batch_size</code> <code>int | None</code> <p>Override the configured cap. <code>1</code> collapses batching (one request per group) \u2014 used when <code>continuous_batching</code> is off.</p> <code>None</code> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def schedule(self, max_batch_size: int | None = None) -&gt; list[CompatibilityGroup]:\n    r\"\"\"Drain the queue into compatibility-group batches.\n\n    Args:\n        max_batch_size: Override the configured cap. ``1`` collapses batching\n            (one request per group) \u2014 used when ``continuous_batching`` is off.\n    \"\"\"\n    if not self._queue:\n        return []\n    cap = max_batch_size if max_batch_size is not None else self.config.max_batch_size\n    groups = group_requests(self._queue, max_batch_size=cap)\n    self._queue.clear()\n    return groups\n</code></pre>"},{"location":"api/worldkernels/scheduler/scheduler/#worldkernels.scheduler.scheduler.Scheduler.run_scheduled","title":"run_scheduled","text":"<pre><code>run_scheduled(max_batch_size: int | None = None) -&gt; dict[str, tuple['LatentState', 'Observation']]\n</code></pre> <p>Schedule pending requests, dispatch each group batched, and collect results.</p> <p>Returns a mapping from <code>session_id</code> to <code>(new_state, observation)</code>.</p> Source code in <code>worldkernels/scheduler/scheduler.py</code> <pre><code>def run_scheduled(\n    self, max_batch_size: int | None = None\n) -&gt; dict[str, tuple[\"LatentState\", \"Observation\"]]:\n    r\"\"\"Schedule pending requests, dispatch each group batched, and collect results.\n\n    Returns a mapping from ``session_id`` to ``(new_state, observation)``.\n    \"\"\"\n    results: dict[str, tuple[LatentState, Observation]] = {}\n    for group in self.schedule(max_batch_size=max_batch_size):\n        outputs = self.worker.execute(group.requests)\n        for request, output in zip(group.requests, outputs):\n            results[request.session_id] = output\n    return results\n</code></pre>"},{"location":"api/worldkernels/serving/","title":"serving","text":""},{"location":"api/worldkernels/serving/#worldkernels.serving","title":"worldkernels.serving","text":"<p>Modules:</p> Name Description <code>auth</code> <p>API key authentication middleware.</p> <code>routes</code> <p>REST API routes for the WorldKernels serving layer.</p> <code>server</code> <p>FastAPI application factory for WorldKernels.</p> <code>websocket</code> <p>WebSocket frame streaming.</p>"},{"location":"api/worldkernels/serving/auth/","title":"auth","text":""},{"location":"api/worldkernels/serving/auth/#worldkernels.serving.auth","title":"worldkernels.serving.auth","text":"<p>API key authentication middleware.</p>"},{"location":"api/worldkernels/serving/auth/#worldkernels.serving.auth._api_key_header","title":"_api_key_header  <code>module-attribute</code>","text":"<pre><code>_api_key_header = APIKeyHeader(name='Authorization', auto_error=False)\n</code></pre>"},{"location":"api/worldkernels/serving/auth/#worldkernels.serving.auth.require_api_key","title":"require_api_key","text":"<pre><code>require_api_key(expected_key: str | None)\n</code></pre> <p>Return a FastAPI dependency that validates the Bearer token.</p> Source code in <code>worldkernels/serving/auth.py</code> <pre><code>def require_api_key(expected_key: str | None):\n    r\"\"\"Return a FastAPI dependency that validates the Bearer token.\"\"\"\n\n    async def _verify(key: str | None = Security(_api_key_header)) -&gt; None:\n        if expected_key is None:\n            return\n        if key is None:\n            raise HTTPException(status_code=401, detail=\"Missing API key.\")\n        token = key.removeprefix(\"Bearer \").strip()\n        if token != expected_key:\n            raise HTTPException(status_code=401, detail=\"Invalid API key.\")\n\n    return _verify\n</code></pre>"},{"location":"api/worldkernels/serving/routes/","title":"routes","text":""},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes","title":"worldkernels.serving.routes","text":"<p>REST API routes for the WorldKernels serving layer.</p> <p>Routes are bound to an <code>AsyncEngine</code>: model loading, session creation, and stepping go through its async, continuous-batching API; cheap registry reads touch the underlying <code>WorldEngine</code> directly.</p>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.LoadModelRequest","title":"LoadModelRequest","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>class LoadModelRequest(BaseModel):\n    model_id: str\n    alias: str | None = None\n    variant: str | None = None\n    ckpt_path: str | None = None\n    trust_remote_code: bool = False\n    kwargs: dict[str, Any] = Field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.LoadModelRequest.model_id","title":"model_id  <code>instance-attribute</code>","text":"<pre><code>model_id: str\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.LoadModelRequest.alias","title":"alias  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>alias: str | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.LoadModelRequest.variant","title":"variant  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>variant: str | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.LoadModelRequest.ckpt_path","title":"ckpt_path  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ckpt_path: str | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.LoadModelRequest.trust_remote_code","title":"trust_remote_code  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>trust_remote_code: bool = False\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.LoadModelRequest.kwargs","title":"kwargs  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kwargs: dict[str, Any] = Field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest","title":"CreateSessionRequest","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>class CreateSessionRequest(BaseModel):\n    world: str\n    height: int = 480\n    width: int = 848\n    fps: int = 24\n    num_inference_steps: int = 4\n    guidance_scale: float = 1.0\n    frames_per_step: int = 8\n    initial_prompt: str | None = None\n    initial_image: str | None = None\n    seed: int | None = None\n    overrides: dict[str, Any] | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.world","title":"world  <code>instance-attribute</code>","text":"<pre><code>world: str\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.height","title":"height  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>height: int = 480\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.width","title":"width  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>width: int = 848\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.fps","title":"fps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fps: int = 24\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.num_inference_steps","title":"num_inference_steps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_inference_steps: int = 4\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.guidance_scale","title":"guidance_scale  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>guidance_scale: float = 1.0\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.frames_per_step","title":"frames_per_step  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>frames_per_step: int = 8\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.initial_prompt","title":"initial_prompt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_prompt: str | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.initial_image","title":"initial_image  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>initial_image: str | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.seed","title":"seed  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>seed: int | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.CreateSessionRequest.overrides","title":"overrides  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>overrides: dict[str, Any] | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.StepRequest","title":"StepRequest","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>class StepRequest(BaseModel):\n    action_type: str = \"null\"\n    payload: dict[str, Any] = Field(default_factory=dict)\n    modalities: list[str] = Field(default_factory=lambda: [\"frames\"])\n    decode: bool = True\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.StepRequest.action_type","title":"action_type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>action_type: str = 'null'\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.StepRequest.payload","title":"payload  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>payload: dict[str, Any] = Field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.StepRequest.modalities","title":"modalities  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>modalities: list[str] = Field(default_factory=lambda: ['frames'])\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.StepRequest.decode","title":"decode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>decode: bool = True\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest","title":"ClipRequest","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>class ClipRequest(BaseModel):\n    steps: int = 6\n    fps: int = 24\n    format: str = \"gif\"\n    actions: str | None = None\n    action_type: str = \"null\"\n    payload: dict[str, Any] = Field(default_factory=dict)\n    close: bool = True\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest.steps","title":"steps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>steps: int = 6\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest.fps","title":"fps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fps: int = 24\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest.format","title":"format  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>format: str = 'gif'\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest.actions","title":"actions  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>actions: str | None = None\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest.action_type","title":"action_type  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>action_type: str = 'null'\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest.payload","title":"payload  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>payload: dict[str, Any] = Field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.ClipRequest.close","title":"close  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>close: bool = True\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.RestoreRequest","title":"RestoreRequest","text":"<p>               Bases: <code>BaseModel</code></p> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>class RestoreRequest(BaseModel):\n    checkpoint_id: str\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.RestoreRequest.checkpoint_id","title":"checkpoint_id  <code>instance-attribute</code>","text":"<pre><code>checkpoint_id: str\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes.configure_routes","title":"configure_routes","text":"<pre><code>configure_routes(async_engine: 'AsyncEngine', auth_dep: Any = None) -&gt; APIRouter\n</code></pre> <p>Build a <code>/v1</code> router bound to an <code>AsyncEngine</code> and optional auth.</p> <p>The router is created per call so each app owns its own routes.</p> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>def configure_routes(async_engine: \"AsyncEngine\", auth_dep: Any = None) -&gt; APIRouter:\n    r\"\"\"Build a `/v1` router bound to an `AsyncEngine` and optional auth.\n\n    The router is created per call so each app owns its own routes.\n    \"\"\"\n    router = APIRouter(prefix=\"/v1\")\n    engine = async_engine.engine\n    deps = [Depends(auth_dep)] if auth_dep is not None else []\n\n    @router.get(\"/worlds\", tags=[\"worlds\"], dependencies=deps)\n    async def list_worlds() -&gt; dict[str, Any]:\n        return {\"worlds\": engine.list_worlds()}\n\n    @router.get(\"/config\", tags=[\"config\"], dependencies=deps)\n    async def get_config() -&gt; dict[str, Any]:\n        from worldkernels.config.profiles import resolve_runtime_config\n        from worldkernels.config.runtime import ALL_TOGGLE_FIELDS\n\n        _, sources = resolve_runtime_config()\n        cfg = engine.config\n        return {\n            \"config\": {\n                f: {\"value\": getattr(cfg, f), \"source\": sources.get(f, \"default\")}\n                for f in (\"device\", \"max_sessions\", *ALL_TOGGLE_FIELDS)\n            },\n            \"tiers\": engine.list_tiers(),\n        }\n\n    @router.post(\"/worlds\", tags=[\"worlds\"], status_code=201, dependencies=deps)\n    async def load_model(req: LoadModelRequest) -&gt; dict[str, str]:\n        try:\n            await async_engine.load_model(\n                req.model_id,\n                alias=req.alias,\n                variant=req.variant,\n                ckpt_path=req.ckpt_path,\n                trust_remote_code=req.trust_remote_code,\n                **req.kwargs,\n            )\n        except WorldAlreadyLoadedError as exc:\n            raise HTTPException(status_code=409, detail=str(exc))\n        except WorldNotFoundError as exc:\n            raise HTTPException(status_code=404, detail=str(exc))\n        except WorldInitError as exc:\n            raise HTTPException(status_code=500, detail=str(exc))\n        return {\"status\": \"loaded\", \"world\": req.alias or req.model_id.split(\"/\")[-1]}\n\n    @router.post(\"/worlds:stream\", tags=[\"worlds\"], dependencies=deps)\n    async def load_model_stream(req: LoadModelRequest) -&gt; StreamingResponse:\n        r\"\"\"Stream bootstrap progress as SSE events (phase, status, message, fraction).\"\"\"\n        from worldkernels.bootstrap import ProgressController\n\n        queue: asyncio.Queue = asyncio.Queue()\n        loop = asyncio.get_running_loop()\n\n        def sink(event: dict[str, Any] | None) -&gt; None:\n            loop.call_soon_threadsafe(queue.put_nowait, event)\n\n        async def run_load() -&gt; None:\n            try:\n                progress = ProgressController(mode=\"sink\", sink=sink)\n                await asyncio.to_thread(\n                    engine.load_model,\n                    req.model_id,\n                    alias=req.alias,\n                    variant=req.variant,\n                    ckpt_path=req.ckpt_path,\n                    progress=progress,\n                    trust_remote_code=req.trust_remote_code,\n                    **req.kwargs,\n                )\n                sink({\"phase\": \"ready\", \"status\": \"done\", \"message\": req.alias or req.model_id})\n            except Exception as exc:\n                sink({\"phase\": \"failed\", \"status\": \"failed\", \"message\": str(exc)})\n            sink(None)\n\n        async def event_stream():\n            task = asyncio.create_task(run_load())\n            while True:\n                event = await queue.get()\n                if event is None:\n                    break\n                yield f\"data: {json.dumps(event)}\\n\\n\"\n            await task\n\n        return StreamingResponse(event_stream(), media_type=\"text/event-stream\")\n\n    @router.delete(\"/worlds/{world_id}\", tags=[\"worlds\"], dependencies=deps)\n    async def unload_model(world_id: str) -&gt; dict[str, str]:\n        try:\n            await async_engine.unload_model(world_id)\n        except WorldNotFoundError as exc:\n            raise HTTPException(status_code=404, detail=str(exc))\n        return {\"status\": \"unloaded\", \"world\": world_id}\n\n    @router.get(\"/sessions\", tags=[\"sessions\"], dependencies=deps)\n    async def list_sessions() -&gt; dict[str, Any]:\n        summaries = [\n            _session_summary(engine.get_session(sid))\n            for sid in engine.list_sessions()\n            if engine.get_session(sid) is not None\n        ]\n        return {\"sessions\": summaries}\n\n    @router.post(\"/sessions\", tags=[\"sessions\"], status_code=201, dependencies=deps)\n    async def create_session(req: CreateSessionRequest) -&gt; dict[str, Any]:\n        config = WorldConfig(\n            height=req.height,\n            width=req.width,\n            fps=req.fps,\n            num_inference_steps=req.num_inference_steps,\n            guidance_scale=req.guidance_scale,\n            frames_per_step=req.frames_per_step,\n            initial_prompt=req.initial_prompt,\n            initial_image=req.initial_image,\n        )\n        try:\n            sess = await async_engine.create_session(\n                req.world, config=config, seed=req.seed, overrides=req.overrides\n            )\n        except WorldNotFoundError as exc:\n            raise HTTPException(status_code=404, detail=str(exc))\n        except SessionLimitError as exc:\n            raise HTTPException(status_code=429, detail=str(exc))\n        except VRAMExhaustedError as exc:\n            raise HTTPException(status_code=507, detail=str(exc))\n        return _session_summary(sess)\n\n    @router.get(\"/sessions/{session_id}\", tags=[\"sessions\"], dependencies=deps)\n    async def get_session(session_id: str) -&gt; dict[str, Any]:\n        return _session_summary(_require_session(engine, session_id))\n\n    @router.delete(\"/sessions/{session_id}\", tags=[\"sessions\"], dependencies=deps)\n    async def delete_session(session_id: str) -&gt; dict[str, str]:\n        _require_session(engine, session_id)\n        await async_engine.close_session(session_id)\n        return {\"status\": \"terminated\", \"session_id\": session_id}\n\n    @router.post(\"/sessions/{session_id}/step\", tags=[\"sessions\"], dependencies=deps)\n    async def step(session_id: str, req: StepRequest) -&gt; dict[str, Any]:\n        from worldkernels.core.action import Action\n\n        action = Action(action_type=req.action_type, payload=req.payload)\n        try:\n            obs = await async_engine.step(\n                session_id, action, modalities=req.modalities, decode=req.decode\n            )\n        except SessionNotFoundError as exc:\n            raise HTTPException(status_code=404, detail=str(exc))\n        except (SessionTerminatedError, SessionPausedError) as exc:\n            raise HTTPException(status_code=410, detail=str(exc))\n        except RuntimeError as exc:\n            raise HTTPException(status_code=409, detail=str(exc))\n        except WorldKernelError as exc:\n            raise HTTPException(status_code=500, detail=str(exc))\n        return _observation_to_dict(obs, session_id)\n\n    @router.post(\"/sessions/{session_id}/clip\", tags=[\"sessions\"], dependencies=deps)\n    async def clip(session_id: str, req: ClipRequest) -&gt; Response:\n        r\"\"\"Step ``steps`` times and return the frames as an encoded ``gif``/``mp4`` file.\n\n        With ``actions`` (a server-side ``.npy`` path) each step is driven by the next\n        ``[chunk_size, action_dim]`` chunk; otherwise ``action_type``/``payload`` is reused.\n        \"\"\"\n        from worldkernels.core.action import Action, load_action_chunks\n\n        sess = _require_session(engine, session_id)\n        h, w = (sess.config.height, sess.config.width) if sess.config else (480, 848)\n        chunks = load_action_chunks(req.actions, req.steps) if req.actions else None\n        raw: list[bytes] = []\n        for i in range(req.steps):\n            if chunks is not None:\n                action = Action(\"continuous\", {\"joints\": chunks[min(i, len(chunks) - 1)]})\n            else:\n                action = Action(action_type=req.action_type, payload=req.payload)\n            try:\n                obs = await async_engine.step(\n                    session_id, action, modalities=[\"frames\"], decode=True\n                )\n            except SessionNotFoundError as exc:\n                raise HTTPException(status_code=404, detail=str(exc))\n            except (SessionTerminatedError, SessionPausedError) as exc:\n                raise HTTPException(status_code=410, detail=str(exc))\n            except WorldKernelError as exc:\n                raise HTTPException(status_code=500, detail=str(exc))\n            raw.extend(bytes(f) for f in (obs.frames or []) if isinstance(f, (bytes, bytearray)))\n        if not raw:\n            raise HTTPException(\n                status_code=409, detail=\"no frames produced (try decode=true world)\"\n            )\n        data, media = _encode_clip(raw, w, h, req.fps, req.format)\n        if req.close:\n            await async_engine.close_session(session_id)\n        return Response(content=data, media_type=media)\n\n    @router.post(\"/sessions/{session_id}/checkpoint\", tags=[\"sessions\"], dependencies=deps)\n    async def checkpoint(session_id: str) -&gt; dict[str, str]:\n        sess = _require_session(engine, session_id)\n        return {\"checkpoint_id\": sess.checkpoint(), \"session_id\": session_id}\n\n    @router.post(\"/sessions/{session_id}/restore\", tags=[\"sessions\"], dependencies=deps)\n    async def restore(session_id: str, req: RestoreRequest) -&gt; dict[str, str]:\n        sess = _require_session(engine, session_id)\n        try:\n            sess.restore(req.checkpoint_id)\n        except WorldKernelError as exc:\n            raise HTTPException(status_code=404, detail=str(exc))\n        return {\"status\": \"restored\", \"checkpoint_id\": req.checkpoint_id}\n\n    @router.post(\n        \"/sessions/{session_id}/branch\", tags=[\"sessions\"], status_code=201, dependencies=deps\n    )\n    async def branch(session_id: str) -&gt; dict[str, Any]:\n        sess = _require_session(engine, session_id)\n        branched = sess.branch()\n        engine._sessions[branched.session_id] = branched\n        return _session_summary(branched)\n\n    register_websocket_routes(router, async_engine)\n    return router\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes._encode_clip","title":"_encode_clip","text":"<pre><code>_encode_clip(raw: list[bytes], w: int, h: int, fps: int, fmt: str) -&gt; tuple[bytes, str]\n</code></pre> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>def _encode_clip(raw: list[bytes], w: int, h: int, fps: int, fmt: str) -&gt; tuple[bytes, str]:\n    from io import BytesIO\n\n    from PIL import Image\n\n    imgs = [Image.frombytes(\"RGB\", (w, h), bytes(f)) for f in raw]\n    buf = BytesIO()\n    if fmt == \"gif\":\n        imgs[0].save(\n            buf,\n            format=\"GIF\",\n            save_all=True,\n            append_images=imgs[1:],\n            duration=max(1, 1000 // fps),\n            loop=0,\n        )\n        return buf.getvalue(), \"image/gif\"\n    if fmt == \"mp4\":\n        try:\n            import imageio.v3 as iio\n            import numpy as np\n\n            iio.imwrite(buf, np.stack([np.asarray(im) for im in imgs]), extension=\".mp4\", fps=fps)\n        except Exception as exc:\n            raise HTTPException(\n                status_code=501, detail=f\"mp4 encode unavailable ({exc}); use format=gif\"\n            )\n        return buf.getvalue(), \"video/mp4\"\n    raise HTTPException(status_code=400, detail=f\"unknown format {fmt!r}; use gif or mp4\")\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes._require_session","title":"_require_session","text":"<pre><code>_require_session(engine: Any, session_id: str) -&gt; Any\n</code></pre> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>def _require_session(engine: Any, session_id: str) -&gt; Any:\n    sess = engine.get_session(session_id)\n    if sess is None:\n        raise HTTPException(status_code=404, detail=f\"Session '{session_id}' not found.\")\n    return sess\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes._session_summary","title":"_session_summary","text":"<pre><code>_session_summary(sess: Any) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>def _session_summary(sess: Any) -&gt; dict[str, Any]:\n    return {\n        \"session_id\": sess.session_id,\n        \"world_id\": sess.world_id,\n        \"status\": sess.status.value,\n        \"step_index\": sess.step_index,\n        \"seed\": sess.seed,\n        \"parent_session_id\": sess.parent_session_id,\n        \"created_at\": sess.created_at.isoformat(),\n    }\n</code></pre>"},{"location":"api/worldkernels/serving/routes/#worldkernels.serving.routes._observation_to_dict","title":"_observation_to_dict","text":"<pre><code>_observation_to_dict(obs: Any, session_id: str) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/serving/routes.py</code> <pre><code>def _observation_to_dict(obs: Any, session_id: str) -&gt; dict[str, Any]:\n    result: dict[str, Any] = {\n        \"session_id\": session_id,\n        \"step_index\": obs.step_index,\n        \"generation_time_ms\": obs.generation_time_ms,\n        \"decode_skipped\": obs.decode_skipped,\n    }\n    if obs.frames is not None:\n        result[\"frames\"] = [\n            base64.b64encode(f).decode(\"ascii\") if isinstance(f, (bytes, bytearray)) else None\n            for f in obs.frames\n        ]\n        result[\"num_frames\"] = len(obs.frames)\n    if obs.stage_timing is not None:\n        result[\"stage_timing\"] = obs.stage_timing.as_dict()\n    return result\n</code></pre>"},{"location":"api/worldkernels/serving/server/","title":"server","text":""},{"location":"api/worldkernels/serving/server/#worldkernels.serving.server","title":"worldkernels.serving.server","text":"<p>FastAPI application factory for WorldKernels.</p>"},{"location":"api/worldkernels/serving/server/#worldkernels.serving.server.create_app","title":"create_app","text":"<pre><code>create_app(server_config: ServerConfig | None = None, device: str = 'cuda', runtime_config=None) -&gt; FastAPI\n</code></pre> <p>Build the FastAPI application with engine, routes, and metrics wired up.</p> <p>Parameters:</p> Name Type Description Default <code>server_config</code> <code>ServerConfig | None</code> <p>HTTP-layer config (host/port/auth/CORS/max_sessions).</p> <code>None</code> <code>device</code> <code>str</code> <p>Compute device for the engine.</p> <code>'cuda'</code> <code>runtime_config</code> <p>Optional <code>RuntimeConfig</code> (component toggles). <code>None</code> resolves defaults (honoring <code>WK_*</code> env vars).</p> <code>None</code> Source code in <code>worldkernels/serving/server.py</code> <pre><code>def create_app(\n    server_config: ServerConfig | None = None,\n    device: str = \"cuda\",\n    runtime_config=None,\n) -&gt; FastAPI:\n    r\"\"\"Build the FastAPI application with engine, routes, and metrics wired up.\n\n    Args:\n        server_config: HTTP-layer config (host/port/auth/CORS/max_sessions).\n        device: Compute device for the engine.\n        runtime_config: Optional `RuntimeConfig` (component toggles). ``None``\n            resolves defaults (honoring ``WK_*`` env vars).\n    \"\"\"\n    cfg = server_config or ServerConfig()\n\n    app = FastAPI(\n        title=\"WorldKernels\",\n        description=\"GPU-first world model simulation engine\",\n        version=\"0.1.0\",\n        root_path=cfg.root_path,\n    )\n    app.add_middleware(\n        CORSMiddleware,\n        allow_origins=cfg.allowed_origins,\n        allow_methods=cfg.allowed_methods,\n        allow_headers=cfg.allowed_headers,\n        allow_credentials=cfg.allow_credentials,\n    )\n\n    engine = WorldEngine(runtime_config, device=device, max_sessions=cfg.max_sessions)\n    async_engine = AsyncEngine(engine)\n    app.state.engine = engine\n    app.state.async_engine = async_engine\n\n    router = configure_routes(async_engine, auth_dep=require_api_key(cfg.api_key))\n    app.include_router(router)\n\n    @app.get(\"/health\")\n    async def health() -&gt; dict[str, str]:\n        return {\"status\": \"ok\"}\n\n    @app.get(\"/metrics\")\n    async def metrics() -&gt; Response:\n        from worldkernels.runtime import metrics as wk_metrics\n\n        return Response(content=wk_metrics.render(), media_type=wk_metrics.CONTENT_TYPE_LATEST)\n\n    @app.on_event(\"shutdown\")\n    async def _shutdown() -&gt; None:\n        await async_engine.shutdown()\n\n    return app\n</code></pre>"},{"location":"api/worldkernels/serving/websocket/","title":"websocket","text":""},{"location":"api/worldkernels/serving/websocket/#worldkernels.serving.websocket","title":"worldkernels.serving.websocket","text":"<p>WebSocket frame streaming.</p> <p>A client opens <code>/v1/sessions/{id}/stream</code>, then sends one JSON action per message and receives one JSON observation back. Long-lived sessions avoid the per-step HTTP handshake and let the engine pipeline steps.</p>"},{"location":"api/worldkernels/serving/websocket/#worldkernels.serving.websocket.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['register_websocket_routes']\n</code></pre>"},{"location":"api/worldkernels/serving/websocket/#worldkernels.serving.websocket._observation_payload","title":"_observation_payload","text":"<pre><code>_observation_payload(obs: Any) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/serving/websocket.py</code> <pre><code>def _observation_payload(obs: Any) -&gt; dict[str, Any]:\n    payload: dict[str, Any] = {\n        \"step_index\": obs.step_index,\n        \"generation_time_ms\": obs.generation_time_ms,\n        \"decode_skipped\": obs.decode_skipped,\n    }\n    if obs.frames is not None:\n        payload[\"frames\"] = [\n            base64.b64encode(f).decode(\"ascii\") if isinstance(f, (bytes, bytearray)) else None\n            for f in obs.frames\n        ]\n    if obs.stage_timing is not None:\n        payload[\"stage_timing\"] = obs.stage_timing.as_dict()\n    return payload\n</code></pre>"},{"location":"api/worldkernels/serving/websocket/#worldkernels.serving.websocket.register_websocket_routes","title":"register_websocket_routes","text":"<pre><code>register_websocket_routes(router: APIRouter, async_engine: 'AsyncEngine') -&gt; None\n</code></pre> <p>Attach the session-streaming WebSocket route to <code>router</code>.</p> Source code in <code>worldkernels/serving/websocket.py</code> <pre><code>def register_websocket_routes(router: APIRouter, async_engine: \"AsyncEngine\") -&gt; None:\n    r\"\"\"Attach the session-streaming WebSocket route to ``router``.\"\"\"\n\n    @router.websocket(\"/sessions/{session_id}/stream\")\n    async def stream(websocket: WebSocket, session_id: str) -&gt; None:\n        from worldkernels.core.action import Action\n        from worldkernels.core.errors import WorldKernelError\n\n        await websocket.accept()\n        if async_engine.engine.get_session(session_id) is None:\n            await websocket.send_json({\"error\": f\"session {session_id!r} not found\"})\n            await websocket.close()\n            return\n        try:\n            while True:\n                message = await websocket.receive_json()\n                action = Action(\n                    action_type=message.get(\"action_type\", \"null\"),\n                    payload=message.get(\"payload\", {}),\n                )\n                try:\n                    obs = await async_engine.step(\n                        session_id,\n                        action,\n                        modalities=message.get(\"modalities\", [\"frames\"]),\n                        decode=message.get(\"decode\", True),\n                    )\n                except WorldKernelError as exc:\n                    await websocket.send_json({\"error\": str(exc)})\n                    continue\n                await websocket.send_json(_observation_payload(obs))\n        except WebSocketDisconnect:\n            return\n</code></pre>"},{"location":"api/worldkernels/ui/","title":"ui","text":""},{"location":"api/worldkernels/ui/#worldkernels.ui","title":"worldkernels.ui","text":"<p>Shared low-level terminal UI kernel: theme, console, verbosity, components, live displays.</p> <p>Modules:</p> Name Description <code>components</code> <p>Reusable terminal renderables: rules, tables, labeled fields, status lines, banner.</p> <code>console</code> <p>Cached rich Console plus terminal capability detection.</p> <code>events</code> <p>Unified phase-event model shared by the progress UI and SSE streaming.</p> <code>live</code> <p>rich.Live driver over a renderable-provider callable, with mode degradation.</p> <code>logging_setup</code> <p>logging-to-handler pipeline gated to CLI verbosity.</p> <code>progress</code> <p>Phase-keyed and step-keyed progress renderers, gradient bars, sparklines, working status.</p> <code>theme</code> <p>Signature palette: lime brand, per-mode accents, glyphs, and gradient ramps.</p> <code>verbosity</code> <p>Process-global CLI verbosity and output mode.</p>"},{"location":"api/worldkernels/ui/#worldkernels.ui.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['console', 'capabilities', 'rule', 'table', 'print_table', 'field', 'ok', 'err', 'info', 'line', 'status', 'kv_rows', 'metric_spark', 'banner', 'GLYPHS', 'style', 'set_mode', 'get_mode', 'accent', 'ramp', 'sparkline', 'working', 'Verbosity', 'OutputMode', 'get_verbosity', 'set_verbosity', 'get_output_mode', 'set_output_mode', 'resolve_output_mode', 'PhaseEvent', 'PhaseStatus', 'LiveDisplay', 'PhaseProgress', 'StepProgress', 'configure_logging']\n</code></pre>"},{"location":"api/worldkernels/ui/#worldkernels.ui._LAZY","title":"_LAZY  <code>module-attribute</code>","text":"<pre><code>_LAZY: dict[str, tuple[str, str]] = {'console': ('worldkernels.ui.console', 'console'), 'capabilities': ('worldkernels.ui.console', 'capabilities'), 'rule': ('worldkernels.ui.components', 'rule'), 'table': ('worldkernels.ui.components', 'table'), 'print_table': ('worldkernels.ui.components', 'print_table'), 'field': ('worldkernels.ui.components', 'field'), 'ok': ('worldkernels.ui.components', 'ok'), 'err': ('worldkernels.ui.components', 'err'), 'info': ('worldkernels.ui.components', 'info'), 'line': ('worldkernels.ui.components', 'line'), 'status': ('worldkernels.ui.components', 'status'), 'kv_rows': ('worldkernels.ui.components', 'kv_rows'), 'metric_spark': ('worldkernels.ui.components', 'metric_spark'), 'banner': ('worldkernels.ui.components', 'banner'), 'GLYPHS': ('worldkernels.ui.theme', 'GLYPHS'), 'style': ('worldkernels.ui.theme', 'style'), 'set_mode': ('worldkernels.ui.theme', 'set_mode'), 'get_mode': ('worldkernels.ui.theme', 'get_mode'), 'accent': ('worldkernels.ui.theme', 'accent'), 'ramp': ('worldkernels.ui.theme', 'ramp'), 'sparkline': ('worldkernels.ui.progress', 'sparkline'), 'working': ('worldkernels.ui.progress', 'working'), 'Verbosity': ('worldkernels.ui.verbosity', 'Verbosity'), 'OutputMode': ('worldkernels.ui.verbosity', 'OutputMode'), 'get_verbosity': ('worldkernels.ui.verbosity', 'get_verbosity'), 'set_verbosity': ('worldkernels.ui.verbosity', 'set_verbosity'), 'get_output_mode': ('worldkernels.ui.verbosity', 'get_output_mode'), 'set_output_mode': ('worldkernels.ui.verbosity', 'set_output_mode'), 'resolve_output_mode': ('worldkernels.ui.verbosity', 'resolve_output_mode'), 'PhaseEvent': ('worldkernels.ui.events', 'PhaseEvent'), 'PhaseStatus': ('worldkernels.ui.events', 'PhaseStatus'), 'LiveDisplay': ('worldkernels.ui.live', 'LiveDisplay'), 'PhaseProgress': ('worldkernels.ui.progress', 'PhaseProgress'), 'StepProgress': ('worldkernels.ui.progress', 'StepProgress'), 'configure_logging': ('worldkernels.ui.logging_setup', 'configure_logging')}\n</code></pre>"},{"location":"api/worldkernels/ui/#worldkernels.ui.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/ui/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY:\n        module_path, attr = _LAZY[name]\n        val = getattr(_importlib.import_module(module_path), attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/ui/components/","title":"components","text":""},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components","title":"worldkernels.ui.components","text":"<p>Reusable terminal renderables: rules, tables, labeled fields, status lines, banner.</p>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components._BANNER_ROWS","title":"_BANNER_ROWS  <code>module-attribute</code>","text":"<pre><code>_BANNER_ROWS = ('\u2566 \u2566\u2566\u2554\u2550', '\u2551\u2551\u2551\u2560\u2569\u2557', '\u255a\u2569\u255d\u2569 \u2569')\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components._BANNER_HI","title":"_BANNER_HI  <code>module-attribute</code>","text":"<pre><code>_BANNER_HI = '#ECFCCB bold'\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.rule","title":"rule","text":"<pre><code>rule(title: str = '', style: str = 'rule') -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def rule(title: str = \"\", style: str = \"rule\") -&gt; None:\n    from rich.rule import Rule\n    from rich.text import Text\n\n    from worldkernels.ui.theme import style as theme_style\n\n    con = console()\n    line_style = theme_style(style)\n    if title:\n        con.print(\n            Rule(\n                Text(title, style=theme_style(\"heading\")),\n                characters=\"\u2500\",\n                style=line_style,\n                align=\"left\",\n            )\n        )\n    else:\n        con.print(Rule(characters=\"\u2500\", style=line_style))\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.table","title":"table","text":"<pre><code>table(*columns: str, title: str | None = None) -&gt; Any\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def table(*columns: str, title: str | None = None) -&gt; Any:\n    from rich.table import Table\n    from rich.text import Text\n\n    from worldkernels.ui.theme import style as theme_style\n\n    head = theme_style(\"heading\")\n    t = Table(\n        title=Text(title, style=head) if title else None,\n        box=None,\n        show_header=bool(columns),\n        header_style=head,\n        title_justify=\"left\",\n        pad_edge=False,\n        padding=(0, 3, 0, 0),\n    )\n    for col in columns:\n        t.add_column(col, overflow=\"fold\")\n    return t\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.print_table","title":"print_table","text":"<pre><code>print_table(t: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def print_table(t: Any) -&gt; None:\n    console().print(t)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.field","title":"field","text":"<pre><code>field(label: str, value: Any, label_style: str | None = None, value_style: str | None = None) -&gt; None\n</code></pre> <p>Render a markup-safe <code>label: value</code> line with a colored label.</p> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def field(\n    label: str,\n    value: Any,\n    label_style: str | None = None,\n    value_style: str | None = None,\n) -&gt; None:\n    r\"\"\"Render a markup-safe ``label: value`` line with a colored label.\"\"\"\n    from rich.text import Text\n\n    from worldkernels.ui.theme import style as theme_style\n\n    t = Text()\n    t.append(f\"{label}: \", style=label_style or theme_style(\"key\"))\n    t.append(str(value), style=value_style)\n    console().print(t)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.ok","title":"ok","text":"<pre><code>ok(msg: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def ok(msg: str) -&gt; None:\n    _glyph_line(\"done\", msg)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.err","title":"err","text":"<pre><code>err(msg: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def err(msg: str) -&gt; None:\n    _glyph_line(\"failed\", msg)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.info","title":"info","text":"<pre><code>info(msg: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def info(msg: str) -&gt; None:\n    _glyph_line(\"skipped\", msg)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.line","title":"line","text":"<pre><code>line(text: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def line(text: str) -&gt; None:\n    console().print(text)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components._glyph_line","title":"_glyph_line","text":"<pre><code>_glyph_line(kind: str, msg: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def _glyph_line(kind: str, msg: str) -&gt; None:\n    from rich.text import Text\n\n    from worldkernels.ui.theme import GLYPHS, style as theme_style\n\n    t = Text()\n    t.append(f\"{GLYPHS[kind]} \", style=theme_style(kind))\n    t.append(msg)\n    console().print(t)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.status","title":"status","text":"<pre><code>status(msg: str) -&gt; Iterator[None]\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>@contextmanager\ndef status(msg: str) -&gt; Iterator[None]:\n    from worldkernels.ui.theme import style as theme_style\n\n    con = console()\n    if con.is_terminal and not is_quiet_env():\n        with con.status(msg, spinner=\"dots\", spinner_style=theme_style(\"accent\")):\n            yield\n    else:\n        yield\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.metric_spark","title":"metric_spark","text":"<pre><code>metric_spark(label: str, values: Iterable[float], width: int = 40) -&gt; None\n</code></pre> <p>Print <code>label: &lt;ramp-tinted sparkline&gt;</code> for a series of values.</p> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def metric_spark(label: str, values: Iterable[float], width: int = 40) -&gt; None:\n    r\"\"\"Print ``label: &lt;ramp-tinted sparkline&gt;`` for a series of values.\"\"\"\n    from rich.text import Text\n\n    from worldkernels.ui.progress import sparkline\n    from worldkernels.ui.theme import style as theme_style\n\n    vals = list(values)\n    if not vals:\n        return\n    t = Text()\n    t.append(f\"{label}: \", style=theme_style(\"key\"))\n    t.append_text(sparkline(vals, width))\n    console().print(t)\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.kv_rows","title":"kv_rows","text":"<pre><code>kv_rows(t: Any, pairs: Iterable[tuple[str, Any]], value_style: str | None = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def kv_rows(t: Any, pairs: Iterable[tuple[str, Any]], value_style: str | None = None) -&gt; None:\n    from rich.text import Text\n\n    from worldkernels.ui.theme import style as theme_style\n\n    key_style = theme_style(\"key\")\n    for k, v in pairs:\n        t.add_row(Text(str(k), style=key_style), Text(str(v), style=value_style or \"\"))\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components._banner_text","title":"_banner_text","text":"<pre><code>_banner_text(ver: str, sweep: int | None) -&gt; Any\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def _banner_text(ver: str, sweep: int | None) -&gt; Any:\n    from rich.text import Text\n\n    from worldkernels.ui.theme import BANNER_RAMP\n\n    logo = Text()\n    for r, (row, base) in enumerate(zip(_BANNER_ROWS, BANNER_RAMP)):\n        for c, ch in enumerate(row):\n            logo.append(ch, style=_BANNER_HI if sweep == c else base)\n        if r == 1:\n            logo.append(\"   worldkernels\", style=\"bold\")\n            logo.append(f\"  v{ver}\" if ver else \"\", style=\"dim\")\n        elif r == 2:\n            logo.append(\"   GPU-first world model simulation engine\", style=\"dim\")\n        logo.append(\"\\n\")\n    return logo\n</code></pre>"},{"location":"api/worldkernels/ui/components/#worldkernels.ui.components.banner","title":"banner","text":"<pre><code>banner() -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/components.py</code> <pre><code>def banner() -&gt; None:\n    con = console()\n    if not con.is_terminal:\n        return\n    import time\n\n    from rich.live import Live\n\n    try:\n        from worldkernels import __version__ as ver\n    except Exception:\n        ver = \"\"\n\n    with Live(_banner_text(ver, None), console=con, refresh_per_second=30) as live:\n        for s in range(len(_BANNER_ROWS[0]) + 2):\n            live.update(_banner_text(ver, s if s &lt; len(_BANNER_ROWS[0]) else None))\n            time.sleep(0.045)\n        live.update(_banner_text(ver, None))\n</code></pre>"},{"location":"api/worldkernels/ui/console/","title":"console","text":""},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console","title":"worldkernels.ui.console","text":"<p>Cached rich Console plus terminal capability detection.</p>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console._CONSOLE","title":"_CONSOLE  <code>module-attribute</code>","text":"<pre><code>_CONSOLE: Any = None\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.Capabilities","title":"Capabilities  <code>dataclass</code>","text":"Source code in <code>worldkernels/ui/console.py</code> <pre><code>@dataclass(frozen=True)\nclass Capabilities:\n    is_terminal: bool\n    no_color: bool\n    width: int\n    is_ci: bool\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.Capabilities.is_terminal","title":"is_terminal  <code>instance-attribute</code>","text":"<pre><code>is_terminal: bool\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.Capabilities.no_color","title":"no_color  <code>instance-attribute</code>","text":"<pre><code>no_color: bool\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.Capabilities.width","title":"width  <code>instance-attribute</code>","text":"<pre><code>width: int\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.Capabilities.is_ci","title":"is_ci  <code>instance-attribute</code>","text":"<pre><code>is_ci: bool\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.Capabilities.__init__","title":"__init__","text":"<pre><code>__init__(is_terminal: bool, no_color: bool, width: int, is_ci: bool) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.console","title":"console","text":"<pre><code>console() -&gt; Any\n</code></pre> Source code in <code>worldkernels/ui/console.py</code> <pre><code>def console() -&gt; Any:\n    global _CONSOLE\n    if _CONSOLE is None:\n        from rich.console import Console\n\n        _CONSOLE = Console()\n    return _CONSOLE\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.capabilities","title":"capabilities","text":"<pre><code>capabilities() -&gt; Capabilities\n</code></pre> Source code in <code>worldkernels/ui/console.py</code> <pre><code>def capabilities() -&gt; Capabilities:\n    con = console()\n    return Capabilities(\n        is_terminal=bool(con.is_terminal),\n        no_color=bool(os.environ.get(\"NO_COLOR\")),\n        width=con.width,\n        is_ci=bool(os.environ.get(\"CI\")),\n    )\n</code></pre>"},{"location":"api/worldkernels/ui/console/#worldkernels.ui.console.is_quiet_env","title":"is_quiet_env","text":"<pre><code>is_quiet_env() -&gt; bool\n</code></pre> Source code in <code>worldkernels/ui/console.py</code> <pre><code>def is_quiet_env() -&gt; bool:\n    return bool(os.environ.get(\"WORLDKERNELS_QUIET\"))\n</code></pre>"},{"location":"api/worldkernels/ui/events/","title":"events","text":""},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events","title":"worldkernels.ui.events","text":"<p>Unified phase-event model shared by the progress UI and SSE streaming.</p>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseStatus","title":"PhaseStatus  <code>module-attribute</code>","text":"<pre><code>PhaseStatus = Literal['pending', 'running', 'done', 'skipped', 'failed']\n</code></pre>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseEvent","title":"PhaseEvent  <code>dataclass</code>","text":"Source code in <code>worldkernels/ui/events.py</code> <pre><code>@dataclass(frozen=True)\nclass PhaseEvent:\n    phase: str\n    status: PhaseStatus\n    message: str = \"\"\n    fraction: float | None = None\n\n    def as_dict(self) -&gt; dict[str, Any]:\n        return {\n            \"phase\": self.phase,\n            \"status\": self.status,\n            \"message\": self.message,\n            \"fraction\": self.fraction,\n        }\n</code></pre>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseEvent.phase","title":"phase  <code>instance-attribute</code>","text":"<pre><code>phase: str\n</code></pre>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseEvent.status","title":"status  <code>instance-attribute</code>","text":"<pre><code>status: PhaseStatus\n</code></pre>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseEvent.message","title":"message  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>message: str = ''\n</code></pre>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseEvent.fraction","title":"fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fraction: float | None = None\n</code></pre>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseEvent.__init__","title":"__init__","text":"<pre><code>__init__(phase: str, status: PhaseStatus, message: str = '', fraction: float | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/ui/events/#worldkernels.ui.events.PhaseEvent.as_dict","title":"as_dict","text":"<pre><code>as_dict() -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/ui/events.py</code> <pre><code>def as_dict(self) -&gt; dict[str, Any]:\n    return {\n        \"phase\": self.phase,\n        \"status\": self.status,\n        \"message\": self.message,\n        \"fraction\": self.fraction,\n    }\n</code></pre>"},{"location":"api/worldkernels/ui/live/","title":"live","text":""},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live","title":"worldkernels.ui.live","text":"<p>rich.Live driver over a renderable-provider callable, with mode degradation.</p>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay","title":"LiveDisplay","text":"<p>Drive in-place updates from a <code>renderable()</code> provider.</p> <p>In AUTO (tty) mode it wraps <code>rich.Live</code>; in PLAIN/JSON/QUIET it stays inert and the caller emits its own line/json/nothing. Single live renderer, no duplication.</p> Source code in <code>worldkernels/ui/live.py</code> <pre><code>class LiveDisplay:\n    r\"\"\"Drive in-place updates from a ``renderable()`` provider.\n\n    In AUTO (tty) mode it wraps ``rich.Live``; in PLAIN/JSON/QUIET it stays inert and\n    the caller emits its own line/json/nothing. Single live renderer, no duplication.\n    \"\"\"\n\n    def __init__(\n        self,\n        renderable: Callable[[], Any],\n        mode: OutputMode,\n        refresh_per_second: int = 10,\n        transient: bool = False,\n    ) -&gt; None:\n        self._renderable = renderable\n        self._mode = mode\n        self._refresh = refresh_per_second\n        self._transient = transient\n        self._live: Any = None\n\n    @property\n    def active(self) -&gt; bool:\n        return self._live is not None\n\n    def __enter__(self) -&gt; \"LiveDisplay\":\n        if self._mode == OutputMode.AUTO:\n            try:\n                from rich.live import Live\n\n                from worldkernels.ui.console import console\n\n                self._live = Live(\n                    self._renderable(),\n                    console=console(),\n                    refresh_per_second=self._refresh,\n                    transient=self._transient,\n                )\n                self._live.__enter__()\n            except ImportError:\n                self._live = None\n        return self\n\n    def update(self) -&gt; None:\n        if self._live is not None:\n            self._live.update(self._renderable())\n\n    def __exit__(self, exc_type: Any, exc: Any, tb: Any) -&gt; None:\n        if self._live is not None:\n            self._live.update(self._renderable(), refresh=True)\n            self._live.__exit__(exc_type, exc, tb)\n            self._live = None\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay._renderable","title":"_renderable  <code>instance-attribute</code>","text":"<pre><code>_renderable = renderable\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay._mode","title":"_mode  <code>instance-attribute</code>","text":"<pre><code>_mode = mode\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay._refresh","title":"_refresh  <code>instance-attribute</code>","text":"<pre><code>_refresh = refresh_per_second\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay._transient","title":"_transient  <code>instance-attribute</code>","text":"<pre><code>_transient = transient\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay._live","title":"_live  <code>instance-attribute</code>","text":"<pre><code>_live: Any = None\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay.active","title":"active  <code>property</code>","text":"<pre><code>active: bool\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay.__init__","title":"__init__","text":"<pre><code>__init__(renderable: Callable[[], Any], mode: OutputMode, refresh_per_second: int = 10, transient: bool = False) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/live.py</code> <pre><code>def __init__(\n    self,\n    renderable: Callable[[], Any],\n    mode: OutputMode,\n    refresh_per_second: int = 10,\n    transient: bool = False,\n) -&gt; None:\n    self._renderable = renderable\n    self._mode = mode\n    self._refresh = refresh_per_second\n    self._transient = transient\n    self._live: Any = None\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay.__enter__","title":"__enter__","text":"<pre><code>__enter__() -&gt; 'LiveDisplay'\n</code></pre> Source code in <code>worldkernels/ui/live.py</code> <pre><code>def __enter__(self) -&gt; \"LiveDisplay\":\n    if self._mode == OutputMode.AUTO:\n        try:\n            from rich.live import Live\n\n            from worldkernels.ui.console import console\n\n            self._live = Live(\n                self._renderable(),\n                console=console(),\n                refresh_per_second=self._refresh,\n                transient=self._transient,\n            )\n            self._live.__enter__()\n        except ImportError:\n            self._live = None\n    return self\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay.update","title":"update","text":"<pre><code>update() -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/live.py</code> <pre><code>def update(self) -&gt; None:\n    if self._live is not None:\n        self._live.update(self._renderable())\n</code></pre>"},{"location":"api/worldkernels/ui/live/#worldkernels.ui.live.LiveDisplay.__exit__","title":"__exit__","text":"<pre><code>__exit__(exc_type: Any, exc: Any, tb: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/live.py</code> <pre><code>def __exit__(self, exc_type: Any, exc: Any, tb: Any) -&gt; None:\n    if self._live is not None:\n        self._live.update(self._renderable(), refresh=True)\n        self._live.__exit__(exc_type, exc, tb)\n        self._live = None\n</code></pre>"},{"location":"api/worldkernels/ui/logging_setup/","title":"logging_setup","text":""},{"location":"api/worldkernels/ui/logging_setup/#worldkernels.ui.logging_setup","title":"worldkernels.ui.logging_setup","text":"<p>logging-to-handler pipeline gated to CLI verbosity.</p>"},{"location":"api/worldkernels/ui/logging_setup/#worldkernels.ui.logging_setup._LEVELS","title":"_LEVELS  <code>module-attribute</code>","text":"<pre><code>_LEVELS = {Verbosity.QUIET: logging.ERROR, Verbosity.NORMAL: logging.WARNING, Verbosity.VERBOSE: logging.INFO, Verbosity.DEBUG: logging.DEBUG}\n</code></pre>"},{"location":"api/worldkernels/ui/logging_setup/#worldkernels.ui.logging_setup._PLAIN_FORMAT","title":"_PLAIN_FORMAT  <code>module-attribute</code>","text":"<pre><code>_PLAIN_FORMAT = '%(levelname)s %(asctime)s [%(filename)s:%(lineno)d] %(message)s'\n</code></pre>"},{"location":"api/worldkernels/ui/logging_setup/#worldkernels.ui.logging_setup._PLAIN_DATEFMT","title":"_PLAIN_DATEFMT  <code>module-attribute</code>","text":"<pre><code>_PLAIN_DATEFMT = '%m-%d %H:%M:%S'\n</code></pre>"},{"location":"api/worldkernels/ui/logging_setup/#worldkernels.ui.logging_setup.configure_logging","title":"configure_logging","text":"<pre><code>configure_logging(verbosity: Verbosity) -&gt; None\n</code></pre> <p>Install a single handler on the <code>worldkernels</code> logger at the mapped level.</p> <p>TTY sessions get a RichHandler; plain/json/quiet output modes get a compact line format (<code>INFO 07-12 10:00:00 [serve.py:42] msg</code>).</p> Source code in <code>worldkernels/ui/logging_setup.py</code> <pre><code>def configure_logging(verbosity: Verbosity) -&gt; None:\n    r\"\"\"Install a single handler on the ``worldkernels`` logger at the mapped level.\n\n    TTY sessions get a RichHandler; plain/json/quiet output modes get a\n    compact line format (``INFO 07-12 10:00:00 [serve.py:42] msg``).\n    \"\"\"\n    from worldkernels.ui.verbosity import OutputMode, resolve_output_mode\n\n    level = _LEVELS[verbosity]\n    root = logging.getLogger(\"worldkernels\")\n    root.handlers.clear()\n    handler: logging.Handler\n    if resolve_output_mode() == OutputMode.AUTO:\n        from rich.logging import RichHandler\n\n        from worldkernels.ui.console import console\n\n        handler = RichHandler(\n            console=console(),\n            show_path=False,\n            rich_tracebacks=True,\n            markup=False,\n            show_time=verbosity &gt;= Verbosity.DEBUG,\n        )\n    else:\n        handler = logging.StreamHandler()\n        handler.setFormatter(logging.Formatter(_PLAIN_FORMAT, datefmt=_PLAIN_DATEFMT))\n    handler.setLevel(level)\n    root.addHandler(handler)\n    root.setLevel(level)\n    root.propagate = False\n</code></pre>"},{"location":"api/worldkernels/ui/progress/","title":"progress","text":""},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress","title":"worldkernels.ui.progress","text":"<p>Phase-keyed and step-keyed progress renderers, gradient bars, sparklines, working status.</p>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PHASES","title":"PHASES  <code>module-attribute</code>","text":"<pre><code>PHASES: tuple[str, ...] = ('resolve', 'deps', 'packages', 'weights', 'init')\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._LABELS","title":"_LABELS  <code>module-attribute</code>","text":"<pre><code>_LABELS: dict[str, str] = {'resolve': 'resolving', 'deps': 'runtime deps', 'packages': 'model package', 'weights': 'weights', 'init': 'initializing'}\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._SPARK","title":"_SPARK  <code>module-attribute</code>","text":"<pre><code>_SPARK = '\u2581\u2582\u2583\u2584\u2585\u2586\u2587\u2588'\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._SPIN","title":"_SPIN  <code>module-attribute</code>","text":"<pre><code>_SPIN = '\u280b\u2819\u2839\u2838\u283c\u2834\u2826\u2827\u2807\u280f'\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._VERBS","title":"_VERBS  <code>module-attribute</code>","text":"<pre><code>_VERBS = ('Working', 'Crunching', 'Churning', 'Cooking')\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseState","title":"PhaseState  <code>dataclass</code>","text":"Source code in <code>worldkernels/ui/progress.py</code> <pre><code>@dataclass\nclass PhaseState:\n    name: str\n    status: PhaseStatus = \"pending\"\n    message: str = \"\"\n    fraction: float | None = None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseState.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseState.status","title":"status  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>status: PhaseStatus = 'pending'\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseState.message","title":"message  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>message: str = ''\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseState.fraction","title":"fraction  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>fraction: float | None = None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseState.__init__","title":"__init__","text":"<pre><code>__init__(name: str, status: PhaseStatus = 'pending', message: str = '', fraction: float | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseProgress","title":"PhaseProgress","text":"<p>Five-phase model-load renderer holding phase state and the rich Table.</p> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>class PhaseProgress:\n    r\"\"\"Five-phase model-load renderer holding phase state and the rich Table.\"\"\"\n\n    def __init__(self, phases: tuple[str, ...] = PHASES) -&gt; None:\n        self.states = {p: PhaseState(p) for p in phases}\n\n    def set(\n        self, phase: str, status: PhaseStatus, message: str = \"\", fraction: float | None = None\n    ) -&gt; bool:\n        if phase not in self.states:\n            return False\n        ps = self.states[phase]\n        ps.status, ps.message, ps.fraction = status, message, fraction\n        return True\n\n    def render(self) -&gt; Any:\n        from rich.table import Table\n        from rich.text import Text\n\n        t = Table(show_header=False, show_edge=False, box=None, padding=(0, 1, 0, 0))\n        t.add_column(width=2, justify=\"center\")\n        t.add_column(width=15)\n        t.add_column()\n        for name, ps in self.states.items():\n            label = Text(_LABELS.get(name, name), style=\"bold\" if ps.status == \"running\" else \"dim\")\n            body = ps.message\n            if ps.fraction is not None:\n                body = f\"{ps.message}  {bar(ps.fraction)} {int(ps.fraction * 100):3d}%\"\n            t.add_row(_glyph_text(ps.status), label, Text(body))\n        return t\n\n    def plain_line(self, phase: str, status: PhaseStatus, message: str) -&gt; str | None:\n        from worldkernels.ui.theme import GLYPHS\n\n        if status == \"pending\":\n            return None\n        g = GLYPHS.get(status)\n        if g is None:\n            return None\n        return f\"{g} {_LABELS.get(phase, phase):&lt;15s} {message}\"\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseProgress.states","title":"states  <code>instance-attribute</code>","text":"<pre><code>states = {p: (PhaseState(p)) for p in phases}\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseProgress.__init__","title":"__init__","text":"<pre><code>__init__(phases: tuple[str, ...] = PHASES) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def __init__(self, phases: tuple[str, ...] = PHASES) -&gt; None:\n    self.states = {p: PhaseState(p) for p in phases}\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseProgress.set","title":"set","text":"<pre><code>set(phase: str, status: PhaseStatus, message: str = '', fraction: float | None = None) -&gt; bool\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def set(\n    self, phase: str, status: PhaseStatus, message: str = \"\", fraction: float | None = None\n) -&gt; bool:\n    if phase not in self.states:\n        return False\n    ps = self.states[phase]\n    ps.status, ps.message, ps.fraction = status, message, fraction\n    return True\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseProgress.render","title":"render","text":"<pre><code>render() -&gt; Any\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def render(self) -&gt; Any:\n    from rich.table import Table\n    from rich.text import Text\n\n    t = Table(show_header=False, show_edge=False, box=None, padding=(0, 1, 0, 0))\n    t.add_column(width=2, justify=\"center\")\n    t.add_column(width=15)\n    t.add_column()\n    for name, ps in self.states.items():\n        label = Text(_LABELS.get(name, name), style=\"bold\" if ps.status == \"running\" else \"dim\")\n        body = ps.message\n        if ps.fraction is not None:\n            body = f\"{ps.message}  {bar(ps.fraction)} {int(ps.fraction * 100):3d}%\"\n        t.add_row(_glyph_text(ps.status), label, Text(body))\n    return t\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.PhaseProgress.plain_line","title":"plain_line","text":"<pre><code>plain_line(phase: str, status: PhaseStatus, message: str) -&gt; str | None\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def plain_line(self, phase: str, status: PhaseStatus, message: str) -&gt; str | None:\n    from worldkernels.ui.theme import GLYPHS\n\n    if status == \"pending\":\n        return None\n    g = GLYPHS.get(status)\n    if g is None:\n        return None\n    return f\"{g} {_LABELS.get(phase, phase):&lt;15s} {message}\"\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress","title":"StepProgress","text":"<p>Iteration progress for bench/run: gradient bar + live stat column (mode-accented).</p> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>class StepProgress:\n    r\"\"\"Iteration progress for bench/run: gradient bar + live stat column (mode-accented).\"\"\"\n\n    def __init__(self, total: int, label: str, mode: OutputMode) -&gt; None:\n        self.total = total\n        self.label = label\n        self.mode = mode\n        self._progress: Any = None\n        self._task: Any = None\n\n    def __enter__(self) -&gt; \"StepProgress\":\n        if self.mode == OutputMode.AUTO:\n            from rich.progress import Progress, SpinnerColumn, TextColumn, TimeElapsedColumn\n\n            from worldkernels.ui.console import console\n            from worldkernels.ui.theme import accent\n\n            acc = accent()\n            self._progress = Progress(\n                SpinnerColumn(style=acc),\n                TextColumn(\"{task.description}\", style=acc),\n                _gradient_bar_column(),\n                TextColumn(\"{task.completed}/{task.total}\", style=\"dim\"),\n                TextColumn(\"{task.fields[stat]}\", style=\"dim\"),\n                TimeElapsedColumn(),\n                console=console(),\n                transient=True,\n            )\n            self._progress.__enter__()\n            self._task = self._progress.add_task(self.label, total=self.total, stat=\"\")\n        return self\n\n    def advance(self, stat: str = \"\") -&gt; None:\n        if self._progress is not None:\n            self._progress.update(self._task, advance=1, stat=stat)\n\n    def __exit__(self, exc_type: Any, exc: Any, tb: Any) -&gt; None:\n        if self._progress is not None:\n            self._progress.__exit__(exc_type, exc, tb)\n            self._progress = None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress.total","title":"total  <code>instance-attribute</code>","text":"<pre><code>total = total\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress.label","title":"label  <code>instance-attribute</code>","text":"<pre><code>label = label\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress.mode","title":"mode  <code>instance-attribute</code>","text":"<pre><code>mode = mode\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress._progress","title":"_progress  <code>instance-attribute</code>","text":"<pre><code>_progress: Any = None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress._task","title":"_task  <code>instance-attribute</code>","text":"<pre><code>_task: Any = None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress.__init__","title":"__init__","text":"<pre><code>__init__(total: int, label: str, mode: OutputMode) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def __init__(self, total: int, label: str, mode: OutputMode) -&gt; None:\n    self.total = total\n    self.label = label\n    self.mode = mode\n    self._progress: Any = None\n    self._task: Any = None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress.__enter__","title":"__enter__","text":"<pre><code>__enter__() -&gt; 'StepProgress'\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def __enter__(self) -&gt; \"StepProgress\":\n    if self.mode == OutputMode.AUTO:\n        from rich.progress import Progress, SpinnerColumn, TextColumn, TimeElapsedColumn\n\n        from worldkernels.ui.console import console\n        from worldkernels.ui.theme import accent\n\n        acc = accent()\n        self._progress = Progress(\n            SpinnerColumn(style=acc),\n            TextColumn(\"{task.description}\", style=acc),\n            _gradient_bar_column(),\n            TextColumn(\"{task.completed}/{task.total}\", style=\"dim\"),\n            TextColumn(\"{task.fields[stat]}\", style=\"dim\"),\n            TimeElapsedColumn(),\n            console=console(),\n            transient=True,\n        )\n        self._progress.__enter__()\n        self._task = self._progress.add_task(self.label, total=self.total, stat=\"\")\n    return self\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress.advance","title":"advance","text":"<pre><code>advance(stat: str = '') -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def advance(self, stat: str = \"\") -&gt; None:\n    if self._progress is not None:\n        self._progress.update(self._task, advance=1, stat=stat)\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.StepProgress.__exit__","title":"__exit__","text":"<pre><code>__exit__(exc_type: Any, exc: Any, tb: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def __exit__(self, exc_type: Any, exc: Any, tb: Any) -&gt; None:\n    if self._progress is not None:\n        self._progress.__exit__(exc_type, exc, tb)\n        self._progress = None\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._Working","title":"_Working","text":"Source code in <code>worldkernels/ui/progress.py</code> <pre><code>class _Working:\n    def __init__(self, label: str, verbs: Sequence[str], counter: Callable[[], str] | None) -&gt; None:\n        self.label = label\n        self.verbs = list(verbs) or [label]\n        self.counter = counter\n        self._t0 = time.monotonic()\n\n    def __rich_console__(self, console: Any, options: Any) -&gt; Any:\n        from rich.text import Text\n\n        from worldkernels.ui.theme import accent\n\n        el = time.monotonic() - self._t0\n        frame = _SPIN[int(el * 10) % len(_SPIN)]\n        verb = self.verbs[int(el / 2) % len(self.verbs)]\n        t = Text()\n        t.append(f\"{frame} \", style=accent())\n        t.append(f\"{verb}\u2026 \", style=f\"{accent()} bold\")\n        t.append(f\"\u00b7 {self.counter() if self.counter else f'{el:4.1f}s'}\", style=\"dim\")\n        yield t\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._Working.label","title":"label  <code>instance-attribute</code>","text":"<pre><code>label = label\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._Working.verbs","title":"verbs  <code>instance-attribute</code>","text":"<pre><code>verbs = list(verbs) or [label]\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._Working.counter","title":"counter  <code>instance-attribute</code>","text":"<pre><code>counter = counter\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._Working._t0","title":"_t0  <code>instance-attribute</code>","text":"<pre><code>_t0 = time.monotonic()\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._Working.__init__","title":"__init__","text":"<pre><code>__init__(label: str, verbs: Sequence[str], counter: Callable[[], str] | None) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def __init__(self, label: str, verbs: Sequence[str], counter: Callable[[], str] | None) -&gt; None:\n    self.label = label\n    self.verbs = list(verbs) or [label]\n    self.counter = counter\n    self._t0 = time.monotonic()\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._Working.__rich_console__","title":"__rich_console__","text":"<pre><code>__rich_console__(console: Any, options: Any) -&gt; Any\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def __rich_console__(self, console: Any, options: Any) -&gt; Any:\n    from rich.text import Text\n\n    from worldkernels.ui.theme import accent\n\n    el = time.monotonic() - self._t0\n    frame = _SPIN[int(el * 10) % len(_SPIN)]\n    verb = self.verbs[int(el / 2) % len(self.verbs)]\n    t = Text()\n    t.append(f\"{frame} \", style=accent())\n    t.append(f\"{verb}\u2026 \", style=f\"{accent()} bold\")\n    t.append(f\"\u00b7 {self.counter() if self.counter else f'{el:4.1f}s'}\", style=\"dim\")\n    yield t\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.bar","title":"bar","text":"<pre><code>bar(frac: float, width: int = 16) -&gt; str\n</code></pre> <p>Glyph progress bar.</p> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def bar(frac: float, width: int = 16) -&gt; str:\n    r\"\"\"Glyph progress bar.\"\"\"\n    frac = max(0.0, min(1.0, frac))\n    filled = int(frac * width)\n    return \"\u2588\" * filled + \"\u2591\" * (width - filled)\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._hex_to_rgb","title":"_hex_to_rgb","text":"<pre><code>_hex_to_rgb(h: str) -&gt; tuple[int, int, int]\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def _hex_to_rgb(h: str) -&gt; tuple[int, int, int]:\n    h = h.lstrip(\"#\")\n    return int(h[0:2], 16), int(h[2:4], 16), int(h[4:6], 16)\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._ramp_at","title":"_ramp_at","text":"<pre><code>_ramp_at(stops: tuple[str, str, str], t: float) -&gt; str\n</code></pre> <p>Interpolate a 3-stop hex ramp at <code>t</code> in [0, 1].</p> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def _ramp_at(stops: tuple[str, str, str], t: float) -&gt; str:\n    r\"\"\"Interpolate a 3-stop hex ramp at ``t`` in [0, 1].\"\"\"\n    t = max(0.0, min(1.0, t))\n    if t &lt;= 0.5:\n        a, b, tt = stops[0], stops[1], t * 2\n    else:\n        a, b, tt = stops[1], stops[2], (t - 0.5) * 2\n    ar, ag, ab = _hex_to_rgb(a)\n    br, bg, bb = _hex_to_rgb(b)\n    r = round(ar + (br - ar) * tt)\n    g = round(ag + (bg - ag) * tt)\n    bl = round(ab + (bb - ab) * tt)\n    return f\"#{r:02X}{g:02X}{bl:02X}\"\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.sparkline","title":"sparkline","text":"<pre><code>sparkline(values: Sequence[float], width: int = 0) -&gt; Any\n</code></pre> <p>Ramp-tinted <code>\u2581\u2582\u2583\u2585\u2587</code> sparkline of the last <code>width</code> values (rich Text).</p> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def sparkline(values: Sequence[float], width: int = 0) -&gt; Any:\n    r\"\"\"Ramp-tinted ``\u2581\u2582\u2583\u2585\u2587`` sparkline of the last ``width`` values (rich Text).\"\"\"\n    from rich.text import Text\n\n    from worldkernels.ui.theme import ramp\n\n    vals = list(values)\n    if width and len(vals) &gt; width:\n        vals = vals[-width:]\n    t = Text()\n    if not vals:\n        return t\n    lo, hi = min(vals), max(vals)\n    span = (hi - lo) or 1.0\n    stops = ramp()\n    n = len(vals)\n    for i, v in enumerate(vals):\n        level = int((v - lo) / span * (len(_SPARK) - 1))\n        t.append(_SPARK[level], style=_ramp_at(stops, i / max(1, n - 1)))\n    return t\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._glyph_text","title":"_glyph_text","text":"<pre><code>_glyph_text(status: PhaseStatus) -&gt; Any\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def _glyph_text(status: PhaseStatus) -&gt; Any:\n    from rich.text import Text\n\n    from worldkernels.ui.theme import GLYPHS, style\n\n    return Text(GLYPHS.get(status, \"\u00b7\"), style=style(status))\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress._gradient_bar_column","title":"_gradient_bar_column","text":"<pre><code>_gradient_bar_column(width: int = 28) -&gt; Any\n</code></pre> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>def _gradient_bar_column(width: int = 28) -&gt; Any:\n    from rich.progress import ProgressColumn\n    from rich.text import Text\n\n    from worldkernels.ui.theme import ramp\n\n    class _GradientBar(ProgressColumn):\n        def render(self, task: Any) -&gt; Any:\n            total = task.total or 0\n            frac = (task.completed / total) if total else 0.0\n            filled = int(frac * width)\n            stops = ramp()\n            txt = Text()\n            for i in range(width):\n                if i &lt; filled:\n                    txt.append(\"\u2501\", style=_ramp_at(stops, i / max(1, width - 1)))\n                else:\n                    txt.append(\"\u254c\", style=\"bright_black\")\n            return txt\n\n    return _GradientBar()\n</code></pre>"},{"location":"api/worldkernels/ui/progress/#worldkernels.ui.progress.working","title":"working","text":"<pre><code>working(label: str, verbs: Sequence[str] | None = None, counter: Callable[[], str] | None = None) -&gt; Iterator[Any]\n</code></pre> <p>Animated working status: spinning glyph + rotating verb + live counter (TTY only).</p> Source code in <code>worldkernels/ui/progress.py</code> <pre><code>@contextmanager\ndef working(\n    label: str,\n    verbs: Sequence[str] | None = None,\n    counter: Callable[[], str] | None = None,\n) -&gt; Iterator[Any]:\n    r\"\"\"Animated working status: spinning glyph + rotating verb + live counter (TTY only).\"\"\"\n    from worldkernels.ui.verbosity import resolve_output_mode\n\n    if resolve_output_mode() != OutputMode.AUTO:\n        yield None\n        return\n\n    from rich.live import Live\n\n    from worldkernels.ui.console import console\n\n    w = _Working(label, verbs or _VERBS, counter)\n    live = Live(w, console=console(), refresh_per_second=12, transient=True)\n    live.__enter__()\n    try:\n        yield w\n    finally:\n        live.__exit__(None, None, None)\n</code></pre>"},{"location":"api/worldkernels/ui/theme/","title":"theme","text":""},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme","title":"worldkernels.ui.theme","text":"<p>Signature palette: lime brand, per-mode accents, glyphs, and gradient ramps.</p> <p>Content text stays the terminal's default fg (legible on light and dark); the accent only decorates (glyphs, rules, bars, spinners, headings). The active mode picks the accent \u2014 set once per command via <code>set_mode</code>. Truecolor hex; rich downsamples for 256/16-color and strips under NO_COLOR.</p>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.MODES","title":"MODES  <code>module-attribute</code>","text":"<pre><code>MODES: Final[dict[str, str]] = {'serve': '#84CC16', 'generate': '#A855F7', 'measure': '#F59E0B', 'fetch': '#14B8A6', 'read': '#94A3B8', 'danger': '#F43F5E'}\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.MODE_RAMPS","title":"MODE_RAMPS  <code>module-attribute</code>","text":"<pre><code>MODE_RAMPS: Final[dict[str, tuple[str, str, str]]] = {'serve': ('#A3E635', '#22C55E', '#14B8A6'), 'generate': ('#C084FC', '#A855F7', '#7C3AED'), 'measure': ('#FCD34D', '#F59E0B', '#D97706'), 'fetch': ('#5EEAD4', '#14B8A6', '#0D9488'), 'read': ('#CBD5E1', '#94A3B8', '#64748B'), 'danger': ('#FB7185', '#F43F5E', '#BE123C')}\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme._SEMANTIC","title":"_SEMANTIC  <code>module-attribute</code>","text":"<pre><code>_SEMANTIC: Final[dict[str, str]] = {'success': '#16A34A', 'error': '#E5484D', 'warn': '#F59E0B', 'info': 'dim', 'muted': 'dim', 'metric': ''}\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.GLYPHS","title":"GLYPHS  <code>module-attribute</code>","text":"<pre><code>GLYPHS: Final[dict[str, str]] = {'done': '\u2713', 'failed': '\u2717', 'running': '\u25b8', 'pending': '\u00b7', 'skipped': '\u00b7'}\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.STATUS_STYLE","title":"STATUS_STYLE  <code>module-attribute</code>","text":"<pre><code>STATUS_STYLE: Final[dict[str, str]] = {'done': 'success', 'failed': 'error', 'running': 'accent', 'pending': 'muted', 'skipped': 'muted'}\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.BANNER_RAMP","title":"BANNER_RAMP  <code>module-attribute</code>","text":"<pre><code>BANNER_RAMP: Final[tuple[str, ...]] = ('#A3E635 bold', '#84CC16 bold', '#14B8A6 bold')\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme._DEFAULT_MODE","title":"_DEFAULT_MODE  <code>module-attribute</code>","text":"<pre><code>_DEFAULT_MODE: Final[str] = 'serve'\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme._MODE","title":"_MODE  <code>module-attribute</code>","text":"<pre><code>_MODE: str = _DEFAULT_MODE\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.set_mode","title":"set_mode","text":"<pre><code>set_mode(mode: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/theme.py</code> <pre><code>def set_mode(mode: str) -&gt; None:\n    global _MODE\n    _MODE = mode if mode in MODES else _DEFAULT_MODE\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.get_mode","title":"get_mode","text":"<pre><code>get_mode() -&gt; str\n</code></pre> Source code in <code>worldkernels/ui/theme.py</code> <pre><code>def get_mode() -&gt; str:\n    return _MODE\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.accent","title":"accent","text":"<pre><code>accent() -&gt; str\n</code></pre> <p>Active mode's accent hex.</p> Source code in <code>worldkernels/ui/theme.py</code> <pre><code>def accent() -&gt; str:\n    r\"\"\"Active mode's accent hex.\"\"\"\n    return MODES[_MODE]\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.ramp","title":"ramp","text":"<pre><code>ramp() -&gt; tuple[str, str, str]\n</code></pre> <p>Active mode's 3-stop gradient.</p> Source code in <code>worldkernels/ui/theme.py</code> <pre><code>def ramp() -&gt; tuple[str, str, str]:\n    r\"\"\"Active mode's 3-stop gradient.\"\"\"\n    return MODE_RAMPS[_MODE]\n</code></pre>"},{"location":"api/worldkernels/ui/theme/#worldkernels.ui.theme.style","title":"style","text":"<pre><code>style(name: str) -&gt; str\n</code></pre> <p>Resolve a semantic/status/accent name to a rich style; pass raw styles through.</p> <p><code>accent</code>/<code>heading</code>/<code>key</code>/<code>rule</code> follow the active mode; semantic names are fixed.</p> Source code in <code>worldkernels/ui/theme.py</code> <pre><code>def style(name: str) -&gt; str:\n    r\"\"\"Resolve a semantic/status/accent name to a rich style; pass raw styles through.\n\n    `accent`/`heading`/`key`/`rule` follow the active mode; semantic names are fixed.\n    \"\"\"\n    if name in (\"accent\", \"heading\"):\n        return f\"{accent()} bold\"\n    if name in (\"key\", \"rule\"):\n        return accent()\n    if name in _SEMANTIC:\n        return _SEMANTIC[name]\n    if name in STATUS_STYLE:\n        return style(STATUS_STYLE[name])\n    return name\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/","title":"verbosity","text":""},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity","title":"worldkernels.ui.verbosity","text":"<p>Process-global CLI verbosity and output mode.</p>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity._VERBOSITY","title":"_VERBOSITY  <code>module-attribute</code>","text":"<pre><code>_VERBOSITY: Verbosity = Verbosity.NORMAL\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity._OUTPUT","title":"_OUTPUT  <code>module-attribute</code>","text":"<pre><code>_OUTPUT: OutputMode = OutputMode.AUTO\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.Verbosity","title":"Verbosity","text":"<p>               Bases: <code>IntEnum</code></p> Source code in <code>worldkernels/ui/verbosity.py</code> <pre><code>class Verbosity(enum.IntEnum):\n    QUIET = 0\n    NORMAL = 1\n    VERBOSE = 2\n    DEBUG = 3\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.Verbosity.QUIET","title":"QUIET  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>QUIET = 0\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.Verbosity.NORMAL","title":"NORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NORMAL = 1\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.Verbosity.VERBOSE","title":"VERBOSE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>VERBOSE = 2\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.Verbosity.DEBUG","title":"DEBUG  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>DEBUG = 3\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.OutputMode","title":"OutputMode","text":"<p>               Bases: <code>Enum</code></p> Source code in <code>worldkernels/ui/verbosity.py</code> <pre><code>class OutputMode(enum.Enum):\n    AUTO = \"auto\"\n    PLAIN = \"plain\"\n    JSON = \"json\"\n    QUIET = \"quiet\"\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.OutputMode.AUTO","title":"AUTO  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>AUTO = 'auto'\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.OutputMode.PLAIN","title":"PLAIN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PLAIN = 'plain'\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.OutputMode.JSON","title":"JSON  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>JSON = 'json'\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.OutputMode.QUIET","title":"QUIET  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>QUIET = 'quiet'\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.get_verbosity","title":"get_verbosity","text":"<pre><code>get_verbosity() -&gt; Verbosity\n</code></pre> Source code in <code>worldkernels/ui/verbosity.py</code> <pre><code>def get_verbosity() -&gt; Verbosity:\n    return _VERBOSITY\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.set_verbosity","title":"set_verbosity","text":"<pre><code>set_verbosity(v: Verbosity) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/verbosity.py</code> <pre><code>def set_verbosity(v: Verbosity) -&gt; None:\n    global _VERBOSITY\n    _VERBOSITY = v\n    if v == Verbosity.QUIET:\n        os.environ[\"WORLDKERNELS_QUIET\"] = \"1\"\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.get_output_mode","title":"get_output_mode","text":"<pre><code>get_output_mode() -&gt; OutputMode\n</code></pre> Source code in <code>worldkernels/ui/verbosity.py</code> <pre><code>def get_output_mode() -&gt; OutputMode:\n    return _OUTPUT\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.set_output_mode","title":"set_output_mode","text":"<pre><code>set_output_mode(m: OutputMode) -&gt; None\n</code></pre> Source code in <code>worldkernels/ui/verbosity.py</code> <pre><code>def set_output_mode(m: OutputMode) -&gt; None:\n    global _OUTPUT\n    _OUTPUT = m\n</code></pre>"},{"location":"api/worldkernels/ui/verbosity/#worldkernels.ui.verbosity.resolve_output_mode","title":"resolve_output_mode","text":"<pre><code>resolve_output_mode() -&gt; OutputMode\n</code></pre> <p>Collapse AUTO to QUIET/PLAIN/AUTO using env, verbosity, and tty state.</p> Source code in <code>worldkernels/ui/verbosity.py</code> <pre><code>def resolve_output_mode() -&gt; OutputMode:\n    r\"\"\"Collapse AUTO to QUIET/PLAIN/AUTO using env, verbosity, and tty state.\"\"\"\n    from worldkernels.ui.console import console, is_quiet_env\n\n    if is_quiet_env() or _VERBOSITY == Verbosity.QUIET:\n        return OutputMode.QUIET\n    if _OUTPUT != OutputMode.AUTO:\n        return _OUTPUT\n    return OutputMode.AUTO if console().is_terminal else OutputMode.PLAIN\n</code></pre>"},{"location":"api/worldkernels/utils/","title":"utils","text":""},{"location":"api/worldkernels/utils/#worldkernels.utils","title":"worldkernels.utils","text":"<p>Shared utilities: optional-dependency handling and device detection.</p> <p>Modules:</p> Name Description <code>device_info</code> <p>Hardware target detection for device-specific dependency selection.</p> <code>import_utils</code> <p>Deferred optional-dependency handling.</p>"},{"location":"api/worldkernels/utils/#worldkernels.utils.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['PlaceholderModule', 'optional_import', 'LazyLoader', 'detect_target_device']\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader","title":"LazyLoader","text":"<p>               Bases: <code>ModuleType</code></p> <p>Defer a module's import to first attribute access.</p> <p>Useful for modules with import-time side effects (e.g. op registration) that we want to keep out of the cold path. On first attribute access, the real module is imported and patched into <code>parent_globals</code> + <code>sys.modules</code> so subsequent accesses are free.</p> <p>Parameters:</p> Name Type Description Default <code>local_name</code> <code>str</code> <p>Binding name in the caller's module (e.g. <code>\"diffusers\"</code>).</p> required <code>parent_globals</code> <code>dict</code> <p>The caller's <code>globals()</code> dict.</p> required <code>module_name</code> <code>str</code> <p>Fully qualified import path.</p> required Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>class LazyLoader(ModuleType):\n    r\"\"\"Defer a module's import to first attribute access.\n\n    Useful for modules with import-time side effects (e.g. op registration) that\n    we want to keep out of the cold path. On first attribute access, the real\n    module is imported and patched into ``parent_globals`` + ``sys.modules`` so\n    subsequent accesses are free.\n\n    Args:\n        local_name: Binding name in the caller's module (e.g. ``\"diffusers\"``).\n        parent_globals: The caller's ``globals()`` dict.\n        module_name: Fully qualified import path.\n    \"\"\"\n\n    def __init__(self, local_name: str, parent_globals: dict, module_name: str) -&gt; None:\n        super().__init__(module_name)\n        self._local_name = local_name\n        self._parent_globals = parent_globals\n        self._module_name = module_name\n\n    def _load(self) -&gt; ModuleType:\n        import sys as _sys\n\n        module = importlib.import_module(self._module_name)\n        self._parent_globals[self._local_name] = module\n        _sys.modules[self._module_name] = module\n        self.__dict__.update(module.__dict__)\n        return module\n\n    def __getattr__(self, item: str) -&gt; Any:\n        module = self._load()\n        return getattr(module, item)\n\n    def __dir__(self) -&gt; list[str]:\n        module = self._load()\n        return dir(module)\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader._local_name","title":"_local_name  <code>instance-attribute</code>","text":"<pre><code>_local_name = local_name\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader._parent_globals","title":"_parent_globals  <code>instance-attribute</code>","text":"<pre><code>_parent_globals = parent_globals\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader._module_name","title":"_module_name  <code>instance-attribute</code>","text":"<pre><code>_module_name = module_name\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader.__init__","title":"__init__","text":"<pre><code>__init__(local_name: str, parent_globals: dict, module_name: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __init__(self, local_name: str, parent_globals: dict, module_name: str) -&gt; None:\n    super().__init__(module_name)\n    self._local_name = local_name\n    self._parent_globals = parent_globals\n    self._module_name = module_name\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader._load","title":"_load","text":"<pre><code>_load() -&gt; ModuleType\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def _load(self) -&gt; ModuleType:\n    import sys as _sys\n\n    module = importlib.import_module(self._module_name)\n    self._parent_globals[self._local_name] = module\n    _sys.modules[self._module_name] = module\n    self.__dict__.update(module.__dict__)\n    return module\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(item: str) -&gt; Any\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __getattr__(self, item: str) -&gt; Any:\n    module = self._load()\n    return getattr(module, item)\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.LazyLoader.__dir__","title":"__dir__","text":"<pre><code>__dir__() -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __dir__(self) -&gt; list[str]:\n    module = self._load()\n    return dir(module)\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.PlaceholderModule","title":"PlaceholderModule","text":"<p>               Bases: <code>_PlaceholderBase</code></p> <p>Stand-in for a missing optional package.</p> <p>Accessing any attribute raises <code>ImportError</code> naming the extra to install. Construction never raises, so a placeholder can be bound and passed around exactly like the real module.</p> <p>Parameters:</p> Name Type Description Default <code>pkg_name</code> <code>str</code> <p>The import name that failed (e.g. <code>\"flash_attn\"</code>).</p> required <code>extra</code> <code>str</code> <p>The <code>worldkernels</code> extra that provides it (e.g. <code>\"flash-attn\"</code>).</p> required Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>class PlaceholderModule(_PlaceholderBase):\n    r\"\"\"Stand-in for a missing optional package.\n\n    Accessing any attribute raises `ImportError` naming the extra to\n    install. Construction never raises, so a placeholder can be bound and\n    passed around exactly like the real module.\n\n    Args:\n        pkg_name: The import name that failed (e.g. ``\"flash_attn\"``).\n        extra: The ``worldkernels`` extra that provides it (e.g. ``\"flash-attn\"``).\n    \"\"\"\n\n    def __init__(self, pkg_name: str, extra: str) -&gt; None:\n        self._pkg_name = pkg_name\n        self._extra = extra\n\n    def _raise(self, path: str | None = None) -&gt; Any:\n        target = f\"{self._pkg_name}.{path}\" if path else self._pkg_name\n        raise ImportError(\n            f\"{target!r} requires the optional package {self._pkg_name!r}. \"\n            f\"Install it with: pip install 'worldkernels[{self._extra}]'\"\n        )\n\n    def __getattr__(self, name: str) -&gt; _PlaceholderModuleAttr:\n        return _PlaceholderModuleAttr(self, name)\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.PlaceholderModule._pkg_name","title":"_pkg_name  <code>instance-attribute</code>","text":"<pre><code>_pkg_name = pkg_name\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.PlaceholderModule._extra","title":"_extra  <code>instance-attribute</code>","text":"<pre><code>_extra = extra\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.PlaceholderModule.__init__","title":"__init__","text":"<pre><code>__init__(pkg_name: str, extra: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __init__(self, pkg_name: str, extra: str) -&gt; None:\n    self._pkg_name = pkg_name\n    self._extra = extra\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.PlaceholderModule._raise","title":"_raise","text":"<pre><code>_raise(path: str | None = None) -&gt; Any\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def _raise(self, path: str | None = None) -&gt; Any:\n    target = f\"{self._pkg_name}.{path}\" if path else self._pkg_name\n    raise ImportError(\n        f\"{target!r} requires the optional package {self._pkg_name!r}. \"\n        f\"Install it with: pip install 'worldkernels[{self._extra}]'\"\n    )\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.PlaceholderModule.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str) -&gt; _PlaceholderModuleAttr\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __getattr__(self, name: str) -&gt; _PlaceholderModuleAttr:\n    return _PlaceholderModuleAttr(self, name)\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.detect_target_device","title":"detect_target_device","text":"<pre><code>detect_target_device() -&gt; TargetDevice\n</code></pre> <p>Return the hardware target.</p> <p>Honors the <code>WK_TARGET_DEVICE</code> environment variable; otherwise probes torch for CUDA / HIP, falling back to <code>\"cpu\"</code>. Does not import torch unless the env var is unset.</p> Source code in <code>worldkernels/utils/device_info.py</code> <pre><code>def detect_target_device() -&gt; TargetDevice:\n    r\"\"\"Return the hardware target.\n\n    Honors the ``WK_TARGET_DEVICE`` environment variable; otherwise probes\n    torch for CUDA / HIP, falling back to ``\"cpu\"``. Does not import torch\n    unless the env var is unset.\n    \"\"\"\n    forced = os.environ.get(\"WK_TARGET_DEVICE\")\n    if forced in (\"cuda\", \"rocm\", \"cpu\"):\n        return forced  # type: ignore[return-value]\n    try:\n        import torch\n    except ImportError:\n        return \"cpu\"\n    if getattr(torch.version, \"hip\", None):\n        return \"rocm\"\n    if getattr(torch.version, \"cuda\", None) and torch.cuda.is_available():\n        return \"cuda\"\n    return \"cpu\"\n</code></pre>"},{"location":"api/worldkernels/utils/#worldkernels.utils.optional_import","title":"optional_import","text":"<pre><code>optional_import(pkg: str, extra: str) -&gt; ModuleType | PlaceholderModule\n</code></pre> <p>Import <code>pkg</code>, returning a <code>PlaceholderModule</code> if it is absent.</p> <p>Parameters:</p> Name Type Description Default <code>pkg</code> <code>str</code> <p>Import name of the optional package.</p> required <code>extra</code> <code>str</code> <p>The <code>worldkernels</code> extra that provides it.</p> required Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def optional_import(pkg: str, extra: str) -&gt; ModuleType | PlaceholderModule:\n    r\"\"\"Import ``pkg``, returning a `PlaceholderModule` if it is absent.\n\n    Args:\n        pkg: Import name of the optional package.\n        extra: The ``worldkernels`` extra that provides it.\n    \"\"\"\n    try:\n        return importlib.import_module(pkg)\n    except ImportError:\n        return PlaceholderModule(pkg, extra)\n</code></pre>"},{"location":"api/worldkernels/utils/device_info/","title":"device_info","text":""},{"location":"api/worldkernels/utils/device_info/#worldkernels.utils.device_info","title":"worldkernels.utils.device_info","text":"<p>Hardware target detection for device-specific dependency selection.</p>"},{"location":"api/worldkernels/utils/device_info/#worldkernels.utils.device_info.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['detect_target_device']\n</code></pre>"},{"location":"api/worldkernels/utils/device_info/#worldkernels.utils.device_info.TargetDevice","title":"TargetDevice  <code>module-attribute</code>","text":"<pre><code>TargetDevice = Literal['cuda', 'rocm', 'cpu']\n</code></pre>"},{"location":"api/worldkernels/utils/device_info/#worldkernels.utils.device_info.detect_target_device","title":"detect_target_device","text":"<pre><code>detect_target_device() -&gt; TargetDevice\n</code></pre> <p>Return the hardware target.</p> <p>Honors the <code>WK_TARGET_DEVICE</code> environment variable; otherwise probes torch for CUDA / HIP, falling back to <code>\"cpu\"</code>. Does not import torch unless the env var is unset.</p> Source code in <code>worldkernels/utils/device_info.py</code> <pre><code>def detect_target_device() -&gt; TargetDevice:\n    r\"\"\"Return the hardware target.\n\n    Honors the ``WK_TARGET_DEVICE`` environment variable; otherwise probes\n    torch for CUDA / HIP, falling back to ``\"cpu\"``. Does not import torch\n    unless the env var is unset.\n    \"\"\"\n    forced = os.environ.get(\"WK_TARGET_DEVICE\")\n    if forced in (\"cuda\", \"rocm\", \"cpu\"):\n        return forced  # type: ignore[return-value]\n    try:\n        import torch\n    except ImportError:\n        return \"cpu\"\n    if getattr(torch.version, \"hip\", None):\n        return \"rocm\"\n    if getattr(torch.version, \"cuda\", None) and torch.cuda.is_available():\n        return \"cuda\"\n    return \"cpu\"\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/","title":"import_utils","text":""},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils","title":"worldkernels.utils.import_utils","text":"<p>Deferred optional-dependency handling.</p> <p>A missing optional package becomes a <code>PlaceholderModule</code> rather than an <code>ImportError</code> at import time. The placeholder is safe to pass around and assign; it raises only when an attribute is used, with a message naming the <code>worldkernels</code> extra to install.</p>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['PlaceholderModule', 'optional_import', 'LazyLoader']\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderBase","title":"_PlaceholderBase","text":"Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>class _PlaceholderBase:\n    def _raise(self) -&gt; Any:\n        raise NotImplementedError\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderBase._raise","title":"_raise","text":"<pre><code>_raise() -&gt; Any\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def _raise(self) -&gt; Any:\n    raise NotImplementedError\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderModuleAttr","title":"_PlaceholderModuleAttr","text":"<p>               Bases: <code>_PlaceholderBase</code></p> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>class _PlaceholderModuleAttr(_PlaceholderBase):\n    def __init__(self, module: \"PlaceholderModule\", path: str) -&gt; None:\n        self._module = module\n        self._path = path\n\n    def __getattr__(self, name: str) -&gt; \"_PlaceholderModuleAttr\":\n        return _PlaceholderModuleAttr(self._module, f\"{self._path}.{name}\")\n\n    def __call__(self, *args: Any, **kwargs: Any) -&gt; Any:\n        self._module._raise(self._path)\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderModuleAttr._module","title":"_module  <code>instance-attribute</code>","text":"<pre><code>_module = module\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderModuleAttr._path","title":"_path  <code>instance-attribute</code>","text":"<pre><code>_path = path\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderModuleAttr.__init__","title":"__init__","text":"<pre><code>__init__(module: 'PlaceholderModule', path: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __init__(self, module: \"PlaceholderModule\", path: str) -&gt; None:\n    self._module = module\n    self._path = path\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderModuleAttr.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str) -&gt; '_PlaceholderModuleAttr'\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __getattr__(self, name: str) -&gt; \"_PlaceholderModuleAttr\":\n    return _PlaceholderModuleAttr(self._module, f\"{self._path}.{name}\")\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderModuleAttr.__call__","title":"__call__","text":"<pre><code>__call__(*args: Any, **kwargs: Any) -&gt; Any\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __call__(self, *args: Any, **kwargs: Any) -&gt; Any:\n    self._module._raise(self._path)\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils._PlaceholderModuleAttr._raise","title":"_raise","text":"<pre><code>_raise() -&gt; Any\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def _raise(self) -&gt; Any:\n    raise NotImplementedError\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.PlaceholderModule","title":"PlaceholderModule","text":"<p>               Bases: <code>_PlaceholderBase</code></p> <p>Stand-in for a missing optional package.</p> <p>Accessing any attribute raises <code>ImportError</code> naming the extra to install. Construction never raises, so a placeholder can be bound and passed around exactly like the real module.</p> <p>Parameters:</p> Name Type Description Default <code>pkg_name</code> <code>str</code> <p>The import name that failed (e.g. <code>\"flash_attn\"</code>).</p> required <code>extra</code> <code>str</code> <p>The <code>worldkernels</code> extra that provides it (e.g. <code>\"flash-attn\"</code>).</p> required Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>class PlaceholderModule(_PlaceholderBase):\n    r\"\"\"Stand-in for a missing optional package.\n\n    Accessing any attribute raises `ImportError` naming the extra to\n    install. Construction never raises, so a placeholder can be bound and\n    passed around exactly like the real module.\n\n    Args:\n        pkg_name: The import name that failed (e.g. ``\"flash_attn\"``).\n        extra: The ``worldkernels`` extra that provides it (e.g. ``\"flash-attn\"``).\n    \"\"\"\n\n    def __init__(self, pkg_name: str, extra: str) -&gt; None:\n        self._pkg_name = pkg_name\n        self._extra = extra\n\n    def _raise(self, path: str | None = None) -&gt; Any:\n        target = f\"{self._pkg_name}.{path}\" if path else self._pkg_name\n        raise ImportError(\n            f\"{target!r} requires the optional package {self._pkg_name!r}. \"\n            f\"Install it with: pip install 'worldkernels[{self._extra}]'\"\n        )\n\n    def __getattr__(self, name: str) -&gt; _PlaceholderModuleAttr:\n        return _PlaceholderModuleAttr(self, name)\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.PlaceholderModule._pkg_name","title":"_pkg_name  <code>instance-attribute</code>","text":"<pre><code>_pkg_name = pkg_name\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.PlaceholderModule._extra","title":"_extra  <code>instance-attribute</code>","text":"<pre><code>_extra = extra\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.PlaceholderModule.__init__","title":"__init__","text":"<pre><code>__init__(pkg_name: str, extra: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __init__(self, pkg_name: str, extra: str) -&gt; None:\n    self._pkg_name = pkg_name\n    self._extra = extra\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.PlaceholderModule._raise","title":"_raise","text":"<pre><code>_raise(path: str | None = None) -&gt; Any\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def _raise(self, path: str | None = None) -&gt; Any:\n    target = f\"{self._pkg_name}.{path}\" if path else self._pkg_name\n    raise ImportError(\n        f\"{target!r} requires the optional package {self._pkg_name!r}. \"\n        f\"Install it with: pip install 'worldkernels[{self._extra}]'\"\n    )\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.PlaceholderModule.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str) -&gt; _PlaceholderModuleAttr\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __getattr__(self, name: str) -&gt; _PlaceholderModuleAttr:\n    return _PlaceholderModuleAttr(self, name)\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader","title":"LazyLoader","text":"<p>               Bases: <code>ModuleType</code></p> <p>Defer a module's import to first attribute access.</p> <p>Useful for modules with import-time side effects (e.g. op registration) that we want to keep out of the cold path. On first attribute access, the real module is imported and patched into <code>parent_globals</code> + <code>sys.modules</code> so subsequent accesses are free.</p> <p>Parameters:</p> Name Type Description Default <code>local_name</code> <code>str</code> <p>Binding name in the caller's module (e.g. <code>\"diffusers\"</code>).</p> required <code>parent_globals</code> <code>dict</code> <p>The caller's <code>globals()</code> dict.</p> required <code>module_name</code> <code>str</code> <p>Fully qualified import path.</p> required Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>class LazyLoader(ModuleType):\n    r\"\"\"Defer a module's import to first attribute access.\n\n    Useful for modules with import-time side effects (e.g. op registration) that\n    we want to keep out of the cold path. On first attribute access, the real\n    module is imported and patched into ``parent_globals`` + ``sys.modules`` so\n    subsequent accesses are free.\n\n    Args:\n        local_name: Binding name in the caller's module (e.g. ``\"diffusers\"``).\n        parent_globals: The caller's ``globals()`` dict.\n        module_name: Fully qualified import path.\n    \"\"\"\n\n    def __init__(self, local_name: str, parent_globals: dict, module_name: str) -&gt; None:\n        super().__init__(module_name)\n        self._local_name = local_name\n        self._parent_globals = parent_globals\n        self._module_name = module_name\n\n    def _load(self) -&gt; ModuleType:\n        import sys as _sys\n\n        module = importlib.import_module(self._module_name)\n        self._parent_globals[self._local_name] = module\n        _sys.modules[self._module_name] = module\n        self.__dict__.update(module.__dict__)\n        return module\n\n    def __getattr__(self, item: str) -&gt; Any:\n        module = self._load()\n        return getattr(module, item)\n\n    def __dir__(self) -&gt; list[str]:\n        module = self._load()\n        return dir(module)\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader._local_name","title":"_local_name  <code>instance-attribute</code>","text":"<pre><code>_local_name = local_name\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader._parent_globals","title":"_parent_globals  <code>instance-attribute</code>","text":"<pre><code>_parent_globals = parent_globals\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader._module_name","title":"_module_name  <code>instance-attribute</code>","text":"<pre><code>_module_name = module_name\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader.__init__","title":"__init__","text":"<pre><code>__init__(local_name: str, parent_globals: dict, module_name: str) -&gt; None\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __init__(self, local_name: str, parent_globals: dict, module_name: str) -&gt; None:\n    super().__init__(module_name)\n    self._local_name = local_name\n    self._parent_globals = parent_globals\n    self._module_name = module_name\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader._load","title":"_load","text":"<pre><code>_load() -&gt; ModuleType\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def _load(self) -&gt; ModuleType:\n    import sys as _sys\n\n    module = importlib.import_module(self._module_name)\n    self._parent_globals[self._local_name] = module\n    _sys.modules[self._module_name] = module\n    self.__dict__.update(module.__dict__)\n    return module\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(item: str) -&gt; Any\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __getattr__(self, item: str) -&gt; Any:\n    module = self._load()\n    return getattr(module, item)\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.LazyLoader.__dir__","title":"__dir__","text":"<pre><code>__dir__() -&gt; list[str]\n</code></pre> Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def __dir__(self) -&gt; list[str]:\n    module = self._load()\n    return dir(module)\n</code></pre>"},{"location":"api/worldkernels/utils/import_utils/#worldkernels.utils.import_utils.optional_import","title":"optional_import","text":"<pre><code>optional_import(pkg: str, extra: str) -&gt; ModuleType | PlaceholderModule\n</code></pre> <p>Import <code>pkg</code>, returning a <code>PlaceholderModule</code> if it is absent.</p> <p>Parameters:</p> Name Type Description Default <code>pkg</code> <code>str</code> <p>Import name of the optional package.</p> required <code>extra</code> <code>str</code> <p>The <code>worldkernels</code> extra that provides it.</p> required Source code in <code>worldkernels/utils/import_utils.py</code> <pre><code>def optional_import(pkg: str, extra: str) -&gt; ModuleType | PlaceholderModule:\n    r\"\"\"Import ``pkg``, returning a `PlaceholderModule` if it is absent.\n\n    Args:\n        pkg: Import name of the optional package.\n        extra: The ``worldkernels`` extra that provides it.\n    \"\"\"\n    try:\n        return importlib.import_module(pkg)\n    except ImportError:\n        return PlaceholderModule(pkg, extra)\n</code></pre>"},{"location":"api/worldkernels/worker/","title":"worker","text":""},{"location":"api/worldkernels/worker/#worldkernels.worker","title":"worldkernels.worker","text":"<p>Worker layer: device infrastructure and the compute runner.</p> <p>Modules:</p> Name Description <code>worker</code> <p>Worker \u2014 the device-infrastructure layer.</p> <code>world_runner</code> <p>WorldRunner \u2014 the compute layer.</p>"},{"location":"api/worldkernels/worker/#worldkernels.worker.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Worker', 'WorldRunner']\n</code></pre>"},{"location":"api/worldkernels/worker/#worldkernels.worker._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'Worker': ('worldkernels.worker.worker', 'Worker'), 'WorldRunner': ('worldkernels.worker.world_runner', 'WorldRunner')}\n</code></pre>"},{"location":"api/worldkernels/worker/#worldkernels.worker.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/worker/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module {__name__!r} has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/worker/worker/","title":"worker","text":""},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker","title":"worldkernels.worker.worker","text":"<p>Worker \u2014 the device-infrastructure layer.</p> <p>One worker owns one device: it binds the CUDA context, initializes the distributed parallel state, and holds a <code>WorldRunner</code>. The scheduler dispatches step requests to it. Multi-rank workers and out-of-process isolation are added by the distributed step of the restructure.</p>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['Worker']\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker","title":"Worker","text":"<p>Owns one device and the runner that executes on it.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>str</code> <p>Target device string.</p> required <code>dtype</code> <code>'torch.dtype'</code> <p>Compute dtype.</p> required <code>parallel_config</code> <code>'ParallelConfig | None'</code> <p>Parallelism degrees; single-rank by default.</p> <code>None</code> Source code in <code>worldkernels/worker/worker.py</code> <pre><code>class Worker:\n    r\"\"\"Owns one device and the runner that executes on it.\n\n    Args:\n        device: Target device string.\n        dtype: Compute dtype.\n        parallel_config: Parallelism degrees; single-rank by default.\n    \"\"\"\n\n    def __init__(\n        self,\n        device: str,\n        dtype: \"torch.dtype\",\n        parallel_config: \"ParallelConfig | None\" = None,\n        config: \"RuntimeConfig | None\" = None,\n    ) -&gt; None:\n        from worldkernels.config import ParallelConfig\n        from worldkernels.distributed import init_distributed, is_initialized\n\n        self.device = device\n        self.dtype = dtype\n        self.parallel_config = parallel_config or ParallelConfig()\n        self.config = config\n        if not is_initialized():\n            init_distributed(self.parallel_config)\n        self.runner = WorldRunner(device, dtype, config=config)\n\n    def execute(\n        self,\n        requests: list[\"StepRequest\"],\n    ) -&gt; list[tuple[\"LatentState\", \"Observation\"]]:\n        r\"\"\"Run a batch of step requests on this worker's device.\"\"\"\n        return self.runner.run_batch(requests)\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype = dtype\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker.parallel_config","title":"parallel_config  <code>instance-attribute</code>","text":"<pre><code>parallel_config = parallel_config or ParallelConfig()\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker.runner","title":"runner  <code>instance-attribute</code>","text":"<pre><code>runner = WorldRunner(device, dtype, config=config)\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker.__init__","title":"__init__","text":"<pre><code>__init__(device: str, dtype: 'torch.dtype', parallel_config: 'ParallelConfig | None' = None, config: 'RuntimeConfig | None' = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/worker/worker.py</code> <pre><code>def __init__(\n    self,\n    device: str,\n    dtype: \"torch.dtype\",\n    parallel_config: \"ParallelConfig | None\" = None,\n    config: \"RuntimeConfig | None\" = None,\n) -&gt; None:\n    from worldkernels.config import ParallelConfig\n    from worldkernels.distributed import init_distributed, is_initialized\n\n    self.device = device\n    self.dtype = dtype\n    self.parallel_config = parallel_config or ParallelConfig()\n    self.config = config\n    if not is_initialized():\n        init_distributed(self.parallel_config)\n    self.runner = WorldRunner(device, dtype, config=config)\n</code></pre>"},{"location":"api/worldkernels/worker/worker/#worldkernels.worker.worker.Worker.execute","title":"execute","text":"<pre><code>execute(requests: list['StepRequest']) -&gt; list[tuple['LatentState', 'Observation']]\n</code></pre> <p>Run a batch of step requests on this worker's device.</p> Source code in <code>worldkernels/worker/worker.py</code> <pre><code>def execute(\n    self,\n    requests: list[\"StepRequest\"],\n) -&gt; list[tuple[\"LatentState\", \"Observation\"]]:\n    r\"\"\"Run a batch of step requests on this worker's device.\"\"\"\n    return self.runner.run_batch(requests)\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/","title":"world_runner","text":""},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner","title":"worldkernels.worker.world_runner","text":"<p>WorldRunner \u2014 the compute layer.</p> <p>Holds the executor and runs batched step requests under a <code>ForwardContext</code>. Owns the runtime-feature instances (latent pool, denoise-step cache, paged KV cache + block allocator) gated by <code>RuntimeConfig</code>; off-flags fall through to the eager path with identical numerics.</p>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WorldRunner']\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner","title":"WorldRunner","text":"<p>Runs world-model steps on one device.</p> <p>Parameters:</p> Name Type Description Default <code>device</code> <code>str</code> <p>Target device string.</p> required <code>dtype</code> <code>'torch.dtype'</code> <p>Compute dtype.</p> required <code>config</code> <code>'RuntimeConfig | None'</code> <p>Runtime config (component toggles).</p> <code>None</code> Source code in <code>worldkernels/worker/world_runner.py</code> <pre><code>class WorldRunner:\n    r\"\"\"Runs world-model steps on one device.\n\n    Args:\n        device: Target device string.\n        dtype: Compute dtype.\n        config: Runtime config (component toggles).\n    \"\"\"\n\n    def __init__(\n        self,\n        device: str,\n        dtype: \"torch.dtype\",\n        config: \"RuntimeConfig | None\" = None,\n    ) -&gt; None:\n        self.device = device\n        self.dtype = dtype\n        self.config = config\n        self.executor = Executor(device=device, dtype=dtype, config=config)\n\n        self.pool: \"LatentPool | None\" = None\n        self.teacache: \"TeaCache | None\" = None\n        self.block_manager: \"BlockManager | None\" = None\n        self.kv_cache: \"KVCacheManager | None\" = None\n\n        if config is not None and config.latent_pool:\n            from worldkernels.runtime.memory.latent_pool import LatentPool\n\n            self.pool = LatentPool(device=device)\n\n        if config is not None and config.teacache:\n            from worldkernels.runtime.cache.teacache import TeaCache\n\n            self.teacache = TeaCache()\n\n    def ensure_kv_cache(self, frames_per_block: int = 16, num_blocks: int = 64) -&gt; None:\n        r\"\"\"Allocate the paged KV substrate on first causal-world load.\n\n        No-op when ``kv_cache_paged`` is off or already created.\n        \"\"\"\n        if self.kv_cache is not None:\n            return\n        if self.config is None or not self.config.kv_cache_paged:\n            return\n        from worldkernels.runtime.memory.block_manager import BlockManager\n        from worldkernels.runtime.memory.kv_cache import KVCacheManager\n\n        self.block_manager = BlockManager(\n            block_shape=(frames_per_block,),\n            num_blocks=num_blocks,\n            device=self.device,\n            dtype=self.dtype,\n        )\n        self.kv_cache = KVCacheManager(self.block_manager)\n\n    def run_batch(\n        self,\n        requests: list[\"StepRequest\"],\n    ) -&gt; list[tuple[\"LatentState\", \"Observation\"]]:\n        r\"\"\"Execute a batch of step requests under a forward context.\n\n        Per-session overrides on the first request (the safe subset) are\n        overlaid onto the runner defaults for the duration of the batch.\n        \"\"\"\n        overrides = (requests[0].overrides or {}) if requests else {}\n\n        teacache_on = overrides.get(\"teacache\", True)\n        cache_backend = self.teacache if teacache_on else None\n\n        attn = overrides.get(\"attention_backend\") or self._default_attention_backend()\n        attn = None if attn == \"auto\" else attn\n\n        iter_batch = overrides.get(\"iteration_batching\")\n        if iter_batch is None:\n            iter_batch = bool(self.config and self.config.iteration_batching)\n\n        ctx = ForwardContext(\n            cache_backend=cache_backend,\n            pool=self.pool,\n            attention_backend=attn,\n            iteration_batching=iter_batch,\n        )\n        with set_forward_context(ctx):\n            return self.executor.execute_batched(requests)\n\n    def _default_attention_backend(self) -&gt; str | None:\n        if self.config is None:\n            return None\n        return self.config.attention_backend\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device = device\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype = dtype\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.config","title":"config  <code>instance-attribute</code>","text":"<pre><code>config = config\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.executor","title":"executor  <code>instance-attribute</code>","text":"<pre><code>executor = Executor(device=device, dtype=dtype, config=config)\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.pool","title":"pool  <code>instance-attribute</code>","text":"<pre><code>pool: 'LatentPool | None' = None\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.teacache","title":"teacache  <code>instance-attribute</code>","text":"<pre><code>teacache: 'TeaCache | None' = None\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.block_manager","title":"block_manager  <code>instance-attribute</code>","text":"<pre><code>block_manager: 'BlockManager | None' = None\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.kv_cache","title":"kv_cache  <code>instance-attribute</code>","text":"<pre><code>kv_cache: 'KVCacheManager | None' = None\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.__init__","title":"__init__","text":"<pre><code>__init__(device: str, dtype: 'torch.dtype', config: 'RuntimeConfig | None' = None) -&gt; None\n</code></pre> Source code in <code>worldkernels/worker/world_runner.py</code> <pre><code>def __init__(\n    self,\n    device: str,\n    dtype: \"torch.dtype\",\n    config: \"RuntimeConfig | None\" = None,\n) -&gt; None:\n    self.device = device\n    self.dtype = dtype\n    self.config = config\n    self.executor = Executor(device=device, dtype=dtype, config=config)\n\n    self.pool: \"LatentPool | None\" = None\n    self.teacache: \"TeaCache | None\" = None\n    self.block_manager: \"BlockManager | None\" = None\n    self.kv_cache: \"KVCacheManager | None\" = None\n\n    if config is not None and config.latent_pool:\n        from worldkernels.runtime.memory.latent_pool import LatentPool\n\n        self.pool = LatentPool(device=device)\n\n    if config is not None and config.teacache:\n        from worldkernels.runtime.cache.teacache import TeaCache\n\n        self.teacache = TeaCache()\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.ensure_kv_cache","title":"ensure_kv_cache","text":"<pre><code>ensure_kv_cache(frames_per_block: int = 16, num_blocks: int = 64) -&gt; None\n</code></pre> <p>Allocate the paged KV substrate on first causal-world load.</p> <p>No-op when <code>kv_cache_paged</code> is off or already created.</p> Source code in <code>worldkernels/worker/world_runner.py</code> <pre><code>def ensure_kv_cache(self, frames_per_block: int = 16, num_blocks: int = 64) -&gt; None:\n    r\"\"\"Allocate the paged KV substrate on first causal-world load.\n\n    No-op when ``kv_cache_paged`` is off or already created.\n    \"\"\"\n    if self.kv_cache is not None:\n        return\n    if self.config is None or not self.config.kv_cache_paged:\n        return\n    from worldkernels.runtime.memory.block_manager import BlockManager\n    from worldkernels.runtime.memory.kv_cache import KVCacheManager\n\n    self.block_manager = BlockManager(\n        block_shape=(frames_per_block,),\n        num_blocks=num_blocks,\n        device=self.device,\n        dtype=self.dtype,\n    )\n    self.kv_cache = KVCacheManager(self.block_manager)\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner.run_batch","title":"run_batch","text":"<pre><code>run_batch(requests: list['StepRequest']) -&gt; list[tuple['LatentState', 'Observation']]\n</code></pre> <p>Execute a batch of step requests under a forward context.</p> <p>Per-session overrides on the first request (the safe subset) are overlaid onto the runner defaults for the duration of the batch.</p> Source code in <code>worldkernels/worker/world_runner.py</code> <pre><code>def run_batch(\n    self,\n    requests: list[\"StepRequest\"],\n) -&gt; list[tuple[\"LatentState\", \"Observation\"]]:\n    r\"\"\"Execute a batch of step requests under a forward context.\n\n    Per-session overrides on the first request (the safe subset) are\n    overlaid onto the runner defaults for the duration of the batch.\n    \"\"\"\n    overrides = (requests[0].overrides or {}) if requests else {}\n\n    teacache_on = overrides.get(\"teacache\", True)\n    cache_backend = self.teacache if teacache_on else None\n\n    attn = overrides.get(\"attention_backend\") or self._default_attention_backend()\n    attn = None if attn == \"auto\" else attn\n\n    iter_batch = overrides.get(\"iteration_batching\")\n    if iter_batch is None:\n        iter_batch = bool(self.config and self.config.iteration_batching)\n\n    ctx = ForwardContext(\n        cache_backend=cache_backend,\n        pool=self.pool,\n        attention_backend=attn,\n        iteration_batching=iter_batch,\n    )\n    with set_forward_context(ctx):\n        return self.executor.execute_batched(requests)\n</code></pre>"},{"location":"api/worldkernels/worker/world_runner/#worldkernels.worker.world_runner.WorldRunner._default_attention_backend","title":"_default_attention_backend","text":"<pre><code>_default_attention_backend() -&gt; str | None\n</code></pre> Source code in <code>worldkernels/worker/world_runner.py</code> <pre><code>def _default_attention_backend(self) -&gt; str | None:\n    if self.config is None:\n        return None\n    return self.config.attention_backend\n</code></pre>"},{"location":"api/worldkernels/worlds/","title":"worlds","text":""},{"location":"api/worldkernels/worlds/#worldkernels.worlds","title":"worldkernels.worlds","text":"<p>World-model taxonomy, registry, and hub.</p> <p>Modules:</p> Name Description <code>base</code> <p>World-model taxonomy.</p> <code>dreamdojo</code> <p>DreamDojo \u2014 an action-conditioned interactive world model, natively owned.</p> <code>dummy</code> <p>DummyWorld \u2014 a CPU-safe world model for development and testing.</p> <code>hub</code> <p>Model hub: maps HF repo IDs and short aliases to world models and generators.</p> <code>registry</code> <p>World model registry with built-in adapters and entry_points plugin discovery.</p> <code>remote</code> <p>Out-of-process world proxy for isolated models.</p> <code>rollout</code> <p>Rollout policies for presenting a one-shot video generator as a world model.</p>"},{"location":"api/worldkernels/worlds/#worldkernels.worlds.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WorldModel', 'InteractiveWorldModel', 'GeneratorWorld', 'AbstractWorld', 'get_world_class', 'list_worlds', 'register_world']\n</code></pre>"},{"location":"api/worldkernels/worlds/#worldkernels.worlds._LAZY_IMPORTS","title":"_LAZY_IMPORTS  <code>module-attribute</code>","text":"<pre><code>_LAZY_IMPORTS: dict[str, tuple[str, str]] = {'WorldModel': ('worldkernels.worlds.base', 'WorldModel'), 'InteractiveWorldModel': ('worldkernels.worlds.base', 'InteractiveWorldModel'), 'GeneratorWorld': ('worldkernels.worlds.base', 'GeneratorWorld'), 'AbstractWorld': ('worldkernels.worlds.base', 'AbstractWorld'), 'get_world_class': ('worldkernels.worlds.registry', 'get_world_class'), 'list_worlds': ('worldkernels.worlds.registry', 'list_worlds'), 'register_world': ('worldkernels.worlds.registry', 'register_world')}\n</code></pre>"},{"location":"api/worldkernels/worlds/#worldkernels.worlds.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str)\n</code></pre> Source code in <code>worldkernels/worlds/__init__.py</code> <pre><code>def __getattr__(name: str):\n    if name in _LAZY_IMPORTS:\n        module_path, attr = _LAZY_IMPORTS[name]\n        mod = _importlib.import_module(module_path)\n        val = getattr(mod, attr)\n        globals()[name] = val\n        return val\n    raise AttributeError(f\"module 'worldkernels.worlds' has no attribute {name!r}\")\n</code></pre>"},{"location":"api/worldkernels/worlds/base/","title":"base","text":""},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base","title":"worldkernels.worlds.base","text":"<p>World-model taxonomy.</p> <p>A <code>WorldModel</code> is the interactive, stateful, session-bound layer the engine schedules: <code>encode_action -&gt; transition -&gt; decode_observation</code>.</p> <p>Two concrete kinds resolve the world-model vs video-generator distinction:</p> <ul> <li><code>InteractiveWorldModel</code> \u2014 a true action-conditioned world model   whose transition is a learned function of the action and causal history.</li> <li><code>GeneratorWorld</code> \u2014 a one-shot <code>VideoGenerator</code>   presented as a world model via an explicit, swappable   <code>RolloutPolicy</code>. Its rollout is open-loop   resampling, not a learned transition, and it says so in its metadata.</li> </ul>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['WorldModel', 'InteractiveWorldModel', 'GeneratorWorld', 'AbstractWorld']\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.AbstractWorld","title":"AbstractWorld  <code>module-attribute</code>","text":"<pre><code>AbstractWorld = WorldModel\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel","title":"WorldModel","text":"<p>               Bases: <code>ABC</code></p> <p>Interactive, stage-decomposed world model.</p> <p>Subclasses implement the three stage methods plus <code>profile_vram</code> and <code>create_initial_state</code>.</p> Class metadata <ul> <li><code>transition_mode</code>: bidirectional / causal / hybrid attention regime.</li> <li><code>supports_streaming</code>: incremental frame emission.</li> <li><code>supports_kv_cache</code>: KV caching in the transition stage.</li> <li><code>supports_iteration_batching</code>: exposes <code>transition_iter()</code> so the   scheduler can join sessions into a batch mid-denoise.</li> <li><code>rollout_kind</code>: <code>\"learned_transition\"</code> for a true world model,   <code>\"open_loop_resample\"</code> for a wrapped generator.</li> </ul> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>class WorldModel(ABC):\n    r\"\"\"Interactive, stage-decomposed world model.\n\n    Subclasses implement the three stage methods plus ``profile_vram`` and\n    ``create_initial_state``.\n\n    Class metadata:\n      - ``transition_mode``: bidirectional / causal / hybrid attention regime.\n      - ``supports_streaming``: incremental frame emission.\n      - ``supports_kv_cache``: KV caching in the transition stage.\n      - ``supports_iteration_batching``: exposes `transition_iter()` so the\n        scheduler can join sessions into a batch mid-denoise.\n      - ``rollout_kind``: ``\"learned_transition\"`` for a true world model,\n        ``\"open_loop_resample\"`` for a wrapped generator.\n    \"\"\"\n\n    name: str = \"\"\n    default_config: WorldConfig | None = None\n\n    stage_exec_modes: dict[StageType, StageExecMode] = {\n        StageType.ENCODE: StageExecMode.SINGLE_SHOT,\n        StageType.TRANSITION: StageExecMode.ITERATIVE,\n        StageType.DECODE: StageExecMode.SINGLE_SHOT,\n    }\n\n    transition_mode: TransitionMode = TransitionMode.BIDIRECTIONAL\n    supports_streaming: bool = False\n    supports_kv_cache: bool = False\n    supports_iteration_batching: bool = False\n    rollout_kind: str = \"learned_transition\"\n\n    @abstractmethod\n    def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n        r\"\"\"Load weights, allocate buffers, move to device.\"\"\"\n\n    def warmup(self, config: WorldConfig) -&gt; None:\n        r\"\"\"Optional dummy forward pass to warm caches and JIT-compile.\"\"\"\n\n    @abstractmethod\n    def encode_action(self, action: Action) -&gt; torch.Tensor:\n        r\"\"\"Convert a typed action into a conditioning tensor.\"\"\"\n\n    @abstractmethod\n    def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n        r\"\"\"Advance the world by one step. Core compute hot path.\"\"\"\n\n    def transition_iter(\n        self,\n        state: LatentState,\n        action_encoded: torch.Tensor,\n    ) -&gt; Iterator[LatentState]:\n        r\"\"\"Yield intermediate states across the transition's inner loop.\n\n        Optional: implemented only by worlds that set\n        ``supports_iteration_batching = True``. Lets the scheduler drive the\n        denoise loop step-by-step so sessions can join a batch mid-flight.\n        \"\"\"\n        raise NotImplementedError(\n            f\"{type(self).__name__} does not support iteration-level batching\"\n        )\n\n    @abstractmethod\n    def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n        r\"\"\"Decode latent state into the requested observation modalities.\"\"\"\n\n    def profile_vram(self, config: WorldConfig) -&gt; float:\n        r\"\"\"Estimate per-session VRAM (MB). Subclasses override either this\n        or the legacy ``estimate_vram_mb``.\"\"\"\n        return self.estimate_vram_mb(config)\n\n    def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n        return self.profile_vram(config)\n\n    @abstractmethod\n    def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n        r\"\"\"Create the initial latent state for a new session.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = ''\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.default_config","title":"default_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.stage_exec_modes","title":"stage_exec_modes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_exec_modes: dict[StageType, StageExecMode] = {StageType.ENCODE: StageExecMode.SINGLE_SHOT, StageType.TRANSITION: StageExecMode.ITERATIVE, StageType.DECODE: StageExecMode.SINGLE_SHOT}\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.transition_mode","title":"transition_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_mode: TransitionMode = TransitionMode.BIDIRECTIONAL\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.supports_streaming","title":"supports_streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_streaming: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.supports_kv_cache","title":"supports_kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_kv_cache: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.supports_iteration_batching","title":"supports_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_iteration_batching: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.rollout_kind","title":"rollout_kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rollout_kind: str = 'learned_transition'\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.initialize","title":"initialize  <code>abstractmethod</code>","text":"<pre><code>initialize(device: str, dtype: dtype) -&gt; None\n</code></pre> <p>Load weights, allocate buffers, move to device.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n    r\"\"\"Load weights, allocate buffers, move to device.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.warmup","title":"warmup","text":"<pre><code>warmup(config: WorldConfig) -&gt; None\n</code></pre> <p>Optional dummy forward pass to warm caches and JIT-compile.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def warmup(self, config: WorldConfig) -&gt; None:\n    r\"\"\"Optional dummy forward pass to warm caches and JIT-compile.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.encode_action","title":"encode_action  <code>abstractmethod</code>","text":"<pre><code>encode_action(action: Action) -&gt; Tensor\n</code></pre> <p>Convert a typed action into a conditioning tensor.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef encode_action(self, action: Action) -&gt; torch.Tensor:\n    r\"\"\"Convert a typed action into a conditioning tensor.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.transition","title":"transition  <code>abstractmethod</code>","text":"<pre><code>transition(state: LatentState, action_encoded: Tensor) -&gt; LatentState\n</code></pre> <p>Advance the world by one step. Core compute hot path.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n    r\"\"\"Advance the world by one step. Core compute hot path.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.transition_iter","title":"transition_iter","text":"<pre><code>transition_iter(state: LatentState, action_encoded: Tensor) -&gt; Iterator[LatentState]\n</code></pre> <p>Yield intermediate states across the transition's inner loop.</p> <p>Optional: implemented only by worlds that set <code>supports_iteration_batching = True</code>. Lets the scheduler drive the denoise loop step-by-step so sessions can join a batch mid-flight.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def transition_iter(\n    self,\n    state: LatentState,\n    action_encoded: torch.Tensor,\n) -&gt; Iterator[LatentState]:\n    r\"\"\"Yield intermediate states across the transition's inner loop.\n\n    Optional: implemented only by worlds that set\n    ``supports_iteration_batching = True``. Lets the scheduler drive the\n    denoise loop step-by-step so sessions can join a batch mid-flight.\n    \"\"\"\n    raise NotImplementedError(\n        f\"{type(self).__name__} does not support iteration-level batching\"\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.decode_observation","title":"decode_observation  <code>abstractmethod</code>","text":"<pre><code>decode_observation(state: LatentState, modalities: list[str]) -&gt; Observation\n</code></pre> <p>Decode latent state into the requested observation modalities.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n    r\"\"\"Decode latent state into the requested observation modalities.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(config: WorldConfig) -&gt; float\n</code></pre> <p>Estimate per-session VRAM (MB). Subclasses override either this or the legacy <code>estimate_vram_mb</code>.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def profile_vram(self, config: WorldConfig) -&gt; float:\n    r\"\"\"Estimate per-session VRAM (MB). Subclasses override either this\n    or the legacy ``estimate_vram_mb``.\"\"\"\n    return self.estimate_vram_mb(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.estimate_vram_mb","title":"estimate_vram_mb","text":"<pre><code>estimate_vram_mb(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n    return self.profile_vram(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.WorldModel.create_initial_state","title":"create_initial_state  <code>abstractmethod</code>","text":"<pre><code>create_initial_state(config: WorldConfig, seed: int) -&gt; LatentState\n</code></pre> <p>Create the initial latent state for a new session.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n    r\"\"\"Create the initial latent state for a new session.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel","title":"InteractiveWorldModel","text":"<p>               Bases: <code>WorldModel</code></p> <p>A true action-conditioned world model.</p> <p>The transition is a learned function of the action and causal history. Marker base for genuine simulators (e.g. DreamDojo), distinct from <code>GeneratorWorld</code>.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>class InteractiveWorldModel(WorldModel):\n    r\"\"\"A true action-conditioned world model.\n\n    The transition is a learned function of the action and causal history.\n    Marker base for genuine simulators (e.g. DreamDojo), distinct from\n    `GeneratorWorld`.\n    \"\"\"\n\n    rollout_kind: str = \"learned_transition\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.rollout_kind","title":"rollout_kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rollout_kind: str = 'learned_transition'\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = ''\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.default_config","title":"default_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.stage_exec_modes","title":"stage_exec_modes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_exec_modes: dict[StageType, StageExecMode] = {StageType.ENCODE: StageExecMode.SINGLE_SHOT, StageType.TRANSITION: StageExecMode.ITERATIVE, StageType.DECODE: StageExecMode.SINGLE_SHOT}\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.transition_mode","title":"transition_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_mode: TransitionMode = TransitionMode.BIDIRECTIONAL\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.supports_streaming","title":"supports_streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_streaming: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.supports_kv_cache","title":"supports_kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_kv_cache: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.supports_iteration_batching","title":"supports_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_iteration_batching: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.initialize","title":"initialize  <code>abstractmethod</code>","text":"<pre><code>initialize(device: str, dtype: dtype) -&gt; None\n</code></pre> <p>Load weights, allocate buffers, move to device.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n    r\"\"\"Load weights, allocate buffers, move to device.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.warmup","title":"warmup","text":"<pre><code>warmup(config: WorldConfig) -&gt; None\n</code></pre> <p>Optional dummy forward pass to warm caches and JIT-compile.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def warmup(self, config: WorldConfig) -&gt; None:\n    r\"\"\"Optional dummy forward pass to warm caches and JIT-compile.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.encode_action","title":"encode_action  <code>abstractmethod</code>","text":"<pre><code>encode_action(action: Action) -&gt; Tensor\n</code></pre> <p>Convert a typed action into a conditioning tensor.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef encode_action(self, action: Action) -&gt; torch.Tensor:\n    r\"\"\"Convert a typed action into a conditioning tensor.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.transition","title":"transition  <code>abstractmethod</code>","text":"<pre><code>transition(state: LatentState, action_encoded: Tensor) -&gt; LatentState\n</code></pre> <p>Advance the world by one step. Core compute hot path.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n    r\"\"\"Advance the world by one step. Core compute hot path.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.transition_iter","title":"transition_iter","text":"<pre><code>transition_iter(state: LatentState, action_encoded: Tensor) -&gt; Iterator[LatentState]\n</code></pre> <p>Yield intermediate states across the transition's inner loop.</p> <p>Optional: implemented only by worlds that set <code>supports_iteration_batching = True</code>. Lets the scheduler drive the denoise loop step-by-step so sessions can join a batch mid-flight.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def transition_iter(\n    self,\n    state: LatentState,\n    action_encoded: torch.Tensor,\n) -&gt; Iterator[LatentState]:\n    r\"\"\"Yield intermediate states across the transition's inner loop.\n\n    Optional: implemented only by worlds that set\n    ``supports_iteration_batching = True``. Lets the scheduler drive the\n    denoise loop step-by-step so sessions can join a batch mid-flight.\n    \"\"\"\n    raise NotImplementedError(\n        f\"{type(self).__name__} does not support iteration-level batching\"\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.decode_observation","title":"decode_observation  <code>abstractmethod</code>","text":"<pre><code>decode_observation(state: LatentState, modalities: list[str]) -&gt; Observation\n</code></pre> <p>Decode latent state into the requested observation modalities.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n    r\"\"\"Decode latent state into the requested observation modalities.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(config: WorldConfig) -&gt; float\n</code></pre> <p>Estimate per-session VRAM (MB). Subclasses override either this or the legacy <code>estimate_vram_mb</code>.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def profile_vram(self, config: WorldConfig) -&gt; float:\n    r\"\"\"Estimate per-session VRAM (MB). Subclasses override either this\n    or the legacy ``estimate_vram_mb``.\"\"\"\n    return self.estimate_vram_mb(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.estimate_vram_mb","title":"estimate_vram_mb","text":"<pre><code>estimate_vram_mb(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n    return self.profile_vram(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.InteractiveWorldModel.create_initial_state","title":"create_initial_state  <code>abstractmethod</code>","text":"<pre><code>create_initial_state(config: WorldConfig, seed: int) -&gt; LatentState\n</code></pre> <p>Create the initial latent state for a new session.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@abstractmethod\ndef create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n    r\"\"\"Create the initial latent state for a new session.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState","title":"_GeneratorState  <code>dataclass</code>","text":"<p>Per-session state for a <code>GeneratorWorld</code>.</p> <p>Holds the generator's opaque conditioning plus the most recent latent and decoded clip, so <code>decode_observation</code> needs no recompute.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>@dataclass\nclass _GeneratorState:\n    r\"\"\"Per-session state for a `GeneratorWorld`.\n\n    Holds the generator's opaque conditioning plus the most recent latent and\n    decoded clip, so ``decode_observation`` needs no recompute.\n    \"\"\"\n\n    conditioning: Any\n    latent: torch.Tensor | None = None\n    video: torch.Tensor | None = None\n\n    def clone(self) -&gt; \"_GeneratorState\":\n        return _GeneratorState(\n            conditioning=self.conditioning.clone(),\n            latent=None if self.latent is None else self.latent.clone(),\n            video=None if self.video is None else self.video.clone(),\n        )\n\n    def to(self, device: Any) -&gt; \"_GeneratorState\":\n        return _GeneratorState(\n            conditioning=self.conditioning.to(device),\n            latent=None if self.latent is None else self.latent.to(device),\n            video=None if self.video is None else self.video.to(device),\n        )\n\n    @property\n    def nelement(self) -&gt; int:\n        n = getattr(self.conditioning, \"nelement\", 0)\n        n = n if isinstance(n, int) else n\n        for t in (self.latent, self.video):\n            if t is not None:\n                n += t.nelement()\n        return n\n\n    @property\n    def element_size(self) -&gt; int:\n        return 2\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.conditioning","title":"conditioning  <code>instance-attribute</code>","text":"<pre><code>conditioning: Any\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.latent","title":"latent  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>latent: Tensor | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.video","title":"video  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>video: Tensor | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.nelement","title":"nelement  <code>property</code>","text":"<pre><code>nelement: int\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.element_size","title":"element_size  <code>property</code>","text":"<pre><code>element_size: int\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.__init__","title":"__init__","text":"<pre><code>__init__(conditioning: Any, latent: Tensor | None = None, video: Tensor | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.clone","title":"clone","text":"<pre><code>clone() -&gt; '_GeneratorState'\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def clone(self) -&gt; \"_GeneratorState\":\n    return _GeneratorState(\n        conditioning=self.conditioning.clone(),\n        latent=None if self.latent is None else self.latent.clone(),\n        video=None if self.video is None else self.video.clone(),\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base._GeneratorState.to","title":"to","text":"<pre><code>to(device: Any) -&gt; '_GeneratorState'\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def to(self, device: Any) -&gt; \"_GeneratorState\":\n    return _GeneratorState(\n        conditioning=self.conditioning.to(device),\n        latent=None if self.latent is None else self.latent.to(device),\n        video=None if self.video is None else self.video.to(device),\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld","title":"GeneratorWorld","text":"<p>               Bases: <code>WorldModel</code></p> <p>A one-shot video generator presented as an interactive world model.</p> <p>Wraps a <code>VideoGenerator</code> and advances it with an explicit <code>RolloutPolicy</code>. The rollout is open-loop resampling \u2014 the generator has no learned memory of the action history beyond the single carried frame \u2014 and the world's metadata says so. Actions are text prompts.</p> <p>Parameters:</p> Name Type Description Default <code>generator</code> <code>str</code> <p>Pipeline-registry key of the wrapped video generator.</p> required <code>rollout</code> <code>str</code> <p>Rollout-policy name (currently <code>\"last_frame\"</code>).</p> <code>'last_frame'</code> <code>num_inference_steps</code> <code>int</code> <p>Denoising steps per generation.</p> <code>40</code> <code>guidance_scale</code> <code>float</code> <p>Classifier-free guidance scale.</p> <code>5.0</code> <code>num_frames</code> <code>int</code> <p>Frames generated per step.</p> <code>81</code> <code>negative_prompt</code> <code>str</code> <p>Negative prompt held for CFG.</p> <code>''</code> <code>**generator_kwargs</code> <code>Any</code> <p>Forwarded to the generator pipeline's constructor.</p> <code>{}</code> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>class GeneratorWorld(WorldModel):\n    r\"\"\"A one-shot video generator presented as an interactive world model.\n\n    Wraps a `VideoGenerator` and advances it\n    with an explicit `RolloutPolicy`. The\n    rollout is open-loop resampling \u2014 the generator has no learned memory of\n    the action history beyond the single carried frame \u2014 and the world's\n    metadata says so. Actions are text prompts.\n\n    Args:\n        generator: Pipeline-registry key of the wrapped video generator.\n        rollout: Rollout-policy name (currently ``\"last_frame\"``).\n        num_inference_steps: Denoising steps per generation.\n        guidance_scale: Classifier-free guidance scale.\n        num_frames: Frames generated per step.\n        negative_prompt: Negative prompt held for CFG.\n        **generator_kwargs: Forwarded to the generator pipeline's constructor.\n    \"\"\"\n\n    transition_mode = TransitionMode.BIDIRECTIONAL\n    supports_streaming = False\n    supports_kv_cache = False\n    supports_iteration_batching = False\n    rollout_kind = \"open_loop_resample\"\n\n    _ROLLOUTS = {\"last_frame\": \"LastFrameRolloutPolicy\"}\n\n    def __init__(\n        self,\n        *,\n        generator: str,\n        rollout: str = \"last_frame\",\n        num_inference_steps: int = 40,\n        guidance_scale: float = 5.0,\n        num_frames: int = 81,\n        negative_prompt: str = \"\",\n        **generator_kwargs: Any,\n    ) -&gt; None:\n        if rollout not in self._ROLLOUTS:\n            raise ValueError(\n                f\"unknown rollout policy {rollout!r}; available: {sorted(self._ROLLOUTS)}\"\n            )\n        self.name = f\"generator:{generator}\"\n        self._generator_key = generator\n        self._generator_kwargs = generator_kwargs\n        self._rollout_name = rollout\n        self.num_inference_steps = num_inference_steps\n        self.guidance_scale = guidance_scale\n        self.num_frames = num_frames\n        self.negative_prompt = negative_prompt\n        self.device: str = \"cpu\"\n        self.dtype: torch.dtype = torch.float32\n        self.generator: VideoGenerator | None = None\n        self.rollout: RolloutPolicy | None = None\n        self._warned = False\n\n    def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n        from worldkernels.models import get_pipeline_class\n        from worldkernels.worlds import rollout as rollout_mod\n\n        self.device = device\n        self.dtype = dtype\n        self.rollout = getattr(rollout_mod, self._ROLLOUTS[self._rollout_name])()\n        generator_cls = get_pipeline_class(self._generator_key)\n        self.generator = generator_cls(**self._generator_kwargs)\n        self.generator.load(device, dtype)\n\n    def _warn_once(self) -&gt; None:\n        if not self._warned:\n            log.warning(\n                \"'%s' is a video generator presented as a world model via the '%s' \"\n                \"rollout policy (rollout_kind=%s); each step is open-loop resampling, \"\n                \"not a learned state transition.\",\n                self.name,\n                self.rollout.name if self.rollout else self._rollout_name,\n                self.rollout_kind,\n            )\n            self._warned = True\n\n    def encode_action(self, action: Action) -&gt; torch.Tensor:\n        prompt = action.payload.get(\"prompt\", \"\") if action.payload else \"\"\n        if not prompt or self.generator is None:\n            return torch.empty(0, device=self.device)\n        return self.generator.encode_prompt(prompt)\n\n    def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n        from worldkernels.core.session import LatentState as _LatentState\n\n        assert self.generator is not None and self.rollout is not None\n        gen_state: _GeneratorState = state.data\n        conditioning = gen_state.conditioning\n        if action_encoded.numel() &gt; 0:\n            conditioning = self.generator.apply_prompt(conditioning, action_encoded)\n\n        import time\n\n        result: GenerationResult = self.generator.generate(\n            conditioning,\n            num_steps=self.num_inference_steps,\n            guidance=self.guidance_scale,\n            num_frames=self.num_frames,\n            seed=int(time.perf_counter() * 1000) % (2**31),\n        )\n        next_image = self.rollout.next_image_cond(result.video)\n        next_conditioning = self.generator.advance(conditioning, next_image)\n        return _LatentState(\n            data=_GeneratorState(next_conditioning, result.latent, result.video),\n            device=state.device,\n        )\n\n    def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n        import time\n\n        from worldkernels.core.observation import Observation\n\n        t0 = time.perf_counter()\n        gen_state: _GeneratorState = state.data\n        frames = None\n        latent_out = None\n\n        if \"frames\" in modalities and gen_state.video is not None:\n            video_uint8 = ((gen_state.video + 1.0) * 0.5).clamp(0, 1).mul(255).to(torch.uint8)\n            frames = [\n                video_uint8[0, :, t].permute(1, 2, 0).cpu().numpy().tobytes()\n                for t in range(video_uint8.shape[2])\n            ]\n        if \"latent\" in modalities:\n            latent_out = gen_state.latent\n\n        return Observation(\n            step_index=0,\n            generation_time_ms=(time.perf_counter() - t0) * 1000.0,\n            frames=frames,\n            latent=latent_out,\n        )\n\n    def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n        from worldkernels.core.session import LatentState as _LatentState\n\n        assert self.generator is not None, \"Generator not loaded \u2014 call initialize() first\"\n        self._warn_once()\n        conditioning = self.generator.initial_conditioning(\n            prompt=config.initial_prompt or \"\",\n            negative_prompt=self.negative_prompt,\n            image=config.initial_image,\n            height=config.height,\n            width=config.width,\n            frames_per_step=config.frames_per_step,\n            seed=seed,\n        )\n        return _LatentState(data=_GeneratorState(conditioning), device=self.device)\n\n    def profile_vram(self, config: WorldConfig) -&gt; float:\n        if self.generator is None:\n            return 1024.0\n        return self.generator.profile_vram(\n            height=config.height, width=config.width, num_frames=self.num_frames\n        )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.transition_mode","title":"transition_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_mode = TransitionMode.BIDIRECTIONAL\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.supports_streaming","title":"supports_streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_streaming = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.supports_kv_cache","title":"supports_kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_kv_cache = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.supports_iteration_batching","title":"supports_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_iteration_batching = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.rollout_kind","title":"rollout_kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rollout_kind = 'open_loop_resample'\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld._ROLLOUTS","title":"_ROLLOUTS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>_ROLLOUTS = {'last_frame': 'LastFrameRolloutPolicy'}\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name = f'generator:{generator}'\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld._generator_key","title":"_generator_key  <code>instance-attribute</code>","text":"<pre><code>_generator_key = generator\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld._generator_kwargs","title":"_generator_kwargs  <code>instance-attribute</code>","text":"<pre><code>_generator_kwargs = generator_kwargs\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld._rollout_name","title":"_rollout_name  <code>instance-attribute</code>","text":"<pre><code>_rollout_name = rollout\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.num_inference_steps","title":"num_inference_steps  <code>instance-attribute</code>","text":"<pre><code>num_inference_steps = num_inference_steps\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.guidance_scale","title":"guidance_scale  <code>instance-attribute</code>","text":"<pre><code>guidance_scale = guidance_scale\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.num_frames","title":"num_frames  <code>instance-attribute</code>","text":"<pre><code>num_frames = num_frames\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.negative_prompt","title":"negative_prompt  <code>instance-attribute</code>","text":"<pre><code>negative_prompt = negative_prompt\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: dtype = torch.float32\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.generator","title":"generator  <code>instance-attribute</code>","text":"<pre><code>generator: VideoGenerator | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.rollout","title":"rollout  <code>instance-attribute</code>","text":"<pre><code>rollout: RolloutPolicy | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld._warned","title":"_warned  <code>instance-attribute</code>","text":"<pre><code>_warned = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.default_config","title":"default_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.stage_exec_modes","title":"stage_exec_modes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_exec_modes: dict[StageType, StageExecMode] = {StageType.ENCODE: StageExecMode.SINGLE_SHOT, StageType.TRANSITION: StageExecMode.ITERATIVE, StageType.DECODE: StageExecMode.SINGLE_SHOT}\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.__init__","title":"__init__","text":"<pre><code>__init__(*, generator: str, rollout: str = 'last_frame', num_inference_steps: int = 40, guidance_scale: float = 5.0, num_frames: int = 81, negative_prompt: str = '', **generator_kwargs: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def __init__(\n    self,\n    *,\n    generator: str,\n    rollout: str = \"last_frame\",\n    num_inference_steps: int = 40,\n    guidance_scale: float = 5.0,\n    num_frames: int = 81,\n    negative_prompt: str = \"\",\n    **generator_kwargs: Any,\n) -&gt; None:\n    if rollout not in self._ROLLOUTS:\n        raise ValueError(\n            f\"unknown rollout policy {rollout!r}; available: {sorted(self._ROLLOUTS)}\"\n        )\n    self.name = f\"generator:{generator}\"\n    self._generator_key = generator\n    self._generator_kwargs = generator_kwargs\n    self._rollout_name = rollout\n    self.num_inference_steps = num_inference_steps\n    self.guidance_scale = guidance_scale\n    self.num_frames = num_frames\n    self.negative_prompt = negative_prompt\n    self.device: str = \"cpu\"\n    self.dtype: torch.dtype = torch.float32\n    self.generator: VideoGenerator | None = None\n    self.rollout: RolloutPolicy | None = None\n    self._warned = False\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.initialize","title":"initialize","text":"<pre><code>initialize(device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n    from worldkernels.models import get_pipeline_class\n    from worldkernels.worlds import rollout as rollout_mod\n\n    self.device = device\n    self.dtype = dtype\n    self.rollout = getattr(rollout_mod, self._ROLLOUTS[self._rollout_name])()\n    generator_cls = get_pipeline_class(self._generator_key)\n    self.generator = generator_cls(**self._generator_kwargs)\n    self.generator.load(device, dtype)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld._warn_once","title":"_warn_once","text":"<pre><code>_warn_once() -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def _warn_once(self) -&gt; None:\n    if not self._warned:\n        log.warning(\n            \"'%s' is a video generator presented as a world model via the '%s' \"\n            \"rollout policy (rollout_kind=%s); each step is open-loop resampling, \"\n            \"not a learned state transition.\",\n            self.name,\n            self.rollout.name if self.rollout else self._rollout_name,\n            self.rollout_kind,\n        )\n        self._warned = True\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.encode_action","title":"encode_action","text":"<pre><code>encode_action(action: Action) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def encode_action(self, action: Action) -&gt; torch.Tensor:\n    prompt = action.payload.get(\"prompt\", \"\") if action.payload else \"\"\n    if not prompt or self.generator is None:\n        return torch.empty(0, device=self.device)\n    return self.generator.encode_prompt(prompt)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.transition","title":"transition","text":"<pre><code>transition(state: LatentState, action_encoded: Tensor) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n    from worldkernels.core.session import LatentState as _LatentState\n\n    assert self.generator is not None and self.rollout is not None\n    gen_state: _GeneratorState = state.data\n    conditioning = gen_state.conditioning\n    if action_encoded.numel() &gt; 0:\n        conditioning = self.generator.apply_prompt(conditioning, action_encoded)\n\n    import time\n\n    result: GenerationResult = self.generator.generate(\n        conditioning,\n        num_steps=self.num_inference_steps,\n        guidance=self.guidance_scale,\n        num_frames=self.num_frames,\n        seed=int(time.perf_counter() * 1000) % (2**31),\n    )\n    next_image = self.rollout.next_image_cond(result.video)\n    next_conditioning = self.generator.advance(conditioning, next_image)\n    return _LatentState(\n        data=_GeneratorState(next_conditioning, result.latent, result.video),\n        device=state.device,\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.decode_observation","title":"decode_observation","text":"<pre><code>decode_observation(state: LatentState, modalities: list[str]) -&gt; Observation\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n    import time\n\n    from worldkernels.core.observation import Observation\n\n    t0 = time.perf_counter()\n    gen_state: _GeneratorState = state.data\n    frames = None\n    latent_out = None\n\n    if \"frames\" in modalities and gen_state.video is not None:\n        video_uint8 = ((gen_state.video + 1.0) * 0.5).clamp(0, 1).mul(255).to(torch.uint8)\n        frames = [\n            video_uint8[0, :, t].permute(1, 2, 0).cpu().numpy().tobytes()\n            for t in range(video_uint8.shape[2])\n        ]\n    if \"latent\" in modalities:\n        latent_out = gen_state.latent\n\n    return Observation(\n        step_index=0,\n        generation_time_ms=(time.perf_counter() - t0) * 1000.0,\n        frames=frames,\n        latent=latent_out,\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.create_initial_state","title":"create_initial_state","text":"<pre><code>create_initial_state(config: WorldConfig, seed: int) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n    from worldkernels.core.session import LatentState as _LatentState\n\n    assert self.generator is not None, \"Generator not loaded \u2014 call initialize() first\"\n    self._warn_once()\n    conditioning = self.generator.initial_conditioning(\n        prompt=config.initial_prompt or \"\",\n        negative_prompt=self.negative_prompt,\n        image=config.initial_image,\n        height=config.height,\n        width=config.width,\n        frames_per_step=config.frames_per_step,\n        seed=seed,\n    )\n    return _LatentState(data=_GeneratorState(conditioning), device=self.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def profile_vram(self, config: WorldConfig) -&gt; float:\n    if self.generator is None:\n        return 1024.0\n    return self.generator.profile_vram(\n        height=config.height, width=config.width, num_frames=self.num_frames\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.warmup","title":"warmup","text":"<pre><code>warmup(config: WorldConfig) -&gt; None\n</code></pre> <p>Optional dummy forward pass to warm caches and JIT-compile.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def warmup(self, config: WorldConfig) -&gt; None:\n    r\"\"\"Optional dummy forward pass to warm caches and JIT-compile.\"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.transition_iter","title":"transition_iter","text":"<pre><code>transition_iter(state: LatentState, action_encoded: Tensor) -&gt; Iterator[LatentState]\n</code></pre> <p>Yield intermediate states across the transition's inner loop.</p> <p>Optional: implemented only by worlds that set <code>supports_iteration_batching = True</code>. Lets the scheduler drive the denoise loop step-by-step so sessions can join a batch mid-flight.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def transition_iter(\n    self,\n    state: LatentState,\n    action_encoded: torch.Tensor,\n) -&gt; Iterator[LatentState]:\n    r\"\"\"Yield intermediate states across the transition's inner loop.\n\n    Optional: implemented only by worlds that set\n    ``supports_iteration_batching = True``. Lets the scheduler drive the\n    denoise loop step-by-step so sessions can join a batch mid-flight.\n    \"\"\"\n    raise NotImplementedError(\n        f\"{type(self).__name__} does not support iteration-level batching\"\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/base/#worldkernels.worlds.base.GeneratorWorld.estimate_vram_mb","title":"estimate_vram_mb","text":"<pre><code>estimate_vram_mb(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n    return self.profile_vram(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/","title":"dreamdojo","text":""},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo","title":"worldkernels.worlds.dreamdojo","text":"<p>DreamDojo \u2014 an action-conditioned interactive world model, natively owned.</p>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DEFAULT_VARIANT","title":"DEFAULT_VARIANT  <code>module-attribute</code>","text":"<pre><code>DEFAULT_VARIANT = '2b_pretrain'\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.ACTION_DIM","title":"ACTION_DIM  <code>module-attribute</code>","text":"<pre><code>ACTION_DIM = 384\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld","title":"DreamDojoWorld","text":"<p>               Bases: <code>InteractiveWorldModel</code></p> <p>Action-conditioned video world model (DreamDojo 2B/14B, all released variants).</p> <p>Actions are relative robot joint chunks \\(\\in \\mathbb{R}^{12 \\times 384}\\) in the universal DreamDojo action layout. Each <code>transition</code> generates one 13-frame clip via the native <code>DreamDojoPipeline</code> and carries the last decoded frame as the next conditioning prefix.</p> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>class DreamDojoWorld(InteractiveWorldModel):\n    r\"\"\"Action-conditioned video world model (DreamDojo 2B/14B, all released variants).\n\n    Actions are relative robot joint chunks \\(\\in \\mathbb{R}^{12 \\times 384}\\)\n    in the universal DreamDojo action layout. Each `transition` generates one\n    13-frame clip via the native `DreamDojoPipeline`\n    and carries the last decoded frame as the next conditioning prefix.\n    \"\"\"\n\n    name = \"dreamdojo\"\n    stage_exec_modes = {\n        StageType.ENCODE: StageExecMode.SINGLE_SHOT,\n        StageType.TRANSITION: StageExecMode.ITERATIVE,\n        StageType.DECODE: StageExecMode.SINGLE_SHOT,\n    }\n    transition_mode = TransitionMode.BIDIRECTIONAL\n    supports_streaming = False\n    supports_kv_cache = False\n\n    def __init__(\n        self,\n        ckpt_path: str | None = None,\n        variant: str = DEFAULT_VARIANT,\n        action_dim: int = ACTION_DIM,\n        chunk_size: int = 12,\n        num_inference_steps: int = 35,\n        guidance_scale: float = 0.0,\n        **kwargs: Any,\n    ) -&gt; None:\n        if variant not in CKPT_DIRS:\n            raise ValueError(f\"unknown DreamDojo variant {variant!r}; known: {sorted(CKPT_DIRS)}\")\n        self.ckpt_path = ckpt_path\n        self.variant = variant\n        self.action_dim = action_dim\n        self.chunk_size = chunk_size\n        self.num_inference_steps = num_inference_steps\n        self.guidance_scale = guidance_scale\n        self.device: str = \"cpu\"\n        self.dtype: torch.dtype = torch.float32\n        self.pipeline: DreamDojoPipeline | None = None\n\n    def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n        self.device = device\n        self.dtype = dtype\n        self.pipeline = DreamDojoPipeline(\n            self.variant,\n            num_steps=self.num_inference_steps,\n            guidance=self.guidance_scale,\n        ).load(device, dtype, self.ckpt_path)\n\n    def _geometry(self, config: WorldConfig) -&gt; tuple[int, int, int]:\n        r\"\"\"Snap the generic WorldConfig defaults to DreamDojo's native 480x640.\"\"\"\n        height = NATIVE_HEIGHT if config.height == 480 else config.height\n        width = NATIVE_WIDTH if config.width == 848 else config.width\n        frames = self.chunk_size if config.frames_per_step == 8 else config.frames_per_step\n        return height, width, frames\n\n    def warmup(self, config: WorldConfig) -&gt; None:\n        if self.pipeline is None:\n            return\n        height, width, _ = self._geometry(config)\n        cond = torch.zeros(1, 3, 1, height, width, dtype=torch.uint8, device=self.device)\n        null_action = torch.zeros(1, self.chunk_size, self.action_dim, device=self.device)\n        self.pipeline.generate_clip(cond, null_action, num_steps=1, seed=0)\n\n    def encode_action(self, action: Action) -&gt; torch.Tensor:\n        if action.action_type == \"null\":\n            return torch.zeros(\n                1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n            )\n        if \"latent_action\" in action.payload or \"frames\" in action.payload:\n            return self._encode_latent_action(action)\n\n        joints = action.payload.get(\"joints\", [0.0] * self.action_dim)\n        joints_t = torch.tensor(joints, device=self.device, dtype=self.dtype)\n\n        if joints_t.ndim == 1:\n            joints_t = joints_t.unsqueeze(0).expand(self.chunk_size, -1)\n        elif joints_t.ndim == 2 and joints_t.shape[0] != self.chunk_size:\n            pad_len = self.chunk_size - joints_t.shape[0]\n            if pad_len &gt; 0:\n                joints_t = torch.cat(\n                    [\n                        joints_t,\n                        torch.zeros(pad_len, self.action_dim, device=self.device, dtype=self.dtype),\n                    ],\n                    dim=0,\n                )\n            else:\n                joints_t = joints_t[: self.chunk_size]\n\n        return joints_t.unsqueeze(0)\n\n    def _encode_latent_action(self, action: Action) -&gt; torch.Tensor:\n        r\"\"\"Universal action chunk carrying a continuous latent action in ``[352:384)``.\n\n        ``payload[\"latent_action\"]`` supplies latent actions directly (``(32,)``\n        broadcast across the chunk or ``(chunk_size, 32)`` per frame);\n        ``payload[\"frames\"]`` supplies ``(T, 3, H, W)`` uint8 source frames whose\n        consecutive pairs the LAM encodes.\n        \"\"\"\n        from worldkernels.models.dreamdojo.lam import LATENT_ACTION_SLICE\n\n        if \"latent_action\" in action.payload:\n            z = torch.as_tensor(\n                action.payload[\"latent_action\"], device=self.device, dtype=self.dtype\n            )\n        else:\n            frames = torch.as_tensor(action.payload[\"frames\"])\n            z = self._lam_encoder().encode_video(frames).to(device=self.device, dtype=self.dtype)\n\n        if z.ndim == 1:\n            z = z.unsqueeze(0).expand(self.chunk_size, -1)\n        elif z.shape[0] != self.chunk_size:\n            pad = self.chunk_size - z.shape[0]\n            z = torch.cat([z, z[-1:].expand(pad, -1)], dim=0) if pad &gt; 0 else z[: self.chunk_size]\n\n        chunk = torch.zeros(\n            1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n        )\n        chunk[0, :, LATENT_ACTION_SLICE] = z\n        return chunk\n\n    def _lam_encoder(self):\n        if getattr(self, \"_lam\", None) is None:\n            from worldkernels.models.dreamdojo.checkpoint import download_lam_checkpoint\n            from worldkernels.models.dreamdojo.lam import LatentActionEncoder\n\n            self._lam = LatentActionEncoder.from_checkpoint(\n                download_lam_checkpoint(), device=self.device\n            )\n        return self._lam\n\n    def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n        assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n        ws: WorldState = state.data\n        action = (\n            action_encoded\n            if action_encoded.numel() &gt; 0\n            else torch.zeros(\n                1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n            )\n        )\n        cond_frame = ws.conditioning.image_cond\n        assert cond_frame is not None\n        seed = int(ws.extras[\"seed\"].item()) + ws.step_index\n\n        latent, video = self.pipeline.generate_clip(cond_frame.unsqueeze(2), action, seed=seed)\n        video_uint8 = self.pipeline.to_uint8(video)\n        new_state = WorldState(\n            latent=BlockHandle(latent),\n            conditioning=ConditioningBundle(image_cond=video_uint8[:, :, -1]),\n            meta=ws.meta,\n            step_index=ws.step_index + 1,\n            extras={\"seed\": ws.extras[\"seed\"], \"video\": video_uint8},\n        )\n        return LatentState(data=new_state, device=state.device)\n\n    def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n        assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n        t0 = time.perf_counter()\n        ws: WorldState = state.data\n        frames = None\n        latent_out = None\n\n        if \"frames\" in modalities:\n            video_uint8 = ws.extras.get(\"video\")\n            if video_uint8 is None:\n                video_uint8 = self.pipeline.to_uint8(self.pipeline.decode_latent(ws.latent.tensor))\n            frames = [\n                video_uint8[0, :, t].permute(1, 2, 0).cpu().numpy().tobytes()\n                for t in range(video_uint8.shape[2])\n            ]\n        if \"latent\" in modalities:\n            latent_out = ws.latent.tensor\n\n        return Observation(\n            step_index=ws.step_index,\n            generation_time_ms=(time.perf_counter() - t0) * 1000.0,\n            frames=frames,\n            latent=latent_out,\n        )\n\n    def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n        assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n        height, width, frames = self._geometry(config)\n        config.height, config.width, config.frames_per_step = height, width, frames\n\n        if config.initial_image is not None:\n            cond_frame = _load_image_uint8(config.initial_image, height, width, self.device)\n        else:\n            cond_frame = torch.zeros(1, 3, height, width, dtype=torch.uint8, device=self.device)\n\n        h, w = height // 8, width // 8\n        ws = WorldState(\n            latent=BlockHandle(\n                torch.zeros(1, 16, 4, h, w, device=self.device, dtype=torch.float32)\n            ),\n            conditioning=ConditioningBundle(image_cond=cond_frame),\n            meta=WorldStateMeta(\n                height=height,\n                width=width,\n                dtype=str(self.dtype),\n                transition_mode=self.transition_mode.value,\n                frames_per_step=frames,\n            ),\n            extras={\"seed\": torch.tensor(seed, dtype=torch.int64)},\n        )\n        return LatentState(data=ws, device=self.device)\n\n    def profile_vram(self, config: WorldConfig) -&gt; float:\n        height, width, frames = self._geometry(config)\n        pipeline = self.pipeline or DreamDojoPipeline(self.variant)\n        return pipeline.profile_vram(height=height, width=width, num_frames=frames)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name = 'dreamdojo'\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.stage_exec_modes","title":"stage_exec_modes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_exec_modes = {StageType.ENCODE: StageExecMode.SINGLE_SHOT, StageType.TRANSITION: StageExecMode.ITERATIVE, StageType.DECODE: StageExecMode.SINGLE_SHOT}\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.transition_mode","title":"transition_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_mode = TransitionMode.BIDIRECTIONAL\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.supports_streaming","title":"supports_streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_streaming = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.supports_kv_cache","title":"supports_kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_kv_cache = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.ckpt_path","title":"ckpt_path  <code>instance-attribute</code>","text":"<pre><code>ckpt_path = ckpt_path\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.variant","title":"variant  <code>instance-attribute</code>","text":"<pre><code>variant = variant\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.action_dim","title":"action_dim  <code>instance-attribute</code>","text":"<pre><code>action_dim = action_dim\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.chunk_size","title":"chunk_size  <code>instance-attribute</code>","text":"<pre><code>chunk_size = chunk_size\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.num_inference_steps","title":"num_inference_steps  <code>instance-attribute</code>","text":"<pre><code>num_inference_steps = num_inference_steps\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.guidance_scale","title":"guidance_scale  <code>instance-attribute</code>","text":"<pre><code>guidance_scale = guidance_scale\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: dtype = torch.float32\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.pipeline","title":"pipeline  <code>instance-attribute</code>","text":"<pre><code>pipeline: DreamDojoPipeline | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.default_config","title":"default_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.supports_iteration_batching","title":"supports_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_iteration_batching: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.rollout_kind","title":"rollout_kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rollout_kind: str = 'learned_transition'\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.__init__","title":"__init__","text":"<pre><code>__init__(ckpt_path: str | None = None, variant: str = DEFAULT_VARIANT, action_dim: int = ACTION_DIM, chunk_size: int = 12, num_inference_steps: int = 35, guidance_scale: float = 0.0, **kwargs: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def __init__(\n    self,\n    ckpt_path: str | None = None,\n    variant: str = DEFAULT_VARIANT,\n    action_dim: int = ACTION_DIM,\n    chunk_size: int = 12,\n    num_inference_steps: int = 35,\n    guidance_scale: float = 0.0,\n    **kwargs: Any,\n) -&gt; None:\n    if variant not in CKPT_DIRS:\n        raise ValueError(f\"unknown DreamDojo variant {variant!r}; known: {sorted(CKPT_DIRS)}\")\n    self.ckpt_path = ckpt_path\n    self.variant = variant\n    self.action_dim = action_dim\n    self.chunk_size = chunk_size\n    self.num_inference_steps = num_inference_steps\n    self.guidance_scale = guidance_scale\n    self.device: str = \"cpu\"\n    self.dtype: torch.dtype = torch.float32\n    self.pipeline: DreamDojoPipeline | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.initialize","title":"initialize","text":"<pre><code>initialize(device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n    self.device = device\n    self.dtype = dtype\n    self.pipeline = DreamDojoPipeline(\n        self.variant,\n        num_steps=self.num_inference_steps,\n        guidance=self.guidance_scale,\n    ).load(device, dtype, self.ckpt_path)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld._geometry","title":"_geometry","text":"<pre><code>_geometry(config: WorldConfig) -&gt; tuple[int, int, int]\n</code></pre> <p>Snap the generic WorldConfig defaults to DreamDojo's native 480x640.</p> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def _geometry(self, config: WorldConfig) -&gt; tuple[int, int, int]:\n    r\"\"\"Snap the generic WorldConfig defaults to DreamDojo's native 480x640.\"\"\"\n    height = NATIVE_HEIGHT if config.height == 480 else config.height\n    width = NATIVE_WIDTH if config.width == 848 else config.width\n    frames = self.chunk_size if config.frames_per_step == 8 else config.frames_per_step\n    return height, width, frames\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.warmup","title":"warmup","text":"<pre><code>warmup(config: WorldConfig) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def warmup(self, config: WorldConfig) -&gt; None:\n    if self.pipeline is None:\n        return\n    height, width, _ = self._geometry(config)\n    cond = torch.zeros(1, 3, 1, height, width, dtype=torch.uint8, device=self.device)\n    null_action = torch.zeros(1, self.chunk_size, self.action_dim, device=self.device)\n    self.pipeline.generate_clip(cond, null_action, num_steps=1, seed=0)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.encode_action","title":"encode_action","text":"<pre><code>encode_action(action: Action) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def encode_action(self, action: Action) -&gt; torch.Tensor:\n    if action.action_type == \"null\":\n        return torch.zeros(\n            1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n        )\n    if \"latent_action\" in action.payload or \"frames\" in action.payload:\n        return self._encode_latent_action(action)\n\n    joints = action.payload.get(\"joints\", [0.0] * self.action_dim)\n    joints_t = torch.tensor(joints, device=self.device, dtype=self.dtype)\n\n    if joints_t.ndim == 1:\n        joints_t = joints_t.unsqueeze(0).expand(self.chunk_size, -1)\n    elif joints_t.ndim == 2 and joints_t.shape[0] != self.chunk_size:\n        pad_len = self.chunk_size - joints_t.shape[0]\n        if pad_len &gt; 0:\n            joints_t = torch.cat(\n                [\n                    joints_t,\n                    torch.zeros(pad_len, self.action_dim, device=self.device, dtype=self.dtype),\n                ],\n                dim=0,\n            )\n        else:\n            joints_t = joints_t[: self.chunk_size]\n\n    return joints_t.unsqueeze(0)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld._encode_latent_action","title":"_encode_latent_action","text":"<pre><code>_encode_latent_action(action: Action) -&gt; Tensor\n</code></pre> <p>Universal action chunk carrying a continuous latent action in <code>[352:384)</code>.</p> <p><code>payload[\"latent_action\"]</code> supplies latent actions directly (<code>(32,)</code> broadcast across the chunk or <code>(chunk_size, 32)</code> per frame); <code>payload[\"frames\"]</code> supplies <code>(T, 3, H, W)</code> uint8 source frames whose consecutive pairs the LAM encodes.</p> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def _encode_latent_action(self, action: Action) -&gt; torch.Tensor:\n    r\"\"\"Universal action chunk carrying a continuous latent action in ``[352:384)``.\n\n    ``payload[\"latent_action\"]`` supplies latent actions directly (``(32,)``\n    broadcast across the chunk or ``(chunk_size, 32)`` per frame);\n    ``payload[\"frames\"]`` supplies ``(T, 3, H, W)`` uint8 source frames whose\n    consecutive pairs the LAM encodes.\n    \"\"\"\n    from worldkernels.models.dreamdojo.lam import LATENT_ACTION_SLICE\n\n    if \"latent_action\" in action.payload:\n        z = torch.as_tensor(\n            action.payload[\"latent_action\"], device=self.device, dtype=self.dtype\n        )\n    else:\n        frames = torch.as_tensor(action.payload[\"frames\"])\n        z = self._lam_encoder().encode_video(frames).to(device=self.device, dtype=self.dtype)\n\n    if z.ndim == 1:\n        z = z.unsqueeze(0).expand(self.chunk_size, -1)\n    elif z.shape[0] != self.chunk_size:\n        pad = self.chunk_size - z.shape[0]\n        z = torch.cat([z, z[-1:].expand(pad, -1)], dim=0) if pad &gt; 0 else z[: self.chunk_size]\n\n    chunk = torch.zeros(\n        1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n    )\n    chunk[0, :, LATENT_ACTION_SLICE] = z\n    return chunk\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld._lam_encoder","title":"_lam_encoder","text":"<pre><code>_lam_encoder()\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def _lam_encoder(self):\n    if getattr(self, \"_lam\", None) is None:\n        from worldkernels.models.dreamdojo.checkpoint import download_lam_checkpoint\n        from worldkernels.models.dreamdojo.lam import LatentActionEncoder\n\n        self._lam = LatentActionEncoder.from_checkpoint(\n            download_lam_checkpoint(), device=self.device\n        )\n    return self._lam\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.transition","title":"transition","text":"<pre><code>transition(state: LatentState, action_encoded: Tensor) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n    assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n    ws: WorldState = state.data\n    action = (\n        action_encoded\n        if action_encoded.numel() &gt; 0\n        else torch.zeros(\n            1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n        )\n    )\n    cond_frame = ws.conditioning.image_cond\n    assert cond_frame is not None\n    seed = int(ws.extras[\"seed\"].item()) + ws.step_index\n\n    latent, video = self.pipeline.generate_clip(cond_frame.unsqueeze(2), action, seed=seed)\n    video_uint8 = self.pipeline.to_uint8(video)\n    new_state = WorldState(\n        latent=BlockHandle(latent),\n        conditioning=ConditioningBundle(image_cond=video_uint8[:, :, -1]),\n        meta=ws.meta,\n        step_index=ws.step_index + 1,\n        extras={\"seed\": ws.extras[\"seed\"], \"video\": video_uint8},\n    )\n    return LatentState(data=new_state, device=state.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.decode_observation","title":"decode_observation","text":"<pre><code>decode_observation(state: LatentState, modalities: list[str]) -&gt; Observation\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n    assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n    t0 = time.perf_counter()\n    ws: WorldState = state.data\n    frames = None\n    latent_out = None\n\n    if \"frames\" in modalities:\n        video_uint8 = ws.extras.get(\"video\")\n        if video_uint8 is None:\n            video_uint8 = self.pipeline.to_uint8(self.pipeline.decode_latent(ws.latent.tensor))\n        frames = [\n            video_uint8[0, :, t].permute(1, 2, 0).cpu().numpy().tobytes()\n            for t in range(video_uint8.shape[2])\n        ]\n    if \"latent\" in modalities:\n        latent_out = ws.latent.tensor\n\n    return Observation(\n        step_index=ws.step_index,\n        generation_time_ms=(time.perf_counter() - t0) * 1000.0,\n        frames=frames,\n        latent=latent_out,\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.create_initial_state","title":"create_initial_state","text":"<pre><code>create_initial_state(config: WorldConfig, seed: int) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n    assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n    height, width, frames = self._geometry(config)\n    config.height, config.width, config.frames_per_step = height, width, frames\n\n    if config.initial_image is not None:\n        cond_frame = _load_image_uint8(config.initial_image, height, width, self.device)\n    else:\n        cond_frame = torch.zeros(1, 3, height, width, dtype=torch.uint8, device=self.device)\n\n    h, w = height // 8, width // 8\n    ws = WorldState(\n        latent=BlockHandle(\n            torch.zeros(1, 16, 4, h, w, device=self.device, dtype=torch.float32)\n        ),\n        conditioning=ConditioningBundle(image_cond=cond_frame),\n        meta=WorldStateMeta(\n            height=height,\n            width=width,\n            dtype=str(self.dtype),\n            transition_mode=self.transition_mode.value,\n            frames_per_step=frames,\n        ),\n        extras={\"seed\": torch.tensor(seed, dtype=torch.int64)},\n    )\n    return LatentState(data=ws, device=self.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def profile_vram(self, config: WorldConfig) -&gt; float:\n    height, width, frames = self._geometry(config)\n    pipeline = self.pipeline or DreamDojoPipeline(self.variant)\n    return pipeline.profile_vram(height=height, width=width, num_frames=frames)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.transition_iter","title":"transition_iter","text":"<pre><code>transition_iter(state: LatentState, action_encoded: Tensor) -&gt; Iterator[LatentState]\n</code></pre> <p>Yield intermediate states across the transition's inner loop.</p> <p>Optional: implemented only by worlds that set <code>supports_iteration_batching = True</code>. Lets the scheduler drive the denoise loop step-by-step so sessions can join a batch mid-flight.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def transition_iter(\n    self,\n    state: LatentState,\n    action_encoded: torch.Tensor,\n) -&gt; Iterator[LatentState]:\n    r\"\"\"Yield intermediate states across the transition's inner loop.\n\n    Optional: implemented only by worlds that set\n    ``supports_iteration_batching = True``. Lets the scheduler drive the\n    denoise loop step-by-step so sessions can join a batch mid-flight.\n    \"\"\"\n    raise NotImplementedError(\n        f\"{type(self).__name__} does not support iteration-level batching\"\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoWorld.estimate_vram_mb","title":"estimate_vram_mb","text":"<pre><code>estimate_vram_mb(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n    return self.profile_vram(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld","title":"DreamDojoStudentWorld","text":"<p>               Bases: <code>DreamDojoWorld</code></p> <p>Self-Forcing-distilled DreamDojo student: causal per-frame streaming.</p> <p>Each <code>transition</code> consumes a 12-action chunk and rolls the causal student three latent frames (12 pixels) forward against a rolling 3-frame KV window, carrying the last 9 decoded pixels and 8 action transitions as the next chunk's context \u2014 the reference chunked streaming scheme with self-consistent absolute action alignment. Requires a distilled checkpoint (<code>ckpt_path</code>); the architecture is weight-compatible with teacher checkpoints for smoke runs.</p> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>class DreamDojoStudentWorld(DreamDojoWorld):\n    r\"\"\"Self-Forcing-distilled DreamDojo student: causal per-frame streaming.\n\n    Each `transition` consumes a 12-action chunk and rolls the causal student\n    three latent frames (12 pixels) forward against a rolling 3-frame KV\n    window, carrying the last 9 decoded pixels and 8 action transitions as the\n    next chunk's context \u2014 the reference chunked streaming scheme with\n    self-consistent absolute action alignment. Requires a distilled checkpoint\n    (``ckpt_path``); the architecture is weight-compatible with teacher\n    checkpoints for smoke runs.\n    \"\"\"\n\n    name = \"dreamdojo_student\"\n    transition_mode = TransitionMode.CAUSAL\n    supports_streaming = True\n\n    NEW_LATENT_FRAMES = 3\n    KV_WINDOW = 3\n\n    def __init__(self, *args: Any, student_steps: int = 4, **kwargs: Any) -&gt; None:\n        super().__init__(*args, **kwargs)\n        self.student_steps = student_steps\n\n    def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n        assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n        ws: WorldState = state.data\n        fresh = (\n            action_encoded\n            if action_encoded.numel() &gt; 0\n            else torch.zeros(\n                1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n            )\n        )\n        history = ws.extras.get(\"action_history\")\n        window_actions = fresh if history is None else torch.cat([history.to(fresh), fresh], dim=1)\n        context_px = ws.extras.get(\"context_px\")\n        if context_px is None:\n            cond_frame = ws.conditioning.image_cond\n            assert cond_frame is not None\n            context_px = cond_frame.unsqueeze(2)\n        seed = int(ws.extras[\"seed\"].item()) + ws.step_index\n\n        latents, new_video = self.pipeline.stream_chunk(\n            context_px,\n            window_actions,\n            student_steps=self.student_steps,\n            window=self.KV_WINDOW,\n            new_frames=self.NEW_LATENT_FRAMES,\n            seed=seed,\n        )\n        new_uint8 = self.pipeline.to_uint8(new_video)\n        context_len = (self.KV_WINDOW - 1) * 4 + 1\n        keep_actions = (self.KV_WINDOW - 1) * 4\n        new_state = WorldState(\n            latent=BlockHandle(latents.float()),\n            conditioning=ConditioningBundle(image_cond=new_uint8[:, :, -1]),\n            meta=ws.meta,\n            step_index=ws.step_index + 1,\n            extras={\n                \"seed\": ws.extras[\"seed\"],\n                \"video\": new_uint8,\n                \"context_px\": new_uint8[:, :, -context_len:],\n                \"action_history\": window_actions[:, -keep_actions:].detach(),\n            },\n        )\n        return LatentState(data=new_state, device=state.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name = 'dreamdojo_student'\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.transition_mode","title":"transition_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_mode = TransitionMode.CAUSAL\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.supports_streaming","title":"supports_streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_streaming = True\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.NEW_LATENT_FRAMES","title":"NEW_LATENT_FRAMES  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NEW_LATENT_FRAMES = 3\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.KV_WINDOW","title":"KV_WINDOW  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>KV_WINDOW = 3\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.student_steps","title":"student_steps  <code>instance-attribute</code>","text":"<pre><code>student_steps = student_steps\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.default_config","title":"default_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.stage_exec_modes","title":"stage_exec_modes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_exec_modes = {StageType.ENCODE: StageExecMode.SINGLE_SHOT, StageType.TRANSITION: StageExecMode.ITERATIVE, StageType.DECODE: StageExecMode.SINGLE_SHOT}\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.supports_kv_cache","title":"supports_kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_kv_cache = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.supports_iteration_batching","title":"supports_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_iteration_batching: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.rollout_kind","title":"rollout_kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rollout_kind: str = 'learned_transition'\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.ckpt_path","title":"ckpt_path  <code>instance-attribute</code>","text":"<pre><code>ckpt_path = ckpt_path\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.variant","title":"variant  <code>instance-attribute</code>","text":"<pre><code>variant = variant\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.action_dim","title":"action_dim  <code>instance-attribute</code>","text":"<pre><code>action_dim = action_dim\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.chunk_size","title":"chunk_size  <code>instance-attribute</code>","text":"<pre><code>chunk_size = chunk_size\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.num_inference_steps","title":"num_inference_steps  <code>instance-attribute</code>","text":"<pre><code>num_inference_steps = num_inference_steps\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.guidance_scale","title":"guidance_scale  <code>instance-attribute</code>","text":"<pre><code>guidance_scale = guidance_scale\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: dtype = torch.float32\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.pipeline","title":"pipeline  <code>instance-attribute</code>","text":"<pre><code>pipeline: DreamDojoPipeline | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.__init__","title":"__init__","text":"<pre><code>__init__(*args: Any, student_steps: int = 4, **kwargs: Any) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def __init__(self, *args: Any, student_steps: int = 4, **kwargs: Any) -&gt; None:\n    super().__init__(*args, **kwargs)\n    self.student_steps = student_steps\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.transition","title":"transition","text":"<pre><code>transition(state: LatentState, action_encoded: Tensor) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n    assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n    ws: WorldState = state.data\n    fresh = (\n        action_encoded\n        if action_encoded.numel() &gt; 0\n        else torch.zeros(\n            1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n        )\n    )\n    history = ws.extras.get(\"action_history\")\n    window_actions = fresh if history is None else torch.cat([history.to(fresh), fresh], dim=1)\n    context_px = ws.extras.get(\"context_px\")\n    if context_px is None:\n        cond_frame = ws.conditioning.image_cond\n        assert cond_frame is not None\n        context_px = cond_frame.unsqueeze(2)\n    seed = int(ws.extras[\"seed\"].item()) + ws.step_index\n\n    latents, new_video = self.pipeline.stream_chunk(\n        context_px,\n        window_actions,\n        student_steps=self.student_steps,\n        window=self.KV_WINDOW,\n        new_frames=self.NEW_LATENT_FRAMES,\n        seed=seed,\n    )\n    new_uint8 = self.pipeline.to_uint8(new_video)\n    context_len = (self.KV_WINDOW - 1) * 4 + 1\n    keep_actions = (self.KV_WINDOW - 1) * 4\n    new_state = WorldState(\n        latent=BlockHandle(latents.float()),\n        conditioning=ConditioningBundle(image_cond=new_uint8[:, :, -1]),\n        meta=ws.meta,\n        step_index=ws.step_index + 1,\n        extras={\n            \"seed\": ws.extras[\"seed\"],\n            \"video\": new_uint8,\n            \"context_px\": new_uint8[:, :, -context_len:],\n            \"action_history\": window_actions[:, -keep_actions:].detach(),\n        },\n    )\n    return LatentState(data=new_state, device=state.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.initialize","title":"initialize","text":"<pre><code>initialize(device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n    self.device = device\n    self.dtype = dtype\n    self.pipeline = DreamDojoPipeline(\n        self.variant,\n        num_steps=self.num_inference_steps,\n        guidance=self.guidance_scale,\n    ).load(device, dtype, self.ckpt_path)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.warmup","title":"warmup","text":"<pre><code>warmup(config: WorldConfig) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def warmup(self, config: WorldConfig) -&gt; None:\n    if self.pipeline is None:\n        return\n    height, width, _ = self._geometry(config)\n    cond = torch.zeros(1, 3, 1, height, width, dtype=torch.uint8, device=self.device)\n    null_action = torch.zeros(1, self.chunk_size, self.action_dim, device=self.device)\n    self.pipeline.generate_clip(cond, null_action, num_steps=1, seed=0)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.encode_action","title":"encode_action","text":"<pre><code>encode_action(action: Action) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def encode_action(self, action: Action) -&gt; torch.Tensor:\n    if action.action_type == \"null\":\n        return torch.zeros(\n            1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n        )\n    if \"latent_action\" in action.payload or \"frames\" in action.payload:\n        return self._encode_latent_action(action)\n\n    joints = action.payload.get(\"joints\", [0.0] * self.action_dim)\n    joints_t = torch.tensor(joints, device=self.device, dtype=self.dtype)\n\n    if joints_t.ndim == 1:\n        joints_t = joints_t.unsqueeze(0).expand(self.chunk_size, -1)\n    elif joints_t.ndim == 2 and joints_t.shape[0] != self.chunk_size:\n        pad_len = self.chunk_size - joints_t.shape[0]\n        if pad_len &gt; 0:\n            joints_t = torch.cat(\n                [\n                    joints_t,\n                    torch.zeros(pad_len, self.action_dim, device=self.device, dtype=self.dtype),\n                ],\n                dim=0,\n            )\n        else:\n            joints_t = joints_t[: self.chunk_size]\n\n    return joints_t.unsqueeze(0)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.transition_iter","title":"transition_iter","text":"<pre><code>transition_iter(state: LatentState, action_encoded: Tensor) -&gt; Iterator[LatentState]\n</code></pre> <p>Yield intermediate states across the transition's inner loop.</p> <p>Optional: implemented only by worlds that set <code>supports_iteration_batching = True</code>. Lets the scheduler drive the denoise loop step-by-step so sessions can join a batch mid-flight.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def transition_iter(\n    self,\n    state: LatentState,\n    action_encoded: torch.Tensor,\n) -&gt; Iterator[LatentState]:\n    r\"\"\"Yield intermediate states across the transition's inner loop.\n\n    Optional: implemented only by worlds that set\n    ``supports_iteration_batching = True``. Lets the scheduler drive the\n    denoise loop step-by-step so sessions can join a batch mid-flight.\n    \"\"\"\n    raise NotImplementedError(\n        f\"{type(self).__name__} does not support iteration-level batching\"\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.decode_observation","title":"decode_observation","text":"<pre><code>decode_observation(state: LatentState, modalities: list[str]) -&gt; Observation\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n    assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n    t0 = time.perf_counter()\n    ws: WorldState = state.data\n    frames = None\n    latent_out = None\n\n    if \"frames\" in modalities:\n        video_uint8 = ws.extras.get(\"video\")\n        if video_uint8 is None:\n            video_uint8 = self.pipeline.to_uint8(self.pipeline.decode_latent(ws.latent.tensor))\n        frames = [\n            video_uint8[0, :, t].permute(1, 2, 0).cpu().numpy().tobytes()\n            for t in range(video_uint8.shape[2])\n        ]\n    if \"latent\" in modalities:\n        latent_out = ws.latent.tensor\n\n    return Observation(\n        step_index=ws.step_index,\n        generation_time_ms=(time.perf_counter() - t0) * 1000.0,\n        frames=frames,\n        latent=latent_out,\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def profile_vram(self, config: WorldConfig) -&gt; float:\n    height, width, frames = self._geometry(config)\n    pipeline = self.pipeline or DreamDojoPipeline(self.variant)\n    return pipeline.profile_vram(height=height, width=width, num_frames=frames)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.estimate_vram_mb","title":"estimate_vram_mb","text":"<pre><code>estimate_vram_mb(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n    return self.profile_vram(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld.create_initial_state","title":"create_initial_state","text":"<pre><code>create_initial_state(config: WorldConfig, seed: int) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n    assert self.pipeline is not None, \"Pipeline not loaded \u2014 call initialize() first\"\n    height, width, frames = self._geometry(config)\n    config.height, config.width, config.frames_per_step = height, width, frames\n\n    if config.initial_image is not None:\n        cond_frame = _load_image_uint8(config.initial_image, height, width, self.device)\n    else:\n        cond_frame = torch.zeros(1, 3, height, width, dtype=torch.uint8, device=self.device)\n\n    h, w = height // 8, width // 8\n    ws = WorldState(\n        latent=BlockHandle(\n            torch.zeros(1, 16, 4, h, w, device=self.device, dtype=torch.float32)\n        ),\n        conditioning=ConditioningBundle(image_cond=cond_frame),\n        meta=WorldStateMeta(\n            height=height,\n            width=width,\n            dtype=str(self.dtype),\n            transition_mode=self.transition_mode.value,\n            frames_per_step=frames,\n        ),\n        extras={\"seed\": torch.tensor(seed, dtype=torch.int64)},\n    )\n    return LatentState(data=ws, device=self.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld._geometry","title":"_geometry","text":"<pre><code>_geometry(config: WorldConfig) -&gt; tuple[int, int, int]\n</code></pre> <p>Snap the generic WorldConfig defaults to DreamDojo's native 480x640.</p> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def _geometry(self, config: WorldConfig) -&gt; tuple[int, int, int]:\n    r\"\"\"Snap the generic WorldConfig defaults to DreamDojo's native 480x640.\"\"\"\n    height = NATIVE_HEIGHT if config.height == 480 else config.height\n    width = NATIVE_WIDTH if config.width == 848 else config.width\n    frames = self.chunk_size if config.frames_per_step == 8 else config.frames_per_step\n    return height, width, frames\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld._encode_latent_action","title":"_encode_latent_action","text":"<pre><code>_encode_latent_action(action: Action) -&gt; Tensor\n</code></pre> <p>Universal action chunk carrying a continuous latent action in <code>[352:384)</code>.</p> <p><code>payload[\"latent_action\"]</code> supplies latent actions directly (<code>(32,)</code> broadcast across the chunk or <code>(chunk_size, 32)</code> per frame); <code>payload[\"frames\"]</code> supplies <code>(T, 3, H, W)</code> uint8 source frames whose consecutive pairs the LAM encodes.</p> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def _encode_latent_action(self, action: Action) -&gt; torch.Tensor:\n    r\"\"\"Universal action chunk carrying a continuous latent action in ``[352:384)``.\n\n    ``payload[\"latent_action\"]`` supplies latent actions directly (``(32,)``\n    broadcast across the chunk or ``(chunk_size, 32)`` per frame);\n    ``payload[\"frames\"]`` supplies ``(T, 3, H, W)`` uint8 source frames whose\n    consecutive pairs the LAM encodes.\n    \"\"\"\n    from worldkernels.models.dreamdojo.lam import LATENT_ACTION_SLICE\n\n    if \"latent_action\" in action.payload:\n        z = torch.as_tensor(\n            action.payload[\"latent_action\"], device=self.device, dtype=self.dtype\n        )\n    else:\n        frames = torch.as_tensor(action.payload[\"frames\"])\n        z = self._lam_encoder().encode_video(frames).to(device=self.device, dtype=self.dtype)\n\n    if z.ndim == 1:\n        z = z.unsqueeze(0).expand(self.chunk_size, -1)\n    elif z.shape[0] != self.chunk_size:\n        pad = self.chunk_size - z.shape[0]\n        z = torch.cat([z, z[-1:].expand(pad, -1)], dim=0) if pad &gt; 0 else z[: self.chunk_size]\n\n    chunk = torch.zeros(\n        1, self.chunk_size, self.action_dim, device=self.device, dtype=self.dtype\n    )\n    chunk[0, :, LATENT_ACTION_SLICE] = z\n    return chunk\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo.DreamDojoStudentWorld._lam_encoder","title":"_lam_encoder","text":"<pre><code>_lam_encoder()\n</code></pre> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def _lam_encoder(self):\n    if getattr(self, \"_lam\", None) is None:\n        from worldkernels.models.dreamdojo.checkpoint import download_lam_checkpoint\n        from worldkernels.models.dreamdojo.lam import LatentActionEncoder\n\n        self._lam = LatentActionEncoder.from_checkpoint(\n            download_lam_checkpoint(), device=self.device\n        )\n    return self._lam\n</code></pre>"},{"location":"api/worldkernels/worlds/dreamdojo/#worldkernels.worlds.dreamdojo._load_image_uint8","title":"_load_image_uint8","text":"<pre><code>_load_image_uint8(image: str | Any, height: int, width: int, device: str) -&gt; Tensor\n</code></pre> <p>Load an initial image as a <code>(1, 3, H, W)</code> uint8 conditioning frame.</p> Source code in <code>worldkernels/worlds/dreamdojo.py</code> <pre><code>def _load_image_uint8(image: str | Any, height: int, width: int, device: str) -&gt; torch.Tensor:\n    r\"\"\"Load an initial image as a ``(1, 3, H, W)`` uint8 conditioning frame.\"\"\"\n    if isinstance(image, str):\n        from PIL import Image\n\n        img = Image.open(image).convert(\"RGB\").resize((width, height))\n        import numpy as np\n\n        arr = torch.from_numpy(np.asarray(img)).permute(2, 0, 1)\n        return arr.unsqueeze(0).to(device)\n\n    img_t = torch.as_tensor(image)\n    if img_t.ndim == 3 and img_t.shape[0] != 3:\n        img_t = img_t.permute(2, 0, 1)\n    if torch.is_floating_point(img_t):\n        img_t = (img_t.clamp(0, 1) * 255.0).to(torch.uint8)\n    if img_t.ndim == 3:\n        img_t = img_t.unsqueeze(0)\n    return img_t.to(device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/","title":"dummy","text":""},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy","title":"worldkernels.worlds.dummy","text":"<p>DummyWorld \u2014 a CPU-safe world model for development and testing.</p> <p>Returns random noise with no real weights or compute. Reference template for <code>InteractiveWorldModel</code> implementations.</p>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy._LATENT_C","title":"_LATENT_C  <code>module-attribute</code>","text":"<pre><code>_LATENT_C = 4\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy._LATENT_FACTOR","title":"_LATENT_FACTOR  <code>module-attribute</code>","text":"<pre><code>_LATENT_FACTOR = 8\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld","title":"DummyWorld","text":"<p>               Bases: <code>InteractiveWorldModel</code></p> <p>World model that returns random noise. No real weights, no compute.</p> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>class DummyWorld(InteractiveWorldModel):\n    r\"\"\"World model that returns random noise. No real weights, no compute.\"\"\"\n\n    name = \"dummy\"\n\n    stage_exec_modes = {\n        StageType.ENCODE: StageExecMode.SINGLE_SHOT,\n        StageType.TRANSITION: StageExecMode.SINGLE_SHOT,\n        StageType.DECODE: StageExecMode.SINGLE_SHOT,\n    }\n\n    transition_mode = TransitionMode.BIDIRECTIONAL\n    supports_streaming = False\n    supports_kv_cache = False\n    supports_iteration_batching = True\n\n    def __init__(self) -&gt; None:\n        self.device: str = \"cpu\"\n        self.dtype: torch.dtype = torch.float32\n        self._initialized = False\n\n    def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n        self.device = device\n        self.dtype = dtype\n        self._initialized = True\n        # tiny \"weights\" the runtime quantization gate can attach to\n        self._quant_target = torch.nn.Linear(8, 8).to(device=device)\n\n    def warmup(self, config: WorldConfig) -&gt; None:\n        _ = self.create_initial_state(config, seed=0)\n\n    def encode_action(self, action: Action) -&gt; torch.Tensor:\n        return torch.randn(1, _LATENT_C, device=self.device, dtype=self.dtype)\n\n    def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n        pool, cache = _runtime_features()\n\n        if cache is not None:\n            cache.should_compute(state.data)\n\n        if pool is not None:\n            noise = pool.acquire(state.data.shape, state.data.dtype)\n            torch.randn(*state.data.shape, out=noise)\n            noise.mul_(0.1)\n            new_data = state.data + noise\n            pool.release(noise)\n        else:\n            new_data = state.data + torch.randn_like(state.data) * 0.1\n\n        return LatentState(data=new_data, device=state.device)\n\n    def transition_iter(\n        self, state: LatentState, action_encoded: torch.Tensor\n    ) -&gt; Iterator[LatentState]:\n        r\"\"\"Yield 3 intermediate states \u2014 the iteration-batching seam.\n\n        Uses the runtime pool + cache via the active `ForwardContext` so the\n        same toggles apply on either dispatch path.\n        \"\"\"\n        pool, cache = _runtime_features()\n        cur = state.data\n        for _ in range(3):\n            if cache is not None:\n                cache.should_compute(cur)\n            if pool is not None:\n                noise = pool.acquire(cur.shape, cur.dtype)\n                torch.randn(*cur.shape, out=noise)\n                noise.mul_(0.033)\n                cur = cur + noise\n                pool.release(noise)\n            else:\n                cur = cur + torch.randn_like(cur) * 0.033\n            yield LatentState(data=cur, device=state.device)\n\n    def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n        t0 = time.perf_counter()\n        frames = None\n        depth = None\n        audio = None\n\n        h = state.data.shape[-2] * _LATENT_FACTOR\n        w = state.data.shape[-1] * _LATENT_FACTOR\n        if \"frames\" in modalities:\n            frame_tensor = torch.randint(0, 256, (h, w, 3), dtype=torch.uint8, device=\"cpu\")\n            frames = [frame_tensor.numpy().tobytes()]\n        if \"depth\" in modalities:\n            depth = torch.rand(h, w, dtype=torch.float32, device=\"cpu\").numpy().tobytes()\n        if \"audio\" in modalities:\n            audio = torch.zeros(8000, dtype=torch.float32, device=\"cpu\").numpy().tobytes()\n\n        return Observation(\n            step_index=0,\n            generation_time_ms=(time.perf_counter() - t0) * 1000.0,\n            frames=frames,\n            depth=depth,\n            audio=audio,\n        )\n\n    def profile_vram(self, config: WorldConfig) -&gt; float:\n        lh = config.height // _LATENT_FACTOR\n        lw = config.width // _LATENT_FACTOR\n        return _LATENT_C * lh * lw * 4 / (1024 * 1024) + 1.0\n\n    def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n        gen = torch.Generator(device=\"cpu\").manual_seed(seed)\n        lh = config.height // _LATENT_FACTOR\n        lw = config.width // _LATENT_FACTOR\n        data = torch.randn(1, _LATENT_C, lh, lw, generator=gen, dtype=self.dtype, device=\"cpu\").to(\n            self.device\n        )\n        return LatentState(data=data, device=self.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name = 'dummy'\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.stage_exec_modes","title":"stage_exec_modes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_exec_modes = {StageType.ENCODE: StageExecMode.SINGLE_SHOT, StageType.TRANSITION: StageExecMode.SINGLE_SHOT, StageType.DECODE: StageExecMode.SINGLE_SHOT}\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.transition_mode","title":"transition_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_mode = TransitionMode.BIDIRECTIONAL\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.supports_streaming","title":"supports_streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_streaming = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.supports_kv_cache","title":"supports_kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_kv_cache = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.supports_iteration_batching","title":"supports_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_iteration_batching = True\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str = 'cpu'\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: dtype = torch.float32\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld._initialized","title":"_initialized  <code>instance-attribute</code>","text":"<pre><code>_initialized = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.default_config","title":"default_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.rollout_kind","title":"rollout_kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rollout_kind: str = 'learned_transition'\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.__init__","title":"__init__","text":"<pre><code>__init__() -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def __init__(self) -&gt; None:\n    self.device: str = \"cpu\"\n    self.dtype: torch.dtype = torch.float32\n    self._initialized = False\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.initialize","title":"initialize","text":"<pre><code>initialize(device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n    self.device = device\n    self.dtype = dtype\n    self._initialized = True\n    # tiny \"weights\" the runtime quantization gate can attach to\n    self._quant_target = torch.nn.Linear(8, 8).to(device=device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.warmup","title":"warmup","text":"<pre><code>warmup(config: WorldConfig) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def warmup(self, config: WorldConfig) -&gt; None:\n    _ = self.create_initial_state(config, seed=0)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.encode_action","title":"encode_action","text":"<pre><code>encode_action(action: Action) -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def encode_action(self, action: Action) -&gt; torch.Tensor:\n    return torch.randn(1, _LATENT_C, device=self.device, dtype=self.dtype)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.transition","title":"transition","text":"<pre><code>transition(state: LatentState, action_encoded: Tensor) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n    pool, cache = _runtime_features()\n\n    if cache is not None:\n        cache.should_compute(state.data)\n\n    if pool is not None:\n        noise = pool.acquire(state.data.shape, state.data.dtype)\n        torch.randn(*state.data.shape, out=noise)\n        noise.mul_(0.1)\n        new_data = state.data + noise\n        pool.release(noise)\n    else:\n        new_data = state.data + torch.randn_like(state.data) * 0.1\n\n    return LatentState(data=new_data, device=state.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.transition_iter","title":"transition_iter","text":"<pre><code>transition_iter(state: LatentState, action_encoded: Tensor) -&gt; Iterator[LatentState]\n</code></pre> <p>Yield 3 intermediate states \u2014 the iteration-batching seam.</p> <p>Uses the runtime pool + cache via the active <code>ForwardContext</code> so the same toggles apply on either dispatch path.</p> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def transition_iter(\n    self, state: LatentState, action_encoded: torch.Tensor\n) -&gt; Iterator[LatentState]:\n    r\"\"\"Yield 3 intermediate states \u2014 the iteration-batching seam.\n\n    Uses the runtime pool + cache via the active `ForwardContext` so the\n    same toggles apply on either dispatch path.\n    \"\"\"\n    pool, cache = _runtime_features()\n    cur = state.data\n    for _ in range(3):\n        if cache is not None:\n            cache.should_compute(cur)\n        if pool is not None:\n            noise = pool.acquire(cur.shape, cur.dtype)\n            torch.randn(*cur.shape, out=noise)\n            noise.mul_(0.033)\n            cur = cur + noise\n            pool.release(noise)\n        else:\n            cur = cur + torch.randn_like(cur) * 0.033\n        yield LatentState(data=cur, device=state.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.decode_observation","title":"decode_observation","text":"<pre><code>decode_observation(state: LatentState, modalities: list[str]) -&gt; Observation\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n    t0 = time.perf_counter()\n    frames = None\n    depth = None\n    audio = None\n\n    h = state.data.shape[-2] * _LATENT_FACTOR\n    w = state.data.shape[-1] * _LATENT_FACTOR\n    if \"frames\" in modalities:\n        frame_tensor = torch.randint(0, 256, (h, w, 3), dtype=torch.uint8, device=\"cpu\")\n        frames = [frame_tensor.numpy().tobytes()]\n    if \"depth\" in modalities:\n        depth = torch.rand(h, w, dtype=torch.float32, device=\"cpu\").numpy().tobytes()\n    if \"audio\" in modalities:\n        audio = torch.zeros(8000, dtype=torch.float32, device=\"cpu\").numpy().tobytes()\n\n    return Observation(\n        step_index=0,\n        generation_time_ms=(time.perf_counter() - t0) * 1000.0,\n        frames=frames,\n        depth=depth,\n        audio=audio,\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def profile_vram(self, config: WorldConfig) -&gt; float:\n    lh = config.height // _LATENT_FACTOR\n    lw = config.width // _LATENT_FACTOR\n    return _LATENT_C * lh * lw * 4 / (1024 * 1024) + 1.0\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.create_initial_state","title":"create_initial_state","text":"<pre><code>create_initial_state(config: WorldConfig, seed: int) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def create_initial_state(self, config: WorldConfig, seed: int) -&gt; LatentState:\n    gen = torch.Generator(device=\"cpu\").manual_seed(seed)\n    lh = config.height // _LATENT_FACTOR\n    lw = config.width // _LATENT_FACTOR\n    data = torch.randn(1, _LATENT_C, lh, lw, generator=gen, dtype=self.dtype, device=\"cpu\").to(\n        self.device\n    )\n    return LatentState(data=data, device=self.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy.DummyWorld.estimate_vram_mb","title":"estimate_vram_mb","text":"<pre><code>estimate_vram_mb(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n    return self.profile_vram(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/dummy/#worldkernels.worlds.dummy._runtime_features","title":"_runtime_features","text":"<pre><code>_runtime_features() -&gt; 'tuple[LatentPool | None, TeaCache | None]'\n</code></pre> <p>Return (pool, cache) from the active ForwardContext, or (None, None).</p> Source code in <code>worldkernels/worlds/dummy.py</code> <pre><code>def _runtime_features() -&gt; \"tuple[LatentPool | None, TeaCache | None]\":\n    r\"\"\"Return (pool, cache) from the active ForwardContext, or (None, None).\"\"\"\n    try:\n        from worldkernels.runtime.forward_context import get_forward_context\n\n        ctx = get_forward_context()\n    except RuntimeError:\n        return None, None\n    return getattr(ctx, \"pool\", None), getattr(ctx, \"cache_backend\", None)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/","title":"hub","text":""},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub","title":"worldkernels.worlds.hub","text":"<p>Model hub: maps HF repo IDs and short aliases to world models and generators.</p>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub._WAN_NEGATIVE_PROMPT","title":"_WAN_NEGATIVE_PROMPT  <code>module-attribute</code>","text":"<pre><code>_WAN_NEGATIVE_PROMPT = 'overexposed, static, blurred details, subtitles, worst quality, low quality, JPEG compression residue, ugly, deformed, disfigured, misshapen limbs, fused fingers, still picture, cluttered background, three legs, walking backwards'\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub._HUB","title":"_HUB  <code>module-attribute</code>","text":"<pre><code>_HUB: dict[str, ModelCard] = {}\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub._EXTRA_SENTINELS","title":"_EXTRA_SENTINELS  <code>module-attribute</code>","text":"<pre><code>_EXTRA_SENTINELS: dict[str, str] = {'diffusion': 'diffusers'}\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub._DREAMDOJO_WEIGHTS","title":"_DREAMDOJO_WEIGHTS  <code>module-attribute</code>","text":"<pre><code>_DREAMDOJO_WEIGHTS = 'worldkernels.models.dreamdojo.checkpoint:provision_dreamdojo_weights'\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.GitPackage","title":"GitPackage  <code>dataclass</code>","text":"<p>Spec for a github-only python package fetched on first use.</p> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>@dataclass(frozen=True)\nclass GitPackage:\n    r\"\"\"Spec for a github-only python package fetched on first use.\"\"\"\n\n    name: str\n    url: str\n    import_check: str | None = None\n    env_path_var: str | None = None\n    ref: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.GitPackage.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.GitPackage.url","title":"url  <code>instance-attribute</code>","text":"<pre><code>url: str\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.GitPackage.import_check","title":"import_check  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>import_check: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.GitPackage.env_path_var","title":"env_path_var  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>env_path_var: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.GitPackage.ref","title":"ref  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ref: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.GitPackage.__init__","title":"__init__","text":"<pre><code>__init__(name: str, url: str, import_check: str | None = None, env_path_var: str | None = None, ref: str | None = None) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.Component","title":"Component  <code>dataclass</code>","text":"<p>Sub-model component with its own pip extra and import sentinel.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Component identifier (e.g. <code>\"wan-vae\"</code>).</p> required <code>extra</code> <code>str</code> <p>The worldkernels extra that provides it.</p> required <code>sentinel</code> <code>str</code> <p>A module path used to detect whether the extra is installed.</p> required <code>deps</code> <code>tuple[str, ...]</code> <p>PEP 508 specs for the component's deps (used by the resolver).</p> <code>()</code> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>@dataclass(frozen=True)\nclass Component:\n    r\"\"\"Sub-model component with its own pip extra and import sentinel.\n\n    Args:\n        name: Component identifier (e.g. ``\"wan-vae\"``).\n        extra: The worldkernels extra that provides it.\n        sentinel: A module path used to detect whether the extra is installed.\n        deps: PEP 508 specs for the component's deps (used by the resolver).\n    \"\"\"\n\n    name: str\n    extra: str\n    sentinel: str\n    deps: tuple[str, ...] = ()\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.Component.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.Component.extra","title":"extra  <code>instance-attribute</code>","text":"<pre><code>extra: str\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.Component.sentinel","title":"sentinel  <code>instance-attribute</code>","text":"<pre><code>sentinel: str\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.Component.deps","title":"deps  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>deps: tuple[str, ...] = ()\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.Component.__init__","title":"__init__","text":"<pre><code>__init__(name: str, extra: str, sentinel: str, deps: tuple[str, ...] = ()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard","title":"ModelCard  <code>dataclass</code>","text":"<p>Metadata for a known model.</p> <p>Parameters:</p> Name Type Description Default <code>adapter</code> <code>str</code> <p>World-registry key (<code>\"dreamdojo\"</code>, <code>\"generator_world\"</code> \u2026).</p> required <code>kind</code> <code>Literal['world', 'generator']</code> <p><code>\"world\"</code> or <code>\"generator\"</code>.</p> <code>'world'</code> <code>generator</code> <code>str | None</code> <p>Pipeline-registry key (for kind=generator).</p> <code>None</code> <code>hf_repo</code> <code>str | None</code> <p>HuggingFace repo ID.</p> <code>None</code> <code>default_kwargs</code> <code>dict[str, Any]</code> <p>Constructor kwargs merged under user kwargs.</p> <code>dict()</code> <code>description</code> <code>str</code> <p>Human-readable summary.</p> <code>''</code> <code>pip_extra</code> <code>str | None</code> <p>Optional worldkernels extra to auto-install.</p> <code>None</code> <code>git_packages</code> <code>list[GitPackage]</code> <p>GitHub-only python deps fetched on first use.</p> <code>list()</code> <code>variants</code> <code>dict[str, dict[str, Any]]</code> <p>Named variant kwargs (<code>{\"2b_gr1\": {\"variant\": \"2b_gr1\"}}</code>).</p> <code>dict()</code> <code>auth_required</code> <code>bool</code> <p>Whether the HF repo is gated.</p> <code>False</code> <code>allow_patterns</code> <code>list[str] | None</code> <p>HF snapshot_download patterns.</p> <code>None</code> <code>variant_pattern</code> <code>list[str] | None</code> <p>Per-variant HF download patterns (<code>{variant}</code> is substituted).</p> <code>None</code> <code>weights_provider</code> <code>str | None</code> <p><code>\"module:func\"</code> that provisions weights itself, taking the variant and returning a local path. Set for models whose weights cannot be fetched by a plain snapshot (e.g. DCP checkpoints needing conversion, or monorepos where a blanket snapshot would mirror hundreds of GB). When set, <code>provision_weights</code> calls it instead of <code>snapshot_download</code>.</p> <code>None</code> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>@dataclass(frozen=True)\nclass ModelCard:\n    r\"\"\"Metadata for a known model.\n\n    Args:\n        adapter: World-registry key (``\"dreamdojo\"``, ``\"generator_world\"`` \u2026).\n        kind: ``\"world\"`` or ``\"generator\"``.\n        generator: Pipeline-registry key (for kind=generator).\n        hf_repo: HuggingFace repo ID.\n        default_kwargs: Constructor kwargs merged under user kwargs.\n        description: Human-readable summary.\n        pip_extra: Optional worldkernels extra to auto-install.\n        git_packages: GitHub-only python deps fetched on first use.\n        variants: Named variant kwargs (``{\"2b_gr1\": {\"variant\": \"2b_gr1\"}}``).\n        auth_required: Whether the HF repo is gated.\n        allow_patterns: HF snapshot_download patterns.\n        variant_pattern: Per-variant HF download patterns (``{variant}`` is substituted).\n        weights_provider: ``\"module:func\"`` that provisions weights itself, taking the\n            variant and returning a local path. Set for models whose weights cannot be\n            fetched by a plain snapshot (e.g. DCP checkpoints needing conversion, or\n            monorepos where a blanket snapshot would mirror hundreds of GB). When set,\n            ``provision_weights`` calls it instead of ``snapshot_download``.\n    \"\"\"\n\n    adapter: str\n    kind: Literal[\"world\", \"generator\"] = \"world\"\n    generator: str | None = None\n    hf_repo: str | None = None\n    default_kwargs: dict[str, Any] = field(default_factory=dict)\n    description: str = \"\"\n    pip_extra: str | None = None\n    git_packages: list[GitPackage] = field(default_factory=list)\n    variants: dict[str, dict[str, Any]] = field(default_factory=dict)\n    auth_required: bool = False\n    allow_patterns: list[str] | None = None\n    variant_pattern: list[str] | None = None\n    weights_provider: str | None = None\n    isolation: Literal[\"auto\", \"shared\", \"isolated\"] = \"auto\"\n    constraints: list[str] = field(default_factory=list)\n    components: list[Component] = field(default_factory=list)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.adapter","title":"adapter  <code>instance-attribute</code>","text":"<pre><code>adapter: str\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.kind","title":"kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>kind: Literal['world', 'generator'] = 'world'\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.generator","title":"generator  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>generator: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.hf_repo","title":"hf_repo  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>hf_repo: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.default_kwargs","title":"default_kwargs  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_kwargs: dict[str, Any] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description: str = ''\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.pip_extra","title":"pip_extra  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pip_extra: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.git_packages","title":"git_packages  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>git_packages: list[GitPackage] = field(default_factory=list)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.variants","title":"variants  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>variants: dict[str, dict[str, Any]] = field(default_factory=dict)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.auth_required","title":"auth_required  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>auth_required: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.allow_patterns","title":"allow_patterns  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>allow_patterns: list[str] | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.variant_pattern","title":"variant_pattern  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>variant_pattern: list[str] | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.weights_provider","title":"weights_provider  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>weights_provider: str | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.isolation","title":"isolation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>isolation: Literal['auto', 'shared', 'isolated'] = 'auto'\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.constraints","title":"constraints  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>constraints: list[str] = field(default_factory=list)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.components","title":"components  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>components: list[Component] = field(default_factory=list)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ModelCard.__init__","title":"__init__","text":"<pre><code>__init__(adapter: str, kind: Literal['world', 'generator'] = 'world', generator: str | None = None, hf_repo: str | None = None, default_kwargs: dict[str, Any] = dict(), description: str = '', pip_extra: str | None = None, git_packages: list[GitPackage] = list(), variants: dict[str, dict[str, Any]] = dict(), auth_required: bool = False, allow_patterns: list[str] | None = None, variant_pattern: list[str] | None = None, weights_provider: str | None = None, isolation: Literal['auto', 'shared', 'isolated'] = 'auto', constraints: list[str] = list(), components: list[Component] = list()) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.register_model","title":"register_model","text":"<pre><code>register_model(name: str, card: ModelCard) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>def register_model(name: str, card: ModelCard) -&gt; None:\n    _HUB[name] = card\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.get_model_card","title":"get_model_card","text":"<pre><code>get_model_card(name: str) -&gt; ModelCard | None\n</code></pre> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>def get_model_card(name: str) -&gt; ModelCard | None:\n    return _HUB.get(name)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.list_models","title":"list_models","text":"<pre><code>list_models() -&gt; dict[str, ModelCard]\n</code></pre> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>def list_models() -&gt; dict[str, ModelCard]:\n    return dict(_HUB)\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.infer_card_from_hf","title":"infer_card_from_hf","text":"<pre><code>infer_card_from_hf(repo_id: str) -&gt; ModelCard | None\n</code></pre> <p>Synthesize a minimal card for an unknown <code>org/repo</code> reference.</p> <p>Probes the HF Hub for the model card metadata; if the repo doesn't exist, returns <code>None</code>. Adapter selection is best-effort based on tags and falls back to <code>\"generator_world\"</code> for diffusion-style repos.</p> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>def infer_card_from_hf(repo_id: str) -&gt; ModelCard | None:\n    r\"\"\"Synthesize a minimal card for an unknown ``org/repo`` reference.\n\n    Probes the HF Hub for the model card metadata; if the repo doesn't exist,\n    returns ``None``. Adapter selection is best-effort based on tags and falls\n    back to ``\"generator_world\"`` for diffusion-style repos.\n    \"\"\"\n    try:\n        from huggingface_hub import HfApi\n        from huggingface_hub.errors import RepositoryNotFoundError\n    except ImportError:\n        return None\n\n    try:\n        info = HfApi().model_info(repo_id)\n    except RepositoryNotFoundError:\n        return None\n    except Exception as exc:\n        log.debug(\"HF probe failed for %s: %s\", repo_id, exc)\n        return None\n\n    tags = set(info.tags or [])\n    adapter = \"generator_world\"\n    pip_extra = \"diffusion\"\n    if \"diffusers\" in tags:\n        adapter = \"generator_world\"\n        pip_extra = \"diffusion\"\n\n    return ModelCard(\n        adapter=adapter,\n        hf_repo=repo_id,\n        description=f\"auto-inferred from {repo_id}\",\n        pip_extra=pip_extra,\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.ensure_model_deps","title":"ensure_model_deps","text":"<pre><code>ensure_model_deps(model_id: str) -&gt; None\n</code></pre> <p>Auto-install missing pip extras for a model.</p> <p>Back-compat shim retained for <code>model:inspect</code> and tests. New code should use <code>worldkernels.bootstrap.prepare</code>, which integrates with the unified progress UI.</p> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>def ensure_model_deps(model_id: str) -&gt; None:\n    r\"\"\"Auto-install missing pip extras for a model.\n\n    Back-compat shim retained for ``model:inspect`` and tests. New code should\n    use ``worldkernels.bootstrap.prepare``, which integrates with the unified\n    progress UI.\n    \"\"\"\n    import importlib as _importlib\n\n    card = _HUB.get(model_id)\n    if card is None or card.pip_extra is None:\n        return\n    sentinel = _EXTRA_SENTINELS.get(card.pip_extra)\n    if sentinel is None:\n        return\n    try:\n        _importlib.import_module(sentinel)\n        return\n    except ImportError:\n        pass\n\n    import os as _os\n\n    if _os.environ.get(\"WORLDKERNELS_NO_AUTO_INSTALL\"):\n        raise ImportError(\n            f\"Missing dependencies for '{model_id}'. \"\n            f\"Install with: pip install 'worldkernels[{card.pip_extra}]'\"\n        )\n    import subprocess as _subprocess\n    import sys as _sys\n\n    extra = f\"worldkernels[{card.pip_extra}]\"\n    log.info(\"Auto-installing missing dependencies: pip install '%s' ...\", extra)\n    _subprocess.check_call([_sys.executable, \"-m\", \"pip\", \"install\", extra])\n    log.info(\"Dependencies installed successfully.\")\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub.resolve_model","title":"resolve_model","text":"<pre><code>resolve_model(model_id: str, **user_kwargs: Any) -&gt; tuple[str, dict[str, Any]]\n</code></pre> <p>Resolve a model identifier to <code>(world_registry_key, merged_kwargs)</code>.</p> <p>Deprecated; use <code>worldkernels.bootstrap.prepare</code> for new code. Retained for legacy callers that don't need the bootstrap pipeline (e.g., <code>model:inspect</code>).</p> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>def resolve_model(model_id: str, **user_kwargs: Any) -&gt; tuple[str, dict[str, Any]]:\n    r\"\"\"Resolve a model identifier to ``(world_registry_key, merged_kwargs)``.\n\n    Deprecated; use ``worldkernels.bootstrap.prepare`` for new code. Retained for\n    legacy callers that don't need the bootstrap pipeline (e.g., ``model:inspect``).\n    \"\"\"\n    card = _HUB.get(model_id)\n    if card is None:\n        return model_id, user_kwargs\n    merged = {**card.default_kwargs, **user_kwargs}\n    if card.generator is not None:\n        merged.setdefault(\"generator\", card.generator)\n    return card.adapter, merged\n</code></pre>"},{"location":"api/worldkernels/worlds/hub/#worldkernels.worlds.hub._register_builtins","title":"_register_builtins","text":"<pre><code>_register_builtins() -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/hub.py</code> <pre><code>def _register_builtins() -&gt; None:\n    register_model(\n        \"dummy\",\n        ModelCard(adapter=\"dummy\", description=\"CPU-safe dummy world for testing\"),\n    )\n\n    _dreamdojo_variants = {\n        \"2b_pretrain\": \"DreamDojo 2B pretrained (general)\",\n        \"2b_gr1\": \"DreamDojo 2B fine-tuned on GR-1 robot\",\n        \"2b_agibot\": \"DreamDojo 2B fine-tuned on AgiBot\",\n        \"2b_g1\": \"DreamDojo 2B fine-tuned on G1 robot\",\n        \"2b_yam\": \"DreamDojo 2B fine-tuned on YAM robot\",\n        \"14b_pretrain\": \"DreamDojo 14B pretrained (general)\",\n        \"14b_gr1\": \"DreamDojo 14B fine-tuned on GR-1 robot\",\n        \"14b_agibot\": \"DreamDojo 14B fine-tuned on AgiBot\",\n        \"14b_g1\": \"DreamDojo 14B fine-tuned on G1 robot\",\n        \"14b_yam\": \"DreamDojo 14B fine-tuned on YAM robot\",\n    }\n\n    dreamdojo_card = ModelCard(\n        adapter=\"dreamdojo\",\n        kind=\"world\",\n        hf_repo=\"nvidia/DreamDojo\",\n        default_kwargs={\"variant\": \"2b_pretrain\"},\n        description=\"DreamDojo action-conditioned video world model (native)\",\n        pip_extra=\"diffusion\",\n        variants={k: {\"variant\": k} for k in _dreamdojo_variants},\n        weights_provider=_DREAMDOJO_WEIGHTS,\n    )\n    register_model(\"dreamdojo\", dreamdojo_card)\n    register_model(\"nvidia/DreamDojo\", dreamdojo_card)\n    register_model(\"DreamDojo\", dreamdojo_card)\n\n    register_model(\n        \"dreamdojo_student\",\n        ModelCard(\n            adapter=\"dreamdojo_student\",\n            kind=\"world\",\n            pip_extra=\"diffusion\",\n            default_kwargs={\"variant\": \"2b_gr1\"},\n            description=(\n                \"DreamDojo distilled causal student (real-time streaming); \"\n                \"requires a user-supplied distilled checkpoint via ckpt_path\"\n            ),\n        ),\n    )\n\n    for variant, desc in _dreamdojo_variants.items():\n        short = f\"dreamdojo-{variant.replace('_', '-')}\"\n        register_model(\n            short,\n            ModelCard(\n                adapter=\"dreamdojo\",\n                kind=\"world\",\n                hf_repo=\"nvidia/DreamDojo\",\n                default_kwargs={\"variant\": variant},\n                description=desc,\n                pip_extra=\"diffusion\",\n                weights_provider=_DREAMDOJO_WEIGHTS,\n            ),\n        )\n\n    _wan_variants = {\n        \"wan2.2-ti2v-5b\": (\n            \"Wan-AI/Wan2.2-TI2V-5B-Diffusers\",\n            5.0,\n            \"Wan2.2 TI2V-5B text/image-to-video (dense, 720p)\",\n        ),\n        \"wan2.1-i2v-14b\": (\n            \"Wan-AI/Wan2.1-I2V-14B-480P-Diffusers\",\n            3.0,\n            \"Wan2.1 I2V-14B image-to-video (480p)\",\n        ),\n    }\n    for short, (repo, flow_shift, desc) in _wan_variants.items():\n        card = ModelCard(\n            adapter=\"generator_world\",\n            kind=\"generator\",\n            generator=\"wan_i2v\",\n            hf_repo=repo,\n            default_kwargs={\n                \"repo\": repo,\n                \"pipeline_class\": \"WanImageToVideoPipeline\",\n                \"flow_shift\": flow_shift,\n                \"negative_prompt\": _WAN_NEGATIVE_PROMPT,\n            },\n            description=desc,\n            pip_extra=\"diffusion\",\n        )\n        register_model(short, card)\n        register_model(repo, card)\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/","title":"registry","text":""},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry","title":"worldkernels.worlds.registry","text":"<p>World model registry with built-in adapters and entry_points plugin discovery.</p>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry._REGISTRY","title":"_REGISTRY  <code>module-attribute</code>","text":"<pre><code>_REGISTRY: dict[str, type[WorldModel]] = {}\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry._plugins_loaded","title":"_plugins_loaded  <code>module-attribute</code>","text":"<pre><code>_plugins_loaded = False\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry.register_world","title":"register_world","text":"<pre><code>register_world(name: str, cls: type[WorldModel]) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/registry.py</code> <pre><code>def register_world(name: str, cls: type[WorldModel]) -&gt; None:\n    if name in _REGISTRY:\n        log.warning(\"Overwriting existing world registration: %s\", name)\n    _REGISTRY[name] = cls\n    log.debug(\"Registered world: %s -&gt; %s\", name, cls.__qualname__)\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry.get_world_class","title":"get_world_class","text":"<pre><code>get_world_class(name: str) -&gt; type[WorldModel]\n</code></pre> <p>Look up a registered world model class by name.</p> <p>Checks the adapter registry first, then resolves through the hub (which maps HF repo IDs and short aliases to adapter names).</p> Source code in <code>worldkernels/worlds/registry.py</code> <pre><code>def get_world_class(name: str) -&gt; type[WorldModel]:\n    r\"\"\"Look up a registered world model class by name.\n\n    Checks the adapter registry first, then resolves through the hub\n    (which maps HF repo IDs and short aliases to adapter names).\n    \"\"\"\n    _ensure_plugins_loaded()\n    if name in _REGISTRY:\n        return _REGISTRY[name]\n\n    from worldkernels.worlds.hub import get_model_card\n\n    card = get_model_card(name)\n    if card is not None and card.adapter in _REGISTRY:\n        return _REGISTRY[card.adapter]\n\n    available = \", \".join(sorted(_REGISTRY)) or \"(none)\"\n    raise KeyError(f\"World '{name}' not found in registry. Available: {available}\")\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry.list_worlds","title":"list_worlds","text":"<pre><code>list_worlds() -&gt; list[str]\n</code></pre> <p>Return sorted list of all registered world names.</p> Source code in <code>worldkernels/worlds/registry.py</code> <pre><code>def list_worlds() -&gt; list[str]:\n    r\"\"\"Return sorted list of all registered world names.\"\"\"\n    _ensure_plugins_loaded()\n    return sorted(_REGISTRY)\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry._register_builtins","title":"_register_builtins","text":"<pre><code>_register_builtins() -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/registry.py</code> <pre><code>def _register_builtins() -&gt; None:\n    from worldkernels.worlds.base import GeneratorWorld\n    from worldkernels.worlds.dummy import DummyWorld\n\n    register_world(\"dummy\", DummyWorld)\n    register_world(\"generator_world\", GeneratorWorld)\n\n    try:\n        from worldkernels.worlds.dreamdojo import DreamDojoStudentWorld, DreamDojoWorld\n\n        register_world(\"dreamdojo\", DreamDojoWorld)\n        register_world(\"dreamdojo_student\", DreamDojoStudentWorld)\n    except ImportError:\n        log.debug(\"DreamDojo world not available\")\n</code></pre>"},{"location":"api/worldkernels/worlds/registry/#worldkernels.worlds.registry._ensure_plugins_loaded","title":"_ensure_plugins_loaded","text":"<pre><code>_ensure_plugins_loaded() -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/registry.py</code> <pre><code>def _ensure_plugins_loaded() -&gt; None:\n    global _plugins_loaded\n    if _plugins_loaded:\n        return\n    _plugins_loaded = True\n\n    # Built-ins first\n    _register_builtins()\n\n    # Then third-party plugins via entry_points\n    try:\n        from importlib.metadata import entry_points\n\n        for ep in entry_points().select(group=\"worldkernels.worlds\"):\n            try:\n                cls = ep.load()\n                # Don't overwrite built-ins with their own entry_points\n                if ep.name not in _REGISTRY:\n                    register_world(ep.name, cls)\n            except Exception:\n                log.warning(\"Failed to load world plugin: %s\", ep.name, exc_info=True)\n    except Exception:\n        log.debug(\"entry_points discovery unavailable\", exc_info=True)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/","title":"remote","text":""},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote","title":"worldkernels.worlds.remote","text":"<p>Out-of-process world proxy for isolated models.</p> <p><code>RemoteWorld</code> implements the same <code>InteractiveWorldModel</code> interface as an in-process world but forwards every method to a subprocess via Unix-socket RPC and CUDA IPC (with host-shm fallback). The engine + scheduler cannot tell the difference; the executor doesn't distinguish.</p> <p>The worker process lives in a per-model isolated venv (see <code>runtime/envs.py</code>).</p>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.log","title":"log  <code>module-attribute</code>","text":"<pre><code>log = logging.getLogger(__name__)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RemoteWorld', 'WorkerHandle']\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.WorkerHandle","title":"WorkerHandle  <code>dataclass</code>","text":"Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>@dataclass\nclass WorkerHandle:\n    process: subprocess.Popen\n    socket_path: Path\n    client: RpcClient\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.WorkerHandle.process","title":"process  <code>instance-attribute</code>","text":"<pre><code>process: Popen\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.WorkerHandle.socket_path","title":"socket_path  <code>instance-attribute</code>","text":"<pre><code>socket_path: Path\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.WorkerHandle.client","title":"client  <code>instance-attribute</code>","text":"<pre><code>client: RpcClient\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.WorkerHandle.__init__","title":"__init__","text":"<pre><code>__init__(process: Popen, socket_path: Path, client: RpcClient) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld","title":"RemoteWorld","text":"<p>               Bases: <code>InteractiveWorldModel</code></p> <p>Proxy to a <code>WorldModel</code> running in an isolated subprocess.</p> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>class RemoteWorld(InteractiveWorldModel):\n    r\"\"\"Proxy to a `WorldModel` running in an isolated subprocess.\"\"\"\n\n    name = \"remote\"\n\n    def __init__(\n        self,\n        model_id: str,\n        adapter: str,\n        ctor_kwargs: dict[str, Any] | None = None,\n        env_root: Path | None = None,\n        worker_module: str = \"worldkernels.runtime.worker\",\n        log_level: str = \"INFO\",\n    ) -&gt; None:\n        self.model_id = model_id\n        self.adapter = adapter\n        self.ctor_kwargs = ctor_kwargs or {}\n        self.env_root = env_root or envs.env_path(model_id)\n        self.worker_module = worker_module\n        self.log_level = log_level\n        self.device: str = \"cuda\"\n        self.dtype: torch.dtype = torch.bfloat16\n        self._worker: WorkerHandle | None = None\n\n    def _spawn(self) -&gt; WorkerHandle:\n        socket_path = default_socket_path(self.model_id)\n        py = envs.venv_python(self.model_id)\n        if not py.exists():\n            py_str = \"python\"\n        else:\n            py_str = str(py)\n        cmd = [\n            py_str,\n            \"-m\",\n            self.worker_module,\n            \"--socket\",\n            str(socket_path),\n            \"--log-level\",\n            self.log_level,\n        ]\n        log.info(\"spawning worker: %s\", \" \".join(cmd))\n        env = os.environ.copy()\n        env.setdefault(\"PYTHONUNBUFFERED\", \"1\")\n        proc = subprocess.Popen(cmd, env=env)\n        client = RpcClient(socket_path)\n        try:\n            client.connect()\n        except ConnectionError:\n            proc.terminate()\n            raise\n        return WorkerHandle(process=proc, socket_path=socket_path, client=client)\n\n    def _ensure_worker(self) -&gt; WorkerHandle:\n        if self._worker is None or self._worker.process.poll() is not None:\n            if self._worker is not None:\n                log.warning(\n                    \"worker for %s died (exit=%s); respawning\",\n                    self.model_id,\n                    self._worker.process.returncode,\n                )\n                try:\n                    from worldkernels.runtime import metrics\n\n                    metrics.inc_worker_respawns(self.model_id)\n                except Exception:\n                    pass\n            self._worker = self._spawn()\n        return self._worker\n\n    def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n        self.device = device\n        self.dtype = dtype\n        handle = self._ensure_worker()\n        handle.client.call(\n            \"init\",\n            adapter=self.adapter,\n            kwargs=self.ctor_kwargs,\n            device=device,\n            dtype=str(dtype),\n        )\n\n    def warmup(self, config: \"WorldConfig\") -&gt; None:\n        handle = self._ensure_worker()\n        handle.client.call(\"warmup\", config=_config_to_dict(config))\n\n    def encode_action(self, action: \"Action\") -&gt; torch.Tensor:\n        handle = self._ensure_worker()\n        res = handle.client.call(\n            \"encode_action\",\n            action_type=action.action_type,\n            payload=action.payload,\n        )\n        return _RemoteRef(self, res[\"handle\"])  # type: ignore[return-value]\n\n    def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n        handle = self._ensure_worker()\n        state_h = _ref_handle(state.data)\n        action_h = _ref_handle(action_encoded)\n        res = handle.client.call(\"transition\", state_handle=state_h, action_handle=action_h)\n        return LatentState(data=_RemoteRef(self, res[\"handle\"]), device=self.device)\n\n    def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n        handle = self._ensure_worker()\n        state_h = _ref_handle(state.data)\n        res = handle.client.call(\"decode\", state_handle=state_h, modalities=modalities)\n        frames: list[Any] | None = None\n        raw = res.get(\"frames\")\n        if raw is not None:\n            frames = [_unwrap_frame(f) for f in raw]\n        latent = None\n        if res.get(\"latent_ipc\") is not None:\n            latent = ipc.recv_tensor(_dict_to_ipc(res[\"latent_ipc\"]))\n        return Observation(\n            step_index=int(res.get(\"step_index\", 0)),\n            generation_time_ms=float(res.get(\"generation_time_ms\", 0.0)),\n            frames=frames,\n            latent=latent,\n        )\n\n    def create_initial_state(self, config: \"WorldConfig\", seed: int) -&gt; LatentState:\n        handle = self._ensure_worker()\n        res = handle.client.call(\n            \"create_initial_state\", config=_config_to_dict(config), seed=int(seed)\n        )\n        return LatentState(data=_RemoteRef(self, res[\"handle\"]), device=self.device)\n\n    def profile_vram(self, config: \"WorldConfig\") -&gt; float:\n        handle = self._ensure_worker()\n        res = handle.client.call(\"profile_vram\", config=_config_to_dict(config))\n        return float(res.get(\"vram_mb\", 0.0))\n\n    def health(self) -&gt; dict[str, Any]:\n        handle = self._ensure_worker()\n        return handle.client.call(\"health\")\n\n    def close(self) -&gt; None:\n        if self._worker is None:\n            return\n        try:\n            self._worker.client.call(\"close\")\n        except Exception as exc:\n            log.debug(\"worker close errored: %s\", exc)\n        finally:\n            self._worker.client.close()\n            self._terminate(self._worker.process)\n            self._worker = None\n\n    def _terminate(self, proc: subprocess.Popen, timeout: float = 5.0) -&gt; None:\n        if proc.poll() is not None:\n            return\n        try:\n            proc.terminate()\n            proc.wait(timeout=timeout)\n        except subprocess.TimeoutExpired:\n            proc.send_signal(signal.SIGKILL)\n            proc.wait(timeout=2.0)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name = 'remote'\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.model_id","title":"model_id  <code>instance-attribute</code>","text":"<pre><code>model_id = model_id\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.adapter","title":"adapter  <code>instance-attribute</code>","text":"<pre><code>adapter = adapter\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.ctor_kwargs","title":"ctor_kwargs  <code>instance-attribute</code>","text":"<pre><code>ctor_kwargs = ctor_kwargs or {}\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.env_root","title":"env_root  <code>instance-attribute</code>","text":"<pre><code>env_root = env_root or envs.env_path(model_id)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.worker_module","title":"worker_module  <code>instance-attribute</code>","text":"<pre><code>worker_module = worker_module\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.log_level","title":"log_level  <code>instance-attribute</code>","text":"<pre><code>log_level = log_level\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.device","title":"device  <code>instance-attribute</code>","text":"<pre><code>device: str = 'cuda'\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.dtype","title":"dtype  <code>instance-attribute</code>","text":"<pre><code>dtype: dtype = torch.bfloat16\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld._worker","title":"_worker  <code>instance-attribute</code>","text":"<pre><code>_worker: WorkerHandle | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.default_config","title":"default_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default_config: WorldConfig | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.stage_exec_modes","title":"stage_exec_modes  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>stage_exec_modes: dict[StageType, StageExecMode] = {StageType.ENCODE: StageExecMode.SINGLE_SHOT, StageType.TRANSITION: StageExecMode.ITERATIVE, StageType.DECODE: StageExecMode.SINGLE_SHOT}\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.transition_mode","title":"transition_mode  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>transition_mode: TransitionMode = TransitionMode.BIDIRECTIONAL\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.supports_streaming","title":"supports_streaming  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_streaming: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.supports_kv_cache","title":"supports_kv_cache  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_kv_cache: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.supports_iteration_batching","title":"supports_iteration_batching  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>supports_iteration_batching: bool = False\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.rollout_kind","title":"rollout_kind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rollout_kind: str = 'learned_transition'\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.__init__","title":"__init__","text":"<pre><code>__init__(model_id: str, adapter: str, ctor_kwargs: dict[str, Any] | None = None, env_root: Path | None = None, worker_module: str = 'worldkernels.runtime.worker', log_level: str = 'INFO') -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def __init__(\n    self,\n    model_id: str,\n    adapter: str,\n    ctor_kwargs: dict[str, Any] | None = None,\n    env_root: Path | None = None,\n    worker_module: str = \"worldkernels.runtime.worker\",\n    log_level: str = \"INFO\",\n) -&gt; None:\n    self.model_id = model_id\n    self.adapter = adapter\n    self.ctor_kwargs = ctor_kwargs or {}\n    self.env_root = env_root or envs.env_path(model_id)\n    self.worker_module = worker_module\n    self.log_level = log_level\n    self.device: str = \"cuda\"\n    self.dtype: torch.dtype = torch.bfloat16\n    self._worker: WorkerHandle | None = None\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld._spawn","title":"_spawn","text":"<pre><code>_spawn() -&gt; WorkerHandle\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def _spawn(self) -&gt; WorkerHandle:\n    socket_path = default_socket_path(self.model_id)\n    py = envs.venv_python(self.model_id)\n    if not py.exists():\n        py_str = \"python\"\n    else:\n        py_str = str(py)\n    cmd = [\n        py_str,\n        \"-m\",\n        self.worker_module,\n        \"--socket\",\n        str(socket_path),\n        \"--log-level\",\n        self.log_level,\n    ]\n    log.info(\"spawning worker: %s\", \" \".join(cmd))\n    env = os.environ.copy()\n    env.setdefault(\"PYTHONUNBUFFERED\", \"1\")\n    proc = subprocess.Popen(cmd, env=env)\n    client = RpcClient(socket_path)\n    try:\n        client.connect()\n    except ConnectionError:\n        proc.terminate()\n        raise\n    return WorkerHandle(process=proc, socket_path=socket_path, client=client)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld._ensure_worker","title":"_ensure_worker","text":"<pre><code>_ensure_worker() -&gt; WorkerHandle\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def _ensure_worker(self) -&gt; WorkerHandle:\n    if self._worker is None or self._worker.process.poll() is not None:\n        if self._worker is not None:\n            log.warning(\n                \"worker for %s died (exit=%s); respawning\",\n                self.model_id,\n                self._worker.process.returncode,\n            )\n            try:\n                from worldkernels.runtime import metrics\n\n                metrics.inc_worker_respawns(self.model_id)\n            except Exception:\n                pass\n        self._worker = self._spawn()\n    return self._worker\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.initialize","title":"initialize","text":"<pre><code>initialize(device: str, dtype: dtype) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def initialize(self, device: str, dtype: torch.dtype) -&gt; None:\n    self.device = device\n    self.dtype = dtype\n    handle = self._ensure_worker()\n    handle.client.call(\n        \"init\",\n        adapter=self.adapter,\n        kwargs=self.ctor_kwargs,\n        device=device,\n        dtype=str(dtype),\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.warmup","title":"warmup","text":"<pre><code>warmup(config: 'WorldConfig') -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def warmup(self, config: \"WorldConfig\") -&gt; None:\n    handle = self._ensure_worker()\n    handle.client.call(\"warmup\", config=_config_to_dict(config))\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.encode_action","title":"encode_action","text":"<pre><code>encode_action(action: 'Action') -&gt; Tensor\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def encode_action(self, action: \"Action\") -&gt; torch.Tensor:\n    handle = self._ensure_worker()\n    res = handle.client.call(\n        \"encode_action\",\n        action_type=action.action_type,\n        payload=action.payload,\n    )\n    return _RemoteRef(self, res[\"handle\"])  # type: ignore[return-value]\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.transition","title":"transition","text":"<pre><code>transition(state: LatentState, action_encoded: Tensor) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def transition(self, state: LatentState, action_encoded: torch.Tensor) -&gt; LatentState:\n    handle = self._ensure_worker()\n    state_h = _ref_handle(state.data)\n    action_h = _ref_handle(action_encoded)\n    res = handle.client.call(\"transition\", state_handle=state_h, action_handle=action_h)\n    return LatentState(data=_RemoteRef(self, res[\"handle\"]), device=self.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.decode_observation","title":"decode_observation","text":"<pre><code>decode_observation(state: LatentState, modalities: list[str]) -&gt; Observation\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def decode_observation(self, state: LatentState, modalities: list[str]) -&gt; Observation:\n    handle = self._ensure_worker()\n    state_h = _ref_handle(state.data)\n    res = handle.client.call(\"decode\", state_handle=state_h, modalities=modalities)\n    frames: list[Any] | None = None\n    raw = res.get(\"frames\")\n    if raw is not None:\n        frames = [_unwrap_frame(f) for f in raw]\n    latent = None\n    if res.get(\"latent_ipc\") is not None:\n        latent = ipc.recv_tensor(_dict_to_ipc(res[\"latent_ipc\"]))\n    return Observation(\n        step_index=int(res.get(\"step_index\", 0)),\n        generation_time_ms=float(res.get(\"generation_time_ms\", 0.0)),\n        frames=frames,\n        latent=latent,\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.create_initial_state","title":"create_initial_state","text":"<pre><code>create_initial_state(config: 'WorldConfig', seed: int) -&gt; LatentState\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def create_initial_state(self, config: \"WorldConfig\", seed: int) -&gt; LatentState:\n    handle = self._ensure_worker()\n    res = handle.client.call(\n        \"create_initial_state\", config=_config_to_dict(config), seed=int(seed)\n    )\n    return LatentState(data=_RemoteRef(self, res[\"handle\"]), device=self.device)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.profile_vram","title":"profile_vram","text":"<pre><code>profile_vram(config: 'WorldConfig') -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def profile_vram(self, config: \"WorldConfig\") -&gt; float:\n    handle = self._ensure_worker()\n    res = handle.client.call(\"profile_vram\", config=_config_to_dict(config))\n    return float(res.get(\"vram_mb\", 0.0))\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.health","title":"health","text":"<pre><code>health() -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def health(self) -&gt; dict[str, Any]:\n    handle = self._ensure_worker()\n    return handle.client.call(\"health\")\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.close","title":"close","text":"<pre><code>close() -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def close(self) -&gt; None:\n    if self._worker is None:\n        return\n    try:\n        self._worker.client.call(\"close\")\n    except Exception as exc:\n        log.debug(\"worker close errored: %s\", exc)\n    finally:\n        self._worker.client.close()\n        self._terminate(self._worker.process)\n        self._worker = None\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld._terminate","title":"_terminate","text":"<pre><code>_terminate(proc: Popen, timeout: float = 5.0) -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def _terminate(self, proc: subprocess.Popen, timeout: float = 5.0) -&gt; None:\n    if proc.poll() is not None:\n        return\n    try:\n        proc.terminate()\n        proc.wait(timeout=timeout)\n    except subprocess.TimeoutExpired:\n        proc.send_signal(signal.SIGKILL)\n        proc.wait(timeout=2.0)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.transition_iter","title":"transition_iter","text":"<pre><code>transition_iter(state: LatentState, action_encoded: Tensor) -&gt; Iterator[LatentState]\n</code></pre> <p>Yield intermediate states across the transition's inner loop.</p> <p>Optional: implemented only by worlds that set <code>supports_iteration_batching = True</code>. Lets the scheduler drive the denoise loop step-by-step so sessions can join a batch mid-flight.</p> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def transition_iter(\n    self,\n    state: LatentState,\n    action_encoded: torch.Tensor,\n) -&gt; Iterator[LatentState]:\n    r\"\"\"Yield intermediate states across the transition's inner loop.\n\n    Optional: implemented only by worlds that set\n    ``supports_iteration_batching = True``. Lets the scheduler drive the\n    denoise loop step-by-step so sessions can join a batch mid-flight.\n    \"\"\"\n    raise NotImplementedError(\n        f\"{type(self).__name__} does not support iteration-level batching\"\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote.RemoteWorld.estimate_vram_mb","title":"estimate_vram_mb","text":"<pre><code>estimate_vram_mb(config: WorldConfig) -&gt; float\n</code></pre> Source code in <code>worldkernels/worlds/base.py</code> <pre><code>def estimate_vram_mb(self, config: WorldConfig) -&gt; float:\n    return self.profile_vram(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._RemoteRef","title":"_RemoteRef  <code>dataclass</code>","text":"<p>Opaque server-side handle. Looks like a tensor/state to the engine.</p> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>@dataclass\nclass _RemoteRef:\n    r\"\"\"Opaque server-side handle. Looks like a tensor/state to the engine.\"\"\"\n\n    world: RemoteWorld\n    handle: str\n\n    def release(self) -&gt; None:\n        try:\n            self.world._ensure_worker().client.call(\"release\", handle=self.handle)\n        except Exception:\n            pass\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._RemoteRef.world","title":"world  <code>instance-attribute</code>","text":"<pre><code>world: RemoteWorld\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._RemoteRef.handle","title":"handle  <code>instance-attribute</code>","text":"<pre><code>handle: str\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._RemoteRef.__init__","title":"__init__","text":"<pre><code>__init__(world: RemoteWorld, handle: str) -&gt; None\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._RemoteRef.release","title":"release","text":"<pre><code>release() -&gt; None\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def release(self) -&gt; None:\n    try:\n        self.world._ensure_worker().client.call(\"release\", handle=self.handle)\n    except Exception:\n        pass\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._ref_handle","title":"_ref_handle","text":"<pre><code>_ref_handle(obj: Any) -&gt; str\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def _ref_handle(obj: Any) -&gt; str:\n    if isinstance(obj, _RemoteRef):\n        return obj.handle\n    raise WorldKernelError(f\"remote world expected a server-side handle, got {type(obj).__name__}\")\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._unwrap_frame","title":"_unwrap_frame","text":"<pre><code>_unwrap_frame(envelope: dict[str, Any]) -&gt; Any\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def _unwrap_frame(envelope: dict[str, Any]) -&gt; Any:\n    kind = envelope.get(\"kind\")\n    if kind == \"bytes\":\n        return bytes(envelope[\"data\"])\n    if kind == \"ipc\":\n        return ipc.recv_tensor(_dict_to_ipc(envelope[\"handle\"]))\n    return envelope\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._dict_to_ipc","title":"_dict_to_ipc","text":"<pre><code>_dict_to_ipc(d: dict[str, Any]) -&gt; IpcHandle\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def _dict_to_ipc(d: dict[str, Any]) -&gt; ipc.IpcHandle:\n    return ipc.IpcHandle(\n        mode=d[\"mode\"],\n        shape=tuple(d[\"shape\"]),\n        dtype=d[\"dtype\"],\n        device=d[\"device\"],\n        payload=d[\"payload\"],\n    )\n</code></pre>"},{"location":"api/worldkernels/worlds/remote/#worldkernels.worlds.remote._config_to_dict","title":"_config_to_dict","text":"<pre><code>_config_to_dict(config: Any) -&gt; dict[str, Any]\n</code></pre> Source code in <code>worldkernels/worlds/remote.py</code> <pre><code>def _config_to_dict(config: Any) -&gt; dict[str, Any]:\n    if hasattr(config, \"model_dump\"):\n        return config.model_dump()\n    if hasattr(config, \"__dict__\"):\n        return {k: v for k, v in vars(config).items() if not k.startswith(\"_\")}\n    return dict(config)\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/","title":"rollout","text":""},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout","title":"worldkernels.worlds.rollout","text":"<p>Rollout policies for presenting a one-shot video generator as a world model.</p> <p>A <code>GeneratorWorld</code> wraps a stateless video generator. The rollout policy is the explicit, swappable rule that turns the generator's just-produced clip into the conditioning for the next step. Making this a named object \u2014 rather than a hidden <code>last_frame</code> hack \u2014 keeps the \"generator presented as a world\" relationship honest and self-describing.</p>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.__all__","title":"__all__  <code>module-attribute</code>","text":"<pre><code>__all__ = ['RolloutPolicy', 'LastFrameRolloutPolicy']\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.RolloutPolicy","title":"RolloutPolicy","text":"<p>               Bases: <code>ABC</code></p> <p>Rule mapping a generated clip to the next step's image conditioning.</p> <p>Subclasses declare <code>name</code> and <code>rollout_kind</code>; the latter is surfaced in world metadata so callers know the rollout is not a learned transition.</p> Source code in <code>worldkernels/worlds/rollout.py</code> <pre><code>class RolloutPolicy(ABC):\n    r\"\"\"Rule mapping a generated clip to the next step's image conditioning.\n\n    Subclasses declare ``name`` and ``rollout_kind``; the latter is surfaced in\n    world metadata so callers know the rollout is not a learned transition.\n    \"\"\"\n\n    name: ClassVar[str]\n    rollout_kind: ClassVar[str]\n\n    @abstractmethod\n    def next_image_cond(self, generated_video: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        r\"\"\"Return the next conditioning frame from a generated clip.\n\n        Args:\n            generated_video: Clip of shape ``(1, 3, frames, H, W)`` in ``[-1, 1]``.\n\n        Returns:\n            Conditioning frame of shape ``(1, 3, H, W)`` in ``[0, 1]``.\n        \"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.RolloutPolicy.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.RolloutPolicy.rollout_kind","title":"rollout_kind  <code>class-attribute</code>","text":"<pre><code>rollout_kind: str\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.RolloutPolicy.next_image_cond","title":"next_image_cond  <code>abstractmethod</code>","text":"<pre><code>next_image_cond(generated_video: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> <p>Return the next conditioning frame from a generated clip.</p> <p>Parameters:</p> Name Type Description Default <code>generated_video</code> <code>'torch.Tensor'</code> <p>Clip of shape <code>(1, 3, frames, H, W)</code> in <code>[-1, 1]</code>.</p> required <p>Returns:</p> Type Description <code>'torch.Tensor'</code> <p>Conditioning frame of shape <code>(1, 3, H, W)</code> in <code>[0, 1]</code>.</p> Source code in <code>worldkernels/worlds/rollout.py</code> <pre><code>@abstractmethod\ndef next_image_cond(self, generated_video: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    r\"\"\"Return the next conditioning frame from a generated clip.\n\n    Args:\n        generated_video: Clip of shape ``(1, 3, frames, H, W)`` in ``[-1, 1]``.\n\n    Returns:\n        Conditioning frame of shape ``(1, 3, H, W)`` in ``[0, 1]``.\n    \"\"\"\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.LastFrameRolloutPolicy","title":"LastFrameRolloutPolicy","text":"<p>               Bases: <code>RolloutPolicy</code></p> <p>Condition the next step on the last decoded frame of the previous clip.</p> <p>This is open-loop resampling, not a learned state transition: the generator has no memory of the action history beyond the single carried frame.</p> Source code in <code>worldkernels/worlds/rollout.py</code> <pre><code>class LastFrameRolloutPolicy(RolloutPolicy):\n    r\"\"\"Condition the next step on the last decoded frame of the previous clip.\n\n    This is open-loop resampling, not a learned state transition: the generator\n    has no memory of the action history beyond the single carried frame.\n    \"\"\"\n\n    name: ClassVar[str] = \"last_frame\"\n    rollout_kind: ClassVar[str] = \"open_loop_resample\"\n\n    def next_image_cond(self, generated_video: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n        last = generated_video[:, :, -1]\n        return ((last + 1.0) * 0.5).clamp(0.0, 1.0)\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.LastFrameRolloutPolicy.name","title":"name  <code>class-attribute</code>","text":"<pre><code>name: str = 'last_frame'\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.LastFrameRolloutPolicy.rollout_kind","title":"rollout_kind  <code>class-attribute</code>","text":"<pre><code>rollout_kind: str = 'open_loop_resample'\n</code></pre>"},{"location":"api/worldkernels/worlds/rollout/#worldkernels.worlds.rollout.LastFrameRolloutPolicy.next_image_cond","title":"next_image_cond","text":"<pre><code>next_image_cond(generated_video: 'torch.Tensor') -&gt; 'torch.Tensor'\n</code></pre> Source code in <code>worldkernels/worlds/rollout.py</code> <pre><code>def next_image_cond(self, generated_video: \"torch.Tensor\") -&gt; \"torch.Tensor\":\n    last = generated_video[:, :, -1]\n    return ((last + 1.0) * 0.5).clamp(0.0, 1.0)\n</code></pre>"},{"location":"examples/basics/multi_file_demo/","title":"Multi File Demo","text":""},{"location":"examples/basics/multi_file_demo/#multi-file-demo","title":"Multi File Demo","text":"<p>Source https://github.com/soran-ghaderi/worldkernels/tree/main/examples/basics/multi_file_demo.</p> <p>This example demonstrates a directory-based example with multiple files.</p> <p>Configuration is loaded from settings.</p>"},{"location":"examples/basics/multi_file_demo/#example-materials","title":"Example materials","text":"settings.yaml <pre><code>world: dummy\nheight: 64\nwidth: 64\nframes_per_step: 1\nmodalities:\n  - frames\n</code></pre>"},{"location":"examples/basics/quickstart/","title":"Quickstart","text":""},{"location":"examples/basics/quickstart/#quickstart","title":"Quickstart","text":"<p>Source https://github.com/soran-ghaderi/worldkernels/blob/main/examples/basics/quickstart.py.</p> <pre><code>from worldkernels import Action, WorldConfig, WorldKernel\n\n\ndef main() -&gt; None:\n    wk = WorldKernel(device=\"cpu\")\n    wk.load_model(\"dummy\")\n    session = wk.create_session(\"dummy\", config=WorldConfig(height=64, width=64), seed=7)\n    observation = session.step(Action(\"keyboard\", {\"keys\": [\"W\"]}), modalities=[\"frames\"])\n    print(\"step:\", observation.step_index, \"frames:\", len(observation.frames or []))\n    session.close()\n    wk.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"examples/diffusion_models_huggingFace/","title":"Diffusion Models Huggingface","text":""},{"location":"examples/diffusion_models_huggingFace/#diffusion-models-huggingface","title":"Diffusion Models Huggingface","text":"<p>Source https://github.com/soran-ghaderi/worldkernels/blob/main/examples/diffusion_models_huggingFace.py.</p> <pre><code>import imageio\nimport torch\nfrom diffusers import DiffusionPipeline, DPMSolverMultistepScheduler\n\n\ndef main() -&gt; None:\n    if torch.cuda.is_available():\n        device = \"cuda\"\n    else:\n        device = \"cpu\"\n\n    pipe = DiffusionPipeline.from_pretrained(\n        \"damo-vilab/text-to-video-ms-1.7b\",\n        torch_dtype=torch.float16 if device == \"cuda\" else torch.float32,\n    )\n\n    pipe = pipe.to(device)\n\n    pipe.scheduler = DPMSolverMultistepScheduler.from_config(pipe.scheduler.config)\n\n    prompts = [\n        \"A futuristic city with flying cars\",\n        \"A dragon flying over snowy mountains\",\n        \"A robot walking in a neon cyberpunk street\",\n    ]\n\n    for i, prompt in enumerate(prompts):\n        result = pipe(prompt, num_inference_steps=40, num_frames=24)\n\n        frames = result.frames\n\n        output_path = f\"video_{i}.mp4\"\n\n        imageio.mimsave(output_path, frames, fps=8)\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"examples/dreamdojo_conditioning/","title":"Dreamdojo Conditioning","text":""},{"location":"examples/dreamdojo_conditioning/#dreamdojo-conditioning","title":"Dreamdojo Conditioning","text":"<p>Source https://github.com/soran-ghaderi/worldkernels/blob/main/examples/dreamdojo_conditioning.py.</p> <pre><code>r\"\"\"Conditioning DreamDojo: init image and action chunks (joints or latent actions).\n\nDreamDojo is an action-conditioned image-to-video world model. Its conditioning\nsignals are wired through the public API as shown below. Run with:\n\n    python examples/dreamdojo_conditioning.py --image init.png --steps 3\n\nRequires a CUDA GPU and downloads the 2B checkpoint + Wan2.1 VAE on first run.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\n\nimport numpy as np\n\nfrom worldkernels import Action, WorldConfig, WorldEngine\n\nACTION_DIM = 384  # DreamDojo robot joint vector size\nCHUNK_SIZE = 12  # frames per transition the action conditions\n\n\ndef synthetic_action(step: int) -&gt; Action:\n    r\"\"\"A reproducible synthetic joint trajectory of shape (CHUNK_SIZE, ACTION_DIM).\n\n    Real use: replace ``joints`` with your robot's per-frame joint vectors (a teleop log, a\n    policy rollout, etc.). A 1-D ``(ACTION_DIM,)`` vector is broadcast across the chunk;\n    a 2-D ``(CHUNK_SIZE, ACTION_DIM)`` array gives a distinct action per frame.\n    \"\"\"\n    t = np.linspace(0.0, 1.0, CHUNK_SIZE)[:, None]\n    phase = 0.5 * step\n    joints = 0.6 * np.sin(2 * np.pi * (t + phase) + np.arange(ACTION_DIM)[None, :] * 0.01)\n    return Action(\"joints\", {\"joints\": joints.astype(np.float32).tolist()})\n\n\ndef main() -&gt; None:\n    ap = argparse.ArgumentParser()\n    ap.add_argument(\"--image\", default=None, help=\"path to the initial frame (anchors the video)\")\n    ap.add_argument(\"--variant\", default=\"2b_pretrain\")\n    ap.add_argument(\"--steps\", type=int, default=3)\n    ap.add_argument(\"--height\", type=int, default=480)\n    ap.add_argument(\"--width\", type=int, default=640)\n    ap.add_argument(\"--latent-actions\", action=\"store_true\", help=\"use 32-dim latent actions\")\n    args = ap.parse_args()\n\n    wk = WorldEngine(device=\"cuda\")\n    wk.load_model(\"dreamdojo\", variant=args.variant)\n    world_key = next(iter(wk.list_worlds()))\n\n    cfg = WorldConfig(\n        height=args.height,\n        width=args.width,\n        initial_prompt=\"a robot arm picking up a red cube on a desk\",\n        initial_image=args.image,\n    )\n    session = wk.create_session(world_key, config=cfg, seed=0)\n\n    for i in range(args.steps):\n        if args.latent_actions:\n            z = (0.5 * np.sin(np.arange(32) * 0.3 + i)).astype(np.float32).tolist()\n            action = Action(\"latent\", {\"latent_action\": z})\n        else:\n            action = synthetic_action(i)\n        obs = session.step(action, modalities=[\"frames\"])\n        n = len(obs.frames) if obs.frames else 0\n        print(f\"step {i}: {n} frames, {obs.generation_time_ms:.0f}ms\")\n\n    session.close()\n    wk.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"examples/dummy_session/","title":"Dummy Session","text":""},{"location":"examples/dummy_session/#dummy-session","title":"Dummy Session","text":"<p>Source https://github.com/soran-ghaderi/worldkernels/blob/main/examples/dummy_session.py.</p> <pre><code>\"\"\"End-to-end example: create a DummyWorld session and step through it.\n\nRun with:\n    python examples/dummy_session.py\n\nNo GPU required. Works on CPU.\n\"\"\"\n\nfrom worldkernels import Action, WorldConfig, WorldKernel\n\n\ndef main() -&gt; None:\n    # 1. Create the engine (CPU for laptop dev)\n    wk = WorldKernel(device=\"cpu\")\n\n    # 2. Load the built-in dummy world\n    wk.load_model(\"dummy\")\n    print(f\"Loaded worlds: {wk.list_worlds()}\")\n\n    # 3. Create a session with small resolution for fast iteration\n    config = WorldConfig(height=64, width=64, frames_per_step=1)\n    session = wk.create_session(\"dummy\", config=config, seed=42)\n    print(f\"Session: {session.id}, status={session.status.value}\")\n\n    # 4. Take a few steps\n    for i in range(5):\n        action = Action(\"keyboard\", {\"keys\": [\"W\"]})\n        obs = session.step(action, modalities=[\"frames\"])\n        frame_size = len(obs.frames[0]) if obs.frames else 0\n        print(\n            f\"  step {session.step_index}: \"\n            f\"gen_time={obs.generation_time_ms:.2f}ms, \"\n            f\"frame_bytes={frame_size}\"\n        )\n\n    # 5. Checkpoint and branch\n    ckpt_id = session.checkpoint()\n    print(f\"\\nCheckpoint saved: {ckpt_id}\")\n\n    branched = session.branch()\n    print(f\"Branched session: {branched.id} (parent={branched.parent_session_id})\")\n\n    # Take a different action in the branch\n    obs_branch = branched.step(\n        Action(\"keyboard\", {\"keys\": [\"A\"]}),\n        modalities=[\"frames\", \"depth\"],\n    )\n    print(\n        f\"  branch step {branched.step_index}: \"\n        f\"gen_time={obs_branch.generation_time_ms:.2f}ms, \"\n        f\"has_depth={obs_branch.depth is not None}\"\n    )\n\n    # Original session continues independently\n    obs_orig = session.step(\n        Action(\"keyboard\", {\"keys\": [\"D\"]}),\n        modalities=[\"frames\"],\n    )\n    print(f\"  original step {session.step_index}: gen_time={obs_orig.generation_time_ms:.2f}ms\")\n\n    # 6. Restore to checkpoint\n    session.restore(ckpt_id)\n    print(f\"\\nRestored to {ckpt_id}, step_index still={session.step_index}\")\n\n    # 7. Cleanup\n    session.close()\n    branched.close()\n    wk.shutdown()\n    print(\"Done.\")\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"examples/gr1_prepare/","title":"Gr1 Prepare","text":""},{"location":"examples/gr1_prepare/#gr1-prepare","title":"Gr1 Prepare","text":"<p>Source https://github.com/soran-ghaderi/worldkernels/blob/main/examples/gr1_prepare.py.</p> <pre><code>r\"\"\"Prepare a DreamDojo GR-1 conditioning chunk from a PhysicalAI-Robotics-GR00T-Teleop-GR1 episode.\n\nTurns one LeRobot episode (an ego-view ``.mp4`` + a ``.parquet`` of robot states/actions) into the\ntwo inputs the action-conditioned ``dreamdojo --variant 2b_gr1`` model expects:\n\n  - ``gr1_first_frame.png``  : the real first RGB frame, resized to the GR-1 native 480x640.\n  - ``gr1_actions.npy``      : an ``[N, 384]`` action sequence; for GR-1 only ``[:, :29]`` is filled\n                               (4-step end-effector deltas), the rest zero, per groot_dreams\n                               ``dataset.py`` ``action_seq[:, :29] = delta_actions``.\n\nThen run, e.g.:\n\n```bash\nworldkernels run dreamdojo --variant 2b_gr1 --height 480 --width 640 --steps 6 \\\n  -i datasets/gr1_cucumber/gr1_first_frame.png \\\n  --actions datasets/gr1_cucumber/gr1_actions.npy \\\n  --profile fast --set attention_backend=sdpa -o ./out/gr1/\n```\n\nThe 29-dim action reproduces NVIDIA's groot_dreams transform for the ``gr1`` embodiment:\nconcatenate the action modality keys in config order (``left_arm``, ``right_arm``, ``left_hand``,\n``right_hand``, ``waist`` = 7+7+6+6+3), each normalized to \\([-1, 1]\\) by per-key ``min_max`` from\nthe dataset ``stats.json``, sampled at ``timestep_interval=2``, then 4-step deltas\n``action[t:t+4] - action[t-1]`` placed in ``action_seq[:, :29]``. Column ranges are read from the\ndataset's own ``meta/modality.json`` (not hardcoded). The only un-modeled detail is the per-sample\nbase-index/horizon windowing (here the whole episode is processed as one stream); validate on GPU.\n\"\"\"\n\nfrom __future__ import annotations\n\nimport argparse\nimport json\nfrom pathlib import Path\n\nimport numpy as np\n\n# Action modality keys for the gr1 embodiment, in concat order (groot_configs.py\n# construct_modality_config_and_transforms): left_arm(7)+right_arm(7)+left_hand(6)+right_hand(6)\n# +waist(3) = 29. Column ranges are resolved per-dataset from meta/modality.json.\n_GR1_ACTION_KEYS = [\"left_arm\", \"right_arm\", \"left_hand\", \"right_hand\", \"waist\"]\n_TIMESTEP_INTERVAL = 2\n_ACTION_DIM = 384\n_GR1_EEF_DIM = 29\n_CHUNK = 12\n\n\ndef _read_parquet(path: Path) -&gt; np.ndarray:\n    try:\n        import pyarrow.parquet as pq\n    except ImportError as exc:  # pragma: no cover - user env hint\n        raise SystemExit(\"need pyarrow: `uv pip install pyarrow`\") from exc\n    col = pq.read_table(path, columns=[\"action\"]).column(\"action\").to_pylist()\n    return np.asarray(col, dtype=np.float64)  # [T, 1030]\n\n\ndef _action_key_ranges(modality_path: Path) -&gt; list[tuple[str, int, int]]:\n    action_map = json.loads(modality_path.read_text())[\"action\"]\n    ranges = []\n    for key in _GR1_ACTION_KEYS:\n        if key not in action_map:\n            raise SystemExit(f\"action key {key!r} missing from {modality_path}\")\n        entry = action_map[key]\n        ranges.append((key, int(entry[\"start\"]), int(entry[\"end\"])))\n    return ranges\n\n\ndef _min_max_normalize(\n    action: np.ndarray, stats_path: Path, ranges: list[tuple[str, int, int]]\n) -&gt; np.ndarray:\n    r\"\"\"Per-key ``min_max`` to \\([-1, 1]\\): \\(2 (x - \\min)/(\\max - \\min) - 1\\), 0 where min==max.\"\"\"\n    stats = json.loads(stats_path.read_text())[\"action\"]\n    lo = np.asarray(stats[\"min\"], dtype=np.float64)\n    hi = np.asarray(stats[\"max\"], dtype=np.float64)\n    cols = []\n    for _, a, b in ranges:\n        x = action[:, a:b]\n        span = hi[a:b] - lo[a:b]\n        mask = span != 0\n        norm = np.zeros_like(x)\n        norm[:, mask] = 2.0 * (x[:, mask] - lo[a:b][mask]) / span[mask] - 1.0\n        cols.append(norm)\n    return np.concatenate(cols, axis=1)  # [T, 29]\n\n\ndef _delta_actions(eef: np.ndarray) -&gt; np.ndarray:\n    r\"\"\"4-step eef deltas, matching groot_dreams dataset.py: action[t:t+4] - action[t-1].\"\"\"\n    out = [eef[t : t + 4] - eef[t - 1] for t in range(1, len(eef) - 1, 4)]\n    return np.concatenate(out, axis=0) if out else np.zeros((0, _GR1_EEF_DIM))\n\n\ndef main() -&gt; None:\n    ap = argparse.ArgumentParser(description=__doc__)\n    ap.add_argument(\"--task-dir\", required=True, help=\"\u2026/In-lab_Eval/gr1_unified.&lt;task&gt;_robot\")\n    ap.add_argument(\"--episode\", type=int, default=0)\n    ap.add_argument(\"--camera\", default=\"observation.images.ego_view_freq20\")\n    ap.add_argument(\"--out-dir\", default=\".\")\n    ap.add_argument(\"--height\", type=int, default=480)\n    ap.add_argument(\"--width\", type=int, default=640)\n    ap.add_argument(\n        \"--zero-actions\", action=\"store_true\", help=\"emit static (zero) actions baseline\"\n    )\n    args = ap.parse_args()\n\n    task = Path(args.task_dir).expanduser()\n    out = Path(args.out_dir).expanduser()\n    out.mkdir(parents=True, exist_ok=True)\n    ep = f\"episode_{args.episode:06d}\"\n\n    import imageio.v3 as iio\n\n    mp4 = task / \"videos\" / \"chunk-000\" / args.camera / f\"{ep}.mp4\"\n    frame = iio.imread(mp4, index=0, plugin=\"pyav\")  # [H,W,3] uint8\n    try:\n        from PIL import Image\n\n        frame = np.asarray(Image.fromarray(frame).resize((args.width, args.height)))\n    except ImportError:\n        pass\n    frame_path = out / \"gr1_first_frame.png\"\n    iio.imwrite(frame_path, frame)\n\n    n_steps = 0\n    if args.zero_actions:\n        action_seq = np.zeros((_CHUNK, _ACTION_DIM), dtype=np.float32)\n    else:\n        action_1030 = _read_parquet(task / \"data\" / \"chunk-000\" / f\"{ep}.parquet\")\n        action_1030 = action_1030[::_TIMESTEP_INTERVAL]\n        ranges = _action_key_ranges(task / \"meta\" / \"modality.json\")\n        norm = _min_max_normalize(action_1030, task / \"meta\" / \"stats.json\", ranges)\n        deltas = _delta_actions(norm).astype(np.float32)  # [N, 29]\n        action_seq = np.zeros((len(deltas), _ACTION_DIM), dtype=np.float32)\n        action_seq[:, :_GR1_EEF_DIM] = deltas\n        n_steps = int(np.ceil(len(deltas) / _CHUNK))\n    actions_path = out / \"gr1_actions.npy\"\n    np.save(actions_path, action_seq)\n\n    print(f\"first frame : {frame_path}  ({args.height}x{args.width})\")\n    print(f\"actions     : {actions_path}  shape {action_seq.shape}  (~{n_steps or 1} chunks)\")\n    print(\"\\nrun:\")\n    print(\n        f\"  worldkernels run dreamdojo --variant 2b_gr1 --height {args.height} \"\n        f\"--width {args.width} --steps {max(n_steps, 1)} -i {frame_path} \"\n        f\"--actions {actions_path} --profile fast --set attention_backend=sdpa -o ./out/gr1/\"\n    )\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>"},{"location":"examples/basics/","title":"Overview","text":""},{"location":"examples/basics/#basics-examples","title":"Basics Examples","text":"<p>Minimal category examples for docs generation.</p> <ul> <li>Single-file examples appear directly.</li> <li>Directory examples include extra materials.</li> </ul> <p>Generated from <code>examples/basics</code>.</p> <ul> <li>Multi File Demo</li> <li>Quickstart</li> </ul>"},{"location":"cli/","title":"Overview","text":""},{"location":"cli/#cli-reference","title":"CLI Reference","text":"<p>Command-line interface for WorldKernels.</p>"},{"location":"cli/#usage","title":"Usage","text":"<pre><code>worldkernels &lt;command&gt; [options]\n</code></pre>"},{"location":"cli/#commands","title":"Commands","text":"Command Description <code>bench</code> <code>latency</code>: Measure single-session step latency.; <code>profile</code>: Run steps under torch.profiler and emit a trace file.; <code>startup</code>: Measure model load + warmup time.; <code>throughput</code>: Measure multi-session concurrent throughput.; <code>vram</code>: Profile VRAM estimates across resolutions. <code>collect-env</code> Collect environment info: GPU, deps, plugins, hub, local cache, isolated envs. <code>config-show</code> Show the resolved runtime config (component toggles) and each flag's source. <code>inspect</code> Show model metadata: transition mode, VRAM estimate, capabilities. <code>models</code> List models: locally cached by default, or the full hub with --all. <code>plugins</code> List discovered entry_point plugins. <code>pull</code> Pre-fetch a model: install deps, clone packages, download weights. No server. <code>rm</code> Remove a model from the local cache (weights + manifest). <code>run</code> Headless run: bootstrap a model, step N times, optionally save frames/video. <code>serve</code> Start the WorldKernels HTTP/WebSocket server."},{"location":"cli/#global-options","title":"Global Options","text":"Flag Description <code>--quiet</code>, <code>-q</code> Suppress non-essential output. <code>--verbose</code>, <code>-v</code> Increase verbosity (-vv for debug). <code>--debug</code> Debug logging. <code>--output</code> Output rendering mode. Choices: auto, plain, json. <code>--version</code>, <code>-V</code> show program's version number and exit <code>--help</code>, <code>-h</code> Show help message and exit <code>--help=&lt;group\\|keyword\\|all&gt;</code> Search help: one config group, a keyword across options, or everything"},{"location":"cli/bench/","title":"Bench","text":""},{"location":"cli/bench/#worldkernels-bench","title":"worldkernels bench","text":""},{"location":"cli/bench/#subcommands","title":"Subcommands","text":"Subcommand Description <code>latency</code> Measure single-session step latency. <code>profile</code> Run steps under torch.profiler and emit a trace file. <code>startup</code> Measure model load + warmup time. <code>throughput</code> Measure multi-session concurrent throughput. <code>vram</code> Profile VRAM estimates across resolutions."},{"location":"cli/bench/#worldkernels-bench-latency","title":"worldkernels bench latency","text":"<p>Measure single-session step latency.</p> <pre><code>worldkernels bench latency [--world STR] [--device STR] [--steps INT] [--height INT] [--width INT] [--profile STR] [--config STR] [--set STR] [--dtype STR] [--quantization STR] [--torch-compile BOOL] [--cuda-graphs BOOL] [--continuous-batching BOOL] [--iteration-batching BOOL] [--teacache BOOL] [--trajectory-cache BOOL] [--kv-cache-paged BOOL] [--latent-pool BOOL] [--offload-idle BOOL] [--attention-backend STR] [--isolation STR] [--tensor-parallel-size INT] [--data-parallel-size INT] [--pipeline-parallel-size INT] [--ulysses-degree INT] [--ring-degree INT] [--cfg-parallel-size INT] [--block-frames INT] [--gpu-memory-fraction FLOAT] [--enable-kv-paging BOOL] [--enable-trajectory-cache BOOL] [--offload-to-cpu BOOL] [--offload-to-nvme BOOL] [--nvme-path STR] [--max-batch-size INT] [--max-concurrent-sessions INT] [--policy STR] [--preemption-mode STR] [--enable-iteration-batching BOOL] [--admission-headroom-mb FLOAT]\n</code></pre>"},{"location":"cli/bench/#options","title":"Options","text":"Flag Type Default Description <code>--world</code> <code>str</code> <code>'dummy'</code> World model to benchmark. <code>--device</code> <code>str</code> <code>'cpu'</code> \u2014 <code>--steps</code> <code>int</code> <code>100</code> \u2014 <code>--height</code> <code>int</code> <code>64</code> \u2014 <code>--width</code> <code>int</code> <code>64</code> \u2014 <code>--profile</code> <code>str</code> \u2014 Named config profile (baseline, fast, ...). <code>--config</code> <code>str</code> \u2014 YAML config file; env (WK_*) and explicit CLI flags win over its values. <code>--set</code> <code>str</code> \u2014 Comma-separated runtime overrides (e.g. teacache=off,attention_backend=sdpa). <code>--dtype</code> <code>str</code> \u2014 (default: auto) Choices: auto, bf16, fp16, fp32. <code>--quantization</code> <code>str</code> \u2014 (default: none) Choices: none, int8, int4. <code>--torch-compile</code> <code>bool</code> \u2014 (default: True) <code>--cuda-graphs</code> <code>bool</code> \u2014 (default: True) <code>--continuous-batching</code> <code>bool</code> \u2014 (default: True) <code>--iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--teacache</code> <code>bool</code> \u2014 (default: False) <code>--trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--kv-cache-paged</code> <code>bool</code> \u2014 (default: True) <code>--latent-pool</code> <code>bool</code> \u2014 (default: True) <code>--offload-idle</code> <code>bool</code> \u2014 (default: True) <code>--attention-backend</code> <code>str</code> \u2014 (default: auto) Choices: auto, flash, sdpa. <code>--isolation</code> <code>str</code> \u2014 (default: auto) Choices: auto, shared, isolated. <code>--tensor-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--data-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--pipeline-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--ulysses-degree</code> <code>int</code> \u2014 (default: 1) <code>--ring-degree</code> <code>int</code> \u2014 (default: 1) <code>--cfg-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--block-frames</code> <code>int</code> \u2014 (default: 16) <code>--gpu-memory-fraction</code> <code>float</code> \u2014 (default: 0.8) <code>--enable-kv-paging</code> <code>bool</code> \u2014 (default: True) <code>--enable-trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-cpu</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-nvme</code> <code>bool</code> \u2014 (default: False) <code>--nvme-path</code> <code>str</code> \u2014 (default: None) <code>--max-batch-size</code> <code>int</code> \u2014 (default: 8) <code>--max-concurrent-sessions</code> <code>int</code> \u2014 (default: 16) <code>--policy</code> <code>str</code> \u2014 (default: fcfs) Choices: fcfs, priority. <code>--preemption-mode</code> <code>str</code> \u2014 (default: swap) Choices: swap, recompute. <code>--enable-iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--admission-headroom-mb</code> <code>float</code> \u2014 (default: 512.0)"},{"location":"cli/bench/#worldkernels-bench-profile","title":"worldkernels bench profile","text":"<p>Run steps under torch.profiler and emit a trace file.</p> <pre><code>worldkernels bench profile [--world STR] [--device STR] [--steps INT] [--height INT] [--width INT] [--trace STR]\n</code></pre>"},{"location":"cli/bench/#options_1","title":"Options","text":"Flag Type Default Description <code>--world</code> <code>str</code> <code>'dummy'</code> World model to benchmark. <code>--device</code> <code>str</code> <code>'cuda'</code> \u2014 <code>--steps</code> <code>int</code> <code>10</code> \u2014 <code>--height</code> <code>int</code> <code>64</code> \u2014 <code>--width</code> <code>int</code> <code>64</code> \u2014 <code>--trace</code> <code>str</code> <code>'wk_profile'</code> Chrome trace output basename."},{"location":"cli/bench/#worldkernels-bench-startup","title":"worldkernels bench startup","text":"<p>Measure model load + warmup time.</p> <pre><code>worldkernels bench startup [--world STR] [--device STR]\n</code></pre>"},{"location":"cli/bench/#options_2","title":"Options","text":"Flag Type Default Description <code>--world</code> <code>str</code> <code>'dummy'</code> World model to benchmark. <code>--device</code> <code>str</code> <code>'cpu'</code> \u2014"},{"location":"cli/bench/#worldkernels-bench-throughput","title":"worldkernels bench throughput","text":"<p>Measure multi-session concurrent throughput.</p> <pre><code>worldkernels bench throughput [--world STR] [--device STR] [--steps INT] [--height INT] [--width INT] [--sessions INT] [--profile STR] [--config STR] [--set STR] [--dtype STR] [--quantization STR] [--torch-compile BOOL] [--cuda-graphs BOOL] [--continuous-batching BOOL] [--iteration-batching BOOL] [--teacache BOOL] [--trajectory-cache BOOL] [--kv-cache-paged BOOL] [--latent-pool BOOL] [--offload-idle BOOL] [--attention-backend STR] [--isolation STR] [--tensor-parallel-size INT] [--data-parallel-size INT] [--pipeline-parallel-size INT] [--ulysses-degree INT] [--ring-degree INT] [--cfg-parallel-size INT] [--block-frames INT] [--gpu-memory-fraction FLOAT] [--enable-kv-paging BOOL] [--enable-trajectory-cache BOOL] [--offload-to-cpu BOOL] [--offload-to-nvme BOOL] [--nvme-path STR] [--max-batch-size INT] [--max-concurrent-sessions INT] [--policy STR] [--preemption-mode STR] [--enable-iteration-batching BOOL] [--admission-headroom-mb FLOAT]\n</code></pre>"},{"location":"cli/bench/#options_3","title":"Options","text":"Flag Type Default Description <code>--world</code> <code>str</code> <code>'dummy'</code> World model to benchmark. <code>--device</code> <code>str</code> <code>'cpu'</code> \u2014 <code>--steps</code> <code>int</code> <code>50</code> \u2014 <code>--height</code> <code>int</code> <code>64</code> \u2014 <code>--width</code> <code>int</code> <code>64</code> \u2014 <code>--sessions</code> <code>int</code> <code>4</code> \u2014 <code>--profile</code> <code>str</code> \u2014 Named config profile (baseline, fast, ...). <code>--config</code> <code>str</code> \u2014 YAML config file; env (WK_*) and explicit CLI flags win over its values. <code>--set</code> <code>str</code> \u2014 Comma-separated runtime overrides (e.g. teacache=off,attention_backend=sdpa). <code>--dtype</code> <code>str</code> \u2014 (default: auto) Choices: auto, bf16, fp16, fp32. <code>--quantization</code> <code>str</code> \u2014 (default: none) Choices: none, int8, int4. <code>--torch-compile</code> <code>bool</code> \u2014 (default: True) <code>--cuda-graphs</code> <code>bool</code> \u2014 (default: True) <code>--continuous-batching</code> <code>bool</code> \u2014 (default: True) <code>--iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--teacache</code> <code>bool</code> \u2014 (default: False) <code>--trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--kv-cache-paged</code> <code>bool</code> \u2014 (default: True) <code>--latent-pool</code> <code>bool</code> \u2014 (default: True) <code>--offload-idle</code> <code>bool</code> \u2014 (default: True) <code>--attention-backend</code> <code>str</code> \u2014 (default: auto) Choices: auto, flash, sdpa. <code>--isolation</code> <code>str</code> \u2014 (default: auto) Choices: auto, shared, isolated. <code>--tensor-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--data-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--pipeline-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--ulysses-degree</code> <code>int</code> \u2014 (default: 1) <code>--ring-degree</code> <code>int</code> \u2014 (default: 1) <code>--cfg-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--block-frames</code> <code>int</code> \u2014 (default: 16) <code>--gpu-memory-fraction</code> <code>float</code> \u2014 (default: 0.8) <code>--enable-kv-paging</code> <code>bool</code> \u2014 (default: True) <code>--enable-trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-cpu</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-nvme</code> <code>bool</code> \u2014 (default: False) <code>--nvme-path</code> <code>str</code> \u2014 (default: None) <code>--max-batch-size</code> <code>int</code> \u2014 (default: 8) <code>--max-concurrent-sessions</code> <code>int</code> \u2014 (default: 16) <code>--policy</code> <code>str</code> \u2014 (default: fcfs) Choices: fcfs, priority. <code>--preemption-mode</code> <code>str</code> \u2014 (default: swap) Choices: swap, recompute. <code>--enable-iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--admission-headroom-mb</code> <code>float</code> \u2014 (default: 512.0)"},{"location":"cli/bench/#worldkernels-bench-vram","title":"worldkernels bench vram","text":"<p>Profile VRAM estimates across resolutions.</p> <pre><code>worldkernels bench vram [--world STR] [--device STR] [--resolutions STR]\n</code></pre>"},{"location":"cli/bench/#options_4","title":"Options","text":"Flag Type Default Description <code>--world</code> <code>str</code> <code>'dummy'</code> World model to benchmark. <code>--device</code> <code>str</code> <code>'cuda'</code> \u2014 <code>--resolutions</code> <code>str</code> <code>'256x256,480x848,720x1280'</code> \u2014"},{"location":"cli/bench/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/collect-env/","title":"Collect-env","text":""},{"location":"cli/collect-env/#worldkernels-collect-env","title":"worldkernels collect-env","text":"<p>Collect environment info: GPU, deps, plugins, hub, local cache, isolated envs.</p>"},{"location":"cli/collect-env/#usage","title":"Usage","text":"<pre><code>worldkernels collect-env\n</code></pre>"},{"location":"cli/collect-env/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/config-show/","title":"Config-show","text":""},{"location":"cli/config-show/#worldkernels-config-show","title":"worldkernels config-show","text":"<p>Show the resolved runtime config (component toggles) and each flag's source.</p>"},{"location":"cli/config-show/#usage","title":"Usage","text":"<pre><code>worldkernels config-show [--json BOOL] [--profile STR] [--config STR] [--set STR] [--dtype STR] [--quantization STR] [--torch-compile BOOL] [--cuda-graphs BOOL] [--continuous-batching BOOL] [--iteration-batching BOOL] [--teacache BOOL] [--trajectory-cache BOOL] [--kv-cache-paged BOOL] [--latent-pool BOOL] [--offload-idle BOOL] [--attention-backend STR] [--isolation STR] [--tensor-parallel-size INT] [--data-parallel-size INT] [--pipeline-parallel-size INT] [--ulysses-degree INT] [--ring-degree INT] [--cfg-parallel-size INT] [--block-frames INT] [--gpu-memory-fraction FLOAT] [--enable-kv-paging BOOL] [--enable-trajectory-cache BOOL] [--offload-to-cpu BOOL] [--offload-to-nvme BOOL] [--nvme-path STR] [--max-batch-size INT] [--max-concurrent-sessions INT] [--policy STR] [--preemption-mode STR] [--enable-iteration-batching BOOL] [--admission-headroom-mb FLOAT]\n</code></pre>"},{"location":"cli/config-show/#options","title":"Options","text":"Flag Type Default Description <code>--json</code> <code>bool</code> <code>False</code> Emit JSON. <code>--profile</code> <code>str</code> \u2014 Named config profile (baseline, fast, ...). <code>--config</code> <code>str</code> \u2014 YAML config file; env (WK_*) and explicit CLI flags win over its values. <code>--set</code> <code>str</code> \u2014 Comma-separated runtime overrides (e.g. teacache=off,attention_backend=sdpa). <code>--dtype</code> <code>str</code> \u2014 (default: auto) Choices: auto, bf16, fp16, fp32. <code>--quantization</code> <code>str</code> \u2014 (default: none) Choices: none, int8, int4. <code>--torch-compile</code> <code>bool</code> \u2014 (default: True) <code>--cuda-graphs</code> <code>bool</code> \u2014 (default: True) <code>--continuous-batching</code> <code>bool</code> \u2014 (default: True) <code>--iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--teacache</code> <code>bool</code> \u2014 (default: False) <code>--trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--kv-cache-paged</code> <code>bool</code> \u2014 (default: True) <code>--latent-pool</code> <code>bool</code> \u2014 (default: True) <code>--offload-idle</code> <code>bool</code> \u2014 (default: True) <code>--attention-backend</code> <code>str</code> \u2014 (default: auto) Choices: auto, flash, sdpa. <code>--isolation</code> <code>str</code> \u2014 (default: auto) Choices: auto, shared, isolated. <code>--tensor-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--data-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--pipeline-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--ulysses-degree</code> <code>int</code> \u2014 (default: 1) <code>--ring-degree</code> <code>int</code> \u2014 (default: 1) <code>--cfg-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--block-frames</code> <code>int</code> \u2014 (default: 16) <code>--gpu-memory-fraction</code> <code>float</code> \u2014 (default: 0.8) <code>--enable-kv-paging</code> <code>bool</code> \u2014 (default: True) <code>--enable-trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-cpu</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-nvme</code> <code>bool</code> \u2014 (default: False) <code>--nvme-path</code> <code>str</code> \u2014 (default: None) <code>--max-batch-size</code> <code>int</code> \u2014 (default: 8) <code>--max-concurrent-sessions</code> <code>int</code> \u2014 (default: 16) <code>--policy</code> <code>str</code> \u2014 (default: fcfs) Choices: fcfs, priority. <code>--preemption-mode</code> <code>str</code> \u2014 (default: swap) Choices: swap, recompute. <code>--enable-iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--admission-headroom-mb</code> <code>float</code> \u2014 (default: 512.0)"},{"location":"cli/config-show/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/inspect/","title":"Inspect","text":""},{"location":"cli/inspect/#worldkernels-inspect","title":"worldkernels inspect","text":"<p>Show model metadata: transition mode, VRAM estimate, capabilities.</p>"},{"location":"cli/inspect/#usage","title":"Usage","text":"<pre><code>worldkernels inspect [--model STR] [--device STR] [--config-json STR]\n</code></pre>"},{"location":"cli/inspect/#options","title":"Options","text":"Flag Type Default Description <code>--model</code> <code>str</code> <code>'dummy'</code> Model: short alias, HF repo id, HF URL, or local checkpoint path. <code>--device</code> <code>str</code> <code>'cpu'</code> \u2014 <code>--config-json</code> <code>str</code> \u2014 WorldConfig overrides as JSON."},{"location":"cli/inspect/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/models/","title":"Models","text":""},{"location":"cli/models/#worldkernels-models","title":"worldkernels models","text":"<p>List models: locally cached by default, or the full hub with --all.</p>"},{"location":"cli/models/#usage","title":"Usage","text":"<pre><code>worldkernels models [--all BOOL]\n</code></pre>"},{"location":"cli/models/#options","title":"Options","text":"Flag Type Default Description <code>--all</code> <code>bool</code> <code>False</code> Show the full hub catalog."},{"location":"cli/models/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/plugins/","title":"Plugins","text":""},{"location":"cli/plugins/#worldkernels-plugins","title":"worldkernels plugins","text":"<p>List discovered entry_point plugins.</p>"},{"location":"cli/plugins/#usage","title":"Usage","text":"<pre><code>worldkernels plugins\n</code></pre>"},{"location":"cli/plugins/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/pull/","title":"Pull","text":""},{"location":"cli/pull/#worldkernels-pull","title":"worldkernels pull","text":"<p>Pre-fetch a model: install deps, clone packages, download weights. No server.</p>"},{"location":"cli/pull/#usage","title":"Usage","text":"<pre><code>worldkernels pull [--model STR] [--variant STR] [--ckpt-path STR]\n</code></pre>"},{"location":"cli/pull/#options","title":"Options","text":"Flag Type Default Description <code>--model</code> <code>str</code> <code>'dummy'</code> Model: short alias, HF repo id, HF URL, or local checkpoint path. <code>--variant</code> <code>str</code> \u2014 Model variant (e.g. 2b_gr1). <code>--ckpt-path</code> <code>str</code> \u2014 Local checkpoint path override."},{"location":"cli/pull/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/rm/","title":"Rm","text":""},{"location":"cli/rm/#worldkernels-rm","title":"worldkernels rm","text":"<p>Remove a model from the local cache (weights + manifest).</p>"},{"location":"cli/rm/#usage","title":"Usage","text":"<pre><code>worldkernels rm [--model STR] [--variant STR]\n</code></pre>"},{"location":"cli/rm/#options","title":"Options","text":"Flag Type Default Description <code>--model</code> <code>str</code> \u2014 Model: short alias, HF repo id, HF URL, or local checkpoint path. <code>--variant</code> <code>str</code> \u2014 Only remove this variant."},{"location":"cli/rm/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/run/","title":"Run","text":""},{"location":"cli/run/#worldkernels-run","title":"worldkernels run","text":"<p>Headless run: bootstrap a model, step N times, optionally save frames/video.</p>"},{"location":"cli/run/#usage","title":"Usage","text":"<pre><code>worldkernels run [--model STR] [--steps INT] [--action-type STR] [--height INT] [--width INT] [--device STR] [--seed INT] [--output-dir STR] [--output-format STR] [--fps INT] [--video-codec STR] [--modalities STR] [--decode BOOL] [--prompt STR] [--image STR] [--actions STR] [--text-encoder STR] [--variant STR] [--ckpt-path STR] [--no-fetch BOOL] [--num-inference-steps INT] [--guidance-scale FLOAT] [--profile STR] [--config STR] [--set STR] [--dtype STR] [--quantization STR] [--torch-compile BOOL] [--cuda-graphs BOOL] [--continuous-batching BOOL] [--iteration-batching BOOL] [--teacache BOOL] [--trajectory-cache BOOL] [--kv-cache-paged BOOL] [--latent-pool BOOL] [--offload-idle BOOL] [--attention-backend STR] [--isolation STR] [--tensor-parallel-size INT] [--data-parallel-size INT] [--pipeline-parallel-size INT] [--ulysses-degree INT] [--ring-degree INT] [--cfg-parallel-size INT] [--block-frames INT] [--gpu-memory-fraction FLOAT] [--enable-kv-paging BOOL] [--enable-trajectory-cache BOOL] [--offload-to-cpu BOOL] [--offload-to-nvme BOOL] [--nvme-path STR] [--max-batch-size INT] [--max-concurrent-sessions INT] [--policy STR] [--preemption-mode STR] [--enable-iteration-batching BOOL] [--admission-headroom-mb FLOAT]\n</code></pre>"},{"location":"cli/run/#options","title":"Options","text":"Flag Type Default Description <code>--model</code> <code>str</code> <code>'dummy'</code> Model: short alias, HF repo id, HF URL, or local checkpoint path. <code>--steps</code> <code>int</code> <code>10</code> Number of world steps. <code>--action-type</code> <code>str</code> <code>'null'</code> \u2014 <code>--height</code> <code>int</code> <code>480</code> \u2014 <code>--width</code> <code>int</code> <code>848</code> \u2014 <code>--device</code> <code>str</code> <code>'cuda'</code> \u2014 <code>--seed</code> <code>int</code> <code>0</code> \u2014 <code>--output-dir</code>, <code>-o</code> <code>str</code> \u2014 Directory for saved outputs. <code>--output-format</code> <code>str</code> <code>'frames'</code> Choices: frames, video, both. <code>--fps</code> <code>int</code> <code>24</code> \u2014 <code>--video-codec</code> <code>str</code> <code>'libx264'</code> \u2014 <code>--modalities</code> <code>str</code> <code>'frames'</code> Comma-separated modalities. <code>--decode</code> <code>bool</code> <code>True</code> \u2014 <code>--prompt</code> <code>str</code> \u2014 Initial text prompt. <code>--image</code>, <code>-i</code> <code>str</code> \u2014 Initial image path. <code>--actions</code> <code>str</code> \u2014 Path to a .npy of GR-1 action chunks ([T,384] or [N,12,384]) to drive an action-conditioned rollout (see examples/gr1_prepare.py). Default: null actions. <code>--text-encoder</code> <code>str</code> \u2014 'on' loads the reason1-7B encoder (CPU-offloaded) for real prompt conditioning; default off uses neutral text and relies on image + action conditioning. Choices: auto, off, on. <code>--variant</code> <code>str</code> \u2014 Model variant (e.g. 2b_gr1). <code>--ckpt-path</code> <code>str</code> \u2014 Local checkpoint path override. <code>--no-fetch</code> <code>bool</code> <code>False</code> Fail instead of downloading missing deps/weights. <code>--num-inference-steps</code> <code>int</code> \u2014 \u2014 <code>--guidance-scale</code> <code>float</code> \u2014 \u2014 <code>--profile</code> <code>str</code> \u2014 Named config profile (baseline, fast, ...). <code>--config</code> <code>str</code> \u2014 YAML config file; env (WK_*) and explicit CLI flags win over its values. <code>--set</code> <code>str</code> \u2014 Comma-separated runtime overrides (e.g. teacache=off,attention_backend=sdpa). <code>--dtype</code> <code>str</code> \u2014 (default: auto) Choices: auto, bf16, fp16, fp32. <code>--quantization</code> <code>str</code> \u2014 (default: none) Choices: none, int8, int4. <code>--torch-compile</code> <code>bool</code> \u2014 (default: True) <code>--cuda-graphs</code> <code>bool</code> \u2014 (default: True) <code>--continuous-batching</code> <code>bool</code> \u2014 (default: True) <code>--iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--teacache</code> <code>bool</code> \u2014 (default: False) <code>--trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--kv-cache-paged</code> <code>bool</code> \u2014 (default: True) <code>--latent-pool</code> <code>bool</code> \u2014 (default: True) <code>--offload-idle</code> <code>bool</code> \u2014 (default: True) <code>--attention-backend</code> <code>str</code> \u2014 (default: auto) Choices: auto, flash, sdpa. <code>--isolation</code> <code>str</code> \u2014 (default: auto) Choices: auto, shared, isolated. <code>--tensor-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--data-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--pipeline-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--ulysses-degree</code> <code>int</code> \u2014 (default: 1) <code>--ring-degree</code> <code>int</code> \u2014 (default: 1) <code>--cfg-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--block-frames</code> <code>int</code> \u2014 (default: 16) <code>--gpu-memory-fraction</code> <code>float</code> \u2014 (default: 0.8) <code>--enable-kv-paging</code> <code>bool</code> \u2014 (default: True) <code>--enable-trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-cpu</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-nvme</code> <code>bool</code> \u2014 (default: False) <code>--nvme-path</code> <code>str</code> \u2014 (default: None) <code>--max-batch-size</code> <code>int</code> \u2014 (default: 8) <code>--max-concurrent-sessions</code> <code>int</code> \u2014 (default: 16) <code>--policy</code> <code>str</code> \u2014 (default: fcfs) Choices: fcfs, priority. <code>--preemption-mode</code> <code>str</code> \u2014 (default: swap) Choices: swap, recompute. <code>--enable-iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--admission-headroom-mb</code> <code>float</code> \u2014 (default: 512.0)"},{"location":"cli/run/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"},{"location":"cli/serve/","title":"Serve","text":""},{"location":"cli/serve/#worldkernels-serve","title":"worldkernels serve","text":"<p>Start the WorldKernels HTTP/WebSocket server.</p>"},{"location":"cli/serve/#usage","title":"Usage","text":"<pre><code>worldkernels serve [--model STR] [--host STR] [--port INT] [--max-sessions INT] [--api-key STR] [--allowed-origins STR] [--allowed-methods STR] [--allowed-headers STR] [--allow-credentials BOOL] [--ssl-keyfile STR] [--ssl-certfile STR] [--uvicorn-log-level STR] [--disable-access-log BOOL] [--root-path STR] [--device STR] [--variant STR] [--ckpt-path STR] [--no-fetch BOOL] [--num-inference-steps INT] [--guidance-scale FLOAT] [--profile STR] [--config STR] [--set STR] [--dtype STR] [--quantization STR] [--torch-compile BOOL] [--cuda-graphs BOOL] [--continuous-batching BOOL] [--iteration-batching BOOL] [--teacache BOOL] [--trajectory-cache BOOL] [--kv-cache-paged BOOL] [--latent-pool BOOL] [--offload-idle BOOL] [--attention-backend STR] [--isolation STR] [--tensor-parallel-size INT] [--data-parallel-size INT] [--pipeline-parallel-size INT] [--ulysses-degree INT] [--ring-degree INT] [--cfg-parallel-size INT] [--block-frames INT] [--gpu-memory-fraction FLOAT] [--enable-kv-paging BOOL] [--enable-trajectory-cache BOOL] [--offload-to-cpu BOOL] [--offload-to-nvme BOOL] [--nvme-path STR] [--max-batch-size INT] [--max-concurrent-sessions INT] [--policy STR] [--preemption-mode STR] [--enable-iteration-batching BOOL] [--admission-headroom-mb FLOAT]\n</code></pre>"},{"location":"cli/serve/#options","title":"Options","text":"Flag Type Default Description <code>--model</code> <code>str</code> \u2014 Model to pre-load. Model: short alias, HF repo id, HF URL, or local checkpoint path. <code>--host</code> <code>str</code> <code>'0.0.0.0'</code> Bind address. <code>--port</code> <code>int</code> <code>8000</code> Bind port. <code>--max-sessions</code> <code>int</code> <code>4</code> Concurrent session cap. <code>--api-key</code>, <code>-k</code> <code>str</code> \u2014 Require this bearer token. <code>--allowed-origins</code> <code>str</code> <code>'*'</code> Comma-separated CORS origins. <code>--allowed-methods</code> <code>str</code> <code>'*'</code> Comma-separated CORS methods. <code>--allowed-headers</code> <code>str</code> <code>'*'</code> Comma-separated CORS headers. <code>--allow-credentials</code> <code>bool</code> <code>False</code> Allow CORS credentials. <code>--ssl-keyfile</code> <code>str</code> \u2014 TLS key file; enables https. <code>--ssl-certfile</code> <code>str</code> \u2014 TLS certificate file. <code>--uvicorn-log-level</code> <code>str</code> <code>'info'</code> uvicorn log level. Choices: debug, info, warning, error, critical, trace. <code>--disable-access-log</code> <code>bool</code> <code>False</code> Disable uvicorn access log. <code>--root-path</code> <code>str</code> <code>''</code> ASGI root_path when behind a proxy. <code>--device</code> <code>str</code> <code>'cuda'</code> \u2014 <code>--variant</code> <code>str</code> \u2014 Model variant (e.g. 2b_gr1). <code>--ckpt-path</code> <code>str</code> \u2014 Local checkpoint path override. <code>--no-fetch</code> <code>bool</code> <code>False</code> Fail instead of downloading missing deps/weights. <code>--num-inference-steps</code> <code>int</code> \u2014 \u2014 <code>--guidance-scale</code> <code>float</code> \u2014 \u2014 <code>--profile</code> <code>str</code> \u2014 Named config profile (baseline, fast, ...). <code>--config</code> <code>str</code> \u2014 YAML config file; env (WK_*) and explicit CLI flags win over its values. <code>--set</code> <code>str</code> \u2014 Comma-separated runtime overrides (e.g. teacache=off,attention_backend=sdpa). <code>--dtype</code> <code>str</code> \u2014 (default: auto) Choices: auto, bf16, fp16, fp32. <code>--quantization</code> <code>str</code> \u2014 (default: none) Choices: none, int8, int4. <code>--torch-compile</code> <code>bool</code> \u2014 (default: True) <code>--cuda-graphs</code> <code>bool</code> \u2014 (default: True) <code>--continuous-batching</code> <code>bool</code> \u2014 (default: True) <code>--iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--teacache</code> <code>bool</code> \u2014 (default: False) <code>--trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--kv-cache-paged</code> <code>bool</code> \u2014 (default: True) <code>--latent-pool</code> <code>bool</code> \u2014 (default: True) <code>--offload-idle</code> <code>bool</code> \u2014 (default: True) <code>--attention-backend</code> <code>str</code> \u2014 (default: auto) Choices: auto, flash, sdpa. <code>--isolation</code> <code>str</code> \u2014 (default: auto) Choices: auto, shared, isolated. <code>--tensor-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--data-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--pipeline-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--ulysses-degree</code> <code>int</code> \u2014 (default: 1) <code>--ring-degree</code> <code>int</code> \u2014 (default: 1) <code>--cfg-parallel-size</code> <code>int</code> \u2014 (default: 1) <code>--block-frames</code> <code>int</code> \u2014 (default: 16) <code>--gpu-memory-fraction</code> <code>float</code> \u2014 (default: 0.8) <code>--enable-kv-paging</code> <code>bool</code> \u2014 (default: True) <code>--enable-trajectory-cache</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-cpu</code> <code>bool</code> \u2014 (default: True) <code>--offload-to-nvme</code> <code>bool</code> \u2014 (default: False) <code>--nvme-path</code> <code>str</code> \u2014 (default: None) <code>--max-batch-size</code> <code>int</code> \u2014 (default: 8) <code>--max-concurrent-sessions</code> <code>int</code> \u2014 (default: 16) <code>--policy</code> <code>str</code> \u2014 (default: fcfs) Choices: fcfs, priority. <code>--preemption-mode</code> <code>str</code> \u2014 (default: swap) Choices: swap, recompute. <code>--enable-iteration-batching</code> <code>bool</code> \u2014 (default: True) <code>--admission-headroom-mb</code> <code>float</code> \u2014 (default: 512.0)"},{"location":"cli/serve/#source","title":"Source","text":"<p>Defined in <code>worldkernels/cli/main.py</code>.</p>"}]}